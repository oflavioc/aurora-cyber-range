# INSTRUMENTACAO — mapeia acao da aplicacao a event_type do catalogo.
#
# Autoridade: 09_EVENT_MODEL.md secao 6.
#
# E AQUI que a evidencia `auto` dos objetivos e declarada. O core responde por
# COMO PONTUAR a competencia; o pack responde por ONDE ELA SE MANIFESTA; e este
# arquivo diz QUAL ACAO DA APLICACAO produz o evento.
#
# `objective_ids` NAO aparece aqui nem no envelope (09 secao 1.2). Se a
# academus-api precisasse saber que uma consulta satisfaz OBJ-03, o dominio
# passaria a conhecer o desenho de exercicio e a fronteira core/adapter vazaria.
# O binding evento -> objetivo vive em scenarios/<pack>/objectives.yaml.
#
# NASCEU COMO ESQUELETO NA FASE 1, com os dois hooks que 09 secao 6 traz como
# exemplo normativo. A frase seguinte dizia que "os demais chegam com a API, na
# Fase 3" — e envelheceu: a Fase 3 entregou rotas que nao emitem evento nenhum,
# e o PRIMEIRO `append` do adapter e a consulta da trilha, que e da Fase 6.
#
# `audit_query_performed` deixou de ser hook sem produtor na peca 3A da Fase 6:
# `GET /audit/grade-changes` existe, e `domains/academus/api/emissor.py` grava.
# Os `payload_fields` daqui sao EXATAMENTE os quatro que aquele metodo escreve —
# acrescentar campo la sem declarar aqui faria o hook descrever um evento que
# nao e o emitido.
#
# `vpn_access_revoked` SAIU DAQUI — B1 da quarta auditoria da Fase 6.
#
# Ele estava declarado com `producer: federated-identity-simulator`, que e da
# Fase 11, e a justificativa era a da P5-2: destinatario declarado e melhor que
# ausencia. O argumento vale para SECAO DE REQUISITO, e nao aqui — e a diferenca
# e o que `06` T9 exige deste arquivo: *"cada `event_type` declarado em
# `observability_hooks.yaml` e emitido pela acao correspondente"*, com a etiqueta
# **(Fase 6)**. Enquanto o hook ficasse, o criterio era insatisfazivel por
# construcao, e a fase nao fechava.
#
# NAO SE PERDE NADA QUE ESTE ARQUIVO GUARDASSE: o `event_type` continua no
# catalogo de `contracts/events.schema.yaml`, com `effect_class` e `metric_side`,
# e e de la que a Fase 11 le o contrato do evento. O que saiu foi a afirmacao de
# que a instrumentacao EXISTE.
#
# O hook volta no commit que trouxer o `federated-identity-simulator`, e
# `scripts/check_hooks_com_emissor.py` recusa se ele voltar antes do emissor.
# Esta linha nao depende de alguem se lembrar: e a direcao (a) do verificador.

hooks:
  - event_type: audit_query_performed
    trigger: "GET /audit/grade-changes com filtro de período e agrupamento por usuário"
    producer: academus-api
    payload_fields: [period_start, period_end, group_by, result_count]
