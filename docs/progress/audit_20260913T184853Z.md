# Auditoria de checkpoint - 2026-09-13T18:48:53.229728+00:00

- fase: **7**
- commit auditado: `dc88ef1a04feb33c6fcafe63a32f5b0a57702990`
- session_id: `85598ac9-cfb5-47b9-9cb0-be99fe3d9753`
- modo: `headless`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have everything I need. Here is the report.

# AUDITORIA — FASE 7

## VEREDITO: FAIL

Base de comparação: `22f5f49bfa30ca3fd32911a4749b41063b8d1ac4` (fornecida pelo lançador, não resolvida por mim). `HEAD` = `dc88ef1a04feb33c6fcafe63a32f5b0a57702990`, árvore `b67d4d2b1963ef0a84defa63d2d9047107df890e` — confere com a árvore que as provas gravadas declaram. Guarda de base: **PORTA**, verbatim do lançador.

**Gates executados nesta rodada (R2 §1):**

| Gate | Resultado |
|---|---|
| `python -m unittest discover -s tests` (venv do lançador) | **856 testes, OK**, 0 falhas, **0 skips** — os 9 `skipIf(_URL is None)` não dispararam; os testes de serviço rodaram |
| `scripts/check_provas_de_container.py` | **rc=0** — árvore `b67d4d2b1963` confere; `demo` rc=0 e `reinicio` rc=0, build completo verde |
| `scripts/check_prova_do_seed.py` | rc=0 — 3.543.783 linhas, 150,2 s / 150,4 s < 300 s; 20 tabelas SHA-256 idênticas |
| **`scripts/check_prova_do_exercicio_4h.py`** | **rc=1 — REPROVOU** (ver B1) |
| `scripts/check_prova_do_exercicio_4h_probes.py` | rc=0 — 9 venenos, 2 controles positivos (o verificador é são; o vermelho é real) |
| `check_core_boundary` · `check_event_envelope` · `check_contract_literals` · `check_security_constraints` · `check_synthetic_data` · `codegen.py --check` | rc=0 (6/6) |
| `check_progress_consistency` · `check_regras_do_linter` · `check_gabarito_fora_do_git` · `check_readme_atual` | rc=0 |
| `check_secoes_de_seguranca` (8 seções) · `check_core_contract_imports` (18 arq./8 raízes) · `check_api_surface` (34 rotas) · `check_contract_examples` (13 pos., 95 neg.) · `check_gate_coverage` (437 arq.) · `check_allowlist_do_auditor` (69/56/13) | rc=0 |
| `range-cli scenario lint tests/fixtures/pack_minimo` | rc=2, achado correto — **o console script agora é alcançável** (M2 fechado) |

---

## FINDINGS

### BLOCKER

- **[B1] A prova do exercício de 4 h não existe, e o verificador criado nesta rodada para exigi-la REPROVA — os itens 6 e 9 da DoD continuam sem prova, que é exatamente o que o H2 cobrava**

  **Arquivo:** `scripts/check_prova_do_exercicio_4h.py:123-131` (direção (a), `EVIDENCIA = ".aurora-prova-do-exercicio-4h.json"` em `:52`) × `scripts/start_checkpoint_audit.sh:518` e `:685` (os dois produtores existentes) × `docs/progress/fase_7.md:3006` (H2 → "**CORRIGIDO**") e `:1231` (P7-3 → `RESOLVIDA`)

  **Requisito violado:** `06_ACCEPTANCE_TESTS.md` T12 — *"`dryrun` percorre todos os caminhos sem erro"* e *"**Reconstrução da projeção do zero para o exercício de 4 h do `ransomware-universidade` roda em < 3 s**, com máquina, data e stack declaradas"*; `07_IMPLEMENTATION_PHASES.md` §"Fase 7" itens 6 e 9.

  **Evidência — observada por execução, não suposta.** `scripts/check_prova_do_exercicio_4h.py` saiu com **rc=1** e imprimiu:

  ```
  H2 - a prova do exercicio de 4 h e desta arvore e deste pack
    `.aurora-prova-do-exercicio-4h.json` nao existe ou nao e JSON legivel.
    Os itens 6 e 9 da DoD ficam sem prova, e ISTO NAO DEGRADA PARA OK.
  ```

  Confirmado na árvore, em três medições independentes:
  1. **O artefato não está aqui.** `ls -a | grep -i prova` devolve só `.aurora-prova-do-seed.json` e `.aurora-provas-de-container.json`. O terceiro não existe.
  2. **Nada o produz no fluxo de auditoria.** `grep -n "prova_do_exercicio_4h" scripts/start_checkpoint_audit.sh` → **nenhuma ocorrência**. O lançador tem bloco próprio para gravar as provas de container (`:518`) e para medir a prova do seed (`:685`), com os quatro estados de cada uma (`GRAVADAS`/`NAO PASSARAM`, `MEDIDA`/`REPROVOU`/`COPIADA`/`AUSENTE`). Para a prova de 4 h não há bloco nenhum. `git diff --stat <base> HEAD -- scripts/start_checkpoint_audit.sh` é **vazio**.
  3. **O verificador não está quebrado.** Os probes passam nas 7 direções (9 venenos + 2 controles positivos, incluindo o modo declarado "pack ausente do checkout"). O vermelho é ausência real, e o próprio arquivo declara que ausência **não degrada para OK** (`:28`, `:123-131`).

  O pack tampouco está no checkout (`ls scenarios` → `No such file or directory`; `.gitignore:125`), então não há nem a perna (g) de conferência por hash.

  **Por que é blocker — três consequências concretas:**
  1. **O item 9 é o veredito da fase.** `06_ACCEPTANCE_TESTS.md:147` diz com todas as letras: *"Passar do envelope medido lá é sinal; **o veredito é este critério**"*. Ele chega a mim exatamente como chegou na 2ª rodada — transcrição em `fase_7.md`, sem objeto verificável.
  2. **O H2 não foi corrigido, foi metade-corrigido, e o registro afirma o contrário.** `fase_7.md:3006` marca **CORRIGIDO** e `:1231` marca a P7-3 como `RESOLVIDA` "de verdade". Nasceu o mecanismo — que é bom e bem construído — e ninguém o executou nem lhe deu produtor. Um verificador cujo insumo nada produz não fecha o achado: ele o torna vermelho e visível, que é o estado em que a fase está agora.
  3. **É a classe que este repositório documenta e repete.** `user-scope/hooks/readonly_bash.py:126-139` fixa a regra *"script novo que precise ser executado pelo auditor entra aqui por nome, **no commit que o cria**"* — e o verificador **entrou** na allowlist (`:345`). O que não entrou foi o produtor no lançador. É a mesma assimetria que o B1 da rodada anterior pagou (pacote declarado no `pyproject`, ausente do `Dockerfile`), cometida na correção do achado seguinte.

---

### HIGH

- **[H1] A prova negativa do verificador novo não entra no CI, enquanto as duas irmãs da mesma família entram — o gate que decide dois itens de DoD não tem gate**

  **Arquivo:** `.github/workflows/invariants.yml:296-297` (`check_prova_do_seed_probes.py`) e `:324-325` (`check_provas_de_container_probes.py`) × `scripts/check_prova_do_exercicio_4h_probes.py` (**ausente do workflow**)

  **Requisito violado:** `.claude/rules/gates.md` (R10) §"Nascimento de um gate" e proibição 9 — *"Checagem nova entra no `pipeline.yaml`"*; `.claude/rules/design-decisions.md` (R13), linha *"Probes-only no CI para predicados do lançador/worktree"*, que é precisamente o desenho que as duas irmãs seguem.

  **Evidência:** `grep -n "prova_do_exercicio" .github/workflows/invariants.yml` → **nenhuma ocorrência**. As duas irmãs têm passo nomeado e comentário explicando por que só os probes rodam (`:287-297` e `:313-325`). O terceiro membro da família, criado nesta rodada e declarado "a classe P4-10" em `fase_7.md:3006`, não tem passo. Medido: os probes passam localmente (rc=0, 9 venenos), logo o custo de incluí-los é zero.

  **Por que é HIGH:** o `check_prova_do_exercicio_4h` é o único caminho pelo qual os itens 6 e 9 deixam de ser atestação — é o que `check_allowlist_do_auditor.py:129-133` declara ao dizer que o auditor confere *"a PROVA GRAVADA … não a transcrição"*. Sem os probes no CI, nada impede que uma edição futura o faça degradar para "ok" por ausência, que é a direção (a) e a propriedade central do objeto. Verificador sem prova negativa executada continuamente é verificador cuja propriedade se aceita da palavra de quem o escreveu — a frase está escrita no próprio `readonly_bash.py:300-302`.

- **[H2] O item 7 da DoD não passa pela letra: não existe migrador, e `SUPPORTED_SCHEMA_VERSIONS = (2,)` contraria `04` §4 sem `spec-change`**

  **Arquivo:** `range-core/engine/loader/pack_loader.py:126`; `range-core/engine/migrations/__init__.py:96` (`MIGRACOES = MappingProxyType({})`); `tests/test_migrations.py:38` (`test_nao_ha_migracao_declarada`)

  **Requisito violado:** `04_SCENARIO_SCHEMA.md` §4 — *"Engine declara `SUPPORTED_SCHEMA_VERSIONS = [N, N-1]`"*, *"Pack em N-1 carrega com migração em memória e aviso no boot"*, *"Migrações em `range-core/engine/migrations/v<n>_to_v<n+1>.py`, cada uma com teste"*; `07` §"Fase 7" item 7; `06` T12 primeiro critério.

  **Evidência:** `ls range-core/engine/migrations/` → só `__init__.py`; `MIGRACOES` vazio; a classe de teste chama-se `ORegistroEstaVazioEIssoEADeclaracao`. Com `(2,)` não há N-1: pack v1 é **recusado**, não **migrado**. A 2ª metade do item (v0 recusado com instrução) passa e está testada.

  **Por que é HIGH, e o que mudou desde a rodada anterior:** o rito agora está correto — a divergência virou **P7-18** (`fase_7.md:1246`, `ABERTA`), com texto de `spec-change` proposto e apresentação ao proprietário no fechamento, e a §9 re-qualifica o fechamento para *"8 pela letra e o item 7 pela forma"*. Isso é exatamente *"pare e explique, não contorne"* do `CLAUDE.md`. **Não há ação de implementador pendente aqui**, e este achado não sustenta o FAIL: registro-o porque o item de DoD continua sem passar, e `CLAUDE.md` §"Fluxo por fase" condiciona a conclusão da fase a **todos** passarem. A resolução é decisão do operador no ⏸.

---

### MEDIUM

**Nenhum.** Verifiquei a regra de achado órfão (P7-6) item a item contra a 2ª rodada (`docs/progress/audit_20260913T065544Z.md`): M1 e M2 foram **corrigidos** (medidos abaixo), H1 → **P7-18** (`:1246`), H3 metade-mecanismo → **P7-19** (`:1247`), L1 → **P7-20** (`:1248`), H3 metade-dado → P1-7/P5-4/P6-5 reclassificadas de `ENTREGA` para `ABERTA` com gatilho real (`:1214`, `:1217`, `:1221`). Nenhum achado M/L da rodada anterior ficou sem linha na §6.

Correções confirmadas por execução, não por leitura do registro:
- **M1** — `tools/check_contract_literals.py:86` e `tools/check_security_constraints.py:44-53` incluem `range_cli` e `dados_sinteticos`; os dois verificadores saem rc=0 sobre a árvore.
- **M2** — `user-scope/hooks/readonly_bash.py:108` ganhou `PREFIXO_DO_VENV`; medido: `.aurora-audit/venv/Scripts/range-cli scenario lint tests/fixtures/pack_minimo` executa e devolve rc=2 com o achado correto. A grafia era rc=127 na rodada anterior.
- **B1 da rodada anterior** — `Dockerfile:64-71` copia os dois pacotes; build completo verde e as duas provas de container rc=0 sobre esta árvore. Os itens 1 e 4 da DoD da Fase 4 voltam a **PASS**.

### LOW

**Nenhum.**

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `lint` recusa inject sem objetivo e sem `noise: true` | **PASS** | `tests/test_range_cli_lint.py:255` e `:273` — as duas pernas (recusa + `noise: true` dispensa), executadas nos 856. Negativos de contrato entre os 95 de `check_contract_examples` |
| 2 — `event_type` inexistente em condição de branch, **com posição** | **PASS** | `tests/test_range_cli_lint.py:294` e `:311` — o segundo prova que a coluna aponta para o **valor**, não para a chave; é o caso em que um teste frouxo passaria |
| 3 — condição por juízo do facilitador recusada | **PASS** | `tests/test_range_cli_lint.py:329`, com posição; `branch_condition` com `oneOf` fechado + `additionalProperties: false` |
| 4 — `branch_policy` do manifesto aplicada | **PASS** | `tests/test_branching.py:159-206` — excesso de pontos, excesso de caminhos, política ausente não inventa default, e a linha do ponto caindo para a do `at_inject` |
| 5 — branch sem `reconverge_at` recusada | **PASS** | `contracts/scenario.schema.v2.yaml` `required: [id, at_inject, evaluate, reconverge_at]`; negativo entre os 95 executados; `tests/test_range_cli_lint.py:486` |
| 6 — `dryrun` percorre todos os caminhos | **PARCIAL** | **PASS sobre fixture:** `tests/test_range_cli_dryrun.py:27-69` (4 eixos, incl. lint-antes-da-travessia, `branch_walk_impossible` e o caso sem `branches.yaml`) + `tests/test_branching.py:293-368`. **NÃO VERIFICADO sobre o pack real** — B1 |
| 7 — pack v1 migra automaticamente; v0 recusado com instrução | **FALHA na 1ª metade (letra)** | Não existe migrador; `SUPPORTED_SCHEMA_VERSIONS = (2,)`. 2ª metade **PASS** e testada. Disposto como P7-18, com dono — ver H2 |
| 8 — fato citado no `GM_NOTES.md` e ausente do `ground_truth.yaml` é recusado | **PASS** | `range-core/engine/citacoes.py` com a forma derivada de `ground_truth.schema.yaml §$defs/fact_id_pattern` (`tests/test_citacoes_de_fato.py:88`, e `:91` recusa alto se o `$def` sumir). Recusa **na carga**, contra o pack materializado (`:224`, `:236`), com controles positivos (`:213`, `:218`, `:252`). **Dirigido por `fact_id`, não por seed** |
| 9 — reconstrução do exercício de 4 h em < 3 s | **NÃO VERIFICADO** | `check_prova_do_exercicio_4h.py` rc=1 por ausência do artefato — B1 |
| *(Fase 4, itens 1 e 4 — regressão da rodada anterior)* | **PASS — regressão fechada** | `check_provas_de_container.py` rc=0 sobre a árvore `b67d4d2b1963`; `demo` e `reinicio` rc=0 |

---

## TESTES QUE NÃO PROVAM O REQUISITO

**Nenhum encontrado.** A suíte resiste aos padrões que procuro, e verifiquei os eixos de risco por leitura das asserções, não do nome do teste:

- **Nenhum `skip` disparou.** Os 9 `skipIf(_URL is None)` ficaram inativos com os serviços no ar — `Ran 856 tests … OK`, sem `(skipped=N)`. Skip aqui seria defeito, e não houve.
- **Nenhum mock de comportamento nos módulos da fase.** Os únicos `mock.patch` da árvore são `tests/test_contract_rules.py:143,158` (substitui o `Draft202012Validator` para provar que o harness detecta validador quebrado — o mock é o *objeto sob teste*, não o dublê do requisito) e `tests/test_telas.py:131,149` (indireção de diretório). Nenhum dos dois está no diff desta fase.
- **Item 8 é dirigido por `fact_id`, e a forma vem do contrato.** `test_a_forma_e_a_de_fact_id_pattern` (`:88`) lê o padrão do `$defs`, e `test_contrato_sem_o_def_recusa_alto` (`:91`) impede que um literal coincidente satisfaça a asserção. É o desenho que distingue "lê o contrato" de "tem a regex certa por sorte".
- **Item 2 tem o discriminante certo.** `:311` exige `linha[coluna-1:] == "vpn_acess_revoked"` — apontar para a chave `event:` (que também está "certo") reprovaria. O requisito é *localizar*, e o teste mede localização.
- **Controles positivos presentes em toda recusa** (`test_pack_limpo_sai_zero`, `test_CONTROLE_POSITIVO_o_pack_integro_carrega`, `test_par_correto_nao_tem_achado`), sem os quais nenhuma recusa significaria nada.
- **O verificador novo tem prova negativa real:** 9 venenos e 2 controles, incluindo o modo declarado "pack ausente do checkout" — é o que me permitiu afirmar que o rc=1 de B1 é ausência, e não verificador quebrado.

Observação sem finding (mantida da rodada anterior, e continuo concordando): `branching.percorre` enumera um caminho por braço de cada ponto, e não o produto cartesiano entre pontos de linhas distintas. `04` §6/§9 ("um ponto de ramificação por linha") e T12 não exigem o produto, e as janelas são por linha — leitura defensável, registrada em `fase_7.md` §5.2 **antes** do código.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **O pack `ransomware-universidade` — inteiro.** `scenarios/` não existe neste checkout (`.gitignore:125`, decisão da Fase 5) e não entra no hash da árvore. Não pude conferir nenhuma afirmação de `fase_7.md` §8.3: 40 injects, 5 `decision_point`s, 2 `capability_gap`s, 4 `media_event`s, `content_hash`, nem os 0,025 s. Isto é B1, e não uma limitação aceita: a fase construiu o mecanismo que fecharia essa lacuna e não o alimentou.
2. **A medição do item 9.** `scripts/medida_do_exercicio_4h.py` e `scripts/prova_do_exercicio_4h.py` estão fora da allowlist por decisão declarada e conferida (`check_allowlist_do_auditor.py:129-138`, rc=0) — exigem Postgres e truncam o store de medição. O caminho previsto para mim era ler a **prova gravada**, e ela não existe.
3. **As provas de container (P4-10) e a prova do seed (M2).** Não as vi rodar. As duas conferem pela árvore `b67d4d2b1963` — igual à deste checkout — e imprimiram saída íntegra; ambas rc=0. O limite é o declarado na P7-2: a prova amarra **árvore**, não commit, e a árvore cobre só conteúdo rastreado.
4. **O ambiente da suíte não é o canônico.** O venv do lançador é **Python 3.14.7** (`.aurora-audit/venv/pyvenv.cfg`), contra `00_MASTER_SPEC.md` §8 e o `python-version: "3.12"` do CI. Os 856 passaram, logo não houve vermelho a diagnosticar por causa (R2 §3) — mas a suíte que rodei não é a do CI Linux 3.12. A diferença aparece como `ResourceWarning` em massa do `psycopg`, já registrada na P7-14.
5. **O pipeline da Estrutura Agêntica** (`bash .claude/verify/run.sh`, `compliance-audit.sh`). Fora da allowlist do auditor; não executado. O hook de prompt registra que nenhum pipeline verde foi gravado nesta máquina — declarado, não omitido.
6. **P7-10 — o gabarito só tem Linha B.** `fase_7.md:1238` declara que o `ground_truth.yaml` materializado não tem fato de `initial_access`/`exfiltration`, contra `03` §3.1 e `04` §3/§9, com `TTRV` incomputável. Não pude medir — o arquivo não está na árvore. Está `ABERTA` com gatilho *"fechamento desta fase — apresentação ao proprietário"*, logo **não é achado órfão**; registro que ela pesa sobre o insumo do item 9.
7. **Comportamento de runtime em container** além das duas provas gravadas. Inalcançável por construção (o auditor não sobe stack).
