# Auditoria de checkpoint - 2026-08-17T22:14:10.467348+00:00

- fase: **4**
- commit auditado: `3a5ee71c9aaa114d918a78659bb125932ca0ef20`
- session_id: `0caadd0a-21db-45b7-b09e-c84b433b63e1`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have gathered sufficient evidence. Here is my audit.

```text
# AUDITORIA — FASE 4

## VEREDITO: PASS

## FINDINGS

### BLOCKER
nenhum encontrado.

### HIGH
nenhum encontrado.

### MEDIUM
nenhum encontrado.

### LOW

- [L1] O commit auditado altera o hook que define o envelope de capacidade do próprio auditor
  Arquivo: user-scope/hooks/readonly_bash.py:6-253 (diff vs 486df18); prova em
           scripts/phase0_negative_tests.py — "OK: fonte versionada e copia
           instalada de readonly_bash.py identicas"
  Requisito violado: CLAUDE.md §"Fluxo por fase" — *"um auditor definido pelo commit
           que ele audita pode ser enfraquecido por esse mesmo commit"*. Não há
           seção de `docs/spec/` violada; por isso é LOW e não BLOCKER.
  Evidência: a definição do subagente vive fora do repositório, como a norma manda,
           mas o hook que restringe o auditor é versionado em `user-scope/hooks/` e o
           harness prova que a cópia instalada em `~/.claude/hooks/` é IDÊNTICA à
           versionada. O commit sob auditoria portanto alterou, de fato, a regra que
           me constrangeu nesta sessão. As mudanças são aditivas e verifiquei as
           mitigações executando o harness: 64 leituras liberadas, 42 escritas
           bloqueadas, 14 formas × 8 grafias de alvo, **"0 escritas nao bloqueadas"**;
           `grava_provas_de_container` e `reancorar_sessao` são excluídos por decisão
           escrita E provados bloqueados por probe; a relaxação de `>` é
           quote-aware e continua bloqueando — confirmado empiricamente, meu
           `2>tests_verbose.txt` foi recusado.
  Por que é low e não blocker: a direção da mudança é de ampliação de verificação
           (verificadores read-only entram), não de redução; nenhuma escrita escapa;
           e cada exclusão é provada por mecanismo, não afirmada por comentário.
           Registro-o porque a estrutura — hook sincronizado a partir da árvore
           auditada — permanece, e ela é a forma que a norma nomeia.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — sequência do DEMO ponta a ponta sem intervenção manual | PASS | `check_provas_de_container.py` rc=0, SHA `3a5ee71c9aaa` conferido contra HEAD: prova `demo` rc=0 contra dois containers + Postgres + Redis. Sequência íntegra: matrícula 201 → start → disparo A01 → telão saúde 100→90 em 63 ms → plateia com `texto_para_plateia` → matrícula **503** → rollback → saúde 100, epoch 1 → **mesma requisição 201**. `scripts/demo_fase4.py` é passo de CI (`invariants.yml:604`). Eu **não vi rodar** — ver limites. |
| 2 — wallboard atualiza em < 1 s via WebSocket | PASS | Duas provas independentes. Protocolo: `tests/test_range_api.py:197` (frame na MESMA chamada que gravou o evento), `:219` (UM frame por evento, 3 telas ≤ 2 leituras, store instrumentado — não é duplo), `:245` (ausência de `time`/`threading`/`sched` por AST em `app.py` e `hub.py`). Relógio: 63 ms medidos na prova de container, orçamento 1,0 s. |
| 3 — refresh recupera estado corrente (wallboard e participant-view) | PASS | `tests/test_range_api.py:175` — conexão nova recebe o estado corrente, com o par que discrimina (antes `texto=""`, depois contém "matrícula"). `:143` cobre **os dois** canais, pós-disparo, com igualdade byte a byte entre frame do WS e corpo do snapshot HTTP — o que prova que o wallboard também entrega estado corrente no `accept`. Gate `check_api_surface.py` cobra que canal e snapshot declarem a mesma projeção e a mesma visibilidade. |
| 4 — reinício do container do engine restaura a partir do event store | PASS | `check_provas_de_container.py`, prova `reinicio` rc=0 sobre este SHA, atravessando `docker restart`: pausado→`StartedAt 21:48:02→21:48:10`, clock congelado em T+5s; retomado→reinício, clock T+5s→T+14s com 9 s fora do ar. O par de T5 inteiro. Reforçado em processo por `tests/test_restauracao.py:317` e cross-process por `:457` (este SKIPPED aqui, sem banco). |
| 5 — rollback anotado na timeline | PASS | `tests/test_projecoes.py:680` — `{"motivo","para"}` + rótulo ROLLBACK, com o par negativo (o disparo NÃO é anotado); `:695` epoch por entrada. Ao nível de rota: `tests/test_range_api.py:423`. No container: "3 entradas, rollback anotado, **disparo preservado**" — asserção explícita de `00` §5.5 (rollback não apaga). |
| 6 — índice de saúde a partir dos `severity_weight` ativos | PASS | `tests/test_projecoes.py:279-375`: função pura; 100 sem nada fora do default; peso maior baixa mais; flag `number` contribui em proporção (0.5→50, 1.0→0); e o eixo de **sinal** — flag de default `true` que cai PIORA o índice — verificado contra `domains/academus/flags.yaml` real, com a flag nomeada por constante gerada, não por literal. |

**Invariantes arquiteturais (executados nesta árvore, todos rc=0):**
`check_core_boundary` · `check_contract_literals` · `check_event_envelope` ·
`check_security_constraints` · `check_synthetic_data` · `codegen.py --check` ·
`check_store_read_surface` (leitura total, sem filtro por epoch) ·
`check_fold_authority` · `check_core_contract_imports` · `check_api_surface` (+66 eixos
de probe) · `check_web_sem_derivacao` · `check_telas_sem_vocabulario` ·
`check_banner_de_simulacao` · `check_pinned_images` · `check_gate_coverage` ·
`check_progress_consistency` · `check_spec_flags` · `phase0_negative_tests`.
`objective_ids` não aparece em nenhum caminho de emissão — só em contratos (exemplos
negativos), verificadores e probes. `range-core/` não importa `domains/`; o único
`__import__` do core (`api/app.py:411`) resolve o próprio pacote.

**Suíte:** `python -m unittest discover -s tests` → **335 testes, OK, 73 skipped**.

## TESTES QUE NÃO PROVAM O REQUISITO

nenhum encontrado. Especificamente conferido e **não** achado:

- **Zero mocks na suíte inteira** — nenhum `unittest.mock`, `MagicMock`, `patch(` ou
  `Mock()`. `TestClient` fala ASGI de verdade; `StoreContado` é a classe real com um
  contador, não um duplo.
- **Zero `skip`/`xfail` incondicional.** Os 73 skips vêm todos de
  `skipIf(_URL is None)` / `exige_banco` — condicionais à ausência de Postgres,
  com razão impressa e instrução de execução.
- **T6 é testado no payload da API, não na UI** — `test_projecoes.py:579` faz varredura
  recursiva de chaves e valores sobre o corpo serializado, como `06` T6 exige, e
  `test_a_plateia_NAO_tem_como_receber_o_resto_do_inject` fecha a garantia pelo **tipo**
  (`Inject` não tem `linha`, `descricao_facilitador`, `titulo`), não por filtro.
- **DoD 2 sem cronômetro é reforço, não substituição** — a prova estrutural convive com
  a medição real de 63 ms na stack.
- **Os gates têm anti-vacuidade provada**: diretório vazio, vocabulário vazio, spec
  ilegível e bundle ausente **reprovam com rc=2** em vez de passar por não ter o que
  olhar. `check_provas_de_container` reprova em 13 eixos, incluindo ausência, SHA
  divergente e "verde e mudo".
- **A mutação em `tests/mutation_harness.py` é correção, não enfraquecimento**: o
  registro entrou no `try` para que módulos mutados não sobrevivessem em `sys.modules`.
- **As deleções em `test_api_degradacao.py`/`test_api_rbac.py` são a renomeação
  PT→EN das rotas (P4-1)**, não remoção de asserção: os 18 testes de degradação
  seguem presentes e ampliados.
- **`/console` público não é ampliação silenciosa de `05` §8**: o `spec-change` da P4-9
  foi mergeado em `486df18`, que é a base; li `05` §8 nesta árvore e ele já isenta
  "a casca estática do gm-console e a rota que troca credencial por token". A âncora foi
  regravada para `486df18` no mesmo passo do rebase, e bate com a base do lançador.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Não vi as provas de container rodarem.** Isto é desenho declarado (P4-10, opção A),
   não omissão: o lançador as executou na máquina do operador, e a allowlist não me dá
   Docker nem rede. O que amarra a evidência a este commit é o SHA que o arquivo carrega
   — `3a5ee71c9aaa`, conferido contra o meu `HEAD` — e verifiquei o verificador nos 13
   eixos, inclusive que ausência e SHA divergente **reprovam**. Continua sendo evidência
   mediada: eu julgo o mecanismo e o vínculo, não a execução.
2. **73 testes pularam por ausência de Postgres/Redis**, como o lançador anunciou
   (`alembic upgrade head` falhou). Ficaram sem execução nesta sessão: business state em
   Postgres, restauração cross-process, cache de projeção e queda de sessão em outro
   processo. O mesmo caminho foi exercitado de ponta a ponta pelas provas de container
   com Postgres e Redis reais — mas por evidência mediada, não por mim.
3. **Os eixos de BUNDLE de `check_telas_sem_vocabulario` e `check_banner_de_simulacao`
   não rodaram aqui.** `dist/` está no `.gitignore` e não há build nesta árvore: os dois
   reportaram "0 do bundle" e "3 telas (fonte)". Afirmo que o **fonte** carrega o banner
   normativo e não vaza vocabulário; que o **bundle servido** também os carrega está
   coberto por `invariants.yml:550,556` (`--exige-bundle` depois do build) e pelo build
   dentro da prova de container, não por mim.
4. **Renderização, contraste e legibilidade a 10 m** (`01` §6) — sem navegador. Limite
   já declarado pela própria fase em `fase_4.md` §2.2, e eu o confirmo como limite.
5. **Isolamento de rede da sala** (`05` §8) — verifiquei o que é verificável: bind em
   `127.0.0.1` em todas as portas publicadas de `docker-compose.yml`, e `0.0.0.0` apenas
   dentro do namespace do container, ao lado da linha que publica em loopback. Que a
   rede do exercício esteja isolada é propriedade de deploy e não a alcanço.
6. **A metade de `06` T6 que fala do `operador`** não foi verificada porque **não tem
   sujeito nesta fase**: T6 diz "Fases 4–5", o NON-GOAL da Fase 4 é "três papéis de
   facilitação", e nenhuma rota admite `operador`. Conferi que a base para ela existe e
   falha fechada — `Superficie.e_publica` casa por igualdade exata, recusa a carga se
   uma rota pública tiver parâmetro, e o middleware exige `claims.role == PAPEL_DO_CONSOLE`.
   O diferimento é declarado, não silencioso.
7. **Sincronia da cópia instalada do `guarda de branch`** — o harness reportou
   `AUSENTE: .git/hooks/pre-commit`, esperado num worktree de auditoria. A sincronia
   do `readonly_bash.py` e do `sentinela_de_branch.py` **foi** conferida.
8. **Oito falsos bloqueios do hook (P23 reaberto)** limitaram a forma dos meus comandos
   — perdi `-v` no unittest e laços de shell. São declarados pelo harness, e a direção é
   segura: "8 leituras legítimas bloqueadas e **0 escritas não bloqueadas**".

---

**Nenhum BLOCKER. Veredito PASS.** Os seis itens da DoD têm evidência executável, o par
de T5 está provado nos dois casos e atravessando `docker restart`, e os quatro
invariantes arquiteturais passam com prova negativa contra violação plantada. As
pendências abertas — P4-2, P4-4, P4-5, P4-6, P4-7, P4-8 — estão datadas para fases
futuras e nenhuma delas é item de DoD desta fase.
```
