# Auditoria de checkpoint - 2026-08-14T15:30:38.115075+00:00

- fase: **0**
- commit auditado: `c47759b943fc3ec74180778ba641e5b850dfcb5d`
- session_id: `634611e3-1759-4e3b-adee-3a5277940934`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 0

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- **[B1] Item 4 da DoD não satisfeito, e o defeito é maior do que o próprio registro declara**
  Arquivo: `user-scope/hooks/readonly_bash.py:163`
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done item 4 ("Hook do auditor bloqueia escrita deliberada e **libera testes/verificadores de leitura**"); `CLAUDE.md` §Secrets (".env.example é permitido"); `05_SECURITY_REQUIREMENTS.md` §6 (".env.example versionado")
  Evidência: executei `cat .env.example` nesta auditoria e recebi `BLOQUEADO: checkpoint-auditor sem escrita deliberada (leitura de secret por caminho de shell)`. A regex `\.env(?:\.[A-Za-z0-9_.-]+)?` casa `.env.example`. Este é **exatamente o overmatch P17**, corrigido em `.claude/settings.json` no commit `0b425f1` (que trocou `Read(.env.*)` por `.env.local`/`.env.*.local`) e **deixado intacto no hook**. Os dois mecanismos que implementam a mesma norma divergem. Consequência medida: este bloqueio **não está** entre os 4 falsos bloqueios declarados pelo harness (`laço de shell`, `smoke test do PHASE_0_CHECKLIST`, `leitura fora do worktree`, `listagem fora do worktree`), e o harness imprimiu "4 leituras legitimas bloqueadas" na mesma execução em que este quinto existia. `docs/progress/fase_0.md:619` afirma "**4 falsos bloqueios, os quatro por decisão registrada**" — a afirmação é falsa como contagem.
  Por que é blocker: o item 4 da DoD está objetivamente descumprido e a célula de evidência que o sustenta está incorreta. Por `CLAUDE.md` §"Fluxo por fase", uma fase só está concluída quando **todos** os itens da DoD passam. Concretamente: o auditor não consegue ler pelo shell o arquivo que a §6 da spec de segurança exige versionado, nem executar o smoke test que o próprio checklist prescreve (`PHASE_0_CHECKLIST.md:72-75`) — verifiquei que esse smoke test também é recusado.

### HIGH

- **[H1] O harness prova "estas 17 leituras passam", não "leitura legítima passa" — enumeração no eixo de leitura**
  Arquivo: `scripts/phase0_negative_tests.py:120-153` (`LEITURA_LEGITIMA`)
  Requisito violado: `PHASE_0_CHECKLIST.md` §Teste negativo obrigatório ("Um verificador que nunca falhou contra uma violação plantada não é um verificador")
  Evidência: `LEITURA_LEGITIMA` e `FALSOS_BLOQUEIOS` são listas fixas escritas à mão. O bloqueio de `.env.example` (B1) passou pelo harness sem nenhuma reprovação, porque não está enumerado em lista nenhuma. O commit candidato `c47759b` declara ter substituído "enumeração de sigilos por PROPRIEDADE" — mas fez isso **apenas no eixo de escrita** (parêntese não citado). O eixo de leitura continua sendo memória de quem escreveu a lista, que é a falha que o próprio commit diz estar corrigindo.
  Por que é HIGH: enquanto o critério de "libera leitura" for uma lista, o harness sairá verde com qualquer número de falsos bloqueios não lembrados, e o número publicado na DoD continuará sendo o tamanho da lista, não a medida do defeito.

### MEDIUM

- **[M1] A correção central do commit candidato anterior (P9/P24) não tem probe**
  Arquivo: `tools/check_security_constraints.py:36`; ausência em `scripts/phase0_negative_tests.py`
  Requisito violado: `PHASE_0_CHECKLIST.md` §Teste negativo obrigatório; §DoD item 2
  Evidência: `SCANNED_DIRS` passou a incluir `.claude` e `user-scope`, com a justificativa "sem eles o verificador de seguranca nao olhava para o arquivo que decide o que o auditor pode rodar". Nenhum probe do harness planta violação de segurança em `.claude/` ou `user-scope/` — o único probe é `range-core/_phase0_probe_security.py`. Verifiquei que a extensão **não é inerte** (`_common.py:104` usa `base.rglob("*")` a partir de `root/".claude"`, e `PRUNED_DIRS` em `_common.py:40-48` não exclui `.claude`), mas a detecção nesses diretórios não é provada por execução — é inferida.
  Por que é MEDIUM: o benefício alegado do commit vale rc=0 na árvore limpa, e nada mais. É exatamente a classe "item feito sem evidência executável".

- **[M2] A fase editou o documento que define sua própria DoD, no mesmo commit que o código, sem gate**
  Arquivo: `docs/process/PHASE_0_CHECKLIST.md:10-11` (commit `2065d24`); `.github/workflows/invariants.yml:70-72`
  Requisito violado: `CLAUDE.md` §"A especificação é imutável durante a implementação" (princípio); `PHASE_0_CHECKLIST.md` §Entregáveis
  Evidência: o commit `2065d24` removeu o entregável `log_audit.py` da lista e acrescentou `scripts/audit_report.py`, alterando código no mesmo commit. O job `spec_freeze` exclui `docs/process/` do conjunto CODE **de propósito** (comentário em `invariants.yml:70-72`: "docs/process/ fica de fora: e documentacao descritiva"). Como a DoD da Fase 0 mora em `docs/process/`, nenhum gate impede que a fase reescreva seu próprio critério de aceitação junto com o código. Registro atenuante: a capacidade foi preservada e é verificável — `scripts/audit_report.py` existe e é invocado pelo launcher em `scripts/start_checkpoint_audit.sh:75`, e `log_audit.py`/`SubagentStop` não são citados em `docs/spec/` (grep sem ocorrências).
  Por que é MEDIUM: não houve perda funcional nem violação literal (a imutabilidade normativa cobre `docs/spec/`), mas o mecanismo que impede acoplamento spec↔código tem um ponto cego sobre o documento que define o "pronto" desta fase.

### LOW

- **[L1] Assimetria entre os dois mecanismos de negação de secret**
  Arquivo: `.claude/settings.json:8-11` vs `user-scope/hooks/readonly_bash.py:163`
  Requisito violado: `05_SECURITY_REQUIREMENTS.md` §6; `CLAUDE.md` §Secrets
  Evidência: o deny por ferramenta cobre exatamente `.env`, `.env.local`, `.env.*.local`, `secrets/**`. Um arquivo como `.env.production` com segredo real não é negado por `Read`/`Edit`. O hook, ao contrário, **super**-casa (B1). Um mecanismo erra para menos, o outro para mais, sobre a mesma norma.
  Por que é LOW: `.env.production` não existe nesta fase e o caminho de shell fica coberto pelo overmatch do hook; o risco é latente para a Fase 1.

- **[L2] Casamento em texto cru gera falso bloqueio por substring**
  Arquivo: `user-scope/hooks/readonly_bash.py` (regra "acesso de rede")
  Evidência: um payload JSON contendo a sequência `\nc = 6` foi recusado com "acesso de rede" — `nc` casou dentro de `\nc`, com o `\` funcionando como fronteira de palavra. Payload construído por mim, portanto artificial; registro apenas como evidência adicional da classe de defeito do H1.

## DoD — item a item

Referência: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done.

| Item | Status | Evidência |
|---|---|---|
| 1 — seis verificadores liberam árvore limpa | ✅ PASS | Executados por mim: `core_boundary`, `contract_literals`, `event_envelope`, `security`, `synthetic`, `codegen --check` — todos `rc=0` |
| 2 — os seis detectam violações externas do harness | ✅ PASS | `python scripts/phase0_negative_tests.py` → 13 probes de verificador, todos `rc=1`. `expect_fail` (`:67-100`) exige rc **exatamente 1**, saída não vazia e menção ao arquivo plantado — rejeita crash disfarçado de detecção. `git status --porcelain` limpo após a execução (sem resíduo de probe) |
| 3 — hook bloqueia import de `domains/`, edição de `docs/spec/`, literal de flag | ✅ PASS | Executados: import `domains` em `range-core/x.py` → `exit=2`; edição de `docs/spec/00_MASTER_SPEC.md` → `exit=2`. Literal de flag verificado por leitura (`check_architecture.py:46`), com regex ancorada em aspas; comentário contendo o nome da flag corretamente **não** dispara (`rc=0`) |
| 4 — hook do auditor bloqueia escrita e libera leitura | ⛔ **FAIL** | **B1.** Escrita: 33 formas bloqueadas + 32 provas de alvo, **0 escritas não bloqueadas** — esta metade passa. Leitura: 4 falsos bloqueios declarados **+ 1 não declarado** (`.env.example`) medido por mim |
| 5 — hook do `scenario-designer` bloqueia fora de `scenarios/` | ✅ PASS | Executado: `Write` em `range-core/nope.py` → `exit=2` com destino absoluto na mensagem |
| 6 — `ground_truth.yaml` e `GM_NOTES.md` não estão no `.gitignore` | ✅ PASS | Lidos `.gitignore` e seu diff: aparecem apenas em comentário; entradas efetivas são `.aurora-worktrees/`, `.last_audit_session`, `scenarios/**/evidence/` |
| 7 — `.env`/secrets negados em `.claude/settings.json` | ⚠️ PARCIAL | Deny presente e alinhado a `CLAUDE.md`; ressalva do L1. `.env.example` lido: só placeholders, sem valor real |
| 8 — Auto Mode desabilitado | ✅ PASS | `.claude/settings.json` sem alteração de modo no diff; `defaultMode: default` |
| 9 — primeiro push deixa `arquitetura` e `seguranca` verdes | ❓ NÃO VERIFICÁVEL | Atestação do operador. `spec_freeze` é PR-only confirmado por leitura (`invariants.yml:47`) |
| 10 — PR descartável: spec+código reprova | ❓ NÃO VERIFICÁVEL | Atestação. Existem `origin/dod10-descartavel`, `origin/dod11-descartavel` |
| 11 — PR descartável: título `spec-change:` | ❓ NÃO VERIFICÁVEL | Atestação. Lógica lida em `invariants.yml:120-123` |
| 12 — branch protection exige os três contexts | ❓ NÃO VERIFICÁVEL | Atestação; exige API do GitHub |
| 13 — `spec-v1.0` criada após CI e branch protection | ❓ PARCIAL | Tag existe, aponta para `b4abd9e` (2026-08-13 13:44), anterior ao tip de `main` `edd9527`. A **ordem** relativa ao CI não é observável daqui |
| 14 — `/doctor` sem apontamentos | ❓ NÃO VERIFICÁVEL | Fora do alcance do worktree |

Entregáveis do §Entregáveis conferidos por `git ls-files`: os 11 documentos de `docs/spec/`, `CLAUDE.md`, dois agentes, três hooks de projeto, `scripts/audit_report.py`, seis verificadores em `tools/`, harness, launcher, `.gitignore`, `bootstrap.sh`, `finalize_phase0.sh`, `WORKFLOW.md`, `user-scope/`. Todos presentes.

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`LEITURA_LEGITIMA` / `FALSOS_BLOQUEIOS`** (`scripts/phase0_negative_tests.py:120-153`) — provam que 17 comandos nomeados passam e que 4 nomeados falham. Não provam a propriedade "leitura legítima é liberada". O caso `.env.example` demonstra a lacuna empiricamente (H1/B1).
2. **Probe de segurança restrito a `range-core/`** — nenhum probe exercita `.claude/` nem `user-scope/`, que são precisamente os diretórios que o commit candidato acrescentou a `SCANNED_DIRS` (M1). O harness sai verde tendo testado a árvore antiga.
3. **Observação, não finding:** o harness afirma o estado **medido** do hook nas quatro combinações, de modo que uma correção acidental também reprova. É intencional e está declarado (`:111-117`). Registro para que não seja lido como teste frágil.

Nenhum `skip`/`xfail` sem justificativa foi encontrado. Nenhuma asserção sobre mock. Nenhuma violação dos quatro invariantes arquiteturais (não há `range-core/` nem `domains/` nesta fase; os verificadores que os guardam existem e falham contra probes). Nenhuma violação de `05_SECURITY_REQUIREMENTS.md` §1–§5 encontrada: não há exploit, payload, criptografia de arquivo como efeito, IOC nem anexo; `check_synthetic_data.py` e `check_security_constraints.py` saem `rc=0` e falham contra probes plantados.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 9 a 13 da DoD** (CI verde no primeiro push, os dois PRs descartáveis, branch protection com `enforce_admins`, ordem de criação da tag). Exigem `gh`/API do GitHub, fora da allowlist do hook do auditor e fora do worktree. Registro o que o `fase_0.md:624-628` já declara explicitamente: são **atestação do operador**, camada 3 das quatro verdades, não evidência observável. Não os converto em PASS.
- **Item 14 (`/doctor`)** — não executável daqui.
- **Paridade entre a fonte versionada e a cópia instalada** de `readonly_bash.py` em `~/.claude/hooks/`. O harness afirma tê-la verificado ("fonte versionada e copia instalada identicas"), mas não pude confirmar independentemente: leitura fora do worktree é um dos falsos bloqueios declarados. Como o hook instalado é o que efetivamente governa esta auditoria, é uma verificação que o auditor não consegue fazer sobre si mesmo.
- **Comportamento real de detecção do `check_security_constraints.py` dentro de `.claude/` e `user-scope/`** — confirmei apenas que o walker alcança esses diretórios; não pude plantar probe (sem escrita).
- **Se os 4 falsos bloqueios declarados + o que encontrei esgotam o conjunto.** Encontrei o quinto por acaso, tentando ler um arquivo permitido. Não tenho como enumerar o complemento de uma lista; é precisamente o argumento do H1.

---

**Resumo.** A metade de escrita do item 4 está sólida e bem provada — 0 escapes, com o eixo de substituição finalmente definido por propriedade. A metade de leitura não está, e o número publicado na DoD (`4 falsos bloqueios`) é desmentido por um caso que levei menos de dez minutos para encontrar. Somado a isso, a correção central do commit candidato anterior não tem probe. O veredito FAIL coincide com a autoavaliação da própria fase (item 4 ⚠️, item 15 ⛔) — a diferença é que agora há um quinto caso medido, e ele mostra que o defeito não é a lista estar incompleta, é ainda haver lista.
