# Auditoria de checkpoint - 2026-08-22T17:17:28.070463+00:00

- fase: **6**
- commit auditado: `21e23e77f1fae19f039d071bb9cf0b8ac1c5e5af`
- session_id: `819a17c1-6264-4fd9-8dcd-4d17789814e1`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have what I need. Writing the report.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

Guarda de base: **PORTA** (P3-7, verbatim do lançador). A auditoria vale como gate.

Suíte real, medida neste worktree sobre `21e23e77f1fa`:
`Ran 701 tests in 62.254s` → `FAILED (failures=7, errors=49)`. Zero skips.

## FINDINGS

### BLOCKER

- **[B1] A guarda de boot do emissor lê `Superficie` como se fosse `dict`; a `academus-api` não sobe**
  Arquivo: `domains/academus/api/app.py:310` (função `confere_emissor_declarado`, 294–318), chamada em `app.py:346` dentro de `montar`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 6, item 1 da DoD; `06_ACCEPTANCE_TESTS.md` T9 (1º critério); `00_MASTER_SPEC.md` §5.5
  Evidência: `autenticacao.superficie` é o `Superficie` de `domains/academus/api/surface.py:89-96` — `@dataclass(frozen=True, slots=True)`, sem `.get`. A linha 310 faz `superficie.get("rotas")`. Traceback idêntico em 49 testes:
  `AttributeError: 'Superficie' object has no attribute 'get'`
  em `test_api_degradacao` (18), `test_api_rbac` (16), `test_business_state_postgres` (4), `test_modelo_completo` (3), `test_queda_de_sessao` (5), `test_trilha_de_auditoria` (4). Os 7 `FAIL` restantes são o `mutation_harness` acusando `test_a_suite_esta_verde_sem_mutacao` — a âncora da prova negativa caiu junto.
  A função irmã `confere_flags_declaradas` (`domains/academus/api/degradacao.py:223`) acessa corretamente `superficie.rotas.values()`. As duas foram escritas contra formas diferentes do mesmo objeto.
  Por que é blocker: `montar()` levanta `AttributeError` sempre que `emissor is None`. Não é degradação — é o processo morrendo no boot. A suíte inteira das Fases 3 e 5 está vermelha, e o harness de mutação perdeu a âncora, o que anula todas as provas negativas que dependem dela.

- **[B2] A rota que declara `emite` nunca recebe emissor em produção — o item 1 da DoD não se realiza no caminho real**
  Arquivo: `domains/academus/api/processo.py:96-113` (chamada de `montar` sem `emissor=`); superfície em `domains/academus/api_surface.yaml:373-379`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 6, item 1 da DoD; `06_ACCEPTANCE_TESTS.md` T9 (1º critério); `09_EVENT_MODEL.md` §6
  Evidência: `api_surface.yaml:373-379` declara `GET /audit/grade-changes` com `status: implementada` e `emite: audit_query_performed`. A fábrica de produção `criar()` monta com `autenticacao=`, `repositorio=` e `degradador=` — e **nenhum** `emissor=`. Logo `emissor is None` no único caminho que o container executa.
  Isto é independente de B1: corrigido só o tipo, a guarda passa a fazer o que o próprio docstring dela promete (`app.py:314-318`) e **recusa o boot** com `RuntimeError`. Corrigido só o wiring, o `AttributeError` de B1 fica latente atrás do `return` antecipado da linha 306-307.
  Por que é blocker: o item 1 da DoD da fase — *"consultar a trilha com filtro de período emite `audit_query_performed`"* — é falso em produção nas duas direções. Ou a API não sobe, ou subiria muda. O verificador `check_hooks_com_emissor` declara essa cegueira ele próprio: *"O que isto NAO prova: que alguma ROTA chame o emissor"* (P6-7).

- **[B3] A stack de containers não sobe neste commit**
  Arquivo: evidência em `.aurora-provas-de-container.json` (SHA `21e23e77f1fa`, confere com o checkout); verificador `scripts/check_provas_de_container.py`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 4, itens 1 e 4 da DoD (gate de regressão permanente); `06_ACCEPTANCE_TESTS.md` T5
  Evidência: `python scripts/check_provas_de_container.py` → `rc=2`. No JSON: `pack rc=0` (o pacote materializou), `stack rc=1`, e as duas provas em `rc=125` — *"NAO EXECUTADA: a stack de containers nao subiu"*. A linha operativa da saída do `up`: `container aurora-provas-range-api exited (1)`.
  Por que é blocker: a correção do B1 da quinta rodada (commit `297401b`, materialização do pack) foi aplicada e **não** destravou a stack. Os itens 1 e 4 permanecem NÃO VERIFICADO, e a causa do `exit 1` do `range-api` não está capturada — o gravador registra a saída do `up`, mas não `docker compose logs` dos containers que morrem.

### HIGH

Nenhum além dos blockers acima.

### MEDIUM

- **[M1] O gravador de provas não captura log de container que morre**
  Arquivo: `scripts/grava_provas_de_container.py:123-143`, impressão em `scripts/check_provas_de_container.py:336-364`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 4, itens 1 e 4 (verificabilidade do gate)
  Evidência: o pacote imprime `pack`, `stack` e as duas provas. Com `stack rc=1` e `range-api exited (1)`, a saída entregue ao auditor não contém nenhuma linha do processo que falhou — o diagnóstico do B3 fica fora do alcance de quem julga. O próprio arquivo já reconhece a classe do problema para o caso `pack` (`check_provas_de_container.py:343-347`: *"a falha de materializacao chegaria ao auditor como um `up` que nao subiu, e a causa ficaria a dois passos"*). A mesma distância existe agora um degrau adiante.

### LOW

- **[L1] Vazamento de conexões psycopg na suíte**
  Arquivo: transversal; visível em toda execução de `tests/` com Postgres no ar
  Requisito violado: nenhum artigo da spec — registrado por afetar a auditabilidade
  Evidência: centenas de `ResourceWarning: <psycopg.Connection ...> was deleted while still open` intercaladas ao progresso da suíte. A saída de 701 testes passa de 99 kB e o sumário fica soterrado. Não altera veredito de teste; encarece a leitura de qualquer rodada futura.

## DoD — item a item

Fase 6, os dezessete itens de `07_IMPLEMENTATION_PHASES.md`.

| Item | Status | Evidência |
|---|---|---|
| 1 — trilha com filtro emite `audit_query_performed` | **FAIL** | B1 + B2. `processo.py:96` monta sem emissor; `api_surface.yaml:378` declara `emite`. Verde só em `test_api_emissao_pela_rota.py:89`, que injeta emissor sempre |
| 2 — cada par declaração × verificação disparável e testado | PASS | `test_metrics_declaracao.py`, `test_metrics_verificacao.py` verdes |
| 3 — desconto por **união**, nunca soma | PASS | `test_metrics_epoch.py` verde; 2 mutações plantadas e detectadas (registro §394-398) |
| 4 — nenhum evento de epoch `rehearsal` em cálculo | PASS | `test_metrics_epoch.py` verde |
| 5 — declaração não altera ground truth | PASS | `test_simulation_state.py::test_declaracao_NUNCA_move_flag`, seis declarações contra o estado padrão, com ramo plantado em `test_simulation_state_probes.py` |
| 6 — declaração prematura lista eventos incompatíveis | PASS | `test_aar_timeline.py` verde |
| 7 — pack sem `verification_predicates` não carrega | PASS | `test_pack_completo_materializado.py:181::test_completo_sem_verification_predicates_RECUSA` |
| 8 — `TTIV` por limiar de calibração | PASS | `test_metrics_calibracao.py` verde |
| 9 — Brier só no escopo revisado; lacuna em separado | PASS | `test_metrics_calibracao.py:78,93,182,188` |
| 10 — rubrica ausente ou divergente impede carga | PASS | `test_pack_loader.py:435,443` — as duas metades |
| 11 — divergência ≥ 2 pontos gera alerta | PASS | `test_aar_timeline.py:297-330`, com as bordas |
| 12 — nove siglas pelo critério de `00` §3.2 | PASS | `test_derivacao_das_nove_siglas.py:141-286`; 5 violações plantadas na tabela, 5 reprovadas |
| 13 — `metric_side` em todo `event_type`, quatro lados disjuntos | PASS | `contracts/events.schema.yaml:692-716`; `test_cobertura_dos_contratos.py` verde |
| 14 — folha de predicado obedece à conjunção de `09` §4.0 | PASS | `test_verificacao.py` verde; `check_api_surface.py` rc=0 |
| 15 — insumo tipado por lado, ponto único de montagem | PASS | `check_insumo_de_metrica.py` rc=0: três tipos próprios, montagem única em `range-core/metrics/insumo.py::monta` |
| 16 — união, `rehearsal` e start são cálculo do consumidor | PASS | `test_metrics_epoch.py`, `test_inject_engine.py:783` |
| 17 — `inject_fired` emite marcadores de start no payload | PASS | `test_inject_engine.py:783::test_todo_inject_fired_carrega_os_dois_marcadores` |

Gates de regressão de fases anteriores:

| Item | Status | Evidência |
|---|---|---|
| Fase 4, item 1 — DEMO ponta a ponta | **NÃO VERIFICADO** | B3. `check_provas_de_container.py` rc=2, prova `demo` rc=125 |
| Fase 4, item 4 — reinício do container restaura do store | **NÃO VERIFICADO** | B3. Prova `reinicio` rc=125 |
| Fase 5, M2 — seed completo | PASS | `check_prova_do_seed.py` rc=0, SHA `21e23e77f1fa` confere; 3.543.783 linhas, 141,7 s e 143,2 s contra orçamento de 300 s; 20 tabelas com SHA-256 igual |
| T1 — fronteira core/adapter e ausência de `objective_ids` | PASS | `check_core_boundary.py` rc=0, `check_event_envelope.py` rc=0 |
| T2 — literais de flag e `event_type`; codegen | PASS | `check_contract_literals.py` rc=0, `codegen.py --check` rc=0 |
| T15 — segurança transversal | PASS | `check_security_constraints.py` rc=0, `check_synthetic_data.py` rc=0, `check_gabarito_fora_do_git.py` rc=0 |

Demais verificadores executados, todos rc=0: `check_store_read_surface`, `check_core_contract_imports`, `check_fold_authority`, `check_gate_coverage`, `check_readme_atual`, `check_allowlist_do_auditor`, `check_api_surface`, `check_spec_examples`, `check_contract_examples`, `check_hooks_com_emissor`.

## TESTES QUE NÃO PROVAM O REQUISITO

- **`tests/test_api_emissao.py:37-50` — a guarda é provada contra um `dict` literal que produção nunca produz.**
  `SUPERFICIE_COM_EMISSAO` e `SUPERFICIE_SEM_EMISSAO` são dicionários escritos à mão. Os quatro testes de `GuardaDeBoot` (linhas 56, 63, 68, 77) chamam `confere_emissor_declarado` diretamente com eles e passam. O chamador real, `app.py:346`, passa um `Superficie`. É exatamente esta escolha de forma que deixou o B1 chegar ao commit candidato: o teste exercita `.get` sobre algo que tem `.get`, e a produção não tem. O teste mais verde da peça 3 é o que prova menos.

- **`tests/test_api_emissao_pela_rota.py:80-89` — o ramo negativo existe e nunca é invocado.**
  O helper `monta(self, linhas=(), com_emissor: bool = True)` constrói `emissor = Emissor(...) if com_emissor else None`. Nenhuma chamada em toda a suíte passa `com_emissor=False` (varredura: zero ocorrências). O parâmetro foi criado para o caso negativo e o caso negativo nunca rodou — e ele roda sobre o `Superficie` real, ou seja, teria acusado o B1 na primeira execução.

- **`tests/test_api_emissao.py:1-16` — o docstring reivindica mais do que a suíte cobre.**
  Afirma provar *"a guarda de boot recusa quando a superfície declara `emite` e não há emissor ligado"*. O que está provado é que uma função pura recusa um dicionário. A guarda, no boot, não recusa: ela quebra.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **A causa do `exit 1` do container `range-api`.** A evidência amarrada ao SHA registra `stack rc=1` e a linha de saída, mas não os logs do container. `docker` está fora da allowlist do auditor por desenho (P2-19), e `demo_fase4.py` / `prova_reinicio_de_container.py` não são executáveis por mim. Não posso afirmar que o B1 é a causa: o B1 está em `domains/academus/api/`, e o container que reportou saída não-zero é o `range-api`. Os dois podem ser o mesmo defeito ou dois; não distingo com o que tenho.
- **Os itens 1 e 4 da DoD da Fase 4.** Permanecem NÃO VERIFICADO, e é o que o próprio verificador determina. Não os marquei PASS por silêncio nem FAIL por inferência.
- **A medição do seed (M2 da Fase 5) e as provas de container.** Não as vi rodar. O que as amarra a este commit é o SHA gravado, conferido contra o do checkout — e no caso do seed ele confere. Isso é procedência, não observação.
- **Se a suíte fica verde com o B1 corrigido.** Os 49 erros compartilham um traceback, e os 7 `FAIL` do harness são consequência declarada da âncora vermelha. É a leitura mais provável, mas não a executei: corrigir não é meu papel, e um `montar` que passe a completar pode expor asserções que hoje nem chegam a rodar.
- **CI.** Não inspecionei execução de workflow. Um verde de CI é de outro commit até prova em contrário.
```

Resumo em uma linha: **FAIL** — a `academus-api` não sobe (`Superficie` lida como `dict`), a rota que declara `emite` nunca recebe emissor em produção, e a stack de containers não sobe; 49 erros e 7 falhas em 701 testes. Os quinze itens de DoD da Fase 6 que não dependem disso estão bem provados, inclusive os difíceis — partição de insumo, derivação das nove siglas e epoch como cálculo do consumidor.
