# Auditoria de checkpoint - 2026-08-18T06:47:49.662806+00:00

- fase: **5**
- commit auditado: `719d84a91d26f07751a282b62b45a366620dcb38`
- session_id: `cd97bc6e-3d3e-44de-bf40-967f71c7587e`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 5

## VEREDITO: FAIL

---

## FINDINGS

### BLOCKER

**[B1] O dataset da Linha B carrega o gabarito in-band: a classe de cada caso é legível na própria trilha, sem análise**

**Arquivo:** `domains/academus/seed/dataset.py:404, 418, 427, 439, 442, 450, 466, 479, 489, 507` (e o efeito em `domains/academus/seed/carga.py:96-100`)

**Requisito violado:** `02_DOMAIN_ACADEMUS.md` §6 e §6.2; `02_DOMAIN_ACADEMUS.md` §3; `00_MASTER_SPEC.md` §3 (camadas 1 e 2)

**Evidência — o que eu observei, não o que suponho:**

O gerador escreve prefixos que nomeiam o conjunto dentro de colunas de negócio da trilha e das autorizações:

```
dataset.py:404   f"g-ind-{i:03d}"   -> audit_trail.object_id   (22 indevidos comprovados)
dataset.py:427   f"g-amb-{i:03d}"   -> audit_trail.object_id   (11 ambíguos)
dataset.py:450   f"g-sus-{i:03d}"   -> audit_trail.object_id   (34 legítimos suspeitos)
dataset.py:466   f"g-mnt-{i:03d}"   -> audit_trail.object_id   (60 de ruído)
dataset.py:489   f"g-del-{i:03d}"   -> audit_trail.object_id   (18 delegadas)
dataset.py:507   f"g-nrm-{i:05d}"   -> audit_trail.object_id   (legítimos normais)
dataset.py:418   f"AUT-AMB-{i:03d}" -> rectification_authorizations.authorization_id
dataset.py:439   f"AUT-SUS-{i:03d}" -> rectification_authorizations.authorization_id
dataset.py:442   f"PR-SUS-{i:03d}"  -> rectification_authorizations.process_number
dataset.py:479   f"PR-DEL-{i:03d}"  -> access_delegations.process_number
```

O mapeamento posicional está confirmado: `dataset.py:559` põe `objeto` em `object_id` de `trilha.Registro`, e `carga.py:96-100` grava a tupla de `_encadeia` (`dataset.py:566-569`) nas colunas `(... object_type, object_id, payload ...)`.

Segunda instância do mesmo defeito, na mesma trilha — o par de notas é **constante por conjunto** (`dataset.py:406-407, 428, 451, 467, 490, 508`):

| conjunto | `previous_value` → `new_value` |
|---|---|
| ambíguos (11) | 5.0 → 6.5 |
| suspeitos (34) | 5.5 → 7.0 |
| ruído (60) | 7.0 → 7.0 |
| delegadas (18) | 6.0 → 6.5 |
| normais | 6.0 → 7.0 |

Um `GROUP BY (previous_value, new_value)` particiona cinco dos seis conjuntos; os indevidos são o único conjunto com valores variáveis, logo saem por eliminação.

Contraste que fecha a evidência: no caminho **real** da aplicação, `domains/academus/api/repositorio.py:219` grava `object_id=str(registro.grade_id)` — um id numérico. As linhas semeadas não têm sequer a mesma forma das linhas que a aplicação escreve.

**Por que é blocker:**

`02` §6 define o exercício: *"Encontrar 40 eventos estranhos é trivial depois que alguém abre o audit log. O exercício não é esse. O exercício é demonstrar, com confiança declarada, quais eventos são indevidos."* Com estes rótulos, `SELECT * FROM audit_trail WHERE object_id LIKE 'g-ind-%'` devolve exatamente os 22, e `LIKE 'g-sus-%'` devolve os 34 que existem para induzir overconfidence. A calibração de `02` §6.2 — que é o produto pedagógico da Linha B e o insumo do Brier de `03` §5 — deixa de medir julgamento e passa a medir se alguém leu a coluna.

É literalmente o modo de falha que `02` §3 nomeia: *"Sem ela, 'fora da janela = fraude' e o exercício vira busca por filtro `WHERE within_window = false`."* Aqui o filtro é mais direto ainda.

E é a confusão de camadas de `00_MASTER_SPEC.md` §3: a classificação é **ground truth** (camada 1, `line_b_cases.set` em `ground_truth.yaml`), e ela está materializada dentro da **evidência observável** (camada 2), que é o artefato que o participante investiga por `02` §4 e §7.

A própria fase estabeleceu este padrão de severidade: o terceiro vazamento que ela fechou (`dataset.py:516-532`, ordem em bloco da trilha) é o mesmo defeito, com o argumento escrito no código — *"Quem lesse este repositório público saberia ler o gabarito na PRÓPRIA TRILHA, que é o artefato que o participante investiga"*. Este é pior: não exige ler o repositório, basta olhar a coluna.

Nenhum teste da fase cobre esta direção. `test_seed_determinista.py` e `test_gabarito.py` verificam contagem, disjunção, cobertura, assinaturas de `02` §6.1 e identificadores no `GM_NOTES` — nenhum pergunta se a linha da trilha diz a que conjunto pertence.

---

### HIGH

**[H1] `check_gabarito_fora_do_git.py` declara cobrir a forma `g-ind-000` e não a vê no arquivo que a produz**

**Arquivo:** `scripts/check_gabarito_fora_do_git.py:113-117` (regex) e `:95` (`MODULOS`)

**Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §6; `00_MASTER_SPEC.md` §5.10

**Evidência:** a direção (c) varre `dataset.py` (está em `MODULOS`, linha 95) com `IDENTIFICADORES`, cujo comentário nomeia o alvo explicitamente:

```
#:   DEL-000      delegacao                g-ind-000    objeto da trilha
IDENTIFICADORES = re.compile(
    r"\b(?:GC-[0-9]+|GT-[A-Z0-9]+-[0-9]+|U-P-[0-9]+|A-[0-9]{4,}"
    r"|AUT-[A-Z]+-[0-9]+|PR-[A-Z]+-[0-9]+|DEL-[0-9]+|g-[a-z]{3}-[0-9]+)\b"
)
```

O fonte contém `f"g-ind-{i:03d}"`, `f"AUT-AMB-{i:03d}"` e `f"PR-SUS-{i:03d}"`. Os três casam o prefixo e falham no sufixo numérico, porque os dígitos vêm de placeholder de f-string. `python scripts/check_gabarito_fora_do_git.py` saiu 0 nesta árvore, e as 15 sondas de `check_gabarito_fora_do_git_probes.py` passaram — nenhuma delas planta um identificador construído por f-string.

**Por que é HIGH e não blocker:** é o mecanismo que deixou B1 passar, não o dano em si. Mas o verificador afirma no cabeçalho que a direção (c) *"fecha o buraco que a (a) não vê"*, e ela não fecha para a forma exata que o próprio comentário cita — a mesma classe de "desenho declarado divergindo do mecanismo implementado" que este repositório já puniu em auditorias anteriores.

---

### MEDIUM

**[M1] `06` T8, quarto critério, atribui a recusa a um linter que não existe nesta fase**

**Arquivo:** `tests/test_gabarito.py:91-109`; `domains/academus/seed/gabarito.py:230-240`

**Requisito violado:** `06_ACCEPTANCE_TESTS.md` T8, 4º bullet — *"Todo fato mencionado em `GM_NOTES.md` existe em `ground_truth.yaml`. **Divergência é recusada pelo linter**."*

**Evidência:** existe `fatos_citados()` e um teste que compara os conjuntos. Não há linter que recuse. O registro (`docs/progress/fase_5.md:389`) remete a `07` Fase 7 (*"Fato citado no `GM_NOTES.md` e ausente do `ground_truth.yaml` é recusado"*), mas T8 está rotulado **Fase 5** em `06`, e a fase não escalou o conflito nem o registrou como item inviável.

Agravante de forma: o teste é quase tautológico por construção — `_renderiza` (`gabarito.py:189-192`) monta `TABELA_DE_CASOS` a partir da mesma lista `casos` de onde saem os `declarados`, então `citados ⊆ declarados` só pode falhar se alguém escrever um identificador à mão no template. O `assertGreaterEqual(len(citados), 22)` evita vacuidade, mas não transforma a asserção em prova da propriedade que T8 nomeia.

---

### LOW

**[L1] O seed não gera senha nenhuma, e `05` §8 fala em senha de seed derivada do `RANDOM_SEED`**

**Arquivo:** `domains/academus/seed/dataset.py:214, 220, 222` (`password_hash = None` para todos os usuários); `domains/academus/models/registros.py:141-142, 156`

**Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §8, último parágrafo

**Evidência:** as três formas de `usuarios.append` gravam `None` na posição de `password_hash` (`carga.py:54` confirma a coluna). O docstring do modelo cita `05` §8 como razão da coluna, e nada a preenche. Não há hoje rota de troca senha→token no `academus-api`, então não há valor trivial reutilizável exposto — a propriedade vale por vacuidade. Fica registrado porque a fase entrega o modelo de usuários e o item não tem destinatário declarado em `docs/progress/fase_5.md` §8.

**[L2] `_posicao` recupera a posição da quebra por varredura de dígitos na mensagem**

**Arquivo:** `domains/academus/audit/trilha.py:316-325`

**Requisito violado:** nenhum — não é finding de spec. Observação de fragilidade: a função devolve o primeiro token numérico de `str(ChainBroken)`. Funciona para as três mensagens atuais de `range-core/events/integrity.py:105-121` e os testes confirmam, mas qualquer número inserido antes da sequência numa mensagem futura muda a posição reportada em silêncio. O trade-off está declarado no docstring. Registro como LOW, não como exigência.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Seed completo em < 5 min | **NÃO VERIFICADO** | `docs/progress/fase_5.md:1448` registra 144,3 s / 300 s, 3.543.783 linhas, com máquina, python e data gerados por código em `scripts/prova_seed_completo.py`. O script está fora da allowlist por decisão declarada (`readonly_bash.py:270-285`) e eu não o executei. O caminho de geração+carga roda na suíte em escala reduzida. |
| 2 — Mesmo `RANDOM_SEED` → dataset byte-idêntico | **PASS** | `tests/test_seed_determinista.py::Determinismo`, 3 testes verdes contra Postgres. `carga.dump_canonico` é SHA-256 real sobre `COPY ... TO STDOUT` ordenado das 20 tabelas (`carga.py:191-218`). A direção inversa (seeds distintos → dumps distintos) e a guarda de relógio por AST estão presentes. |
| 3 — `UPDATE`/`DELETE` falham por trigger **e** por permissão de role | **PASS, com limite declarado** | `TrilhaEhAppendOnly`, 4 testes verdes. Trigger e `has_table_privilege` provados **separadamente**, mais o par que impede vacuidade (a role possui `INSERT`+`SELECT`). Migration `0004:190-255`. Limite P5-3 (a role que conecta não é `academus_app`) exercido por `OQueT7NaoProva`, não apenas escrito. |
| 4 — `GET /audit/verify-chain` detecta adulteração induzida | **PASS** | `CadeiaDetectaAdulteracao`, 5 testes verdes: campo alterado, `payload` alterado, linha removida do meio — cada um com a `sequence` exata; mais o limite do truncamento de cauda afirmado. `RotaDeVerificacao` cobre 200/401/403 e forma estável da resposta pelo stack ASGI. |
| 5 — Os seis conjuntos da Linha B nos volumes especificados | **FAIL** | Volumes, disjunção e cobertura provados por `SeisConjuntosDaLinhaB` lendo a tabela de `02` §6.1 (não as constantes), e `check_volumes_da_linha_b.py` cruza spec × gerador nas 4 direções com 8 sondas. **Mas os conjuntos entregues carregam a própria classificação nas colunas — B1.** Existir nos volumes certos não basta quando o dado diz a que conjunto pertence. |
| 6 — `GM_NOTES.md` contém a query de referência que separa indevidos de ambíguos | **PASS** | `test_gabarito.py` — o CI **produz** o artefato e o julga; a query não é só citada, é **executada** e devolve 22 com interseção vazia com os ambíguos. |

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`test_todo_fato_citado_no_GM_NOTES_existe_no_ground_truth`** (`tests/test_gabarito.py:91`) — quase tautológico: `TABELA_DE_CASOS` é renderizada a partir da mesma lista de onde saem os `declarados` (`gabarito.py:186-192`), então a asserção só pode falhar por identificador escrito à mão no template. Ver M1.

2. **`test_a_query_de_referencia_devolve_os_22_e_nenhum_ambiguo`** (`tests/test_seed_determinista.py:231`) — afere contra `dataset.INDEVIDOS`, isto é, contra o gerador. Não é o defeito que o M2 anterior descreve porque `check_volumes_da_linha_b.py` amarra essa constante a `02` §6.1 em CI, e o teste irmão (`test_os_volumes_sao_os_de_02_secao_6_1`) lê a spec. Registrado por completude, não como finding.

3. **A ausência que importa mais** — nenhum teste da fase pergunta se uma linha da trilha revela, por si, a que conjunto pertence. Toda a bateria de anti-vazamento (`test_nenhum_identificador_QUE_APONTA_PARA_LINHA_sobrevive`, `test_o_MAPEAMENTO_caso_para_fato_muda_com_o_seed`) olha para o **`GM_NOTES` renderizado** e para o **repositório**, e nunca para o **dado semeado**. É a lacuna por onde B1 passou verde em 387 testes.

Fora isso, a suíte é forte e nos eixos certos: T7 separa trigger de role em vez de fundir os dois num "tentei e falhou"; a adulteração é feita de verdade (trigger desabilitado) em vez de simulada; o determinismo tem a direção inversa; os limites declarados (`truncamento da cauda`, `OQueT7NaoProva`) são testes que ficam vermelhos quando deixarem de valer. Nada marcado `skip` ou `xfail`.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Item 1 da DoD (< 5 min em escala completa).** `scripts/prova_seed_completo.py` está fora da allowlist do auditor por decisão registrada. Eu li que máquina, data e stack são gerados por código ao lado do número (o que `06` T3 exige), e não confirmei o número.
- **As provas de container (P4-10).** `python scripts/check_provas_de_container.py` saiu 0 e amarrou as duas provas a `719d84a9`. Eu não as vi rodar; o que verifiquei é o SHA.
- **O comportamento do banco em produção.** Os testes de T7 rodaram contra o Postgres efêmero desta sessão. `REVOKE` e trigger não alcançam dono nem superusuário — limite declarado na migration `0004:20-39` e exercido em teste, mas a configuração real de deploy (P5-3) é do operador e não está nesta árvore.
- **O artefato gerado com o `RANDOM_SEED` de produção.** Por desenho, `scenarios/` está fora do Git e nenhum CI o vê. Verifiquei a propriedade do gerador, não o artefato da sala. Observo que **B1 é propriedade do gerador e independe do valor do seed** — os prefixos são literais no código versionado.
- **Escala completa da partição da Linha B.** A disjunção e a cobertura foram exercidas em `ESCALA_REDUZIDA`. O limite (L1) está declarado em `docs/progress/fase_5.md:1452`.
- **A execução do CI.** Li `.github/workflows/invariants.yml` e confirmei que os cinco verificadores novos e suas sondas estão cabeados; não observei um run do GitHub Actions.

---

**Qualquer BLOCKER ⇒ FAIL.** B1 é um único defeito, com correção mecânica óbvia — mas ele atinge exatamente o produto que `02` §6 chama de "o produto de verdade" desta fase, e passou incólume por 387 testes e por um verificador que nomeia a forma vazada no próprio comentário.
