# Auditoria de checkpoint - 2026-09-23T19:10:36.720725+00:00

- fase: **9**
- commit auditado: `36e139cecb6c0f18663e01d5ff1d215a48f07aa0`
- session_id: `f9bfe2da-b1f0-40a7-95c7-082a27d40b1f`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 9

## VEREDITO: FAIL

Objeto: commit `36e139cecb6c0f18663e01d5ff1d215a48f07aa0`, árvore `78af71196260c4f5ccc1c7299ad04db5118735f6`, working tree limpo. Base `c77120c` é ancestral de HEAD e está em `docs/process/phase_anchors.tsv:9`. Guarda de base: **PORTA**, verbatim do lançador. 41 commits, 55 arquivos.

**O que rodei, com as contagens:**
- Suíte completa com o venv da auditoria, Postgres e Redis no ar: **1083 testes, OK, zero pulos.**
- Os seis verificadores de `tools/`, incluindo `codegen.py --check`: **todos rc=0.**
- Verificadores de `scripts/`, todos rc=0: `check_core_contract_imports`, `check_store_read_surface`, `check_fold_authority`, `check_insumo_de_metrica`, `check_gabarito_fora_do_git`, `check_contract_examples` (13 positivos, 97 negativos), `check_banner_de_simulacao` e suas probes (20 OK), `check_secoes_de_seguranca` (8/8), `check_progress_consistency`, `check_readme_atual`, `check_regras_do_linter` (10 regras), `check_spec_examples`, `check_prova_do_exercicio_4h_probes` (12 OK).
- `phase0_negative_tests`: "Todos os seis verificadores falharam contra probes independentes", 112 provas de invariante.

**Provas do lançador, conferidas nesta árvore:**
- `check_provas_de_container.py` → rc=0. As provas `demo` e `reinicio` batem com `78af71196260`.
- `check_prova_do_seed.py` → rc=0. 3.543.783 linhas; 141,8 s e 175,3 s contra 300 s; 20 tabelas com SHA-256 igual.
- `check_prova_do_exercicio_4h.py` → rc=0. Item 7 = **2,750 s**, com 96.650 eventos a 400/min; os 7 arquivos do pack conferem.

## FINDINGS

### BLOCKER

- **[B1] A evidência entregue ao time azul carrega o veredito do gabarito: `credential=compromised` e a classe do fato como assinatura CEF.**
  - **Arquivos:**
    - `domains/academus/evidence_generators/vpn.py:49` escreve `credential={credential_state}`.
    - `domains/academus/evidence_generators/identity_audit.py:37` escreve o campo `credential_state`.
    - `domains/academus/evidence_generators/cef.py:78` usa `fact_class` como `signature`, e `:97` põe `credential_state` em `cs1`.
    - A origem é `domains/academus/seed/linha_a.py:145`: `"credential_state": "compromised"` no fato `initial_access`, que projeta em `[vpn, identity_audit, cef]` (`:147`).
  - **Requisito violado:**
    - `00_MASTER_SPEC.md` §3: ground truth (o que ocorreu) é distinto de evidência observável (o que pode ser descoberto).
    - `09_EVENT_MODEL.md` §2: `observable_evidence` é "o que o ambiente permite descobrir".
    - `08_EVIDENCE_SIMULATOR.md` §3: o `vpn.log` carrega "autenticação sem MFA, horário anômalo, geolocalização inconsistente". Estado de comprometimento não está nessa lista.
  - **Evidência:**
    - Leitura dos três geradores e do fato. O formato de fio está confirmado no arquivo versionado `tests/fixtures/pack_exemplo/evidence/vpn.log:2`: `... mfa=- credential=-`. O campo sai em toda linha do `vpn.log`, e o fato real grava `credential=compromised`.
    - O próprio implementador classifica esse atributo como gabarito: `precursor.py:42-43` diz *"SEM `actor` e SEM `credential_state` … atribuição é o achado, não o insumo"*.
    - `vpn.py:11-15` recusa `suspicious=true` porque "entregaria a resposta e misturaria as camadas de `00` §3". Três fontes fazem exatamente isso com outro nome.
    - O CEF rotula as linhas `initial_access` e `exfiltration`, que são a classificação do gabarito na kill chain, não um sinal de SIEM.
    - Nenhum teste guarda contra isso: `grep compromised|credential` em `tests/test_evidence_generators.py` não devolve nenhuma asserção negativa.
  - **Por que é blocker:** o fato declara `discoverability.requires: "correlacionar horario fora de expediente com ausencia de MFA"` (`linha_a.py:150`). A segunda linha do `vpn.log` entrega a conclusão pronta. Evidência observável passa a afirmar ground truth, que é a confusão de camadas que `00` §3 diz invalidar o AAR. Toda medição de detecção sobre a Linha A passaria a medir leitura de rótulo.

### HIGH

- **[H1] "Um contrato só" é falso: o `cef.log` e o `telemetry_emitted` projetam o mesmo fato com vocabulário, severidade e campos divergentes.**
  - **Arquivos:**
    - O arquivo: `cef.py:78` (signature = `fact_class`), `:88` (severidade fixa 5), `:97-98` (`cs1=credential_state`, `cs2=mfa`), sem `outcome`.
    - O evento: `range-core/telemetry/forwarder.py:104-112` usa a assinatura, a severidade e o `outcome` de `domains/academus/telemetry_events.yaml`. Por exemplo, `initial_access` vira `SERVICE_ACCOUNT_ANOMALY` com severidade 7 (`:46-49`).
    - `forwarder.py:60-63` diz que usar `cs1`–`cs4` para outra coisa é indevido, e `cef.py` faz isso.
  - **Requisito violado:**
    - `08` §1: "contradição entre fontes torna-se estruturalmente impossível".
    - `08` §2: "unifica evidence-simulator e telemetry-forwarder sob um contrato só".
    - `02` §10: as doze assinaturas, e `cs1`–`cs4` reservados a contexto de domínio.
    - `07` §Fase 9, item 4.
  - **Evidência:**
    - São dois caminhos de código em camadas diferentes, cada um com o seu mapeamento. Para o mesmo fato, o SIEM de arquivo diz `initial_access|…|5` e o event store diz `SERVICE_ACCOUNT_ANOMALY`, severidade 7, `outcome=success`.
    - O teste do item 4 (`tests/test_telemetry_forwarder.py:159-186`) compara só `src` e `suser`, e localiza a linha CEF pela `fact_class`. Ou seja, ele depende da divergência para funcionar.
    - O registro afirma "um gerador só" (`docs/progress/fase_9.md:436`).

- **[H2] `evidence verify` não confere a cobertura por fato do manifesto, e o teste "DIRIGIDA_POR_FATO" nunca chama `conferir`.**
  - **Arquivos:**
    - `range-core/evidence/build.py:186-199` compara só os **nomes de arquivo**. `projects_facts` e `format` do `MANIFEST.json` nunca são lidos depois da validação de schema.
    - `tests/test_evidence_build.py:243-253` (`test_a_conferencia_e_DIRIGIDA_POR_FATO`) inspeciona `self.manifesto`, a saída do build, e não invoca `_conferir()`.
    - `contracts/evidence.schema.yaml`, bloco `x-aurora-linter-rules`: a regra de `format` declara como mecanismo `projetar (FonteSemFormato)`, mas quem lê o manifesto é o `verify`, e ele não confere esse campo.
  - **Requisito violado:** `07` §Fase 9, item 2 ("valida consistência fato → projeções, dirigido por fato"); `06` T13, 1º critério; `08` §7 (o manifesto carrega "os `fact_id` que projeta… permite ao teste verificar cobertura").
  - **Evidência:** leitura integral de `conferir`. Um `MANIFEST.json` com `projects_facts` ou `format` adulterado, e arquivos intactos, passa por todos os degraus: schema, `ground_truth_hash`, conjunto de arquivos, `sha256` e reprojeção. O `fact_id` só existe no manifesto, porque as fontes não o carregam (`05` §6). É justamente a amarração por fato que fica sem verificação.

- **[H3] O registro declara para o item 7 um número e uma margem que a prova gravada desmente — reincidência do H3 da 1ª rodada.**
  - **Arquivos:**
    - `docs/progress/fase_9.md:439` diz "**≈ 2 s** … Cinco execuções entre 1,73 e 2,15 s. **O número é o da prova gravada**".
    - `:260` diz "folga ≥ 28%"; `:286-289` diz que "cruzar os 3 s exigiria um desvio de 0,85 s a partir do pior caso observado".
    - `.aurora-prova-do-exercicio-4h.json:33` grava `total_s: 2.7496744000236504`.
  - **Requisito violado:** R2 §1; `06` T13 (último critério), que o registro diz decidir pela prova.
  - **Evidência:** `check_prova_do_exercicio_4h.py` imprime "item 7 2.750 s". A folga real é de 8%, o valor fica 0,60 s acima do "pior caso" declarado, e cai **dentro** da faixa de 500/min (2,626–3,084 s) que a §2.8 rejeitou por "não ter margem reprodutível". O critério < 3 s passa. O argumento de método que escolheu 400/min, que é o que a §2.8 põe no centro, é falsificado pelo artefato que o sustenta.

- **[H4] O 1º critério de T13 (usuário, IP e timestamp mutuamente consistentes, dirigido por fato) não tem teste que o prove.**
  - **Arquivos:**
    - `tests/test_evidence_generators.py:164-170` e `:278-285` usam `linha_a.facts(SEED)`, ou seja, são dirigidos por seed, e fazem substring sobre o arquivo inteiro.
    - Nenhum teste compara `exercise_time` entre as projeções de um mesmo fato.
    - `tests/test_evidence_elenco.py:269-278` afirma `assertIn("svc_academus", linha)` sobre uma string literal escrita no próprio teste.
  - **Requisito violado:** `06` T13, 1º critério ("Teste dirigido por fato, não por seed").
  - **Evidência:** grep por `consisten|timestamp` em `tests/test_evidence_*.py` só encontra docstrings. Os testes "por fato" de `test_evidence_elenco` não executam gerador.

### MEDIUM

- **[M1] O `evidence/` versionado do pack de exemplo reprova `evidence verify` em checkout Windows.**
  - **Arquivo:** `tests/fixtures/pack_exemplo/evidence/{vpn.log,MANIFEST.json}`, sem entrada em `.gitattributes`.
  - **Requisito violado:** R7 §1/§2, R2 §2; o comentário do passo em `.github/workflows/invariants.yml:700-712`.
  - **Evidência:** `range-cli evidence verify tests/fixtures/pack_exemplo` sai com **rc=2** ("vpn.log: … sha256 7d828c… contra 071f27…"). `git ls-files --eol` mostra `i/lf w/crlf attr/`. Diagnóstico de causa: é CRLF de checkout, não o gerador. O gate de CI (Linux) passa; na plataforma do operador, o mesmo comando acusa uma edição manual que não houve.

- **[M2] "Nenhum gerador inventa entidade" só tem guarda para IP, e o texto afirma que o `verify` fecha a lacuna.**
  - **Arquivos:** `range-core/evidence/build.py:19-23` e `range-core/evidence/elenco.py:45-47` dizem que "a reprojeção pega o ator inventado". `domains/academus/evidence_generators/email.py:63-64` tem fallbacks inventados (`"ti"`, `"usuario"`).
  - **Requisito violado:** `07` §Fase 9, item 1; `08` §2.
  - **Evidência:** a reprojeção executa o **mesmo** gerador. Um ator inventado deterministicamente se reproduz byte a byte, e `conferir` devolve lista vazia. A afirmação é falsa por construção, e `test_nenhum_gerador_inventa_entidade` (`test_evidence_generators.py:370-376`) cobre só o que `EntidadeInventada` vê, que é endereço IP.

- **[M3] `delivery_mode` do manifesto é sempre `pre_positioned`: o `evidence_release` do pack nunca chega a ele.**
  - **Arquivos:** `range_cli/cli.py`, `_evidence_build`, chama `construir` sem `entrega`/`atrasos`; `range-core/evidence/manifesto.py:93`.
  - **Requisito violado:** `08` §5 e §7 ("modo de entrega" no manifesto); `04` §3 (`evidence_release` em inject).
  - **Evidência:** leitura do CLI. Para todo pack com `evidence_release`, o manifesto diz ao facilitador que a fonte está disponível desde o start, o que é falso.

- **[M4] Nenhum código de produto emite `telemetry_emitted` no event store.**
  - **Arquivos:** `range-core/telemetry/forwarder.py:195` (`self.emissor(programado.payload)`, com o payload cru e sem envelope).
  - **Requisito violado:** `07` §Fase 9, nota do item 7 ("vai para o event store como qualquer outro"); `01` §4, Telemetry State ("Log + event store"); a "segunda metade" do item 4 que o registro reivindica em `fase_9.md:436`.
  - **Evidência:** grep por `TELEMETRY_EMITTED|Replay(` fora de `tests/` e `scripts/medida_*` não devolve nada. `Replay` só é instanciado com `list.append`, e o binding de contrato da M2 anterior não tem emissor real que o exercite.

### LOW

- **[L1] `phishing_delivery` vira `AUTH_FAIL` com `outcome: failure`**, com o comentário "gateway recusou o remetente forjado" (`telemetry_events.yaml:72-75`). Enquanto isso, `email.eml` mostra a mensagem entregue à conta que depois é comprometida. É uma contradição entre fontes do mesmo fato (`08` §1).
- **[L2] O README diz "medido em 22/09/2026, no fechamento da Fase 9"** (`README.md:123`), mas `fase_9.md:3` declara a fase **ABERTA** e `check_readme_atual` informa "última fase concluída: 8".

**Achados órfãos da rodada anterior:** nenhum. M1–M4 e L1–L2 da 1ª auditoria têm disposição em `fase_9.md` §2.9, e a verificação confirma M1, M2, L1 e L2 na árvore. O H3 não é órfão: ele foi reportado como corrigido e reincide, e aparece acima como H3.

## DoD — item a item
| Item | Status | Evidência |
|---|---|---|
| 1 — toda fonte é projeção de `fact_id`; nenhum gerador inventa entidade | **PARCIAL** | As fontes derivam dos fatos (`projecao.py:238-239`); `EntidadeInventada` cobre IP. A guarda de ator/destino não existe, e a alegação de que o `verify` a cobre é falsa (M2). As suítes `test_evidence_elenco` (18+2), `test_evidence_projecao` (29+2) e `test_evidence_generators` (28+2) passam. |
| 2 — `evidence verify` dirigido por fato | **PARCIAL** | A reprojeção funciona e o `verify` não escreve (`test_evidence_build`, 28+2). `projects_facts`/`format` do manifesto não são conferidos, e o teste do nome não chama `conferir` (H2). |
| 3 — `precursor_events.jsonl` reproduzível; edição detectada por hash | **PASS** | `test_evidence_build.py:263-303`; `build.py:209-224` com mensagem de hash distinta da de conluio. |
| 4 — telemetria CEF é projeção | **PARCIAL** | As duas saídas derivam do fato, mas divergem entre si (H1), e nenhum produto emite o evento (M4). |
| 5 — sem anexo, binário, IOC real, domínio roteável | **PASS** | `projetar` → `IOCEncontrado` com tokenização que corta `=`; `.eml` sem multipart; `check_synthetic_data` e `check_banner_de_simulacao` rc=0. O banner fica na 1ª linha (`banner.py:118-133`). B1 não é IOC, é gabarito. |
| 6 — replay respeita o clock de exercício | **PASS (unitário)** | `forwarder.py:187` só lê `elapsed_seconds()`; 7 casos com dublê. Não há integração com `ExerciseClock` nem com o store (M4). |
| 7 — reconstrução < 3 s com `telemetry_emitted` no volume de 4 h | **PASS** | A prova amarrada à árvore dá 2,750 s < 3 s. A sustentação escrita no registro está errada (H3). |

## TESTES QUE NÃO PROVAM O REQUISITO
1. `tests/test_evidence_build.py:243-253`, `test_a_conferencia_e_DIRIGIDA_POR_FATO`: não invoca `conferir`; testa a saída do build (H2).
2. `tests/test_evidence_elenco.py:269-278`, `test_o_elenco_responde_por_ator_e_destino_pela_PRESENCA`: `assertIn` sobre uma literal do próprio teste; não há projeção envolvida (H4).
3. `tests/test_telemetry_forwarder.py:147-186`: a "prova de valor" do item 4 compara só `src`/`suser`, e acha a linha CEF pela `fact_class`, então codifica a divergência de assinatura em vez de detectá-la (H1).
4. `tests/test_evidence_generators.py:370-376`, `test_nenhum_gerador_inventa_entidade`: cobre só IP, e o docstring alega o item 1 inteiro (M2).
5. `tests/test_evidence_generators.py:164-170`, `:278-285`: consistência dirigida por seed e por substring do arquivo inteiro, sem timestamp (H4).
6. `tests/test_telemetry_forwarder.py:286-293` (pausa): exercita o dublê, que já não avança pausado. Mantenho como limite de instrumento, como na 1ª rodada.

## O QUE EU NÃO CONSEGUI VERIFICAR
- **As medições do lançador** (container, seed, 4 h): não as vi rodar. Conferi só a amarração pela árvore `78af7119…` e li a saída que os verificadores imprimem. A prova do seed declara python 3.14.7; registro sem julgar.
- **O arquivo de evidência realmente gerado para `ransomware-universidade`:** `evidence build` escreve, e não é permitido ao auditor. B1 se apoia na leitura dos geradores, do fato em `linha_a.py` e no formato confirmado em `tests/fixtures/pack_exemplo/evidence/vpn.log`, não num `vpn.log` do pack aberto.
- **A projeção dos fatos da Linha B em `database_audit`:** depende de gabarito gerado a partir do banco, e não o materializei.
- **O CI Linux sobre este commit:** M1 afirma o comportamento no checkout Windows; no Linux, a leitura do código e dos atributos indica que o passo passa, mas não o executei.
- **`bash .claude/verify/run.sh`:** fora da allowlist do auditor, não executado. Os verificadores foram rodados individualmente, como listado acima.
