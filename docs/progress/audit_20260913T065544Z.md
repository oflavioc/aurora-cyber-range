# Auditoria de checkpoint - 2026-09-13T06:55:44.156905+00:00

- fase: **7**
- commit auditado: `043c33cdeb01b8fbea47cd945859a172c271128b`
- session_id: `84be6e1d-58f9-4604-9272-d66b9c5efe2a`
- modo: `headless`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 7

## VEREDITO: FAIL

Base de comparação: `22f5f49bfa30ca3fd32911a4749b41063b8d1ac4` (fornecida pelo lançador). `HEAD` = `043c33cdeb01b8fbea47cd945859a172c271128b`, árvore `8311d748188dc33cecbeee3db6fb40258a8b5836`. Âncora gravada (`docs/process/phase_anchors.tsv:49`) e coerente com a base — a auditoria é **porta**, conforme o veredito verbatim do lançador.

**Gates executados nesta rodada (R2 §1):**

| Gate | Resultado |
|---|---|
| `python -m unittest discover -s tests` (venv do lançador) | **856 testes, OK**, 0 falhas, **0 skips** — os testes de serviço rodaram |
| `scripts/check_provas_de_container.py` | **rc=2 — REPROVOU** (saída íntegra lida; causa real, ver B1) |
| `scripts/check_prova_do_seed.py` | rc=0 — árvore `8311d748188d` confere; 3.543.783 linhas, 140,9 s / 142,2 s < 300 s |
| `tools/check_core_boundary` · `check_event_envelope` · `check_contract_literals` · `check_security_constraints` · `check_synthetic_data` · `codegen.py --check` | rc=0 (6/6) |
| `scripts/check_core_contract_imports` + probes (7 eixos) | rc=0 |
| `scripts/check_progress_consistency` + probes (11 eixos) | rc=0 |
| `scripts/check_regras_do_linter` + probes (8 defeitos plantados, 8 detectados) | rc=0 |
| `check_secoes_de_seguranca` + probes (14 eixos) · `check_insumo_de_metrica` · `check_fold_authority` · `check_store_read_surface` · `check_gate_coverage` | rc=0 |
| `check_api_surface` · `check_contract_examples` (13 pos., 95 neg.) · `check_spec_examples` (20 blocos) · `check_gabarito_fora_do_git` · `check_readme_atual` · `check_allowlist_do_auditor` | rc=0 |

Nenhum `@unittest.skip`/`expectedFailure` incondicional; nenhum mock na suíte (único `patch`/`monkeypatch` é uma menção em comentário de `tests/mutation_harness.py:56`). Os 9 `skipIf(_URL is None)` não dispararam.

---

## FINDINGS

### BLOCKER

- **[B1] A imagem não constrói: o `pyproject.toml` desta fase declara dois pacotes de topo que o `Dockerfile` não copia**

  **Arquivo:** `pyproject.toml:241` (`"range_cli"`) e `pyproject.toml:255` (`"dados_sinteticos"`) × `Dockerfile:59-63` (COPY) e `Dockerfile:70` (`RUN pip install --no-cache-dir . -c constraints.txt`)

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §"Fase 4" itens 1 e 4 e `06_ACCEPTANCE_TESTS.md` T5 (o par do reinício de container); `04_SCENARIO_SCHEMA.md` §8 (a superfície `range-cli` é entregável desta fase e tem de existir no produto entregue); `00_MASTER_SPEC.md` §8 (stack Docker Compose).

  **Evidência — observada, não suposta.** `scripts/check_provas_de_container.py` saiu com rc=2 e imprimiu a saída íntegra do build:

  ```
  #21 3.084       error: package directory 'range_cli' does not exist
  #21 ERROR: process "/bin/sh -c pip install --no-cache-dir . -c constraints.txt"
             did not complete successfully: exit code: 1
  target migrations: failed to solve
  --- prova `demo`      (rc=125)  NAO EXECUTADA: a stack nao subiu (rc=1)
  --- prova `reinicio`  (rc=125)  NAO EXECUTADA: a stack nao subiu (rc=1)
  ```

  Confirmado na árvore: `Dockerfile:59-63` copia apenas `pyproject.toml constraints.txt alembic.ini`, `alembic/`, `contracts/`, `range-core/`, `domains/`. `git diff --stat <base> HEAD -- Dockerfile .dockerignore docker-compose.yml` é **vazio** — a fase criou os dois pacotes e não tocou o `Dockerfile`. O `.dockerignore` não os exclui (lido; ambos estão no contexto), logo a causa é exclusivamente a ausência do `COPY`.

  **Por que é blocker — quatro consequências concretas:**
  1. **Nenhuma imagem do produto constrói.** Os três serviços (`migrations`, `range-api`, `academus-api`) saem da mesma imagem.
  2. **Os itens 1 e 4 da DoD da Fase 4 regridem de PASS para NÃO VERIFICADO**, pelo mecanismo que a P4-10 criou exatamente para isso. Não é ausência de ambiente: é vermelho por conteúdo rastreado.
  3. **O CI fica vermelho no PR.** `.github/workflows/invariants.yml:14-17` dispara em `pull_request`, e o job `contratos` roda `docker compose up -d --build --wait range-api academus-api` (`:862`). O mesmo build, a mesma falha.
  4. **Mesmo corrigindo `range_cli`, o container segue morto:** `range-core/engine/loader/pack_loader.py:81` faz `from dados_sinteticos import achados_no_valor` no topo — import incondicional do loader de pack. Sem `dados_sinteticos/` na imagem, o engine não importa.

  O `pyproject.toml` documenta esta exata assimetria **quatro vezes** (`:189-198`, `:207-211`, `:218-231`, `:250-255`, `:321-326`: *"o que resolve pela árvore com o CWD na raiz some na instalação, e o sintoma só aparece no container"*). A instalação editável do venv de auditoria resolve pela árvore — é por isso que os 856 testes passam verdes sobre uma imagem que não existe.

---

### HIGH

- **[H1] O item 7 da DoD é declarado cumprido sem mecanismo, e o código contraria a letra de `04` §4 sem `spec-change`**

  **Arquivo:** `range-core/engine/loader/pack_loader.py:126` (`SUPPORTED_SCHEMA_VERSIONS: tuple[int, ...] = (2,)`); `range-core/engine/migrations/__init__.py:96` (`MIGRACOES = MappingProxyType({})`) e `:70-73`; `docs/progress/fase_7.md:613-632` e `:2953` ("DoD 9/9")

  **Requisito violado:** `04_SCENARIO_SCHEMA.md` §4 — *"Engine declara `ENGINE_VERSION` e `SUPPORTED_SCHEMA_VERSIONS = [N, N-1]`"*, *"Pack em N-1 carrega com migração em memória e aviso no boot"*, *"Migrações em `range-core/engine/migrations/v<n>_to_v<n+1>.py`, cada uma com teste"*. `07_IMPLEMENTATION_PHASES.md` §"Fase 7" item 7; `06_ACCEPTANCE_TESTS.md` T12 primeiro critério. `CLAUDE.md` §"A especificação é imutável durante a implementação" e §"Fluxo por fase" (*"Item inviável → pare e explique, não contorne"*).

  **Evidência:** não existe `v<n>_to_v<n+1>.py` (`ls range-core/engine/migrations/` → só `__init__.py`). `MIGRACOES` está vazio. `tests/test_migrations.py:35` chama-se `ORegistroEstaVazioEIssoEADeclaracao` e o teste `:38` afirma `test_nao_ha_migracao_declarada`. Com `SUPPORTED_SCHEMA_VERSIONS = (2,)` **não existe N-1 suportada**: um pack v1 é *recusado*, não *migrado*. O próprio módulo (`:70-73`) diz: *"a assimetria com a norma — que pede `[N, N-1]` — é DELIBERADA"*, e `pack_loader.py:738` imprime ao usuário *"`04` §4 fixa `SUPPORTED_SCHEMA_VERSIONS = [N, N-1]`; aqui ele é…"*. O registro declara a divergência (`fase_7.md:621`: *"O item é satisfeito pela forma normativa, e não pela literal"*) e ainda assim fecha em **9/9** (`:2953`).

  **Por que é HIGH:** o argumento técnico está certo — migrador identidade é pior que nenhum, e eu não o recomendaria. O defeito é de rito, e é o que o `CLAUDE.md` proíbe nominalmente: a saída para "a spec está errada" é PR `spec-change:` com aprovação humana, não docstring. Hoje três documentos normativos (`04` §4, `07` DoD 7, `06` T12) afirmam um comportamento que o código recusa por decisão própria, e o item entra no fechamento como passado. Nenhuma linha da §6 carrega isso como pendência — logo, depois do merge, a divergência não tem dono.

- **[H2] Os itens 6 (metade de pack) e 9 da DoD repousam em artefato que não existe em nenhum lugar verificável, e a P7-3 foi fechada com premissa que o próprio registro contradiz**

  **Arquivo:** `docs/progress/fase_7.md:2073-2083` (P7-3 → `RESOLVIDA`) × `docs/progress/fase_7.md:2849` (§8.1) e `:2940-2943` (§8.3); tabela `:1231`

  **Requisito violado:** `06_ACCEPTANCE_TESTS.md` T12 — *"`dryrun` percorre todos os caminhos sem erro"* e *"**Reconstrução da projeção do zero para o exercício de 4 h do `ransomware-universidade` roda em < 3 s**, com máquina, data e stack declaradas"*; `07_IMPLEMENTATION_PHASES.md` §"Fase 7" itens 6 e 9.

  **Evidência:** a P7-3 (*"a árvore não cobre o pack, e o pack é insumo da prova"*) é fechada em `:2079-2083` com o argumento *"Um artefato que é função dos insumos não precisa ser hasheado junto da árvore… o pack não é mais conteúdo independente: é saída de código versionado."* O **mesmo registro**, em §8.1 (`:2849`), diz o contrário: *"nenhum comando produz os arquivos não-gabarito (`manifest`, `injects`, `branches`, `objectives`, `information_distribution`)"*. E §8.3 (`:2940-2942`) confirma: *"os sete arquivos existem no worktree da fase e em cópia de segurança no checkout principal"*, com o destino deles ainda por decidir na **P7-9 (`ABERTA`)**. Ou seja: **cinco dos sete arquivos** do pack medido são autoria manual, não são função de insumo versionado, não estão na árvore e não têm artefato de prova amarrado à árvore — ao contrário das provas de container (P4-10) e da prova do seed (M2), que existem exatamente para esta classe. Consequência medida: eu não pude verificar nenhuma das afirmações de `:2903-2917` — *"`lint`: sem achados"*, *"`dryrun`: 2 pontos, 4 caminhos"*, `content_hash a6a2ac92…`, `0.025s`.

  **Por que é HIGH:** o item 9 é, pela própria §1 do registro (`:46`) e pela nota de T12 (`:147`: *"o veredito é este critério"*), o veredito da fase — e ele chega ao auditor como **atestação**, com a pendência que nomeava o buraco marcada `RESOLVIDA` por um argumento que não vale para o artefato que foi medido. `check_allowlist_do_auditor.py:129-132` fecha o círculo declarando que *"o auditor confere a medição transcrita no registro da fase, por leitura"*.

- **[H3] Três pendências marcadas `ENTREGA` não foram entregues, e o mecanismo as isenta de migrar — saem da fase sem destinatário**

  **Arquivo:** `docs/progress/fase_7.md:1214` (P1-7), `:1217` (P5-4), `:1221` (P6-5); `scripts/check_progress_consistency.py:153-158`

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §"Registro de progresso" (*"gravar … pendências e próxima fase"*); `docs/process/WORKFLOW.md` §auditoria, na regra que **esta própria fase** acrescentou (`git diff … WORKFLOW.md`: *"Todo achado que não for corrigido no ciclo da rodada nasce com linha na §6 do `fase_N.md` corrente"*).

  **Evidência:** `check_progress_consistency.py:153-158` define `NAO_MIGRA = ("ENTREGA",)` com o comentário *"não é cobrada na fase seguinte — o que a cobra é a Definition of Done dela"*. Nenhuma das três é item da DoD da Fase 7 (que tem 9 itens, nenhum sobre elas). E as três não foram entregues:
  - **P1-7** — a própria célula `Vence em` diz *"**NÃO fechou na peça 2**"* (`:1214`), confirmado em `:660-662`.
  - **P5-4** — `:1420` diz *"Vence em: a peça 2"*; `:636` e `range-core/engine/migrations/__init__.py:54` dizem *"Saiu desta fase por isso"*.
  - **P6-5** — `:1562-1584` a declara *"entrega desta fase"* por decisão do operador; medido no contrato: `contracts/assessment.schema.yaml:168-187` ainda tem só `period`, `population`, `criterion` — **nenhuma lista de `case_id`**, e `additionalProperties: false`.

  **Por que é HIGH:** é a classe que a P7-6 acabou de fechar, reaberta por outro caminho e **mecanizada**: por construção do verificador, uma pendência `ENTREGA` não entregue não aparece na tabela da Fase 8 nem em DoD nenhuma. Nada fica vermelho. A P6-5 é a mais caras das três — é decisão do operador, tomada na peça 7 da Fase 6, e `03_EXERCISE_DESIGN.md` §5.3 depende dela para separar erro de julgamento de lacuna de cobertura.

---

### MEDIUM

- **[M1] Os dois pacotes de topo criados nesta fase são invisíveis a dois dos verificadores de invariante**

  **Arquivo:** `tools/check_contract_literals.py:82` (`SCANNED_DIRS = ("range-core", "domains", "tests")`); `tools/check_security_constraints.py:41` (`SCANNED_DIRS = ("range-core", "domains", "tools", "scripts", ".claude", "user-scope")`) × `pyproject.toml:241,255`

  **Requisito violado:** `06_ACCEPTANCE_TESTS.md` T2 — *"Nenhuma string literal de flag ou de `event_type` fora dos geradores. Verificado por `tools/check_contract_literals.py`"*; `06` T15 e `05_SECURITY_REQUIREMENTS.md` §1 — *"`tools/check_security_constraints.py` falha contra violação plantada"*; `CLAUDE.md` §"Invariantes arquiteturais" 2 e 3.

  **Evidência:** `range_cli/` e `dados_sinteticos/` são código instalado (`pyproject.toml:241,255`) e o próprio `invariants.yml` os classifica como CODE do `spec_freeze` justificando que *"`range_cli/lint.py` executa as validações obrigatórias de `04` §6.2"* e que `dados_sinteticos/` *"é o predicado que executa `05` §3 e a cláusula de IOC da §5.2"*. Nenhum dos dois está nos `SCANNED_DIRS` dos dois verificadores acima. Medido: hoje a violação **não existe** — `grep -n "academus\.\|prontus\." range_cli/*.py dados_sinteticos/*.py` devolve apenas caminhos de módulo (`range_cli/__init__.py:8`, `range_cli/cli.py:381`), que são import legítimo de raiz de composição. O ponto cego é latente, não explorado.

  **Por que é MEDIUM e não menor:** é literalmente a mensagem que `check_gate_coverage` deu nesta mesma fase (`invariants.yml`, comentário: *"diretório novo no topo nasce invisível ao gate, e nada avisa"*). A fase consertou essa cegueira para o `spec_freeze` e para o guarda de imports do core (P7-8), e deixou os dois scanners de `tools/` fora do conserto — três direções da mesma classe, duas fechadas.

- **[M2] A allowlist do auditor admite a FORMA `range-cli scenario …` e o executável é inalcançável — os quatro itens de DoD cujo sujeito é o verbo só puderam ser julgados por chamada em processo**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:102` (`rf"^{SAFE_ENV_PREFIX}range-cli\s+scenario\s+(validate|lint|dryrun)\b"` — **sem `PREFIXO_DO_VENV`**)

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §"Fase 7" itens 1, 2, 3 e 6 (todos enunciados como `range-cli scenario lint`/`dryrun`); a regra escrita no próprio arquivo em `:128-133` (*"Script novo que precise ser executado pelo auditor entra aqui por nome, no commit que o cria"*) e a lição de `:12-41`.

  **Evidência — medida por execução:** `range-cli scenario lint tests/fixtures/pack_minimo` → `bash: range-cli: command not found` (rc=127). A grafia alcançável, `.aurora-audit/venv/Scripts/range-cli scenario lint …`, é **bloqueada pelo hook** ("fora da allowlist"). É a terceira ocorrência que `readonly_bash.py:19-30` descreve com todas as letras — *"a FORMA estava admitida … e o INTERPRETADOR não era alcançável"* — cometida na fase que **criou** o comando.

  **Por que é MEDIUM e não HIGH:** o comportamento dos quatro itens está provado por gate executado — `tests/test_range_cli_dryrun.py:24` e `tests/test_range_cli_lint.py:739-767` chamam `cli.main([...])` com `argv`, parser e códigos de saída reais, e rodaram nos 856. O que fica sem prova é o console script entregue (`pyproject.toml:262`) — que é justamente o que B1 mostra estar quebrado no produto.

---

### LOW

- **[L1] `04` §8 declara `scenario validate` e `scenario migrate`; nenhum dos dois existe, e a ausência não tem linha na §6**

  **Arquivo:** `tests/test_range_cli_materialize.py:200-224` (`test_os_outros_subcomandos_de_04_secao_8_NAO_existem`); `docs/progress/fase_7.md:799-802`

  **Requisito violado:** `04_SCENARIO_SCHEMA.md` §8 — declara `range-cli scenario validate <path>` e `range-cli scenario migrate <path>` como verbos próprios, com lista de checagem própria, e fecha com *"`validate`, `lint` e `evidence verify` rodam no CI"*.

  **Evidência:** o teste afirma a ausência dos dois por `assertRaises(SystemExit)` no parser. O registro (`:799-802`) reconhece que *"`04` §8 dá nomes e listas de checagem **distintos** a `validate` e a `lint`"* e usa isso para justificar a colheita — mas não dispõe da ausência do verbo. `readonly_bash.py:102` allowlista `validate`, isto é, o ferramental também o espera. A §6 não tem linha para isso (a P7-9 é sobre `lint` no CI, não sobre `validate`). O argumento do docstring (*"um `validate` seria um `lint` com menos checagens"*) é defensável; o que falta é destinatário — `04` §8 está mapeado às Fases 2 e 7, e a 7 é a última.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `lint` recusa inject sem objetivo e sem `noise: true` | **PASS** | `tests/test_range_cli_lint.py:255-292` — as **duas** pernas (recusa + `noise: true` dispensa), posição ancorada em `- id: A01` derivada do texto da fixture; executado nos 856. Negativo de contrato em `scenario.schema.v2.yaml`, entre os 95 executados por `check_contract_examples.py` |
| 2 — `event_type` inexistente em condição de branch, **com posição** | **PASS** | `tests/test_range_cli_lint.py:294-327`: caminho `$.branches[0].evaluate[0].when.all[0].event`, e `:311` prova que a **coluna aponta para o VALOR** (`linha[coluna-1:] == "vpn_acess_revoked"`), não para a chave. Negativo novo em `contracts/scenario.schema.v2.yaml:1153-1176`, executado |
| 3 — condição por juízo do facilitador recusada | **PASS** | `tests/test_range_cli_lint.py:329-353`, posição em `facilitator_thinks_response_was_good`; `branch_condition` com `oneOf` fechado + `additionalProperties: false` |
| 4 — `branch_policy` do manifesto aplicada | **PASS** | `tests/test_branching.py:156-206` — excesso de pontos por linha, excesso de caminhos, política ausente não inventa default, e a linha do ponto caindo para a do `at_inject`. `confere_branch_policy` entra em `_passos` (`branching.py:168-170`), logo vale na carga **e** no lint |
| 5 — branch sem `reconverge_at` recusada | **PASS** | `contracts/scenario.schema.v2.yaml:537` `required: [id, at_inject, evaluate, reconverge_at]`; negativo `:1141` entre os 95 executados; `tests/test_range_cli_lint.py:486`. `reconvergence_required` removido em coerência com `04` §2 (`additionalProperties: false` em `:196` recusa o campo) |
| 6 — `dryrun` percorre todos os caminhos | **PARCIAL** | **PASS sobre fixture:** `tests/test_range_cli_dryrun.py:27-69` (4 eixos, incl. lint-antes-da-travessia e recusa de percurso) e `tests/test_branching.py:285-368` (duas sequências, janela que exclui o `next` irmão, 3 recusas de travessia). **NÃO VERIFICADO sobre o pack real** — ver H2 |
| 7 — pack v1 migra automaticamente; v0 recusado com instrução | **FALHA na 1ª metade** | Não existe migrador (`ls range-core/engine/migrations/` → só `__init__.py`; `MIGRACOES` vazio; `tests/test_migrations.py:38`). `SUPPORTED_SCHEMA_VERSIONS = (2,)` (`pack_loader.py:126`) contra `[N, N-1]` de `04` §4. **2ª metade PASS**: a recusa instrui em quatro perguntas (`pack_loader.py:688-740`), testada. Ver H1 |
| 8 — fato citado no `GM_NOTES.md` e ausente do `ground_truth.yaml` é recusado | **PASS** | `range-core/engine/citacoes.py` — forma derivada de `ground_truth.schema.yaml §$defs/fact_id_pattern` (`:72-81`), duas redes (bem-formado + quase-formado `:92`), três lados conferidos. `tests/test_citacoes_de_fato.py:145-268`: recusa **na carga** contra o pack **materializado**, com controle positivo e a perna "sem `GM_NOTES` carrega". **Dirigido por `fact_id`, não por seed** |
| 9 — reconstrução do exercício de 4 h em < 3 s | **NÃO VERIFICADO** | `fase_7.md:2907-2918` transcreve 0,025 s contra 3 s, com máquina/data/stack geradas por código (forma de T12 satisfeita). Não há caminho executável nem artefato amarrado à árvore; o pack medido não está na árvore. `check_allowlist_do_auditor.py:129-132` declara a exclusão de `medida_do_exercicio_4h`. Ver H2 |
| *(Fase 4, itens 1 e 4 — regressão)* | **NÃO VERIFICADO** | `check_provas_de_container.py` rc=2, por defeito de conteúdo rastreado. Ver B1 |

---

## TESTES QUE NÃO PROVAM O REQUISITO

**Nenhum encontrado.** Ao contrário — esta suíte resiste bem aos padrões que eu procuro:

- `_BaseDeLint.um_achado` (`test_range_cli_lint.py:195-208`) exige **exatamente um** achado do sítio esperado, e o docstring diz por quê: *"uma fixture que plantasse dois defeitos passaria provando o errado"*. É discriminante, não asserção de presença.
- `assertPosicaoNaAncora` (`:210-225`) **deriva** a linha esperada do texto da fixture e exige que a âncora apareça exatamente uma vez — não compara contra número mágico; e exige `posicao.exata`, o que impede que posição aproximada satisfaça um critério que pede posição.
- `test_dod_2_a_coluna_aponta_para_o_VALOR_e_nao_para_a_chave` (`:311`) é o caso em que um teste frouxo passaria: o requisito é *localizar*, e apontar para `event:` (que está certo) satisfaria uma asserção de linha.
- `test_option_de_OUTRO_decision_point_e_recusado_com_posicao` (`test_branching.py:222`) isola o discriminante real (existência global × existência no ponto *indicado*), e a unicidade do achado impede que a regra antiga produza o verde.
- `test_since_uma_origem.py:99-152` prova as duas guardas contra um **contrato sintético** que declara outro qualificador — o único desenho que distingue "lê o contrato" de "tem o literal certo por coincidência".
- `test_range_cli_materialize.py:231` existe precisamente para que o teste de *ausência* de verbos (`:200`) não passe por remoção acidental de `lint`.
- `test_pack_limpo_sai_zero`/`OPackLimpoNaoTemAchado` — controles positivos presentes; sem eles nenhuma recusa significaria nada.

Observação sem finding: `branching.percorre` enumera um caminho por braço de cada ponto, e não o produto cartesiano entre pontos de linhas distintas. `04` §6/§9 ("um ponto de ramificação por linha") e T12 ("percorre todos os caminhos sem erro") não exigem o produto, e as janelas são por linha — leitura defensável, registrada em `fase_7.md` §5.2 antes do código.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **O pack `ransomware-universidade` — inteiro.** `scenarios/` está fora do Git (decisão da Fase 5) e o pack não está no hash da árvore. Não pude conferir nenhuma afirmação de `fase_7.md:2901-2917`: 40 injects (22/11/7), 5 `decision_point`s, 2 `capability_gap`s, 4 `media_event`s, `lint` sem achados, `dryrun` com 2 pontos e 4 caminhos, `content_hash a6a2ac92…`, e a medida de 0,025 s. Isto está reportado como H2 porque a P7-3 foi fechada afirmando que este buraco deixou de existir.
2. **A medição do item 9.** `scripts/medida_do_exercicio_4h.py` está fora da allowlist por decisão declarada (`check_allowlist_do_auditor.py:129-132`) — exige Postgres, trunca e escreve o store de medição. Li a transcrição; não a executei, e reexecutá-la em outra máquina produziria outro número, não confirmaria este.
3. **A prova do seed (M2) e as provas de container (P4-10).** Não as vi rodar. A primeira confere pela árvore `8311d748188d` (= a deste checkout) e imprime medição íntegra; a segunda **reprovou**, e a causa está em conteúdo rastreado — foi o que sustentou B1.
4. **P7-10 — o gabarito só tem Linha B.** `fase_7.md:1238` declara que o `ground_truth.yaml` materializado não tem fato de `initial_access`/`exfiltration`, contra `03` §3.1 e `04` §3/§9 (*"Linhas A + B + ruído"*), com `TTRV` incomputável. Não pude medir — o arquivo não está na árvore. A pendência está `ABERTA` com gatilho *"fechamento desta fase — apresentação ao proprietário"*, logo **não é achado órfão**; registro aqui que ela pesa sobre o insumo do item 9, e que a decisão é do operador (ampliar o gerador ou `spec-change`).
5. **O console script instalado.** `range-cli` não é alcançável pelo Bash do auditor (M2); julguei os verbos por `cli.main([argv])` em processo.
6. **O ambiente da suíte não é o canônico.** O venv do lançador é **Python 3.14.7** (`.aurora-audit/venv/pyvenv.cfg`), contra `00_MASTER_SPEC.md` §8 e `python-version: "3.12"` do CI. Os 856 passaram, então não houve vermelho a diagnosticar (R2 §3) — mas a suíte que eu rodei não é a do CI Linux 3.12, e a diferença aparece como `ResourceWarning` em massa do `psycopg` (já registrado como parte da P7-14).
7. **Comportamento de runtime em container.** Inalcançável por construção — e, nesta rodada, inexistente: a imagem não constrói.
8. **Achados órfãos de rodada anterior.** Não há. A 1ª rodada desta fase foi **ABORTADA no launch** (`docs/progress/audit_20260913T062514Z.md`; `fase_7.md:2958`) — OAuth expirado, nada auditado, nenhum finding M/L a conferir. H3 é achado desta rodada sobre pendências herdadas, não achado órfão.
