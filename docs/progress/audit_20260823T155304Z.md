# Auditoria de checkpoint - 2026-08-23T15:53:04.737217+00:00

- fase: **6**
- commit auditado: `c3051dc54aeae4f971247fd0b17af82e5361ef8f`
- session_id: `e0179ea4-8c7c-4d05-bfba-eceb95e920d6`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

All verifiers, both SHA-bound proofs, and the full suite executed. Report follows.

```text
# AUDITORIA — FASE 6

## VEREDITO: PASS

Guarda de base (verbatim do lançador): P3-7 — fase 6, branch default
`b794ce232e2489bdf218a62af8c516edcbcd7b83` — a auditoria é PORTA.

Execução real neste worktree (HEAD c3051dc54aea):

  suíte            735 testes, `OK`, 0 skip, 0 xfail, 0 falha  (71,4 s)
  contratos        13 positivos, 88 negativos, 11 instâncias reais — rc=0
  spec examples    20 blocos, 16 validados, 4 com motivo — rc=0
  harness negativo os 6 verificadores + 112 provas de invariante do hook — rc=0
  P4-10            2 provas de container, SHA c3051dc54aea, ambas rc=0
  M2 (Fase 5)      prova do seed, SHA c3051dc54aea, 3.543.783 linhas, 140,1/144,5 s
  verificadores    core_boundary, contract_literals, event_envelope, codegen --check,
                   insumo_de_metrica(+probes), hooks_com_emissor(+probes),
                   fabrica_liga_emissor(+probes), api_surface, store_read_surface,
                   fold_authority, core_contract_imports, gate_coverage, spec_flags,
                   secoes_de_seguranca, gabarito_fora_do_git, trilha_de_auditoria,
                   allowlist_do_auditor, progress_consistency, readme_atual,
                   security_constraints, synthetic_data — todos rc=0
  spec             `git diff docs/spec/` contra a base: VAZIO (nenhuma edição de spec no PR)

## FINDINGS

### BLOCKER
nenhum encontrado.

### HIGH
nenhum encontrado.

### MEDIUM

- [M1] A condição (4) da contrassinatura é inerte em produção: `actor_id` é a própria persona
  Arquivo: `range-core/participant/api/app.py:101`
    `return {"token": tokens.issue(persona, persona, secret=sessao.segredo)}`
    — `sub` recebe o mesmo valor de `persona`; `_declara` propaga
    `actor_id = claims.sub` (`app.py:135,150,158`).
  Requisito violado: `03_EXERCISE_DESIGN.md` §3.4, condição (4) e a frase que a
    fundamenta — *"A condição (4) existe porque (2) não a cobre: um mesmo operador
    com duas credenciais satisfaria as personas e assinaria sozinho."*
  Evidência: com `sub == persona`, `actor_id` é função de `persona`. Satisfeita a
    condição (2) (`pro_reitoria` → `ti`), a (4) é satisfeita por construção — a
    verificação `antecedente.actor_id == actor_id`
    (`range-core/declarations/contrassinatura.py:106`) nunca pode disparar em
    produção. O caso exato que §3.4 nomeia — um operador de posse das duas
    credenciais de ambiente — grava o par com `actor_id` distintos e passa.
    Confirmado por execução: `tests/test_participant_emissao_pela_rota.py:134`
    afirma `evento.actor_id == persona`.
  Por que é medium e não blocker: a condição está implementada **na letra** da
    §3.4 e testada (`tests/test_participant_contrassinatura.py`), e o limite está
    declarado em `range-core/participant/api/tokens.py:34-38` e em
    `docs/progress/fase_6.md`. Nenhum item da DoD da Fase 6 cobra credencial por
    humano, e nem `01` §6 nem `05` §8 a exigem. O que fica é que um leitor de §3.4
    conclui que a autocontrassinatura por posse dupla é barrada, e ela não é.

- [M2] O predicado normativo de contenção de `03` §3.1 não é avaliável quando o fato existe
  Arquivo: `range-core/engine/verificacao.py:259-275`
  Requisito violado: `03_EXERCISE_DESIGN.md` §3.1 — `absence_of` com `since: self`
    significa *"não há fato desta classe a partir do instante em que este predicado
    passou a ser avaliado"*, e a seção declara que o `ransomware-universidade`
    materializa exfiltração em `T-17d`, antes de qualquer ação da equipe.
  Evidência: `avalia()` levanta `SemGramaticaTemporal` — recusa alta, não `False` —
    quando `classe in mundo.fatos`. Como `Mundo.fatos` carrega classes e não
    instantes, todo cenário que materialize o fato antes da resposta faz a avaliação
    de contenção **abortar** em vez de responder. A recusa acontece na avaliação, em
    exercício, que é o instante que `confere_folhas_temporais`
    (`range-core/engine/loader/pack_loader.py:573-605`) foi criada para evitar nas
    folhas irmãs `before`/`after`.
  Por que é medium e não blocker: está registrada como pendência **P6-3**
    (`docs/progress/fase_6.md:720-797`) com gatilho, medição e decisão do
    proprietário; é hoje **inalcançável** — medi: nada em `range-core/` ou
    `domains/` emite `fact_materialized`, e o único `append` desse tipo na árvore é
    à mão em `tests/test_aar_timeline.py`. Nenhum item da DoD desta fase fica sem
    evidência por causa disso: TTCV e TTRV são disparáveis e testados
    (`tests/test_metrics_verificacao.py:133,163`; `tests/test_laco_de_verificacao.py`).
    O item entra na Fase 7, que é quem traz o pack.

### LOW

- [L1] `admite()` e `comparavel()` não têm consumidor de produção
  Arquivo: `range-core/objectives/projecao.py:119-132` e `:220-236`
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T9 — *"objetivo com evidência `auto`
    não satisfeita não é classificado como `excellent`"*.
  Evidência: `grep -rn "admite("` em `range-core/`, `domains/`, `scripts/` devolve
    apenas a definição e quatro chamadas em `tests/test_objectives_projecao.py`.
    O mecanismo existe e discrimina nas duas direções, mas nada no caminho de
    produção o invoca — quem classificaria é o AAR, da Fase 10.
    A metade equivalente de `comparavel()` está declarada como cadeia de duas
    metades em `docs/progress/fase_6.md:150-172`; a de `admite()` não está —
    `grep -n "excellent" docs/progress/fase_6.md` não devolve nada.
  Por que é low: a fronteira Fase 6 computa / Fase 10 renderiza é legítima e usada
    de forma consistente na fase; o que falta é o registro declarando-a para este
    item, e não o mecanismo.

- [L2] `409` para justificativa ausente contradiz a semântica documentada da própria rota
  Arquivo: `range-core/participant/api/app.py:164-167` (o `409` é de recusa de
    **estado**: contrassinatura sem antecedente, fora de ordem, par já fechado) vs.
    `range-core/participant/api/emissor.py:134-139` (justificativa vazia, que é
    pedido malformado, sai pelo mesmo `EmissaoRecusada`).
  Requisito violado: nenhum — `03` §3.4 exige a justificativa e não fixa status.
  Evidência: `tests/test_participant_emissao_pela_rota.py:183-207` afirma `409` e
    documenta a discrepância no próprio docstring. Registrada como **P6-8**.
  Por que é low: o invariante que importa — *"o store fica vazio"* — é o que a
    asserção principal afirma (`:206`). É superfície, não semântica de evento.

- [L3] As violações plantadas nos computadores de métrica não são reproduzíveis
  Arquivo: `docs/progress/fase_6.md:391-400`
  Requisito violado: nenhum item de DoD — `06` T10 exige *"todo verificador tem
    teste negativo próprio"*, e os verificadores têm (`*_probes.py`, todos executados
    acima).
  Evidência: a tabela declara 5 + 4 + 2 + 5 violações plantadas em
    `metrics/declaracao.py`, `metrics/verificacao.py`, `metrics/epoch.py` e na
    derivação, "fora da árvore". `grep -rln "mutation_harness" tests/` devolve cinco
    arquivos, **nenhum** deles de métrica; não há `test_metrics_*_probes.py`.
    A afirmação é atestação: eu não consigo reexecutá-la.
  Por que é low: as unidades correspondentes têm cobertura direta e não-tautológica
    (verificado caso a caso abaixo), e a classe de defeito que o harness pegaria é
    coberta estruturalmente por `check_insumo_de_metrica`, que **rodou** com seis
    negativos e o controle positivo.

## DoD — item a item

| # | Item | Status | Evidência |
|---|---|---|---|
| 1 | Consultar a trilha com filtro de período emite `audit_query_performed` | PASS | Rota real por `TestClient` + emissor real + asserção sobre o evento no store: `tests/test_api_emissao_pela_rota.py:153-263`; guarda de boot sobre a superfície REAL em `:123-150`; `check_hooks_com_emissor` rc=0 (10 hooks/10 emissões, 4 direções); `check_fabrica_liga_emissor` rc=0 (3 serviços) |
| 2 | Cada par declaração × verificação é disparável e testado | PASS | TTCD/TTCV: `tests/test_aar_timeline.py:129-140`; TTRD: `tests/test_metrics_declaracao.py:151`; TTRV: `tests/test_metrics_verificacao.py:133`; TTID: `:175-246` de `test_metrics_declaracao.py`; TTIV: `tests/test_metrics_verificacao.py:316-353` |
| 3 | Desconto do intervalo de `technical_failure` pela **união**, nunca pela soma | PASS | `range-core/metrics/epoch.py:280-297` (`uniao`) e `:300-327` (recorte à janela); `tests/test_metrics_epoch.py:105-165` (sobreposto, contido, adjacente, disjunto, ordem, vazio) e `:263-289` |
| 4 | Nenhum evento de epoch com `reason: rehearsal` entra em cálculo | PASS | `range-core/metrics/epoch.py:128-201`; `tests/test_metrics_epoch.py:200-258`; e o lado da verificação obedece ao mesmo critério (`tests/test_metrics_verificacao.py:497-575`) |
| 5 | Declaração não altera nenhum valor de ground truth | PASS | `tests/test_simulation_state.py:276-313` (as seis declarações contra os defaults, asserção única e localizada) + `tests/test_simulation_state_probes.py:98…` (ramo plantado no `_writes_of` deixa o teste vermelho) |
| 6 | Declaração prematura produz lista de eventos incompatíveis no AAR | PASS | `range-core/aar/timeline.py:182-244`; `tests/test_aar_timeline.py:177-218` — fato dentro lista, fato antes não lista, ação de participante na janela **não** entra, extremo entra |
| 7 | Pack sem `verification_predicates` não carrega | PASS | `contracts/ground_truth.schema.yaml:24` (`required`) + fixture negativa `:546` entre os 88 executados; e o loader de produção recusa: `tests/test_pack_completo_materializado.py:181-196`, com controle positivo `:223-225` e o negativo-do-negativo apenas-manifesto `:242-284` |
| 8 | `TTIV` computado por limiar de calibração, não por predicado de mundo | PASS | `range-core/metrics/verificacao.py:196-227` (Brier recalculado por prefixo, `<=` limiar, `SIGLA_DO_LIMIAR` fora de `SIGLA_POR_PREDICADO`); `tests/test_metrics_verificacao.py:316-353` |
| 9 | Brier só dentro do escopo revisado; lacuna de cobertura em separado | PASS | `range-core/metrics/calibracao.py:134-162` (escopo), `:245-261` (`_lacunas`), `:212-223` (três listas, sem compensação); `tests/test_metrics_calibracao.py` |
| 10 | Rubrica ausente ou em versão divergente impede carga do pack | PASS | `range-core/engine/loader/contract_rules.py:177-198` + `pack_loader.py:719-731`; `tests/test_pack_loader.py:435-449` (as duas metades pelo loader real) e fixtures `contracts/scenario.schema.v2.yaml:957-981` executadas por `check_contract_examples` |
| 11 | Divergência ≥ 2 pontos entre avaliadores gera alerta | PASS | `range-core/aar/timeline.py:281-320` (amplitude entre extremos, última nota do avaliador, não resolve); `tests/test_aar_timeline.py:296-352` |
| 12 | Cada uma das nove siglas classificada pelo critério de `00` §3.2, batendo com `03` §3.0 | PASS | `tests/test_derivacao_das_nove_siglas.py` — lê a tabela do documento, aplica a conjunção, e cruza com as siglas que os **computadores rodando** produzem (`:228-299`). Não é tautologia: entradas e operação vêm da spec |
| 13 | Todo `event_type` tem `metric_side`; os quatro lados são disjuntos | PASS | `range-core/engine/loader/contract_rules.py:123-153` (cobertura exata nas duas direções, valores no conjunto, lado vazio reprova); 3 fixtures negativas em `scripts/check_contract_examples_probes.py:120-145`, executadas |
| 14 | Folha de predicado obedece à conjunção de `09` §4.0, com probe das três violações + controle positivo | PASS | `contract_rules.py:155-173` (interseção `state_effect` ∧ `verification`); as três formas isoladas em `contracts/ground_truth.schema.yaml:694`, `:712`, `:731`, mais typo `:650`, `containment_declared` `:669` e `decision_made` `:745`; controle positivo nos exemplos que usam `vpn_access_revoked`. Todos entre os 88 negativos executados |
| 15 | Cada computador recebe só o seu lado + epoch + escalares; só o AAR tem as duas metades | PASS | `range-core/metrics/insumo.py` (tipos próprios, ponto único, escalares); `scripts/check_insumo_de_metrica.py` rc=0 e `_probes.py` com 6 negativos + o positivo do consumidor que só anota; `tests/test_metrics_insumo.py:88-190`; e `tests/test_aar_timeline.py:142-164` prova que a janela não sai de computador nenhum |
| 16 | União, exclusão de `rehearsal` e seleção de start são cálculo do consumidor | PASS | `range-core/metrics/insumo.py:152` (epoch **inteira** aos dois lados); `tests/test_metrics_epoch.py:374-441` afirma a **presença do insumo antes da conta**, que é exatamente o que T10 exige para o número certo não aparecer por ausência; seleção de start em `metrics/declaracao.py:149-165` |
| 17 | `inject_fired` emite no payload os marcadores de start, com teste de emissão | PASS | `contracts/events.schema.yaml` (`inject_fired_payload`, fechado); derivação de três pernas em `pack_loader.py:761-798`; carimbo em `inject_engine.py::fire`; `tests/test_inject_engine.py:745-793` (todo disparo, identidade com a carga, e o inject de ruído sem impacto) |

## TESTES QUE NÃO PROVAM O REQUISITO

nenhum encontrado.

Sondei especificamente as sete formas que esta função existe para pegar, e as
sete deram negativo:

- **asserção sobre mock/espião** — `grep -rln "unittest.mock\|MagicMock\|patch("`
  devolve dois arquivos, e nenhum é de Fase 6: `test_telas.py` e
  `test_contract_rules.py:143,158`, este último trocando o *validador* para provar
  que o adaptador reprova. Nenhuma asserção de emissão é sobre espião — as duas
  suítes de emissão afirmam sobre o evento que foi para o store.
- **`skip`/`xfail` sem justificativa** — 0 skips na execução. Os `skipIf` da árvore
  são todos de Postgres opcional e todos rodaram (serviços ativos).
- **teste dirigido por seed em vez de por identidade** — não se aplica à fase.
- **teste que compara a tabela consigo mesma** — `test_derivacao_das_nove_siglas.py`
  foi o candidato óbvio, e ele evita: as entradas são as três colunas de condição
  publicadas em `03` §3.0 e a operação é a conjunção de `00` §3.2. Editar `Resultado`
  sem editar as colunas reprova.
- **guarda testada contra objeto que não é o de produção** — foi um B1 anterior desta
  fase, e está fechado: `tests/test_api_emissao_pela_rota.py:123-150` constrói a
  `Superficie` real por `carregar()` e passa por `montar`, com o controle positivo ao
  lado.
- **negativo sem controle positivo** — verifiquei um a um: `check_insumo_de_metrica_probes`
  tem o consumidor que só anota; `check_fabrica_liga_emissor_probes` tem "superfície
  sem `emite` não exige produtor"; `check_hooks_com_emissor_probes` tem o eixo da
  vacuidade nas duas raízes; `_verify_completude` tem o apenas-manifesto.
- **cobertura por lista escrita no teste** — as nove rotas, as nove siglas e os
  `event_type` esperados são todos **derivados** da superfície, do catálogo real ou
  da saída dos computadores.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **As duas provas de container (P4-10)** — não as vi rodar. O que amarra a
   evidência a este commit é o SHA gravado, conferido por `check_provas_de_container.py`
   contra o `HEAD` deste checkout, e os 13 eixos do probe (ausência, SHA divergente,
   verde-e-mudo, versionado) rodaram verdes aqui.
2. **A prova do seed completo (M2)** — mesma forma e mesmo limite. `MEDIDA` neste
   worktree pelo lançador, SHA conferido, 9 eixos do probe verdes.
3. **`GET /audit/grade-changes` contra Postgres real emitindo o evento** — a emissão é
   provada com repositório-duplo de um método. A consulta SQL é exercitada em
   `tests/test_trilha_de_auditoria.py`, mas os dois caminhos não se cruzam num teste
   só. O limite está declarado no cabeçalho de `tests/test_api_emissao_pela_rota.py:22-31`,
   com argumento que aceito.
4. **Os passos de CI que não rodam aqui** — build de imagem, instalação em `/tmp`,
   `docker compose up` com as duas APIs. Li o diff de `.github/workflows/invariants.yml`
   (materialização do pack antes do `up`; contagem de contratos agora derivada do
   diretório em vez de `== 6`) e não os executei.
5. **Se as violações plantadas de `docs/progress/fase_6.md:391-400` de fato deixam os
   testes vermelhos** — ver [L3]. Não há artefato na árvore que eu possa executar.
6. **O comportamento sob um pack de 4 h** — é entregável e critério da Fase 7 (T12).
   O laço de verificação foi exercitado com packs de teste, não com carga real.
7. **Se `SemGramaticaTemporal` [M2] morde na prática** — verifiquei que nada emite
   `fact_materialized` hoje (`grep` sobre `range-core/` e `domains/`), logo o ramo é
   inalcançável neste commit. Não posso afirmar o que acontece quando o produtor
   existir; por desenho, ele bate na exceção, e a decisão da gramática acontece ali.
8. **Deploy (`05` §6)** — bind em `127.0.0.1`, túnel, portas não publicadas em
   produção. Fora do escopo desta fase e não exercitado.
```
