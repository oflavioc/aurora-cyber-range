# Auditoria de checkpoint - 2026-08-23T00:28:17.127520+00:00

- fase: **6**
- commit auditado: `ddb1d87b225afb9d76f627ad89084db475dfe7ea`
- session_id: `828c9730-538c-4a72-aff3-65b68115342b`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have everything I need. Full analysis complete.

# AUDITORIA — FASE 6

## VEREDITO: **FAIL**

## FINDINGS

### BLOCKER

- **[B1] O commit da fase quebra o DEMO ponta a ponta: token de domínio emitido sem `persona` recebe 401 na primeira chamada à `academus-api`**

  **Arquivo:** `scripts/demo_fase4.py:70`, `:139`, `:170` (consumidor) × `domains/academus/api/tokens.py:67` e `domains/academus/api/auth.py:169-176` (verificador)

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §Fase 4, item 1 da DoD — *"a sequência do DEMO ponta a ponta, sem intervenção manual"* — que `scripts/check_provas_de_container.py` (P4-10) mantém como porta permanente de regressão. Também `00_MASTER_SPEC.md` §Definition of Done: *"Uma fase só está concluída quando todos os itens da Definition of Done passam"*.

  **Evidência:** — observada, não suposta:

  `python scripts/check_provas_de_container.py` sai **rc=1**. Não é divergência de SHA (o arquivo está amarrado a `ddb1d87`, e o verificador aceitou o vínculo); é a prova substantiva reprovando:

  ```
  [e] a prova `demo` REPROVOU com rc=1.
        item 1 da DoD - a sequencia do DEMO ponta a ponta, sem intervencao manual
  --- prova `demo`  (rc=1)  ... python scripts/demo_fase4.py
  DEMO INTERROMPIDO: matricula antes do inject respondeu 401: b'{"detail":"autenticacao exigida"}'
  --- prova `reinicio`  (rc=0)  ...
  ```

  Cadeia causal fechada, lida no código deste checkout:

  1. `scripts/demo_fase4.py:70` importa `from range_core.api import tokens` — o emissor do **núcleo**, cujo `verify` exige `["sub", "role", "exp"]` (`range-core/api/tokens.py:227`). Ele **não emite `persona`**.
  2. `scripts/demo_fase4.py:170` assina com esse emissor (`tokens.issue("A-1001", "aluno", secret=segredo)`) e envia para `{ACADEMUS}/enrollment` (`:167`). O mesmo vale para `token_do_aluno` (`:139`), reusado em `:220` e `:258`.
  3. Este commit passou a `academus-api` a verificar com o emissor **próprio**, cujo `EXIGIDAS = ["sub", "role", "persona", "exp"]` (`domains/academus/api/tokens.py:67`) e cujo docstring declara *"**Não há default**"* para `persona` (`:51`).
  4. `domains/academus/api/auth.py:170` chama esse `verify`; token sem `persona` levanta `TokenInvalid` → `HTTPException(401, "autenticacao exigida")` (`:172-176`).

  O DEMO morre no **primeiro** passo funcional ("a matrícula funciona ANTES", `:164-173`), antes de exercitar degradação, rollback ou restauração.

  **Por que a suíte não pegou:** os testes foram atualizados para o contrato novo e o script não. `tests/test_api_degradacao.py:188-195` passa a persona explicitamente (`emitir_token(sub, papel, PERSONA)`, com `PERSONA = "ti"` em `:87`), pelo emissor de domínio. Os 716 testes ficam verdes exercitando o caminho corrigido, enquanto o único artefato que exercita a montagem real ponta a ponta continua no caminho antigo. O commit atualizou o consumidor de teste e deixou o consumidor de produção para trás.

  **Por que é blocker:** o item 1 da DoD da Fase 4 é gate permanente e está **reprovando por defeito deste commit**, não por ausência de ambiente. A stack sobe, a `academus-api` responde, e nenhuma matrícula do DEMO autentica — o roteiro de facilitação que a Fase 4 entregou está inoperante em `HEAD`. Além disso o commit `ddb1d87` se intitula *"a persona entra pelo token"*: a mudança de contrato foi propagada aos testes e não ao chamador real, que é precisamente a classe de defeito que a prova de container existe para não deixar passar em silêncio.

### HIGH
Nenhum encontrado.

### MEDIUM
Nenhum encontrado.

### LOW
Nenhum encontrado.

## DoD — item a item

### Fase 6 (`07_IMPLEMENTATION_PHASES.md` §Fase 6)

| Item | Status | Evidência |
|---|---|---|
| 1. Consultar trilha com filtro de período emite `audit_query_performed` | PASS | `tests/test_api_emissao_pela_rota.py` — exercita a **rota real** por `TestClient` com emissor real e afirma sobre o evento no store; o cabeçalho (`:7-20`) registra que a versão anterior chamava `registrar_consulta` direto e "apagar o corpo da rota deixava a suíte verde" |
| 2. Cada par declaração × verificação disparável e testado | PASS | `tests/test_metrics_verificacao.py:124-157` (`test_as_tres_siglas_saem_sempre`, TTCV/TTRV, `test_um_predicado_nao_arrasta_o_outro`) |
| 3. Desconto pela **união**, nunca pela soma | PASS | `tests/test_metrics_epoch.py:114-131` afirma união **< soma** explicitamente; `:131` cobre contido-em; `:149` controle positivo disjunto |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | PASS | `tests/test_metrics_epoch.py:219,235`; `tests/test_metrics_verificacao.py:261` |
| 5. Declaração não altera ground truth | PASS | `tests/test_metrics_verificacao.py:173-184`; `tests/test_simulation_state.py:276` (`test_declaracao_NUNCA_move_flag`) |
| 6. Declaração prematura lista eventos incompatíveis no AAR | PASS | `tests/test_aar_timeline.py:129-212`, com `:197` excluindo ação de participante e `:188` excluindo fato anterior |
| 7. Pack sem `verification_predicates` não carrega | PASS | `tests/test_pack_loader.py:464-617` |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | PASS | `tests/test_metrics_verificacao.py:318-422`, incl. `test_o_limiar_vem_do_INSUMO_e_nao_de_constante` (`:407`) e bordas `≤` (`:387`, `:402`) |
| 9. Brier só no escopo revisado; lacuna em separado | PASS | `tests/test_metrics_calibracao.py:93,104,182,188,198` |
| 10. Rubrica ausente ou versão divergente impede carga | PASS | `tests/test_rubrics_library.py:104,116,157` |
| 11. Divergência ≥ 2 pontos gera alerta | PASS | `tests/test_aar_timeline.py:299-340`, com `:308` (um ponto não alerta) e `:314` (extremos, não consecutivos) |
| 12. Nove siglas classificadas pelo critério de `00` §3.2 | PASS | `tests/test_derivacao_das_nove_siglas.py`; `tests/test_aar_timeline.py:387-401` |
| 13. Todo `event_type` tem `metric_side`; quatro lados disjuntos | PASS | `range-core/engine/loader/contract_rules.py:123-160`; probes plantam os 4 eixos — `scripts/check_contract_examples_probes.py:124-143`, rc=0 |
| 14. Folha de predicado obedece à conjunção de `09` §4.0, com 3 violações + controle positivo | PASS | `tests/test_verificacao.py`, `tests/test_laco_de_verificacao.py`; `check_contract_examples.py` rc=0 (88 negativos) |
| 15. Cada computador recebe só o seu lado; só o AAR tem as duas metades | PASS | `scripts/check_insumo_de_metrica.py` rc=0 — 3 tipos próprios, ponto único de montagem `range-core/metrics/insumo.py::monta`, nenhum construtor invocado em `range-core` nem `domains` |
| 16. Desconto/exclusão/start são cálculo do consumidor, não recorte na montagem | PASS | `tests/test_metrics_epoch.py:261-272`; `tests/test_metrics_insumo.py:106` |
| 17. `inject_fired` emite os marcadores de start, com teste de emissão | PASS | `tests/test_inject_engine.py:760` (`test_todo_inject_fired_carrega_os_dois_marcadores`) |

### Itens gated pelas provas de container

| Item | Status | Evidência |
|---|---|---|
| Fase 4 item 1 — DEMO ponta a ponta | **NÃO VERIFICADO / REPROVADO** | `check_provas_de_container.py` rc=1; prova `demo` rc=1 com 401. Ver **B1** — aqui não é silêncio, é defeito positivo e diagnosticado |
| Fase 4 item 4 — reinício de container | **NÃO VERIFICADO** | A prova `reinicio` isolada saiu rc=0, mas o verificador agregado recusa; pela regra do lançador o item não sobe a PASS enquanto ele recusar |

### Invariantes arquiteturais (`CLAUDE.md` §Invariantes)

| Invariante | Status | Evidência |
|---|---|---|
| `range-core/` não importa `domains/` | PASS | `.claude/hooks/check_architecture.py` rc=0 |
| Nenhum literal de flag fora dos geradores | PASS | `tools/check_contract_literals.py` rc=0 |
| Nenhum `event_type` fora do catálogo | PASS | `check_api_surface.py` rc=0 — "33 `event_type` no catalogo, e todo `emite` sai dele" |
| Nenhum evento carrega `objective_ids` | PASS | `check_architecture.py` rc=0 |

### Prova do seed completo (M2 da Fase 5)

| Item | Status | Evidência |
|---|---|---|
| Seed completo, medido neste worktree | PASS | `check_prova_do_seed.py`: **MEDIDA** sobre `ddb1d87b225a`, SHA confere com este checkout. 3.543.783 linhas; 141,6 s e 150,6 s contra orçamento de 300 s; 20 tabelas com SHA-256 idêntico nas duas cargas |

## TESTES QUE NÃO PROVAM O REQUISITO

**Nenhum encontrado** — e isso é afirmação medida, não ausência de procura.

Procurei especificamente as classes que me cabem e encontrei o oposto: os testes desta fase são incomumente resistentes ao atalho.

- `tests/test_metrics_epoch.py:106-131` afirma união **e** soma, com a soma calculada ao lado e declarada diferente. Uma implementação que somasse passaria nos casos disjuntos e é reprovada aqui. Há controle positivo (`:149`) que impede uma `uniao` degenerada que fundisse tudo de passar.
- `tests/test_metrics_verificacao.py:407` afirma que o limiar vem do **insumo**, não de constante — fechando a forma em que TTIV "funciona" por número embutido.
- `tests/test_api_emissao_pela_rota.py` afirma sobre o **evento no store**, não sobre espião nem sobre assinatura, e o cabeçalho registra a medição que motivou a troca ("apagar o corpo da rota deixava a suíte inteira verde").
- `scripts/check_insumo_de_metrica_probes.py` e `check_fabrica_liga_emissor_probes.py` carregam controle positivo além dos negativos, impedindo um verificador que reprovasse tudo de passar.
- Verificadores desta fase imprimem o próprio limite ("O que isto NAO prova: que alguma ROTA chame o emissor"), o que é o contrário de sobredeclaração.
- **Zero skips**: a suíte reportou `OK` puro em 716 testes, sem `(skipped=N)` — os testes que dependem de Postgres rodaram de fato.

O defeito do B1 não é um teste que mente. É o inverso: **os testes foram corrigidos e o chamador de produção não**. A lacuna é de cobertura — nenhum teste exercita o caminho em que o emissor do núcleo assina para a superfície de domínio —, e é exatamente essa lacuna que a prova de container existe para cobrir. O aparato funcionou; ele pegou o defeito.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **As provas de container, executando.** Rodaram na máquina do operador; eu vi a saída gravada e conferi o vínculo com `ddb1d87` pelo SHA. Não vi a stack subir. O que afirmo sobre o B1 vem da saída gravada mais a leitura do código deste checkout, e a cadeia causal (401 ← `verify` exigindo `persona` ← emissor do núcleo sem `persona`) foi fechada por leitura, não por reexecução do DEMO.
- **A medição do seed.** Idem: `MEDIDA` neste worktree pelo lançador, SHA conferido, mas eu não a vi rodar. Os números (141,6 s / 150,6 s / 3.543.783 linhas) são os gravados.
- **Fase 4 item 4 (reinício de container).** A prova isolada saiu rc=0, mas como o verificador agregado recusa, não elevo o item a PASS. Se o B1 for corrigido, este item provavelmente sobe junto — mas isso é previsão, não verificação.
- **A consulta SQL da trilha de auditoria.** `tests/test_api_emissao_pela_rota.py` testa a instrumentação com repositório duplo, por decisão declarada no próprio arquivo (`:22-29`). O caminho SQL real é exercitado onde a stack existe — isto é, pelas provas de container, que estão reprovando. A emissão está provada; a consulta em Postgres, não, nesta rodada.
- **Cobertura semântica das rubricas BARS.** Verifiquei estrutura (9 competências, 5 níveis, escala, versão) e as recusas. Se o **texto** de cada âncora descreve comportamento observável e discriminante é juízo de conteúdo pedagógico, fora do que consigo verificar por execução.
- **Duas rotas `planejadas`.** `check_api_surface.py` reporta "34 rotas declaradas — 32 implementadas e conferidas por AST, 2 planejadas". Não investiguei se alguma das duas pertence a esta fase.

---

**Um BLOCKER ⇒ FAIL.** Sem ponderação: o resto da fase está bem construído — e o B1 é uma linha de import mais duas chamadas, não um defeito de desenho —, mas a regra não admite compensação, e o item da DoD que ele derruba é gate permanente.
