# Auditoria de checkpoint - 2026-09-17T17:00:14.037212+00:00

- fase: **8**
- commit auditado: `722a8f506485b1654f81dbba873b4de5ef1a013c`
- session_id: `c34baa4f-7b6f-4b2e-a764-02ac73f03092`
- modo: `headless`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have enough to render the verdict.

# AURORA CYBER RANGE — AUDITORIA — FASE 8

## VEREDITO: **FAIL**

## FINDINGS

### BLOCKER

**[B1] A suíte está vermelha no commit candidato — 2 erros causados pela própria fase**

- **Arquivo:** `domains/academus/seed/gabarito.py:172` (`"facts": linha_a.facts(seed) + fatos`) → quebra `tests/test_gabarito.py:284` e `tests/test_gabarito.py:354`
- **Requisito violado:** `CLAUDE.md` §Fluxo por fase ("rodar testes de aceitação" antes do commit candidato); R10 §1 (nunca enfraquecer/deixar gate vermelho); `07` §Fase 8 DoD
- **Evidência:** execução real, `.aurora-audit/venv/Scripts/python -m unittest discover -s tests` neste worktree, com Postgres/Redis ativos:
  ```
  Ran 924 tests in 101.254s
  FAILED (errors=2)
  ERROR: test_os_fatos_sao_DISTINTOS_entre_os_dois_seeds — KeyError: 'dest'
  ERROR: test_o_MAPEAMENTO_caso_para_fato_muda_com_o_seed — KeyError: 'dest'
  ```
  Nenhum `skipped` reportado — os testes de serviço rodaram. **Causa diagnosticada (R2 §3), e não atribuída por suposição:** `gerar` passou a devolver `linha_a.facts(seed) + fatos`; dos três fatos de `linha_a.py`, apenas o primeiro tem `dest` (`linha_a.py:96`); os de `privilege_escalation` (:107) e `exfiltration` (:115) não têm. `tests/test_gabarito.py` itera **todos** os fatos lendo `f["dest"]`. `git log c38f890..722a8f5 -- tests/test_gabarito.py` retorna **vazio**: o produtor mudou e o consumidor não foi acompanhado.
- **Por que é blocker:** os dois testes destruídos são exatamente os que provam que o gabarito **sai do `RANDOM_SEED`** — a propriedade que sustenta a decisão de manter `scenarios/` fora do Git (`CLAUDE.md` §Ground truth: *"gabarito é quais casos, e eles saem do `RANDOM_SEED`"*, INV-9). Eles não estão falhando com asserção: estão **erroando antes de asserir**, então a determinismo-por-seed passou a ser **não provada**, não apenas medida de outro jeito. `docs/progress/fase_8.md:253` afirma "Suíte 858 → 868" e lista o P7-10 como `RESOLVIDA` "Provado"; a suíte real do candidato é 924 e vermelha.

---

**[B2] Os quatro filtros do Console de investigação são gravados no evento e nunca aplicados à consulta**

- **Arquivo:** `domains/academus/api/app.py:283` (`repositorio.alteracoes_de_nota(inicio, fim, agrupar)`) × `app.py:292-295` (os filtros indo para o emissor); `domains/academus/api/repositorio.py:297-298` (assinatura sem os filtros)
- **Requisito violado:** `02_DOMAIN_ACADEMUS.md` §7:124 — *"Console de investigação: consulta à trilha de auditoria com filtros de período, usuário, IP, janela e autorização"*; `00_MASTER_SPEC.md` §3 (camada 2, evidência observável) e §5.7
- **Evidência:** a rota aceita `filter_user`, `filter_ip`, `filter_window`, `filter_authorization` (`app.py:250-253`), os repassa a `emissor.registrar_consulta(...)` (`app.py:292-295`), que os grava no payload de `audit_query_performed` (`emissor.py:133-136`). A consulta SQL recebe apenas `(inicio, fim, agrupar)` — `repositorio.alteracoes_de_nota(self, inicio, fim, agrupar_por_usuario)` não tem parâmetro para eles. O cliente também não filtra, e corretamente: `investigation-console/main.tsx:9-10` — *"pinta as linhas e o `total` que vieram prontos — INV-7"*. **Ninguém filtra.** O comentário de `main.tsx:15-16` é a evidência mais direta: *"Filtro vazio nao vira parametro: mandar `filter_user=\"\"` registraria na trilha um filtro que nao foi aplicado"* — a precaução existe para o filtro vazio, enquanto todo filtro **preenchido** é registrado e igualmente não aplicado.
- **Por que é blocker:** dois danos concretos. (1) Quatro dos cinco filtros que `02` §7 exige do Console não existem — o investigador que filtra por `U-142` recebe **toda** a massa do período, e `02` §6 põe as alterações indevidas dentro de massa de legítimas justamente para que separá-las seja o trabalho que OBJ-03/OBJ-04 medem; a ferramenta que a spec dá à equipe não foi entregue. (2) Pior, o evento afirma falso sobre o mundo: `audit_query_performed` carrega `filter_user: "U-142"` ao lado de um `result_count` que é a contagem da consulta **sem** filtro. Isso é `effect_class: observation` entrando na camada de evidência observável com uma proposição que não ocorreu, e é o insumo que o AAR vai dobrar — a confusão entre camadas de verdade que `00` §3 existe para impedir.

---

**[B3] O item 1 da DoD não tem evidência executável ligando a flag ao Modo "Prova em andamento"**

- **Arquivo:** `domains/academus/api/degradacao.py:230-244` (`ProvaEmAndamento.frame`), `domains/academus/api/app.py:166-202` (`GET /exam/session-status`), `domains/academus/api/repositorio.py:183-206` (`sessoes_de_prova`), `domains/academus/api/processo.py:133-144` (descoberta de `flag_da_prova`)
- **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §Fase 8, item 1 — *"Modo 'Prova em andamento' perde sessões **conforme** `lms_session_drop_rate`"*; R2 §1
- **Evidência:** busca no diretório `tests/` inteiro por `ProvaEmAndamento|sessoes_de_prova|app.state.prova|prova=` → **"No matches found"**. Busca por `exam/session-status` → uma única ocorrência, `tests/test_prova_andamento.py:64`, e ali é `ROTA = "/exam/session-status"`, usada apenas como **string de sal** do `derive_seed` na função pura, nunca como chamada HTTP. Idem `FLAG = ACADEMUS_LMS_SESSION_DROP_RATE` (:65): entra como parâmetro `flag: str` de hashing, jamais como flag lida do estado de simulação. As 14 asserções de `test_prova_andamento.py` exercitam `prova_andamento.frame(seed, rota, flag, sujeitos, **taxa**, minuto)` — com `taxa` **passada como argumento**.
- **Por que é blocker:** a cláusula que o item de DoD cobra é "conforme `lms_session_drop_rate`", e é precisamente o trecho não coberto. Nada prova que `float(estado.flags.get(self.flag) or 0.0)` (`degradacao.py:242`) lê a flag certa, que o handler monta o frame, ou que `processo.criar()` acha a entrada `proporcional` na superfície. O modo de falha é silencioso e alinhado ao `or 0.0`: se a descoberta de `flag_da_prova` (`processo.py:136-144`) devolver `None`, `montar` recebe `prova=None`, o handler cai no ramo `frame = {sessao: ALIVE for sessao in sessoes}` (`app.py:197`) e **o Modo nunca derruba ninguém** — verde na suíte, sala sem impacto. É o padrão "teste que passa sem provar o requisito": um modelo de sobrevivência impecavelmente testado, desconectado do insumo que o requisito nomeia.

### HIGH

**[H1] A prova do exercício de 4 h reprova contra esta árvore, e a fase mexeu no gerador que a alimenta**

- **Arquivo:** artefato lido por `scripts/check_prova_do_exercicio_4h.py`; causa material em `domains/academus/seed/gabarito.py:172` e `domains/academus/seed/linha_a.py`
- **Requisito violado:** `07` §Fase 7 item 9 (reconstrução < 3 s para o exercício de 4 h); `06` T3
- **Evidência:** execução real, rc=1: *"a prova foi gravada sobre a arvore `6cc01ca2cfb36f9fbf5ff948457ace914d63b603` e este checkout e `22f62f2de...`. O codigo que mediu e OUTRO — meca de novo."* As outras duas provas passaram (`check_provas_de_container.py` e `check_prova_do_seed.py`, ambas conferindo `22f62f2db5e9`).
- **Por que é HIGH e não bookkeeping:** o enunciado do lançador diz que os itens 6 e 9 da DoD da Fase 7 *"se decidem por ela, nunca pela transcrição do registro"* — e ela reprova. Não é divergência cosmética de rebase: a Fase 8 alterou `gabarito.gerar` para prefixar `linha_a.facts(seed)` e trocou os `verification_predicates` do pack, de modo que o pack de 4 h que a prova mede **mudou de conteúdo** nesta árvore. O número < 3 s está, hoje, sem objeto medido.

**[H2] A entrada de `pytest` na allowlist do auditor não alcança o interpretador do worktree — quarta ocorrência da classe que o próprio arquivo documenta**

- **Arquivo:** `user-scope/hooks/readonly_bash.py:82` — `rf"^{SAFE_ENV_PREFIX}(pytest|python\s+-m\s+pytest)\b"`, **sem** `{PREFIXO_DO_VENV}`
- **Requisito violado:** `docs/progress/fase_7.md` §P7-12 (aberta, gatilho "esta fase"); a doutrina escrita em `readonly_bash.py:19-30`
- **Evidência:** medido nesta auditoria. `python -m pytest` → `C:\Python314\python.exe: No module named pytest`. `pytest` → rc=127. `.aurora-audit/venv/Scripts/python -m pytest ...` → **BLOQUEADO pela allowlist**. As entradas vizinhas de `unittest` (:99), `range-cli` (:108-109) e `tools/` (:111) **têm** o prefixo; a de `pytest` não. Consegui rodar a suíte apenas pela forma `unittest`.
- **Por que é HIGH:** o cabeçalho do próprio arquivo (:28-30) enumera três ocorrências desta classe — comando ausente (H3, Fase 1), forma ausente (B1, Fase 2), **interpretador inalcançável com a forma presente** (B1, Fase 6) — e escreve: *"Sem este paragrafo escrito, a QUARTA ocorrencia entra por uma quarta porta."* Esta é a quarta, na própria linha que a correção de `range-cli` (M2 da 2ª auditoria da Fase 7) não varreu. Se a suíte do projeto migrar para `pytest`, o auditor volta a julgar por leitura.

### MEDIUM

**[M1] Cinco suítes novas entram sem linha no registro canônico de contagens**

- **Arquivo:** `.claude/verify/expected_suites.json` (`"suites": {}`, vazio) × `tests/test_prova_andamento.py`, `test_console_investigacao.py`, `test_reported_isolation.py`, `test_continuidade.py`, `test_linha_a.py`
- **Requisito violado:** R10 §3 — *"Contagens pinadas vivem no registro canônico... suíte nova entra no registro **no mesmo PR**"*
- **Evidência:** o registro está com `suites`, `heavy` e `visual` todos `{}` enquanto a execução real conta 924 testes. Nenhuma das cinco suítes da fase foi registrada. Condição de fundo é pré-existente (o arquivo não está no diff), mas a obrigação de registrar **no mesmo PR** é desta fase.

**[M2] O registro da fase não tem os itens de DoD com status e evidência**

- **Arquivo:** `docs/progress/fase_8.md` — salta de §2 (linha 39) para §6 (linha 53)
- **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §Registro de progresso — *"gravar `docs/progress/fase_<n>.md` com: resumo técnico, estrutura de diretórios, endpoints criados, migrations, variáveis de ambiente, **itens de DoD com status**, pendências e próxima fase"*
- **Evidência:** §1 traz os quatro itens de DoD como **plano** (linhas 21-26), sem coluna de status nem de evidência. Ausentes: resumo técnico, estrutura de diretórios, endpoints criados, migrations, variáveis de ambiente, próxima fase. `check_progress_consistency.py` passa porque julga a tabela-resumo de pendências, não esta lista.
- **Nota de processo:** a §7 de fechamento é escrita por quem implementou, após o veredito — não a cobro aqui. O que falta e é cobrável no candidato é a **linha de status com evidência por item de DoD**, que é o insumo desta auditoria.

### LOW

Nenhum.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Modo "Prova em andamento" perde sessões conforme `lms_session_drop_rate` | **FALHA** | Modelo de sobrevivência correto e bem testado (`prova_andamento.py`; 14 testes em `test_prova_andamento.py`, verdes). Mas zero cobertura de `ProvaEmAndamento`, `sessoes_de_prova`, `GET /exam/session-status` e da descoberta da flag em `processo.py` — a cláusula "conforme a flag" não tem prova executável. **[B3]** |
| 2 — Console de investigação emite os marcadores automáticos | **FALHA** | O marcador `auto` é emitido e provado pela projeção (`test_console_investigacao.py`, oráculo independente via `objetivos_de`/`project` — bom desenho). Porém o Console entregue não tem 4 dos 5 filtros de `02` §7, e o evento registra filtros não aplicados junto de um `result_count` de outra consulta. **[B2]** |
| 3 — Cada persona vê apenas sua camada `reported` | **PASSA** | `reported.py::project` é conjunção de duas pernas (camada reportável **E** `persona == P`), whitelist e não blocklist. Provado nos dois níveis: projeção pura (`test_reported_isolation.py:182-219`) **e pela rota real com `TestClient`, sobre o payload da API** — `test_rota_isola_entre_personas` (:271), `test_rota_nao_vaza_ground_truth_nem_camadas_superiores` (:262, substring no corpo inteiro), 401 sem token (:277) e com token inválido (:282). Não verifica UI. |
| 4 — As sete ações de continuidade aplicam efeito mecânico e custo | **PASSA** | `continuidade.py` cobre exatamente as sete do enum fechado do contrato, com oráculo independente (`test_a_tabela_cobre_exatamente_as_sete_do_contrato`). Efeito mecânico provado **pelo fold** (`test_cada_acao_aplica_o_efeito_no_estado`, `test_o_fold_escreve_a_flag_nomeada_no_payload`), custo provado não-vazio, negativos presentes (422 fora do enum, 403 persona errada, 401 sem token, `objective_ids` embutido recusado). INV-4 preservado: a tabela de domínio entra por injeção em `montar()`, nomes de flag chegam como dado. |

**Invariantes e segurança — todos verdes, executados nesta árvore:** `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `codegen.py --check`, `check_security_constraints`, `check_synthetic_data`, `check_gabarito_fora_do_git`, `check_banner_de_simulacao` (6 telas), `check_web_sem_derivacao` (22 arquivos, 14 métodos proibidos), `check_api_surface` (37 rotas, 35 implementadas), `check_insumo_de_metrica`, `check_store_read_surface`, `check_fold_authority`, `check_progress_consistency`. Âncora da Fase 8 gravada em `phase_anchors.tsv:50` como `c38f890a...`, idêntica à base desta auditoria. `docs/spec/` intocada pelo diff — `spec_freeze` respeitado.

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_prova_andamento.py` (arquivo inteiro)** — prova propriedades matemáticas de uma função pura que recebe `taxa` como argumento. `ROTA = "/exam/session-status"` (:64) e `FLAG = ACADEMUS_LMS_SESSION_DROP_RATE` (:65) dão ao arquivo a aparência de cobrir rota e flag; ambos são apenas sal de hash. O requisito — perder sessões *conforme a flag* — não é tocado. **[B3]**

2. **`tests/test_console_investigacao.py::RepositorioFalso` (:151)** — `def alteracoes_de_nota(self, inicio, fim, agrupar)`: o duplo tem a assinatura de **três** parâmetros, o que torna estruturalmente impossível ao gate perceber que os quatro filtros não chegam à consulta. O cabeçalho declara o recorte (:141-142, *"a instrumentação é o que está sob teste, não a consulta SQL"*), e a matriz de mutantes M1–M5 (:56-70) enumera cinco mutantes todos sobre **gravação de payload** — nenhum sobre *aplicação* de filtro. O gate é honesto sobre o que mede; o problema é que o item de DoD foi dado por cumprido com ele. **[B2]**

3. **`tests/test_gabarito.py::DoisSeedsDiferentes`** — deixou de provar o que provava: erra com `KeyError` antes de qualquer asserção sobre determinismo por seed. Um teste que erra no setup da asserção não é um teste que falha; é um requisito que parou de ser medido. **[B1]**

## O QUE EU NÃO CONSEGUI VERIFICAR

- **As três provas externas, quanto à execução.** Não vi rodar nenhuma delas. Container e seed conferem o hash de árvore `22f62f2db5e9` contra este checkout e passam; a de 4 h **reprova** e está reportada em **[H1]**. Vale integralmente a limitação P7-2 declarada no enunciado: a prova amarra a **árvore rastreada**, não o commit, e `scenarios/` não entra no hash.
- **`.claude/verify/`** — `gen_pins.py --check`, `check_baseline.py`, `check_boundary.py`, `check_state.py`, `check_tdd.py`, `run.sh` e `compliance-audit.sh` estão **fora da allowlist do auditor** (ela admite `python tools/` e `python scripts/`, não `python .claude/verify/`). Não pude conferir a coerência do registry de pins (R8 §1) nem a seção `deny` do boundary, embora `pins.json` esteja no diff com 89 linhas alteradas. O hook do `UserPromptSubmit` avisa a cada prompt que nenhum pipeline verde está registrado nesta máquina; não pude executá-lo para resolver isso. Adjacente ao **[H2]**.
- **`pytest`** — não executável neste worktree (**[H2]**). Rodei a suíte pela forma canônica `unittest`, que é a do CI. Se houver teste coletável só por `pytest`, ele não entrou nos 924.
- **Comportamento em navegador** das três telas novas (`exam-mode`, `investigation-console`, `persona-panel`). Li o fonte e rodei os gates estáticos (`check_banner_de_simulacao`, `check_web_sem_derivacao`); não executei `npm test`/`tsc`. A leitura de `investigation-console/main.tsx` foi suficiente para concluir que o cliente **não** filtra — o que fortalece **[B2]** em vez de depender de execução.
- **A campanha de mutantes** (R3 §5) — os mutantes estão descritos em prosa nos cabeçalhos dos gates e, em `test_prova_andamento.py`, há uma classe `MatrizDeMutantes` que os executa. Não verifiquei se a campanha foi re-executada após o green, como R3 §5 exige.
- **Regressão contra a base.** Não rodei a suíte em `c38f890` para a prova de não-regressão canônica de R2 §3. Para **[B1]** isso não foi necessário: a cadeia causal é legível no diff (`gabarito.py:172` prefixa fatos sem `dest`; `test_gabarito.py` não foi tocado pela fase) e é suficiente para atribuir a causa ao código da fase, não ao ambiente.

---

**Três BLOCKERs ⇒ FAIL**, sem ponderação. Os itens 3 e 4 da DoD estão bem construídos e bem provados — a isolação por persona verificada sobre o payload da API, e o efeito de continuidade verificado pelo fold, são exatamente a forma certa. Os itens 1 e 2 entregaram a metade que dava para testar isoladamente e deixaram sem prova (item 1) ou sem implementação (item 2) a metade que o requisito nomeia; e a fase deixou a suíte vermelha por um consumidor não varrido quando o produtor mudou.
