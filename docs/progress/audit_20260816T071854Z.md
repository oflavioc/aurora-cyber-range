# Auditoria de checkpoint - 2026-08-16T07:18:54.304551+00:00

- fase: **2**
- commit auditado: `b304c195808f686c1491041dbdca86e4f15f30a4`
- session_id: `4745fae7-1b46-4279-a48e-aaf9ad25eca0`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have enough to render a verdict. Writing the report.

```text
# AUDITORIA — FASE 2

## VEREDITO: FAIL
```

## FINDINGS

### BLOCKER

**[B1] A suíte de testes da fase é inexecutável pelo mecanismo de auditoria; 7 dos 9 itens de DoD não têm prova verificável**

- **Arquivo:** `user-scope/hooks/readonly_bash.py:21` (allowlist), contra `.github/workflows/invariants.yml:301`
- **Requisito violado:** `CLAUDE.md` §"Fluxo por fase"; regra versionada em `user-scope/hooks/readonly_bash.py:50-55` — *"Script novo que precise ser executado pelo auditor entra aqui por nome, **no commit que o cria**"*
- **Evidência observada:** a allowlist do auditor admite `pytest|python -m pytest` (linha 21). A suíte da Fase 2 é **unittest**: `.github/workflows/invariants.yml:301` roda `python -m unittest discover -s tests`. Executei `python -m unittest discover -s tests` e recebi `BLOQUEADO: comando fora da allowlist do auditor`. Executei `python -m pytest` e recebi `No module named pytest` — pytest não é dependência do projeto (`pyproject.toml`, `constraints.txt`). Não há venv no worktree (`Glob **/pyvenv.cfg` e `**/pytest.exe`: nenhum resultado). O diff `main...HEAD` não toca `user-scope/` em nenhum dos 52 arquivos.
- **Alcance:** os mesmos 4 verificadores novos da fase também ficam inexecutáveis — `scripts/check_store_read_surface.py`, `check_store_read_surface_probes.py`, `check_core_contract_imports.py`, `check_core_contract_imports_probes.py` são steps de CI (`invariants.yml:71-89`) e nenhum está na allowlist, que só nomeia `phase0_negative_tests|check_contract_examples|check_contract_examples_probes|check_spec_examples|check_spec_examples_probes|check_progress_consistency` (linhas 61-65). `tests/mutation_harness.py` — a prova negativa central alegada por `b304c19` — idem.
- **Por que é blocker:** a tabela de DoD em `docs/progress/fase_2.md:1055-1065` cita, para 7 dos 9 itens, exclusivamente nomes de testes unittest. Nenhum deles pode ser executado nesta auditoria. O resultado é que **a fase se autocertifica**: o único registro de que os testes passam é a afirmação do próprio commit. Isto é exatamente a degradação que o hook nomeia em `:44-48` ao justificar as entradas da Fase 1 — *"o auditor não pode executar o mecanismo que fecha o item e volta a avaliar por leitura de código — que é exatamente o modo de auditar que o item existe para não aceitar"* (H3 da segunda auditoria da Fase 1). A classe reincide, na fase que criou a primeira suíte de testes real do projeto.

### HIGH

**[H1] O branch da fase altera `docs/spec/` e código no mesmo diff contra `main`**

- **Arquivo:** `docs/spec/{00,01,03,06,07,09}` + 26 arquivos de código, no diff `main...b304c19`
- **Requisito violado:** `CLAUDE.md` §"A especificação é imutável durante a implementação" — *"Alterar spec e código no mesmo PR é proibido"*; mecanizado em `.github/workflows/invariants.yml:163-171`
- **Evidência observada:** `git diff --name-only main...HEAD -- docs/spec/` devolve 6 arquivos; o mesmo diff sobre o conjunto `CODE` do job `spec_freeze` devolve 26. Com `SPEC>0` e `CODE>0`, o job sai `1` com *"PR altera spec e codigo no mesmo PR. Separe as mudancas."* Os três `spec-change` estão **corretos isoladamente** — verifiquei por `git show --stat`: `a3aded5` (3 arquivos, só `docs/spec/`), `c160522` (5, só `docs/spec/`), `f52e924` (2, só `docs/spec/`) —, mas foram mergeados na branch da fase (`72f27f0`, `a4ef371`) em vez de em `main`, e `main` não os contém.
- **Por que é HIGH e não BLOCKER:** a disciplina de autoria foi respeitada e o defeito é de integração, não de conteúdo — some ao mergear os três `spec-change` em `main` antes do PR da fase. Mas, no estado do commit candidato, o PR da Fase 2 é **immergível pelo seu próprio gate**.

### MEDIUM

Nenhum.

### LOW

**[L1] Docstring de `rollback` contradiz a implementação que documenta**

- **Arquivo:** `range-core/engine/inject_engine.py:438`
- **Evidência:** a docstring afirma ``technical_failure` e RECUSADO — ver o cabecalho do modulo e a P2-4`. O código imediatamente abaixo, `:462-463`, **aceita** e grava `payload[FROZEN_INTERVAL] = self._frozen_interval(ancora)`. O cabeçalho do módulo (`:62-65`) está correto e usa o pretérito (*"recusava… Ele existe desde a P2-4"*); a docstring do método ficou no presente.
- **Nota:** não é desvio de spec — é contradição interna verificável, da classe que o próprio projeto rastreia (`invariants.yml:156-159`, *"documentação que sobrevive à mudança e a contradiz"*).

**[L2] Docstring de `bench_reconstruction.py` cita a forma revogada do item 8**

- **Arquivo:** `scripts/bench_reconstruction.py:2` e `:6-7`
- **Evidência:** *"mede o item 8 da DoD: reconstrucao completa em menos de 3 s"* e *"«reconstrucao … deve completar em < 3 s para exercicio de 4 h»"*. Essa é a redação **realocada** para as Fases 7 e 9 pelo `spec-change` `item-8-volume-de-4h`. `06_ACCEPTANCE_TESTS.md:32` é explícito: a curva *"não prova o critério de 4 h… Chamar uma de prova da outra seria atestação com um passo intermediário"*. A tabela de DoD (`fase_2.md:1064`) e a §3.12 fazem a distinção corretamente; só o script ficou para trás.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Quatro marcas em todo evento | **NÃO VERIFICADO** | Prova alegada é `test_event_store.Carimbo.*` (unittest, inexecutável — B1). Contrato confere: `contracts/events.schema.yaml` valida por `check_contract_examples.py` (rc=0, 10 positivos / 66 negativos) |
| 2 — `RANDOM_SEED` por código do `range-core` | **PARCIAL** | `range-core/determinism.py` existe (193 linhas) e é código do core, não atestação. Comportamento (3 recusas, 2 fontes) depende de `tests/test_determinism.py` — inexecutável (B1) |
| 3 — PAUSAR congela clock e bloqueia agendado | **NÃO VERIFICADO** | `test_exercise_clock.Pausar.*`, `test_inject_engine.PausaBloqueiaAgendado` — inexecutáveis (B1) |
| 4 — A01 duas vezes → projeção idêntica | **NÃO VERIFICADO** | `test_simulation_state.Propriedades.test_p3_*` + prova negativa por mutação — inexecutáveis (B1) |
| 5 — Rollback grava, incrementa epoch, não apaga | **NÃO VERIFICADO** | Duas das três metades vivem em `test_event_store_postgres.py`, que faz `@unittest.skipIf(_URL is None)` (`:49,74,118,179`). Sem `AURORA_TEST_DATABASE_URL` o teste **pula**, mesmo que executável |
| 6 — `participant_action` de epoch anterior legível e marcada | **NÃO VERIFICADO** | Idem; "sobrevive ao reinício" também é teste de Postgres |
| 7 — `technical_failure` registra extremos em `exercise_timestamp` | **PARCIAL — a metade de contrato PASSA** | `$defs/frozen_interval` existe (`events.schema.yaml:436-448`), `technical_failure` exige o intervalo e os outros três motivos não o admitem; 4 fixtures negativas, incluindo `:838` *"technical_failure sem frozen_interval: o item 7 sumiria em silêncio"*. Validado por `check_contract_examples.py` rc=0. `_frozen_interval` implementado (`inject_engine.py:480`). Comportamento não verificado (B1) |
| 8 — Curva volume → tempo com ponto de quebra, máquina/data/stack | **PASS** | `scripts/bench_reconstruction.py:201-202` imprime `platform.platform()`, versão do Python, Postgres, psycopg e revisão de migration — gerados, não anotados à mão. Ponto de quebra medido em ~150 mil eventos (2,874 s), estouro em 200 mil (4,304 s), formas realista e patológica (`fase_2.md:575`, `:1064`). Forma correta pós-`spec-change`, com a distinção de `06:32` respeitada na DoD |
| 9 — Flag não declarada impede boot com mensagem clara | **NÃO VERIFICADO** | `test_pack_loader.FlagNaoDeclarada` (4 asserções) — inexecutável (B1) |

**Invariantes arquiteturais — todos PASS, por execução:**

| Verificador | rc |
|---|---|
| `tools/check_core_boundary.py` | 0 |
| `tools/check_contract_literals.py` | 0 |
| `tools/check_event_envelope.py` | 0 |
| `tools/codegen.py --check` | 0 |
| `tools/check_security_constraints.py` | 0 |
| `tools/check_synthetic_data.py` | 0 |
| `scripts/check_contract_examples.py` | 0 |
| `scripts/check_spec_examples.py` | 0 |
| `scripts/check_progress_consistency.py` | 0 |

**Catálogo de eventos — PASS.** Conferi `contracts/events.schema.yaml:510-561` contra `09_EVENT_MODEL.md` §4.1 entrada a entrada: **33 tipos** (3 `ground_truth`, 3 `observable_evidence`, 17 `participant_action`, 3 `evaluator_assessment`, 7 `facilitation`), `effect_class` idêntico ao normativo em todos, incluindo os seis casos justificados — `verification_predicate_satisfied: machine`, `decision_made: declaration`, `communication_submitted`/`regulatory_notice_submitted: state_effect`, `fact_materialized`/`attack_stage_reached: state_effect`. `exercise_resumed` e `separate_incident_declared` presentes.

**`objective_ids` — PASS.** Proibido em três níveis no contrato (`:129,134,148`), com duas fixtures negativas incluindo a de payload aninhado (`:758` *"a proibicao de 09 secao 1.2 nao e so na raiz"*).

**Segurança — nada encontrado.** `check_security_constraints.py` e `check_synthetic_data.py` rc=0. Não há evidência gerada nesta fase; nenhum IOC, anexo ou dado fora de faixa de documentação no diff.

## TESTES QUE NÃO PROVAM O REQUISITO

Não pude executar nenhum teste da fase, então **não afirmo que os testes provam nem que deixam de provar**. Duas observações estruturais, obtidas por leitura:

1. **Os skips de Postgres são justificados, mas deslocam a prova.** `tests/test_event_store_postgres.py:49,74,118,179` usam `@unittest.skipIf(_URL is None, RAZAO)` — com razão declarada, o que satisfaz a regra de skip. Porém os itens 5 e 6 da DoD apoiam suas metades de *"grava"*, *"sem apagar"* e *"sobrevive ao reinício"* **exclusivamente** nesses testes. Num ambiente sem `AURORA_TEST_DATABASE_URL`, a suíte fica verde com esses itens não exercitados. O CI cobre isso (serviço Postgres + `alembic upgrade head`, `invariants.yml:220-290`), e o comentário `:288` mostra que a armadilha foi antecipada — mas a garantia depende inteiramente do CI, não da suíte.
2. **A prova negativa existe e é do tipo certo.** `tests/mutation_harness.py:167-191` monta casos que exigem que cada mutação seja detectada **pelos testes certos** (`esperados: set[str]`), não apenas que algo fique vermelho, e `:181` verifica que a suíte está verde sem mutação — que é a guarda contra o harness que "detecta" por já estar quebrado. É o desenho correto. Não pude executá-lo.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **A suíte inteira** (`tests/`, 11 arquivos, ~3.400 linhas) — B1. Isto é o centro da auditoria, não uma nota de rodapé: 7 dos 9 itens de DoD dependem dela.
- **O harness de mutação e as 16 mutações novas alegadas por `b304c19`** — inexecutável. A alegação do commit permanece não confirmada.
- **Os 4 verificadores novos da fase** (`check_store_read_surface`, `check_core_contract_imports` e seus probes) — fora da allowlist. Não pude confirmar que a superfície de leitura do store e a whitelist de imports do core realmente se comportam como declarado; só que os steps existem no CI.
- **`scripts/demo_fase2.py`** — não está na allowlist. O DEMO SCRIPT da fase não foi executado por mim.
- **`scripts/bench_reconstruction.py`** — exige Postgres e não está na allowlist. Os números de `fase_2.md` §3.8 (~150 mil eventos, 2,874 s) foram lidos, não reproduzidos. O item 8 está marcado PASS **pela forma** — o script comprovadamente gera máquina/data/stack por código e a curva tem ponto de quebra declarado —, não pela reprodução da medição.
- **Se os três `spec-change` tiveram aprovação humana prévia**, como `CLAUDE.md` exige. Verifiquei a forma dos commits, não o processo de aprovação; PRs e revisões não são inspecionáveis daqui.
- **Estado real de `main`** — `git branch`/`for-each-ref` sobre refs remotos não foi consultado; concluí que `main` não contém os `spec-change` a partir de `git log main..HEAD`, que os lista.

---

**Resumo.** A engenharia desta fase é de qualidade alta e visível: o catálogo bate com a spec entrada a entrada, `objective_ids` está fechado inclusive em payload aninhado, o `frozen_interval` tem schema e fixtures negativas, o item 8 respeita a distinção que o `spec-change` criou, e os nove verificadores executáveis passam. **Nada disso muda o veredito.** A Fase 2 é a primeira a produzir uma suíte de testes real e não estendeu o mecanismo de auditoria para executá-la — obrigação escrita no próprio arquivo que a define e criada em resposta a um achado anterior desta mesma linhagem. O efeito é que a fase cujo DoD é quase todo comportamental chega ao checkpoint com o comportamento verificável apenas pela palavra de quem a implementou. Corrigido B1, a maior parte desta tabela provavelmente vira PASS por execução — mas "provavelmente" é precisamente o que um checkpoint não pode emitir.
