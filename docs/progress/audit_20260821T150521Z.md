# Auditoria de checkpoint - 2026-08-21T15:05:21.144463+00:00

- fase: **6**
- commit auditado: `5fd6f72c8f1da577e25327ac28dd624951019d59`
- session_id: `35577648-c953-40e3-b591-fc0106a2605c`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have completed the audit. Here is the report.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] Pack sem `verification_predicates` carrega — o item de DoD não tem mecanismo
  Arquivo: `range-core/engine/loader/pack_loader.py:346-349`, `:428-457`;
           `contracts/scenario.schema.v2.yaml:24-27`;
           `range-core/engine/verificacao.py:266`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §Fase 6, item 7 da DoD
                     ("Pack sem `verification_predicates` não carrega");
                     `06_ACCEPTANCE_TESTS.md` T10, 5º critério (mesma frase)
  Evidência:
    - `x-aurora-documents` (`scenario.schema.v2.yaml:24-27`) mapeia apenas
      `manifest.yaml`, `injects.yaml` e `branches.yaml`. `ground_truth.yaml`
      **não está no mapa**.
    - `_verify_schema` (`pack_loader.py:442-444`) e `_verify_rules`
      (`pack_loader.py:546-548`) iteram exatamente esse mapa. Logo o loader de
      produção **nunca valida `ground_truth.yaml`** contra
      `contracts/ground_truth.schema.yaml`, que é onde vive
      `required: [facts, verification_predicates]` (`ground_truth.schema.yaml:24`).
    - `pack_loader.py:346-349` resolve o campo com
      `(documentos.get("ground_truth.yaml") or {}).get("verification_predicates") or {}`
      — ausência vira `{}` em silêncio.
    - `tests/fixtures/pack_minimo/` contém `manifest.yaml`, `injects.yaml` e
      `objectives.yaml` e **nenhum `ground_truth.yaml`**; `test_carrega_o_pack_minimo`
      (`tests/test_pack_loader.py:107`) passa. Executado: suíte 637 testes, `OK`.
    - `range-core/engine/verificacao.py:266`: `if not self.predicados:` retorna sem
      avaliar. O laço degrada para no-op em vez de recusar.
    - `required_for_complete_pack` (`scenario.schema.v2.yaml:48-51`), que lista
      `ground_truth.yaml`, não é lido por código nenhum — `grep` em toda a árvore
      acha apenas o registro, dois docstrings e `docs/progress/fase_1.md`.
    - O próprio módulo ainda declara o item como pendente:
      `pack_loader.py:39-42` — *"**Nao verificados aqui** ... `verification_predicates`
      obrigatorios (Fase 6, T10)"*.
    - Nenhum teste da árvore planta um pack sem `verification_predicates` e exige
      recusa: `grep -rn verification_predicates tests/` devolve apenas leitura do
      `$defs` (`test_metrics_verificacao.py:443`) e as folhas temporais
      (`test_pack_loader.py:434,452,459`).
  Por que é blocker: um pack incompleto sobe o engine com o avaliador de predicados
  inerte. `TTCV` e `TTRV` nunca marcam, o par declaração × verificação perde a metade
  que `00_MASTER_SPEC.md` §3 chama de "o achado", e nada fica vermelho — a falha que
  `09_EVENT_MODEL.md` §4 nomeia como "a mais cara possível". A única prova existente é
  a fixture negativa `ground_truth.schema.yaml:546`, executada por
  `check_contract_examples.py`: ela prova que o **schema** recusaria, não que o
  **carregamento** recusa. O schema nunca é aplicado nesse caminho.

- [B2] Item 1 da DoD sem prova executável — a emissão de `audit_query_performed` não é exercitada
  Arquivo: `domains/academus/api/app.py:239-247`;
           `tests/test_api_emissao.py:100-145`;
           `scripts/check_api_surface.py:457-504`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §Fase 6, item 1 da DoD
                     ("Consultar a trilha com filtro de período emite
                     `audit_query_performed`"); `06_ACCEPTANCE_TESTS.md` T9, 1º critério
                     ("Cada `event_type` declarado em `observability_hooks.yaml` é
                     emitido pela ação correspondente")
  Evidência:
    - `grep -rn "grade-changes" tests/` devolve **duas** ocorrências, ambas em
      `tests/test_api_emissao.py:41,60`, e as duas são uma superfície **fabricada**
      no próprio teste. Nenhum teste chama a rota real.
    - `tests/test_api_emissao.py:24` importa de `app` somente
      `confere_emissor_declarado`; não importa `montar`, e não há `TestClient` contra
      a `academus-api` nesse arquivo.
    - `test_o_emissor_recebe_exatamente_os_campos_declarados`
      (`test_api_emissao.py:100-115`) chama `EmissorEspiao().registrar_consulta(...)`
      diretamente e afirma sobre o espião — asserção sobre mock, não sobre o
      comportamento da rota.
    - `test_os_campos_do_hook_e_os_do_emissor_sao_o_mesmo_conjunto`
      (`:117-145`) compara `payload_fields` do YAML com
      `inspect.signature(Emissor.registrar_consulta)` — prova a **assinatura**,
      não a chamada.
    - `check_api_surface.py:457-504` valida apenas a **declaração** `emite`:
      presença, pertinência ao catálogo e camada. Executado: `RC=0`, e a saída
      confirma o escopo — *"33 `event_type` no catalogo, e todo `emite` sai dele"*.
      Não há verificação por AST de que o corpo da rota invoque o emissor.
    - `confere_emissor_declarado` é guarda de **boot**: exige que exista um emissor
      quando a superfície declara `emite`. Não exige que a rota o chame.
    - Consequência mecânica: apagar `app.py:239-247` deixa a suíte, os probes e
      `check_api_surface.py` **todos verdes**.
  Por que é blocker: é o primeiro item da DoD da fase e o gatilho da evidência `auto`
  de OBJ-03 (`09_EVENT_MODEL.md` §6). Um `emissor.registrar_consulta` que deixe de ser
  chamado produz objetivo que nunca é satisfeito, sem nada acusar até o exercício ao
  vivo — exatamente a classe que `04_SCENARIO_SCHEMA.md` §6.2 e `09` §4 descrevem.

### HIGH

- [H1] Cópia instalada do `readonly_bash.py` divergente da fonte versionada: o verificador central da peça 5 é inalcançável e o harness negativo sai 1
  Arquivo: `~/.claude/hooks/readonly_bash.py` (instalada) vs
           `user-scope/hooks/readonly_bash.py:365-389` (versionada, deste commit)
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T10, critério
                     "A recusa é estrutural... construtor só no ponto de montagem";
                     `07` §Fase 6 DoD itens 14 e 15
  Evidência:
    - Executado `scripts/phase0_negative_tests.py`: **rc=1**, última linha
      `FAIL: readonly_bash.py fonte versionada e copia instalada DIVERGEM.
      Copie user-scope/hooks/readonly_bash.py para ~/.claude/hooks/`.
      É um step obrigatório do job `arquitetura` (`.github/workflows/invariants.yml:336`).
    - `grep` na cópia instalada não acha `check_insumo_de_metrica`; o diff
      `git diff <base> HEAD -- user-scope/hooks/readonly_bash.py` mostra a entrada
      sendo acrescentada à fonte versionada (`+rf"|check_insumo_de_metrica|check_insumo_de_metrica_probes"`).
    - Tentativa de executar `scripts/check_insumo_de_metrica.py`: **BLOQUEADO**
      pelo hook instalado.
    - `scripts/check_allowlist_do_auditor.py` sai `RC=0` porque lê a fonte
      versionada (`check_allowlist_do_auditor.py:77`), não a instalada.
  Observação de escopo: o commit fez a sua parte — a entrada está na fonte versionada
  e o CI roda os dois scripts (`invariants.yml:316-320`). O defeito está na cópia
  instalada, fora da árvore. Registro como HIGH e não como BLOCKER por isso, mas a
  consequência para esta auditoria é real e está em "O QUE EU NÃO CONSEGUI VERIFICAR".

### MEDIUM

- [M1] Teste afirma no nome e no docstring uma propriedade que não exercita — competência de `bars_score_submitted`
  Arquivo: `tests/test_aar_timeline.py:354-367`; `range-core/aar/timeline.py:296-302`;
           `contracts/events.schema.yaml:496-500`
  Requisito violado: `03_EXERCISE_DESIGN.md` §2.3 (as nove competências), pela via que o
                     próprio contrato declara: `events.schema.yaml:496-500` justifica
                     não enumerar `competency` dizendo *"O cruzamento e por teste"*
  Evidência: `ACompetenciaConfereComORubricario` declara *"A competencia pontuada tem de
  ser uma das nove de `03` §2.3"*, mas `test_as_competencias_da_suite_existem_no_contrato_de_rubrica`
  (`:362-367`) só afirma `{"incident_triage","escalation","analytical_rigor"} <= declaradas`
  — três literais escritos no próprio teste. `timeline.py:296-302` aceita **qualquer**
  `str` como competência e agrupa por ela; `bars_score_payload`
  (`events.schema.yaml:505-514`) declara `competency: {type: string, minLength: 1}`.
  Um `bars_score_submitted` com `competency: "banana"` atravessa contrato, projeção e
  alerta sem que nada recuse. O cruzamento que o contrato invoca como razão de não
  duplicar a enumeração não existe na direção que importa.

- [M2] Docstring do `pack_loader` descreve como pendente uma entrega desta própria fase
  Arquivo: `range-core/engine/loader/pack_loader.py:39-42`
  Requisito violado: `07` §Fase 6, itens 7 e 10 da DoD
  Evidência: o bloco diz *"**Nao verificados aqui**, e cada um tem fase com item de DoD
  proprio: `verification_predicates` obrigatorios (Fase 6, T10), rubrica ausente ou em
  versao divergente (Fase 6, T9)"*. A metade da rubrica **foi** implementada aqui nesta
  fase, via `x-aurora-ref: rubric_library` (`scenario.schema.v2.yaml:154`) resolvido em
  `_verify_rules` (`pack_loader.py:529-541`), com dois testes em
  `test_pack_loader.py:405-419`. A frase ficou falsa dentro do módulo que a fase alterou.
  A outra metade continua verdadeira — e é o B1.

### LOW

- [L1] Contrato cita arquivo de teste inexistente
  Arquivo: `contracts/events.schema.yaml:493`
  Requisito violado: nenhuma cláusula normativa; é a classe que o próprio repositório
                     nomeia em `.github/workflows/invariants.yml:429-432`
                     ("comentario que cita arquivo inexistente e a linhagem P10/P15/P22")
  Evidência: o comentário afirma *"`tests/test_aar_divergencia.py` cruza os dois"*.
  `ls tests/` não lista esse arquivo, e `grep -rn test_aar_divergencia .` devolve
  apenas esta linha. Os dois cruzamentos existem, em
  `tests/test_aar_timeline.py:362` e `:369` — a referência é que está errada.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Consulta à trilha com filtro de período emite `audit_query_performed` | **FAIL** | B2 — nenhum teste exercita a rota; espião + `inspect.signature` |
| 2. Cada par declaração × verificação é disparável e testado | PASS | `test_metrics_declaracao.py:138,149,191`; `test_metrics_verificacao.py:124,131,359` — TTCD/TTCV, TTRD/TTRV, TTID/TTIV |
| 3. Desconto por **união**, nunca soma | PASS | `test_metrics_epoch.py:114,131,142,263,272` — sobrepostos, contido, adjacente, `test_o_desconto_sobre_eles_e_a_uniao_e_nao_a_soma` |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | PASS | `test_metrics_epoch.py:207,219,402`; `test_metrics_verificacao.py:261` |
| 5. Declaração não altera ground truth | PASS | `test_metrics_verificacao.py:173,179,184`; `test_metrics_declaracao.py:350,358` |
| 6. Declaração prematura → incompatíveis no AAR | PASS | `test_aar_timeline.py:177-219` (`OsEventosIncompativeis`), com o negativo `:188` e `:197` |
| 7. Pack sem `verification_predicates` não carrega | **FAIL** | B1 — `pack_minimo` sem `ground_truth.yaml` carrega verde |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | PASS | `test_metrics_verificacao.py:355-407`, incl. `test_o_limiar_vem_do_INSUMO_e_nao_de_constante` |
| 9. Brier só no escopo revisado; lacuna em separado | PASS | `test_metrics_calibracao.py:93,104,182,188,198,278` |
| 10. Rubrica ausente ou em versão divergente impede carga | PASS | `test_pack_loader.py:405,413`; fixtures `scenario.schema.v2.yaml:935,948`; `check_contract_examples.py` RC=0 |
| 11. Divergência ≥ 2 pontos gera alerta | PASS | `test_aar_timeline.py:299,308,314,322,327` — limiar exato e o negativo de 1 ponto |
| 12. As nove siglas classificadas pelo critério de `00` §3.2 | PASS | `test_derivacao_das_nove_siglas.py` — tabela lida de `03` §3.0 (`:101-132`) e conjunção aplicada; `:286` deriva da saída real dos dois computadores |
| 13. Todo `event_type` tem `metric_side`; lados disjuntos | PASS | `contract_rules.py:127-153`; conjunção confere com `09` §4.0 (5 tipos referenciáveis) |
| 14. Folha de predicado obedece à conjunção, com probes | PASS | 6 fixtures negativas em `ground_truth.schema.yaml:650,669,694,712,731,745` + positivos; `check_contract_examples.py`: 88 negativos executados, RC=0 |
| 15. Cada computador recebe só o seu lado; só o AAR tem as duas metades | **NÃO VERIFICADO** | Testes de unidade passam (`test_metrics_insumo.py:89,97,162,183`), mas `check_insumo_de_metrica.py` — a exigência (2)/(3) de `00` §3.2 — é inalcançável (H1) |
| 16. Desconto, `rehearsal` e seleção de start são cálculo do consumidor | PASS | `insumo.py:152-165` entrega epoch inteira aos dois lados; `test_metrics_epoch.py:428,435`; `test_metrics_declaracao.py:254,266` |
| 17. `inject_fired` emite os marcadores de start no payload, com teste de emissão | PASS | `test_inject_engine.py:781,794,809` afirmam sobre o payload do evento realmente emitido |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_api_emissao.py:100` `test_o_emissor_recebe_exatamente_os_campos_declarados`** — asserção sobre `EmissorEspiao`, chamado diretamente pelo teste. Não toca a rota. (B2)
2. **`tests/test_api_emissao.py:117` `test_os_campos_do_hook_e_os_do_emissor_sao_o_mesmo_conjunto`** — compara YAML com `inspect.signature`. Prova fidelidade de assinatura; o requisito é de **emissão**. (B2)
3. **`tests/test_aar_timeline.py:362` `test_as_competencias_da_suite_existem_no_contrato_de_rubrica`** — nome e docstring afirmam que a competência pontuada tem de ser uma das nove; o corpo verifica que três literais do próprio teste estão no enum. Competência arbitrária não é recusada em lugar nenhum. (M1)
4. **A fixture `ground_truth.schema.yaml:546` (`'sem verification_predicates: ... pack nao carrega'`)** — passa e é executada, mas prova que o *schema* recusaria. O loader nunca aplica esse schema a `ground_truth.yaml`, então a fixture não sustenta o item de DoD que a `reason` dela cita. (B1)

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 1 e 4 da DoD da Fase 4** (regressão de container). `scripts/check_provas_de_container.py` **recusou**, rc=2: as provas `demo` e `reinicio` saíram rc=125, com `docker compose up` falhando por daemon ausente. Conforme instruído, permanecem **NÃO VERIFICADO** — nunca PASS por silêncio.
- **Prova do seed completo (M2 da Fase 5).** `scripts/check_prova_do_seed.py` **recusou**, rc=1: gravada sobre `c119718c615fd3a614231a91d56ff646ff14ed71`, e este checkout é `5fd6f72…`. Medição transportada de outro commit, tratada como não medida.
- **Item 15 da DoD — a costura fraca de `00` §3.2.** `check_insumo_de_metrica.py` e os probes dele estão bloqueados pelo hook instalado (H1). Os testes de unidade de `test_metrics_insumo.py` cobrem tipo próprio e conteúdo dos insumos, mas **não** cobrem a exigência (2) — construtor invocado só em `monta`, varredura de AST sobre `range-core/` e `domains/`. Um segundo montador em qualquer módulo da árvore passaria despercebido nesta auditoria. `00` §3.2 já declara essa costura fraca; aqui ela ficou **não medida**.
- **Testes que exigem Postgres/Redis**: 140 dos 637 pularam (`OK (skipped=140)`). Isso inclui a trilha de auditoria, o business state e a projeção materializada. Nada da Fase 6 depende deles nos itens que julguei PASS, mas a rota `/audit/grade-changes` só seria exercitável ponta a ponta com banco — o que **não** desculpa B2, porque a emissão é testável com repositório falso e a árvore não tem esse teste em forma nenhuma.
- **`scripts/phase0_negative_tests.py` saiu 1** neste ambiente (H1). Os 300+ eixos individuais passaram; a única linha vermelha é a divergência entre cópia instalada e fonte versionada. Não pude distinguir, com as ferramentas disponíveis, se o CI trata a ausência de `~/.claude/hooks/` como skip — logo não afirmo que o CI está verde nesse step.
- **Não executei o cliente web** (`npm`, `tsc`, `vite`): fora do alcance nesta sessão e sem relevância para os itens desta fase.
```

**Resumo:** dois BLOCKERs, ambos do mesmo formato — item de DoD dado como fechado cujo mecanismo não existe no caminho de produção, com verde sustentado por prova adjacente (fixture de schema nunca aplicada em B1; espião e assinatura em B2). Em ambos, remover o comportamento exigido não torna nada vermelho. Regra da casa: qualquer BLOCKER ⇒ FAIL.
