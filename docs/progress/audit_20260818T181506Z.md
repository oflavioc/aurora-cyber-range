# Auditoria de checkpoint - 2026-08-18T18:15:06.404351+00:00

- fase: **5**
- commit auditado: `027f07e700869e33190de5b79a5d55aeffd834f4`
- session_id: `685800af-fe5b-4b1a-89df-6af231cf4358`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have enough evidence. Compiling the report.

# AUDITORIA — FASE 5

## VEREDITO: PASS

Base confirmada: `HEAD` = `027f07e70086`, árvore limpa, âncora da Fase 5 em `docs/process/phase_anchors.tsv` = `fd34c444…`, idêntica à base informada. Guarda de base: **PORTA**.

## FINDINGS

### BLOCKER
Nenhum.

### HIGH
Nenhum.

### MEDIUM

- **[M1] A role restrita não alcança a conexão real da aplicação (P5-3)**
  Arquivo: `alembic/versions/0004_trilha_de_auditoria.py:214`; `domains/academus/audit/trilha.py:219`; `tests/test_trilha_de_auditoria.py:147-168`
  Requisito violado: nenhum — `02_DOMAIN_ACADEMUS.md` §4 item 2 e `05_SECURITY_REQUIREMENTS.md` §7 exigem que a role `academus_app` exista `INSERT`-only com `REVOKE UPDATE/DELETE/TRUNCATE`, e ela existe. Registro como MEDIUM por consequência operacional, não por desvio de spec.
  Evidência: `has_table_privilege('academus_app', …)` retorna falso para os três verbos (teste executado, verde). Mas `OQueT7NaoProva::test_a_role_QUE_CONECTA_ainda_possui_UPDATE_na_trilha` passa — isto é, a role que conecta **ainda possui** `UPDATE` —, e `test_a_role_que_conecta_pode_voltar_de_SET_ROLE` demonstra `RESET ROLE` desfazendo o `SET LOCAL ROLE` da escrita.
  Por que não é blocker: o trigger `tg_audit_trail_imutavel` recusa `UPDATE`/`DELETE` a **todos**, inclusive ao dono (verificado por teste real: exceção contendo "append-only"), e a cadeia de hash detecta reescrita de quem desabilitar o trigger. O limite está declarado em três lugares (migration, docstring do teste, §7.1 do registro) e **exercido por dois testes que ficam vermelhos quando ele fechar**. Destinatário nomeado: operador, gatilho "primeiro deploy com participante real".

### LOW

- **[L1] Posição da quebra extraída por parsing de prosa**
  Arquivo: `domains/academus/audit/trilha.py:316-325` (`_posicao`)
  Requisito: `06_ACCEPTANCE_TESTS.md` T7 — "reportar a posição exata da quebra".
  Evidência: `ChainBroken` (`range-core/events/integrity.py:85`) não carrega campo estruturado; `_posicao` devolve o primeiro token numérico da mensagem. Neste mesmo diff a mensagem do event store perdeu o trecho `evento {event_id}` — se um identificador iniciado por dígito precedesse a sequência, a posição reportada seria outra. Os três casos atuais têm teste verde.

- **[L2] Partição da Linha B exercida apenas em `ESCALA_REDUZIDA`**
  Arquivo: `tests/test_seed_determinista.py:37`, `domains/academus/seed/dataset.py:126,137`
  Requisito: `07` Fase 5 item 5 / T8 critério 2 (`02` §6.1 dá "milhares" aos legítimos normais).
  Evidência: a suíte roda com `normais_na_trilha=40`; a escala completa usa 3.000, e `scripts/prova_seed_completo.py` mede tempo e SHA por tabela, **não** os seis conjuntos. Verifiquei independentemente que o risco é estrutural­mente baixo: `dataset.py:373-386` constrói `restantes` excluindo `conta_alvo` e `comuns` excluindo `delegantes`, de modo que os pools de conta são disjuntos por construção e a disjunção/cobertura não depende do volume de fundo. Limite declarado na linha do item 5 e em §10.3.

- **[L3] Sem produtor em disco para os artefatos de gabarito**
  Arquivo: `domains/academus/seed/gabarito.py:92`
  Requisito: `07` Fase 5 OUTPUTS — "`GM_NOTES.md` do pack".
  Evidência: `gabarito.gerar()` devolve o artefato em memória; não há comando que o escreva em `scenarios/`. O `range-cli` é escopo da Fase 7 (`00` §7). O teste produz e julga o artefato, então o item 6 tem prova executável; o que falta é a ergonomia de facilitador.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Seed completo em < 5 min | PASS | `check_prova_do_seed.py` rc=0, SHA gravado = `027f07e70086` = este checkout. **146,2 s e 153,8 s**, orçamento 300 s; 3.543.783 linhas; máquina/data/stack gerados por código. Não presenciei a medição (ver limites). |
| 2 — Mesmo `RANDOM_SEED` → dataset byte-idêntico | PASS | Mesma prova: **20 tabelas com SHA-256 igual nas duas execuções**. Na suíte, as duas direções: `test_o_mesmo_seed_produz_o_mesmo_dump` (20 tabelas, dump não vazio) e `test_seeds_DIFERENTES_produzem_dumps_diferentes`, mais `test_o_gerador_nao_le_relogio` por AST. |
| 3 — `UPDATE`/`DELETE` falham por trigger **e** por role | PASS (ver M1) | `TrilhaEhAppendOnly`: 2 testes de trigger (exceção "append-only") + `has_table_privilege` negando os 3 verbos + o par que impede vacuidade (role **possui** `INSERT`/`SELECT`). Mecanismos provados **separadamente**. |
| 4 — `/audit/verify-chain` detecta adulteração induzida | PASS | `CadeiaDetectaAdulteracao`: campo alterado → posição 2; `payload` (nota) alterado → posição 1; linha removida do meio → posição 3 com motivo "buraco ou reordenacao"; truncamento de cauda declarado como limite **e afirmado por teste**. Rota pelo stack ASGI: 200/403/401 e forma estável do corpo. |
| 5 — Seis conjuntos nos volumes especificados | PASS (ver L2) | `SeisConjuntosDaLinhaB`: volumes **lidos de `02` §6.1 pela própria spec** (não das constantes), disjunção par a par e união = trilha inteira, mais as seis características de §6.1 no dado. `check_volumes_da_linha_b.py` cruza spec × constantes em 8 eixos. |
| 6 — `GM_NOTES.md` com a query de referência | PASS (ver L3) | `test_o_GM_NOTES_contem_a_query_de_referencia` + `test_a_query_do_GM_NOTES_EXECUTADA_devolve_os_22_e_nenhum_ambiguo` — a query é **executada**, não só encontrada como texto. |

Aceitação: **T7** três critérios cobertos; **T8** quatro critérios cobertos (inclusive o linter, que **recusa** em vez de comparar); **T6** rota nova declara `papeis: [secretaria]`, com 403/401 provados.

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado. As três armadilhas típicas foram procuradas e estão fechadas — em dois casos por correção explícita de auditoria anterior:

- **Imutabilidade só por trigger:** não. `has_table_privilege` consulta o catálogo do Postgres, e o próprio teste explica que o resultado independe do trigger estar ativo.
- **Volume comparado com o gerador contra si mesmo:** corrigido (M2 anterior). `test_os_volumes_sao_os_de_02_secao_6_1` importa `volumes_da_spec` e lê `02` §6.1; os probes provam que trocar uma constante reprova.
- **Linter que compara em vez de recusar:** corrigido (M1 anterior). `test_o_LINTER_recusa_fato_ausente_do_ground_truth` exige `assertRaises(GabaritoDivergente)`, e `test_o_linter_roda_DENTRO_do_gerador` prova por AST que `conferir` é chamada dentro de `gerar`.
- **Asserção sobre mock:** nenhuma. Os 402 testes rodaram contra Postgres real.
- **`skip`/`xfail` sem justificativa:** nenhum. Os `skipIf` existentes são todos `URL is None`, e a suíte reportou **`OK` sem contagem de `skipped`** — ou seja, zero pulos nesta execução.

Execução real observada: `python -m unittest discover -s tests` → **Ran 402 tests, OK**. Invariantes 1–4 verdes (`check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `codegen --check`), com `phase0_negative_tests.py` provando que os seis verificadores ainda reprovam contra violação plantada. Verificadores novos da fase (`trilha`, `volumes_da_linha_b`, `gabarito_fora_do_git`, `secoes_de_seguranca`, `allowlist_do_auditor`, `prova_do_seed`) todos com prova negativa executada e verde.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Não vi rodar as provas de container (P4-10) nem a medição do seed (M2).** Ambas exigem Docker/Postgres fora da allowlist. O que confirmei é o que o mecanismo permite: `check_provas_de_container.py` e `check_prova_do_seed.py` saem 0 e imprimem a saída íntegra **depois** de conferir o SHA gravado contra este checkout, e seus probes demonstram recusa por ausência, SHA divergente, arquivo versionado, contexto incompleto e item declarado como falho. A amarração é o SHA, não a palavra de quem mediu.
- **O número do item 1 é de máquina.** 146,2 s/153,8 s valem para Windows-11 / Python 3.12.10 do operador. Reexecutar em outra máquina produziria outro número e não confirmaria este.
- **O `GM_NOTES.md`/`ground_truth.yaml` do exercício real, com o `RANDOM_SEED` de produção, nunca é visto por CI nenhum** — declarado pelo próprio `check_gabarito_fora_do_git.py`. A propriedade provada é do gerador.
- **A partição em escala completa não foi executada.** Meu argumento em L2 é estrutural (leitura de `dataset.py:373-386`), não medição.
- **Os passos de container do `invariants.yml`** (build das telas, subida da stack, `demo_fase4`, reinício) não rodam aqui; julguei-os por leitura do workflow mais as provas gravadas.
- **`.git/hooks/pre-commit` ausente neste worktree**, então a sincronia do guarda de branch não foi conferida nesta execução — o próprio harness reporta isso e é o comportamento esperado fora do escopo de usuário.
