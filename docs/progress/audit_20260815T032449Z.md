# Auditoria de checkpoint - 2026-08-15T03:24:49.489254+00:00

- fase: **1**
- commit auditado: `457a423cc5336b24267876784dad036945d1fb5d`
- session_id: `b9f937f2-953f-441d-ae4e-ac478456ce5c`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 1

Commit auditado: `457a423cc5336b24267876784dad036945d1fb5d` (worktree fixado). Diff contra `main`: 67 arquivos, `docs/spec/` **intocado** (`git diff main...HEAD -- docs/spec/` vazio; o spec-change `81ea877` está em `main`, em commit próprio, sem código junto — regra de `CLAUDE.md` §"A especificação é imutável" respeitada).

## VEREDITO: PASS

## FINDINGS

### BLOCKER

Nenhum.

Conferi especificamente a hipótese que o operador levantou — motivo bem formatado e falso sobrevivendo à nova exigência de forma. **Li os 7 motivos de `IGNORADOS` contra o texto da spec, um a um** (é o método que produziu o B1 da rodada anterior). Os 7 são verdadeiros:

| Entrada | Bloco na spec | Motivo declarado | Verificação |
|---|---|---|---|
| `01 §3. Relógios` [0] | `01_ARCHITECTURE.md:90-97` | fragmento do envelope | **verdadeiro** — só `exercise_time`, `exercise_timestamp`, `wall_timestamp`, `clock_multiplier`; faltam os 5 obrigatórios de `09` §1.1 |
| `02 §6.2` [0] | `02_DOMAIN_ACADEMUS.md:87-95` | submissão de assessment, runtime da Fase 6, sem contrato | **verdadeiro** — `{case_id, classification, confidence, evidence, rationale}`; nenhum dos seis contratos o cobre |
| `03 §2.2 Formato` [0] | `03_EXERCISE_DESIGN.md:87-99` | rubrica BARS, artefato de `range-core/rubrics/` | **verdadeiro** — `03` §2.1 localiza o artefato no core; rubrica não é um dos seis contratos |
| `03 §4` [0] | `03_EXERCISE_DESIGN.md:197-213` | `information_distribution.yaml` sem contrato (P1-20) | **verdadeiro** — `scenario.schema.v2.yaml:52-57` o lista em `package_files.optional` e nenhum `$defs` o descreve |
| `03 §5.1 Submissão` [0] | `03_EXERCISE_DESIGN.md:223-231` | idem 02 §6.2 | **verdadeiro** — mesmo bloco, mesmo artefato |
| `05 §3 Dados` [0] | `05_SECURITY_REQUIREMENTS.md:55-57` | fragmento de uma linha; `nao_e_instancia_de: evidence` | **verdadeiro** — `{is_synthetic, age_range}` não é `MANIFEST.json` (`evidence.schema.yaml:22 required: [generated_from, sources]`) |
| `09 §6` [0] | `09_EVENT_MODEL.md:206-217` | `observability_hooks.yaml`; `nao_e_instancia_de: objectives` | **verdadeiro** — `hooks/trigger/payload_fields`; guardado por `check_contract_literals.py`, com probe (`phase0_negative_tests.py`, "hook fora do catalogo", rc=1) |

O bloco que era o B1 (`09 §6` índice 1) saiu de `IGNORADOS` e passou a ser **reivindicado e validado** (`objectives.schema.yaml:223-227`; contagem subiu de 12 para 13 validados, ignorados de 8 para 7).

### HIGH

- **[H1] O único gate que prova o item 1 da DoD continua sem bloquear merge**
  Arquivo: `.github/workflows/invariants.yml:160-202` (job `contratos`); `docs/process/WORKFLOW.md:118-133`; `docs/progress/fase_1.md:248` (P1-18 ⚠️ aberta)
  Requisito violado: `docs/process/WORKFLOW.md` §"GitHub / branch protection" (quatro required contexts; *"a fase não deve ser declarada concluída até a proteção equivalente ser configurada e comprovada"*), `07_IMPLEMENTATION_PHASES.md` §"Template obrigatório" (DoD binária)
  Evidência: o job `contratos` é o único que executa `check_contract_examples.py`, `check_contract_examples_probes.py`, `check_spec_examples.py` e `check_spec_examples_probes.py` — toda a prova executável dos itens 1 e da camada de fidelidade. O próprio commit registra que ele foi removido dos required status checks e que *"um PR com fixture quebrada pode ser mergeado com ele vermelho"*. Estado inalterado desde a terceira auditoria.
  Por que é high e não blocker: o diagnóstico está correto e a ordem de reposição é restrição real do GitHub (check inédito não é exigível); as quatro provas rodam e eu as executei nesta rodada (rc=0 nas quatro). O que não existe hoje é a propriedade "gate". Não consigo inspecionar a branch protection daqui.

### MEDIUM

- **[M1] A mecanização cobre 2 das 7 entradas, e o gatilho dela é auto-declarado e não cruzado com nada**
  Arquivo: `scripts/check_spec_examples.py:141-154` (`autoridades()`) e `:291-314`; `contracts/scenario.schema.v2.yaml:717-741`
  Requisito violado: a garantia que o próprio verificador declara (`scripts/check_spec_examples.py:35-38`, `:236-237` — *"regra sem probe é o defeito que este projeto passou a fase inteira punindo"*)
  Evidência: montei o mapa de autoridade a partir dos seis `x-aurora-authority` e cruzei com as 7 chaves de `IGNORADOS`. Só `05 §3` e `09 §6` caem sob seção reivindicada; as outras cinco — `01 §3`, `02 §6.2`, `03 §2.2`, `03 §4`, `03 §5.1` — **não disparam exigência nenhuma**, porque nenhum contrato declara autoridade sobre `(01,3)`, `(02,6)`, `(03,2)`, `(03,4)` e `(03,5)`. Nada verifica se a lista de autoridade é completa: `scenario.schema.v2.yaml` reivindica um bloco de exemplo em `08 §5` (`:738-741`) sem declarar autoridade sobre `08` (`:717-719`), e `evidence.schema.yaml`, que declara autoridade sobre `08 §5` (`:246-247`), não reivindica bloco nenhum. Ou seja: `x-aurora-spec-examples ⊄ x-aurora-authority`, e ninguém reclama.
  Consequência concreta: o caminho do B1 permanece aberto para qualquer seção fora do mapa — mover um bloco inconveniente para `IGNORADOS` sob `(03,2)`, por exemplo, não exige justificativa alguma. O registro é honesto ao chamar isso de *"gatilho, não garantia"* (`fase_1.md:97`), mas não quantifica a cobertura, e 2/7 é o número.

- **[M2] Os seis eixos de probe provam presença de forma, e as duas ramificações novas do verificador não têm probe**
  Arquivo: `scripts/check_spec_examples_probes.py:35-75` e `:126-141`; ramos sem probe em `scripts/check_spec_examples.py:308-313` (`nao_e_instancia_de` apontando contrato que não reivindica a seção) e `:258-265` (mesmo bloco reivindicado por dois contratos)
  Requisito violado: `scripts/check_spec_examples.py:35-38` e a doutrina de `06_ACCEPTANCE_TESTS.md` §T2/§T1 aplicada pelo próprio projeto (verificador sem prova negativa não é verificador)
  Evidência: o eixo 3 (`objectives.schema.yaml`, `sections: [1]` → `[1, 2]`) planta **ausência** de `nao_e_instancia_de` e espera *"e o motivo nao diz por que o"*. Nenhum dos seis eixos planta um `nao_e_instancia_de` presente e mentiroso, nem um apontando contrato errado. Confirmei por leitura que a única checagem sobre o conteúdo é `declarado in donos` (`:308`): o texto de `"motivo"` nunca é lido por máquina. Portanto **a resposta à pergunta do operador é: os probes verificam apenas a forma**; o conteúdo dos 7 motivos foi verificado por mim, à mão, e não pela suíte.

- **[M3] O DEMO SCRIPT da Fase 1 não é executável, e o registro da fase não o menciona**
  Arquivo: `docs/spec/07_IMPLEMENTATION_PHASES.md:51` (DEMO da Fase 1) contra `:17` (tabela: `range-cli` é entregável da Fase 7); `docs/progress/fase_1.md` (nenhuma ocorrência de "DEMO"; única menção a `range-cli` em `:599`, sobre a Fase 9)
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §"Template obrigatório por fase" (`DEMO SCRIPT — sequência exata a executar para demonstrar`)
  Evidência: `git ls-files` traz 114 arquivos e nenhum `range-cli`. A primeira metade do DEMO — *"`range-cli scenario validate` recusa pack com flag inexistente"* — é inexecutável nesta fase e conflita com os NON-GOALS dela (*"nenhuma lógica de negócio"*). A segunda metade — *"import de `domains/` dentro do core falha no CI"* — está provada (`phase0_negative_tests.py`, `check_core_boundary.py` rc=1 contra `range-core/_phase0_probe_bad.py`). O registro não trata da metade impossível: nem a executa, nem a escala.

### LOW

- **[L1]** `docs/progress/fase_1.md:244` mantém P1-14 como *"⚠️ aberta, aguarda decisão de modelagem"* e `:614-620` a descreve como buraco vivo, mas o contrato já a fecha: `contracts/ground_truth.schema.yaml:229-252` restringe a folha `event` a `x-aurora-ref: event_catalog_state_effect`, com fixtures negativas para `containment_declared` (`:649-661`) e `decision_made` (`:667-680`), e o registro `event_catalog_state_effect` é de fato construído e aplicado (`scripts/check_contract_examples.py:135-141`, `:265-279`; 61 negativas recusadas, rc=0). Registro contradizendo o artefato — mesma família do L1 da quarta auditoria.
- **[L2]** `docs/progress/fase_1.md:191` afirma *"27 leituras, 36 escritas, 112 provas"*; a saída real do harness nesta rodada é *"libera **29** leituras legitimas e bloqueia 36 escritas deliberadas … = 112 provas"* (`scripts/phase0_negative_tests.py:951-961`). Duas leituras de diferença entre registro e artefato.
- **[L3]** As duas cópias das faixas sintéticas continuam divergentes, e o comentário declara alinhamento: `contracts/evidence.schema.yaml:176-178` diz *"Alinhados abaixo"*, e `:203-209` lista seis sufixos, enquanto `tools/check_synthetic_data.py:82` admite também `localhost` e `local` como TLDs reservados. `local` não está em nenhuma das RFCs que o próprio docstring cita (RFC 2606/6761). Sem consequência hoje — `scenarios/` não existe —, e é exatamente o modo de divergirem em silêncio que o comentário `:180-182` descreve.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis contratos existem e validam exemplos positivos e negativos | ✅ (com H1 sobre o gate) | `check_contract_examples.py` rc=0 — 6 contratos, 9 positivos, 61 negativos recusados **pela camada declarada**, 2 instâncias reais; `check_contract_examples_probes.py` rc=0 nos 12 eixos; `check_spec_examples.py` rc=0 — 20 blocos, 13 validados, 7 ignorados; `check_spec_examples_probes.py` rc=0 nos 6 eixos. O B1 da rodada anterior está corrigido na raiz (bloco reivindicado, não excluído) |
| 2. Constantes tipadas em Python e TypeScript para flags e event types | ✅ | `contracts/generated/events.{py,ts}`; `domains/{academus,prontus}/generated/flags.{py,ts}` (12+2 flags, namespacing conforme `01` §5.1); `codegen.py --check` rc=0 |
| 3. Teste de fronteira core/adapter passa (por AST) | ✅ | `check_core_boundary.py` rc=0; rc=1 contra probe plantado; `_common.parse_python` transforma erro de sintaxe em `ContractError` em vez de pular arquivo |
| 4. `event_type` fora do catálogo é rejeitado | ✅ (no que existe nesta fase) | Catálogo em `$defs/event_type_*` conferido **linha a linha** contra `09` §4.1: 3+3+17+3+6 = 32, idênticos; `x-aurora-registry.effect_class` conferido tipo a tipo contra a tabela de `09` §4.0/§4.1, 32/32 corretos; `check_contract_literals.py` rc=1 contra literal e contra hook fora do catálogo. Rejeição *na emissão* (T2) só é exercitável na Fase 2 |
| 5. Os seis verificadores liberam árvore limpa e cada um falha contra violação plantada | ✅ | Os seis rc=0 nesta árvore; `phase0_negative_tests.py` rc=0 — 15 probes de verificador + 112 provas de hook; 10 falsos bloqueios afirmados (P23), 0 escritas não bloqueadas |
| 6. `codegen.py --check` detecta dessincronia e não escreve | ✅ | Três probes rc=1 (artefato ausente, divergente, artefatos de evento); `sys.dont_write_bytecode` nos verificadores; CI não usa `git diff` depois dele (`invariants.yml:26-30`), conforme `06` T2 |
| 7. `docker compose up` sobe Postgres e Redis | ⚠️ não verificável aqui | Por leitura: imagens pinadas por digest, `ports` amarrados a `127.0.0.1`, `POSTGRES_PASSWORD` sem default (`05` §6 e §8). A execução de 14/08 em `fase_1.md:261-274` é auto-relato que não reproduzi |
| ~~8~~. `RANDOM_SEED` lido de `.env` | ➡️ fora do escopo | `07_IMPLEMENTATION_PHASES.md:53-61` não traz mais o item na Fase 1; ele aparece na Fase 2 |

Transversal (T15): `pyproject.toml:38-74` com versões exatas **inclusive o backend de build** (`setuptools==84.0.0` — L2 da rodada anterior fechado); `constraints.txt` fixa o fecho transitivo; `.env.example` só com placeholders e `SYSLOG_HOST=192.0.2.10` (RFC 5737); `check_security_constraints.py` e `check_synthetic_data.py` rc=0.

## TESTES QUE NÃO PROVAM O REQUISITO

1. **A exigência de `nao_e_instancia_de` é de forma, e a suíte não tem eixo para conteúdo falso.** Confirmado por leitura de `check_spec_examples.py:291-314`: o campo `"motivo"` nunca é comparado com nada; só o valor de `nao_e_instancia_de` é cruzado com o mapa de autoridade. Um motivo bem formatado e falso passa. Foi minha leitura dos 7 motivos contra o texto — não a suíte — que sustenta a ausência de BLOCKER nesta rodada.
2. **O gatilho alcança 2 das 7 entradas** (M1). Nas outras cinco a lista de exclusão continua sendo prosa sem verificador, como era antes da mecanização.
3. **`x-aurora-authority` é declarado pelo mesmo autor que escolhe o que vai para `IGNORADOS`**, e nada verifica sua completude nem sua coerência com `x-aurora-spec-examples` (M1). A mecanização subiu o escape hatch um nível, não o fechou.
4. **Dois ramos novos do verificador não têm probe** (M2): `nao_e_instancia_de` apontando contrato errado, e reivindicação duplicada.
5. **`check_contract_examples.py` continua sendo laço fechado** — fixtures dentro do contrato que julgam, escritas pelo mesmo autor no mesmo commit. Isso está declarado no cabeçalho de `check_spec_examples.py:4-14` e é a razão de a camada de fidelidade existir; a limitação persiste para tudo que a spec só diz em prosa.
6. **Alcance declarado, e verdadeiro.** `package_files.required` e `metric_binding` estão fora do alcance de qualquer verificador de exemplo (`check_spec_examples.py:16-23`). Conferi os dois à mão: `scenario.schema.v2.yaml:40-57` está fiel a `04` §1 e §9 (o pacote apenas-manifesto é forma legítima), e `objectives.schema.yaml:174-184` está fiel a `03` §1.3 (nove siglas, escalar, sem `calibração`). Por leitura, não por mecanismo.
7. **Nenhum `skip`/`xfail`** — não há suíte pytest na árvore; a "suíte" desta fase são os seis verificadores, o harness negativo e os quatro executores, e todos foram executados nesta auditoria.
8. **O commit altera a fonte do hook do próprio auditor** (`user-scope/hooks/readonly_bash.py`, +25 linhas). Revisei o diff inteiro: é ampliação **aditiva e por nome explícito** da allowlist de *leitura* para cinco scripts, sem curinga sob `scripts/`, sem argumento livre e sem abrir caminho de escrita; `phase0_negative_tests.py` verifica que fonte versionada e cópia instalada são idênticas. Não é enfraquecimento do auditor.
9. **Nenhuma violação de `05_SECURITY_REQUIREMENTS.md` encontrada.** Sem exploit, sem cripto de arquivo, sem IOC, sem anexo; compose em loopback com digest; `.gitignore` cobrindo `.env*` e `scenarios/**/evidence/`; ranges sintéticos corretos. Os limites de `check_synthetic_data.py` para `.log`/`.eml`/CEF estão **declarados** no docstring (`:22-27`) e endereçados à Fase 9 — por isso não viram finding.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **`docker compose up` (item 7).** Docker não é executável no sandbox de auditoria; verifiquei o compose por leitura.
- **Alembic executável (P1-9).** `python -m alembic.config --help` está fora da allowlist do `readonly_bash.py` — bloqueado, como nas duas rodadas anteriores. O step existe no CI (`invariants.yml:201-202`), mas está no job que hoje não bloqueia merge (H1).
- **Estado real da branch protection (H1).** Sem rede e sem `gh`; o finding se apoia no workflow versionado e no que o próprio commit registra.
- **Se os 7 motivos continuarão verdadeiros.** Verifiquei o estado deste commit. Nada no repositório impede que um motivo se torne falso depois de uma edição de spec ou de contrato — é exatamente o que M1 e M2 descrevem.
- **Fidelidade de `tools/_common.py::parse_yaml`** (único leitor dos contratos nos jobs stdlib) contra um parser conforme: não comparei as saídas.
- **Os contratos onde a spec descreve em prosa sem exemplo.** Comparei campo a campo `01 §3/§5`, `03 §1.1/§1.2/§1.3/§3.1`, `04 §1/§2/§9`, `05 §3`, `08 §3/§5/§7` e `09 §1/§2/§4.0/§4.1/§6`. Fora disso minha verificação é mais fraca e não a apresento como conclusiva.
- **Se `contratos` roda de fato no GitHub** — executei os quatro scripts localmente; a execução em CI não é observável daqui.
