# Auditoria de checkpoint - 2026-08-22T16:00:19.652412+00:00

- fase: **6**
- commit auditado: `48ede9dea9c28feb4a7f3ac0ac5e1670505ea13f`
- session_id: `393b8324-da92-45a0-a05d-a8c9461adc33`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have what I need. Emitting the report.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] O gravador das provas de container não materializa o pack que esta fase tornou obrigatório no boot — os itens 1 e 4 da DoD ficam inverificáveis por construção
  Arquivo: `scripts/grava_provas_de_container.py:209-217` (ausência), contra `.github/workflows/invariants.yml:786-787`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 4 DoD itens 1 e 4, sustentados por P4-10; `06_ACCEPTANCE_TESTS.md` T5
  Evidência: executei `python scripts/check_provas_de_container.py` → `rc=2`. As duas provas saíram `rc=125` (`NAO EXECUTADA: a stack de containers nao subiu`), e a saída gravada da stack termina em `container aurora-provas-range-api exited (1)`. A causa é mecânica e a reconstruí no próprio checkout:
    · `docker-compose.yml:159` e `:212` montam `./.aurora-pack:/pack:ro`;
    · `range-core/api/processo.py:119,136` fazem `exige(AURORA_PACK)` e `load_pack(...)` dentro de `criar()`, a fábrica que o `uvicorn --factory` chama no boot;
    · `.gitignore:52` põe `.aurora-pack/` fora do Git, e `:45` diz que ele "nasce por comando: `python tests/fixtures/pack_completo.py .aurora-pack`";
    · `ls -la .aurora-pack` neste worktree devolve **diretório vazio**;
    · o único lugar da árvore que executa aquele comando é `invariants.yml:787`, num passo rotulado "pacote completo materializado para o container (B1 da Fase 6)". `grep -n "pack\|fixtures\|Prova(" scripts/grava_provas_de_container.py` devolve **uma** linha, e é um comentário sobre acentuação. `grep -n "pack" scripts/start_checkpoint_audit.sh` não devolve materialização nenhuma — o lançador chama o gravador direto em `:441`.
  Por que é blocker: esta fase criou a precondição (pack completo com `verification_predicates`, `tests/fixtures/pack_completo.py` novo, loader que recusa pack incompleto) e atualizou o CI para ela, mas não atualizou o gravador — que é, por P4-10, o **único** caminho pelo qual os itens 1 e 4 deixam de ser NÃO VERIFICADO. A falha é determinística, não é flutuação de ambiente: toda auditoria futura reprovará pelo mesmo motivo, e os dois itens ficam permanentemente sem prova executável. Sem correção, a fase não fecha.

### HIGH

- [H1] O lançador chama `alembic` com o interpretador da máquina, e 140 dos 701 testes pulam
  Arquivo: `scripts/start_checkpoint_audit.sh:389` (e a mesma forma em `:572`)
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T5/T7 (cobertura de Postgres/Redis); a doutrina do B1 desta fase, escrita em `user-scope/hooks/readonly_bash.py:12-41`
  Evidência: `.aurora-audit/stack.log:15` — `C:\Python314\python.exe: No module named alembic.__main__`. A linha 389 é `DATABASE_URL="$AURORA_AUDIT_DB" python -m alembic upgrade head`, com `python` sem qualificação, enquanto `:441` usa corretamente `"$VENV_BIN/python"` para o gravador de provas. A suíte rodou `OK (skipped=140)` de 701 — 20% —, e os pulados incluem toda a cobertura de Postgres e Redis.
  Por que é blocker/consequência: é a mesma classe que o B1 desta fase corrigiu no hook — forma admitida, interpretador inalcançável — reincidindo no lançador, no arquivo cujo próprio comentário (`:321-323`) registra que a migration roda `rc=0` "com o python do venv da auditoria". Não é blocker porque o lançador declara `SERVICOS: AUSENTES` em vez de degradar para verde, e eu reporto o limite; mas nenhuma rodada nesta máquina mede o que T5 e T7 exigem.

### MEDIUM

Nenhum encontrado.

### LOW

Nenhum encontrado.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Consultar a trilha com filtro de período emite `audit_query_performed` | PASS | `tests/test_api_emissao_pela_rota.py` exercita `GET /audit/grade-changes` por `TestClient` com emissor real e afirma sobre o evento no store, não sobre espião; `check_hooks_com_emissor.py` rc=0 nas quatro direções |
| 2. Cada par declaração × verificação disparável e testado | PASS | `tests/test_metrics_declaracao.py::AsTresMetadesDeDeclaracao`; `tests/test_metrics_verificacao.py`; `tests/test_laco_de_verificacao.py` |
| 3. Desconto pela união, nunca pela soma | PASS | `tests/test_metrics_epoch.py::UniaoNuncaSoma` (sobrepostos, contido, adjacente, disjunto, ordem) e `::CongelamentosSaemUnidosDaEscrituracao::test_o_desconto_sobre_eles_e_a_uniao_e_nao_a_soma` |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | PASS | `tests/test_metrics_epoch.py::EnsaioDescartaAEpoch`, `::OEnsaioDescartadoSaiPeloDesconto` |
| 5. Declaração não altera ground truth | PASS | `tests/test_simulation_state.py:276` planta as seis declarações e compara o estado contra os defaults; ramo plantado em `test_simulation_state_probes.py` prova que fica vermelho |
| 6. Declaração prematura produz incompatíveis no AAR | PASS | `tests/test_aar_timeline.py:178-236` |
| 7. Pack sem `verification_predicates` não carrega | PASS | `tests/test_pack_completo_materializado.py:181` |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | PASS | `tests/test_metrics_verificacao.py:355-422`, incluindo `test_o_limiar_vem_do_INSUMO_e_nao_de_constante` |
| 9. Brier só no escopo revisado; lacuna em separado | PASS | `tests/test_metrics_calibracao.py:93-238` |
| 10. Rubrica ausente ou em versão divergente impede a carga | PASS | `tests/test_pack_loader.py:435,443`; versão embutida no id (`contract_rules.py:187-191`) |
| 11. Divergência ≥ 2 pontos gera alerta | PASS | `tests/test_aar_timeline.py:299,308,340` (limiar exato e um ponto abaixo) |
| 12. Nove siglas classificadas pelo critério de `00` §3.2 | PASS | `tests/test_derivacao_das_nove_siglas.py` aplica a conjunção às colunas da spec e cruza com as siglas que os dois computadores de fato produzem |
| 13. Todo `event_type` tem `metric_side`; os quatro lados disjuntos | PASS | `range-core/engine/loader/contract_rules.py:127-153`, conferido na carga; `check_contract_examples.py` rc=0 |
| 14. Folha de predicado obedece à conjunção de `09` §4.0, com probes | PASS | `contract_rules.py:169-173`; 6 negativos `rejected_by: x-aurora-ref:event_catalog_predicate_leaf` em `contracts/ground_truth.schema.yaml`, executados entre os 88 negativos de `check_contract_examples.py` |
| 15. Cada computador recebe só o seu lado, mais epoch e escalares tipados | PASS | `check_insumo_de_metrica.py` rc=0; probes rc=0 com 6 violações plantadas reprovadas e o controle positivo passando |
| 16. Desconto, `rehearsal` e seleção de start são cálculo do consumidor | PASS | `tests/test_metrics_epoch.py:374-441` afirma a **presença** do insumo nos dois lados antes da conta, fechando o modo "número certo por ausência de insumo" |
| 17. `inject_fired` emite no payload os marcadores de start | PASS | `tests/test_inject_engine.py:783` |
| — item 1 (Fase 4, vigente): DEMO ponta a ponta sem intervenção manual | **NÃO VERIFICADO** | `check_provas_de_container.py` rc=2; prova `demo` rc=125. Ver B1 |
| — item 4 (Fase 4, vigente): reinício do CONTAINER do engine, par de T5 | **NÃO VERIFICADO** | `check_provas_de_container.py` rc=2; prova `reinicio` rc=125. Ver B1 |

Invariantes arquiteturais: `check_core_boundary` rc=0, `check_contract_literals` rc=0, `check_event_envelope` rc=0 (`objective_ids` fora do caminho de emissão), `codegen --check` rc=0. Segurança: `check_security_constraints` rc=0, `check_synthetic_data` rc=0, `check_gabarito_fora_do_git` rc=0, `check_secoes_de_seguranca` rc=0 (8 seções). Suíte: 701 testes, `OK (skipped=140)`.

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado. Procurei especificamente as formas que esta fase teria incentivo a produzir, e cada uma estava fechada:

- **Emissão provada ao lado da rota.** Era o B2 da rodada anterior e foi corrigido: `test_api_emissao_pela_rota.py` afirma sobre o evento no store, atravessando a rota real, e o cabeçalho registra que apagar o corpo da rota deixava a suíte verde antes.
- **Tabela comparada consigo mesma.** `test_derivacao_das_nove_siglas.py` poderia ter reescrito a resposta de cada sigla em Python; em vez disso lê as três colunas da spec, aplica a conjunção de `00` §3.2 e cobra a quarta coluna — e a terceira direção roda os dois computadores em vez de ler um mapa interno, defeito que o próprio docstring (`:286-298`) declara ter cometido e corrigido.
- **Número certo por ausência de insumo.** É o modo de falha que T10 nomeia, e `test_metrics_epoch.py::OInsumoChegaInteiroEODescontoECalculo` afirma a presença do `rollback_performed` nos dois lados antes de medir o desconto.
- **Verificador sem prova negativa.** Todos os verificadores novos da fase têm probe, e os executei: `check_insumo_de_metrica_probes` (6 negativos + positivo), `check_hooks_com_emissor_probes` (7 defeitos, incluindo o eixo da vacuidade), `check_venv_da_auditoria_probes` (6 formas alcançáveis, 12 recusadas).
- **`skip`/`xfail` sem justificativa.** Os 140 pulos são todos por ausência de serviço, causada por H1, e não por marcação no código.

Um limite está declarado pelo próprio mecanismo e não é finding: `check_hooks_com_emissor` imprime que vê a **construção** do evento, não o fluxo que a alcança (P6-7). Para o único hook existente, `test_api_emissao_pela_rota.py` cobre essa metade.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 1 e 4 da DoD (Fase 4, vigentes).** Não vi a stack subir e o verificador recusa. São NÃO VERIFICADO, nunca PASS por silêncio — e a causa é o B1, que é defeito desta árvore e não ausência de ambiente.
- **M2 — prova do seed completo.** `check_prova_do_seed.py` rc=1: a medição foi gravada sobre `c119718c` e este checkout é `48ede9de`. Conforme o enunciado, a medição desta rodada não aconteceu e o arquivo veio transportado da árvore principal; trato a divergência como o caso normal, não como anomalia. O item permanece sem medição **deste** commit.
- **Toda a camada de Postgres e Redis.** 140 testes pularam por H1. Não posso afirmar nada sobre trilha de auditoria em banco real, projeção materializada, nem restauração com store persistente — apenas que os verificadores estáticos correspondentes (`check_trilha_de_auditoria`) continuam declarando o mecanismo.
- **Reconstrução < 3 s e volumes de 4 h.** Fora do escopo da Fase 6 (Fases 7 e 9), e sem serviços eu não teria como medir.
- **O artefato gerado com o `RANDOM_SEED` de produção**, conforme o próprio `check_gabarito_fora_do_git` declara: a propriedade provada é do gerador e independe do valor do seed.
- **Não executei Docker.** As provas de container foram gravadas pelo operador; o que amarra a evidência a este commit é o SHA que o arquivo carrega, e nesse eixo ele confere — o que reprovou foi o conteúdo, não a proveniência.

Os dois findings são de mecanismo, não de desenho: o núcleo de métricas, objetivos, rubricas e calibração desta fase está sustentado por testes que provam o requisito, e a partição do insumo tipado tem verificador com prova negativa completa. O que impede o PASS é que a fase mudou o contrato de boot do container e deixou para trás o gravador que responde pelos dois itens de DoD que dependem dele.
```
