# Auditoria de checkpoint - 2026-08-17T23:01:45.841238+00:00

- fase: **4**
- commit auditado: `ee5ebee1d146ffb043e931ad7f35199b5de3585d`
- session_id: `c8853b8e-3bce-4b6a-aae0-8ecafec71acb`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

The evidence file is untracked (axis `[j]` refuses a versioned one), so it cannot be committed stale — it carries the SHA and is regenerated per run. Report follows.

```text
# AUDITORIA — FASE 4

## VEREDITO: PASS

## FINDINGS

### BLOCKER
nenhum.

### HIGH
nenhum.

### MEDIUM
nenhum.

### LOW

- [L1] O hook que restringe o auditor é sincronizado a partir da árvore auditada
  Arquivo: user-scope/hooks/readonly_bash.py:6-166 (diff 486df18..ee5ebee, +267 linhas)
  Requisito violado: CLAUDE.md §"Fluxo por fase" — *"O `checkpoint-auditor` vive em
  `~/.claude/agents/`, fora deste repositório … um auditor definido pelo commit que
  ele audita pode ser enfraquecido por esse mesmo commit."*
  Evidência: o commit candidato altera `readonly_bash.py`, e
  `scripts/phase0_negative_tests.py` imprime *"OK: fonte versionada e copia instalada
  de readonly_bash.py identicas"* — isto é, a cópia que me restringiu nesta sessão vem
  da árvore que estou julgando. A frase de CLAUDE.md tira a DEFINIÇÃO do subagente do
  repositório; o HOOK ficou.
  Por que é LOW e não BLOCKER: medido, não suposto. A direção foi aditiva — as entradas
  novas admitem verificadores de leitura e a P3-8 troca a regra de `>` pela semântica de
  aspas do bash. Nenhuma escrita escapou: o harness prova 42 escritas deliberadas
  bloqueadas mais 14 formas × 8 grafias de alvo, incluindo `>&`, `&>`, `>|`, `<>`,
  `sort -o`, `tee`, `sh -c` com redirecionamento citado e o próprio `grava_provas_de_container`.
  As exclusões deliberadas (`tree`, `sort`, `git --output`, `uniq`) estão nomeadas com a
  forma de escrita de cada uma e provadas por probe.
  Já declarado pelo implementador como **P4-11**, aberta, com vencimento por condição:
  *"o primeiro commit de fase que alterar `readonly_bash.py` em direção que não seja
  estritamente aditiva"* (docs/progress/fase_4.md:2760).

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — a sequência do DEMO roda ponta a ponta sem intervenção manual | PASS | `check_provas_de_container.py` prova `demo`, rc=0, ligada a `ee5ebee1d146` pelo SHA gravado. Contra a stack de containers: fixture → sessão → telão 100 → matrícula 201 → start → telão 90 + 2 destaques → plateia → matrícula 503 → rollback → telão 100, epoch 1 → matrícula 201 de novo. `scripts/demo_fase4.py` aborta por `_exige` em cada passo |
| 2 — wallboard atualiza em < 1 s via WebSocket | PASS | Duas provas independentes. Medida: `demo_fase4.py:183-194`, `ORCAMENTO_DO_FRAME = 1.0` asserido ponta a ponta, **47 ms** observados. Estrutural: `tests/test_range_api.py:197,219,245` — frame na mesma chamada do `append`, UM frame por evento com 3 telas (`StoreContado`, ≤2 leituras), e ausência de `time`/`threading`/`sched` por AST |
| 3 — refresh no wallboard e participant-view recupera o estado corrente | PASS (protocolo) | `test_conectar_agora_recebe_o_estado_CORRENTE` (test_range_api.py:175): conexão nova sem histórico recebe o estado completo, com o par que discrimina. `range-core/web/src/canal.ts:44-50` substitui, não acumula — e `check_web_sem_derivacao.py` (rc=0, 13 arquivos, 14 métodos proibidos) impede o cliente de derivar. O navegador em si não foi exercido — ver limites |
| 4 — reinício do container do engine restaura o exercício do event store | PASS | `check_provas_de_container.py` prova `reinicio`, rc=0, sobre este SHA: `docker restart` com StartedAt 22:52:45→22:52:52 e clock congelado em T+5s; retomado, StartedAt 22:52:52→22:53:01 e T+5s→T+14s com 9 s fora do ar. Em processo: `tests/test_restauracao.py:437,457` sobre Postgres real |
| 5 — rollback aparece anotado na timeline | PASS | `test_o_rollback_aparece_ANOTADO_com_motivo_e_ponto_de_corte` (test_projecoes.py:680) assere `{"motivo","para"}` e que o disparo **não** foi anotado; `test_a_epoch_de_cada_entrada_e_a_do_evento`. No container, a timeline sai com 3 entradas, rollback anotado e disparo preservado — `00` §5.5 |
| 6 — índice de saúde a partir dos `severity_weight` ativos | PASS | `range-core/api/projecoes.py:169-189`, função pura sobre `flags.yaml` + estado. Seis testes (test_projecoes.py:282-335): 100 sem nada fora do default, as **duas** direções de default, peso maior baixa mais, flag `number` em proporção, e o caso real do adapter |
| T5, 4º critério — pausado restaura pausado; retomado restaura correndo | PASS | Os dois casos, no container (prova `reinicio`) e em processo novo sobre Postgres (`test_restauracao.py:317,457`). `test_um_evento_DEPOIS_da_pausa_nao_significa_retomada` fecha a heurística que `09` §4.1 proíbe |
| OUTPUTS — gm-console (autenticado, injects, disparo, rollback) | PASS | `gm-console/main.tsx`: `/session`, lista `/injects`, `fire`, `rollback` com motivo, mais pause/resume e timeline |
| OUTPUTS — wallboard (≥2 painéis por taxonomia + índice) | PASS | 7 `wallboard_group` em `domains/academus/flags.yaml`; painéis derivados sem lista fixa, com probe que planta grupo inexistente (test_projecoes.py:381) |
| OUTPUTS — participant-view | PASS | `plateia()` devolve UM campo (`test_a_plateia_carrega_UM_campo`), e o tipo impede o resto do inject |
| Invariantes 1–4 | PASS | Seis verificadores rc=0 e **todos** reprovando contra violação plantada (`phase0_negative_tests.py`), incluindo `objective_ids` na web e `event_type` fora do catálogo |
| Spec imutável durante implementação | PASS | `git diff 486df18..HEAD -- docs/spec/ contracts/` **vazio**. A P4-9 foi fechada por `spec-change` mergeado em `486df18`, antes da fase, em PR separado |
| `05` §6 / §8 | PASS | `docker-compose.yml` publica só em `127.0.0.1`; middleware ASGI fail-closed cobrindo `http` **e** `websocket` (app.py:144-195); `/docs`, `/redoc`, `/openapi.json` desligados; públicas = as declaradas — wallboard, participant-view, casca do console e `/session`, exatamente as exceções de `05` §8 |
| `05` §4 — banner | PASS | `check_banner_de_simulacao.py` extrai o texto da §4 e o compara nas 3 telas; recusa (rc=2) se o texto não puder ser extraído — o caso em que tudo passaria |
| T15 — dependências pinadas | PASS | `check_pinned_images.py`: 3 imagens por digest, sem divergência entre 4 arquivos, 12 eixos negativos |

**Suíte:** `python -m unittest discover -s tests` → **335 testes, `OK`, ZERO skips**.
Os testes de serviço rodaram: Postgres e Redis foram exercitados (ResourceWarnings de
`psycopg` ao longo da corrida). A rodada anterior mediu 22% menos por migration
falha, e o commit `ee5ebee` corrige o lançador que descartava a causa.

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado. Procurei especificamente as formas que esta função existe para pegar:

- **Isolamento de papel via UI** — não ocorre. `OQueASalaNaoPodeVer` (test_projecoes.py:579)
  faz varredura recursiva de chaves **e valores** sobre o payload serializado, como `06` T6
  exige explicitamente ("teste de payload de API, não de interface").
- **Asserção sobre mock** — não ocorre. `StoreContado` é subclasse do store real com um
  contador, não um duplo. Os testes de reinício usam processo separado e Postgres real
  (`_restaura_em_outro_processo.py`).
- **`skip`/`xfail` sem justificativa** — os 13 `skipIf` são condicionais a variável de
  serviço, com razão que imprime o comando, e **nenhum disparou** nesta execução.
- **Verificador vacuoso** — cada verificador novo recusa (rc=2) diretório vazio, vocabulário
  vazio, bundle ausente e spec ilegível. `check_provas_de_container.py` reprova nos 13 eixos,
  incluindo SHA divergente, evidência ausente, prova verde-e-muda e evidência versionada.
- **Item 2 sem prova real** — a alegação "< 1 s sem cronômetro" poderia esconder ausência de
  medição, mas o DEMO **assere** o orçamento de 1,0 s contra containers reais. Propriedade e
  número, não um no lugar do outro.
- **Fronteira core/domain por comentário** — as 14 ocorrências de "academus" em `range-core/`
  são todas docstring/comentário; o domínio entra por `AURORA_PACK`/`AURORA_FLAGS` como dado
  (processo.py:19-29).

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **As provas de container, rodando.** Não as vi executar — o Docker está fora da minha
   allowlist por desenho. O que amarra a evidência a este commit é o SHA que o arquivo carrega
   e um verificador cujas 13 direções eu **executei**, incluindo `[b] SHA divergente` e
   `[a] ausência`. Aceito a evidência nesses termos, e não por atestação.
2. **Bundle construído.** Não há Node neste worktree: `check_telas_sem_vocabulario` varreu
   "14 arquivos de cliente (0 do bundle)" e o banner foi conferido só na **fonte**. A direção
   fonte-verde/bundle-vermelho está provada por probe e roda no CI com `--exige-bundle`, mas
   eu não a exerci sobre os bundles reais. Também não verifiquei que as três telas compilam
   (`tsc`) — isso é passo de CI.
3. **Navegador.** Item 3 está provado no protocolo, não com refresh real; renderização,
   contraste e legibilidade a 10 m (`01` §6) permanecem fora de alcance. O implementador
   declara os dois em §2.2 do registro, com o argumento de que a D2/D3 encolhem o limite —
   e `check_web_sem_derivacao` o sustenta.
4. **Rede isolada da sala** (`05` §8). Verifiquei bind em `127.0.0.1` e ausência de porta
   publicada; isolamento de rede é propriedade de deploy.
5. **A metade de T6 que é do `operador`** fica para a Fase 5. T6 diz "Fases 4–5" e o NON-GOAL
   desta fase é "três papéis de facilitação" — o adiamento é legítimo pelo próprio rótulo do
   critério, não conveniência.
6. **A guarda de base.** Não a derivei: usei o veredito verbatim do lançador (PORTA) e conferi
   que `docs/process/phase_anchors.tsv` traz a âncora da fase 4 como `486df18…`, idêntica à
   base que me foi dada.
7. **`--headless` do lançador nunca rodou** (commit `3a5ee71`) — limite declarado pelo
   implementador, fora do escopo da DoD desta fase.
```

**Veredito: PASS.** Nenhum BLOCKER, HIGH ou MEDIUM. O único finding é LOW, já registrado como P4-11 pelo próprio implementador, com direção aditiva e cada exclusão provada por probe.

Duas observações que sustentam o veredito mais que qualquer teste isolado: a fase **não tocou** `docs/spec/` nem `contracts/` — a mudança normativa que ela precisava (P4-9) entrou por `spec-change` separado, já em `main`; e os itens 1 e 4 da DoD, que a rodada anterior deixou como NÃO VERIFICADO por ausência de Docker, deixaram de sê-lo por um mecanismo que amarra a evidência ao SHA e reprova nas duas direções — ausência e divergência —, em vez de por atestação.
