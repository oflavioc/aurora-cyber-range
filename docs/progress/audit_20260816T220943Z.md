# Auditoria de checkpoint - 2026-08-16T22:09:43.029917+00:00

- fase: **3**
- commit auditado: `9b7d7533bb042df89c9b8f152fd7c0b392dc6ed7`
- session_id: `94c57455-8dd4-4e29-916d-6085d7a71cf4`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 3

## VEREDITO: PASS

> **Este veredito é LAUDO, não porta.** A guarda de base do lançador declarou, verbatim no prompt, que a base contém 7 commits posteriores à âncora: `merge-base(origin/main, HEAD) = a857d768` ≠ âncora `8c6eca6f`. Verifiquei a topologia por conta própria (`git merge-base --is-ancestor 02383e4 a857d76` → verdadeiro): **as seis primeiras peças da Fase 3, incluindo o código dos quatro itens da DoD, já estão na branch default**. Só os cinco commits de correção (`5b219a7`, `6679284`, `8135055`, `535ccdf`, `9b7d753`) ainda são gatáveis por esta auditoria. Um PASS aqui não é autorização de merge — o merge já aconteceu.

---

## FINDINGS

### BLOCKER

Nenhum.

### HIGH

- **[H1] A fase foi mergeada peça a peça antes do checkpoint: o checkpoint deixou de ser gate para o que importa**
  Arquivo: `docs/process/phase_anchors.tsv:45` (âncora `8c6eca6f8b354c5b2511028341a0ed7a404bbd57`) contra `merge-base(origin/main, HEAD) = a857d7687e4d`
  Requisito violado: `CLAUDE.md` §"Fluxo por fase" (*"Uma fase = um branch = um PR"*, com `start_checkpoint_audit.sh` **antes** de `gh pr create`); `docs/process/WORKFLOW.md` §"A âncora, e por que sem ela a auditoria recusa"
  Evidência: `git log --oneline 8c6eca6..9b7d753` lista 11 commits; `a857d76` (*"fase-3: o fechamento — DoD com prova"*) é o merge-base com a branch default, logo `1b17380`, `02383e4`, `fce18dc`, `4e4e717`, `7011628`, `4dbb205` e `a857d76` já estão em `main`. O commit de `spec-change` `02383e4` (que toca `docs/spec/01_ARCHITECTURE.md`) tem como pai um commit de **código** da própria fase (`1b17380`), portanto não nasceu de uma branch `spec-change/<slug>` a partir de `main`, como `CLAUDE.md` §"A especificação é imutável" exige.
  Por que é HIGH e não BLOCKER: os quatro itens de DoD têm prova executável verde (medida abaixo), e nenhum invariante de produto está violado. Mas a consequência é concreta e não é reparável neste commit — o código dos itens 1 a 4 entrou em `main` sem ter passado por um checkpoint com veredito PASS, e as três auditorias que encontraram H1/H2/M1/L1–L4 correram *depois* do merge. A propriedade que o mecanismo `check_audit_base.py` passou a proteger só vale da Fase 4 em diante.
  Nota: não consegui verificar a composição dos PRs (sem `gh`, sem rede). Se cada peça foi um PR próprio e `02383e4` foi um PR isolado, o gate `spec_freeze` pode ter passado em cada um individualmente — mas a regra de *uma fase = um PR, auditada antes do merge* continua violada em qualquer das leituras.

### MEDIUM

- **[M1] `PostgresEventStore._head()` entra no núcleo sem nenhum teste, com a stack de serviços ATIVA**
  Arquivo: `range-core/events/postgres_store.py:116-138`
  Requisito violado: `01_ARCHITECTURE.md` §4 (Simulation State = Redis projeção + event store) e §4.1 (*"toda projeção é reconstruível do zero"*) — `head()` é a única coisa que decide se a projeção materializada ainda vale
  Evidência: `grep -n "head" tests/test_event_store_postgres.py` → nenhuma ocorrência. A suíte rodou 217 testes com **zero pulos** e `AURORA_TEST_DATABASE_URL` no ar: o teste era executável nesta rodada e não existe. `InMemoryEventStore._head()` é exercitado por `tests/test_projection_cache.py:162-203`; o backend de produção, não.
  Por que é medium: se `_head()` errar em Postgres, `current()` serve projeção velha e a degradação declarada simplesmente não acontece durante o exercício — o modo de falha silencioso que `09_EVENT_MODEL.md` §4 chama de "a falha mais cara possível". Declarado como P3-9 com vencimento Fase 4 (`docs/progress/fase_3.md:728`), o que é atenuante, não fechamento.

- **[M2] Business state das três entidades vive em dicionários de módulo, não em Postgres**
  Arquivo: `domains/academus/models/registros.py:75-92`; escrita em `domains/academus/api/repositorio.py:98-117`
  Requisito violado: `01_ARCHITECTURE.md` §4 (tabela de camadas: *Business State — notas, matrículas, submissões — Postgres*); `02_DOMAIN_ACADEMUS.md` §7 (*"academus-api — FastAPI + SQLAlchemy"*)
  Evidência: `NOTAS: list[Nota]` e `MATRICULAS: list[Matricula]` são objetos de módulo mutados por `POST /matricula` e `POST /turmas/{turma_id}/notas`. Nenhuma migration, nenhum modelo SQLAlchemy. O NON-GOAL da fase é *"modelo completo, seed em escala"* — não é "sem persistência".
  Por que é medium: nota lançada e matrícula efetuada somem no reinício do processo, e `01` §4.3 baseia o desenho inteiro em business state que **não** reverte com rollback. Declarado como P3-5 (`docs/progress/fase_3.md:724`).

- **[M3] `Cota` é estado mutável fora das cinco camadas de `01` §4 e decide quem recebe 503**
  Arquivo: `domains/academus/api/degradacao.py:95-113`, consumido em `:135`
  Requisito violado: `01_ARCHITECTURE.md` §4 (as cinco camadas de estado e sua persistência) e §4.1 (*"toda projeção é reconstruível do zero"*)
  Evidência: `Cota.acumulado` é um `dict` de instância do `Degradador`, sem persistência e sem origem no event store. É ele que faz `academus.lms_session_drop_rate=0.5` produzir `[200, 503, 200, 503, ...]` (`tests/test_api_degradacao.py:295`). Reiniciar o processo rebobina a cota; dois processos servindo a mesma API degradariam de forma diferente para a mesma flag.
  Por que é medium: o efeito visível ao participante deixa de ser função só do fluxo de eventos, o que contradiz a premissa de reconstrução. Declarado como P3-10 (`docs/progress/fase_3.md:729`).

- **[M4] O lint que fecha o item 4 da DoD não varre `tests/` nem `scripts/`**
  Arquivo: `tools/check_contract_literals.py:69` — `SCANNED_DIRS = ("range-core", "domains")`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 3, item 4 da DoD (*"Nenhuma string solta de flag no código-fonte, verificado por lint"*); `06_ACCEPTANCE_TESTS.md` T2 (*"Nenhuma string literal de flag ou de `event_type` fora dos geradores"* — sem restrição de diretório); `CLAUDE.md` invariante 2
  Evidência: executei `python tools/check_contract_literals.py` → rc=0. A varredura cobre `range-core/` e `domains/` e ignora `tests/`, que é exatamente onde esta fase escreveu nomes de flag (`tests/test_api_degradacao.py:44-49`). O próprio registro admite que quem barrou a primeira versão daquele arquivo foi o hook `check_architecture.py`, **que se declara não-gate** (`docs/progress/fase_3.md:701`). Hoje não há literal solto em `tests/` — a lacuna é do gate, não do código.
  Por que é medium: o item 4 da DoD é declarado provado por um mecanismo cujo escopo exclui o único diretório em que a fase correu o risco. É a classe "verificação que parece existir" que a própria §7.3 do registro nomeia.

- **[M5] A prova negativa da guarda de base não é executável pelo auditor, contra a regra que o mesmo intervalo aplica a quatro outros pares**
  Arquivo: `user-scope/hooks/readonly_bash.py:102-131` (alternação de scripts allowlistados) vs. `.github/workflows/invariants.yml:148-149` e `scripts/check_audit_base_probes.py`
  Requisito violado: a regra escrita no próprio hook — *"Script novo que precise ser executado pelo auditor entra aqui por nome, no commit que o cria"* (`user-scope/hooks/readonly_bash.py:71-72`) — e aplicada no mesmo intervalo a `check_spec_flags`, `check_api_surface`, `check_fold_authority` e `check_pinned_images` (`scripts/phase0_negative_tests.py:164-179`)
  Evidência: `python scripts/check_audit_base_probes.py` → `BLOQUEADO: comando fora da allowlist do auditor`. Executei todos os demais verificadores da fase; este, não.
  Por que é medium: `check_audit_base.py` é o mecanismo que decide se uma auditoria é **porta ou laudo** — a decisão mais consequente do aparato — e a única forma de verificar que ele "reprova nos sete/oito eixos" foi ler o código. É a degradação para inferência que o B1 da terceira rodada custou uma auditoria inteira.

### LOW

- **[L1] Flag declarada em `api_surface.yaml` e ausente do estado corrente vira no-op silencioso em execução**
  Arquivo: `domains/academus/api/degradacao.py:131-135`
  Requisito: `01_ARCHITECTURE.md` §5.4 (*"Nenhum serviço lê ou escreve flag não declarada"*; *"flag desconhecida impede subida do engine"*)
  Evidência: `estado.flags.get(entrada.flag)` devolve `None` → `ligada` não dispara e `proporcional` lê `0.0`. O CI cobre o caso (`check_api_surface.py` reprova flag inexistente — provado por probe), mas não há guarda no boot do adapter, ao contrário do loader do engine, que recusa alto. Um `api_surface.yaml` editado no container degradaria nada, em silêncio.

- **[L2] O escopo de objeto do caminho de ESCRITA de nota não é asseverado por teste**
  Arquivo: `tests/test_api_rbac.py:146-250` (classe `EscopoDeObjeto`) vs. `domains/academus/api_surface.yaml:181-193` (`escopo: professor: titular` em `POST /turmas/{turma_id}/notas`)
  Requisito: `07_IMPLEMENTATION_PHASES.md` Fase 3, item 3 da DoD (satisfeito em nível de papel) — o gap é sobre a P3-3, declarada **FECHADA** em `docs/progress/fase_3.md:722`
  Evidência: `grep -n notas tests/` mostra que todas as chamadas a `POST /turmas/T-2001/notas` usam `TITULAR = "P-3001"`. Nenhum teste verifica que `OUTRO_PROFESSOR` recebe 404 ao lançar nota em turma alheia. A classe testa `titular` na leitura da turma, no diário e na matrícula — e o docstring de `test_o_aluno_nao_matricula_o_colega` argumenta justamente que "a escrita também passa pelo escopo". A escrita mais sensível do domínio (nota, o vetor da Linha B) é a que ficou sem asserção. O caminho de código é compartilhado com `turma()`, o que atenua, mas a prova é estrutural e não observada.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `academus.enrollment_offline: true` faz o endpoint de matrícula retornar **503** | **PASS** | `tests/test_api_degradacao.py:177-182` — 201 antes, `inject_fired` real no store, 503 depois; efeito vindo do fold via `current()`, sem `set` manual. Executado: 217/217 OK |
| 2 — `academus.grades_readonly: true` bloqueia POST de nota com **mensagem de negócio** | **PASS** | `tests/test_api_degradacao.py:184-196` — 409 e `"...procure a coordenacao do curso"`; `:198-206` prova que a leitura segue 200, conforme o `effect_ui` da flag (`domains/academus/flags.yaml:106`). A flag foi criada nesta fase (`generated/flags.py:11`) — antes, o item era insatisfazível por construção |
| 3 — RBAC nega acesso cruzado entre perfis | **PASS** | `tests/test_api_rbac.py:76-96` — nas **duas** direções mais um caso positivo, sobre o stack ASGI real (`TestClient`); `:121-136` prova indistinguibilidade entre recurso existente e inexistente na negação; `:262-280` prova falha fechada para rota não declarada |
| 4 — Nenhuma string solta de flag no código-fonte (verificado por lint) | **PASS com ressalva** | `python tools/check_contract_literals.py` → rc=0, e `python scripts/phase0_negative_tests.py` prova que ele reprova contra violação plantada. Ressalva: **M4** — o escopo do lint exclui `tests/` |
| OUTPUTS — JWT | **PASS** | `tests/test_api_tokens.py:104-134` (`alg: none`, sem `exp`, adulterado, outra chave), `:60-84` (recusa alta, placeholder vazio de `.env.example` lido do disco), `:213-221` (o boot do adapter propaga a recusa) |
| OUTPUTS — três entidades (Aluno, Turma, Nota) | **PASS com ressalva** | `domains/academus/models/registros.py:45-92`. Ressalva: **M2** — em memória, não em Postgres |
| OUTPUTS — degradação real em ≥ 3 endpoints | **PASS** | `POST /matricula` (503), `POST /turmas/{id}/notas` (409), `GET /turmas/{id}/diario` (latência 2,5 s + 503 proporcional) — `api_surface.yaml:158-219`, provados em `tests/test_api_degradacao.py:208-218` e `:262-299` |
| NON-GOALS respeitados | **PASS** | Nenhum modelo completo, nenhum seed, nenhuma trilha com hash. Nenhum evento é emitido pela API — coerente com `09_EVENT_MODEL.md` §4.1 (`audit_query_performed` é `observation`, e o DoD que o cobra é da Fase 6) |
| `06` T2 — contratos (Fases 1–3) | **PASS com ressalva** | Os quatro bullets executados: `check_contract_literals`, `check_event_envelope`, `codegen.py --check`, `check_core_boundary` — todos rc=0, com harness negativo verde. Ressalva: **M4** no bullet 3 |
| Invariantes de `CLAUDE.md` (4) | **PASS** | 1: `check_core_boundary.py` rc=0. 2: `check_contract_literals.py` rc=0 (ver M4). 3: idem, catálogo fechado. 4: nenhum evento emitido nesta fase, logo nenhum `objective_ids` — `check_event_envelope.py` rc=0 |
| `05_SECURITY_REQUIREMENTS` | **PASS** | `check_security_constraints.py` e `check_synthetic_data.py` rc=0. §8: `dependencies=[Depends(autoriza), Depends(degrada)]` global, `/openapi.json`, `/docs` e `/redoc` desligados e provados 404 (`tests/test_api_degradacao.py:480-490`). §6: `docker-compose.audit.yml` publica em `127.0.0.1:15432`/`16379`. T15: `constraints.txt` e `pyproject.toml` com `==` em tudo, inclusive `setuptools==84.0.0` |

---

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado no sentido forte — não há mock, não há asserção sobre duplo, não há `skip`/`xfail` não justificado, e a suíte rodou **217 testes com zero pulos** com Postgres e Redis efêmeros no ar. Registro três observações de menor calibre, nenhuma delas um teste vacuoso:

1. `tests/test_api_rbac.py::EscopoDeObjeto` (L2) — a classe promete "a escrita também passa pelo escopo" e exercita a matrícula, mas não o lançamento de nota, que é a escrita que o domínio trata como sensível.
2. `tests/test_api_degradacao.py::AutorizaAntesDeDegradar::test_a_ordem_declarada_na_aplicacao_e_autoriza_depois_degrada` (`:421-433`) inspeciona `app.router.dependencies`, o que é asserção sobre estrutura e não sobre comportamento — mas ela é **complementar** ao teste de comportamento acima dela (`:331-393`), que discrimina de verdade, inclusive pelo canal de temporização. Registrado como forma correta, não como defeito.
3. Não é teste, é ausência: `PostgresEventStore._head()` (M1) — o único componente novo do núcleo sem nenhuma prova, num commit auditado com o serviço no ar.

Confirmo positivamente que os dois defeitos de rodadas anteriores foram fechados de verdade, medindo e não lendo: `test_sem_token_nao_paga_a_latencia_declarada` agora dispara `AVA_LENTO` e afirma o par `[2.5]` com token / `[]` sem token (`:368-393`), e `test_a_serializacao_preserva_o_TIPO_do_valor` agora atravessa uma flag `number` de verdade pelo Redis real, com `assertNotIsInstance(..., bool)` (`tests/test_projection_cache.py:269-294`).

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **A composição dos PRs que levaram a fase para `main`.** Sem rede e sem `gh` na allowlist, só enxergo topologia de commits. Não sei se `02383e4` foi um PR isolado (caso em que `spec_freeze` teria passado em cada PR) ou se veio junto de código. O H1 está enunciado sobre o que é observável: a ordem merge-antes-de-auditoria e o pai de código do commit de `spec-change`.
- **`scripts/check_audit_base_probes.py` (M5).** Bloqueado pela allowlist. Os sete/oito eixos da guarda de base, e a afirmação de que P3-7 está fechada, foram avaliados por **leitura** de `scripts/check_audit_base.py:161-300` — a lógica me pareceu coerente e a guarda demonstravelmente disparou nesta sessão, mas não medi a prova negativa dela.
- **`bench_reconstruction`** (a curva volume → tempo da Fase 2, citada como base do número de 2,874 s em `range-core/state/cache.py:12-14`): fora da allowlist por decisão declarada. Aceitei o número como herdado da Fase 2, não o reproduzi.
- **O caminho de produção completo Postgres + Redis + API.** Cada peça tem teste contra serviço real, mas a composição das três não é montada por nenhum teste nem por nenhum DEMO desta fase — não há processo servidor (`uvicorn` é da Fase 4). O que provei foi `InMemoryEventStore` + `InMemoryProjectionCache` + API, e `PostgresEventStore`/`RedisProjectionCache` isoladamente (com o furo de M1).
- **`.env` real e qualquer segredo.** Fora de alcance por norma; verifiquei apenas `.env.example`, onde `AURORA_JWT_SECRET=` está vazio e o teste que o lê está verde.
- **Comportamento sob concorrência.** `TestClient` é sequencial e a P3-2 declara que não há concorrência nesta fase; portanto não pude exercitar nem o single-flight ausente do cache nem o comportamento da `Cota` (M3) sob requisições simultâneas.
