# Auditoria de checkpoint - 2026-09-18T19:25:53.629771+00:00

- fase: **8**
- commit auditado: `02804c3ca4c1d042788c8aa754c054e7583529c5`
- session_id: `c3e9e831-310d-47f3-b71c-2a9e8d5f060d`
- modo: `headless`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have everything: full suite executed green (949/949, zero skips, rc=0), the three launcher proofs bind to this tree (`d775c63c2424`), and the whole verifier battery — including every negative proof — is green. No BLOCKER.

# AURORA CYBER RANGE — AUDITORIA DE CHECKPOINT

# AUDITORIA — FASE 8

## VEREDITO: PASS

Commit auditado `02804c3ca4c1d042788c8aa754c054e7583529c5` · árvore `d775c63c24246bd21fc41ab9f7cb7505910e5ec7` · base `c38f890a6a6af38cfc10562c9839fd9d9f611d6f` · guarda de base: **PORTA**.

---

## FINDINGS

### BLOCKER

Nenhum. O B1 da 3ª rodada está fechado por execução: `scripts/check_readme_atual.py` → rc=0 (`949` testes, `52` relatórios, 17 predicados conferindo) e `check_readme_atual_probes.py` → rc=0 (16 eixos).

---

### HIGH

- **[H1] Os dois gaps de integração que a própria fase desenhou fecham sem linha na §6 — as três telas que ela construiu não têm rota, e a `participant-api`, que carrega DOIS dos quatro itens de DoD, não tem raiz de composição de produção nem serviço no compose**

  **Arquivo:** `range-core/api/app.py:435-464` (serve `/sala`, `/plateia`, `/console` — e só) × `range-core/web/vite.config.ts:26-33` (seis telas construídas) · `range-core/participant/api/` (sem `processo.py`; `ls` confirma `__init__.py`, `app.py`, `emissor.py`, `tokens.py`) · `docker-compose.yml` (`grep -n participant` → saída vazia) · `docs/progress/fase_8_plan_serving.md:11-22` · `docs/progress/fase_8.md:68` e §6 (`:85-117`)
  **Requisito violado:** `docs/process/WORKFLOW.md:45-52` — *"Todo achado que não for corrigido no ciclo da rodada nasce com linha na §6 do `fase_N.md` corrente"*; `07_IMPLEMENTATION_PHASES.md` §"Registro de progresso" (o registro grava **pendências**); e o contrato que o próprio `fase_8.md:8-14` declara — *"pendência sem lugar é pendência que ninguém encontra"*.
  **Evidência (medida, não suposta):** `fase_8_plan_serving.md:14-22` nomeia os dois gaps em primeira pessoa — *"nenhuma rota as serve"* e *"**Nenhuma raiz de composição de produção** injeta `CONTINUIDADE` + emissor real, e **não há serviço `participant-api` no `docker-compose.yml`**"*. Conferi os dois: `check_api_surface` acusa 37 rotas declaradas, 35 implementadas, e nenhuma delas serve `exam-mode`, `investigation-console` ou `persona-panel`; `range_core.participant.api.app.montar(...)` é chamado **só por teste** (`tests/test_continuidade.py`, `test_reported_isolation.py`, `test_participant_emissao_pela_rota.py`). A §3 do registro (`fase_8.md:68`) dispõe do assunto em uma frase — *"não são exigidos pela DoD desta fase"* — e aponta para um **documento de plano**, que não é pendência, não tem estado do enum fechado, não tem "vence em" e **não migra** para `fase_9.md`.
  **Por que é HIGH e não BLOCKER:** os quatro itens de DoD são comportamentais e estão **provados** (ver tabela abaixo) — `07` §Fase 8 não pede serving, e o auditor da 2ª rodada confirmou itens 3/4 sem ele. Não reprova a fase.
  **Por que é HIGH e não MEDIUM:** não é a forma da P8-4 (cinco superfícies que a fase nunca prometeu). São artefatos que **esta fase construiu** e que ninguém consegue abrir, mais a superfície que sustenta os itens 3 e 4 sem processo que a suba. Ao fechar assim, `main` passa a conter entrega inalcançável sem nenhum registro que diga quem a alcança — e `check_progress_consistency` não pode ver, porque só cruza o que já está na tabela. A correção é barata e é de registro: duas linhas na §6 com estado e vencimento.

---

### MEDIUM

- **[M1] Três pendências herdadas cuja remediação JÁ ESTÁ na base desta branch seguem `ABERTA` na §6 — e uma delas contradiz o registro de origem no mesmo checkout**

  **Arquivo:** `docs/progress/fase_8.md:113` (P7-15), `:114` (P7-16), `:117` (P7-20) e as seções de detalhe `:344-352`, `:422-425` × `docs/progress/fase_7.md:1249`
  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §"Registro de progresso"; `.claude/rules/evidence.md` R2 §1 (o registro afirma estado que a árvore desmente); a classe que o próprio `fase_7.md:1562` nomeia — *"gatilho que já disparou e não venceu é o defeito que a P5-2 documenta"*.
  **Evidência (executada):** `git log --oneline -6 c38f890` mostra que a base da fase **é** `c38f890 fix-finding: o veredito nao e evidencia incompativel (P7-15)` sobre `8b3462e spec-change: alinhamento de 04 §8 e PHASE_0_CHECKLIST ao estado pós-Fase-7 (P7-16, P7-20)`. `git show 8b3462e -- docs/spec/04_SCENARIO_SCHEMA.md` mostra `validate`/`migrate` já removidos como verbos próprios (`04` §8, com a nota da P7-20), e `PHASE_0_CHECKLIST.md:166-172` já diz *"fechados **por atestação** (P34), e não por demonstração"*. A mensagem de `c38f890` termina literalmente em *"P7-15 -> RESOLVIDA"*, e `fase_7.md:1249` registra `RESOLVIDA` — enquanto `fase_8.md:113` a carrega como `ABERTA` com *"vence em: PR spec-change na abertura"*. **Os três vencimentos nomeiam a abertura desta fase, e os três PRs já aconteceram antes do primeiro commit dela.**
  **Consequência:** a §6 da Fase 8 é a pauta que `fase_9.md` herda por gate; três linhas falsas atravessam para lá e reabrem trabalho feito. O `check_progress_consistency` não alcança: ele cobra que o **não-fechado migre**, nunca que o estado migrado ainda seja verdadeiro (provado pelos 12 eixos do probe, que não têm esse eixo).

- **[M2] Pendências com vencimento declarado "esta fase" fecham `ABERTA`, sem redatação e sem nota — a porta que a P7-19 fechou para `ENTREGA` continua aberta para `ABERTA`**

  **Arquivo:** `docs/progress/fase_8.md:93` (P1-7 — *"esta fase, junto do destino do pack"*), `:99` (P6-5 — *"esta fase"*), `:103` (P6-12 — *"material desta fase"*), `:110` (P7-12 — *"a próxima edição da allowlist do auditor, ou esta fase"*), `:100` (P6-6, `LATENTE` — *"ou esta fase"*)
  **Requisito violado:** `docs/progress/fase_8.md:400-420` (o argumento da própria P7-19: *"Uma fase que **fecha** com uma linha ainda `ENTREGA` a faz sumir"*) e `fase_6.md:1417-1419` (*"manter o gatilho original é pior que não ter gatilho nenhum: ele **já disparou**, e um gatilho disparado que não vence é um alarme que a próxima leitura aprende a ignorar"*).
  **Evidência:** a P7-12 é o caso demonstrável: a allowlist do auditor **foi** editada nesta fase (`git show c2df575`, `user-scope/hooks/readonly_bash.py:88`), o gatilho disparou, e a linha segue `ABERTA` com o mesmo texto de vencimento. `scripts/check_progress_consistency.py:285-305` mostra por que nada acusa: a guarda da P7-19 casa `NAO_MIGRA` (`ENTREGA`) contra a linha de Status; `ABERTA` com vencimento "esta fase" passa intocada. Executei o verificador e o probe — rc=0 nos dois, 12 eixos, e nenhum deles é este.
  **Nota de escopo:** a §7 de fechamento não existe no candidato (é escrita após o veredito), e a P7-14 declara a varredura de fechamento como o lugar disso. Registro aqui a lista exata que essa varredura tem de dispor — não como falha de algo já vencido, mas para que ela não seja feita de memória.

- **[M3] `check_telas_sem_vocabulario.py` continua conhecendo TRÊS telas, enquanto a árvore passou a ter seis — e o gate irmão foi estendido nesta mesma fase**

  **Arquivo:** `scripts/check_telas_sem_vocabulario.py:100-102` (`TELAS = ("wallboard-shell", "participant-view", "gm-console")`), usado em `:282` × `range-core/web/vite.config.ts:26-33` e `scripts/check_banner_de_simulacao.py:87-100` (estendido para seis em `4f3adcd`)
  **Requisito violado:** `.claude/rules/gates.md` R10 §1/§2 (gate que deixa de cobrir o universo que julga); `05_SECURITY_REQUIREMENTS.md` §6 / `02_DOMAIN_ACADEMUS.md` §7 (vocabulário de mecanismo e texto de cenário fora do cliente).
  **Evidência:** o comentário de `:100-101` declara a propriedade que o campo sustenta — *"Um bundle que perdesse uma tela passaria por não ter o que varrer, e é por isso que `--exige-bundle` conta"*. Com `TELAS` em três nomes, um build que deixasse de produzir `dist/exam-mode/index.html` passa verde em `--exige-bundle`: a varredura de conteúdo é `rglob` (`:199`) e simplesmente não acha nada para varrer. A fonte das seis telas **é** varrida (rodei: `23 arquivos de cliente varridos (0 do bundle)`, rc=0); o furo é o da completude do artefato. A metade compensatória existe — `check_banner_de_simulacao` cobre as seis, fonte e bundle —, e é ela que impede isto de ser HIGH.

- **[M4] A classe `evidencia` do registro de `05` §4 continua com dono "Fase 8" — a fase que fecha agora —, e é a linha vizinha da que virou P8-2, não varrida junto**

  **Arquivo:** `scripts/check_banner_de_simulacao.py:91` (`"evidencia": (False, "Fase 8 — 08_EVIDENCE_SIMULATOR.md, comentario na primeira linha")`) × `00_MASTER_SPEC.md` §7 (mapa: `08_EVIDENCE_SIMULATOR.md` → **Fase 9**) e `07_IMPLEMENTATION_PHASES.md` §Fase 9
  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §4 (banner em todo artefato gerado, com destinatário declarado); é a forma exata que a 2ª rodada nomeou como M1 e que produziu a `P8-2`.
  **Evidência (executada):** a saída do gate nesta árvore imprime, literalmente, *"Classes ainda sem artefato, com dono: **evidencia (Fase 8** — `08_EVIDENCE_SIMULATOR.md`...); exportacao (Fase 8/9 ...)"*. No mesmo commit em que `exportacao` foi corrigida de `"Fase 8"` para `"Fase 8/9"` e ganhou pendência (`fase_8.md:157-173`), a linha imediatamente acima ficou apontando para a fase corrente. A Fase 8 não produz artefato de evidência — isso é item de `07` §Fase 9 — e a fase fecha com uma classe de `05` §4 deferida para si mesma.

- **[M5] As sete `action_id` do enum FECHADO do contrato estão copiadas à mão no cliente, sem nenhum gate que cruze as duas listas — o lado Python tem oráculo, o TypeScript não**

  **Arquivo:** `range-core/web/persona-panel/tipos.ts:51-59` × `contracts/events.schema.yaml:673-681` (`continuity_action_taken_payload.action_id.enum`) e `domains/academus/continuidade.py:57-86`
  **Requisito violado:** `.claude/rules/modularity.md` R9 §8 (*"Helper único por semântica de invariante — limiar ou distinção usada em dois lugares vive num helper do dono"*); `.claude/rules/documentation.md` R12 §"O que é gerado e não se edita à mão" (o projeto gera `events.ts` e `flags.ts` exatamente para não ter vocabulário fechado escrito à mão no cliente).
  **Evidência:** `grep -rn "ACOES_DE_CONTINUIDADE"` sobre `.py/.ts/.tsx` devolve **quatro** ocorrências, todas dentro de `persona-panel/` — nenhum teste, nenhum verificador. O lado Python **tem** oráculo independente: `tests/test_continuidade.py:276 test_a_tabela_cobre_exatamente_as_sete_do_contrato` lê o enum do YAML e exige igualdade, e ele passa. O cliente não tem o par: acrescentar uma oitava ação ao contrato, ou renomear uma das sete, deixa o painel com um botão que devolve 422 e nada fica vermelho. O comentário de `tipos.ts:44-50` reconhece que o `id` *"é o vocabulário FECHADO do enum"* e argumenta que não é flag nem `event_type` — o que é verdade, e é justamente por isso que INV-3 e INV-5 não o alcançam.

- **[M6] O "cronômetro" de `02` §7:123 é o número que o operador digita: `exercise_minute` é insumo do chamador, e a cadência da queda de sessão não acompanha o relógio de exercício**

  **Arquivo:** `domains/academus/api/app.py:167` (`exercise_minute: int` como parâmetro de query, obrigatório) · `range-core/web/exam-mode/main.tsx:119-126` (campo `<input type="number">`) e `:148-157` (rótulo *"Cronômetro (minuto de exercício)"* pintando `estado.exercise_minute`, que é o eco da entrada)
  **Requisito violado:** `02_DOMAIN_ACADEMUS.md:123` — *"Modo **Prova em andamento**: **cronômetro**, autosave, monitoramento de sessões"*; `01_ARCHITECTURE.md` §3 (*"exercise-clock: T0 definido pelo facilitador; **PAUSAR congela**"* — o relógio de exercício é serviço do servidor, reconstruível do event store).
  **Evidência:** a derivação do frame é do servidor e isso está correto (INV-7 preservado; `check_web_sem_derivacao` rc=0, 22 arquivos, 14 métodos proibidos). O que não é derivado é o **instante**: `ProvaEmAndamento.frame(rota, sujeitos, minuto)` recebe o minuto de fora, e o processo já tem o relógio autoritativo à mão — `processo.py:107-112` constrói `PostgresEventStore(clock_do_store(provisorio), dsn)` e o injeta no mesmo `LeituraDeEstado` que o `ProvaEmAndamento` usa. Consequências concretas: PAUSAR não congela a queda (o chamador escolhe o minuto que quiser), e a rota — cujos papéis são `professor`/`secretaria` (`api_surface.yaml`) — permite pedir o minuto 0 e receber o frame inteiro `alive` com a flag cheia, que é exatamente o par negativo que `ORamoAllAliveMascara` existe para nomear.
  **Por que não é BLOCKER:** o item 1 da DoD cobra *"perde sessões **conforme `lms_session_drop_rate`**"*, e essa cláusula está provada ponta a ponta contra a flag lida do estado (ver tabela). O defeito é do cronômetro, não do "conforme a flag".

---

### LOW

- **[L1]** A L1 da 3ª rodada (prosa de RED obsoleta) foi corrigida **só no arquivo que o achado nomeou**. A mesma prosa, em tempo presente e falsa contra este candidato, sobrevive em `tests/test_console_investigacao.py`: `:434` (*"O defeito atual — handler que encaminha os filtros só ao emissor"*), `:443-445` (*"Hoje o handler grava os filtros no evento e chama a consulta só com `(inicio, fim, agrupar)` ... Essa é a prova de red do B2"*), `:464-466` (*"Hoje o filtro não chega ao SQL ... **Vermelho até a T812**"*), `:505-507` e `:557-563` (*"o defeito **ATUAL**"*, *"a chamada do handler de **hoje**"*). Medido: `app.py:283-291` encaminha os quatro filtros, `repositorio.py:345-363` os aplica, e os três testes são verdes. É a classe "correção varre a linha nomeada, não a classe" que este repositório já documenta em quatro lugares.

- **[L2]** `tests/test_console_investigacao.py:163-168` (`CAMPO_DA_CONSULTA`) é cópia à mão de `domains/api/repositorio.py:303-309` (`_FILTRO_PARA_COLUNA`), sem nada que cruze as duas. Trocar uma coluna na produção e esquecer o teste deixa o `RepositorioFalso` filtrando por um campo que o SQL não usa, e a classe continua verde. Mesma família do [M5], do lado do duplo. O dano de requisito está coberto — `tests/test_console_consulta_servico.py` (13, `@exige_banco`) afere sobre o SQL real e semeia o próprio conjunto —, e é só por isso que fica LOW.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| **1** — Modo "Prova em andamento" perde sessões conforme `lms_session_drop_rate` | **PASS** | `domains/academus/api/prova_andamento.py:64-111` (corte cumulativo `1-(1-taxa)**m` sobre posição fixa de `fracao_do_sujeito`, função pura). `tests/test_prova_andamento.py` (15, **executados**): monotonia no tempo e na taxa, conjunto exato contra oráculo, determinismo, sensibilidade a seed/rota/flag, estabilidade entre **processos** (`_prova_andamento_em_outro_processo.py` — mata o mutante `hash()`), e 4 mutantes demonstrados mortos. A cláusula *"conforme a flag"* é provada pela **rota real** com Postgres+Redis+fold: `tests/test_prova_andamento_integracao.py` (7, `@exige_banco`, **executados, zero pulos**) — taxa 0 → tudo `alive`; inject de taxa cheia → tudo `dropped`; taxa intermediária parte o conjunto; minuto 0 não derruba; e `ORamoAllAliveMascara` é o par negativo que dá poder discriminante ao positivo. Ressalva de cronômetro em **[M6]** — não toca a cláusula cobrada |
| **2** — Console de investigação emite os marcadores automáticos | **PASS** | **Marcador:** `tests/test_console_investigacao.py::OConsoleSetaOMarcadorAuto` afere por `range_core.objectives.projecao.project`, que não lê payload, com controle de ausência **antes** da consulta — oráculo independente da implementação. **Os cinco filtros de `02` §7:124 aplicados à consulta:** `repositorio.py:303-316,345-363` monta o `WHERE` com coluna vinda de `_FILTRO_PARA_COLUNA` (identificador fixo) e valor por binding (R9 §9 — sem concatenação de dado), e `app.py:283-291` os repassa. Provado sobre **SQL real** por `tests/test_console_consulta_servico.py` (13, `@exige_banco`, **executados**): semeia o próprio conjunto na `audit_trail` real, cada filtro isolado recorta subconjunto distinto, interseção de dois eixos, modo agrupado, e `result_count` do evento igual à contagem **filtrada**. Mutantes MS1 (repo que descarta o filtro) e MS2 (repo que troca a coluna) escritos como subclasses do `Repositorio` real e mortos na corrida. `filter_window` é `bool \| None` da rota (`app.py:251`) ao repositório (`:322`), com 422 sem rastro no store para não-booleano |
| **3** — Cada persona vê apenas sua camada `reported` | **PASS** | `range-core/participant/reported.py:83-87` é **conjunção** de duas pernas (`truth_layer ∈ REPORTAVEIS` **E** `evento.persona == persona`), whitelist (`:69`) cobrindo por complemento os cinco valores de `truth_layer`. `tests/test_reported_isolation.py` (10, **executados**) afere **sobre o payload da API** (`GET /participant/view`) e não sobre UI — T6 §73 é explícito: *"teste de payload de API, não de interface"*. Adversarial de verdade: os eventos de `ground_truth`, `evaluator_assessment` e `facilitation` são plantados **marcados para a persona que lê** (`:156-159`), de modo que uma projeção que filtrasse só por persona ainda os devolveria; a asserção de vazamento é `assertNotIn` sobre o **corpo inteiro** (`:262-269`). Persona vem do token (`app.py:353`), nunca de parâmetro; 401 sem token e com token inválido |
| **4** — As sete ações de continuidade aplicam efeito mecânico e custo | **PASS** | `domains/academus/continuidade.py:57-86` cobre as sete, conferidas contra o **enum fechado lido do YAML do contrato** (`test_continuidade.py:276`) — oráculo independente, não lista reescrita; os sete `cost` batem, um a um, com a coluna "Custo" de `02` §9:150-156 (conferi linha a linha). Efeito provado pelo **estado reconstruído**: `simulation_state.py:477-487` aplica `payload["effects"]` genericamente, inclusive o caso de valor `False` (`emergency_document_issuance`). **INV-4 preservado** — a tabela de domínio entra por injeção em `montar()` (`participant/api/app.py:383-386`) e nenhum nome de flag de `academus.*` chega ao core por import (`check_core_boundary`, `check_core_contract_imports`, `check_contract_literals` rc=0). O payload é fechado por `additionalProperties: false` e **recusa `objective_ids` embutido** (`test_continuidade.py:460`). `tests/test_continuidade.py` (20, **executados**): negativos 422/403/401, cada um sem emitir |

**Suíte completa, executada nesta árvore com Postgres e Redis no ar:** `PYTHONDONTWRITEBYTECODE=1 .aurora-audit/venv/Scripts/python -m unittest discover -s tests` → `Ran 949 tests in 107.720s` / `OK` / rc=0. **Zero pulos** — nenhum `skipTest`/`@unittest.skip`/`expectedFailure` disparou; as 8 classes com `skipIf` condicional a DSN rodaram todas. Contagem por suíte da fase conferida contra `.claude/verify/expected_suites.json`: 15 / 16 / 10 / 20 / 10 / 7 / 13, sem `[0, N]` em nenhuma entrada (o M1 da 3ª rodada está fechado).

**Verificadores executados, todos rc=0:** `check_core_boundary` · `check_contract_literals` · `check_event_envelope` · `codegen.py --check` · `check_security_constraints` · `check_synthetic_data` · `check_core_contract_imports` · `check_readme_atual` (+probes, 16 eixos) · `check_progress_consistency` (+probes, 12 eixos) · `check_gabarito_fora_do_git` (+probes, 24 eixos) · `check_banner_de_simulacao` (+probes, 6 telas) · `check_web_sem_derivacao` (+probes, 22 arquivos/14 métodos) · `check_api_surface` (+probes, **70 eixos**, 37 rotas) · `check_telas_sem_vocabulario` (+probes, 23 arquivos/55 termos) · `check_store_read_surface` · `check_insumo_de_metrica` · `check_fold_authority` · `check_spec_flags` · `check_gate_coverage` (481 arquivos classificados) · `check_allowlist_do_auditor` (+probes, 9 eixos) · `check_venv_da_auditoria_probes` (19 formas) · `check_trilha_de_auditoria` · `check_pinned_images` · `check_secoes_de_seguranca` · `check_regras_do_linter` · `check_fabrica_liga_emissor` · `check_hooks_com_emissor` · `check_volumes_da_linha_b`.

**Provas do lançador — as três conferem contra a árvore `d775c63c2424`, idêntica ao `git rev-parse HEAD^{tree}` deste checkout:** `check_provas_de_container` rc=0 (demo ponta a ponta + reinício de container nos dois casos de T5, com as **seis** telas no `npm run build`) · `check_prova_do_seed` rc=0 (3.543.783 linhas; 152,7 s e 146,0 s contra orçamento de 300 s; 20 tabelas com SHA-256 igual) · `check_prova_do_exercicio_4h` rc=0 (650 eventos, 0,260 s contra 3 s, dryrun 4 caminhos; **modo declarado** — o pack não está neste checkout, e vale a segunda perna, os SHA-256 dos sete arquivos).

**Fronteiras de processo:** `git diff <base>..<candidato> -- docs/spec/` → **vazio**; `spec_freeze` respeitado. Âncora em `docs/process/phase_anchors.tsv:50` = `c38f890a…`, idêntica à base declarada. `pins.json._meta.gerado_de_head = d1fcfde` = `HEAD~1`, e `02804c3` altera **apenas** `pins.json` (que se auto-exclui), com as duas únicas mudanças de hash correspondendo aos dois arquivos pinados que `d1fcfde` tocou — `README.md` e `docs/progress/**` estão em `_meta.exclusoes`, com motivo. `git status --porcelain` **vazio** ao fim de tudo (R7 §3). Nenhum binário, PDF ou anexo no diff; `check_synthetic_data` verde sobre a Linha A nova (IP em `198.51.100.0/24`, RFC 5737).

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_console_investigacao.py::MatrizDeAplicacaoDeFiltro` (`:502-574`)** — permanece **auto-contida**: compara dois dublês definidos no próprio arquivo (`RepositorioFalso` × `RepoQueSoAplicaPeriodo`, `:518-534`) e reproduz à mão a chamada do handler em vez de exercitá-lo. Passa sem tocar uma linha de produção. **Não é dano de requisito** — `tests/test_console_consulta_servico.py::MatrizSobreOBancoReal` (`:329-380`) faz a campanha equivalente sobre o `Repositorio` real e Postgres real, e é ela que sustenta o item 2. Registrado porque uma classe que não pode ficar vermelha por defeito de produção não é gate, e porque a prosa dela declara um "defeito ATUAL" que não existe mais (**[L1]**).

2. **`tests/test_console_investigacao.py::OsFiltrosSaoAplicadosAConsulta` (`:429-499`)** — prova a aplicação **contra o dublê**, cujo mapa de colunas (`CAMPO_DA_CONSULTA`, `:163`) é cópia à mão do de produção (**[L2]**). O oráculo verdadeiro do item 2 é o de serviço. A classe é legítima como regressão de encaminhamento; não é a prova do requisito, e o registro (`fase_8.md:64`) já a nomeia corretamente como "sem serviço" desde a correção da 3ª rodada.

Fora esses, **nenhum outro encontrado**. Os quatro itens de DoD têm oráculo independente da implementação (enum lido do YAML, projeção que não lê payload, conjunto semeado no banco pelo próprio teste, estado reconstruído pelo fold), par negativo em cada um, e asserção sobre **payload de API ou estado real** — nunca sobre mock, nunca sobre UI. Nenhuma asserção dirigida por seed onde o requisito é por `fact_id`. Nenhum `skip`/`xfail` sem justificativa, e nenhum disparou.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **O stage `baseline` (R8, pins × HEAD) — nenhum hash conferido.** `.aurora-audit/venv/Scripts/python .claude/verify/check_baseline.py` é **bloqueado pela allowlist** do auditor, que admite `python tools/` e `python scripts/` e não `python .claude/verify/`. Verifiquei apenas a coerência estrutural (`gerado_de_head` = `HEAD~1`; o commit seguinte toca só `pins.json`; as duas linhas alteradas correspondem aos dois arquivos pinados que `HEAD~1` mudou). Idem `check_boundary.py`, `check_state.py`, `check_tdd.py`, `run.sh` e `compliance-audit.sh` — o hook do `UserPromptSubmit` avisou a cada prompt que nenhum pipeline verde está registrado nesta máquina, e não pude resolvê-lo. **Terceira rodada consecutiva com esta mesma limitação**; ela não é achado da fase, mas é superfície do aparato que continua fora do alcance de quem emite o veredito.
- **As sete entradas de `expected_suites.json` individualmente.** Os `cmd` usam `-p "<padrão>"`, forma que a allowlist não admite (a entrada de `unittest` fecha em `tests` com `$`). As contagens declaradas batem com a minha contagem estática de métodos por arquivo (15/16/10/20/10/7/13), e todas rodaram **dentro** da corrida de 949 — mas não as executei isoladas.
- **As três provas do lançador, quanto à execução.** Não vi nenhuma rodar. Vale integralmente a P7-2 declarada no enunciado: a prova amarra a **árvore rastreada** (`d775c63c2424`, que conferi ser a deste checkout), não o commit, e `scenarios/` não entra no hash. A de 4 h vale no **modo declarado**, com a segunda perna sendo os SHA-256 dos sete arquivos do pack.
- **`npm run typecheck` / `npm run build`.** Não há `node_modules` neste worktree. A prova de container mostra `RUN npm run build` completando com as **seis** telas — evidência forte de que a árvore compila —, mas eu não a vi rodar, e os gates de tela que executei (`check_banner_de_simulacao`, `check_web_sem_derivacao`, `check_telas_sem_vocabulario`) são estáticos sobre fonte, com `0 do bundle` varrido aqui.
- **Comportamento em navegador** das três telas novas. Li o fonte inteiro das três. O `<select>` tri-estado do filtro "janela" está correto por leitura e o backend recusa não-booleano com 422 **por execução**; a junta real UI→rota não foi exercitada por mim — e, dado o **[H1]**, ela não tem como ser exercitada por ninguém neste candidato, porque não há rota que sirva as telas.
- **A campanha de mutantes reexecutada após o green (R3 §5).** MS1/MS2 de `test_console_consulta_servico.py` e M1–M4 de `test_prova_andamento.py` **são** suítes reais e os vi passar. Os demais (M1–M7 do console, M1–M4 da isolação) estão descritos em prosa nos cabeçalhos; não verifiquei se a campanha inteira foi reexecutada depois das correções desta rodada.
- **Regressão contra a base.** Não rodei a suíte em `c38f890`. Não foi necessário para nenhum achado: os seis MEDIUM e os dois LOW são estáticos e cada um cita arquivo e linha do candidato; nenhum depende de comparação de execução.
- **`docs/progress/fase_8.md` §7 (fechamento)** não existe, por desenho — é redigida por quem implementou, após o veredito. Não a avaliei. Os itens do **[M2]** e a regra (b) da P7-14 são precisamente o que ela tem de dispor.
- **O número de pulos sem Postgres.** O README passou a declarar `163`; a fase acrescentou 20 testes sob `@exige_banco`, o que é consistente com os 143 anteriores. Não consegui medir a suíte **sem** Postgres — a allowlist não admite manipular variável de ambiente. Afirmo a consistência aritmética, não o valor.

---

**Nenhum BLOCKER ⇒ PASS.** A substância da fase está provada, e provada da forma certa: os quatro itens de DoD têm oráculo independente da implementação, par negativo e asserção sobre payload de API ou sobre estado reconstruído — o item 3 sobre o corpo inteiro da resposta com as camadas superiores plantadas **marcadas para a persona que lê**, o item 2 sobre SQL real com o conjunto semeado pelo próprio teste, o item 4 contra o enum lido do YAML do contrato. Os dois BLOCKERs da 2ª rodada e o B1 da 3ª foram fechados por correção de substância, nunca por afrouxamento de asserção, e os cinco MEDIUM e dois LOW daquela rodada estão todos dispostos — três corrigidos, dois promovidos a `P8-3` e `P8-4`. O que sobra é de outra natureza e é bookkeeping de fronteira: a fase chamada "Web completo" fecha com as três telas que construiu sem rota e com a `participant-api` sem processo, e isso não tem linha na §6 (**[H1]**); a §6 carrega três pendências como abertas cuja correção está na própria base da branch (**[M1]**); e quatro vencimentos que nomeiam "esta fase" expiram sem redatação, pela porta que a P7-19 fechou só para `ENTREGA` (**[M2]**). Nenhum deles reprova a fase; todos eles atravessam para a Fase 9 se não forem escritos agora.
