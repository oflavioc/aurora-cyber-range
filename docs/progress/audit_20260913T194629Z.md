# Auditoria de checkpoint - 2026-09-13T19:46:29.166880+00:00

- fase: **7**
- commit auditado: `a37d4f333cb84bb8ebe0d49ab77c9b62edf06028`
- session_id: `acd3bcae-f3f4-4ec8-82ca-7373fef6c1da`
- modo: `headless`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

All citations confirmed. Here is the report.

# AUDITORIA — FASE 7

## VEREDITO: FAIL

Base: `22f5f49bfa30ca3fd32911a4749b41063b8d1ac4` (fornecida pelo lançador, não resolvida por mim). `HEAD` = `a37d4f333cb84bb8ebe0d49ab77c9b62edf06028`, árvore `2345118e0ece8ef4f3b85144cef774e7f5b18659` — confere com a árvore que os **três** artefatos de prova declaram. Guarda de base: **PORTA**, verbatim do lançador. 4ª rodada desta fase.

**Gates executados (R2 §1):**

| Gate | Resultado |
|---|---|
| `unittest discover -s tests` (venv do lançador) | **856 testes, OK**, 0 falhas, **0 skips** — os 10 `skipIf/skipUnless(_URL)` não dispararam |
| `check_provas_de_container.py` | rc=0 — árvore `2345118e0ece` confere; `demo` rc=0, `reinicio` rc=0, build completo verde |
| `check_prova_do_seed.py` | rc=0 — 3.543.783 linhas, 153,0 s / 158,8 s < 300 s; 20 tabelas SHA-256 idênticas |
| **`check_prova_do_exercicio_4h.py`** | **rc=0 — o B1 da 3ª rodada FECHOU**; 650 eventos, 4 caminhos/2 pontos, 0,028 s contra 3 s |
| `check_prova_do_exercicio_4h_probes.py` | rc=0 — 9 venenos, 2 controles positivos |
| `check_core_boundary`·`check_event_envelope`·`check_contract_literals`·`check_security_constraints`·`check_synthetic_data`·`codegen.py --check` | rc=0 (6/6) |
| `check_progress_consistency`(+11 eixos)·`check_regras_do_linter`(+8)·`check_core_contract_imports`(+7)·`check_secoes_de_seguranca`(+14) | rc=0 |
| `check_insumo_de_metrica`·`check_fold_authority`·`check_store_read_surface`·`check_gate_coverage`(438)·`check_web_sem_derivacao`·`check_banner_de_simulacao` | rc=0 |
| `check_api_surface`(34 rotas)·`check_contract_examples`(13 pos., 95 neg.)·`check_gabarito_fora_do_git`·`check_readme_atual`·`check_allowlist_do_auditor` | rc=0 |
| `.aurora-audit/venv/Scripts/range-cli scenario lint tests/fixtures/pack_minimo` | rc=2, achado correto — console script alcançável |
| `git diff <base> HEAD -- docs/spec/` | **vazio** — nenhuma spec tocada junto com código |

---

## FINDINGS

### BLOCKER

- **[B1] O item 7 da DoD não passa pela letra: não existe migrador, e `SUPPORTED_SCHEMA_VERSIONS = (2,)` contraria três documentos normativos sem `spec-change` — e agora ele é a única coisa entre a fase e o fechamento**

  **Arquivo:** `range-core/engine/loader/pack_loader.py:126` (`SUPPORTED_SCHEMA_VERSIONS: tuple[int, ...] = (2,)`); `range-core/engine/migrations/__init__.py:96` (`MIGRACOES = MappingProxyType({})`); `tests/test_migrations.py:35` (`ORegistroEstaVazioEIssoEADeclaracao`) e `:38` (`test_nao_ha_migracao_declarada`); `docs/progress/fase_7.md:1246` (P7-18, `ABERTA`)

  **Requisito violado:** `04_SCENARIO_SCHEMA.md` §4 — *"Engine declara `SUPPORTED_SCHEMA_VERSIONS = [N, N-1]`"*, *"Pack em N-1 carrega com migração em memória e aviso no boot"*, *"Migrações em `range-core/engine/migrations/v<n>_to_v<n+1>.py`, cada uma com teste"*; `07_IMPLEMENTATION_PHASES.md` §"Fase 7" item 7; `06_ACCEPTANCE_TESTS.md` T12, 1º critério; `CLAUDE.md` §"Fluxo por fase" (*"Uma fase só está concluída quando **todos** os itens da Definition of Done passam"*).

  **Evidência — observada, não suposta.** `ls range-core/engine/migrations/` → só `__init__.py`. `MIGRACOES` é `MappingProxyType({})`. Com `(2,)` **não existe N-1**: pack v1 é *recusado*, não *migrado*. A 2ª metade do item (v0 recusado com instrução) passa e está testada (`pack_loader.py:688-740`). O próprio módulo declara a divergência como deliberada (`migrations/__init__.py:70-73`).

  **Por que é blocker, e por que eu escalo o que a 3ª rodada chamou de HIGH.** O argumento técnico do implementador está certo e eu não o recomendaria diferente — v1 nunca existiu, e migrador-identidade é pior que nenhum. O rito também está correto: a divergência virou P7-18 com texto de `spec-change` proposto e apresentação ao proprietário. **Não há ação de implementador pendente aqui, e nenhum trabalho de implementação destrava este achado.**

  A escalação é de consequência, não de mérito, e tem uma razão só: nas rodadas 2 e 3 este item ficava **atrás de um BLOCKER real**, e a classificação dele não decidia nada. Agora ele é o único item que não passa. Se eu der PASS, a fase funde por rebase em `main` com a DoD em **8/9**, e três documentos normativos passam a afirmar um comportamento que o código recusa — sem nenhum gate que volte a perguntar. O carregador dessa pendência para a Fase 8 é o mecanismo de migração de pendências, que esta mesma auditoria registra como **frágil e aberto (P7-19)**. É exatamente a forma que este repositório documenta como "é assim que um requisito morre sem nada ficar vermelho".

  **O remédio é do operador, não do implementador:** aprovar o `spec-change` proposto em P7-18 (`04` §4 passa a dizer *"`[N, N-1]` a partir da primeira versão que tiver antecessora real; enquanto N-1 não existir, `[N]`, com recusa instruída"*), em PR próprio com aprovação humana. Decidido isso, o item passa pela letra e a reauditoria é formalidade.

---

### HIGH

**Nenhum.** Os dois HIGH da 3ª rodada foram fechados e eu os medi por execução:

- **B1 da 3ª rodada** (a prova de 4 h não existia) — **CORRIGIDO**. `check_prova_do_exercicio_4h.py` sai **rc=0**, a árvore gravada (`2345118e0ece`) é a deste checkout, e o produtor entrou no lançador (`start_checkpoint_audit.sh:585-600`, bloco de transporte com os estados `transportada`/`ausente`). Li o produtor: ausência imprime *"os itens 6 e 9 da DoD ficam NAO VERIFICADO"* e **não degrada para PASS**.
- **H1 da 3ª rodada** (probes fora do CI) — **CORRIGIDO**. `.github/workflows/invariants.yml:300-310`, passo próprio no job `arquitetura`, ao lado das duas irmãs, no desenho probes-only (R13). Os 9 venenos + 2 controles passam localmente.

### MEDIUM

- **[M1] A guarda de IOC/dado sintético alcança só o `ground_truth.yaml` do pack; os outros cinco documentos não são varridos por ninguém — e eles também não estão na árvore**

  **Arquivo:** `range-core/engine/loader/pack_loader.py:576` (`yield lambda: confere_ausencia_de_ioc(ground_truth)` — o único chamador do lado do pack) × `range-core/engine/loader/pack_loader.py:429` (`texto_para_plateia` é lido de `injects.yaml`) × `tools/check_synthetic_data.py:114` (varre a **árvore versionada**) × `.gitignore:125` (`scenarios/` fora do Git)

  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §3 — *"Nenhum endereço, telefone ou e-mail real ou existente; Domínios apenas de faixa reservada a documentação; IPs apenas de faixas de documentação (RFC 5737 / RFC 3849) ou privadas"*; §5.2, 3º bullet — *"As faixas de documentação da §3 continuam obrigatórias em toda evidência"*.

  **Evidência — medida, não suposta.** `grep -rn "achados_na_arvore\|achados_no_valor\|confere_ausencia_de_ioc"` sobre `range-core/ range_cli/ tools/ scripts/ domains/` devolve **um único chamador do lado do pack**: `pack_loader.py:576`, e ele recebe `ground_truth` e nada mais. `manifest.yaml`, `injects.yaml`, `branches.yaml`, `objectives.yaml` e `information_distribution.yaml` não passam pelo predicado em carga nem em lint. E `check_synthetic_data.py` não os alcança por outro caminho: ele varre a árvore versionada, e o pack está fora dela.

  `injects.yaml` é justamente o documento que carrega prosa **voltada ao participante** — `texto_para_plateia` sobe ao `LoadedPack` em `pack_loader.py:429`. Um inject com domínio registrado real ou e-mail existente passa hoje por todas as camadas, e nada fica vermelho.

  **Por que é MEDIUM e não menor:** é literalmente a falsidade que **esta fase nomeou e consertou pela metade**. O docstring de `confere_ausencia_de_ioc` (`:1209-1213`) diz, com todas as letras, que `check_synthetic_data.py` *"varre a árvore versionada, e `scenarios/` está fora do Git… o pack nunca passou por ele"*, e que por isso a frase do `ground_truth.schema.yaml` *"para pack essa frase era falsa"*. A correção foi aplicada a um dos seis documentos; para os outros cinco a mesma frase continua falsa, pelo mesmo motivo. **Não é MEDIUM maior** porque o limite conhecido do predicado (prosa com espaço escapa) está declarado e medido, e porque a regra do registro se chama `sem_ioc_operacional_no_gabarito` — ela não promete o que não entrega, então não há over-claim. O que falta é alcance, e ele não tem linha na §6.

### LOW

**Nenhum.**

---

**Regra de achado órfão (P7-6), conferida item a item.** A 3ª rodada (`audit_20260913T184853Z.md`) teve **zero** MEDIUM e **zero** LOW — não há órfão possível dela. Os M/L da 2ª rodada estão todos dispostos, e eu confirmei por execução, não por leitura do registro: **M1** — `tools/check_contract_literals.py:86` e `tools/check_security_constraints.py:44-53` incluem `range_cli` e `dados_sinteticos`, os dois rc=0; **M2** — `range-cli` do venv executa e devolve rc=2 com o achado correto; **L1** → **P7-20** (`fase_7.md:1248`). Nenhum achado M/L de rodada anterior ficou sem linha na §6.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `lint` recusa inject sem objetivo e sem `noise: true` | **PASS** | `tests/test_range_cli_lint.py:255` e `:273` — as duas pernas; a 2ª impede que uma regra que exigisse `objectives` de todo inject passasse. Executados nos 856 |
| 2 — `event_type` inexistente em condição de branch, **com posição** | **PASS** | `:294` e `:311`; o 2º exige `linha[coluna-1:] == "vpn_acess_revoked"` — apontar para a chave `event:` reprovaria |
| 3 — condição por juízo do facilitador recusada | **PASS** | `:329`, com posição derivada da fixture; `branch_condition` com `oneOf` fechado + `additionalProperties: false` |
| 4 — `branch_policy` do manifesto aplicada | **PASS** | `tests/test_branching.py:159-206`; `confere_branch_policy` entra em `_passos` (`pack_loader.py:564`), logo vale na carga **e** no lint. Manifesto sem política não inventa default |
| 5 — branch sem `reconverge_at` recusada | **PASS** | `scenario.schema.v2.yaml` `required: [id, at_inject, evaluate, reconverge_at]`; negativo entre os 95 executados; `test_range_cli_lint.py:486` |
| 6 — `dryrun` percorre todos os caminhos | **PASS** | Fixture: `tests/test_range_cli_dryrun.py:27-69` (4 eixos, incl. lint-antes-da-travessia e `branch_walk_impossible`). **Pack real: agora verificado** — `.aurora-prova-do-exercicio-4h.json` grava `item_6_dryrun_todos_os_caminhos: true`, 4 caminhos em 2 pontos, e o produtor (`prova_do_exercicio_4h.py:102-107`) **executa** `branching.percorre`, não transcreve |
| 7 — pack v1 migra automaticamente; v0 recusado com instrução | **FALHA na 1ª metade (letra)** | Não existe migrador; `SUPPORTED_SCHEMA_VERSIONS = (2,)`. 2ª metade **PASS** e testada. **B1** |
| 8 — fato citado no `GM_NOTES.md` e ausente do `ground_truth.yaml` é recusado | **PASS** | `range-core/engine/citacoes.py`, forma derivada de `ground_truth.schema.yaml §$defs/fact_id_pattern`; `tests/test_citacoes_de_fato.py:88` lê o padrão do contrato e `:91` recusa alto se o `$def` sumir. Recusa **na carga** contra o pack materializado. **Dirigido por `fact_id`, não por seed** |
| 9 — reconstrução do exercício de 4 h em < 3 s | **PASS** | `check_prova_do_exercicio_4h.py` rc=0: **0,028 s contra 3 s**, 650 eventos, com máquina/data/stack (`Windows-11`, `2026-09-13`, `PostgreSQL 16.4 / psycopg 3.2.12 / migration 0004`) — forma de T12 satisfeita. Amarração dupla: árvore `2345118e0ece` + SHA-256 dos 7 arquivos do pack |
| *(T12, critério 7 — `capability_gap` citando objetivo inexistente, com posição)* | **PASS** | Não está na lista de 9 da DoD, mas é critério de aceitação desta fase. `tests/test_range_cli_lint.py:355`, caminho `$.injects[0].decision_point.options[0].capability_gap.objectives_affected[0]`, posição ancorada em `OBJ-99` |
| *(Fase 4, itens 1 e 4 — regressão das rodadas 2/3)* | **PASS** | `check_provas_de_container.py` rc=0 sobre esta árvore; `demo` e `reinicio` rc=0 |

**DoD: 8 de 9 pela letra.**

---

## TESTES QUE NÃO PROVAM O REQUISITO

**Nenhum encontrado.** Verifiquei por leitura das asserções, não dos nomes:

- **Nenhum skip disparou.** `Ran 856 tests … OK`, sem `(skipped=N)`. Os 10 `skipIf/skipUnless` são todos condicionais a `_URL`, e os serviços estavam no ar. Skip aqui seria defeito; não houve.
- **Nenhum mock de comportamento no código da fase.** Os únicos `mock.patch` da árvore são `tests/test_contract_rules.py:143,158` (o mock é o *objeto sob teste* — prova que o harness detecta validador quebrado) e `tests/test_telas.py:131,149` (indireção de diretório). Nenhum dos dois está no diff desta fase.
- **`um_achado` (`test_range_cli_lint.py:195`) exige exatamente UM achado do sítio esperado** — fixture com dois defeitos passaria provando o errado, e a colheita do linter torna esse engano fácil.
- **`assertPosicaoNaAncora` (`:210`) deriva a linha do texto da fixture**, exige a âncora exatamente uma vez e exige `posicao.exata` — posição aproximada não satisfaz critério que pede posição.
- **Item 8 é dirigido por `fact_id` e lê a forma do contrato** (`:88`), com `:91` recusando alto se o `$def` sumir — o desenho que separa "lê o contrato" de "acertou a regex por sorte".
- **O verificador novo tem prova negativa real e executada:** 9 venenos em 7 direções + 2 controles positivos, incluindo o modo declarado "pack ausente do checkout". Foi o que me permitiu afirmar que o rc=0 é conferência, e não degradação silenciosa.
- **O produtor da prova grava o arquivo mesmo em falha** (`prova_do_exercicio_4h.py:116-121`), de propósito — é assim que "falhou" se distingue de "ninguém rodou". Verifiquei que `lint`, travessia e medição são **executados**, não transcritos.
- **A forma da medição confere com o bench da Fase 2**: `total_s = t_read + t_project`, e `read_all` já inclui a cadeia (`bench_reconstruction.py:45`) — `cadeia_s` é decomposição, não parcela omitida. Não há subcontagem.

Observação sem finding (mantida das rodadas anteriores, e continuo concordando): `branching.percorre` enumera um caminho por braço de cada ponto, e não o produto cartesiano entre pontos de linhas distintas. `04` §6/§9 e T12 não exigem o produto, e as janelas são por linha — leitura registrada em `fase_7.md` §5.2 **antes** do código.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **O conteúdo do pack `ransomware-universidade`.** `scenarios/` não existe neste checkout (`.gitignore:125`, decisão da Fase 5). A prova de 4 h fecha a lacuna **de identidade** (SHA-256 dos sete arquivos) e **de comportamento** (lint limpo, travessia completa, 0,028 s), mas não me deixa ler os bytes. Não pude conferir 40 injects, 5 `decision_point`s, nem inspecionar prosa voltada ao participante — que é a superfície do M1.
2. **As três provas, rodando.** Não vi nenhuma das três executar. O que as amarra a este objeto é o hash da árvore, conferido em cada uma, mais os sete hashes do pack na de 4 h. Limite declarado na P7-2: a prova amarra **árvore**, não commit, e a árvore cobre só conteúdo rastreado.
3. **`gen_pins.py --check` (R8).** Fora da allowlist do auditor; **não executado**. O último commit (`a37d4f3`) é um repin declarado, em commit próprio com motivo, como a R8 exige — mas não pude confirmar a identidade pins × HEAD por execução.
4. **O pipeline da Estrutura Agêntica** (`.claude/verify/run.sh`, `compliance-audit.sh`). Fora da allowlist; não executado. O hook de prompt registra que nenhum pipeline verde foi gravado nesta máquina — declarado, não omitido.
5. **O ambiente da suíte não é o canônico.** O venv do lançador é **Python 3.14.7** (`.aurora-audit/venv/pyvenv.cfg`), contra `00_MASTER_SPEC.md` §8 e o `python-version: "3.12"` do CI. Os 856 passaram, logo não houve vermelho a diagnosticar por causa (R2 §3) — mas a suíte que rodei não é a do CI Linux 3.12. A diferença aparece como `ResourceWarning` em massa do `psycopg`, já registrada na P7-14.
6. **P7-10 — o gabarito só tem Linha B.** `fase_7.md:1238` declara que o `ground_truth.yaml` materializado não tem fato de `initial_access`/`exfiltration`, contra `03` §3.1 e `04` §3/§9, com `TTRV` incomputável. Não pude medir — o arquivo não está na árvore. `ABERTA` com gatilho *"fechamento desta fase"*, logo **não é achado órfão**; registro que ela pesa sobre o insumo do item 9.
7. **P7-11 — condição temporal de branch carrega e nunca ramifica.** Não pude medir de outro modo: o branching de **runtime** não é entregável desta fase (o ponteiro declara o destino em `754a5c9`), então não há caminho executável onde o defeito se manifeste hoje. Tem linha na §6 e o pack de 4 h evita condição temporal, com a omissão declarada.
8. **Comportamento de runtime em container** além das duas provas gravadas. Inalcançável por construção — o auditor não sobe stack.
