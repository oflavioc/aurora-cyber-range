# Auditoria de checkpoint - 2026-08-21T12:13:25.571753+00:00

- fase: **6**
- commit auditado: `4c8d2d6c26394265c5d7c89849e61ebfe1b2e7aa`
- session_id: `1cf55bc3-c0b6-481b-8c6d-632997bbd9c8`
- modo: `interactive`
- veredito detectado: **indeterminado**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have completed what this environment allows. Writing the report.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

O FAIL é por **não-verificabilidade**, não por defeito encontrado. A leitura
estática do diff não achou desvio de spec; a suíte que provaria os itens da DoD
**não executou uma única vez**. Emitir PASS aqui seria certificar por leitura —
que é exatamente o modo de auditar que este aparato existe para recusar
(B1 da Fase 2, H3 da Fase 1, M5 da quarta da Fase 3, todos citados no cabeçalho
de `readonly_bash.py`).

## FINDINGS

### BLOCKER

- [B1] A auditoria não pôde executar nenhum teste da fase: 226 de 226 métodos de
       teste da Fase 6 não rodaram
  Arquivo: `scripts/start_checkpoint_audit.sh:212-239` × `~/.claude/hooks/readonly_bash.py:695-776`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §Fase 6 (DoD, 17 itens);
       `06_ACCEPTANCE_TESTS.md` T9, T10, T11; definição do auditor —
       *"execute a suíte de testes e leia a saída real; não confie no que o
       código afirma fazer, confie no que os testes provam"*
  Evidência: observada, não suposta.
    - `python -m unittest discover -s tests` (a forma exata que o CI roda,
      `invariants.yml:620`) → `Ran 56 tests`, `FAILED (failures=4, errors=67)`.
    - As 67 `errors` são todas de carga: `ModuleNotFoundError: No module named
      'range_core' / 'yaml' / 'jsonschema' / 'fastapi' / 'jwt'`.
    - As 4 `failures` são todas de `test_procedencia_dos_pacotes` /
      `test_procedencia_probes`, e a mensagem é literal:
      `range_core: range_core.state.simulation_state nao pode ser importado
      (No module named 'range_core')`.
    - `which python` → `/c/Python314/python`; `python -m pytest --version` →
      `No module named pytest`; `which pytest python3.12` → não encontrados; o
      `PATH` do shell não contém `.aurora-worktrees/venv/Scripts`.
    - Causa medida: o lançador cria o venv da P3-4 **fora** do worktree
      (`VENV="$ROOT/.aurora-worktrees/venv"`, `start_checkpoint_audit.sh:212`,
      deliberado — *"o worktree E o objeto da auditoria"*) e o entrega só por
      `export PATH="$VENV_BIN:$PATH"` (:237). Esse export não sobrevive ao shell
      do Bash tool, que é inicializado do profile do usuário. O caminho direto
      ao binário é negado por `_alvo_nao_contido`, que recusa caminho absoluto
      existente fora do worktree (:765-774), e `..` é negado por
      `DENIED_ANYWHERE` (:444). `py -3.12` — o interpretador do projeto — não
      está em `ALLOWED`.
    - Contagem do que ficou sem rodar, por arquivo: `test_metrics_epoch` 30,
      `test_metrics_verificacao` 30, `test_metrics_declaracao` 29,
      `test_metrics_calibracao` 25, `test_aar_timeline` 24,
      `test_laco_de_verificacao` 14, `test_objectives_projecao` 13,
      `test_verificacao` 11, `test_metrics_insumo` 9, `test_rubrics_library` 9,
      `test_assessment_contrato` 8, `test_participant_contrassinatura` 8,
      `test_derivacao_das_nove_siglas` 7, `test_api_emissao` 6,
      `test_cobertura_dos_contratos` 3. **Total 226, executados 0.**
    - Efeito colateral confirmando a mesma causa: `check_readme_atual.py`
      reprovou com `a descoberta de testes acusou erro de carga` e
      `README.md: 'testes' afirma ['637'] e a arvore diz '56'`.
  Por que é blocker: a guarda de base declarou **PORTA**, e uma porta que não
  mede não é porta. Dezesseis dos dezessete itens da DoD têm como evidência um
  teste; nenhum foi exercitado. Os probes negativos que sustentam T10 —
  as três violações da folha de predicado, a métrica simples plantada lendo o
  veredito, a metade de declaração plantada lendo a verificação — vivem em
  `check_contract_examples.py` e nos módulos de teste, e **nenhum deles rodou**.
  A consequência concreta é que um defeito de cálculo nesta fase não produz erro,
  produz número errado — `00_MASTER_SPEC.md` §3.2 fecha dizendo *"a métrica
  continua sendo calculada. Nada falha"* —, e é precisamente essa classe que só
  o teste pega.

  Atribuição, dita para não ser lida errado: **isto não é defeito do commit
  candidato.** O diff da Fase 6 não toca nem o lançador nem a regra de alvo do
  hook; a única alteração dele no aparato é a admissão de
  `check_insumo_de_metrica|check_insumo_de_metrica_probes`
  (`user-scope/hooks/readonly_bash.py:331-354`), que segue a regra escrita
  — *"script novo entra por nome, no commit que o cria"* — e que eu executei.

### HIGH

nenhum encontrado.

### MEDIUM

nenhum encontrado.

### LOW

nenhum encontrado.

## DoD — item a item

Legenda: **VERIFICADO** = eu executei o mecanismo, ou li o dado normativo e o
conferi. **NÃO VERIFICADO** = código lido e coerente, teste não executado.
Nenhum item vira PASS por leitura de código.

| Item | Status | Evidência |
|---|---|---|
| 1. Consulta com filtro de período emite `audit_query_performed` | NÃO VERIFICADO | Rota `GET /audit/grade-changes` em `domains/academus/api/app.py:202-248` chama `emissor.registrar_consulta`; `emissor.py:68-98` emite `AUDIT_QUERY_PERFORMED` com os 4 `payload_fields` de `observability_hooks.yaml:30`. `check_api_surface.py` **rc=0** confirma `emite: audit_query_performed` declarado (`api_surface.yaml:378`). Os 6 testes de `test_api_emissao.py` não rodaram (`fastapi` ausente). |
| 2. Cada par declaração × verificação disparável e testado | NÃO VERIFICADO | `tests/test_laco_de_verificacao.py::OCaminhoFechaAteAMetrica` e `test_metrics_verificacao.py` (30 testes) existem e são específicos. Nenhum executou. |
| 3. Desconto por **união**, nunca soma | NÃO VERIFICADO | `range-core/metrics/epoch.py:280-297` implementa fusão por sobreposição e adjacência; `decorrido` (:300-327) recorta à janela. `tests/test_metrics_epoch.py:105-164` afirma explicitamente `união < soma` nos casos sobrepostos, com controle positivo para disjuntos (:149) — teste forte, **não executado**. |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | NÃO VERIFICADO | `epochs_descartadas` / `no_calculo` (`epoch.py:128-192`); consumido em `verificacao.py:138-142` e no lado da declaração. `test_metrics_epoch.py::EnsaioDescartaAEpoch` (5 testes) não executou. |
| 5. Declaração não altera ground truth | **VERIFICADO (estrutural)** + teste não executado | Conferido no dado normativo: a conjunção `effect_class == state_effect ∧ metric_side == verification` sobre `contracts/events.schema.yaml:680-787` produz **exatamente** `fact_materialized, attack_stage_reached, continuity_action_taken, vpn_access_revoked, identity_scope_disabled` — os cinco que `09_EVENT_MODEL.md` §4.0:155 nomeia. Nenhum tipo de lado `declaration` é folha de predicado, logo nenhuma declaração pode satisfazer predicado. `test_laco_de_verificacao.py:258-289` afirma isso com controle positivo; não executou. |
| 6. Declaração prematura → lista de eventos incompatíveis no AAR | NÃO VERIFICADO | `range-core/aar/timeline.py` (320 linhas) computa as três janelas; `test_aar_timeline.py::OsEventosIncompativeis` (4 testes) inclui os negativos certos (fato antes da declaração não entra; ação de participante não entra). Não executou. |
| 7. Pack sem `verification_predicates` não carrega | NÃO VERIFICADO | `contracts/ground_truth.schema.yaml:24` — `required: [facts, verification_predicates]`. **Anterior à fase** (o diff só altera um comentário nesse arquivo). Não localizei teste da Fase 6 plantando pack sem o bloco; a garantia é de schema. `check_contract_examples.py` não executou (`jsonschema` ausente). |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | NÃO VERIFICADO | `range-core/metrics/verificacao.py:163-194` — recalcula o Brier a cada prefixo de `assessment_submitted` e devolve a primeira submissão com `escore <= limiar`; `<=` justificado contra `04` §2. Não há folha de mundo nesse caminho. 30 testes não executados. |
| 9. Brier só no escopo revisado; lacuna de cobertura em separado | NÃO VERIFICADO | `calibracao.py:134-162` itera `sorted(escopo)`; `_lacunas` (:245-260) devolve indevido comprovado **fora** do escopo, em campo próprio de `Calibracao`. Fórmula bate com `03` §5.3:348. Constantes 80/0.2/30/1.0 batem com §5.4:359-360. 25 testes não executados. **Limite declarado:** a resolução da prosa de `review_scope` em `case_id` é P6-5, decidida e adiada para a Fase 7 (`contracts/assessment.schema.yaml:26-34`, `fase_6.md:770-798`); o escopo chega como dado. |
| 10. Rubrica ausente ou em versão divergente impede carga | NÃO VERIFICADO | `contract_rules.py:177-192` — a versão está **dentro do id** (`incident_triage.v2`), então "versão diferente" recusa pelo mesmo mecanismo de "ausente". As 9 competências de `03` §2.3:103 existem em `range-core/rubrics/` com os nomes exatos, `incident_triage.v2` e `crisis_communication.v2` incluídos; 5 âncoras comportamentais conferidas em `incident_triage.v2.yaml`. `test_pack_loader.py:405,413` cobre as duas direções; não executou. |
| 11. Divergência ≥ 2 pontos gera alerta | NÃO VERIFICADO | `aar/timeline.py:94` — `DIVERGENCIA_QUE_ALERTA = 2`, casando `03` §2.4:107. `test_aar_timeline.py::ADivergenciaEntreAvaliadores` (7 testes) cobre extremos vs consecutivos, última nota, um avaliador só, e que o alerta não resolve. Não executou. |
| 12. As nove siglas classificadas pelo critério de `00` §3.2 | NÃO VERIFICADO | `tests/test_derivacao_das_nove_siglas.py` **não é tautológico**: extrai a tabela de `03` §3.0 do documento, aplica a conjunção às três colunas e exige o resultado da quarta; e cruza as siglas contra as que os dois computadores de fato produzem, rodando-os (:228-299). Desenho correto, **não executado**. |
| 13. Todo `event_type` tem `metric_side`; os quatro lados disjuntos | **VERIFICADO (dado)** | Contagem manual sobre `events.schema.yaml:680-733`: verification 7, declaration 9, epoch 5, none 12 = **33**, igual ao catálogo de `09` §4.1. Disjunção por construção de `dict`, com checagem adicional de lado vazio em `contract_rules.py:142-153`. O código da checagem não foi executado. |
| 14. Folha de predicado obedece à conjunção, com probe das 3 violações + controle positivo | NÃO VERIFICADO | `contract_rules.py:169-173` implementa a conjunção. `contracts/ground_truth.schema.yaml` traz **6** exemplos negativos com `rejected_by: x-aurora-ref:event_catalog_predicate_leaf` (:651,670,695,713,732,746), cobrindo fora-do-catálogo, declaração, autorreferência, `assessment_submitted`, comunicação e `decision_made`. O executor é `check_contract_examples.py`, que **não rodou** (`jsonschema` ausente). |
| 15. Cada computador recebe só o seu lado; só o AAR tem as duas metades | **VERIFICADO** | `python scripts/check_insumo_de_metrica.py` → **rc=0**. `python scripts/check_insumo_de_metrica_probes.py` → **rc=0**, com saída literal: *"6 violacoes plantadas, 6 reprovadas; o consumidor que so anota passa, e a arvore limpa passa"*. Os seis negativos incluem alias para `Sequence[Event]`, `NewType` com nome divergente, insumo ganhando o event store, escalar sumindo, e construtor invocado fora de `monta` (no mesmo módulo e em outro arquivo). Confere com `00` §3.2 (1),(2),(3). |
| 16. União, `rehearsal` e seleção de start são cálculo do consumidor | NÃO VERIFICADO | `metrics/insumo.py:152-165` entrega a escrituração de epoch **inteira aos dois lados**; todo consumo está em `verificacao.py:138-142` e em `epoch.py`. `test_metrics_epoch.py:374-447` afirma a **presença do insumo antes da conta**, que é o eixo que distingue "descontado" de "perdido". Não executou. |
| 17. `inject_fired` emite os marcadores de start, com teste de emissão | NÃO VERIFICADO | `inject_engine.py:307-330` carimba `observable_impact` e `requires_response` no payload. `observable_impact` é **derivado na carga** por `_tem_impacto_observavel` (`pack_loader.py:586-608`), com as três pernas de `03` §3:133-137 e a exclusão de `reveals` (§3:141). Conferi as três pernas contra a spec, uma a uma. `test_inject_engine.py` (+51 linhas) não executou. |

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado por leitura — e registro que **leitura é o limite deste
achado**, porque nenhum deles rodou.

O que procurei e não achei:
- **Asserção sobre mock:** zero. `grep` por `Mock|patch\(|MagicMock` nos 15
  arquivos de teste da fase não retornou nada.
- **`skip` / `xfail` sem justificativa:** zero na fase. Os únicos skips da
  árvore são `skipIf(_URL is None)` por ausência de Postgres, todos anteriores
  à Fase 6.
- **Teste tautológico:** o candidato natural era
  `test_derivacao_das_nove_siglas.py` — comparar a tabela consigo mesma. Ele
  evita isso lendo as colunas da spec e aplicando a conjunção, e o próprio
  docstring nomeia o defeito que evita (:10-24). A terceira direção
  (:286-299) rodaria os computadores em vez de ler um mapa — e o docstring
  registra que a versão anterior desse teste **não reprovou** quando devia,
  por estar ancorada em `SIGLA_POR_PREDICADO`.
- **Teste que passaria com a implementação errada:** o caso clássico aqui é
  união vs soma. `test_metrics_epoch.py:114-129` afirma `união < soma` e
  explica que sem essa segunda asserção os casos disjuntos passariam com uma
  implementação somadora. Tem controle positivo (:149-157) contra o erro
  oposto — uma `uniao` que funda tudo.
- **Número certo por ausência de insumo:** coberto de propósito em
  `test_metrics_epoch.py:374-381`, que afirma a presença antes da conta.

Observação sobre o item 7, que é a costura mais fina que encontrei: *"pack sem
`verification_predicates` não carrega"* está garantido por `required:` no
schema, mas **não localizei teste da Fase 6 que plante um pack sem o bloco e
observe a recusa**. Não é finding — o mecanismo existe e é o correto —, mas é
o item da DoD cuja evidência executável eu menos consegui localizar.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **A suíte inteira — 226 testes da fase, 637 na árvore.** É o B1. Sem
   interpretador com as dependências, dezesseis dos dezessete itens da DoD
   ficam sustentados por leitura. Para a próxima rodada, o caminho mais curto é
   `ALLOWED` aceitar `py -3.12 -m unittest discover -s tests` (o interpretador
   do projeto), ou o venv da P3-4 nascer **dentro** do worktree descartável, ou
   o lançador propagar o `PATH` de forma que sobreviva ao shell da ferramenta.
   Não corrijo nada — reporto.

2. **Verificadores que exigem dependências ausentes:**
   `check_spec_examples.py` → `rc=2`, *"dependencias ausentes"*.
   `check_contract_examples.py` (o executor dos 6 negativos do item 14) →
   mesma causa. `check_readme_atual.py` → `rc=1`, mas por discovery quebrado,
   não por defeito da árvore.

3. **Itens 1 e 4 da DoD da Fase 4 — NÃO VERIFICADO, como o lançador instruiu.**
   `python scripts/check_provas_de_container.py` → **rc=2**, recusa com motivo:
   as provas `demo` e `reinicio` saíram `rc=125`, *"NAO EXECUTADA: a stack de
   containers nao subiu"*, com `failed to connect to the docker API`. Não
   degrada para "ok", e eu não o trato como tal.

4. **M2 da Fase 5 — NÃO VERIFICADO.** `python scripts/check_prova_do_seed.py`
   → **rc=1**: *"a prova foi gravada sobre `c119718c...` e este checkout e
   `4c8d2d6c...`. Ela mede OUTRO commit"*. É o caso **TRANSPORTADA** que o
   lançador anunciou — divergência esperada, não anomalia —, e o efeito é o
   mesmo: a medição desta rodada não aconteceu.

5. **Serviços ausentes.** Sem Postgres e sem Redis, os testes de store, cache,
   restauração, projeções e trilha de auditoria pulariam mesmo com
   interpretador bom. Diagnóstico em `.aurora-worktrees/stack.log`, que está
   fora do worktree e portanto fora do meu alcance de leitura.

6. **O que eu consegui executar, e que sustenta o pouco que marquei VERIFICADO:**
   `check_insumo_de_metrica` **rc=0** · `check_insumo_de_metrica_probes`
   **rc=0** (6/6 negativos, mais o controle positivo) · `check_api_surface`
   **rc=0** (34 rotas declaradas, 32 conferidas por AST, 3 claims, nenhum papel
   de exercício no vocabulário do token) · `check_core_contract_imports`
   **rc=0** · `tools/check_contract_literals` **rc=0** ·
   `tools/check_event_envelope` **rc=0** (invariante 4) · `tools/codegen --check`
   **rc=0** · `tools/check_security_constraints` **rc=0** ·
   `tools/check_synthetic_data` **rc=0** · `check_gabarito_fora_do_git`
   **rc=0** · `check_secoes_de_seguranca` **rc=0** (8/8 seções de `05`) ·
   `check_allowlist_do_auditor` **rc=0**.
   Por inspeção direta: `range-core/` não importa nada de `domains/` (invariante
   1) — os únicos imports cruzados são `contracts.generated.*`, que é a
   whitelist da P2-15. Nenhum arquivo de `docs/spec/` no diff, conforme
   `CLAUDE.md` §"A especificação é imutável". Âncora da fase gravada e igual à
   base (`phase_anchors.tsv:48`).
```

**O ponto em uma frase:** não encontrei desvio de spec no código da Fase 6 — a fase parece bem construída, com probes negativos de qualidade incomum — mas **zero dos 226 testes dela rodaram**, e um auditor que certifica por leitura é o defeito que este processo já pagou quatro vezes para não repetir. O FAIL é sobre a auditoria não ter medido, e está atribuído ao aparato, não ao commit.
