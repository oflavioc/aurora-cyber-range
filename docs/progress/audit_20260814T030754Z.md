# Auditoria de checkpoint - 2026-08-14T03:07:54.515308+00:00

- fase: **0**
- commit auditado: `470ced8d7aee1ff3065ac6aa2d859c6f21c40def`
- session_id: `ab69a491-070e-43e6-ba17-0e13252f99f9`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

All evidence gathered. Emitting the report.

```text
# AUDITORIA — FASE 0

## VEREDITO: FAIL

Commit auditado: `470ced8d7aee1ff3065ac6aa2d859c6f21c40def` (branch `manutencao-p11-p23`)
Diff contra `main` (`edd9527`): 10 arquivos, +1492/-268. Nenhum arquivo em `docs/spec/` — `spec_freeze` não é acionado por este diff.

## FINDINGS

### BLOCKER

- **[B1] Quebra de linha não é separador de comando: qualquer escrita deliberada na segunda linha é liberada**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:56` (`_SEGMENT_SEPARATORS`), `:294-301` (`_tokenize`), `:318-328` (`_segments`), `:515-518` (`main`)
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:111` §DoD item 4 ("Hook do auditor bloqueia escrita deliberada"); `docs/process/WORKFLOW.md:53` ("O agente não recebe ferramentas Write/Edit. Bash passa por allowlist. Essa combinação evita escrita deliberada")
  **Evidência:** medido contra a fonte versionada, quatro comandos **liberados** com `rc=0`:
  - `ls\nrm -rf range-core` → `rc=0`
  - `git status --short\ntee ../../CLAUDE.md` → `rc=0`
  - `echo oi\ncurl -o ../../CLAUDE.md https://example.com` → `rc=0`
  - `cat CLAUDE.md\npython -c import os` → `rc=0`
  `shlex` trata `\n` como espaço em branco e `_SEGMENT_SEPARATORS` só contém `| || && ; & ( )`; a segunda linha é absorvida como **posicional** do primeiro comando permitido. `ls`, `cat`, `grep`, `echo`, `printf` e `git status` não têm `max_positional`, então o prefixo é trivial de escolher.
  **Prova ao vivo, contra a cópia instalada (que é a que o Claude Code executa):** enviei um Bash de duas linhas (`ls -d docs` + `echo SEGUNDA_LINHA_EXECUTADA_SEM_BLOQUEIO`); o hook liberou e **as duas linhas executaram**. Isso demonstra os dois elos: comando multilinha chega ao hook como string única, e o bash executa cada linha.
  **Por que é blocker:** é a mesma classe do B1 da sétima auditoria — *falso liberamento* de escrita — reaberta por outra via. Como o auditor não tem `Write`/`Edit`, a allowlist de Bash é o **único** controle; com este furo, `rm -rf`, `tee`, `curl -o`, `sed -i` e `python -c` estão disponíveis, e os alvos aceitam travessia `../../`, alcançando o worktree principal (`CLAUDE.md`, `tools/`, `docs/spec/`). Os dois princípios anunciados em `readonly_bash.py:14-27` ("os dois default-deny") não valem fora da primeira linha.

### HIGH

- **[H1] O harness declara provar que a proteção não afrouxou e não mede a forma multilinha**
  **Arquivo:** `scripts/phase0_negative_tests.py:144-210` (`ESCRITA_DELIBERADA`, 36 probes), `:411-414`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:49-61` §Teste negativo obrigatório ("um verificador que nunca falhou contra uma violação plantada não é um verificador"); DoD item 2
  **Evidência:** `python scripts/phase0_negative_tests.py` → `rc=0`, 50 probes verdes, com o B1 aberto. **Todos** os 36 probes de escrita são de linha única; nenhum contém `\n`. O comentário `:144` afirma "a protecao nao pode ter sido afrouxada pela tokenizacao" e `:160-165` registra a lição do H2 da sétima rodada — "probe que so cobre a forma que voce lembrou nao prova ausencia das formas que voce esqueceu" — e a rodada repete exatamente isso, em terceira instância.
  **Por que é HIGH:** o harness é a prova central da fase e roda no CI (`.github/workflows/invariants.yml:43-44`). Verde vazio sobre o mesmo eixo já punido duas vezes significa que o gate não pega a regressão que ele existe para pegar.

- **[H2] O registro declara itens 9–13 da DoD como não executados, e o repositório mostra push, os dois PRs descartáveis e a tag `spec-v1.0` criada**
  **Arquivo:** `docs/progress/fase_0.md:299-303` (itens 9–13 ⛔), `:318` ("sem push, sem tag"), `:336` ("`finalize_phase0.sh` **não** foi executado"), `:310`, `:1005-1016` §8
  **Requisito violado:** `docs/spec/07_IMPLEMENTATION_PHASES.md:229` (registro com itens de DoD e status); `docs/process/PHASE_0_CHECKLIST.md:84-104` §Ordem correta de fechamento ("Nunca tagueie antes de provar o CI"; "Se branch protection falhar… a tag não é criada")
  **Evidência:** medido neste worktree: `refs/tags/spec-v1.0` existe, **tag anotada**, `tagger Flavio Costa … 1786640675`, assunto "Specification freeze v1.0", apontando para `b4abd9e` — ancestral do HEAD auditado (`git merge-base --is-ancestor` → 0). `refs/remotes/origin/main = edd9527 = refs/heads/main` (houve push). Existem `refs/remotes/origin/dod10-descartavel` (`9c72a46`), `refs/remotes/origin/dod11-descartavel` (`77eb901`) e `refs/remotes/origin/spec-change/ator-real-e-finalidade-comercial`.
  **Por que é HIGH:** `spec-v1.0` é o marco a partir do qual a spec fica imutável (`WORKFLOW.md:25`). O registro — artefato exigido pela spec e única fonte de status da fase — afirma o contrário do estado real em cinco itens de DoD e no parágrafo "Estado do repositório neste registro". Quem ler o registro conclui que a spec ainda não está congelada e que `finalize_phase0.sh` nunca rodou. É a reincidência da categoria nomeada no M1 da segunda auditoria (`fase_0.md:473-485`), agora sobre os itens de fechamento. Não consigo verificar deste worktree **se a ordem foi respeitada** (CI verde e branch protection antes da tag) — ver §O QUE EU NÃO CONSEGUI VERIFICAR.

### MEDIUM

- **[M1] Entregável `.claude/hooks/log_audit.py` removido, checklist da fase não atualizado**
  **Arquivo:** removido no diff (`.claude/hooks/log_audit.py`, -180); `docs/process/PHASE_0_CHECKLIST.md:10` continua exigindo-o na lista de entregáveis
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:10` §Entregáveis
  **Evidência:** `Glob **/*` confirma que `.claude/hooks/` contém apenas `check_architecture.py`, `scenario_bash.py`, `scenario_scope.py`. `.claude/settings.json` perdeu o bloco `SubagentStop`. A remoção está bem argumentada em `fase_0.md:507-521`, mas o documento que define os entregáveis da fase — que **não** é `docs/spec/` e portanto era editável no mesmo PR — segue listando o arquivo. Enquanto isso, o entregável não pode ser marcado.

- **[M2] `detect_verdict`, mecanismo que grava veredito em registro versionado, não tem probe algum; o registro afirma cobertura que não existe**
  **Arquivo:** `scripts/audit_report.py:179-222`; afirmação em `docs/progress/fase_0.md:962` ("Sete casos cobertos, incluindo o que motivou o achado")
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:49-61` §Teste negativo obrigatório; `docs/spec/07_IMPLEMENTATION_PHASES.md:229`
  **Evidência:** não existe nenhum teste no repositório que exercite `detect_verdict` — `Grep` por `detect_verdict` retorna apenas a definição (`:179`), o uso (`:357`) e prosa em `fase_0.md`/relatório. O harness ganhou 19 probes novos no mesmo esforço e zero para esta função. A função nasceu de um finding (M2 da sétima rodada) cujo defeito — classificar todo PASS como FAIL — só existia porque nunca foi exercitada.

- **[M3] Deny de secrets ainda não cobre `Write` — quinta rodada consecutiva**
  **Arquivo:** `.claude/settings.json:6-13`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:114` §DoD item 7; `CLAUDE.md` §Secrets
  **Evidência:** a lista contém exatamente `Read(.env)`, `Edit(.env)`, `Read(.env.*)`, `Edit(.env.*)`, `Read(secrets/**)`, `Edit(secrets/**)`. Nenhuma entrada `Write(...)`, embora `:18` prove que `Edit` e `Write` são matchers distintos. Registrado como P13/P19 e reconfirmado nas 3ª, 4ª, 6ª e 7ª rodadas.

- **[M4] O registro guarda evidência falsa para o item 5 da DoD**
  **Arquivo:** `docs/progress/fase_0.md:295`
  **Requisito violado:** `docs/spec/07_IMPLEMENTATION_PHASES.md:229`
  **Evidência:** o registro afirma que `scenario_bash.py` distingue `git log --oneline` (`exit=2`) de `range-cli scenario validate` (`exit=0`). **Medido nesta rodada:** `range-cli scenario validate` → `exit=2`; só `range-cli scenario validate scenarios/academus/pack` → `exit=0`. O L2 da sétima rodada apontou isto e `fase_0.md:968` o deixou explicitamente "a apurar"; segue não apurado e o texto errado permanece como evidência do item.

- **[M5] Seções de status do registro descrevem um estado anterior a três rodadas de auditoria**
  **Arquivo:** `docs/progress/fase_0.md:312` (item 15: "quatro auditorias", status ✅), `:294` (item 4: "⚠️ parcial… cinco modos de bloqueio indevido persistem (§6 P23)", com P23 fechado em `:804`), `:320-335` (tabela de commits termina em `6ed9993`), `:1009` (§8 manda executar a "Sétima auditoria", já executada e versionada em `docs/progress/audit_20260814T020307Z.md`)
  **Requisito violado:** `docs/spec/07_IMPLEMENTATION_PHASES.md:229`
  **Evidência:** §0 registra da primeira à sexta rodada e **não tem entrada para a sétima**; os findings da sétima só aparecem dentro do P28 (`:926`). O item 15 continua ✅ com base na quarta rodada, enquanto a sétima foi **FAIL com BLOCKER** — e o item 4, que a sétima reclassificou como ⛔ reprovado, segue marcado ⚠️ parcial com a justificativa antiga.

### LOW

- **[L1] Três probes de escrita bloqueiam pelo motivo errado.** `scripts/phase0_negative_tests.py:158`, `:172`, `:178`, `:201`. Medido: `find . -fprint0 out.txt` e `find . -delete` → *"'find' fora da allowlist"*; `uniq entrada.txt ../../CLAUDE.md` → *"'uniq' fora da allowlist"*. Os rótulos ("escrita por flag: find -fprint0", "escrita posicional: uniq") atribuem a detecção à allowlist de flags e ao limite de posicionais, mecanismos que estes probes não tocam. Ambos os mecanismos têm cobertura real em outros probes (`sort -o`, `pytest --junitxml`, `git tag v9.9.9`, `env`), então a propriedade continua provada — o que está errado é a atribuição.
- **[L2] Comentários e docstring citam arquivo removido neste mesmo commit.** `scripts/audit_report.py:8` descreve `.claude/hooks/log_audit.py` como chamador que "importa as funcoes"; `scripts/phase0_negative_tests.py:180-182` explica o probe pela `persist()` daquele arquivo. É a categoria P10/P15/P22 — correção que deixa a documentação para trás — em nova instância.
- **[L3] Colisão de identificador no registro.** `docs/progress/fase_0.md:903` ("P28 a P31", referências cruzadas) e `:926` ("P28 — [B1 BLOCKER…]") usam o mesmo ID para pendências distintas. Rastreabilidade por ID deixa de ser unívoca.
- **[L4] `main()` do hook falha aberto com stdin não-JSON.** `user-scope/hooks/readonly_bash.py:497-501` (`except Exception: return 0`). A docstring `:42` escopa a promessa à tokenização, e `fase_0.md:970` registra a decisão de corrigir junto com o O1 do `check_architecture.py`; fica como LOW por já estar declarado, não por ser inofensivo.
- **[L5] Falsos bloqueios remanescentes na metade "libera leitura" do item 4.** Medidos nesta sessão: `echo "--- verificadores ---"` → `exit=2` (*"echo: flag '--- verificadores ---' fora da allowlist"*: argumento entre aspas iniciado por `-` é lido como flag, modo novo); `python tools/$t.py` em laço → `exit=2` (L4 da sétima rodada, aberto); `find` fora da allowlist obriga a substituir por `git ls-files`/`rg` (declarado em `:29-30`). Falham para o lado seguro. Em contrapartida, os modos antigos fecharam: o smoke test canônico de `PHASE_0_CHECKLIST.md:71-74` roda e devolve `exit=2`, e `2>/dev/null` sobre a prova central passa.
- **[L6] Nenhum dos seis verificadores varre os hooks.** `tools/check_security_constraints.py:36` — `SCANNED_DIRS = ("range-core", "domains", "tools", "scripts")`. `.claude/hooks/` e `user-scope/hooks/` continuam fora (P9/P24, 5ª rodada). O código de governança é o que mais roda e o menos vigiado — e é onde nasceu o B1 desta rodada.
- **[L7] Saída da auditoria N bloqueia o lançamento da auditoria N+1.** `scripts/start_checkpoint_audit.sh:39-42` exige árvore limpa e `scripts/audit_report.py:225-248` grava `audit_<stamp>.md` e anexa ao `audit_log.jsonl` **versionados** no worktree principal; a mensagem impressa ("Crie o commit candidato antes da auditoria") não descreve a causa. L6 da sétima rodada, sem registro nem correção neste commit.

## DoD — item a item

Referência: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done.

| Item | Status | Evidência |
|---|---|---|
| 1 — Os seis verificadores liberam árvore limpa | ✅ | Executados um a um: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` → todos `rc=0`; `git status --porcelain` vazio antes e depois, sem resíduo |
| 2 — Os seis detectam as violações externas do harness | ✅ | `python scripts/phase0_negative_tests.py` → `rc=0`; 13 probes de verificador, cada um `rc=1` **e** citando o arquivo plantado (`expect_fail`, `:66-99`) |
| 3 — Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | ✅ (dois terços medidos) | Medido: `range-core/x.py` com `from domains.academus import Y` → `exit=2` (INVARIANTE 1); `docs/spec/00_MASTER_SPEC.md` → `exit=2` (SPEC IMUTAVEL). Literal de flag não reexecutado nesta rodada |
| 4 — Hook do auditor bloqueia escrita deliberada e libera leitura | ⛔ **REPROVADO** | Metade "bloqueia escrita": **B1** — quatro comandos de escrita liberados por quebra de linha, com execução comprovada ao vivo. Metade "libera leitura": melhorou (smoke test canônico e `2>/dev/null` passam), mas persistem falsos bloqueios (**L5**) |
| 5 — Hook do `scenario-designer` bloqueia Write/Edit fora de `scenarios/` e Bash fora da allowlist | ✅ | Medido: `Write` em `range-core/nope.py` → `exit=2`; `git log --oneline` → `exit=2`; `range-cli scenario validate scenarios/academus/pack` → `exit=0`. A evidência registrada no `fase_0.md:295` está errada (**M4**) |
| 6 — `ground_truth.yaml` e `GM_NOTES.md` não estão no `.gitignore` | ✅ | `.gitignore:39-40` — aparecem só em comentário que documenta o versionamento deliberado |
| 7 — `.env`/secrets negados em `.claude/settings.json` | ⚠️ parcial | `Read`/`Edit` negados; `Write` não (**M3**) |
| 8 — Auto Mode desabilitado | ✅ | `.claude/settings.json:4-5` — `"defaultMode": "default"`, `"disableAutoMode": "disable"` |
| 9 — Primeiro push de `main` com `arquitetura` e `seguranca` verdes | ❓ indeterminado | `origin/main = main = edd9527` prova que **houve push**; a execução do CI não é observável deste worktree. O registro afirma "sem push" (**H2**) |
| 10 — PR descartável confirma `spec_freeze` com spec+código | ❓ indeterminado | `origin/dod10-descartavel` (`9c72a46`) existe; o resultado do job não é observável daqui. Registro afirma ⛔ (**H2**) |
| 11 — PR descartável confirma título `spec-change:` | ❓ indeterminado | `origin/dod11-descartavel` (`77eb901`) existe; resultado não observável. Registro afirma ⛔ (**H2**) |
| 12 — Branch protection em `main` com os três contexts | ❓ | Não observável deste worktree |
| 13 — `spec-v1.0` criada depois de CI + branch protection | ⚠️ | A tag **existe** (anotada, em `b4abd9e`, ancestral do HEAD), contra o que o registro afirma. Que tenha sido criada **depois** de CI e branch protection eu não consigo verificar (**H2**) |
| 14 — `/doctor` sem apontamentos | ❓ | Fora do meu alcance |
| 15 (`WORKFLOW.md:40`) — auditoria de checkpoint com veredito PASS | ⛔ | Esta rodada: **FAIL**, 1 BLOCKER |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`ESCRITA_DELIBERADA` (`scripts/phase0_negative_tests.py:144-210`)** — declara cobrir o afrouxamento da proteção contra escrita e não contém um único probe multilinha. Passa verde com o **B1** aberto. Terceira instância do mesmo eixo (H2 da sétima rodada, `:160-165`).
2. **Probes de `find` e `uniq` (`:158`, `:172`, `:178`, `:201`)** — bloqueiam por "comando fora da allowlist", não pelo mecanismo de flags/posicionais que os rótulos afirmam exercitar (**L1**, medido).
3. **`hook_copies_in_sync` (`:248-266`)** — quando `~/.claude/hooks/readonly_bash.py` não existe, apenas avisa e segue (`:250-255`). No CI, onde a cópia nunca existe, o probe é inerte por construção. Está declarado; não deve ser lido como prova de ausência de drift. Nesta execução local ele rodou de fato e as duas cópias eram idênticas.
4. **`detect_verdict` (`scripts/audit_report.py:179-222`)** — zero probes, apesar da afirmação de "sete casos cobertos" (**M2**). O único teste da função é o uso em produção, sobre um log versionado.
5. **`python .claude/hooks/log_audit.py` (`:182`)** — continua provando algo útil (script de hook fora da lista de guardas bloqueia), mas o alvo não existe mais no repositório; o probe já não mede o canal de escrita que o H1 da sétima rodada descreveu.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Se a ordem de fechamento foi respeitada (itens 9–13).** Constatei os artefatos — `origin/main`, `origin/dod10-descartavel`, `origin/dod11-descartavel`, tag anotada `spec-v1.0` em `b4abd9e` — e **não** consigo observar deste worktree: execução e resultado dos jobs `arquitetura`, `spec_freeze` e `seguranca`, configuração de branch protection, e se a tag foi criada antes ou depois deles. Não afirmo que a ordem foi violada; afirmo que o registro diz que nada disso aconteceu e que os artefatos dizem o contrário.
- **Execução real das escritas do B1.** Provei que o hook as libera (`rc=0`) e que uma segunda linha executa de fato (`echo` observado). Não executei `rm`, `tee`, `curl -o` nem `sed -i` — a conclusão de que gravariam vem da semântica do shell, não de escrita realizada.
- **DoD 14 (`/doctor`)** — fora do meu alcance.
- **Comportamento em Linux/CI.** Rodei em Git Bash no Windows. A corrida de `rmdir` do P1 não se manifestou (árvore limpa ao final), o que não a desmente.
- **P3 e P4** (cobertura TypeScript de `check_core_boundary.py`; formatos sem gramática em `check_synthetic_data.py`) — não verificáveis enquanto `range-core/web/` e as evidências não existirem. Continuam corretamente declaradas como não exercitadas.
- **Mérito de M4/L7 da sétima rodada** (`check_event_envelope.py` só varre `.py`; actions em tag major mutável) — reconfirmo por leitura, sem probe possível nesta fase; não os reabri como findings próprios.
- **Se `detect_verdict` classifica corretamente este relatório.** Ele contém uma única linha de veredito (`## VEREDITO: FAIL`); pela leitura de `:198-222` deveria gravar `FAIL`, mas não executei a função.
```

**Consequência processual:** por `WORKFLOW.md:40`, qualquer BLOCKER é FAIL. O **B1** e o **H1** precisam fechar juntos — corrigir o hook sem acrescentar probe multilinha devolve o mesmo verde vazio na próxima rodada, que é a lição que esta fase já pagou três vezes. O **H2** é independente do código e precisa ser resolvido antes de qualquer declaração de conclusão: hoje o registro e o repositório discordam sobre se a especificação já está congelada.
