# Auditoria de checkpoint - 2026-08-18T06:05:19.845971+00:00

- fase: **5**
- commit auditado: `7e4c897e3436fa1ee748da092665213822b5550e`
- session_id: `548600db-087a-4210-9147-26fbf49d97b4`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have what I need. Here is the report.

```text
# AUDITORIA — FASE 5

## VEREDITO: PASS

Contexto: auditoria é **PORTA** (guarda P3-7, verbatim do lançador).
Base fd34c444 → candidato 7e4c897e, 42 arquivos, +8.162/−112.
Suíte executada neste worktree, com serviços ativos:
**387 testes, `OK`, 0 falhas, 0 `skip`** (`python -m unittest discover -s tests`, 63,9 s).
Seis verificadores de `tools/` — todos rc=0. Provas de container P4-10 — SHA confere.
Nenhum arquivo sob `docs/spec/` no diff (CLAUDE.md, "a spec é imutável durante a implementação").

## FINDINGS

### BLOCKER
nenhum encontrado.

### HIGH
nenhum encontrado.

### MEDIUM

- [M1] Três verificadores novos da fase não entraram na allowlist do auditor — reincidência nomeada
  Arquivo: `user-scope/hooks/readonly_bash.py:97-98` (regra), `:128-217` (lista); ausentes: `scripts/check_gabarito_fora_do_git.py`, `scripts/check_trilha_de_auditoria.py`, `scripts/check_secoes_de_seguranca.py` e os três `_probes`
  Requisito violado: regra escrita no próprio arquivo — *"Script novo que precise ser executado pelo auditor entra aqui por nome, no commit que o cria."* Sustenta `06` T15 e `05` §6/§7, que é o que os três executam.
  Evidência: `python scripts/check_trilha_de_auditoria.py` → `BLOQUEADO: comando fora da allowlist do auditor`. Idem para os outros dois. Os seis são stdlib puro, sem banco e sem rede (`grep` de imports: apenas `re`, `sys`, `subprocess`, `pathlib`, `dataclasses`) — ou seja, exatamente a classe que a regra manda admitir, e não a classe do `bench_reconstruction`, cuja exclusão é decisão registrada por exigir Postgres e minutos. O próprio arquivo documenta esta falha duas vezes: `:156-160` ("ficou fora da allowlist no commit que a criou, contra a regra escrita acima... M5 da quarta auditoria da Fase 3") e `:85-91` ("H3 da segunda auditoria da Fase 1").
  Por que não é blocker: os seis rodam no CI, que é o gate real — `.github/workflows/invariants.yml:195,198,211,214,227,230`. O que se perde é a verificação independente pelo auditor: avaliei os três por leitura, que é o modo de auditar que a regra existe para não aceitar.

- [M2] O teste dos volumes da Linha B afere contra as constantes do próprio gerador, não contra `02` §6.1
  Arquivo: `tests/test_seed_determinista.py:153-166` (`test_os_volumes_sao_os_de_02_secao_6_1`), constantes em `domains/academus/seed/dataset.py:71-75`
  Requisito violado: `06` T8 — *"Os seis conjuntos da Linha B aparecem nos volumes de `02_DOMAIN_ACADEMUS.md` §6.1"*; `07` Fase 5, item 5 da DoD
  Evidência: o `esperado` do teste é montado de `dataset.INDEVIDOS`, `dataset.AMBIGUOS`, `dataset.SUSPEITOS`, `dataset.RUIDO`, `dataset.DELEGADAS` — as constantes que o gerador usa para produzir as linhas. O teste prova coerência entre consulta e gerador, não conformidade com a spec. `grep` por qualquer verificador que leia os volumes de `02` §6.1: nenhum (`scripts/check_trilha_de_auditoria.py` lê `02`, mas a §4). Editar `dataset.INDEVIDOS = 5` deixa a suíte inteira verde e torna T8 falso em silêncio.
  Por que não é blocker: **conferi os valores à mão contra a tabela de `02` §6.1 e eles estão certos** — 22 · 11 · 34 · 60 · 18. O requisito está satisfeito hoje; o que falta é a guarda contra deriva futura. O projeto já tem a forma dessa guarda em `scripts/check_spec_flags.py`, que cruza spec com adapter.

### LOW

- [L1] A partição dos seis conjuntos só é exercida em `ESCALA_REDUZIDA`; o sexto volume de `02` §6.1 é *"milhares"* e vale 40 ali
  Arquivo: `tests/test_seed_determinista.py:160`, `domains/academus/seed/dataset.py:119,130`
  Evidência: os cinco conjuntos plantados não escalam e são os números exatos da spec em ambas as escalas — é o desenho, e está declarado no cabeçalho do arquivo. Só `legitimos_normais` é reduzido de 3.000 para 40. `ESCALA_COMPLETA.normais_na_trilha = 3_000` satisfaz "milhares" por leitura da constante, mas nenhum teste executa a partição em escala completa.

- [L2] `ground_truth` gerado classifica três dos seis conjuntos; manutenção e delegação ficam sem `defensibility`
  Arquivo: `tests/test_gabarito.py:125-130`; enum em `contracts/ground_truth.schema.yaml:339-342`
  Evidência: `02` §6.2 diz *"0.0 para legítimo (inclusive os de aparência suspeita, manutenção e delegação)"*, e o contrato só admite `indevido_comprovado | ambiguo | legitimo_aparencia_suspeita`. **A restrição é herdada da Fase 1 — `contracts/` não está no diff desta fase** — e está registrada como pendência **P5-4**, com destinatário Fase 7 e gatilho declarado (`docs/progress/fase_5.md:1441`). Nada em `06` T8 nem na DoD da Fase 5 a cobra aqui.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Seed completo em < 5 min | **NÃO VERIFICADO** | `scripts/prova_seed_completo.py` exige `AURORA_SEED_DATABASE_URL`, e o hook só admite três prefixos inline (`readonly_bash.py:10`); o script também não está na allowlist. O número — 144,3 s de 300 s, 3.543.783 linhas, Windows-11 · py 3.12.10 · 18/08/2026 — está em `docs/progress/fase_5.md:1389` com máquina, data e stack, na forma que `06` T3 exige. Li o script: mede `gerar + COPY`, orçamento 300 s, sai 1 se estourar. Não o vi rodar. |
| 2. Mesmo `RANDOM_SEED` → dataset byte-idêntico | **PASS** | `Determinismo` (3 testes, verdes aqui) — mesmo seed → mesmo dump em 20 tabelas; **e a direção inversa**, seeds diferentes → dumps diferentes em `students`, `users`, `grades`, `audit_trail`, que é o que impede a prova por vacuidade. Mais `test_o_gerador_nao_le_relogio`, **por AST e não por texto**. Escala completa no mesmo script não verificado (ver item 1). |
| 3. `UPDATE`/`DELETE` falham por trigger **e** por permissão de role | **PASS** | `TrilhaEhAppendOnly` — 4 testes verdes. Os dois mecanismos separados: trigger pela mensagem `append-only` (`0004:240`), role por `has_table_privilege` no catálogo do Postgres, que independe do trigger. Mais o par positivo (`INSERT`/`SELECT` presentes) que impede os três `assertFalse` de passarem com role vazia. |
| 4. `GET /audit/verify-chain` detecta adulteração induzida | **PASS** | `CadeiaDetectaAdulteracao` — campo alterado (posição 2), `payload` alterado (posição 1), linha removida do meio (posição 3, motivo "buraco ou reordenacao"), cada um com a **posição exata** que `06` T7 exige. Adulteração real, com `DISABLE TRIGGER` pelo dono. `RotaDeVerificacao` cobre a rota pelo stack ASGI: 200/403/401 e forma estável. |
| 5. Os seis conjuntos da Linha B nos volumes especificados | **PASS** | `SeisConjuntosDaLinhaB` — contagem, **disjunção par a par** e **união = trilha inteira**. Mais `test_as_caracteristicas_de_02_secao_6_1_estao_no_dado`, que afere as seis assinaturas (IP de lab, 22h–02h, sempre elevando, sem autorização, fora da janela, conta única, grupo de alunos) e não só as contagens. Volumes conferidos à mão contra `02` §6.1 — ver [M2]. |
| 6. `GM_NOTES.md` com a query que separa indevidos de ambíguos | **PASS** | `tests/test_gabarito.py` — o teste **produz** o artefato e o julga, e não confere um arquivo pré-existente. Duas metades: o texto da query está no `GM_NOTES`, e a query **executada** devolve 22 e interseção vazia com os ambíguos. Mais o linter de T8 (todo fato citado existe no `ground_truth`), a validação contra `contracts/ground_truth.schema.yaml`, e o par de dois seeds. |

## TESTES QUE NÃO PROVAM O REQUISITO

Um, e ele é o [M2]: `test_os_volumes_sao_os_de_02_secao_6_1` tem o nome da seção da spec e afere contra as constantes do gerador.

Nada mais. Especificamente, procurei e **não** encontrei:

- **`skip`/`xfail` sem justificativa** — zero `skip` na execução; `grep` por `skipIf|skipUnless|expectedFailure` nos quatro arquivos novos: nenhum. O `@exige_banco` estava armado e o banco estava lá.
- **Asserção sobre mock** — `grep` por `unittest.mock|MagicMock|Mock(`: nenhum. Tudo contra Postgres real e stack ASGI real.
- **Teste de imutabilidade só de trigger** — o contrário: `TrilhaEhAppendOnly` separa deliberadamente trigger de `REVOKE`, com o argumento escrito de por que um teste só não serviria.
- **Isolamento de papel testado na UI** — `06` T6 é payload; a rota nova tem seu conjunto de chaves afirmado (`{linhas, integra, quebra}`), e `test_coluna_nova_nao_vaza_para_a_resposta` e `test_nenhuma_entidade_nova_e_serializavel_pela_API` guardam a superfície que a peça 2 ampliou.
- **Consistência de evidência dirigida por seed** — o par de dois seeds em `test_gabarito.py` é justamente a direção inversa, e o comentário registra que ela **achou** três vazamentos, um deles o gabarito legível na trilha por contagem de linhas.
- **Violação arquitetural** — `tools/check_core_boundary.py`, `check_event_envelope.py`, `check_contract_literals.py`, `check_security_constraints.py`, `check_synthetic_data.py`, `codegen.py --check`: todos rc=0. A primitiva de hash foi **extraída** para o core sem semântica de domínio junto; `domains/` importa `range-core/`, nunca o inverso.
- **Confusão entre camadas de verdade** — a trilha é business state e não carrega `truth_layer`, `simulation_epoch` nem envelope de `09` §1.1, com o argumento em `trilha.py:19-27` apoiado em `01` §4.3.
- **Violação de segurança** — IPs só de RFC 5737 (`198.51.100.0/24`, `203.0.113.0/24`); nenhuma coluna de CPF, e-mail, telefone ou endereço no modelo; banner de `05` §4 na primeira linha do template de `GM_NOTES`; `git ls-files` não retorna `ground_truth.yaml`, `GM_NOTES.md` nem nada sob `scenarios/`, e `scenarios/` está no `.gitignore:89`.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **O `< 5 min` do seed completo (item 1 da DoD).** Não pude executar `scripts/prova_seed_completo.py`: ele exige `AURORA_SEED_DATABASE_URL`, e a allowlist do hook só admite `PYTHONDONTWRITEBYTECODE`, `PYTHONHASHSEED` e `NODE_ENV` como env inline; o script também não está na lista de `scripts/`. Aceito a exclusão como consistente com a decisão registrada que já manteve `bench_reconstruction` fora pelo mesmo motivo (exige Postgres, escreve muito, demora minutos), e o registro traz máquina, data e stack como `06` T3 exige — **mas o número é declaração, não execução que eu tenha visto.** Idem para a metade de escala completa do item 2.
2. **Os três verificadores novos** — [M1]. Avaliei `check_trilha_de_auditoria.py`, `check_gabarito_fora_do_git.py` e `check_secoes_de_seguranca.py` **por leitura do fonte e por leitura do workflow**, não por execução. Não sei, por medição própria, se eles reprovam contra violação plantada; o que sei é que os probes existem, são invocados no CI, e o texto declara os eixos.
3. **As provas de container (P4-10).** `check_provas_de_container.py` confere o SHA `7e4c897e3436` e imprime as duas provas verdes. Não as vi rodar — o que as amarra a este commit é o SHA que o arquivo carrega, como o próprio mecanismo declara.
4. **O `GM_NOTES.md`/`ground_truth.yaml` de produção**, gerados com o `RANDOM_SEED` real. Por desenho não existem no repositório, e nenhum CI os vê. A propriedade provada é a do **gerador**, e ela independe do valor do seed — mas o artefato que vai para a sala nunca foi inspecionado por mim nem por gate nenhum.
5. **A migration em base virgem.** Rodei a suíte contra a base que o lançador migrou. Não executei `alembic upgrade head` do zero, então o caminho `CREATE ROLE` sem `CREATEROLE` e a mensagem de recusa da 0004 não foram exercidos por mim.
6. **`_posicao` (`trilha.py:316-325`)** extrai a posição da quebra procurando o primeiro token numérico na mensagem do core. Está coberto por três casos verdes, e a fragilidade está declarada no docstring — não a exercitei contra mensagens futuras do core.
```

**PASS.** Nenhum BLOCKER, nenhum HIGH. Os dois MEDIUM são reais e verificáveis, e nenhum dos dois torna um item da DoD falso hoje: o [M1] é uma omissão de mecanismo que me custou verificação independente de três verificadores (o CI os roda), e o [M2] é uma guarda ausente contra deriva futura de números que conferi à mão e estão corretos.

O que mais pesou a favor foi a densidade de **provas na direção inversa** — seeds diferentes têm de divergir, a role tem de *possuir* `INSERT`, o limite do `REVOKE` e o truncamento da cauda afirmados como testes que ficarão vermelhos no dia em que deixarem de valer. É o oposto do padrão que eu fui criado para procurar.
