# Auditoria de checkpoint - 2026-08-14T02:03:07.929048+00:00

- fase: **0**
- commit auditado: `42a4779`
- session_id: `19d428eb-e16a-4b93-b387-fa1a95faf8a5`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

Audit complete. Tree left clean, no residue.

---

# AUDITORIA — FASE 0

## VEREDITO: **FAIL**

Commit auditado: `42a4779092edf6fc0744b3905377cf588e2c0d99` (delta vs `main`: `60a7fa7` P23, `1b64166` registro, `42a4779` P11).

---

## FINDINGS

### BLOCKER

- **[B1] A reescrita do `readonly_bash.py` abriu três caminhos de escrita deliberada que a versão anterior bloqueava**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:51-52` (`_REDIRECT_DUP`), `:280-281`; `:77-81` (`_PLAIN_READ` com `sort`), `:261-262`; `:83` (`_FIND_EXECUTING`), `:248-250`

  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:111` §DoD item 4 ("Hook do auditor bloqueia escrita deliberada"); `docs/process/WORKFLOW.md:53` ("O agente não recebe ferramentas `Write`/`Edit`. Bash passa por allowlist. Essa combinação evita escrita deliberada")

  **Evidência — medida nesta sessão, executando apenas o hook, nunca o comando:**

  | Comando submetido ao hook | exit | veredito |
  |---|---|---|
  | `echo x > out.txt` | 2 | bloqueado (controle) |
  | `echo x &> out.txt` | 2 | bloqueado (controle) |
  | **`echo x >& out.txt`** | **0** | **liberado** |
  | `find . -fprint out.txt` | 2 | bloqueado (controle) |
  | **`find . -fprint0 out.txt`** | **0** | **liberado** |
  | **`sort -o out.txt input.txt`** | **0** | **liberado** |
  | **`sort -o ../../tools/check_core_boundary.py evil.txt`** | **0** | **liberado** |
  | **`echo pwned >& ../../CLAUDE.md`** | **0** | **liberado** |

  `>&word` com palavra não-numérica é, no bash, a forma equivalente a `&>word` — redireciona stdout+stderr **para o arquivo**. Confirmado no shell desta sessão (`BASH_VERSION=5.3.15`): `echo x >& /dev/null` executou com `rc=0`, ou seja, o bash aceitou palavra não-numérica após `>&`, o que só a forma de redirecionamento-para-arquivo permite. O hook classifica `>&` em `_REDIRECT_DUP` (`:52`) e pula os dois tokens **sem validar o alvo** (`:280-281`), enquanto bloqueia `>` e `&>` — os três com a mesma semântica de escrita. `sort -o FILE` e `find -fprint0 FILE` gravam arquivo e passam porque `sort` entra em `_PLAIN_READ` sem validação de argumento e `-fprint0` ficou fora de `_FIND_EXECUTING` (que lista `-fprint`, `-fprintf`, `-fls`).

  **Por que é blocker:** o agente `checkpoint-auditor` declara `tools: Read, Grep, Glob, Bash` (`user-scope/agents/checkpoint-auditor.md:4`) — **não recebe `Write` nem `Edit`**. A allowlist de Bash é, portanto, a *única* barreira entre o auditor e a árvore. Com ela furada, o auditor pode criar e sobrescrever arquivo arbitrário; e como os alvos aceitam travessia relativa (`../../`), a escrita **não morre com o worktree descartável** — alcança o worktree principal, incluindo `CLAUDE.md` e os próprios verificadores de `tools/`. Isso derruba o argumento de integridade de `WORKFLOW.md:86` e o item 4 da DoD, que é checklist binária.

  Agravante: é **regressão introduzida pelo commit sob auditoria**. A versão anterior casava `>\s*\S`, que bloqueava `>&` (registrado em `fase_0.md` §6 P16 como falso bloqueio de `2>&1`), e `sort` estava fora da allowlist (P16). A docstring nova afirma o contrário do medido — `user-scope/hooks/readonly_bash.py:26-28`: *"Direcao inversa preservada: escrita deliberada continua bloqueada"* — e `docs/progress/fase_0.md:792` repete: *"A direção inversa não afrouxou, e três buracos fecharam."*

---

### HIGH

- **[H1] Comando da allowlist grava no worktree principal: `python .claude/hooks/log_audit.py`**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:86` (`_PY_HOOK`), `:187-188`; `scripts/audit_report.py:148-171` (`persist`), `:64-83` (`main_worktree_root`); `.claude/hooks/log_audit.py:97-98`

  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:111` §DoD item 4; `docs/process/WORKFLOW.md:53`

  **Evidência:** medido — `python .claude/hooks/log_audit.py` retorna `exit=0` no hook do auditor. Lendo o alvo: `persist()` executa `progress.mkdir(parents=True, exist_ok=True)` (`audit_report.py:151`) e abre `audit_log.jsonl` em modo append (`:169-170`) **incondicionalmente**, e o diretório é resolvido por `main_worktree_root()`, que sobe do worktree de auditoria para o worktree **principal** por `git rev-parse --git-common-dir`. Com `agent_type: "checkpoint-auditor"` no payload (campo controlado por quem escreve o JSON, `log_audit.py:70-71`), o ramo que grava `docs/progress/audit_<stamp>.md` também executa.

  **Por que é HIGH:** é um segundo canal de escrita, independente do B1, e aponta exatamente para o artefato que o P11 acabou de criar como prova versionada de cada rodada. O auditor pode anexar linha ao registro de auditoria e gerar um `audit_*.md` no repositório que ele deveria apenas ler. Os campos `source: "subagent_stop_hook"` e `phase: null` deixam rastro distinguível, o que limita o dano, mas não o impede.

- **[H2] Os 13 probes de escrita deliberada não tocam nenhuma das formas do B1 — o harness afirma o que não mede**

  **Arquivo:** `scripts/phase0_negative_tests.py:126-141` (`ESCRITA_DELIBERADA`), `:164-176` (`expect_hook_blocks`)

  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:59-61` ("Um verificador que nunca falhou contra uma violação plantada não é um verificador"); §DoD item 4

  **Evidência:** o comentário do bloco declara o propósito — `:126`: *"Escrita deliberada: a protecao nao pode ter sido afrouxada pela tokenizacao"*. A lista cobre redirecionamento **apenas** na forma `>` (`git log > out.txt`, `:130`), `tee`, `sed -i`, `find -delete`, `rm`, `env`, crase, `git branch -D`. Não há probe para `>&`, para `sort -o`, nem para qualquer flag de escrita de `find` além de `-delete`. O harness executa e sai `0` (medido: `HARNESS_RC=0`, 20/20 probes de hook verdes) enquanto três afrouxamentos reais estão presentes.

  **Por que é HIGH:** é estruturalmente o mesmo defeito do **B1 da primeira auditoria**, que esta fase já condenou por escrito (`fase_0.md:960`: *"um teste cujo probe vive dentro da região coberta mede a si mesmo"*). O harness passou a cobrir a direção "libera leitura", que era a lacuna conhecida, e a direção "nega escrita" continuou medindo só as formas que o código novo já tratava. Um teste verde foi usado como evidência de que a proteção não afrouxou, e ela afrouxou.

---

### MEDIUM

- **[M1] Modo novo da família P23: o prefixo de descritor (`2>`) não é consumido pelo redirecionamento e vira argumento**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:270-284` (`_check_segment`), `:188`, `:197-198`

  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:111` §DoD item 4 ("libera testes/verificadores de leitura")

  **Evidência:** medido. `python scripts/phase0_negative_tests.py 2>/dev/null` → **exit=2**, motivo *"harness negativo nao aceita argumento"*. `python user-scope/hooks/readonly_bash.py 2>/dev/null` → **exit=2**, motivo *"hook com argumento extra"*. Causa: em `2>/dev/null` o token `2` não é operador, cai no `else` de `:282-284` e entra em `words`; só depois `>` é tratado. O probe (a) do harness (`phase0_negative_tests.py:112`, `git rev-parse main 2>/dev/null`) passa por acidente — o validador de `git` só olha `args[0]` (`:216`) e ignora a palavra extra. O comando bloqueado é a **prova central da fase** com stderr suprimido.

  **Por que é MEDIUM e não LOW:** o P23 foi fechado com a tese de que a causa havia sido trocada e a família encerrada (`fase_0.md:788`); esta é a sexta instância, na implementação nova, e o probe que deveria cobri-la passa pelo motivo errado.

- **[M2] `detect_verdict` registra `FAIL` para qualquer relatório que cite as duas palavras**

  **Arquivo:** `scripts/audit_report.py:131-145`

  **Requisito violado:** `docs/spec/07_IMPLEMENTATION_PHASES.md:229` (registro de progresso fidedigno); finalidade declarada do P11 (`fase_0.md:491-503`)

  **Evidência:** função pura e determinística: `has_fail and has_pass → "FAIL"` (`:141-144`). Qualquer relatório que mencione ambos os tokens — o formato obrigatório do auditor contém a linha `## VEREDITO: PASS | FAIL` e a regra "Qualquer BLOCKER ⇒ FAIL" — é registrado como `FAIL` no `audit_log.jsonl` **versionado**, mesmo quando o veredito é PASS.

  **Por que importa:** o campo existe para acabar com a transcrição manual que produziu três confusões de ID nesta fase. Erra para o lado conservador, e o cabeçalho gravado avisa que a fonte autoritativa é o texto (`:163-164`) — por isso é MEDIUM e não HIGH —, mas o campo legível por máquina é sistematicamente enviesado.

- **[M3] Deny de secrets ainda não cobre `Write`** — `.claude/settings.json:6-13`. Requisito: `PHASE_0_CHECKLIST.md:114` §DoD item 7; `CLAUDE.md` §Secrets. Evidência: a lista contém exatamente `Read(.env)`, `Edit(.env)`, `Read(.env.*)`, `Edit(.env.*)`, `Read(secrets/**)`, `Edit(secrets/**)`; nenhuma entrada `Write(...)`, embora `:18` prove que `Edit` e `Write` são matchers distintos. Quarta rodada consecutiva (P13/P19).

- **[M4] `check_event_envelope.py` só varre `.py`, sem declarar a limitação** — `tools/check_event_envelope.py:43,92`. Requisito: `06_ACCEPTANCE_TESTS.md` T1; `01_ARCHITECTURE.md` §2. Os irmãos `check_contract_literals.py:160` e `check_core_boundary.py:191` varrem TS. Quarta rodada (P14/P20). Não reexaminei o mérito: reconfirmo por leitura, sem probe possível enquanto a camada web não existir.

---

### LOW

- **[L1]** `docs/progress/fase_0.md:788` registra o fechamento do P23 em `7a67305`; esse commit **não é ancestral do HEAD auditado** (verificado: `git log --format=%H | grep -c 7a67305…` → `0`; o commit real é `60a7fa7`, mesmo assunto). O SHA existe no repositório, então a branch foi reescrita depois do registro. Rastreabilidade do registro fica quebrada.
- **[L2]** `docs/progress/fase_0.md:295` afirma, como evidência do DoD item 5, que `scenario_bash.py` devolve `exit=0` para `range-cli scenario validate`. Medido: **exit=2**; o hook exige caminho sob `scenarios/` (`range-cli scenario validate scenarios/academus/pack` → `exit=0`). O item 5 continua satisfeito nas duas direções; a evidência registrada é que está errada.
- **[L3]** Resíduo da família P23 no conjunto de leitura de git: `git cat-file` bloqueado nesta sessão (`readonly_bash.py:68`, `_GIT_READ_SUBCOMMANDS` sem `cat-file`, `merge-base`, `for-each-ref`). Falha para o lado seguro.
- **[L4]** `python tools/$t.py` (caminho com variável, em laço) é bloqueado — `_PY_CHECKER` (`:87`) só casa literal. Fecha para o lado seguro e é defensável, mas obriga o auditor a desenrolar laços sobre os seis verificadores.
- **[L5]** `readonly_bash.py:315-318`: `main()` retorna **0** (libera) quando o stdin não parseia como JSON, enquanto a docstring `:30-31` anuncia "falha fechada". A afirmação está escopada à tokenização, mas a assimetria repete o O1 já registrado para `check_architecture.py` e não está declarada.
- **[L6]** O launcher exige árvore limpa (`scripts/start_checkpoint_audit.sh:39-42`) e a captura do P11 grava no worktree principal (`audit_report.py:148-171`): a saída da auditoria N bloqueia a auditoria N+1 com a mensagem *"Crie o commit candidato antes da auditoria"*, que não descreve a causa real.
- **[L7]** Pendências abertas reconfirmadas por leitura, sem reexame de mérito: P21 (`tools/README.md:3` e `README_FIRST.md:10` ainda dizem que os seis verificadores não existem), P22 (`README_FIRST.md:12`, `PHASE_0_CHECKLIST.md:104` descrevem o `finalize` anterior ao H2), P24 (`tools/check_security_constraints.py:36` não varre `.claude/hooks/` nem `user-scope/hooks/`), P25 (`invariants.yml:12,13,50,128,129` em tag major mutável), P17 (`.env.example` ausente).

---

## DoD — item a item

Referência: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done.

| # | Item | Status | Evidência |
|---|---|---|---|
| 1 | Os seis verificadores liberam árvore limpa | ✅ | Executados um a um: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` → todos `rc=0`. `git status --porcelain` vazio depois, sem resíduo |
| 2 | Os seis detectam as violações externas do harness | ✅ | `python scripts/phase0_negative_tests.py` → `HARNESS_RC=0`; 13 probes de verificador, cada um `rc=1` **e** citando o arquivo plantado |
| 3 | Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | ✅ | Medido: import → `exit=2` (INVARIANTE 1); `docs/spec/00_MASTER_SPEC.md` → `exit=2` (SPEC IMUTAVEL); literal de flag → `exit=2` (INVARIANTE 2); `objective_ids` → `exit=2` (INVARIANTE 4) |
| 4 | Hook do auditor bloqueia escrita deliberada e libera leitura de teste | ⛔ **REPROVADO** | Metade "bloqueia escrita": três formas passam (**B1**) + canal via `log_audit.py` (**H1**). Metade "libera leitura": os cinco modos antigos passam (smoke test canônico de `PHASE_0_CHECKLIST.md:71-73` executa e devolve `exit=2` para `rm -rf` real), mas surge modo novo com prefixo `2>` (**M1**) |
| 5 | Hook do `scenario-designer` bloqueia Write/Edit fora de `scenarios/` e Bash fora da allowlist | ✅ | Medido: `Write` em `range-core/nope.py` → `exit=2`; `git log --oneline` → `exit=2`; `range-cli scenario validate scenarios/...` → `exit=0`; `git status --short` → `exit=0`. Evidência registrada no `fase_0.md` está incorreta (**L2**) |
| 6 | `ground_truth.yaml` e `GM_NOTES.md` **não** estão no `.gitignore` | ✅ | `.gitignore:35` — aparecem só em comentário que documenta o versionamento deliberado |
| 7 | `.env`/secrets negados em `.claude/settings.json` | ⚠️ parcial | `Read`/`Edit` negados; `Write` não (**M3**) |
| 8 | Auto Mode desabilitado | ✅ | `.claude/settings.json:4-5` — `"defaultMode": "default"`, `"disableAutoMode": "disable"` |
| 9 | Primeiro push de `main` com `arquitetura` e `seguranca` verdes | ⛔ | Nenhum push; nenhuma execução de CI observável deste worktree |
| 10 | PR descartável confirma `spec_freeze` com spec+código | ⛔ | Bloqueado por 9 |
| 11 | PR descartável confirma título `spec-change:` | ⛔ | Bloqueado por 9 |
| 12 | Branch protection em `main` | ⛔ | Bloqueado por 9 |
| 13 | `spec-v1.0` criada depois de CI + branch protection | ⛔ | Nenhuma tag |
| 14 | `/doctor` sem apontamentos | ❓ | Não executável por mim |
| 15 (WORKFLOW) | Auditoria de checkpoint com veredito PASS | ⛔ | Esta rodada: **FAIL** |

O próprio registro (`fase_0.md:935`) declara a Fase 0 **aberta**, o que é coerente com 9–13. O item 4, porém, estava marcado ⚠️ parcial por *falso bloqueio*; esta rodada o reclassifica como **reprovado por falso liberamento**, que é a direção oposta e mais grave.

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`ESCRITA_DELIBERADA` (`scripts/phase0_negative_tests.py:126-141`)** — declara provar que "a proteção não pode ter sido afrouxada pela tokenização" e cobre redirecionamento só na forma `>`. Passa verde contra três afrouxamentos reais (`>&`, `sort -o`, `find -fprint0`). É o finding **H2** e a causa de o **B1** ter chegado até aqui.
2. **Probe (a) de leitura legítima (`:112`, `git rev-parse main 2>/dev/null`)** — passa não porque o hook trate o prefixo de descritor, mas porque o validador de `git` ignora a palavra extra `2` (`readonly_bash.py:216`). O mesmo padrão em `python scripts/phase0_negative_tests.py 2>/dev/null` é bloqueado. O probe mede o validador de `git`, não o parser de redirecionamento (**M1**).
3. **`hook_copies_in_sync` (`:179-197`)** — compara fonte versionada e cópia instalada, mas quando `~/.claude/hooks/readonly_bash.py` não existe apenas avisa e segue (`:181-185`). No CI, onde a cópia nunca existe, o probe é sempre inerte. Não é defeito por si — está declarado —, mas não deve ser lido como prova de ausência de drift.
4. **Nenhum probe exercita o hook do auditor contra caminho de destino**: todos os probes de escrita usam alvos no diretório corrente. A travessia `../../` que leva a escrita para fora do worktree descartável (medida em B1) não é coberta por nenhum teste.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **DoD 9 a 13** — push, execução real do CI, `spec_freeze` reprovando nos dois PRs descartáveis, branch protection e tag `spec-v1.0`. Nada disso é observável deste worktree; li o `invariants.yml` e o conjunto `CODE` (`:110-113`, inclui `user-scope/`, `CLAUDE.md`, `.gitignore`) apenas por leitura estática. **Não afirmo que o gate funciona**; afirmo que o texto está lá.
- **DoD 14 (`/doctor`)** — fora do meu alcance.
- **Execução real das escritas do B1** — provei que o hook as **libera** (exit=0) e que o bash 5.3 aceita a forma `>& palavra-não-numérica`. Não executei nenhuma delas para não gravar arquivo. A conclusão de que gravariam vem da semântica documentada de `>&`/`sort -o`/`find -fprint0`, não de execução.
- **Escrita efetiva por `log_audit.py` (H1)** — provei que o hook libera o comando e li o código de `persist()`, que grava incondicionalmente. Não executei.
- **Comportamento do harness em Linux/CI** — rodei em Git Bash no Windows. A corrida de `rmdir` descrita em P1 não se manifestou nesta execução (árvore limpa ao final), o que não a desmente.
- **P3 (cobertura TypeScript de `check_core_boundary.py`) e P4 (formatos sem gramática em `check_synthetic_data.py`)** — não verificáveis enquanto `range-core/web/` e as evidências não existirem. Continuam corretamente declaradas como não exercitadas.
- **Mérito de M4, L7** — reconfirmados por leitura de código e de documento, sem probe novo.
- **Se `7a67305` (L1) foi perdido por rebase ou por amend** — constatei apenas que existe no repositório e não é ancestral do HEAD.

---

**Consequência processual:** por `docs/process/WORKFLOW.md:40`, qualquer BLOCKER é FAIL. **B1** e **H1** precisam fechar antes de `finalize_phase0.sh`, e **H2** junto — corrigir o hook sem acrescentar probe para as formas do B1 devolve o mesmo verde vazio na próxima rodada. Recomendo que a correção do B1 não seja feita caso a caso: a lista de operadores e de flags de escrita é aberta, e essa é exatamente a lição que a linhagem P8→P16→P23 já custou quatro rodadas.
