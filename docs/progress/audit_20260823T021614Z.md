# Auditoria de checkpoint - 2026-08-23T02:16:14.802224+00:00

- fase: **6**
- commit auditado: `8b1a70353c626f18ee3bc0c8e2ff5c361c41ea96`
- session_id: `e1160e9a-1ef6-439c-acd0-c6011f147d7e`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

Auditoria concluída. Suíte real executada (716 testes, `OK`, zero skips), verificadores executados, provas de container e do seed conferidas contra o SHA deste checkout.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] O avaliador não reemite o veredito depois de rollback quando o veredito
      anterior SOBREVIVE ao corte — e TTCV/TTRV somem permanentemente
  Arquivo: range-core/engine/verificacao.py:298-310 (`_ja_satisfeito_na_corrente`)
           e range-core/engine/verificacao.py:346 (a chamada que suprime a emissão)
           × range-core/metrics/verificacao.py:230 (`e.simulation_epoch == corrente`)
  Requisito violado: `09_EVENT_MODEL.md` §3.1, bloco *"O avaliador de predicados e a
           epoch"* — *"A satisfação pertence à epoch em que foi emitida… Depois de um
           rollback, o avaliador reavalia sobre a linhagem corrente; se ela satisfaz,
           emite na epoch nova"* — e o segundo negativo da mesma seção. Também
           `06_ACCEPTANCE_TESTS.md` T10, primeiro critério, e o item 2 da DoD da Fase 6.
  Evidência: os dois módulos usam CRITÉRIOS DIFERENTES para a mesma pergunta.
           O engine suprime a emissão quando existe veredito **na linhagem corrente**
           (`_ja_satisfeito_na_corrente` não olha `simulation_epoch`). O computador de
           métrica seleciona o veredito por **epoch corrente**
           (`range-core/metrics/verificacao.py:230`). Os dois conjuntos divergem
           exatamente quando o corte NÃO alcança o veredito.

           `range-core/events/linhagem.py:138-139` marca como abandonado apenas
           `ancora < j < indice_do_rollback`. Logo, rollback ancorado **em** ou **depois**
           do `verification_predicate_satisfied` deixa o veredito vivo na linhagem.

           Sequência reachable pela superfície de produção
           (`range-core/api/app.py:340` aceita `to_event_id` arbitrário do corpo):
             e0 `exercise_started` (epoch 0)
             e1 `fire(A01)` → move a flag do predicado
             e2 `verification_predicate_satisfied{containment}` (epoch 0)
             e3 `fire(A02)`
             e4 `rollback(to_event_id=e2, reason=technical_failure)`
           No `_avalia` que `range-core/engine/inject_engine.py:498` dispara logo após:
             correntes = [e0, e1, e2, e4]  (só e3 é cortado)
             `avalia(containment)` = True   (a flag de e1 sobrevive)
             `_ja_satisfeito_na_corrente` = True (e2 está na linhagem) → NÃO emite
           No lado da métrica: `current_epoch` = 1 (range-core/events/epoch.py:48),
           `epochs_em_calculo` = {0,1} para `technical_failure`
           (range-core/metrics/epoch.py:158-190), o veredito de epoch 0 atravessa o
           `apenas()` e é então descartado por `simulation_epoch == corrente`.
           Resultado: **TTCV NÃO MARCADA**.

           E é irrecuperável: refazer `fire(A01)` não muda nada — a flag já é `True`,
           `avalia` continua `True` e `_ja_satisfeito_na_corrente` continua `True`.
           O veredito nunca mais é emitido no resto do exercício.

           A suíte não cobre o ramo, e as duas metades são testadas com premissas
           incompatíveis entre si:
           - tests/test_laco_de_verificacao.py:193-217 — os três casos de rollback
             ancoram SEMPRE no evento de abertura (`abertura = self.store.read_all()[0]`),
             que corta o veredito; o único teste que fecha o caminho até a métrica
             (`OCaminhoFechaAteAMetrica`, :220-246) não tem rollback nenhum;
           - tests/test_metrics_verificacao.py:227-231 — o comentário afirma o lado
             oposto: *"`technical_failure` também abandona a epoch, e um veredito
             emitido ANTES dele não sustenta a métrica da epoch nova"*, e para isso os
             testes põem o rollback ANTES do veredito, isto é, evitam o caso.
           A própria documentação do módulo de métrica declara a premissa que o engine
           não honra — range-core/metrics/verificacao.py:48-53: *"o avaliador reemite na
           epoch nova quando a linhagem corrente ainda satisfaz"*.
  Por que é blocker: um `rollback` com `reason: technical_failure` ancorado depois da
           contenção verificada — que é o uso NORMATIVO do motivo, `03` §3.5, *"a equipe
           não é penalizada por bug do ambiente"* — apaga a metade de verificação do par
           TTCD/TTCV para o resto do exercício. Nada falha, nada fica vermelho: a métrica
           continua saindo, NÃO MARCADA, e a janela de asseguração prematura
           (`range-core/aar/timeline.py:201-204`) deixa de existir junto. É literalmente o
           modo de falha que `03` §3.0 e `range-core/engine/loader/pack_loader.py:699`
           chamam de *"o modo mais caro de errar: a métrica não falha, ela deixa de
           marcar"*, e atinge o par que sustenta o OBJ-05 e o objetivo-âncora OBJ-10.

### HIGH

nenhum.

### MEDIUM

- [M1] O próprio `verification_predicate_satisfied` é listado como "evidência
      incompatível com a declaração"
  Arquivo: range-core/aar/timeline.py:222-244 (`_incompativeis`, filtro
           `inicio <= instante(evento) <= fim` sobre `truth_layer == ground_truth`)
  Requisito violado: `03_EXERCISE_DESIGN.md` §3.2 — redação-alvo do AAR:
           *"Contenção declarada em T+31. **Evento incompatível com contenção em T+38.**
           Contenção verificável apenas em T+52."*
  Evidência: `fim` da janela é o instante do veredito, e o extremo é inclusivo. O veredito
           tem `truth_layer: ground_truth` (range-core/engine/verificacao.py:60;
           `09` §4.1), então ele entra na lista. tests/test_aar_timeline.py:212-218
           (`test_o_proprio_veredito_fecha_a_janela_e_e_de_ground_truth`) **asserta** que
           ele está lá, com a justificativa *"o extremo entra, e é deliberado"*.
           A redação-alvo da §3.2 trata o veredito como cláusula separada — *"Contenção
           verificável apenas em T+52"* — e não como um dos itens incompatíveis.
  Por que não é blocker: é erro de rotulagem na saída, não perda de medição; o instante
           continua correto e a renderização é da Fase 10.

### LOW

- [L1] `rehearsal` ganha um segundo efeito de métrica que `09` §3.1 não declara
  Arquivo: range-core/metrics/epoch.py:204-277 (`congelamentos` →
           `_intervalos_de_ensaio`)
  Requisito violado: `09_EVENT_MODEL.md` §3.1, tabela de `reason` — a linha de
           `rehearsal` declara UM efeito de métrica: *"Nenhum evento da epoch entra em
           cálculo"*. Não há desconto de intervalo declarado para ela.
  Evidência: além da exclusão de eventos, o módulo desconta o intervalo de tempo da epoch
           ensaiada da união de congelamentos. O argumento está escrito
           (range-core/metrics/epoch.py:216-226) e a decisão está registrada como P6-4
           em docs/progress/fase_6.md:799. É derivação defensável e argumentada, não
           contradiz a letra — mas acrescenta efeito a um `reason` cuja tabela a spec
           declara fechada por efeito.

- [L2] O computador de `TTID` e o AAR aplicam o predicado de contrassinatura sobre
      eventos que a emissão já filtrou, mas nenhum teste cruza os dois caminhos com
      fluxo produzido pela rota real
  Arquivo: range-core/metrics/declaracao.py:168-186;
           range-core/aar/timeline.py:247-278; range-core/declarations/contrassinatura.py
  Requisito violado: nenhum — é limite de cobertura, não desvio.
  Evidência: os três consumidores compartilham `completa()` (o que fecha a classe D4),
           mas tests/test_participant_contrassinatura.py exercita a EMISSÃO e
           tests/test_metrics_declaracao.py:175-249 exercita o CONSUMO sobre fluxo
           sintético; não há teste que gere o par pela rota e o leve até `TTID`.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| Consultar a trilha com filtro de período emite `audit_query_performed` | PASS | domains/academus/api/app.py:202-248; domains/academus/observability_hooks.yaml:44-48; tests/test_api_emissao_pela_rota.py:153-262 (rota→store, envelope, 4 campos, `result_count` real); `check_hooks_com_emissor` rc=0 (10 hooks / 2 produtores / 10 emissões); `check_fabrica_liga_emissor` rc=0 (3 serviços) |
| Cada par declaração × verificação é disparável e testado | **FALHA** | disparável e testado sem rollback (tests/test_metrics_verificacao.py:123-165; test_laco_de_verificacao.py:220-246); **quebra com rollback — B1** |
| Métricas descontam o intervalo de `technical_failure` pela UNIÃO, nunca pela soma | PASS | range-core/metrics/epoch.py:280-297 (`uniao`) e :300-327 (`decorrido` com recorte à janela); tests/test_metrics_epoch.py; 2 violações plantadas reprovadas (fase_6.md:397) |
| Nenhum evento de epoch com `reason: rehearsal` entra em cálculo | PASS | range-core/metrics/epoch.py:128-201 (`epochs_descartadas`, `no_calculo`, `apenas`); tests/test_metrics_declaracao.py:402; test_metrics_verificacao.py:261 |
| Declaração não altera nenhum valor de ground truth | PASS | tests/test_simulation_state.py:276-313 (as seis declarações, comparação contra o estado padrão) + tests/test_simulation_state_probes.py com o ramo plantado |
| Declaração prematura produz lista de eventos incompatíveis no AAR | PASS c/ ressalva | range-core/aar/timeline.py:182-244; tests/test_aar_timeline.py:126-218 — ver M1 |
| Pack sem `verification_predicates` não carrega | PASS | contracts/ground_truth.schema.yaml:24 (`required`) e :546 (exemplo inválido `rejected_by: schema`); `check_contract_examples` rc=0, 88 negativos; range-core/engine/loader/pack_loader.py:455-508 para o pack completo sem `ground_truth.yaml` |
| `TTIV` computado por limiar de calibração, não por predicado de mundo | PASS | range-core/metrics/verificacao.py:183-214 (recalcula Brier por prefixo, `<=` limiar); tests/test_metrics_verificacao.py:316-431 (inclusive `test_o_limiar_vem_do_INSUMO_e_nao_de_constante`) |
| Brier só dentro do escopo revisado; lacuna de cobertura em separado | PASS | range-core/metrics/calibracao.py:134-261 (`brier`, `_lacunas`, três listas sem compensação); tests/test_metrics_calibracao.py |
| Rubrica ausente ou em versão divergente impede carga do pack | PASS | range-core/engine/loader/contract_rules.py:177-190 + pack_loader.py:719-731 (`load_library()` como `x-aurora-ref`); tests/test_pack_loader.py:435 e :443; 9 rubricas em range-core/rubrics/ = as 9 competências de `03` §2.3 |
| Divergência ≥ 2 pontos entre avaliadores gera alerta | PASS | range-core/aar/timeline.py:281-320 (amplitude entre extremos, última nota por avaliador); tests/test_aar_timeline.py:296-352 |
| As nove siglas classificadas pelo critério de `00` §3.2, batendo com `03` §3.0 | PASS | tests/test_derivacao_das_nove_siglas.py — lê a tabela da spec, aplica a conjunção às colunas publicadas e cruza com as siglas que os DOIS computadores de fato produzem (:228-299); 5 violações plantadas na tabela reprovadas |
| Todo `event_type` tem `metric_side`; os quatro lados são disjuntos | PASS | contracts/events.schema.yaml:692-747 (33 tipos: 7/9/5/12); range-core/engine/loader/contract_rules.py:123-153 (cobertura, valores, lado vazio); `check_contract_examples` cobre os negativos |
| Folha de predicado obedece à conjunção de `09` §4.0, com probe das três violações + controle positivo | PASS | contract_rules.py:169-173 (`state_effect` ∧ `verification` → exatamente os cinco de `09` §4.0); as três fixtures em contracts/ground_truth.schema.yaml:694 (autorreferência), :712 (`assessment_submitted`), :731 (`communication_submitted`), mais o controle positivo `vpn_access_revoked` nos exemplos válidos; tests/test_laco_de_verificacao.py:262-293 com o teste-controle contra o caso vazio |
| Cada computador recebe só o seu lado + epoch + escalares tipados; só o AAR tem as duas metades | PASS | range-core/metrics/insumo.py (3 `NewType`, ponto único `monta`); `check_insumo_de_metrica` rc=0; `_probes` 6/6 reprovadas + positivo do consumidor que só anota; tests/test_metrics_declaracao.py:347-365 e test_metrics_verificacao.py:166-192 (negativos cruzados) |
| Desconto por união, exclusão de `rehearsal` e seleção de start são cálculo do consumidor | PASS | insumo.py:119-165 não recorta; declaracao.py:118-134 e verificacao.py:159-162 chamam `congelamentos`/`epochs_em_calculo` no consumidor; seleção de start em declaracao.py:149-165 sobre o payload |
| `inject_fired` emite no payload os marcadores de start, com teste de emissão | PASS | range-core/engine/inject_engine.py:326-327; derivação das três pernas em pack_loader.py:776-798 (com `reveals` excluído); contracts/events.schema.yaml `inject_fired_payload` fechado; tests/test_inject_engine.py |

## TESTES QUE NÃO PROVAM O REQUISITO

- `tests/test_laco_de_verificacao.py::ReavaliaDepoisDoRollback` (:190-217) — os três
  casos ancoram o rollback no evento de ABERTURA, o que garante que o veredito seja
  cortado. Com essa escolha, `_ja_satisfeito_na_corrente` e `simulation_epoch ==
  corrente` concordam por construção, e o ramo em que divergem (B1) nunca é exercitado.
  `test_redisparo_na_epoch_nova_reemite` é apresentado como o controle do par, e o que
  ele controla é apenas o caso já coberto.
- `tests/test_metrics_verificacao.py::EpochAtravessaAteONumero` (:224-278) — o
  comentário :227-231 declara que o rollback foi movido para ANTES do veredito porque,
  na ordem inversa, *"as duas medidas vinham NÃO VERIFICADAS"*. Isso é o sintoma do B1
  observado e contornado pelo arranjo do teste, em vez de investigado: o fluxo é
  sintético, então o engine não roda e a ausência de reemissão não aparece como defeito.
- `tests/test_aar_timeline.py::test_o_proprio_veredito_fecha_a_janela_e_e_de_ground_truth`
  (:212-218) — asserta o comportamento da implementação (veredito na lista de
  incompatíveis) e não o requisito de `03` §3.2, que separa as duas leituras na
  redação-alvo. Ver M1.

Nenhum teste `skip`/`xfail` sem justificativa: os 8 decoradores condicionais
(`tests/test_event_store_postgres.py`, `test_projecoes.py`, `test_projection_cache.py`,
`test_restauracao.py`) dependem de `AURORA_TEST_DATABASE_URL`, e a execução saiu `OK`
sem `(skipped=N)` — com Postgres e Redis no ar, zero testes pulados em 716.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **B1 não foi executado, foi derivado por leitura.** O worktree do auditor não tem
  ferramenta de escrita, e `python -c` e pytest com arquivo novo estão fora da
  allowlist — não consegui escrever o teste de quatro passos que o demonstra. A
  derivação está fechada sobre código citado linha a linha
  (`linhagem.py:138`, `verificacao.py:306-310` e `:346`, `metrics/verificacao.py:230`,
  `events/epoch.py:48`, `api/app.py:340`), e o comentário de
  `tests/test_metrics_verificacao.py:227-231` corrobora o sintoma pelo outro lado. Se a
  correção for contestada, o caminho é plantar exatamente aquela sequência com o
  `LacoDeVerificacao` ligado.
- **As provas de container (P4-10) e a prova do seed (M2) eu não vi rodar.** Rodei
  `check_provas_de_container.py` (rc=0, 2 provas, `8b1a70353c62`, gravadas
  2026-08-23T01:50:23Z) e `check_prova_do_seed.py` (rc=0, `8b1a70353c62`, 3.543.783
  linhas, 150,0 s / 149,2 s, 20 tabelas com SHA-256 igual). O que está provado é que a
  evidência é DESTE commit; a execução em si é atestação do lançador.
- **Nenhum caminho de produção monta os insumos de métrica.** `range_core.metrics.insumo.monta`
  só tem chamadores em `tests/` — não há rota, projeção materializada nem CLI que
  produza TTCD/TTCV/TTIV fora da suíte. A DoD da Fase 6 não pede essa ligação (o
  renderizador é T14/Fase 10), então não é finding; mas significa que os computadores
  não foram exercitados por nenhum fluxo real, e é essa ausência que deixou o B1 fora
  do alcance de qualquer teste ponta a ponta.
- **`review_scope` não tem produtor na árvore.** O contrato existe
  (`contracts/assessment.schema.yaml:158`) e `escopo_revisado` chega como escalar do
  insumo, mas nada resolve a prosa de `03` §5.1 num conjunto de `case_id`. Está
  registrado e decidido como P6-5, com entrega na Fase 7 por ser mudança de contrato
  (docs/progress/fase_6.md:911-935). Não conta como item de DoD desta fase — `03` §3.4,
  que é a fonte normativa das ações, não lista "declarar escopo revisado" — mas o
  número do Brier em exercício real depende de uma peça que ainda não existe.
- **Comportamento sob concorrência do laço de verificação.** `LacoDeVerificacao.avaliar`
  lê o store inteiro e refaz o fold a cada gravação; não avaliei corrida entre dois
  chamadores nem custo com volume de exercício de 4 h — isso é T12/T13, Fases 7 e 9.
- **CI real.** Julguei `.github/workflows/invariants.yml` por leitura e executei
  localmente os passos que a allowlist alcança. Não vi um run do GitHub Actions sobre
  este commit; em particular os passos de `docker compose` do job `contratos` não foram
  exercitados por mim.
```
