# Auditoria de checkpoint - 2026-08-14T17:37:30.187604+00:00

- fase: **0**
- commit auditado: `1abe68f11b323c50df86517b01a8e2af870981a8`
- session_id: `9b30f5ec-47d1-41e4-8059-6637e0e9916e`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have enough measured evidence. Emitting the report.

```text
# AUDITORIA — FASE 0

## VEREDITO: PASS

Commit auditado: `1abe68f11b323c50df86517b01a8e2af870981a8`
Árvore limpa antes e depois de toda a execução (`git status --short` vazio, `git ls-files --others --exclude-standard` vazio).

## FINDINGS

### BLOCKER
nenhum encontrado.

### HIGH
nenhum encontrado.

### MEDIUM

- [M1] Falso bloqueio novo e não declarado: `>` dentro de argumento citado de comando de leitura
  Arquivo: `user-scope/hooks/readonly_bash.py:98`
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(e) — *"Falso bloqueio novo é finding, pela mesma razão de (d) — degrada a capacidade de auditar."*
  Evidência (medida, não inferida):
  - `grep -rn "\-> None:" tools/codegen.py` → `rc=2`, mensagem *"redirecionamento de saida para arquivo"*
  - `grep -n "=>" .github/workflows/invariants.yml` → `rc=2`, mesma mensagem
  - `git for-each-ref --format='%(refname) -> %(objectname:short)' refs/tags` → `rc=2`, mesma mensagem
  Controles negativos que passaram: `grep -n "def _reject" scripts/phase0_negative_tests.py` → `rc=0`; `grep -n "str | None" tools/codegen.py` → `rc=0`. Portanto o bloqueio vem do `>`, não do `grep`.
  A lista `FALSOS_BLOQUEIOS_CONHECIDOS` (`scripts/phase0_negative_tests.py:197-225`) tem exatamente 5 entradas e nenhuma delas cobre este caso. A entrada declarada da mesma família — *"token .env dentro de padrao de busca com outro alvo"* — declara apenas a instância `.env`, não a de `>`.
  Por que é MEDIUM e não HIGH: a direção é fail-closed (não abre escrita), e a leitura é obtenível por outra grafia ou pela ferramenta `Grep`. Não é LOW porque `->` (anotação de retorno Python) e `=>` (arrow function TS) são buscas rotineiras, e a Fase 1 entrega geradores de constantes em TypeScript — o custo cresce exatamente na próxima fase.

- [M2] `git branch` listando é bloqueado, não está declarado, e o comentário do próprio hook afirma o contrário do mecanismo
  Arquivo: `user-scope/hooks/readonly_bash.py:156-157` (comentário) contra `user-scope/hooks/readonly_bash.py:20` (allowlist)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(e)
  Evidência: `git branch --list` → `rc=2`, *"comando fora da allowlist do auditor"*. `git branch -a` → idem. Não consta de `FALSOS_BLOQUEIOS_CONHECIDOS`.
  O comentário em `:156-157` diz: *"`git branch` listando e leitura legitima e nao pode ser negado inteiro; so a deleção."* O mecanismo nega inteiro — `branch` não está entre os subcomandos allowlistados de `:20` —, e o comentário em `:16-19` do mesmo arquivo afirma explicitamente o oposto: *"`branch` SAIU dos subcomandos. Listar ramos se faz com for-each-ref"*. Os dois comentários do mesmo arquivo se contradizem, e o de `:156-157` contradiz também o código.
  Por que é MEDIUM e não HIGH: a divergência é na direção fail-closed (o mecanismo é mais restritivo que o comentário), e a capacidade de leitura está preservada por `git for-each-ref`, que usei com sucesso para listar 7 ramos e 1 tag. É a mesma classe punida como H1 na 14ª e na 17ª auditorias, mas aquelas afirmavam proteção inexistente (fail-open); esta afirma permissão inexistente.

### LOW

- [L1] `tools/README.md` ainda instrui que os seis verificadores não existem
  Arquivo: `tools/README.md:1-12`
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD itens 1 e 2 (os seis existem e são a prova central)
  Evidência: o arquivo diz *"Este diretório é deliberadamente entregue sem os seis verificadores finais. A primeira tarefa real do Claude Code é implementar…"* e lista os seis. Os seis existem e saem `rc=0` na árvore limpa. É a pendência P21, aberta desde a 3ª auditoria e reconfirmada em rodadas seguintes.

- [L2] O probe que guarda a allowlist depende de uma convenção de formatação que nada garante
  Arquivo: `scripts/phase0_negative_tests.py:647`
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(d) (o harness é o mecanismo que força revisão de comando novo)
  Evidência: a extração é `re.findall(r"\^\{SAFE_ENV_PREFIX\}(\([^)]*\)|[A-Za-z][\w-]*)", bloco)`. Ela só enxerga entradas de `ALLOWED` escritas com o literal `^{SAFE_ENV_PREFIX}`. Hoje as 15 entradas de `readonly_bash.py:20-89` usam todas esse prefixo, então a cobertura é completa **neste** commit. Uma entrada futura escrita sem o prefixo entraria na allowlist sem reprovar o harness e sem passar por `COMANDOS_REVISADOS` — que é precisamente o modo de falha que o docstring do probe diz existir para impedir (*"foi assim que `env` passou onze rodadas"*). Registrado como LOW por ser latente, não atual.

- [L3] Actions do CI presas a tag major mutável
  Arquivo: `.github/workflows/invariants.yml:12,13,50,133,134`
  Requisito violado: nenhum requisito textual da Fase 0; registrado por ser a pendência P25, aberta desde a 4ª auditoria.
  Evidência: `actions/checkout@v4` e `actions/setup-python@v5`. Tag major é mutável; o gate real da fase depende dessas ações.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis verificadores liberam árvore limpa | ✅ PASS | Executados um a um: `check_core_boundary` `rc=0`, `check_contract_literals` `rc=0`, `check_event_envelope` `rc=0`, `check_security_constraints` `rc=0`, `check_synthetic_data` `rc=0`, `codegen.py --check` `rc=0` |
| 2. Os seis detectam as violações externas do harness | ✅ PASS | `python scripts/phase0_negative_tests.py` → `rc=0`, com 13 probes de verificador, cada um exigindo `rc==1` **e** menção ao arquivo plantado (`expect_fail`, `scripts/phase0_negative_tests.py:67-89`). `_reject` levanta `SystemExit(1)` (`:60-64`) — o harness reprova de verdade, não só imprime |
| 3. Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | ✅ PASS | Três smoke tests executados: import `domains` em `range-core/x.py` → `exit=2` (INVARIANTE 1); `docs/spec/00_MASTER_SPEC.md` → `exit=2` (SPEC IMUTAVEL); `'academus.enrollment_offline'` em `domains/academus/svc.py` → `exit=2` (INVARIANTE 2) |
| 4(a). `checkpoint-auditor` não declara `Write` nem `Edit` | ✅ PASS | `user-scope/agents/checkpoint-auditor.md:4` → `tools: Read, Grep, Glob, Bash` |
| 4(b). Hook bloqueia as formas enumeradas, provadas nas duas direções | ✅ PASS | 36 escritas bloqueadas + 24 leituras liberadas, cada probe rodado contra fonte versionada **e** cópia instalada (`_hooks_sob_teste`, `:459-474`), com `cwd` no payload (`:483`). `ESCRITA_CONTIDA_PASSA` e `ABSOLUTO_CONTIDO_PASSA` provam a direção oposta |
| 4(c). Superfície aberta declarada, com prova por grafia | ✅ PASS (vacuamente) | `BURACOS_CONHECIDOS = []` (`:456`); harness reporta 0 escritas não bloqueadas. 112 provas de alvo = 14 formas × 8 grafias (relativa, absoluta, absoluta com letra de unidade, til, `$HOME`, `${HOME}`, outro nome de variável, variável não listada) |
| 4(d). Nenhum comando de escrita não declarado passa | ⚠️ PARCIAL | Não encontrei forma nova. Ver "o que não consegui verificar": não executei escrita real; avaliei por leitura da allowlist (`:11-90`) e das negações (`:92-236`), mais o probe `allowlist_e_a_revisada` (`:624-664`), que **afirma o conjunto** e reprova comando não revisado — não apenas conta |
| 4(e). Hook libera leitura/teste; falso bloqueio novo é finding | ❌ FALHA (M1, M2) | Dois falsos bloqueios medidos e ausentes de `FALSOS_BLOQUEIOS_CONHECIDOS`: `>` citado (`->`, `=>`) e `git branch --list` |
| 5. Hook do scenario-designer bloqueia Write/Edit fora de `scenarios/` e Bash fora da allowlist | ✅ PASS | `scenario_scope.py` com `range-core/nope.py` → `exit=2`. `scenario_bash.py`: `git log --oneline` → `exit=2`; `range-cli scenario validate scenarios/academus/pack1` → `exit=0` (duas direções) |
| 6. `ground_truth.yaml` e `GM_NOTES.md` **não** estão no `.gitignore` | ✅ PASS | `.gitignore` lido integralmente (41 linhas); ausentes, com comentário explícito em `:39-40` |
| 7. `.env`/secrets negados em `.claude/settings.json` | ✅ PASS | `:6-15` nega `Read`/`Edit` de `.env`, `.env.local`, `.env.*.local`, `secrets/**`. **Não** reporto ausência de `Write(...)`: `docs/progress/fase_0.md:515-527` registra a resposta da plataforma de que `Write(path)` não é avaliado e `Edit(path)` cobre todas as ferramentas de edição. Acrescentá-lo seria regra inerte |
| 8. Auto Mode desabilitado | ✅ PASS | `.claude/settings.json:4-5` → `"defaultMode": "default"`, `"disableAutoMode": "disable"` |
| 9. Push em `main` deixa `arquitetura` e `seguranca` verdes; `spec_freeze` é PR-only | ⚠️ PARCIAL | Configuração correta e verificável estaticamente: `invariants.yml:9-44` (arquitetura, os 4 verificadores + harness negativo), `:130-142` (seguranca, os 2 restantes), `:47` (`if: github.event_name == 'pull_request'`). Histórico de execução não verificável daqui |
| 10. PR descartável confirma que `spec_freeze` falha com spec+código juntos | ⛔ NÃO VERIFICÁVEL | Lógica presente em `invariants.yml:120-123`; execução do PR não verificável daqui |
| 11. PR descartável confirma exigência do título `spec-change:` | ⛔ NÃO VERIFICÁVEL | Lógica presente em `invariants.yml:125-128`; execução do PR não verificável daqui |
| 12. Branch protection em `main` exige os três contexts | ⛔ NÃO VERIFICÁVEL | Sem acesso a rede/`gh` na allowlist do auditor |
| 13. `spec-v1.0` criada depois de CI + branch protection | ⚠️ PARCIAL | Tag existe e aponta para `b4abd9e`, ancestral de HEAD. Ordem de criação não verificável daqui |
| 14. `/doctor` sem apontamentos | ⛔ NÃO VERIFICÁVEL | Fora do alcance de Bash |

## TESTES QUE NÃO PROVAM O REQUISITO

Procurei especificamente o padrão "verde que não prova" e, no essencial, **não o encontrei**. O harness é mais forte do que a média desta categoria, e registro o que o torna forte porque isso é o resultado da auditoria, não cortesia:

- `expect_fail` (`:67-89`) exige `rc` **exatamente 1**, saída não vazia e menção explícita ao arquivo plantado. Crash de ferramenta não conta como detecção.
- `_reject` (`:60-64`) levanta `SystemExit(1)`. O harness não é "script que sai com zero".
- Os probes de hook rodam contra a **cópia instalada** além da fonte versionada (`:459-474`), e o harness asserta que as duas são idênticas — sem isso, a fonte íntegra certificaria um hook em execução alterado.
- O payload inclui `cwd` (`:483`), então mede o hook no modo em que ele de fato roda, e não em modo degradado.
- Há direção positiva onde ela costuma faltar: `ESCRITA_CONTIDA_PASSA` e `ABSOLUTO_CONTIDO_PASSA` impedem que a regra de alvo degenere em "bloqueia tudo e passa no teste".
- `allowlist_e_a_revisada` (`:624-664`) asserta o **conjunto** da allowlist, não comandos lembrados: comando novo reprova até entrar em `COMANDOS_REVISADOS`.
- Verificado por execução que nada foi escrito: árvore limpa depois de rodar os seis verificadores e o harness completo. Isso prova, por consequência observável, que `codegen.py --check` é read-only e que o harness limpa os probes plantados.

Uma ressalva, registrada como L2: `allowlist_e_a_revisada` só enxerga entradas de `ALLOWED` escritas com o literal `^{SAFE_ENV_PREFIX}`. Hoje todas as 15 entradas usam, então o probe prova o requisito **neste** commit; ele não protegeria contra uma entrada futura escrita fora dessa convenção.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Não há diff contra `main`.** `git merge-base main HEAD` devolve o próprio HEAD e `git log main..HEAD` é vazio: o commit candidato **é** `main`. Auditei o estado da árvore, não um delta. Consequência declarada: `spec_freeze`, que é PR-only (`invariants.yml:47`), nunca correu sobre estes commits. Isso é o previsto pelo `PHASE_0_CHECKLIST.md` §Ordem correta de fechamento para a Fase 0, mas significa que a minha auditoria não pôde usar a fronteira do PR como fonte de evidência.
2. **Itens 10, 11, 12 e 14 da DoD**: exigem `gh`, rede ou `/doctor`, nenhum na allowlist do auditor. Registrados como não verificáveis, não como PASS.
3. **Item 13**: verifiquei que a tag existe e para onde aponta; **não** verifiquei que foi criada depois do CI verde e da branch protection. Observação factual sem juízo: `docs/spec/` mudou depois da tag, pelo commit `edd9527 spec-change: ator de ameaca real documentado e finalidade comercial` (7 arquivos). Isso é o processo de `spec-change` previsto em `CLAUDE.md`, então **não** reporto como finding; registro porque a tag `spec-v1.0` deixou de representar o conteúdo corrente de `docs/spec/`.
4. **Item 4(d)**: não executei nenhuma escrita real para confirmar bloqueio. Escritas de teste com alvo fora do worktree tocariam o repositório principal, e escritas contidas sujariam o objeto da auditoria. Avaliei por leitura da allowlist e das regras de negação, e pelo harness. Consequência honesta: **não posso afirmar que não existe forma nova de escrita não bloqueada** — posso afirmar que não encontrei nenhuma nas superfícies que examinei (subcomandos `git` fora da allowlist, redirecionamentos, flags de saída, substituição/subshell, composição, expansão de variável, grafias de alvo).
5. **Não consigo invocar diretamente o hook que audito.** `readonly_bash.py` não está na própria allowlist, então não pude alimentá-lo com payloads sintéticos. Medi-o ponta a ponta pela minha própria ferramenta Bash — o que é medição real e não inferência —, apoiado na asserção do harness de que fonte versionada e cópia instalada são idênticas neste commit.
6. **`${ cmd; }` do bash 5.3** não é coberto por `_substituicao_ou_subshell`. Não é finding meu: está declarado como limite no docstring (`readonly_bash.py:383-385`). Não o exercitei.

**Justificativa do veredito.** Nenhum BLOCKER. Os dois MEDIUM são violações reais e medidas do item 4(e), ambas na direção fail-closed, sem abrir escrita e sem me impedir de executar a prova central — rodei o harness completo, os seis verificadores e os cinco smoke tests de hook nesta sessão. Por `docs/process/WORKFLOW.md` §Ciclo por fase, MEDIUM não bloqueia o `finalize`; HIGH bloquearia, e não há HIGH.
```
