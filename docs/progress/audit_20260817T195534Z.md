# Auditoria de checkpoint - 2026-08-17T19:55:34.745408+00:00

- fase: **4**
- commit auditado: `278d015170f3322b59bc81cec8dad6e5a7565ba7`
- session_id: `aaf9ae9a-1a52-4548-9536-07e0c1f36a8b`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 4

## VEREDITO: FAIL

---

## FINDINGS

### BLOCKER

- **[B1] As três telas da fase não carregam o banner obrigatório**

  **Arquivo:**
  - `range-core/web/wallboard-shell/index.html:1-12` e `range-core/web/wallboard-shell/main.tsx:50-113` (header em `:52-61` traz só "Universidade Aurora")
  - `range-core/web/participant-view/index.html:1-12` e `range-core/web/participant-view/main.tsx:30-42`
  - `range-core/web/gm-console/index.html:1-12` e `range-core/web/gm-console/main.tsx:150-168` (header em `:151-152` traz só "AURORA — console")

  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §4 — *"Em toda tela e no rodapé de todo artefato gerado (…): `AMBIENTE SIMULADO — DADOS FICTÍCIOS`"*, sob o preâmbulo do documento: *"não admite exceção, reinterpretação ou flexibilização em nenhuma fase, sob nenhuma justificativa técnica."* Os OUTPUTS da fase em `07_IMPLEMENTATION_PHASES.md` §Fase 4 são exatamente estas três telas.

  **Evidência:** busca por `AMBIENTE SIMULADO`, `DADOS FICT` e por `simulado` (case-insensitive) em `range-core/web/`, `range-core/` e `domains/` sobre `.tsx`, `.ts`, `.html`, `.css`, `.py` não retorna nenhuma ocorrência. A única ocorrência da string no repositório é `contracts/evidence.schema.yaml:232` (`banner_text`), que governa **arquivo de evidência**, não tela. Nenhum verificador cobre §4: `tools/check_security_constraints.py:2` declara seu escopo como *"05_SECURITY_REQUIREMENTS.md secao 1"*, e `tools/check_synthetic_data.py` cobre §3. `docs/progress/fase_4.md:69-73` enumera as frentes de `05` que a fase leu — §8, §6 e T15 — e **§4 não aparece em lugar nenhum do registro de 2.774 linhas**.

  **Por que é blocker:** o wallboard é servido sem token (`range-core/api/app.py:435-442`, `range-core/api_surface.yaml:214-238`) num telão de sala de crise, e a participant-view exibe `texto_para_plateia` narrativo. São exatamente os dois artefatos que §4 existe para marcar, e são as duas superfícies com menos contexto para quem entra na sala. Não há mecanismo que fique vermelho por isso — nem hook, nem CI, nem teste —, então o defeito não é pego em nenhuma fase posterior por construção: ele só some se alguém lembrar. É a classe que o próprio projeto nomeia em `docs/progress/fase_4.md:88` (*"verificável é o que falha vermelho sem um humano olhando"*), aplicada contra a fase que a escreveu.

---

### HIGH

- **[H1] `scripts/phase0_negative_tests.py` sai 1 no commit candidato, e a checagem que falha é da própria fase, com mecanismo divergente do seu irmão**

  **Arquivo:** `scripts/phase0_negative_tests.py:1275` (`instalada.read_bytes() != SENTINELA_FONTE.read_bytes()`) contra `scripts/phase0_negative_tests.py:976-977` (`read_text(...).splitlines()`), que implementam a **mesma propriedade** para dois arquivos.

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §Fase 4 DoD (a fase não pode fechar com item de mecanismo quebrado) e `docs/process/WORKFLOW.md`, que classifica bloqueio indevido como defeito; o passo de CI que o nomeia é `.github/workflows/invariants.yml:176-177`, *"teste negativo dos seis verificadores"*.

  **Evidência:** executado no worktree de auditoria, `python scripts/phase0_negative_tests.py` termina com `rc=1` e a última linha `FAIL: sentinela de branch fonte versionada e copia instalada DIVERGEM. Rode bootstrap.sh`. Os dois arquivos **não divergem em conteúdo**: `diff` mostra o mesmo texto, e `wc -c -l` dá `286 11671` na fonte contra `286 11385` na cópia instalada — 286 bytes de diferença em 286 linhas, um por linha. `head -3 … | cat -A` mostra `^M$` na fonte; `git ls-files --eol` devolve `i/lf w/crlf` para `user-scope/hooks/sentinela_de_branch.py`. `.gitattributes:3-4` (adicionado neste mesmo commit) documenta `core.autocrlf=true` na máquina do operador e força `eol=lf` **apenas para `*.sh`**. A checagem irmã, sobre `readonly_bash.py`, passa porque compara `splitlines()`.

  **Por que é HIGH:** a propriedade que a D15 (`docs/progress/fase_4.md:591-601`) declara garantir — *"a cópia instalada é a que roda, e divergência silenciosa é o pior caso"* — não é afirmada em nenhum lugar que seja porta. No CI (Linux, sem `~/.claude/hooks/`) a checagem cai no ramo `AVISO` de `:1270-1273` e retorna sem verificar nada; na máquina do operador ela dispara contra uma não-diferença. E as três pernas do sentinela são provadas contra `SENTINELA_FONTE` (`:1147`), isto é, contra a **fonte**, nunca contra a cópia instalada — a ponte entre as duas é exatamente esta checagem. Resultado líquido: o harness que o CI chama de prova central está vermelho para quem desenvolve e cego para quem julga.

---

### MEDIUM

- **[M1] `GET /console` é uma terceira superfície sem autenticação, além das duas que a spec nomeia, decidida no registro da fase em vez de escalada**

  **Arquivo:** `range-core/api/app.py:451-464`; `range-core/api_surface.yaml:267-288` (`publica: true`); `tests/test_range_api.py:342-358` afirma que as três telas passam pelo middleware sem token.

  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §8 e `06_ACCEPTANCE_TESTS.md` T15 — *"Nenhum serviço exposto sem autenticação, **exceto** `wallboard` e `participant-view`"*. A lista de exceção é fechada pelo preâmbulo do documento.

  **Evidência:** a rota responde 200 sem `Authorization`. A justificativa está em `docs/progress/fase_4.md:677` (D19) e em `range-core/api_surface.yaml:267-288`: nenhum navegador envia `Authorization` numa navegação, e a casca não carrega dado de exercício — o que `scripts/check_telas_sem_vocabulario.py` verifica (rodado: 13 arquivos, 45 termos, rc=0) e o CI reforça sobre o artefato com `--exige-bundle` (`invariants.yml:523-524`).

  **Por que MEDIUM e não LOW:** o argumento é forte e provavelmente correto — sob leitura por *serviço*, o `range-api` continua autenticado, e `POST /session` já teria de ser pública sob qualquer leitura por *rota*. Mas `CLAUDE.md` §Autoridade manda **parar e perguntar** em vez de resolver por inferência, e `05` proíbe flexibilização "sob nenhuma justificativa técnica". Ampliar por decisão de fase um conjunto que a spec escreve como fechado é a forma que a spec-change existe para carregar. Não é achado de desenho: é o caminho processual que foi pulado.

---

### LOW

- **[L1] O registro afirma sobre si mesmo o resultado de um CI que não podia tê-lo visto**

  **Arquivo:** `docs/progress/fase_4.md:2350-2353` — *"`arquitetura`, `spec_freeze`, `seguranca` e `contratos` (…) estão verdes **sobre o commit que vai ser auditado**"*.

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` §Fase 4 (*"nenhum deles é atestação"*, na leitura que o próprio registro faz em `:52`).

  **Evidência:** `git show --stat 278d015` mostra que o commit candidato altera **um único arquivo**, `docs/progress/fase_4.md`, acrescentando as 54 linhas que contêm essa frase. No instante em que a frase foi escrita, o commit que ela descreve não existia. A execução verde relatada em `:2334-2348` é de um commit anterior.

  **Por que LOW:** o conteúdo do commit é só documentação, então o risco material é nulo, e um push posterior no PR em draft pode ter tornado a frase verdadeira depois do fato. Mas a frase é a única evidência oferecida para o item 4 da DoD, e ela é auto-referente — o padrão exato que `:2303-2305` diz estar corrigindo.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — a sequência do DEMO roda ponta a ponta sem intervenção manual | **NÃO VERIFICADO** | `scripts/demo_fase4.py:114-273` implementa o roteiro literal de `07`, com o par que discrimina (503 depois do disparo, 201 depois do rollback, `:210-258`). Ligado ao CI em `invariants.yml:572-577`. Exige `docker compose` — ausente nesta auditoria. |
| 2 — wallboard atualiza em < 1 s via WebSocket | **PASS (parcial)** | `tests/test_range_api.py:197-217` (o frame chega na mesma chamada que gravou o evento), `:219-243` (um frame por evento, não por cliente), `:245-263` (nem `time`, nem `threading`, nem `sched` no caminho, por AST). Os três rodaram. O cronômetro de parede só existe no DEMO de CI (`demo_fase4.py:183-194`), não executado aqui. |
| 3 — refresh recupera o estado corrente no wallboard e na participant-view | **PASS** | `tests/test_range_api.py:143-160` (frame do canal == corpo do snapshot, byte a byte, nas duas projeções), `:175-191` (conectar agora recebe o estado corrente, com o par antes/depois). Ambos rodaram. Servidor: `range-core/api/app.py:382-400`. |
| 4 — reinício do container do engine restaura o exercício a partir do event store | **NÃO VERIFICADO (container)** / PASS na derivação | `range-core/clock/restauracao.py:163-219` derivando os cinco valores do fluxo; `tests/test_restauracao.py:114-380` rodou (T0, acumulado, multiplicador, origem de epoch, pausa). `tests/test_restauracao.py:382-478` (`EmOutroProcesso`) **PULOU** por ausência de Postgres. `scripts/prova_reinicio_de_container.py:165-260` faz `docker restart` e compara `StartedAt` antes/depois — não executável aqui. |
| 5 — rollback aparece anotado na timeline | **PASS** | `range-core/api/projecoes.py:364-396` (`entrada["rollback"]` com motivo e ponto de corte, mais `epoch` por entrada); `tests/test_projecoes.py:680-702` e `tests/test_range_api.py:423` rodaram. |
| 6 — índice de saúde calculado a partir dos `severity_weight` ativos | **PASS** | `range-core/api/projecoes.py:169-189`, com `esta_ativa` (`:135-146`) tratando default `true` e `false` na mesma direção. `tests/test_projecoes.py:279-377` rodou, incluindo `test_as_DUAS_direcoes_de_default_pioram_o_indice_IGUALMENTE` e `test_peso_maior_baixa_mais`. |
| T5, 4º critério — pausado restaura pausado / retomado restaura correndo | **PASS (em processo)** | `range-core/clock/restauracao.py:93-117` (`paused_in` lê os dois eventos); `tests/test_restauracao.py:314-380` rodou, com o par e com `test_um_evento_DEPOIS_da_pausa_nao_significa_retomada`. A metade de container está em `prova_reinicio_de_container.py:200-260`, não executada. |
| T6, metade desta fase (superfícies públicas) | **PASS** | `tests/test_projecoes.py:579-626` (`OQueASalaNaoPodeVer`) rodou; o wallboard não carrega nome de flag e a plateia carrega um campo. `tests/test_telas.py:66-108` rodou. A metade de `operador` está declarada como Fase 5 em `docs/progress/fase_4.md:118-124`, coerente com o NON-GOAL *"três papéis de facilitação"*. |
| Invariantes arquiteturais 1–4 | **PASS** | `python scripts/phase0_negative_tests.py` confirma que os quatro verificadores reprovam contra violação plantada (fronteira core/domain, literal de flag, `event_type`, `objective_ids` no envelope, inclusive na variante `.tsx`). `scripts/check_core_contract_imports.py` rc=0. |
| `05` §6 — bind e portas | **PASS** | `docker-compose.yml:45,64,143,196` publicam apenas em `127.0.0.1`; `.dockerignore:26-38` exclui `.env`, `secrets/`, `scenarios/` e `docs/`. `scripts/check_pinned_images.py` rc=0, 3 imagens pinadas por digest em 4 arquivos. |
| `05` §4 — banner | **FAIL** | Ver **[B1]**. |

---

## TESTES QUE NÃO PROVAM O REQUISITO

**Nenhum encontrado.** Fui atrás dos padrões que me foram encarregados de procurar, e nesta fase eles não estão:

- **Isolamento de papel por UI em vez de payload** — não ocorre. `tests/test_projecoes.py:579-626` varre os bytes serializados da projeção; `tests/test_api_rbac.py:158-162` compara `resposta.content` entre "não é sua" e "não existe" em vez de status.
- **Asserção sobre mock em vez de comportamento** — os únicos `mock.patch.object` (`tests/test_telas.py:131,149`) trocam o **diretório** de onde as telas são lidas e depois afirmam sobre a resposta HTTP real. É costura de caminho, não asserção sobre o duplo.
- **`skip`/`xfail` sem justificativa** — não há `xfail`. Todos os 73 pulos vêm de três guardas de serviço com razão impressa e comando de reprodução (`tests/_academus_banco.py:50-63`, `tests/test_event_store_postgres.py:49+`, `tests/test_projecoes.py:220`, `tests/test_projection_cache.py:248`, `tests/test_restauracao.py:381`).
- **Verificador que passa por não ter o que olhar** — explicitamente atacado. `scripts/check_web_sem_derivacao_probes.py` e `scripts/check_telas_sem_vocabulario_probes.py` reprovam diretório de cliente vazio, diretório ausente e vocabulário vazio; `tests/test_telas.py:69-72` e `tests/test_range_api.py:284` têm asserção anti-vacuidade explícita. Rodei os dois probes: passam nos 17 e nos 13 eixos.
- **Teste que passaria com implementação sempre-nega ou sempre-libera** — o par que discrimina aparece consistentemente: `test_api_rbac.py:295-302`, `demo_fase4.py:158-167` contra `:247-258`, `prova_reinicio_de_container.py:200-260`.

Vale registrar o contrário do que fui procurar: `scripts/check_api_surface_probes.py` reprova nos 66 eixos, e `scripts/check_web_sem_derivacao_probes.py` prova as **duas** direções — que o gate reprova derivação e que **não** reprova `map`, `length` sem comparação e o índice pronto. Prova negativa de gate que também demonstra o que ele deixa passar é rara, e é o que impede o gate de ser abandonado.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Tudo que exige containers.** `docker compose up` falhou antes de eu começar. Ficaram sem execução: `scripts/demo_fase4.py` (item 1), `scripts/prova_reinicio_de_container.py` (item 4 e a metade de container de T5), `scripts/mede_cache_frio.py` (P3-2 / D22), o passo `web-build` e a varredura `check_telas_sem_vocabulario.py --exige-bundle` sobre o artefato. Li os quatro scripts e as asserções são as certas — em particular a comparação de `StartedAt` em `prova_reinicio_de_container.py:170-175`, que é o que impede a prova de passar com reinício de processo. Mas **li, não medi**.

2. **73 de 335 testes pularam por ausência de Postgres e Redis.** Nominalmente: `test_api_rbac.py` (2 classes), `test_api_degradacao.py` (5), `test_business_state_postgres.py` (3), `test_queda_de_sessao.py` (2), `test_event_store_postgres.py` (5), `test_restauracao.py:382`, `test_projecoes.py:220`, `test_projection_cache.py:248`. Consequência concreta: a varredura de payload de degradação e o RBAC do adapter — que esta fase migrou para Postgres — **não foram exercidos por mim**. O resultado que li foi `Ran 335 tests … OK (skipped=73)`, e li os 73 como pulo, não como verde.

3. **Não pude confirmar nenhum resultado de CI.** Não tenho rede e não tenho `gh`. A afirmação de `docs/progress/fase_4.md:2334-2353` de que os quatro contexts estão verdes é o que sustenta os itens 1 e 4, e ela é **auto-relatada** — ver **[L1]**. Um auditor com acesso ao run do GitHub Actions fecharia os itens 1 e 4 que eu deixei em NÃO VERIFICADO.

4. **A renderização e a legibilidade a 10 m** (`01` §6). Declarado como limite pela própria fase em `:106-113`, e concordo com o limite. Registro, porém, que **[B1] cai dentro dele por acidente e não deveria**: a presença do banner é propriedade do payload/DOM, verificável sem navegador, exatamente como as duas varreduras de vocabulário que a fase construiu.

5. **A causa da divergência de fim de linha em [H1].** Confirmei que os conteúdos são idênticos e que a diferença é CRLF contra LF, mas não consegui determinar por qual caminho a cópia instalada chegou em LF, já que `bootstrap.sh:52` usa `cp`. Isso não muda o achado — a checagem falha, e o mecanismo dela diverge do irmão —, mas muda o conserto, e eu não sei qual é.
