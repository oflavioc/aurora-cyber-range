# Auditoria de checkpoint - 2026-09-24T17:20:08.450261+00:00

- fase: **9**
- commit auditado: `9ba597304d6e6bf1558227ece6d98174f126218c`
- session_id: `0f433e9b-3713-4c31-b759-e11d1ce6ab61`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 9

## VEREDITO: FAIL

A Fase 9 reprova por dois BLOCKERs. O primeiro: o arquivo `database_audit.jsonl` entrega aos participantes o gabarito da Linha B. O segundo: a telemetria gravada no event store não é o mesmo conjunto que o arquivo CEF projeta. A suíte está verde e os verificadores passam, mas nenhum teste cobre esses dois caminhos.

Candidato `9ba5973`, árvore `924cd988a854`, comparado com a base `c77120c`. A guarda de base diz que esta auditoria é PORTA.

**O que rodei:**
- **Suíte:** `.aurora-audit/venv/Scripts/python -m unittest discover -s tests` → **1125 testes, OK, 0 pulados** (Postgres e Redis no ar). A primeira tentativa com o `python` do PATH (3.14 global) falhou por falta de módulos. Isso é ambiente, não defeito da árvore; a rodada acima com o venv é a que vale.
- **Provas gravadas:**
  - `check_provas_de_container.py`: 2/2 OK sobre a árvore `924cd988a854`.
  - `check_prova_do_seed.py`: 3.543.783 linhas; 150,2 s e 158,5 s, contra orçamento de 300 s.
  - `check_prova_do_exercicio_4h.py`: item 7 em **2,026 s** com 96.650 eventos; 7 arquivos do pack idênticos ao disco.
- **Verificadores**, todos rc=0: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check`, `check_banner_de_simulacao`, `check_contract_examples`, `check_gabarito_fora_do_git`, `check_core_contract_imports`, `check_progress_consistency`.
- `range-cli evidence verify tests/fixtures/pack_exemplo` → rc=0.

## FINDINGS

### BLOCKER

- **[B1] `database_audit.jsonl` entrega o gabarito da Linha B: quais linhas são casos, e em que conjunto cada uma está**
  - **Arquivos:**
    - `domains/academus/seed/gabarito.py:170`: `"projections": ["database_audit"]`, acrescentado nesta fase.
    - `gabarito.py:71-75` e `:126-171`: fatos criados só para os três conjuntos de caso, na ordem indevidos → ambíguos → legítimos-suspeitos.
    - `gabarito.py:182`: `facts = linha_a + fatos`.
    - `range-core/evidence/projecao.py:305,328`: preserva a ordem do documento.
    - `domains/academus/evidence_generators/database_audit.py:47-48`.
  - **Requisitos violados:** `00_MASTER_SPEC.md` §3 (evidência observável não afirma ground truth); `05_SECURITY_REQUIREMENTS.md` §6 (gabarito fora de tudo que vai ao participante); CLAUDE.md §"Ground truth" ("gabarito é **quais casos**"); `08` §3 (o arquivo carrega "alterações de nota com IP e sessão", ou seja, a trilha).
  - **Evidência:**
    - `gabarito.gerar` cria fato apenas para as linhas de `indevidos_comprovados`, `ambiguos_legitimos` e `legitimos_suspeitos`. Os conjuntos `ruido_de_manutencao`, `credenciais_compartilhadas` e `legitimos_normais` só são contados (`:174-175`) e não viram fato.
    - Com a projeção nova, o `database_audit.jsonl` contém exatamente a população de casos, sem o ruído.
    - A ordem é por conjunto: `projetar` filtra `ordem_do_documento` sem reordenar, e `jsonl.linhas` não ordena. Logo, as linhas da Linha B logo após a exfiltração são os 22 indevidos comprovados, em sequência. O salto de timestamp na troca de conjunto marca a fronteira.
    - O cabeçalho de `projecao.py:48-52` afirma que o gabarito fixa "a ordem do incidente". Isso é falso para a Linha B, que está em ordem de conjunto.
    - Nenhum teste de `tests/test_evidence_*.py` projeta fato da Linha B: o grep por `gabarito|linha_b|grade_change` não acha nenhuma chamada.
  - **Por que é blocker:** o arquivo pré-posicionado para o time azul (`08` §5) resolve a Linha B inteira. O participante recebe a lista dos casos e a partição por defensibilidade. As métricas de calibração e o OBJ da Linha B passam a medir leitura de ordem de arquivo. É a mesma classe do B1 da 2ª auditoria, por outro canal.

- **[B2] A telemetria que o produto grava no store não é a projeção CEF: inclui fatos que não declaram `cef` e fatos sem `projections`**
  - **Arquivos:**
    - `range-core/engine/loader/pack_loader.py:405-422`: `_telemetria_do_pack` chama `programar(ground_truth.get("facts"))` com **todos** os fatos.
    - `range-core/telemetry/forwarder.py:96-124`: `programar` não olha `projections`.
    - `range-core/engine/inject_engine.py` `start()` → `tick_de_telemetria()`.
  - **Requisitos violados:** `08` §2 (a telemetria CEF é projeção, e fato sem `projections` é invisível ao time azul); `06` T13, segundo critério (fato sem `projections` não aparece em nenhuma fonte); item 4 da DoD.
  - **Evidência:**
    - O `cef.log` recebe só os fatos com `cef`, filtrados pela cobertura em `projecao.py:328`: GT-A-014 e GT-A-031.
    - O store recebe `telemetry_emitted` também para GT-A-019, cuja `projections` é só `[identity_audit]` (`linha_a.py:159`). O próprio registro confirma "três" eventos no `ransomware-universidade`.
    - Recebe também um `GRADE_CHANGE_RETROACTIVE` por fato da Linha B (`projections: [database_audit]`). Isso reproduz o vazamento do B1 no SIEM: telemetria só para as linhas-caso.
    - Um fato sem `projections` com `fact_class` mapeada seria emitido do mesmo jeito.
  - **Por que é blocker:** a afirmação da DoD, "um produtor só, o mesmo dicionário" (`fase_9.md` §3, item 4), é falsa no nível do conjunto. O mesmo exercício mostra no SIEM sinais sem linha no `cef.log`, e o limite de detecção de `08` §2 deixa de valer no produto. Os testes não pegam isso (ver abaixo).

### HIGH

- **[H1] `ingest_time` exigido pela spec virou inexpressável no contrato de telemetria**
  - **Arquivos:** `contracts/events.schema.yaml:775-780` (`telemetry_emitted_payload` com `additionalProperties: false`, sem `ingest_time` nem o instante do fato); `range-core/telemetry/emissao.py:74-83`; `range-core/telemetry/cef.py:40-46` ("o evento não precisa dele").
  - **Requisitos violados:** `00_MASTER_SPEC.md` §5.6 ("Telemetria carrega também `ingest_time`") e `01_ARCHITECTURE.md` §3:99 ("`ingest_time`, distinto de `event_time`").
  - **Evidência:**
    - O grep por `ingest_time` fora de `docs/spec` não acha nada.
    - O `telemetry_emitted` gravado não tem nem o instante do fato (o `T-17d 02:14`, que o `discoverability.requires` manda correlacionar) nem um `ingest_time` distinto.
    - O payload fechado impede acrescentá-los sem mudar o contrato.
  - **Consequência:** a fase que cria o contrato de telemetria fecha, por schema, um campo normativo do MASTER. O item 6 ("replay respeita o clock") fica sem a distinção de marcas que o torna verificável no evento.

### MEDIUM

- **[M1] O teste de consistência mútua de T13 aceita que qualquer fonte deixe de carregar qualquer campo**
  - **Arquivo:** `tests/test_evidence_generators.py:565-579`.
  - **Requisito:** `06` T13, primeiro critério: "todas as projeções declaradas […] apresentam usuário, IP e timestamp".
  - **Evidência:** o filtro `if valores[campo] is not None` exclui de forma genérica qualquer fonte que omita o campo, e só exige `vistos` não vazio. Um `vpn.py` que parasse de escrever `src=` continuaria verde. Não há mutante para isso em `test_evidence_generators_probes.py`, e a exceção do `precursor` para `actor` não está nomeada como a única.

- **[M2] Resíduo do H3 da 2ª auditoria: o harness ainda declara margem que a prova desmente**
  - **Arquivos:** `scripts/medida_do_exercicio_4h.py:123` ("1,729 s PASSA, com 42% de folga") e `:133-134` ("maior volume com margem REPRODUZIVEL"); também `docs/progress/fase_9.md:587`.
  - **Requisito:** R2 §1 e a disposição do H3 registrada em `fase_9.md` §2.10 ("para de declarar margem").
  - **Evidência:** o próprio registro observa 2,75 s (folga de 8%). A correção foi feita só na §2.8.

### LOW

- **[L1] `evidence verify` não confere `window` nem `delivery_mode` do manifesto**
  - **Arquivo:** `range-core/evidence/build.py:217-277`.
  - **Requisito:** `08` §7 (o manifesto declara janela e modo de entrega).
  - **Evidência:** um `MANIFEST.json` com `window` ou `delivery_mode` adulterados, com hashes intactos, passa por todos os degraus.

- **[L2] `MANIFEST.json` é artefato gerado e não tem o banner**
  - **Arquivo:** `range-core/evidence/build.py:137-140`; fixture `tests/fixtures/pack_exemplo/evidence/MANIFEST.json`.
  - **Requisito:** `05` §4 ("rodapé de todo artefato gerado") e `06` T13 ("cada arquivo").

**Achados órfãos da rodada anterior:** nenhum. As tabelas de `fase_9.md` §2.9 e §2.10 dão disposição aos M/L das duas rodadas; a ressalva é o M2 acima.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Toda fonte é projeção de `fact_id`; nenhum gerador inventa entidade | **FAIL** | O motor e as guardas funcionam (elenco, IP, hostname e `VereditoDoGabarito` testados). Mas a projeção da Linha B expõe o gabarito (B1). |
| 2. `evidence verify` dirigido por fato | PASS (com L1) | `conferir` compara `projects_facts` e `format`; 38 testes; CLI rc=0 no fixture. |
| 3. `precursor_events.jsonl` reproduzível; edição detectada por hash | PASS | `test_evidence_build.py`, com os casos de hash e de conluio. |
| 4. Telemetria CEF é projeção | **FAIL** | B2: o store recebe o conjunto de todos os fatos, e o `cef.log` recebe o conjunto dos fatos com `cef`. |
| 5. Sem anexo, binário, IOC ou domínio roteável | PASS | `check_synthetic_data` rc=0; `IOCEncontrado` com a tokenização por `=`, `\|` e espaço testada; `.eml` sem multipart. |
| 6. Replay respeita o clock de exercício | PARCIAL | `Replay` lê `elapsed_seconds()`, com testes de pausa e de não reemissão. No produto, tudo vence em t=0, e o `ingest_time` exigido está ausente (H1). |
| 7. Reconstrução < 3 s com `telemetry_emitted` de 4 h | PASS (não vi rodar) | `check_prova_do_exercicio_4h.py`: 2,026 s, 96.650 eventos, árvore conferida. |
| T13: fato sem `projections` não aparece | **FAIL** | Vale para os arquivos; não vale para a telemetria (B2). |
| T13: banner na 1ª linha | PASS nas fontes (L2 no manifesto) | `banner.py` e `check_banner_de_simulacao` rc=0. |

## TESTES QUE NÃO PROVAM O REQUISITO

- `tests/test_telemetry_forwarder.py:138-140`: o `setUp` **pré-filtra** os fatos por `cef` antes de `programar`. O comentário diz que passar todos "mediria uma composição que não existe", mas é exatamente a composição que `pack_loader.py:422` faz. O teste esconde o B2.
- `tests/test_inject_engine.py:863` (`test_o_START_emite_a_telemetria_pre_posicionada`): compara com `len(self.pack.telemetria)`, que é a própria saída do produto. É tautológico sobre quais fatos viram telemetria.
- `tests/test_evidence_generators.py:561-579`: ver M1.
- Nenhum teste projeta fatos da Linha B em evidência (B1).

## O QUE EU NÃO CONSEGUI VERIFICAR

- **B1 foi estabelecido por leitura de código, não por execução.** A allowlist bloqueia `python -c`, e o pack materializado com o seed de produção não está neste worktree (`.aurora-pack` é o pack reduzido das provas de container). A cadeia `materialize` (`cli.py:174-181`) → `evidence build` → `projetar` → `jsonl.linhas` não tem ponto de reordenação.
- **As três provas gravadas** foram produzidas pelo lançador. Confirmei o hash da árvore; não vi medir.
- **O laudo da 2ª auditoria** (`docs/progress/audit_20260923T191036Z.md`, citado em `fase_9.md:472`) não está versionado, e o hook bloqueia leitura fora do worktree. Conferi os órfãos M/L só pela tabela da §2.10.
- **Se `MANIFEST.json`**, que carrega `fact_id` em `projects_facts`, **chega ao participante** junto do `evidence/`: não há ainda rota de entrega que permita checar.
- **CI Linux** (`invariants.yml`): não executado aqui.
