# Auditoria de checkpoint - 2026-08-18T15:15:30.274709+00:00

- fase: **5**
- commit auditado: `c119718c615fd3a614231a91d56ff646ff14ed71`
- session_id: `39f1d81a-0441-461c-91ef-eabfd1c1e66e`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have enough evidence. Here is the audit.

```text
# AUDITORIA — FASE 5

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] Itens 1 e 2 da DoD sem medição amarrada ao commit candidato
  Arquivo: scripts/check_prova_do_seed.py:93-114 (direções (a) e (b));
          docs/progress/fase_5.md:2128-2134 (o laço, admitido pelo autor)
  Requisito violado: 07_IMPLEMENTATION_PHASES.md § Fase 5, itens 1 e 2 da DoD
                     ("Seed completo em < 5 min"; "Mesmo RANDOM_SEED produz
                     dataset byte-idêntico em duas execuções");
                     06_ACCEPTANCE_TESTS.md T8 primeiro critério; T3 (máquina,
                     data e stack ao lado do número)
  Evidência:
    - `python scripts/check_prova_do_seed.py` sai **rc=1** neste worktree:
      "`.aurora-prova-do-seed.json` nao existe ou nao e JSON legivel. Os itens 1
      e 2 da DoD ficam sem prova, e ISTO NAO DEGRADA PARA OK".
    - A mensagem do próprio commit candidato diz: *"A prova do seed refeita sobre
      `c6e4787`"*. O candidato é `c119718`. Ainda que o arquivo fosse copiado
      para cá, a direção (b) — `doc["commit"] != HEAD` — o reprovaria.
    - docs/progress/fase_5.md:2128-2134 registra o laço e a saída ("a medição é o
      último passo antes do checkpoint... nada é commitado depois dela") — e
      `c119718` é justamente um commit posterior à medição.
    - Na suíte, item 2 só é exercido em `dataset.ESCALA_REDUZIDA`
      (tests/test_seed_determinista.py:116-128) e **item 1 não é exercido em
      lugar nenhum**: o orçamento de 5 min é sobre 3.543.783 linhas, e
      `prova_seed_completo.py` está deliberadamente fora da allowlist
      (user-scope/hooks/readonly_bash.py:279-293).
  Por que é blocker: o item 1 da DoD não tem nenhuma evidência executável neste
  checkout — nem parcial. O gate que esta própria fase construiu para respondê-lo
  está vermelho, e ele foi escrito explicitamente para não degradar para OK.
  Aceitar PASS aqui seria trocar mecanismo por atestação, que é a classe de
  defeito que o M2 desta fase existe para fechar.

### HIGH

- [H1] O lançador nunca entrega `.aurora-prova-do-seed.json` ao worktree de auditoria
  Arquivo: scripts/start_checkpoint_audit.sh:385-425 (trata só as provas de
          container); .gitignore:47 (o arquivo é, corretamente, não-versionado);
          scripts/check_prova_do_seed.py:55 (`REPO_ROOT` = raiz do worktree que
          roda o verificador)
  Requisito violado: 07_IMPLEMENTATION_PHASES.md § Fase 5, itens 1 e 2 da DoD —
                     o mecanismo criado para prová-los não é satisfazível
  Evidência: `grep prova-do-seed scripts/start_checkpoint_audit.sh` não retorna
  nada; o script grava e transporta apenas `grava_provas_de_container.py`
  (linha 415). O arquivo é escrito pelo `prova_seed_completo.py` na raiz do
  worktree do operador, e o worktree de auditoria é criado do zero — logo a
  direção (a) reprova **por construção**, em toda auditoria futura desta e das
  próximas fases.
  Por que é high: não é a ausência de uma medição desta rodada; é um gate que
  nasce permanentemente inalcançável. O `check_provas_de_container.py` recebeu o
  transporte no lançador e funciona; o seu gêmeo do seed foi entregue sem ele, e
  o verificador entrou na allowlist do auditor
  (user-scope/hooks/readonly_bash.py:262-270) como se funcionasse.

### MEDIUM

- [M1] A linha do item 1 no quadro de DoD cita um número que não é de nenhuma medição do candidato
  Arquivo: docs/progress/fase_5.md:1573 ("**144,3 s de 300 s**")
  Requisito violado: 06_ACCEPTANCE_TESTS.md T3 (número de desempenho com máquina,
                     data e stack declaradas *junto do número*) — e a própria
                     §10.8 do registro
  Evidência: o mesmo documento traz três valores: 159,4 s (linha 1028), 144,3 s
  (linhas 1211 e 1573) e 145,1 s / 148,5 s (linha 2111, a medição corrente). O
  commit `c119718`, cujo propósito declarado é "a medicao assinada do candidato",
  atualizou apenas o bloco da §10.8 e deixou o quadro de DoD com o número
  anterior. O quadro é onde se lê "o item 1 passa"; ele é a linha que sobrevive.
  Por que é medium: reintroduz, na linha mais lida do registro, exatamente a
  ambiguidade "número sem dono" que a §10.8 descreve como o sintoma que o M2 veio
  fechar.

- [M2] Item 3 da DoD: a role que a aplicação **conecta** mantém `UPDATE` sobre `audit_trail`
  Arquivo: tests/test_trilha_de_auditoria.py:121-168 (`OQueT7NaoProva`);
          domains/academus/audit/trilha.py:219 (`SET LOCAL ROLE`)
  Requisito violado: 06_ACCEPTANCE_TESTS.md T7 segundo critério ("A role da
                     aplicação não possui UPDATE, DELETE ou TRUNCATE na tabela")
  Evidência: `test_a_role_QUE_CONECTA_ainda_possui_UPDATE_na_trilha` **passa** —
  isto é, afirma que a role de conexão tem `UPDATE`. O `REVOKE` alcança
  `academus_app` (alembic/versions/0004_trilha_de_auditoria.py:218-227), e a
  escrita da trilha assume essa role por `SET LOCAL ROLE`, mas `RESET ROLE` está
  a um comando de distância (test:160-168).
  Por que é medium e não blocker: 02_DOMAIN_ACADEMUS.md §4 exige nominalmente a
  role `academus_app` `INSERT`-only com os três `REVOKE`, e isso está implementado
  e verificado por `has_table_privilege` (test:84-101), não por comportamento. O
  limite está declarado, exercido por teste que fica vermelho quando fechar, e
  registrado como P5-3 — não está escondido atrás de um verde.

### LOW

- [L1] A posição da quebra é extraída por varredura de dígitos na mensagem do core
  Arquivo: domains/academus/audit/trilha.py:316-325 (`_posicao`)
  Requisito violado: 06_ACCEPTANCE_TESTS.md T7 terceiro critério (posição exata)
  Evidência: `_posicao` devolve o primeiro token numérico de `str(ChainBroken)`.
  Uma mensagem futura do core que passe a citar outro número antes da sequência
  muda a posição reportada sem que nada fique vermelho fora dos três casos
  cobertos (test:200-229). A alternativa está declarada no docstring.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Seed completo em < 5 min | **FAIL** | `check_prova_do_seed.py` rc=1 (arquivo ausente); medição gravada sobre `c6e4787`, não sobre o candidato. Nenhum teste da suíte mede escala completa. [B1] |
| 2. Mesmo `RANDOM_SEED` → dataset byte-idêntico | **PARCIAL** | Provado em `ESCALA_REDUZIDA`, nas duas direções: `test_o_mesmo_seed_produz_o_mesmo_dump` (20 tabelas, dump não-vazio) e `test_seeds_DIFERENTES_produzem_dumps_diferentes`; mais `test_o_gerador_nao_le_relogio` por AST. Em escala completa (3,5 M linhas, 20 digests): sem prova amarrada ao candidato. [B1] |
| 3. `UPDATE`/`DELETE` falham por trigger **e** por permissão de role | PASS com limite | `TrilhaEhAppendOnly` — trigger recusa os dois verbos; `has_table_privilege` nega os três a `academus_app`; par positivo (`INSERT`/`SELECT`) impede vacuidade. Migration 0004 traz `CREATE ROLE`, `REVOKE ALL`, `REVOKE UPDATE, DELETE, TRUNCATE` e trigger `BEFORE UPDATE OR DELETE ... FOR EACH ROW`. Limite em [M2] |
| 4. `GET /audit/verify-chain` detecta adulteração induzida | PASS | `CadeiaDetectaAdulteracao` — campo alterado (posição 2), `payload` alterado (posição 1), remoção no meio (posição 3, motivo "buraco ou reordenacao"), cadeia íntegra como controle, e o limite do truncamento de cauda afirmado como teste. Rota pelo stack ASGI com 200/403/401 e forma estável de resposta |
| 5. Os seis conjuntos da Linha B nos volumes especificados | PASS | `SeisConjuntosDaLinhaB` — volumes lidos de `02` §6.1 pelo parser do verificador (não das constantes do gerador), **disjunção** par a par e **união = trilha inteira**; mais `check_volumes_da_linha_b.py` cruzando spec × constantes nas duas direções, sem banco |
| 6. `GM_NOTES.md` contém a query de referência | PASS | `tests/test_gabarito.py` — o CI **produz** o artefato e o julga: texto presente, query **executada** devolvendo 22 e nenhum dos 11, linter recusando fato órfão e recusando `GM_NOTES` sem citação, prova por AST de que `conferir` roda dentro de `gerar`, `ground_truth` validado contra o contrato, e o par de dois seeds provando que a propriedade é do gerador |

Invariantes arquiteturais e segurança (fora do quadro, verificados):
`check_core_boundary`, `check_contract_literals`, `check_event_envelope`,
`codegen --check`, `check_security_constraints`, `check_synthetic_data`,
`phase0_negative_tests` (os seis reprovando contra violação plantada),
`check_api_surface`, `check_fold_authority`, `check_core_contract_imports`,
`check_trilha_de_auditoria`, `check_gabarito_fora_do_git`,
`check_secoes_de_seguranca`, `check_allowlist_do_auditor`,
`check_progress_consistency` — todos verdes. Suíte: **402 testes, OK, zero skip,
zero mock**. IPs apenas em RFC 5737 (dataset.py:85-86); o modelo não tem coluna
de CPF, e-mail, telefone ou endereço; banner de `05` §4 no template do
`GM_NOTES`. A extração da primitiva de hash para `range-core/events/integrity.py`
não leva semântica de domínio, e o invariante 1 permanece.

Alteração da allowlist do auditor (user-scope/hooks/readonly_bash.py): revisada
linha a linha. **Só acrescenta verificadores read-only**, com motivo por entrada,
e mantém fora `prova_seed_completo` e `grava_provas_de_container` (escrita longa,
Docker, DSN arbitrária). Não enfraquece o auditor.

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado — e isso é afirmação, não silêncio. Procurei especificamente as
formas que esta fase tinha motivo para tomar:

- **Teste que compara o gerador consigo mesmo.** Era o M2 da rodada anterior e
  está corrigido na direção certa: `test_os_volumes_sao_os_de_02_secao_6_1` lê
  `docs/spec/02_DOMAIN_ACADEMUS.md` §6.1 pelo parser de
  `check_volumes_da_linha_b.py` e afere o **dado semeado** contra ela.
- **Consistência por seed em vez de por fato.** `test_o_MAPEAMENTO_caso_para_fato_muda_com_o_seed`
  compara conteúdo (`actor`, `dest`), não `fact_id` ordinal — e foi ele que achou
  o vazamento em que as 22 primeiras linhas da trilha eram sempre os indevidos.
- **Imutabilidade só por trigger.** Os dois mecanismos têm testes separados, com
  o par positivo que impede o `assertFalse` triplo de passar por role vazia.
- **Linter que compara em vez de recusar.** `gabarito.conferir` roda **dentro** de
  `gerar` (provado por AST), e há teste para a recusa por vacuidade.
- **Resíduo aceitável:** `test_a_query_do_GM_NOTES_EXECUTADA...` (test_gabarito.py:88)
  ainda afere contra `dataset.INDEVIDOS`. A cadeia fecha porque
  `check_volumes_da_linha_b.py` é gate de CI e cruza essa constante com `02` §6.1
  nas duas direções — mas é o único ponto onde a asserção não toca a spec direto.

Observo, em sentido inverso: vários testes desta fase afirmam **limites** em vez
de escondê-los (`test_truncamento_da_cauda_NAO_e_detectado`, `OQueT7NaoProva`),
de modo que o dia em que o limite cair fica vermelho. É o oposto do defeito que
eu procuro.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 1 e 2 em escala completa.** `prova_seed_completo.py` está fora da minha
  allowlist por decisão registrada (escreve 3,5 M linhas duas vezes, exige DSN),
  e o artefato que o substituiria não está neste worktree. Não afirmo que a
  medição não ocorreu — afirmo que **nada neste checkout a liga a este commit**.
- **As provas de container (P4-10).** Rodei `check_provas_de_container.py`: ele
  confere o SHA `c119718c615f` e imprime as duas provas verdes (DEMO ponta a
  ponta e reinício de container, os dois casos de T5). **Eu não as vi rodar** —
  o que as amarra a este commit é o SHA gravado, e o verificador reprovaria por
  divergência ou por ausência.
- **O artefato gerado com o `RANDOM_SEED` de produção.** O que o CI julga é o
  gerador, com seed de teste. A propriedade provada independe do valor do seed,
  mas o `GM_NOTES.md`/`ground_truth.yaml` que vão à sala nunca são vistos por CI
  nenhum — limite declarado pelo próprio verificador.
- **Comportamento do banco em deploy real.** `REVOKE` e trigger foram verificados
  contra o Postgres efêmero desta sessão, com a migration aplicada. Dono e
  superusuário atravessam os dois, por desenho declarado (D13).
- **Sincronia das cópias instaladas dos hooks.** `phase0_negative_tests.py`
  reportou "2 conferidas, 1 AUSENTES (guarda de branch) — sincronia NAO verificada
  nesta execucao".
- **A base de comparação.** Usei `fd34c444` como dado pelo lançador, conforme
  instruído; não resolvi `main` por conta própria.
```

**Resumo:** a substância da Fase 5 — modelo completo, trilha com hash encadeado, role, trigger, verificação com posição, a partição de seis conjuntos da Linha B e o gabarito gerado-e-julgado — está implementada e provada por testes que, no geral, atacam o requisito e não a sua aparência. O que reprova é de outra natureza: o item 1 da DoD não tem prova alguma neste checkout, o item 2 só tem prova em escala reduzida, e o mecanismo que esta própria fase criou para fechar essa lacuna (`check_prova_do_seed.py`) está vermelho — por ausência do artefato aqui e, independentemente disso, porque a medição foi gravada sobre `c6e4787` e o candidato é `c119718`. O laço está descrito no registro pelo próprio autor; o commit candidato é a terceira volta dele.
