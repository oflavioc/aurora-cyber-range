# Auditoria de checkpoint - 2026-08-14T17:13:37.391079+00:00

- fase: **0**
- commit auditado: `9cbdb9986382bef63428a767794e82fd623a889e`
- session_id: `30f7fdba-e21a-4936-bc03-543812625c49`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have measured enough. Here is the audit.

```text
# AUDITORIA — FASE 0

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] A contenção por alvo é negada para UM nome de variável em UMA grafia; qualquer outra variável de ambiente escapa do worktree de auditoria
  Arquivo: `user-scope/hooks/readonly_bash.py:154-159` (regra de caminho absoluto)
           `scripts/phase0_negative_tests.py:361-367` (`GRAFIAS_DE_ALVO`)
           `scripts/phase0_negative_tests.py:374-380` (`ESCRITA_CONTIDA_PASSA`)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done, item 4(c) e 4(d)
    4(c): "Uma forma só pode ser declarada se **nenhuma grafia do seu alvo** escapar do
          worktree de auditoria — provado no harness contra as quatro grafias
          equivalentes: relativa, absoluta, `~` e variável de ambiente."
    4(d): "Nenhum comando de escrita **não declarado** passa: qualquer forma nova
          encontrada é finding, não defeito aceito."
  Evidência (medida nesta sessão, não inferida):

    | comando                  | resultado                                   |
    |--------------------------|---------------------------------------------|
    | `ls ~/ \| head -3`        | rc=2 — BLOQUEADO "caminho absoluto"         |
    | `ls $HOME/ \| head -3`    | rc=2 — BLOQUEADO "caminho absoluto"         |
    | `ls ${HOME}/ \| head -3`  | **rc=0** — listou `AppData / Contacts / ...` |
    | `ls $USERPROFILE/ \| head -3` | **rc=0** — listou o mesmo diretório     |
    | `ls $TEMP/ \| head -3`    | **rc=0** — listou o `%TEMP%` do usuário     |

    O mesmo alvo, em três grafias não negadas, alcançou fora do worktree de auditoria.
    A regex em `:158` nega literalmente `~[/\\]` e `\$HOME[/\\]`. `${HOME}` não casa
    (há `{` depois do `$`), e nenhuma outra variável é mencionada.

    Admissão de forma de ESCRITA medida:
    `pytest --basetemp=${HOME}/aurora_audit_probe_dir --version`
      → **não** houve bloqueio do hook; o comando chegou ao shell e retornou
        `rc=127 /usr/bin/bash: line 1: pytest: command not found`.
      Um bloqueio do hook seria `rc=2` com "BLOQUEADO". Não foi.

    `GRAFIAS_DE_ALVO:366` exercita a grafia de variável de ambiente com exatamente
    uma entrada — `"$HOME/projeto/CLAUDE.md"`. As 70 "provas de invariante"
    (14 formas × 5 grafias) certificam a grafia lembrada, não a propriedade.

  Por que é blocker: `pytest --basetemp` **remove** o diretório indicado — medição do
    próprio projeto, registrada em `docs/progress/fase_0.md:192` ("`--basetemp`
    **remove** o diretório indicado"). As cinco formas de `ESCRITA_CONTIDA_PASSA`
    (`ruff --fix`, `ruff format`, `eslint --fix`, `pytest --basetemp`,
    `mypy --cache-dir`) são admitidas **porque** se declara que ficam contidas; com
    `${HOME}`/`$USERPROFILE`/`$TEMP` a contenção não existe. Alvo alcançável inclui
    `${HOME}/.claude`, onde vivem `checkpoint-auditor.md` e `readonly_bash.py` —
    os artefatos que o `PHASE_0_CHECKLIST.md:12` coloca em escopo de usuário
    justamente para que o commit auditado não possa enfraquecer o auditor.
    Não executei a forma destrutiva; reporto a admissão medida, não o dano.
    É a mesma classe e a mesma consequência do B1 da 17ª auditoria, que foi BLOCKER.

### HIGH

- [H1] O harness afirma cobertura que não tem: "0 escritas não bloqueadas" com uma escrita admitida
  Arquivo: `scripts/phase0_negative_tests.py:405-423` (`ESCRITA_NAO_BLOQUEADA`, declarada
           "VAZIA desde 2026-08-14, e isso é resultado, não omissão")
  Requisito violado: `PHASE_0_CHECKLIST.md` §DoD item 4(c); §Teste negativo obrigatório
  Evidência: a execução real do harness termina com "0 escritas nao bloqueadas",
    enquanto `pytest --basetemp=${HOME}/...` é admitido (medido em B1). O número que a
    célula da DoD cita como evidência é falso no commit auditado.
  Por que é high: é exatamente o defeito H2 da 1ª auditoria e B2 da 12ª/16ª — harness
    verde com o buraco aberto. Enquanto o número for tratado como prova, a rodada
    seguinte herda a falsa confiança.

- [H2] Falso bloqueio novo e não declarado: caminho absoluto DENTRO do worktree de auditoria
  Arquivo: `user-scope/hooks/readonly_bash.py:154-159`
  Requisito violado: `PHASE_0_CHECKLIST.md` §DoD item 4(e) — "Falso bloqueio novo é finding"
  Evidência: `cat /c/Projetos/aurora-cyber-range/.aurora-worktrees/audit/tools/README.md`
    → rc=2, mensagem "caminho absoluto: **alvo fora do worktree de auditoria**".
    O alvo está DENTRO do worktree de auditoria; a mensagem afirma o contrário.
    Meu primeiro comando da sessão foi bloqueado assim.
    A lista `FALSOS_BLOQUEIOS_CONHECIDOS:197-215` declara apenas leitura/listagem
    **fora** do worktree (`cat ../../README_FIRST.md`, `ls ../..`). Leitura por
    caminho absoluto do próprio objeto da auditoria não está declarada.
  Por que é high: a regra é cega ao prefixo do worktree, então nega por grafia e não
    por alvo — a mesma inversão que o B1 acusa. Empurra a auditoria para inferência,
    que é a degradação classificada HIGH no H4 da 1ª rodada.

### MEDIUM

- [M1] Aspas escapadas em payload caem para "substituição" e bloqueiam o smoke test da própria DoD
  Arquivo: `user-scope/hooks/readonly_bash.py:325-326` (`_substituicao_ou_subshell`,
           `if re.search(r"\\['\"]", cmd): return True`)
  Requisito violado: `PHASE_0_CHECKLIST.md` §DoD item 4(e)
  Evidência: o smoke test do invariante 2 (literal de flag), cujo payload JSON exige
    `\"academus.enrollment_offline\"`, retorna rc=2 com "substituicao de comando ou
    subshell" — sem haver parêntese nem crase no comando. Modo não presente na lista
    de defeitos afirmados.
  Por que é medium: falha para o lado fechado, mas impede verificar por execução um
    dos três invariantes do item 3 da DoD (ver §O QUE EU NÃO CONSEGUI VERIFICAR).

### LOW

- [L1] Deny de secrets não cobre `Write`
  Arquivo: `.claude/settings.json:6-14`
  Requisito violado: `CLAUDE.md` §Secrets
  Evidência: as oito regras cobrem `Read(...)` e `Edit(...)`; não há `Write(...)`.
  Nota: pendência já registrada e afirmada (P13/P19), reconfirmada aqui.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis verificadores liberam árvore limpa | ✅ PASS | Executados um a um: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` — todos rc=0 |
| 2. Os seis detectam as violações externas do harness | ✅ PASS | 13 probes de verificador, todos "OK ... (rc=1)" com citação do arquivo plantado |
| 3. Hook bloqueia import `domains/`, edição `docs/spec/`, literal de flag | ⚠️ PARCIAL | import `domains/` → exit=2 (medido); edição `docs/spec/` → exit=2 (medido); literal de flag → **não executável** (M1), regra lida em `check_architecture.py:46` |
| 4. Separação de papéis do auditor | ⛔ FAIL | ver B1, H1, H2 |
| 4(a) sem `Write`/`Edit` nas tools | ✅ PASS | `user-scope/agents/checkpoint-auditor.md:5` — `tools: Read, Grep, Glob, Bash` |
| 4(b) bloqueia as formas enumeradas | ✅ PASS | 36 escritas deliberadas bloqueadas na execução do harness |
| 4(c) nenhuma grafia do alvo escapa | ⛔ FAIL | B1 — `${HOME}`, `$USERPROFILE`, `$TEMP` escapam |
| 4(d) nenhuma escrita não declarada passa | ⛔ FAIL | B1 — `pytest --basetemp=${HOME}/...` admitido |
| 4(e) libera leitura; falso bloqueio novo é finding | ⛔ FAIL | H2 e M1 |
| 5. Hook do scenario-designer: Write/Edit e Bash | ✅ PASS | `scenario_scope.py` → exit=2 em `range-core/nope.py`; `scenario_bash.py` → exit=2 em `git log --oneline` |
| 6. `ground_truth.yaml` e `GM_NOTES.md` fora do `.gitignore` | ✅ PASS | `.gitignore:39` comentário explícito; nenhuma regra os ignora |
| 7. `.env`/secrets negados em `settings.json` | ⚠️ PARCIAL | 8 regras `Read`/`Edit`; sem `Write` (L1) |
| 8. Auto Mode desabilitado | ✅ PASS | `.claude/settings.json:5` `"disableAutoMode": "disable"` |
| 9. Push de `main`: `arquitetura` e `seguranca` verdes, `spec_freeze` PR-only | ❔ NÃO VERIFICÁVEL | workflow confirma `if: github.event_name == 'pull_request'` em `spec_freeze`; execução real do CI fora do meu alcance |
| 10. PR descartável: `spec_freeze` falha com spec+código juntos | ❔ NÃO VERIFICÁVEL | atestação do operador |
| 11. PR descartável: spec-only exige título `spec-change:` | ❔ NÃO VERIFICÁVEL | atestação do operador |
| 12. Branch protection em `main` | ❔ NÃO VERIFICÁVEL | atestação do operador |
| 13. `spec-v1.0` criada depois de CI + branch protection | ⚠️ PARCIAL | `git tag` → `spec-v1.0` existe; a **ordem** não é verificável por artefato |
| 14. `/doctor` sem apontamentos | ❔ NÃO VERIFICÁVEL | fora do alcance de ferramenta |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`GRAFIAS_DE_ALVO:361-367` — o eixo do alvo é certificado por grafia, não por propriedade.**
   A entrada "variavel de ambiente" é uma só: `"$HOME/projeto/CLAUDE.md"`. As 14 formas
   de `ESCRITA_POR_ALVO` são multiplicadas por essa única grafia, produzindo 70 provas
   que dizem "`$HOME` está negado" e são lidas como "a variável de ambiente está negada".
   `${HOME}`, `$USERPROFILE` e `$TEMP` — medidos — passam. É textualmente o defeito que
   o comentário em `:283-295` diz estar corrigindo ("um probe que só cobre a grafia
   lembrada não prova ausência das grafias esquecidas"), cometido dentro da correção.

2. **`ESCRITA_CONTIDA_PASSA:374-380` prova contenção apenas para o alvo que ele escolhe.**
   `pytest --basetemp=tmp --version` passa e é contido; disso não decorre que
   `pytest --basetemp=<qualquer grafia>` seja contido. A direção "deve passar" está
   correta em desenho, mas é usada como evidência de contenção que ela não estabelece.

3. **A linha de resultado do harness é asserção sobre si mesmo, não sobre o hook.**
   "0 escritas nao bloqueadas" é a contagem de `ESCRITA_NAO_BLOQUEADA`, que é uma lista
   escrita à mão e hoje vazia. O número mede a memória de quem a escreveu, não o
   comportamento do hook — e diverge dele neste commit (H1).

Observação de método, não finding: `main` e `HEAD` são o mesmo SHA
(`9cbdb9986382bef63428a767794e82fd623a889e`), então não houve diff contra `main` para
auditar. Auditei a árvore inteira do commit fixado. O commit não toca `docs/spec/`
(`git show --stat`: 4 arquivos, nenhum sob `docs/spec/`), então não há violação de
`spec_freeze` a reportar apesar de ter entrado direto em `main`.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Invariante 2 (literal de flag) do hook `check_architecture.py`, por execução.** O
  payload exige aspas escapadas e o meu próprio hook as trata como substituição (M1).
  Li a regra em `check_architecture.py:40-52` e ela é plausível, mas leitura de código
  não é medição — é exatamente a degradação que este relatório acusa em H2.
- **Itens 9 a 12 da DoD** (CI verde no primeiro push, os dois PRs descartáveis, branch
  protection com `enforce_admins`). Dependem do estado do GitHub, fora do worktree.
  Permanecem atestação do operador, como as rodadas anteriores registraram.
- **Item 13, a ordem.** Confirmei que a tag `spec-v1.0` existe; não consegui estabelecer
  por artefato que ela foi criada **depois** do CI verde e da branch protection.
- **Item 14 (`/doctor`).** Sem ferramenta para executá-lo.
- **O efeito destrutivo de `pytest --basetemp=${HOME}/...`.** Medi que o hook **admite**
  o comando; deliberadamente não o executei contra alvo real. A semântica destrutiva de
  `--basetemp` é citada da medição do próprio projeto (`fase_0.md:192`), não da minha.
- **Se existem outras grafias além das que testei.** Encontrei três não negadas em uma
  rodada. Não afirmo que sejam as únicas — é precisamente o argumento do §"O passo (a)
  do P35" em `fase_0.md:265-287`: por casamento textual, completude não é demonstrável.
  O que digo é mais fraco e suficiente para o veredito: a propriedade que a DoD exige
  em 4(c) está **refutada** neste commit.
```

**Resumo em uma linha:** FAIL — a correção da 17ª auditoria trocou "policiar capacidade" por "policiar alvo", o que estava certo, mas implementou o alvo negando `~` e `$HOME` e nada mais; `${HOME}`, `$USERPROFILE` e `$TEMP` alcançam fora do worktree e readmitem as cinco formas de escrita declaradas contidas.
