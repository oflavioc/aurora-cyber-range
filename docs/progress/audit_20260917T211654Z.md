# Auditoria de checkpoint - 2026-09-17T21:16:54.797328+00:00

- fase: **8**
- commit auditado: `564de29faafb78d98aea4284bbe0735ff2ced373`
- session_id: `d451b5ba-5f2a-461b-be57-45327f7be4ed`
- modo: `headless`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have everything: full suite executed green (949/949, zero skips, rc=0), the three launcher proofs bind to this tree, and the verifier battery is green except one — which blocks merge.

# AUDITORIA — FASE 8

## VEREDITO: FAIL

---

## FINDINGS

### BLOCKER

- **[B1] O gate de CI `check_readme_atual.py` está VERMELHO no commit candidato — terceira ocorrência da mesma classe dentro desta fase**

  **Arquivo:** `README.md:123`, `README.md:190`, `README.md:252`; gate em `scripts/check_readme_atual.py`, exigido por `.github/workflows/invariants.yml:765` e `:768`
  **Requisito violado:** `.github/workflows/invariants.yml:762-763` — *"`contratos` ja e context obrigatorio, entao isto bloqueia merge"*; R10 §1 (nunca deixar gate vermelho) e §3 (contagens pinadas vivem no registro canônico, conferidas por máquina)
  **Evidência (executada nesta árvore):**
  ```
  .aurora-audit/venv/Scripts/python scripts/check_readme_atual.py        → rc=1
    FALHAS: 3
      README.md: `testes` afirma ['936'] e a arvore diz '949'.   (x2)
      README.md: `relatorios` afirma ['49'] e a arvore diz '51'.
  .aurora-audit/venv/Scripts/python scripts/check_readme_atual_probes.py → rc=1
    FALHA: a arvore limpa reprova no verificador principal.
  ```
  Suíte real: `Ran 949 tests in 137.189s` / `OK` / `[exited with code 0]`, **zero pulos**.
  **Por que é blocker:** dois passos obrigatórios do job `contratos` falham; o PR da fase não pode ser mergeado. E a causa é a **própria correção da rodada anterior**: o commit `ead430a` (*"README acompanha a arvore (868 -> 936)"*) fechou o B1 da 2ª rodada contra a árvore daquele momento; em seguida `b8e3b56` acrescentou `tests/test_console_consulta_servico.py` (13 testes → 949) e `52983c2` versionou os dois laudos (49 → 51 relatórios), e a prosa não foi reatualizada. O mesmo mecanismo que quebrou na 2ª rodada (número escrito em prosa, fixado num instante, ultrapassado pelas peças seguintes) quebrou de novo, no PR seguinte ao que o ensinou.
  **Colateral não coberto pelo gate:** `README.md:123` também detalha *"os **68** da Fase 8 … 61 unitários e **7** de integração"* — hoje são 81, sendo 20 de integração; e `docs/progress/fase_8.md:59` afirma *"a suíte completa passa a 936"*. O verificador confere só o total e o número de relatórios, então essas duas linhas ficariam erradas em silêncio mesmo depois de o total ser corrigido.

---

### HIGH

Nenhum.

---

### MEDIUM

> As quatro primeiras são **achados órfãos** da 2ª rodada (`docs/progress/audit_20260917T201919Z.md`): não foram corrigidas **e** não ganharam linha na §6 do `fase_8.md` corrente. Entram como findings desta rodada, por `docs/process/WORKFLOW.md` §auditoria. A única disposta foi a M1 daquela rodada → `P8-2` (`fase_8.md:90,155-171`), corretamente.

- **[M1] Órfão da 2ª rodada (M3) — o registro canônico admite zero execução como verde, e agora em DUAS entradas, incluindo o próprio oráculo que fechou o B2**

  **Arquivo:** `.claude/verify/expected_suites.json:13` (`test_prova_andamento_integracao`, `"pass": [0, 7]`) e `:16` (`test_console_consulta_servico`, `"pass": [0, 13]`)
  **Requisito violado:** R10 §2 — *"SKIP silencioso é FAIL"*; o intervalo declarado é tolerância de ambiente nomeado, não licença para zero
  **Evidência:** `[0, 7]` é a única suíte que liga `academus.lms_session_drop_rate` ao efeito observável pela rota real (item 1); `[0, 13]` é o oráculo de serviço criado **nesta rodada** para fechar o B2 (item 2) — ambas com `min = 0`. O stage `suites` dá verde para as duas tendo executado nada. A 2ª rodada relatou isto sobre a primeira; a correção do B2 replicou o padrão na segunda, e `fase_8.md` §6 não tem linha sobre nenhuma das duas.

- **[M2] Órfão da 2ª rodada (M4) — a entrada de `pytest` na allowlist continua inerte**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:88` (commit `c2df575`) × `pyproject.toml:149`
  **Requisito violado:** R2 §1 (o que se declara corrigido precisa ser executável); P7-12
  **Evidência (executada):** `.aurora-audit/venv/Scripts/python -m pytest --version` → `No module named pytest`, rc=1. `pyproject.toml:149` declara `test = ["httpx2==2.10.0"]` — `pytest` não é dependência do projeto e o venv do lançador não o tem. O `PREFIXO_DO_VENV` foi corretamente acrescentado à regex, mas o comando que ela agora admite não existe em interpretador nenhum deste worktree. P7-12 tem linha na §6 (`fase_8.md:108`) descrevendo a *classe*; o achado específico — a correção da rodada anterior ter destravado um comando impossível — não tem disposição.

- **[M3] Órfão da 2ª rodada (L1) — "143 pulam" não foi revisto, e a fase multiplicou os testes de serviço**

  **Arquivo:** `README.md:123` e `README.md:252`
  **Requisito violado:** R7 §4 / R10 §2 — a declaração do que não executa sem ambiente é parte do contrato de execução (*"o aurora já nomeia os pulos — manter assim, nunca regredir"*)
  **Evidência:** a Fase 8 acrescentou `test_prova_andamento_integracao.py` (7, `@exige_banco`) e `test_console_consulta_servico.py` (13, `@exige_banco` nas três classes, `tests/_academus_banco.py:63`). Sem Postgres, os pulos passam de 143 para ~163; as duas linhas seguem dizendo 143. O gate confere o total, não este número — é exatamente a fresta que a 2ª rodada nomeou.

- **[M4] Órfão da 2ª rodada (L2) — cinco das sete superfícies de `academus-web` seguem sem destino registrado**

  **Arquivo:** `docs/spec/02_DOMAIN_ACADEMUS.md:117-124` × `docs/progress/fase_8.md` §6
  **Requisito violado:** `docs/process/WORKFLOW.md` §auditoria (achado não corrigido nasce com linha na §6)
  **Evidência:** `02` §7 enumera Portal do Aluno, Portal do Professor, Secretaria, Financeiro, AVA simplificado, Modo "Prova em andamento" e Console de investigação. A fase entrega as duas últimas (mais `persona-panel`). **Não é falha de DoD** — `07` §Fase 8 tem quatro itens e nenhum cobra as demais —, mas a fase intitulada *"Web completo"* fecha sem que nenhuma pendência diga quem constrói as cinco restantes.

- **[M5] A tabela de evidência da fase aponta, para o item 2, a suíte de dublê — e omite o oráculo que de fato prova o requisito**

  **Arquivo:** `docs/progress/fase_8.md:64` × `tests/test_console_consulta_servico.py`
  **Requisito violado:** R2 §1 (o `EVIDÊNCIA` nomeia o que foi executado); `07_IMPLEMENTATION_PHASES.md` §Registro de progresso (itens de DoD com status **e evidência**)
  **Evidência:** a linha do item 2 cita `tests/test_console_investigacao.py` **"(16, serviço)"**. Medido: o arquivo não importa `_academus_banco`, não tem `@exige_banco` e não abre conexão (`grep -c "exige_banco" tests/test_console_investigacao.py` → `0`) — roda sem serviço nenhum. E o arquivo que o data-engineer escreveu nesta rodada para fechar o B2, `tests/test_console_consulta_servico.py` (13 testes sobre Postgres real), **não aparece na tabela**. O leitor do registro conclui que o caminho de banco foi exercitado pela suíte errada, e não encontra a que o exercita.

---

### LOW

- **[L1]** `tests/test_console_consulta_servico.py:44` e `:308-309` afirmam, no presente, *"VERMELHO HOJE: a rota devolve 500 (o driver vazou)"* e *"Verde quando o data-engineer tipar o parâmetro como `bool | None`"*. Nesta árvore o parâmetro **já é** `bool | None` (`domains/academus/api/app.py:251`) e o teste é verde. A prosa é o rastro do commit RED (`b8e3b56`, legítimo por R3 §4), mas lida contra o candidato é falsa.
- **[L2]** `docs/progress/fase_8.md:59` — *"a suíte completa passa a 936"*. Medido: 949. Registrado à parte do [B1] porque `check_readme_atual.py` não lê este arquivo: corrigir só o README deixaria esta linha errada sem nada ficar vermelho.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Modo "Prova em andamento" perde sessões conforme `lms_session_drop_rate` | **PASS** | `tests/test_prova_andamento.py` (15, puro — modelo de sobrevivência) + `tests/test_prova_andamento_integracao.py` (7, `@exige_banco`, **executados**: zero pulos na corrida de 949). A cláusula *"conforme a flag"* é provada pela rota real `GET /exam/session-status` com store+cache+fold+Postgres: taxa 0 → tudo `alive`; inject de taxa cheia → tudo `dropped`; taxa intermediária parte o conjunto; e o par negativo (a mesma rota **sem** `ProvaEmAndamento`, flag em 1.0, frame inteiro vivo) dá poder discriminante ao positivo — é o ramo all-ALIVE que mascararia wiring quebrada. Ressalva em [M1] |
| 2 — Console de investigação emite os marcadores automáticos | **PASS** | **Marcador `auto`:** `tests/test_console_investigacao.py::OConsoleSetaOMarcadorAuto` afere pela projeção `range_core.objectives.projecao.project`, que não lê payload, com controle de ausência antes da consulta — oráculo independente. **Filtros de `02` §7:124:** fechados nesta rodada. `repositorio.py:303-316,345-363` monta os fragmentos `WHERE` com coluna vinda de `_FILTRO_PARA_COLUNA` (identificador fixo) e valor por binding (R9 §9), e `app.py:283-291` os repassa. Provado por `tests/test_console_consulta_servico.py` (13, `@exige_banco`, **executados**): oráculo **independente** — o conjunto de 4 linhas que o próprio arquivo semeia na `audit_trail` real, não uma cópia do mapa de produção; cada filtro isolado recorta um subconjunto diferente; interseção de dois eixos; modo agrupado; `result_count` do evento igual à contagem da consulta **filtrada** (2, não 4). Mutantes MS1 (repo que descarta o filtro) e MS2 (repo que troca a coluna) escritos como subclasses do `Repositorio` real e **demonstrados mortos**. O H1 está fechado ponta a ponta: `filter_window` é `bool \| None` na rota (`app.py:251`), no emissor (`emissor.py:86`) e no repositório (`repositorio.py:322`); a tela virou `<select>` tri-estado (`investigation-console/main.tsx:121-145,170-174`); e `test_filter_window_nao_booleano_e_recusado_sem_500` fixa 422 sem rastro no store. Ressalva em [M5] |
| 3 — Cada persona vê apenas sua camada `reported` | **PASS** | `range-core/participant/reported.py:83-87` é conjunção de duas pernas — `truth_layer ∈ REPORTAVEIS` **E** `evento.persona == persona` —, whitelist (`:69`) e não blocklist, cobrindo por complemento os 5 valores de `truth_layer` do catálogo. `tests/test_reported_isolation.py` (10) afere **sobre o payload da API** (`GET /participant/view`), não sobre UI: eventos de `ground_truth`, `evaluator_assessment` e `facilitation` são plantados **marcados para a persona que lê** (adversarial), e a asserção é `assertNotIn` sobre o corpo inteiro. Persona vem do token (`app.py:353`), nunca de parâmetro; 401 sem token, 403 persona não admitida |
| 4 — As sete ações de continuidade aplicam efeito mecânico e custo | **PASS** | `domains/academus/continuidade.py:57-86` cobre as sete, conferidas contra o **enum fechado do contrato lido do YAML** (`events.schema.yaml:686-694`) — oráculo independente, não lista reescrita. Custos idênticos à coluna "Custo" de `02` §9:150-156, um a um. Efeito provado pelo **estado reconstruído** (`simulation_state.py:477-487`, fold genérico lendo `payload["effects"]`), incluindo o caso de valor `False` (`emergency_document_issuance`). INV-4 preservado: a tabela de domínio entra por injeção em `montar()` (`participant/api/app.py:383-386`) — nenhum nome de flag chega ao core por import. `tests/test_continuidade.py` (20): negativos 422/403/401, cada um sem emitir |

**Invariantes e verificadores — todos executados nesta árvore, todos rc=0 exceto o do [B1]:** `check_core_boundary` · `check_contract_literals` · `check_event_envelope` · `codegen.py --check` · `check_security_constraints` · `check_synthetic_data` · `check_core_contract_imports` · `check_gabarito_fora_do_git` · `check_banner_de_simulacao` (6 telas) · `check_web_sem_derivacao` (22 arquivos, 14 métodos proibidos) · `check_api_surface` (37 rotas, 35 implementadas) · `check_telas_sem_vocabulario` (23 arquivos, 55 termos) · `check_store_read_surface` · `check_insumo_de_metrica` · `check_fold_authority` · `check_spec_flags` · `check_gate_coverage` · `check_allowlist_do_auditor` · `check_trilha_de_auditoria` · `check_pinned_images` · `check_secoes_de_seguranca` · `check_regras_do_linter` · `check_fabrica_liga_emissor` · `check_hooks_com_emissor` · `check_contract_examples` (13 pos / 95 neg) · `check_spec_examples` · `check_volumes_da_linha_b` · `check_progress_consistency` + probes (12 eixos) · **`check_readme_atual` rc=1 e `check_readme_atual_probes` rc=1 → [B1]**.
**Provas do lançador (as três amarradas à árvore `eb3226ce1366`, idêntica à deste checkout):** `check_provas_de_container` rc=0 (demo ponta a ponta + reinício de container nos dois casos de T5) · `check_prova_do_seed` rc=0 (3.543.783 linhas; 172,3 s e 169,3 s contra orçamento de 300 s; 20 tabelas com SHA-256 igual) · `check_prova_do_exercicio_4h` rc=0 (650 eventos, 0,035 s contra 3 s, dryrun 4 caminhos; modo declarado — o pack não está neste checkout).
**Fronteiras de processo:** `git diff --stat <base>..<candidato> -- docs/spec/` → **vazio**; `spec_freeze` respeitado. Âncora em `docs/process/phase_anchors.tsv:50` = `c38f890a…`, idêntica à base declarada pelo lançador. `pins.json._meta.gerado_de_head = 52983c2` = `HEAD~1`, e `564de29` altera **apenas** `pins.json` (que se auto-exclui) — consistente. `git status --porcelain` vazio ao fim de tudo (R7 §3).

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_console_investigacao.py::MatrizDeAplicacaoDeFiltro`** (linhas ~502-574) — a classe continua **auto-contida**: compara dois dublês definidos no próprio arquivo e reproduz à mão o que o handler faz, em vez de exercitá-lo. Permanece intacta desde a 2ª rodada (o único delta no arquivo foi `FILTROS["filter_window"]: "business_hours"` → `True`). **Deixou de ser dano de requisito** — `tests/test_console_consulta_servico.py` agora prova a aplicação sobre o SQL real —, mas segue sendo uma classe que passa sem tocar produção, e o `CAMPO_DA_CONSULTA` do teste (`:163`) segue sendo cópia de `_FILTRO_PARA_COLUNA` (`repositorio.py:303`).
2. **`docs/progress/fase_8.md:64`** — o rótulo "(16, serviço)" atribui a `test_console_investigacao.py` uma execução contra banco que ele não faz. Não é um teste que não prova; é um **registro que afirma prova inexistente**, e por isso está como [M5].

Fora esses, **nenhum outro encontrado**. Os gates dos quatro itens têm oráculo independente da implementação, par negativo e asserção sobre payload/estado real; nenhum `skipTest`, `@unittest.skip` ou `expectedFailure` nos sete arquivos de teste da fase (grep, saída vazia); a suíte completa rodou com **zero pulos**.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **O stage `baseline` (R8, pins × HEAD).** `.aurora-audit/venv/Scripts/python .claude/verify/check_baseline.py` é **bloqueado pela allowlist** do auditor (ela admite `python tools/` e `python scripts/`, não `python .claude/verify/`). Verifiquei só estruturalmente (`gerado_de_head` = `HEAD~1`, e o commit seguinte toca apenas `pins.json`); **não conferi hash nenhum**. Idem `check_boundary.py`, `check_state.py`, `check_tdd.py`, `run.sh` e `compliance-audit.sh` — o hook do `UserPromptSubmit` avisa a cada prompt que nenhum pipeline verde está registrado nesta máquina, e não pude resolvê-lo.
- **As sete entradas de `expected_suites.json` individualmente.** Os `cmd` usam `-p "<padrão>"`, forma que a allowlist não admite (a entrada de `unittest` fecha em `tests` com `$`). As contagens declaradas (15/16/10/20/10/[0,7]/[0,13]) batem com minha contagem estática de métodos, mas não as executei isoladamente — só dentro da corrida completa.
- **O número "143 pulam" do [M3].** Não consegui medir a suíte **sem** Postgres: a allowlist não admite manipular variável de ambiente nem código ad-hoc. Afirmo apenas o que é estático — a fase acrescentou 20 testes sob `@exige_banco` e o número não mudou —, não o valor correto.
- **As três provas do lançador, quanto à execução.** Não vi nenhuma rodar. Vale integralmente a limitação P7-2 declarada no enunciado: a prova amarra a **árvore rastreada**, não o commit, e `scenarios/` não entra no hash. A de 4 h vale no **modo declarado** (o pack não está neste worktree), com a segunda perna sendo os SHA-256 dos sete arquivos.
- **`npm run typecheck` / `npm run build` das três telas novas.** Não há `node_modules` neste worktree. A prova de container mostra `RUN npm run build` completando com as **seis** telas construídas (`exam-mode`, `investigation-console` e `persona-panel` inclusive, com tamanho e tempo por bundle), o que é evidência forte de que a árvore compila — mas eu não vi o build rodar, e os gates de tela que executei (`check_banner_de_simulacao`, `check_web_sem_derivacao`, `check_telas_sem_vocabulario`) são estáticos sobre fonte.
- **Comportamento em navegador** das três telas. Li o fonte; o `<select>` tri-estado do filtro "janela" está correto por leitura, e o backend recusa o não-booleano com 422 por execução — a junta real UI→rota não foi exercitada por mim.
- **A campanha de mutantes re-executada após o green (R3 §5).** Os mutantes MS1/MS2 de `test_console_consulta_servico.py` **são** executados pela suíte e os vi passar; os das demais suítes estão descritos em prosa nos cabeçalhos, e não verifiquei se a campanha inteira foi reexecutada depois das correções desta rodada.
- **Regressão contra a base.** Não rodei a suíte em `c38f890`. Para o [B1] não foi necessário: a cadeia é legível no diff (`git show c38f890:README.md` declara 858; `ead430a` põe 936; `b8e3b56` acrescenta 13 testes; `52983c2` acrescenta 2 relatórios), e é suficiente para atribuir a causa ao código da fase, não ao ambiente.
- **`docs/progress/fase_8.md` §7 (fechamento)** não existe, por desenho: é redigida por quem implementou, após o veredito. Não a avaliei.

---

**Um BLOCKER ⇒ FAIL**, sem ponderação. O conteúdo técnico da fase está, nesta rodada, **provado**: os dois BLOCKERs anteriores foram fechados por correção de substância, não por afrouxamento de asserção — o oráculo de serviço sobre o SQL real é exatamente a forma que faltava (semeia o próprio conjunto, afere identidade e não contagem, e mata dois mutantes escritos contra ele), e o `filter_window` booleano foi levado da rota à tela. Os quatro itens de DoD passam. O que reprova é o mesmo gate de bookkeeping que reprovou na rodada anterior, quebrado pela própria correção daquele gate: o número foi escrito em prosa contra um instante, e as peças seguintes o ultrapassaram no mesmo PR. E quatro achados M/L da 2ª rodada atravessaram o ciclo sem correção e sem linha na §6 — que é precisamente a acumulação silenciosa que a P7-6 existe para impedir.
