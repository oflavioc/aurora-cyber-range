# Auditoria de checkpoint - 2026-08-16T18:46:52.005194+00:00

- fase: **3**
- commit auditado: `a857d7687e4da171ae7fa809d2da87f7e0688888`
- session_id: `a316297d-84cb-4d10-82f5-ff547859c18a`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 3

## VEREDITO: PASS

Nenhum BLOCKER. Os quatro itens da Definition of Done da Fase 3 (`07_IMPLEMENTATION_PHASES.md` §Fase 3) estão provados por teste executável que eu **executei**, não por leitura. Suíte: `python -m unittest discover -s tests` → **211 testes, 0 falhas, 0 pulos** (os serviços estavam ativos, conforme o lançador; nenhum `skip` apareceu). Os seis verificadores de `tools/`, os oito verificadores de `scripts/` da fase, as respectivas provas negativas e o `demo_fase2.py` rodaram limpos.

---

## FINDINGS

### BLOCKER

Nenhum.

### HIGH

- **[H1] A base de comparação entregue ao auditor é o próprio commit candidato — o diff exigido pelo procedimento é vazio, e nada avisa**
  Arquivo: `scripts/start_checkpoint_audit.sh:63-79` (resolução da base); `docs/process/WORKFLOW.md:29-38` e `:49`
  Requisito violado: `docs/process/WORKFLOW.md` §"Ciclo por fase" (auditoria **antes** de `gh pr create`) e §"Por que o auditor formal usa launcher de worktree" — objetivo declarado *"comparação reproduzível contra `main`"*; `CLAUDE.md` §"Fluxo por fase".
  Evidência observada:
  - `git rev-parse HEAD` = `a857d768…`; `git rev-parse origin/main` = `a857d768…`; `git rev-parse main` = `a857d768…`. O prompt do lançador informa como base `a857d768…`, isto é, **BASE_SHA == HEAD_SHA**, e `git diff <base>..HEAD` é vazio por construção.
  - `git for-each-ref` mostra que os ramos da fase existem em SHAs distintos do candidato (`refs/heads/fase-3-fechamento c611ae1`, `refs/remotes/origin/fase-3-api-minima a3b1ad8`), enquanto `refs/heads/main` e `refs/remotes/origin/main` apontam para o candidato: a fase já está em `main` no instante da auditoria.
  - O lançador valida apenas que a base **resolve** (`:76-79`). Não há comparação com `HEAD_SHA`, nem aviso, nem recusa.
  Por que é HIGH: o item 6 da entrada obrigatória do auditor ("o diff do commit candidato contra `main`") vira vacuidade, e a instrução do prompt proíbe explicitamente o auditor de resolver outra base — o auditor é empurrado a improvisar em silêncio (eu improvisei: usei `725f51b`, o fechamento da Fase 2, como base do conjunto de mudanças). É a mesma classe que a P2-16 se propôs a fechar neste mesmo commit — quem fixa a base é o lançador —, com o caso degenerado não coberto. Consequência concreta adicional: rodando sobre `main` já mergeado, qualquer BLOCKER emitido aqui já estaria na branch default. Não é BLOCKER porque não é desvio de item de DoD nem de `docs/spec/`, mas é defeito de um artefato entregue por esta fase (`scripts/start_checkpoint_audit.sh`, +120 linhas no diff).

### MEDIUM

- **[M1] Business state da `academus-api` vive em dicionários de módulo, sem SQLAlchemy e sem Postgres**
  Arquivo: `domains/academus/models/registros.py:75-92`; escrita em `domains/academus/api/repositorio.py:98-117` (`NOTAS.append`, `MATRICULAS.append`)
  Requisito violado: `02_DOMAIN_ACADEMUS.md` §7 — *"**academus-api** — FastAPI + SQLAlchemy"*; `01_ARCHITECTURE.md` §4, tabela de camadas — *Business State … Persistência: **Postgres***.
  Evidência: as três entidades e `Matricula` são `dataclass` em memória; `POST /turmas/{id}/notas` e `POST /matricula` acrescentam a listas globais do processo. Não há modelo SQLAlchemy, migration ou tabela para elas (o `alembic upgrade head` da stack de auditoria cria apenas o event store da Fase 2).
  Por que é MEDIUM e não BLOCKER: nenhum item da DoD da Fase 3 exige persistência, e o corte está **declarado e datado** em `domains/academus/models/registros.py:18-32` e em `docs/progress/fase_3.md` §4.4, com destino na Fase 5. Fica registrado porque a divergência é contra texto normativo (`02` §7 não é condicionado a fase) e porque a Fase 4 exige reinício restaurando estado — o que não vale para nada escrito por estes dois endpoints.

- **[M2] A ordem `autoriza` → `degrada`, que é o argumento de segurança da degradação, não tem teste**
  Arquivo: `domains/academus/api/app.py:69-73` (`dependencies=[Depends(autoriza), Depends(degrada)]`); justificativa em `app.py:17-21` e `domains/academus/api/degradacao.py:158-165`
  Requisito violado: `05_SECURITY_REQUIREMENTS.md` §8 (*"nenhum serviço exposto sem autenticação"*) na leitura que o próprio código declara — *"degradar antes de autenticar entregaria o estado da simulação a quem nem token tem"* —, e `00_MASTER_SPEC.md` §3, consequência normativa 3 (assimetria de informação é desenho).
  Evidência: varredura de `tests/test_api_*.py` por `401` retorna apenas `test_sem_token_e_401_com_www_authenticate` e `test_token_de_outra_chave_e_401_e_nao_403` (`tests/test_api_rbac.py:100,105`), ambos sobre `GET /alunos/{id}`, que **não tem degradação declarada** (`domains/academus/api_surface.yaml:230-237`, `flags: []`). Nenhum teste faz requisição sem token a `/matricula`, `/turmas/{id}/notas` ou `/turmas/{id}/diario` com a flag ligada. A propriedade hoje vale — `autoriza` levanta antes de `degrada` ser resolvida —, mas repousa exclusivamente na ordem interna de resolução de dependências do FastAPI, que nenhum teste nem verificador fixa.
  Por que é MEDIUM: uma inversão futura (ou uma rota marcada `publica: true` com degradação declarada) devolveria 503/latência a chamador não autenticado, expondo estado de simulação pela rede, e a suíte permaneceria verde. É exatamente a classe "verificação que parece existir" que a §7 do próprio registro desta fase nomeia.

### LOW

- **[L1] `POST /turmas/{turma_id}/notas` grava nota sem qualquer registro de auditoria**
  Arquivo: `domains/academus/api/repositorio.py:98-103`; `domains/academus/api/app.py:107-118`
  Requisito: `02_DOMAIN_ACADEMUS.md` §4.1 — *"Alteração de nota — nota anterior, nova nota, usuário, IP, user-agent, timestamp duplo, semestre, disciplina, `within_window`, `authorization_id`"*.
  Evidência: o handler cria `Nota` e retorna; não há `audit_trail`, `within_window` nem `authorization_id` em lugar nenhum do adapter.
  Nota: `07_IMPLEMENTATION_PHASES.md` Fase 3 põe *"auditoria com hash"* nos NON-GOALS e a Fase 5 a cobra por DoD e por `06` T7 — portanto **não é defeito desta fase**. Registrado para que a Fase 5 encontre o endpoint que já escreve nota antes de a trilha existir.

- **[L2] Citação de spec errada em comentário normativo**
  Arquivo: `domains/academus/models/registros.py:36` — *"DADOS SINTETICOS … `05` §7"*.
  Requisito: a seção de dados sintéticos é `05_SECURITY_REQUIREMENTS.md` §3; a §7 é *"Integridade da trilha de auditoria"*.
  Por que registro: a §7.1 do registro desta fase estabelece que *"afirmação sobre a spec dentro de um registro é citação, não paráfrase"*; a mesma disciplina vale para o comentário que justifica o dado.

- **[L3] `autenticacao_do_ambiente` não é exercida por nenhum caminho**
  Arquivo: `domains/academus/api/auth.py:118-123`
  Evidência: varredura no repositório inteiro por `autenticacao_do_ambiente` retorna apenas a definição — nenhum chamador, nenhum teste. O caminho "segredo vem do ambiente e o boot recusa na ausência" é provado só no nível de `range_core.api.tokens.jwt_secret` (`tests/test_api_tokens.py:44-79`), não no nível do adapter.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `academus.enrollment_offline: true` faz o endpoint de matrícula retornar 503 | **PASS** | `tests/test_api_degradacao.py:177` executado: 201 antes, 503 depois de um `inject_fired` real; efeito vindo do fold via `current` (`range-core/state/cache.py:135-157`), não de `set` direto. Status 503 declarado em `domains/academus/api_surface.yaml:168` |
| 2 — `academus.grades_readonly: true` bloqueia POST de nota com mensagem de negócio | **PASS** | `tests/test_api_degradacao.py:184` executado: 409 e `assertIn("coordenacao", …)`. Mensagem em `api_surface.yaml:192`; `tests/test_api_degradacao.py:198` prova que a leitura segue 200, coerente com `effect_ui` em `domains/academus/flags.yaml`. `scripts/check_api_surface.py` reprova mensagem que nomeie flag ou mecanismo (probes executados) |
| 3 — RBAC nega acesso cruzado entre perfis | **PASS** | `tests/test_api_rbac.py:76` executado (as duas direções + um caso positivo que discrimina). Teste é de **payload de API** sobre stack ASGI real, na forma de `06` T6. Complementos executados: 403 sem consultar repositório e indistinguibilidade byte a byte (`:121`, `:200`), escopo de objeto `proprio`/`titular` (`:170`, `:181`), escrita também escopada (`:232`), rota não declarada negada em runtime (`:262`) |
| 4 — Nenhuma string solta de flag no código-fonte (verificado por lint) | **PASS** | `python tools/check_contract_literals.py` executado, rc=0. Verificação independente minha: varredura por `"academus.<nome>"` em todos os `*.py` retorna **apenas** `domains/academus/generated/flags.py`. Os nomes de flag na `api_surface.yaml` são dado declarado, conferidos contra `flags.yaml` por `check_api_surface.py` (probe *"rota consumindo flag que o adapter nao declara"* reprova) |
| OUTPUT — degradação real por flag em **pelo menos três** endpoints | **PASS** | `/matricula` (recusa 503), `/turmas/{id}/notas` (recusa 409), `/turmas/{id}/diario` (latência 2,5 s + recusa proporcional). Todos exercitados em `tests/test_api_degradacao.py` |
| OUTPUT — JWT | **PASS** | `tests/test_api_tokens.py` executado: `alg: none` recusado, token sem `exp` recusado, adulteração, outra chave, expiração com instante fixo, placeholder vazio de `.env.example` recusado |
| OUTPUT — três entidades (Aluno, Turma, Nota) | **PASS com ressalva** | `domains/academus/models/registros.py`; ver [M1] quanto à persistência |
| NON-GOALS respeitados (modelo completo, seed em escala, auditoria com hash) | **PASS** | Nenhum modelo SQLAlchemy, nenhum seed, nenhuma trilha; a API não emite evento (`app.py:30-37`) — coerente com `06` T7 (Fase 5) e com a alocação de `audit_query_performed` à Fase 8 |
| `06` T2 (Fases 1–3) — flag não declarada impede boot com mensagem nomeando flag e arquivo | **PASS** | `tests/test_pack_loader.py:275,279,295` na suíte verde |
| `06` T2 — `event_type` fora do catálogo rejeitado; constantes sincronizadas | **PASS** | `tools/check_event_envelope.py`, `tools/codegen.py --check`, `scripts/check_contract_examples.py` (10 positivos, 66 negativos) executados |
| Invariantes 1–4 | **PASS** | `check_core_boundary`, `check_contract_literals`, `check_event_envelope` rc=0; `check_core_contract_imports` + probes rc=0; `check_store_read_surface` + probes rc=0; `check_fold_authority` + probes rc=0 (2 sítios de construção de `SimulationState`, porta sem método que aceite estado pronto) |
| `06` T15 — nenhuma dependência não pinada | **PASS** | `pyproject.toml` e `constraints.txt`: `fastapi==0.141.1`, `pyjwt==2.13.0`, `redis==8.1.0`, `httpx2==2.10.0` (extra `test`), fecho transitivo declarado linha a linha; `scripts/check_pinned_images.py` + probes rc=0 (2 imagens, 3 arquivos, sem divergência de digest) |
| `05` §8 — nenhum serviço sem autenticação | **PASS com ressalva** | `autoriza` é dependência global e nega rota não declarada; `/docs`, `/redoc` e `/openapi.json` desligados e provados 404 (`tests/test_api_degradacao.py:346`). Ressalva em [M2] |
| `05` §1 — nada ofensivo | **PASS** | `tools/check_security_constraints.py` e `tools/check_synthetic_data.py` rc=0; nenhum efeito de ataque fora de estado; criptografia presente é JWT legítimo, permitido por `CLAUDE.md` |

---

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado. Registro duas costuras que examinei e considerei legítimas, para que não passem por não-examinadas:

1. `tests/test_api_degradacao.py:208` afirma a latência por `self.c.dormidas == [2.5]`, isto é, sobre a função `dormir` injetada (`degradacao.py:128`), e não sobre tempo de parede decorrido. **Não é asserção sobre mock**: o valor `2.5` percorre o caminho de produção inteiro — YAML → `Degradacao.segundos` → `Degradador.aplica` — e o default do parâmetro é `asyncio.sleep`. É a mesma costura de tempo já usada no relógio e no token. O limite real é outro e está declarado em `app.py:30-37`: nesta fase nenhum processo monta a aplicação, então o `asyncio.sleep` de produção nunca é exercido.
2. `tests/test_api_degradacao.py:246` monta um segundo `TestClient` sobre o **mesmo objeto `app` de módulo** (`app.py:50`, `montar` muta `app.state`). O teste continua válido porque cada `setUp` religa o estado, mas a aplicação é singleton de módulo — dois wirings não coexistem. Não afeta nenhuma conclusão desta auditoria.

Também verifiquei que **nenhum** teste da árvore está marcado `skip`/`xfail` sem justificativa: os únicos `skipIf` são os de `AURORA_TEST_DATABASE_URL`/`AURORA_TEST_REDIS_URL` (`tests/test_event_store_postgres.py`, `tests/test_projection_cache.py:231`), e com a stack efêmera no ar **nenhum deles pulou** — 211 testes executados, zero pulos.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **O diff do commit candidato contra `main`.** A base entregue pelo lançador é o próprio candidato (ver [H1]). Auditei o conjunto de mudanças `725f51b..HEAD` (fechamento da Fase 2 até o candidato), escolhido por mim. Consequência: **não pude avaliar a fronteira real do PR** e, portanto, não pude julgar o gate `spec_freeze` para este PR — o intervalo que revisei contém alterações em `docs/spec/00_MASTER_SPEC.md` e `docs/spec/01_ARCHITECTURE.md` (commits `8c6eca6` e `02383e4`, ambos com título `spec-change:`) **e** código, o que só é legítimo se forem PRs separados. Não tenho como confirmar isso deste worktree.
2. **Procedência do `range_core` executado.** A própria P3-4 (`docs/progress/fase_3.md:826-860`) mede que, no worktree de auditoria, `domains` e `contracts` resolvem pelo worktree e `range_core` resolve pela **árvore principal** (instalação editável com caminho absoluto). Como `main` está no mesmo SHA do candidato, os dois coincidem hoje — mas os 211 testes que executei **não provam** que o núcleo executado veio deste checkout. Não há teste que afirme a raiz de resolução dos três pacotes; o próprio registro declara que não o escreveu de propósito.
3. **CI real.** Li `.github/workflows/invariants.yml` e executei localmente todos os steps que a allowlist permite, mas não observei nenhuma execução do GitHub Actions, nem o estado da branch protection (os quatro required contexts de `docs/process/WORKFLOW.md` §Gates).
4. **Comportamento sob concorrência.** A P3-2 (cache frio sem single-flight) permanece aberta por decisão; a suíte é sequencial e não há servidor, então nada nesta auditoria toca duas leituras simultâneas.
5. **`RedisProjectionCache` em carga e o caminho de boot de produção.** O backend Redis foi exercitado pelos testes contra o serviço efêmero, mas nenhum processo desta fase monta `academus-api` + `RedisProjectionCache` + `PostgresEventStore`; a montagem de produção é da Fase 4 e não pôde ser observada.
6. **Afirmações do registro sobre eventos passados** — por exemplo, que o hook recusou a primeira versão de `tests/test_api_degradacao.py` com literais de flag, ou os 21 comandos conferidos contra a cópia instalada do hook (`docs/progress/fase_3.md:862-876`). Verifiquei o **estado final** (nenhum literal de flag fora dos geradores; a allowlist instalada libera os oito verificadores da fase — eu os executei), não o histórico.
