# Auditoria de checkpoint - 2026-08-14T16:05:02.217052+00:00

- fase: **0**
- commit auditado: `f72f9b135bb22fdd0e484cffa9b9173717778270`
- session_id: `a5d76eb2-21d7-406a-9b8d-87a7636a7735`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have sufficient measured evidence. Report follows.

# AUDITORIA — FASE 0

## VEREDITO: **FAIL**

## FINDINGS

### BLOCKER

**[B1] Substituição de comando entre aspas duplas executa comando arbitrário e contorna a allowlist inteira**

Arquivo: `user-scope/hooks/readonly_bash.py:239-258` (`_parentese_fora_de_aspas`), premissa no docstring em `:242-243`
Requisito violado: `PHASE_0_CHECKLIST.md` §Definition of Done item 4(b), 4(c) e 4(d); `docs/process/WORKFLOW.md:53`

Evidência — medida, não inferida, nesta sessão contra o hook instalado:

| Comando | Resultado |
|---|---|
| `echo $(whoami)` | **BLOQUEADO** (`parentese fora de aspas`) |
| `echo "$(whoami)"` | **EXECUTOU** → `fcosta` |
| `echo "$(find . -maxdepth 0 -type d)"` | **EXECUTOU** → `.` |
| `echo "$(sort --help)"` | **EXECUTOU** |
| `echo "$(find ./__nonexistent_aurora_probe__ -delete 2>/dev/null; echo rc=$?)"` | **PASSOU o hook** → `rc=1` (erro do próprio `find`, não bloqueio) |
| `echo "$(ls /c/Projetos/aurora-cyber-range \| head -5)"` | **EXECUTOU** → listou o **worktree principal** |

A premissa do docstring está errada: *"Toda forma de substituicao e de subshell do bash exige PARENTESE NAO CITADO"*. Em POSIX shell, `$(...)` **é executada dentro de aspas duplas** — só aspas simples a suprimem. `_mascara_de_citacao` mascara os dois tipos de aspas igualmente, então `"$( )"` some antes de `:258` testar `"(" in mascarado`.

Consequência composta e medida: (1) comandos fora da allowlist executam; (2) `find` — retirado da allowlist *precisamente* por `-delete`/`-fprint0` — executa; (3) alvos por caminho absoluto **fora do worktree de auditoria** são alcançáveis. Não executei escrita destrutiva, mas os três elos estão medidos individualmente.

Por que é blocker: o item 4(c) diz textualmente *"Escrita que alcança o worktree principal é finding, não defeito aceito: ela derrota o propósito declarado do hook"*. A regra 4(c) — *"comando que escreve por posição sai da allowlist"* — não contém nada, porque `"$( )"` readmite qualquer comando. As 32 provas de invariante de alvo (8 formas × 4 grafias) são todas contornáveis por uma reescrita mecânica.

---

**[B2] O harness afirma "0 escritas não bloqueadas" — propriedade que ele não tem**

Arquivo: `scripts/phase0_negative_tests.py:298-318` (`SUBSTITUICOES`), `:120,152` (`LEITURA_LEGITIMA`), saída de sumário `:785`
Requisito violado: item 4(c) — *"A superfície aberta é declarada"*

Evidência: as 8 entradas de `SUBSTITUICOES` usam **exclusivamente formas não citadas** (`echo $(...)`, `` echo `...` ``, `cat <(...)`, `ls (echo x)`). Nenhuma envolve a substituição em aspas duplas. O harness imprime `DEFEITOS ABERTOS ... 0 escritas nao bloqueadas` — falso, conforme B1.

A entrada que licenciou o buraco é `LEITURA_LEGITIMA:152` — `("parentese dentro de aspas", 'grep -n "foo(bar)" tools/')`. Ela prova apenas que **parêntese literal** entre aspas é inerte, e o hook generalizou para "parêntese entre aspas é permitido", o que é falso para `$(` e crase em aspas duplas.

Por que é blocker: é exatamente a classe que a 11ª e a 13ª rodadas puniram — harness verde declarando propriedade não medida. Um verde aqui certifica ausência de superfície aberta que existe.

### HIGH

**[H1] Item 4(e): falso bloqueio conhecido nem corrigido nem declarado**

Arquivo: `scripts/phase0_negative_tests.py:187-206` (`FALSOS_BLOQUEIOS_CONHECIDOS`); registro em `docs/progress/fase_0.md:1060`
Requisito violado: item 4(e) — *"Falsos bloqueios conhecidos estão listados em `scripts/phase0_negative_tests.py` como defeito afirmado"*

Evidência medida:
- `git log --oneline -1 2>&1` → **BLOQUEADO**, `Segmento: 1`. O hook parte em `&` (separador de background) e `2>&1` vira o segmento `1`, não allowlistado. Controle: `2>/dev/null` passa.
- `grep -n "ground_truth\|GM_NOTES\|\.env" .gitignore` → **BLOQUEADO** como *"leitura de secret por caminho de shell"*. Controle sem o token `.env`: rc=0. O alvo real era `.gitignore`, e isso impede verificar o item 7 da DoD por Bash.
- `2>&1` não aparece em lugar nenhum do harness (busca sem resultado).

`fase_0.md:1060` — linha **adicionada neste commit** — nomeia quatro candidatos a defeito afirmado de 4(e): `merge-base`, `2>&1`, `for-each-ref`/`sort`. Três foram corrigidos e migraram para `LEITURA_LEGITIMA` (`:125`, `:126`, `:136`). O quarto, `2>&1`, foi **silenciosamente descartado** — nem corrigido nem listado. É literalmente o modo de falha que 4(e) nomeia: *"defeito documentado que sai do harness vira defeito esquecido"*.

**[H2] O "Limite declarado" apoia-se em atestação apresentada como demonstração**

Arquivo: `docs/process/PHASE_0_CHECKLIST.md:166-169`
Requisito violado: `00_MASTER_SPEC.md` §3 (quatro verdades) e §5.7; `CLAUDE.md` §"Quatro camadas de verdade"

Evidência: o texto novo afirma que a integridade repousa em branch protection, `spec_freeze` e nos seis verificadores, *"e todos demonstrados funcionando nos itens 9 a 13"*. Mas `docs/progress/fase_0.md:668-672` marca os itens 9 a 13 como *"✅ **por ATESTAÇÃO DO OPERADOR** — não verificado por auditoria"*, e `:681` diz explicitamente: *"Isto é a terceira camada das quatro (declaração), não a segunda... Registrar atestação como se fosse verificação seria exatamente o que o CLAUDE.md proíbe."*

Por que é HIGH: é o argumento de controle compensatório que sustenta a aceitação da incompletude do hook. Com B1 provando que o hook não contém, o controle compensatório carrega o item — e ele está afirmado na terceira camada de verdade dentro do próprio documento normativo da DoD.

### MEDIUM

**[M1] O livro-razão de pendências não fecha, no commit que declara corrigi-lo**
Arquivo: `docs/progress/fase_0.md:1638`
Evidência: diz *"São **vinte e sete**"* e enumera P1–P4 (4) + P8,P9,P11,P13–P17,P19–P25 (15) + P28–P31 (4) + P36–P38 (3) = **26**. Além disso **P34 está marcada `Status: ABERTA — decisão pendente do operador`** (`:1380`) e não consta nem da lista de abertas nem da de fechadas (`P5,P6,P7,P10,P12,P18,P26,P27`). A correção de contagem declarada no commit ("dizia dezenove e omitia P28–P31") corrigiu uma omissão e deixou outra.

**[M2] `docs/process/` fora de qualquer gate — a DoD foi reescrita sem CI**
Arquivo: `.github/workflows/invariants.yml:59,110-123`
Evidência: `SPEC` cobre só `docs/spec/`; `CODE` cobre `range-core/ domains/ contracts/ tools/ scripts/ .claude/ .github/ user-scope/ CLAUDE.md .gitignore *.sh`. `docs/process/PHASE_0_CHECKLIST.md` — o documento que **define os critérios de aceitação julgados nesta auditoria** — não está em nenhum dos dois. Este commit alterou a DoD com `SPEC=0`, então nem a regra de separação nem a exigência de título `spec-change:` chegaram a ser avaliadas. Registrado como P37 e adiado por decisão do operador; levantado por três auditorias independentes anteriores (`:378`, `:505`).

### LOW

**[L1] Mensagem do commit candidato cita IDs de pendência que não existem no conteúdo que ela registra**
Evidência: a mensagem de `f72f9b1` diz *"§6 P32 — a reformulacao"*, *"§6 P33 — docs/process/ fora do conjunto CODE"*, *"§6 P34 — separar phase0_negative_tests.py"*. No arquivo entregue esses conteúdos são **P36** (`:1499`), **P37** (`:1573`) e **P38** (`:1585`); P32/P33/P34 são outras pendências (`:1397`, `:1361`, `:1378`). O próprio `fase_0.md:1659` documenta a renumeração como "a quarta confusão de IDs da fase" — mas a mensagem de commit, que é imutável, ficou com os números antigos.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Seis verificadores liberam árvore limpa | ✅ | rc=0 medido nos seis: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` |
| 2. Seis detectam violações externas | ✅ | 13 probes `OK: ... detectou violacao ... (rc=1)`, com exigência de rc==1 + citação do arquivo plantado (`:67-100`) |
| 3. Hook bloqueia import `domains/`, edição de `docs/spec/`, literal de flag | ⚠️ parcial | Invariante 1 medida: `check_architecture.py` → `exit=2`. As metades `docs/spec/` e literal de flag não foram exercitadas por mim |
| 4(a) Auditor não declara Write/Edit | ✅ | `user-scope/agents/checkpoint-auditor.md:4` → `tools: Read, Grep, Glob, Bash` |
| 4(b) Bloqueia formas enumeradas, nas duas direções | ❌ | B1 — allowlist contornável por `"$( )"` |
| 4(c) Superfície aberta declarada; nenhuma grafia escapa | ❌ | B1/B2 — harness declara 0 escritas não bloqueadas; alcance ao worktree principal medido |
| 4(d) Nenhuma escrita não declarada passa | ❌ | B1 — `find`, `sort`, `whoami` executam |
| 4(e) Libera leitura; falso bloqueio conhecido listado | ❌ | H1 — `2>&1` conhecido, não listado; `.env` em padrão de busca é falso bloqueio novo |
| 5. Hook do scenario-designer | ✅ | `scenario_bash.py` → `exit=2` em `git log --oneline`; `scenario_scope.py` → `exit=2` em `range-core/nope.py` |
| 6. `ground_truth.yaml`/`GM_NOTES.md` não no `.gitignore` | ✅ | `.gitignore:39` marca-os como versionados de propósito; só `scenarios/**/evidence/` é ignorado (`:37`) |
| 7. `.env`/secrets negados em `settings.json` | ✅ | `.claude/settings.json:6-14` — deny de `.env`, `.env.local`, `.env.*.local`, `secrets/**` em Read e Edit |
| 8. Auto Mode desabilitado | ✅ | `settings.json:4-5` — `"defaultMode": "default"`, `"disableAutoMode": "disable"` |
| 9–13. Push/CI/PRs descartáveis/branch protection/tag | ⛔ não verificável | Fechados por atestação do operador (`fase_0.md:668-672,679`) |
| 14. `/doctor` sem apontamentos | ⛔ não verificável | Fora do alcance do worktree |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`SUBSTITUICOES` (`scripts/phase0_negative_tests.py:298-318`)** — as 8 entradas testam só a grafia não citada. O eixo declarado no docstring é "o conteúdo de `$(...)` EXECUTA", mas a variante que de fato executa e passa — `"$( )"` — não é exercitada. O harness fica verde afirmando o eixo que não mede. É a quarta recorrência da armadilha que o próprio comentário em `:293-297` descreve.

2. **`LEITURA_LEGITIMA:152` — `("parentese dentro de aspas", 'grep -n "foo(bar)" tools/')`** — prova que parêntese *literal* entre aspas é inerte e é usada para justificar a regra geral "parêntese dentro de aspas é permitido" (`readonly_bash.py:247-249`). A generalização é falsa e é a causa direta de B1.

3. **`FALSOS_BLOQUEIOS_CONHECIDOS` (`:187-206`)** — o mecanismo reprova se um item declarado passar a ser liberado, mas nada no harness detecta falso bloqueio **novo**. `2>&1` e `.env`-em-padrão passaram despercebidos por construção: só é verificado o que já está na lista.

4. **Item 2 e item 4 no mesmo arquivo** — reconhecido no próprio checklist (`:158-162`, P38): um verde não distingue qual metade passou. Confirmado na prática — a suíte ficou verde com 4(b)(c)(d)(e) falhando.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 9 a 13** (push, CI verde, dois PRs descartáveis, branch protection com `enforce_admins`, tag `spec-v1.0`): `gh` não está na allowlist e execução de CI não é observável de dentro do worktree. Permanecem na terceira camada de verdade. Relevante porque H2 mostra que a DoD os invoca como "demonstrados".
- **Item 14** (`/doctor`).
- **Item 3, metades `docs/spec/` e literal de flag**: exercitei apenas a fronteira core/domain.
- **Não executei escrita destrutiva para fechar B1 por demonstração direta.** Os três elos (execução arbitrária, `find`/`sort` executáveis, alcance a caminho absoluto fora do worktree) estão medidos separadamente; a composição está inferida deles. Deliberado: auditor reporta, não muta o repositório principal.
- **Cópia instalada vs. fonte versionada do `readonly_bash.py`**: o harness afirma identidade (`fonte versionada e copia instalada ... identicas`, verde) e meus probes bateram na cópia instalada com o mesmo comportamento da fonte lida. Não comparei os bytes por conta própria.
- **Diff contra `main` é vazio**: `HEAD`, `main` e `merge-base` são todos `f72f9b1`. A manutenção já foi mergeada, então auditei a árvore, não um delta — a ordem de merge do P35 já tinha sido executada quando este worktree foi criado, e o commit candidato descreve `scripts/phase0_negative_tests.py` como tendo 242 linhas em `main` quando a árvore auditada tem 793.
