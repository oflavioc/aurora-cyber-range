# Auditoria de checkpoint - 2026-09-22T20:03:41.698026+00:00

- fase: **9**
- commit auditado: `b4b5f506a2c7e77304e800d384d1ff6850fe13ce`
- session_id: `75bef4e7-cc27-40df-ade3-1ccaf6c62465`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have everything I need. Writing the report.

```text
# AUDITORIA — FASE 9

## VEREDITO: FAIL

Objeto auditado: `b4b5f506a2c7e77304e800d384d1ff6850fe13ce`, árvore
`4dd194d0c7279ea875f5c82cdfdbb89ca5cba1cc`, working tree limpo
(`git status --porcelain` vazio). Âncora `c77120c` presente em
`docs/process/phase_anchors.tsv:9`. Guarda de base: **PORTA**, verbatim do
lançador.

**Nota de entrada, e ela é material.** O contexto enviado pelo implementador
declara *"Commit candidato `3b3e9c2`"*. O objeto fixado neste checkout é
`b4b5f50` — dois commits adiante (`ddd062f`, `b4b5f50`). Por R2 §4, mensagem de
agente não é evidência: o que vale é o que `git rev-parse HEAD` devolve e o que
o lançador fixou. Toda a evidência daquele resumo foi obtida contra OUTRA
árvore, e o B1 abaixo é a consequência medida disso.

## FINDINGS

### BLOCKER

- [B1] A prova do exercício de 4 h NÃO é desta árvore — o item 7 da DoD fica
  sem prova executável neste objeto
  Arquivo: `.aurora-prova-do-exercicio-4h.json:61` (`"tree":
  "cd735acfde76d508cf852d2009699919c9c07aae"`) × `HEAD^{tree}` =
  `4dd194d0c7279ea875f5c82cdfdbb89ca5cba1cc`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §Fase 9 item 7 e
  `06_ACCEPTANCE_TESTS.md` T13, último critério; o mecanismo que os julga é
  `scripts/check_prova_do_exercicio_4h.py`, nomeado como evidência pelo próprio
  registro em `docs/progress/fase_9.md:356`
  Evidência: executei `python scripts/check_prova_do_exercicio_4h.py` neste
  worktree. **rc=1**, com a mensagem do próprio verificador:

      H2 - a prova do exercicio de 4 h e desta arvore e deste pack
        a prova foi gravada sobre a arvore `cd735acfde76d508cf852d2009699919c9c07aae`
        e este checkout e `4dd194d0c7279ea875f5c82cdfdbb89ca5cba1cc`.
        O codigo que mediu e OUTRO — meca de novo.

  A cadeia causal está medida, não suposta. `git log --format="%h %T"` mostra
  que a árvore `cd735acf` é a do commit `3b3e9c2` ("fase-9: pins do fechamento
  da bateria"). `git diff --stat 3b3e9c2 b4b5f50` devolve dois arquivos
  rastreados alterados depois da gravação: `scripts/start_checkpoint_audit.sh`
  (+63) e `.claude/verify/pins.json` (+2/-2). Os dois commits que os trazem
  (`ddd062f`, `b4b5f50`) órfãos a prova.
  Rodei também `python scripts/check_prova_do_exercicio_4h_probes.py`: **12/12
  OK**, 10 venenos e 7 direções, inclusive `(b) arvore divergente`. O
  instrumento está íntegro — ele não está falhando, ele está **acusando**.
  Por que é blocker: os itens 6 e 9 da DoD da Fase 7 e o **item 7 da DoD desta
  fase** se decidem por este artefato, "nunca pela transcrição do registro" —
  é a instrução do lançador e é o desenho declarado em
  `scripts/check_prova_do_exercicio_4h.py:2-24`. Neste objeto, o único gate que
  sustenta o item 7 sai com rc=1. O registro o marca **VERDE**
  (`docs/progress/fase_9.md:356`). Item de DoD declarado feito cujo gate nomeado
  reprova sobre o commit auditado é exatamente o caso que R10 §2 e a regra do
  "não degrada para ok" existem para não aceitar. Aceitá-lo por saber que as
  duas mudanças são do lançador e não do motor seria eu decidir, por inferência,
  que a medição sobreviveria — que é a atestação com um passo intermediário que
  o próprio `06` T3 nomeia e recusa.

### HIGH

- [H1] `range-cli evidence verify` não roda no CI, e a spec diz que roda
  Arquivo: `.github/workflows/invariants.yml:695-698` (só `scenario lint` e
  `scenario dryrun` sobre `tests/fixtures/pack_exemplo`)
  Requisito violado: `04_SCENARIO_SCHEMA.md` §8, linha 245 — *"`lint`, `dryrun`
  e `evidence verify` **rodam no CI**"*
  Evidência: `grep` por `evidence` em `invariants.yml` devolve zero invocação de
  `range-cli evidence`. Executei
  `range-cli evidence verify tests/fixtures/pack_exemplo` → **rc=2**,
  `MANIFEST.json ausente em tests\fixtures\pack_exemplo\evidence`. O pack
  sanitizado existe, é versionado, e o `ground_truth.yaml` dele declara
  `projections: [vpn]` (`tests/fixtures/pack_exemplo/ground_truth.yaml:16`) —
  havia objeto sobre o qual rodar, e nenhum passo de build/verify foi acrescido.
  Por que é high: esta é a fase que cria o verbo, e a norma de `04` §8 passa a
  ser falsa a partir dela sem nada ficar vermelho. É a forma exata do defeito
  que o `spec-change item-8-volume-de-4h` descreve em
  `07_IMPLEMENTATION_PHASES.md:248` — *"o critério seria verificado na fase X e
  passaria a ser falso aqui, sem nada ficar vermelho, que é como um requisito
  morre"*.

- [H2] O texto normativo do banner ganhou uma SEGUNDA fonte viva, e nada cruza
  as duas — o teste que a julga usa a mesma fonte que o produtor
  Arquivo: `range-core/evidence/banner.py:84` lê
  `contracts/evidence.schema.yaml:239` (`banner_text`);
  `scripts/check_banner_de_simulacao.py:114-120` extrai o texto de
  `docs/spec/05_SECURITY_REQUIREMENTS.md:64` e nunca toca o contrato
  Requisito violado: `05_SECURITY_REQUIREMENTS.md` §4 (INV-10 de
  `.claude/rules/product-invariants.md`), e a própria P1-13 que
  `banner.py:10-17` invoca como justificativa
  Evidência: `grep -n banner_text` em toda a árvore devolve quatro consumidores
  (`range_cli/cli.py:405`, `banner.py:84`, `tests/test_evidence_projecao.py:119`
  e o contrato). `grep -n "contract|evidence.schema|banner_text"` em
  `scripts/check_banner_de_simulacao.py` → **No matches found**.
  `scripts/check_contract_examples.py:194-222` cruza contrato × `dados_sinteticos`
  apenas para `allowed_domain_suffixes`. `scripts/check_spec_examples.py` cobre
  só blocos yaml/json (saída executada: "Blocos yaml/json em docs/spec/: 20") e
  o bloco do banner em `05` §4 é texto puro.
  O teste que deveria fechar isso é tautológico:
  `tests/test_evidence_projecao.py:114-120` afirma
  `banner.texto(CONTRATOS) == CONTRATOS["evidence"][...]["banner_text"]`, e
  `banner.texto` é a função que devolve exatamente esse valor. Ele prova que o
  produtor não reescreve; não prova que o valor é o de `05` §4.
  Por que é high: editar `contracts/evidence.schema.yaml:239` muda o banner de
  todo arquivo de evidência entregue ao time azul, a suíte fica verde (1082/1082,
  medido) e `check_banner_de_simulacao.py` fica verde (executado, rc=0) porque só
  olha telas contra a spec. É a P1-13 pela porta que esta fase abriu — o próprio
  `banner.py:14-17` diz que a copiou para evitá-la, e evitou a terceira cópia
  deixando a segunda sem cruzamento.

- [H3] O número que a DoD cita para o item 7 não é o número que a prova carrega
  — e há três números diferentes para a mesma medição
  Arquivo: `docs/progress/fase_9.md:356` diz **1,884 s**;
  `docs/progress/fase_9.md:253` (tabela da §2.8) diz **1,729 s**;
  `.aurora-prova-do-exercicio-4h.json:33` grava
  `"total_s": 2.119512399949599` para os mesmos 96.650 eventos / 400 por minuto
  Requisito violado: R2 §1 (`.claude/rules/evidence.md`) — *"todo PASS cita o
  gate executado, com contagem"* — e `06_ACCEPTANCE_TESTS.md` T12/T13, que
  exigem o número com máquina, data e stack **ao lado** dele
  Evidência: os três valores estão nos três arquivos citados, sobre o mesmo
  volume declarado (96.650 eventos, 96.000 `telemetry_emitted`, 400/min). O
  resumo do implementador enviado nesta sessão traz um quarto enunciado, "2,120 s",
  que é o da prova. A §3 do registro se declara "o insumo do auditor: cada item
  de DoD com status e a prova executável que o sustenta" (`fase_9.md:345-346`),
  e o número dela não é o da prova executável.
  Por que é high: a §2.8 constrói um argumento inteiro sobre margem — *"42% de
  folga"*, *"não muda de veredito entre execuções"* — e a folga real gravada é
  de 29%, não 42%. O argumento de método que decide o item 7 está apoiado num
  número que o artefato não confirma. Não altera o veredito < 3 s; altera a
  sustentação dele, que é o que `06` T3 e a §2.8 colocam no centro.

### MEDIUM

- [M1] Dois registros de adiamento continuam nomeando a Fase 9 como dona
  pendente de itens que esta fase declara VERDE
  Arquivo: `scripts/check_banner_de_simulacao.py:109` —
  `"evidencia": (False, "Fase 9 — 08_EVIDENCE_SIMULATOR.md, comentario na
  primeira linha")`; `scripts/check_secoes_de_seguranca.py:175-189` — §2 com
  `destinatario=(9, ...)` e nota *"O que falta é a varredura do ARQUIVO gerado,
  que a Fase 9 traz"*
  Requisito violado: `05_SECURITY_REQUIREMENTS.md` §2 e §4; itens 5 da DoD da
  Fase 9; e o próprio registro, que em `docs/progress/fase_9.md:591-595`
  reconhece que *"a classe `evidencia` do mesmo gate ... **é** desta fase"*
  Evidência: executei os dois verificadores. `check_banner_de_simulacao.py`
  imprime *"Classes ainda sem artefato, com dono: evidencia (Fase 9 ...)"*;
  `check_secoes_de_seguranca.py` imprime *"§2 Evidências sintéticas:
  evidence.schema.yaml (+ Fase 9)"*. Ambos rc=0 — porque o adiamento ainda está
  declarado, não porque a varredura passou.
  Por que é medium e não high: o mecanismo existe e é testado no produtor
  (`projecao.py:249-255` levanta `IOCEncontrado`; 8 casos de banner). O defeito
  é que os dois registros que existem para impedir que a próxima fase
  redescubra `05` §2/§4 agora **mentem sobre o estado da árvore**, e por
  construção nenhum deles pode ficar vermelho quando o banner ou a guarda de IOC
  regredirem.

- [M2] `telemetry_emitted_payload` é declarado e não é ligado ao `event_type`
  Arquivo: `contracts/events.schema.yaml:745` define o payload;
  o `allOf` de `:238-339` liga cinco payloads por `if/then`
  (`rollback_performed`, `inject_fired`, `assessment_submitted`,
  `bars_score_submitted`, `continuity_action_taken`) e **não** liga
  `telemetry_emitted`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §Fase 9 item 4 e
  `05_SECURITY_REQUIREMENTS.md` §6, na forma que o registro reivindica em
  `docs/progress/fase_9.md:353` — *"o payload valida contra
  `$defs/telemetry_emitted_payload`, com `fact_id` **inexpressável** (`05` §6)"*
  Evidência: `grep -n "_payload"` em `events.schema.yaml` devolve as cinco
  ligações e a definição órfã. O único consumidor é
  `range-core/telemetry/forwarder.py:129` (`erros_de_payload`), chamado apenas
  por `tests/test_telemetry_forwarder.py:192,212,225` — `grep -n` sobre toda a
  árvore `.py` confirma que nenhum caminho de emissão o invoca. `Replay.tick`
  (`forwarder.py:195`) chama `self.emissor(programado.payload)` sem validar.
  Por que é medium: `programar()` nunca escreve `fact_id`, então não há
  vazamento vivo. Mas "inexpressável" é afirmação sobre o **envelope**, e no
  envelope o payload de `telemetry_emitted` continua aberto como os outros 32 —
  a asserção do registro vale para quem chamar a função, não para quem gravar o
  evento.

- [M3] Contradição interna na seção que o registro manda auditar com mais
  desconfiança
  Arquivo: `docs/progress/fase_9.md:238` abre a §2.8 com *"Medido na peça 6, e é
  o primeiro item de DoD desta fase **que não fecha**"*; `fase_9.md:356` marca o
  item 7 como **VERDE**
  Requisito violado: R12 §"Achados e backlog" (`.claude/rules/documentation.md`)
  e R2 §1 — o registro é o insumo declarado do auditor e afirma as duas coisas
  Evidência: as duas linhas estão no mesmo documento, e o resumo de orientação
  enviado nesta sessão aponta a §2.8 como o ponto nº 1 a julgar. A frase é
  resíduo do estado em que 833/min estourava; ficou depois de 400/min ser
  adotado.

- [M4] P1-13 vence "esta fase", segue `ABERTA`, e a seção de detalhe fecha sem
  disposição
  Arquivo: `docs/progress/fase_9.md:379` (linha da tabela-resumo, `Vence em`:
  *"**esta fase** — é ela que constrói o gerador"*) e `fase_9.md:455-467` (seção
  de detalhe, que termina em "Detalhe em `fase_1.md`" sem disposição)
  Requisito violado: a própria regra de vencimento da tabela da §6, e o espírito
  da P7-6 registrada em `docs/process/WORKFLOW.md` §auditoria
  Evidência: a metade da P1-13 que esta fase fecha está fechada e bem — o motor
  usa `dados_sinteticos`, declarado em `scripts/check_core_contract_imports.py`
  (+15 linhas, executado: rc=0). A metade que **não** fecha é a das faixas de IP:
  `contracts/evidence.schema.yaml:205-214` declara sete faixas e
  `dados_sinteticos/__init__.py:93-101` declara quatro redes de documentação, e
  `scripts/check_contract_examples.py:209-222` cruza **apenas**
  `allowed_domain_suffixes`. Executado: "Contratos: 8 / positivos 13 /
  negativos 95", sem comparação de faixa de IP.
  Contraste deliberado: a P2-11 está na mesma situação de vencimento e **está**
  argumentada como limite declarado (`fase_9.md:330-341`) — essa eu não reporto.
  A P1-13 não tem o parágrafo equivalente.

### LOW

- [L1] `evidence verify` não trata as recusas do motor, e `evidence build` trata
  Arquivo: `range_cli/cli.py:455-470` captura só
  `(ComandoRecusado, ContractSourceError)`; `range_cli/cli.py:438-446` captura
  genericamente e explica por quê
  Requisito violado: `04_SCENARIO_SCHEMA.md` §8.1 (a) — `evidence verify` é
  subcomando de leitura com código de saída; `range-core/evidence/build.py:178`
  mostra que `conferir` chama `projetar`, que levanta `GeradorAusente`,
  `FonteSemFormato`, `EntidadeInventada` e `IOCEncontrado`
  Evidência: leitura das duas funções, lado a lado. Um pack cujo gabarito
  dispare qualquer dessas quatro guardas devolve traceback em vez de
  `RECUSADO` + rc=2.

- [L2] As quatro regras de `x-aurora-linter-rules` do contrato de evidência não
  têm dono conferido por máquina
  Arquivo: `contracts/evidence.schema.yaml:244-248` (lista plana de prosa, sem
  `id`/`mecanismo`/`destinatario`); `scripts/check_regras_do_linter.py:71` fixa
  `CONTRATO = contracts/scenario.schema.v2.yaml` e não lê o de evidência
  Requisito violado: o próprio desenho da P7 declarado em
  `check_regras_do_linter.py:2-32` — *"regra declarada que ninguém executa e
  ninguém deve é exatamente a prosa marcada como contrato que este verificador
  existe para acabar"*
  Evidência: executado, imprime "10 regras declaradas — 8 com mecanismo e 2 com
  destinatário", e as dez são todas do contrato de cenário. As quatro de
  evidência — cobertura de projeção, `format` × `source_formats`, `sha256`,
  banner na primeira linha — são as desta fase e ficam fora do universo.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Toda fonte é projeção de `fact_id`; nenhum gerador inventa entidade | **PASS** | Suíte completa executada neste worktree: **1082 testes, OK, zero pulos, rc=0** (239,2 s, Postgres e Redis no ar). Inclui `test_evidence_elenco` (18) + probes (2), `test_evidence_projecao` (29) + probes (2), `test_evidence_generators` (27) + probes (2), conforme `.claude/verify/expected_suites.json:12-17`. `tools/check_core_boundary.py` rc=0 e `scripts/check_core_contract_imports.py` rc=0 ("19 arquivos declarados, todos batendo") — o gerador de telemetria ficou do lado do adapter. A porta é real: `projecao.py:235-241` levanta `EntidadeInventada`. Limite declarado e verificado no cabeçalho de `elenco.py:31-47`: ausência só para endereço IP |
| 2 — `evidence verify` dirigido por fato | **PASS** (com H1 contra `04` §8) | `build.py:178` reprojeta em memória; o oráculo é o produtor. Suítes `test_evidence_build` (28) + probes (2), verdes na execução acima. Executei `range-cli evidence verify tests/fixtures/pack_exemplo` → rc=2 com achado nomeado, provando que o verbo existe e não degrada para ok. A ressalva é H1: `04` §8 exige que ele rode no CI e ele não roda |
| 3 — `precursor_events.jsonl` reproduzível; edição detectada por hash | **PASS** | `projecao.py:83` fixa o nome que a spec usa (`NOMES_PROPRIOS`); `precursor.py:44-49` omite `actor` e `credential_state`; `manifesto.py:100` grava `sha256` por arquivo e `build.py:209-224` empilha hash + reprojeção com mensagens distintas. Coberto pelas 28+2 de `test_evidence_build`, verdes |
| 4 — Telemetria CEF é projeção, não emissão independente | **PASS** (com M2) | `test_telemetry_forwarder` (20) + probes (2), verdes. A prova de valor existe e é real: `tests/test_telemetry_forwarder.py:147-166` compara `payload[chave] == fato[campo]` e exige a chave presente. `contracts/events.schema.yaml:715-743` declara o enum das doze de `02` §10. Ressalva M2: o payload não está ligado ao `event_type` no envelope |
| 5 — Sem anexo, binário, IOC real, domínio roteável | **PASS** (com H2 e M1) | `tools/check_security_constraints.py` rc=0, `tools/check_synthetic_data.py` rc=0, `scripts/phase0_negative_tests.py` verde ("Todos os seis verificadores falharam contra probes independentes"; 112 provas de invariante do hook). `projecao.py:112-120` tokeniza antes de julgar e `:249-255` levanta `IOCEncontrado` com o predicado único; `email.py:44,65,83` monta o link do elenco + `.example`, sem multipart e sem `Content-Disposition`. Ressalvas: H2 (oráculo do banner) e M1 (registros de adiamento) |
| 6 — Replay respeita o clock de exercício | **PASS** | `forwarder.py:187` lê só `clock.elapsed_seconds()`; não há acesso a relógio de parede em nenhuma das 200 linhas do módulo — verificado por leitura integral. Sete casos em `test_telemetry_forwarder.py:239-303`, verdes. Ver a ressalva de instrumento abaixo |
| 7 — Reconstrução < 3 s com `telemetry_emitted` no volume de 4 h | **FAIL** | `scripts/check_prova_do_exercicio_4h.py` → **rc=1** sobre este objeto: a prova é da árvore `cd735acf` e este checkout é `4dd194d0`. É o B1. A medição em si pode até estar certa; sobre o commit auditado ela não tem objeto, e o verificador que a fase nomeia como evidência reprova |

**Provas do lançador, verificadas neste worktree:**
`python scripts/check_provas_de_container.py` → **rc=0**, 2 provas amarradas a
`4dd194d0c727`, gravadas 2026-09-22T18:59:33Z, `demo` e `reinicio` ambas rc=0.
`python scripts/check_prova_do_seed.py` → **rc=0**, árvore `4dd194d0c727`,
3.543.783 linhas, 158,6 s e 195,7 s contra orçamento de 300 s, 20 tabelas com
SHA-256 igual. As duas amarram a ESTA árvore; só a do exercício de 4 h não.

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_evidence_projecao.py:114-120`** —
   `test_o_texto_e_LIDO_do_contrato_e_nao_reescrito` compara
   `banner.texto(CONTRATOS)` com `CONTRATOS["evidence"][...]["banner_text"]`, e
   `banner.py:84` é a função que devolve exatamente esse valor. O oráculo é a
   mesma fonte que a implementação. O requisito de `06` T13 é *"banner de
   ambiente simulado na primeira linha de cada arquivo"*, e o texto normativo é
   `05` §4 — que nenhum teste e nenhum verificador cruza contra o contrato. É o
   H2.

2. **`tests/test_telemetry_forwarder.py:266-273`** —
   `test_DURANTE_A_PAUSA_nada_novo_vence` usa `ORelogioFalso`, cujo próprio
   `avanca` (`:87-90`) já não avança quando pausado. O caso exercita o dublê, não
   o forwarder: qualquer implementação que lesse `elapsed_seconds()` passaria, e
   uma que lesse o relógio de parede não seria alcançada, porque o dublê não tem
   relógio de parede para ler. **A propriedade continua verdadeira** por
   construção — `Replay` não tem outra fonte de tempo, e eu verifiquei isso lendo
   o módulo inteiro. O registro declara o limite em `fase_9.md:355` ("o forwarder
   não sabe pausar"). Registro como limite de instrumento, não como finding.

3. **`tests/test_telemetry_forwarder.py:166`** — a metade CEF da prova de valor é
   `assertIn(str(fato[campo]), self.cef)`, substring sobre o arquivo inteiro. Um
   `src` que aparecesse em linha de outro fato satisfaz. A metade forte
   (`payload[chave] == fato[campo]`, `:165`) é sólida e é ela que carrega o item;
   a metade CEF afirma menos do que o docstring de `:147-157` promete
   ("comparados lado a lado").

## O QUE EU NÃO CONSEGUI VERIFICAR

- **A medição do item 7 em si.** Não vi rodar, e não podia: o gravador
  `prova_do_exercicio_4h.py` está deliberadamente fora da allowlist do auditor
  (`~/.claude/hooks/readonly_bash.py:345-351`). O que eu podia conferir — a
  amarração da prova ao objeto — eu conferi, e ela reprova. Se a prova for
  regravada contra a árvore candidata, os números 2,119 s / 96.650 eventos
  passam a ter objeto; **eu não afirmo nada sobre se passariam**, porque não os
  medi.
- **As provas de container e do seed.** Rodaram na máquina do operador; eu
  verifiquei apenas o hash de árvore que as amarra (`4dd194d0c727`, ambas
  batendo) e li a saída íntegra que os verificadores imprimem.
- **A prova do exercício de 4 h contra o pack em disco.** `scenarios/` não existe
  neste worktree, então a perna (g) do verificador não se aplicaria nem se a
  árvore batesse — modo declarado em `check_prova_do_exercicio_4h.py:19-24`. Os
  sete SHA-256 do pack medido valem pelo que declaram.
- **O CI do GitHub sobre este commit.** Não executei nem consultei. H1 é
  afirmação sobre o **conteúdo** de `.github/workflows/invariants.yml` nesta
  árvore, que eu li, não sobre uma execução.
- **`.claude/verify/run.sh`** — o hook de prompt pede, e a forma `bash
  .claude/verify/run.sh` não está na allowlist do auditor. Não executado, e
  declarado. Em compensação rodei individualmente: `codegen.py --check`,
  `check_core_boundary`, `check_contract_literals`, `check_event_envelope`,
  `check_security_constraints`, `check_synthetic_data`,
  `check_core_contract_imports`, `check_store_read_surface`,
  `check_fold_authority`, `check_api_surface`, `check_insumo_de_metrica`,
  `check_hooks_com_emissor`, `check_gate_coverage`, `check_allowlist_do_auditor`,
  `check_regras_do_linter`, `check_spec_flags`, `check_spec_examples`,
  `check_contract_examples`, `check_progress_consistency`, `check_readme_atual`,
  `check_banner_de_simulacao`, `check_secoes_de_seguranca`,
  `check_gabarito_fora_do_git`, `check_volumes_da_linha_b`,
  `phase0_negative_tests`, `check_prova_do_exercicio_4h_probes` — **todos rc=0**,
  exceto `check_prova_do_exercicio_4h` (B1).
- **P2-11 e P9-2.** Não as julguei: a fase declara explicitamente que não as
  fecha (`fase_9.md:330-341` e `:504-524`), com argumento, e `06` T13 não as
  cobra. Registro que li e aceitei a declaração — não que as medi.
- **Se a §7 de fechamento contradiz algo.** Está vazia por desenho
  (`fase_9.md:632-635`), e é redigida por quem implementou após o veredito. Nada
  a auditar nela.
```

**Resumo em uma linha:** a fase está bem construída — 1082 testes verdes sem pulos, invariantes arquiteturais intactos, seis geradores que são de fato projeção de fato, e as duas outras provas de lançador amarradas a esta árvore. O que a reprova é mecânico e provavelmente barato de corrigir: dois commits de lançador entraram *depois* da gravação da prova de 4 h e a órfãos, e o item 7 é o item que se decide por ela.
