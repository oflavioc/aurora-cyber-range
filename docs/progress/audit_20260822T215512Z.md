# Auditoria de checkpoint - 2026-08-22T21:55:12.389965+00:00

- fase: **6**
- commit auditado: `bb73a0ecebb817fae0b1426d6b83710e5deb9f3c`
- session_id: `312b7bc6-acc2-45f0-bdb4-3c79dc99256c`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

All checks complete. Writing the report.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

Base de comparação: `0350697614297bd714a0be35b937aefcaaff823b` → `bb73a0ecebb817fae0b1426d6b83710e5deb9f3c`.
Guarda de base: **PORTA** (verbatim do lançador).
Suíte: `python -m unittest discover -s tests` → **707 testes, OK, rc=0, zero skips** (a
saída não traz `(skipped=N)`; os `skipIf` de Postgres não dispararam — os serviços
estavam no ar).
Provas fora de banda, conferidas por SHA contra este checkout: `check_provas_de_container.py`
rc=0 (2 provas, `bb73a0ecebb8`); `check_prova_do_seed.py` rc=0 (`bb73a0ecebb8`, 148,5 s e
152,1 s, 3.543.783 linhas).
Verificadores executados com rc=0: `check_insumo_de_metrica`(+probes),
`check_hooks_com_emissor`(+probes), `check_fabrica_liga_emissor`(+probes),
`check_api_surface`(+probes), `check_contract_examples`, `check_spec_examples`,
`check_gate_coverage`, `check_readme_atual`, `check_secoes_de_seguranca`,
`check_gabarito_fora_do_git`, `check_allowlist_do_auditor`, `check_trilha_de_auditoria`,
`check_core_contract_imports`, `check_store_read_surface`, `check_fold_authority`,
`check_progress_consistency`, e os seis `tools/check_*.py` + `codegen --check`.

## FINDINGS

### BLOCKER

- [B1] `audit_query_performed` é emitido sem `persona`, e `persona` é obrigatório na camada dele
  Arquivo: `domains/academus/api/emissor.py:84-98` (o `EventDraft` passa `actor_id=escopo.sub`
  e **nenhum** `persona`; `range-core/events/store.py:112` dá `persona: str | None = None` e
  `store.py:177` copia esse `None` para o envelope)
  Requisito violado: `09_EVENT_MODEL.md` §1.1 — *"`actor_id` e `persona` são obrigatórios
  quando `truth_layer` for `participant_action` ou `evaluator_assessment`"*; e
  `contracts/events.schema.yaml:154-162`, que codifica isso como
  `if truth_layer == participant_action then required: [actor_id, persona]`.
  `09_EVENT_MODEL.md` §4.1 cataloga `audit_query_performed` como `participant_action`, e
  `domains/academus/api/emissor.py:49` fixa `CAMADA = "participant_action"`.
  Evidência: observada, não suposta —
  (a) o construtor do draft não tem o argumento; `persona` não aparece uma única vez em
      `domains/academus/api/` (`grep persona domains/academus/api/*.py` → vazio);
  (b) a superfície do adapter não tem de onde tirá-lo: `domains/academus/api_surface.yaml:189-192`
      assina `claims: [sub, role, exp]`, e `papeis_de_dominio` são `aluno/professor/secretaria`,
      que o próprio `check_api_surface.py` recusa como vocabulário de persona. Não é variável
      esquecida; é campo que a superfície não produz;
  (c) `contracts/events.schema.yaml:71-73` tipa `persona` como `string, minLength: 1`, então
      nem omitir nem enviar `null` valida;
  (d) nada na árvore pega: a única validação de envelope contra o contrato é
      `tests/test_inject_engine.py:694-729` (`ConformeAoContrato`), que cobre **só** os eventos
      do `InjectEngine` — `grep -rn "iter_errors" tests/*.py` devolve exatamente essas três
      linhas. O evento do adapter nunca é validado;
  (e) o teste que deveria pegar cita a seção e não a exerce: `tests/test_api_emissao_pela_rota.py:147-155`
      tem docstring *"`09` §1.1 e §4.1: a camada e o produtor não são escolha do handler"* e
      afirma sobre `event_type`, `truth_layer` e `producer` — os três campos que §1.1 já teria
      garantido — omitindo o único que §1.1 torna condicionalmente obrigatório;
  (f) nenhuma decisão registrada: `grep persona docs/progress/fase_6.md` só devolve o bloco da
      superfície de participante (linhas 76-104); a lacuna do adapter não é discutida em lugar
      nenhum, nem como pendência `P6-*`.
  Por que é blocker: o evento que fecha o **item 1 da DoD desta fase** entra no event store
  violando o contrato do envelope, e o store é append-only (`00_MASTER_SPEC.md` §5.5 —
  *"nunca apague história"*). Não há como corrigir retroativamente um exercício já gravado.
  A consequência é da classe que `09_EVENT_MODEL.md` §4 chama de mais cara: nada falha hoje —
  o store não valida payload nem envelope —, e o defeito aparece quando um consumidor
  validante existir (Fase 9/10, importação, reconstrução) ou quando o AAR precisar atribuir
  a consulta de auditoria a uma persona, que é o que `00_MASTER_SPEC.md` §3 consequência
  normativa 3 e `03_EXERCISE_DESIGN.md` §6 apoiam na assimetria por persona. É também
  reincidência exata da forma do B2 da sexta rodada — a rota respondia e não gravava; aqui
  ela grava um envelope que o contrato recusa — e a correção não é local: exige decidir de
  onde a `academus-api` tira persona, o que é desenho, não digitação.

### HIGH

- [H1] `confidence` fora de faixa atravessa a rota, vira evento e desloca `TTIV`
  Arquivo: `range-core/participant/api/app.py:241-246` (rota `POST /participant/assessment`
  recebe `corpo: dict` sem modelo) e `range-core/participant/api/app.py:155-163` (`_declara`
  repassa `payload={k: v for k, v in corpo.items() if k not in {...}}` — sem validação)
  Requisito violado: `03_EXERCISE_DESIGN.md` §5.1 (`confidence: 72  # 0–100`) e
  `contracts/assessment.schema.yaml:97-100` (`type: integer, minimum: 0, maximum: 100`),
  que `contracts/events.schema.yaml` reivindica por `$ref` no payload de `assessment_submitted`.
  Evidência: a rota não instancia nenhum validador; o payload chega cru ao
  `Emissor.declarar` (`range-core/participant/api/emissor.py:144-156`), que só exige
  `justificativa`. `range-core/metrics/calibracao.py:226-242` (`_por_caso`) aceita qualquer
  `int`, e `calibracao.py:155-162` calcula `(confidence/100 - defensibility)²` sem faixa.
  `range-core/metrics/verificacao.py:205-214` compara esse Brier com
  `insumo.limiar_de_calibracao` para marcar `TTIV`. O próprio registro da fase mede o efeito:
  `docs/progress/fase_6.md:1248-1255` — `confidence: 900 -> brier = 64.0`, *"desloca o
  instante de `TTIV`, ou o impede para sempre"*.
  Por que é HIGH e não BLOCKER: nenhum item da DoD nem critério de T10/T11 exige a
  aplicação da faixa, e o defeito está declarado como pendência aberta (P6-11,
  `docs/progress/fase_6.md:1244-1281`) com o lugar da correção já escolhido. Declarado não é
  corrigido, mas é o oposto de escondido.

- [H2] O verificador de hooks e o de fábrica não alcançam a `participant-api`, e é ali que
  ficam oito dos nove `event_type` de declaração
  Arquivo: `scripts/check_hooks_com_emissor.py` (varre `domains/` — saída medida:
  *"1 hook(s) em 1 adapter(s)"*); `scripts/check_fabrica_liga_emissor.py` (saída medida:
  *"participant-api: … SERVICOS o declara sem produtor"*, admitido como estado declarado)
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T9, primeiro critério — *"cada `event_type`
  declarado em `observability_hooks.yaml` é emitido pela ação correspondente"* — combinado
  com `03_EXERCISE_DESIGN.md` §1.2: *"`auto` — emitida pela aplicação instrumentada, com
  produtor identificável. **Declarada em `observability_hooks.yaml`**"*.
  Evidência: `domains/academus/observability_hooks.yaml:44-48` declara **um** hook. As nove
  declarações de `03` §3.4 são emitidas por `range-core/participant/api/emissor.py:146-156`,
  que é core e não adapter — `09_EVENT_MODEL.md` §6 escopa o arquivo em
  `domains/<adapter>/`, então não há onde declará-las. `separate_incident_declared` é
  evidência `auto` do OBJ-03 no exemplo normativo de `09` §6 (linhas 256-269) e de
  `03` §1.1, e não está declarado em hook nenhum: `grep -rn separate_incident_declared
  domains/` não devolve o arquivo de hooks.
  Por que é HIGH: a cobertura de T9 é hoje de um `event_type` em trinta e três, e o
  mecanismo que a impõe é estruturalmente cego ao produtor que emite oito das nove
  declarações da fase. O artefato que expõe isso — `objectives.yaml` do pack — é da Fase 7,
  e é lá que a evidência `auto` do OBJ-03 falhará em resolver, com o pack já escrito.

### MEDIUM

- [M1] A validação de envelope contra o contrato cobre um produtor de três
  Arquivo: `tests/test_inject_engine.py:694-729`
  Requisito violado: `09_EVENT_MODEL.md` §1 — *"um envelope só… injects, marcadores de
  evidência, declarações, rollbacks, avaliações… usam a mesma estrutura"*.
  Evidência: `grep -rn "iter_errors" tests/*.py` devolve apenas
  `tests/test_inject_engine.py:725`. O docstring da própria classe declara o papel —
  *"o store não valida — decisão dele —, então esta é a única camada que impede o engine de
  emitir um envelope que o contrato recusa"* — e o commit acrescentou dois produtores novos
  (`domains/academus/api/emissor.py` e `range-core/participant/api/emissor.py`) sem estendê-la.
  É a ausência que deixou B1 passar por seis rodadas de auditoria.

- [M2] `03` §3.4 exige `papel` em cada declaração, e o campo de papel do envelope é o mesmo
  que B1 deixa vazio no adapter
  Arquivo: `03_EXERCISE_DESIGN.md:248` — *"cada uma grava evento com autor, **papel**, ambos
  os relógios, epoch e justificativa livre"*
  Evidência: na `participant-api` o papel é `persona`, e está gravado
  (`range-core/participant/api/app.py:150,157` e `emissor.py:153`) — este lado cumpre.
  O registro fica aqui porque mostra que o campo tem semântica normativa de papel, e não é
  metadado opcional; é o que separa B1 de omissão cosmética.

### LOW

- [L1] Teste tautológico sobre o espião
  Arquivo: `tests/test_api_emissao.py:137-152`
  (`test_o_emissor_recebe_exatamente_os_campos_declarados`)
  Evidência: o corpo chama `espiao.registrar_consulta(**kwargs)` diretamente e depois afirma
  que `espiao.chamadas` contém esses mesmos `kwargs`. Não toca a rota, não toca o `Emissor`
  real e não toca o store: mede o dicionário que ele próprio escreveu.
  Não é blocker nem high porque a propriedade real está provada em
  `tests/test_api_emissao_pela_rota.py:171-179`, e o cabeçalho daquele arquivo (linhas 3-13)
  já registra o achado do B2 que criou a substituição. O que sobra é um caso que não afirma
  nada e que um leitor futuro pode confundir com cobertura.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Consultar a trilha com filtro de período emite `audit_query_performed` | **FALHA** | Emite — `tests/test_api_emissao_pela_rota.py::AConsultaEmiteOEvento` (rota real, `TestClient`, afirmação sobre o evento no store); fábrica liga o emissor — `check_fabrica_liga_emissor.py` rc=0. Mas o envelope emitido viola `09` §1.1 e o contrato: **B1** |
| 2. Cada par declaração × verificação é disparável e testado | PASS | `test_metrics_declaracao.py::AsTresMetadesDeDeclaracao` + `AClausulaHerdadaDoTTID`; `test_metrics_verificacao.py::MarcaOsDoisInstantes` + `TTIVCruzaOLimiarDeCalibracao`; pares montados em `test_aar_timeline.py` (`PARES` = TTCD/TTCV, TTRD/TTRV, TTID/TTIV) |
| 3. Desconto de `technical_failure` pela **união**, nunca pela soma | PASS | `range-core/metrics/epoch.py:280-297` (`uniao`) e `300-327` (`decorrido`, com recorte à janela); `test_metrics_epoch.py`; `test_metrics_verificacao.py::EpochAtravessaAteONumero` mede o encurtamento exato |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | PASS | `epoch.py:128-201` (`epochs_descartadas`/`epochs_em_calculo`/`apenas`), aplicado nos **dois** lados (`declaracao.py:118-120`, `verificacao.py:159-162`); `test_metrics_declaracao.py::OsQuatroEfeitosDeEpoch::test_rehearsal_tira_a_epoch_do_calculo`; `test_metrics_verificacao.py:261` |
| 5. Declaração não altera nenhum valor de ground truth | PASS | `tests/test_simulation_state.py:276-313` (`test_declaracao_NUNCA_move_flag`, as seis declarações, comparação contra o padrão), com prova negativa em `test_simulation_state_probes.py` (ramo plantado no `_writes_of`) |
| 6. Declaração prematura produz lista de eventos incompatíveis no AAR | PASS | `range-core/aar/timeline.py:222-244` (`_incompativeis`, só `ground_truth`, só na asseguração prematura); `test_aar_timeline.py::OsEventosIncompativeis` (4 casos, incluindo o negativo da lacuna) |
| 7. Pack sem `verification_predicates` não carrega | PASS | `contracts/ground_truth.schema.yaml:24`; `pack_loader.py:455-508` (`_verify_completude`) e `511-550` (`_verify_schema`, com `$ref` atravessando contratos); `test_pack_completo_materializado.py:181-196` + o controle positivo em `:223-225` e o apenas-manifesto em `:228-284` |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | PASS | `range-core/metrics/verificacao.py:183-214` (`_instante_do_limiar`, Brier recalculado por prefixo, `<=`), `SIGLA_DO_LIMIAR` **fora** de `SIGLA_POR_PREDICADO`; `test_metrics_verificacao.py::TTIVCruzaOLimiarDeCalibracao` (9 casos, incluindo `test_o_limiar_vem_do_INSUMO_e_nao_de_constante`) |
| 9. Brier só no escopo revisado; lacuna de cobertura em separado | PASS com ressalva | `range-core/metrics/calibracao.py:134-162` e `245-261`; `test_metrics_calibracao.py::OBrierSoOlhaOEscopoRevisado` e `NaoAvaliadoContaComoConfiancaZero`. Ressalva: **H1** — a faixa de `confidence` não é aplicada em caminho nenhum |
| 10. Rubrica ausente ou em versão divergente impede a carga | PASS | `contract_rules.py:177-192` (registro `rubric_library`, versão dentro do id); `pack_loader.py:719-731` passa `load_library()`; `test_pack_loader.py:435` e `:443` (ausente e versão divergente); `range-core/rubrics/library.py:131-150` recusa biblioteca vazia; nove rubricas presentes em `range-core/rubrics/` |
| 11. Divergência ≥ 2 pontos entre avaliadores gera alerta | PASS | `range-core/aar/timeline.py:281-320` (distância entre **extremos**, última nota de cada avaliador, não resolve); `test_aar_timeline.py::ADivergenciaEntreAvaliadores` (7 casos, incluindo o de fronteira em 1 ponto e o de três avaliadores 0/1/2) |
| 12. As nove siglas classificadas pelo critério de `00` §3.2 | PASS | `tests/test_derivacao_das_nove_siglas.py` — lê a tabela de `03` §3.0 **do documento**, aplica a conjunção às colunas, exige o resultado da última coluna, e cruza as nove com as siglas que os dois computadores de fato **produzem** ao rodar (`_siglas_produzidas`, sem lista intermediária) |
| 13. Todo `event_type` tem `metric_side`; os quatro lados são disjuntos | PASS | `contracts/events.schema.yaml:692-747` (33 tipos: 7 `verification`, 9 `declaration`, 5 `epoch`, 12 `none`); `contract_rules.py:123-153` confere cobertura, valores e lado vazio na carga; `test_contract_rules.py` |
| 14. Folha de predicado obedece à conjunção de `09` §4.0, com probe das três violações e do controle positivo | PASS | `contract_rules.py:155-173` (`predicate_leaf` = `state_effect` ∧ `metric_side: verification` → exatamente os cinco de `09` §4.0); fixtures negativas em `contracts/ground_truth.schema.yaml:694` (autorreferência), `:712` (`assessment_submitted`), `:731` (`communication_submitted`), mais `:669` e `:745`; controle positivo no exemplo válido; executadas por `check_contract_examples.py` rc=0 |
| 15. Cada computador recebe apenas o seu lado, mais epoch e escalares; só o AAR tem as duas metades | PASS | `range-core/metrics/insumo.py` (tipos `NewType` distintos, ponto único `monta`, escalares no `InsumoDeVerificacao`); `check_insumo_de_metrica.py` rc=0 e os **6 negativos + 1 positivo** dos probes; `test_metrics_insumo.py::CadaLadoRecebeOSeu` e `EscalaresNoInsumo::test_nenhum_insumo_tem_store_pack_ou_fluxo_total`; `aar/timeline.py::compoe` é o único que recebe as duas listas de `Medida` |
| 16. Desconto, exclusão de `rehearsal` e seleção de start são cálculo do consumidor | PASS | `insumo.py:152-165` entrega a escrituração **inteira** aos dois (`test_metrics_insumo.py::EpochVaiInteiraParaOsDois::test_a_escrituracao_e_a_mesma_nos_dois_insumos`); a seleção de start é `declaracao.py:149-165` sobre o payload, não recorte do montador |
| 17. `inject_fired` emite no payload os marcadores de start, com teste de emissão | PASS | `contracts/events.schema.yaml` `$defs/inject_fired_payload` (`observable_impact`, `requires_response`, `additionalProperties: false`); `range-core/engine/inject_engine.py:310-330` carimba o que a carga derivou; `pack_loader.py:761-800` deriva as três pernas de `03` §3; `test_inject_engine.py::ConformeAoContrato` valida o envelope emitido |

## TESTES QUE NÃO PROVAM O REQUISITO

1. `tests/test_api_emissao.py:137-152` — `test_o_emissor_recebe_exatamente_os_campos_declarados`
   chama o espião e afirma sobre o espião. Zero contato com rota, emissor ou store.
   Tautologia pura. (L1 — a propriedade real tem prova própria noutro arquivo.)

2. `tests/test_api_emissao_pela_rota.py:147-155` — `test_o_envelope_do_evento_e_o_declarado`
   invoca `09` §1.1 na docstring e verifica `event_type`, `truth_layer` e `producer`.
   Nenhum dos três é o que a §1.1 acrescenta; o que ela acrescenta — `persona` obrigatório
   na camada de pessoa — é exatamente o que o código não emite. O teste passa porque não
   pergunta. É o mecanismo pelo qual **B1** sobreviveu a seis rodadas.

3. `tests/test_inject_engine.py:694-729` — `ConformeAoContrato` prova o que diz, e o
   registro aqui é de **escopo**, não de defeito: a docstring afirma ser *"a única camada
   que impede… emitir um envelope que o contrato recusa"*, e ela cobre um dos três
   produtores da árvore. A afirmação é mais larga que o mecanismo. (M1.)

Fora esses três, não encontrei teste dirigido por seed onde deveria ser por `fact_id`, teste
de imutabilidade sem a perna de role, asserção sobre mock substituindo comportamento, nem
`skip`/`xfail` sem justificativa — `grep -rn "skipTest|@unittest.skip|expectedFailure" tests/`
devolve apenas os `skipIf` de Postgres, e nenhum disparou nesta execução.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **As duas provas de container (item 1 e item 4 da DoD da Fase 4, exercitadas de novo aqui).**
  Não as vi rodar. `scripts/check_provas_de_container.py` confere o SHA gravado contra o
  `HEAD` deste checkout e sai rc=0; o que eu verifiquei foi a **amarração ao commit**, não a
  execução. O limite está impresso pela própria saída do script.
- **A prova do seed completo (M2 da Fase 5).** Mesma forma e mesmo limite: `check_prova_do_seed.py`
  rc=0 amarrando `bb73a0ecebb8`; a medição foi do lançador.
- **Se algum consumidor de produção rejeita o envelope sem `persona`.** Eu determinei que ele
  é inválido pelo contrato (`events.schema.yaml:71-73` e `:154-162`) por leitura do schema e do
  construtor. Não consegui executar o validador contra um evento real: a allowlist do auditor
  não admite `python -c` nem script fora da lista, e nenhum teste da árvore faz essa validação
  para o adapter. A afirmação de invalidade é derivada da forma do schema, e é sólida; a
  afirmação de qual consumidor quebra primeiro, não fiz.
- **A árvore de cliente (`web/`).** Nada nela mudou de forma relevante à Fase 6 no diff, e não
  a auditei; `check_web_sem_derivacao`, `check_telas_sem_vocabulario` e `check_banner_de_simulacao`
  não foram executados por não haver mudança de tela nesta fase.
- **A metade da P6-7 que continua aberta** — *"este handler, executado, emite?"* como
  propriedade universal. `check_fabrica_liga_emissor.py` imprime essa fronteira na própria
  saída. Hoje a única rota instrumentada tem prova de execução
  (`test_api_emissao_pela_rota.py`); a propriedade geral não tem mecanismo, e eu não teria
  como verificá-la sem construir um.
- **Comportamento em Postgres/Redis de produção** além do que a suíte exercitou. Os 707
  testes rodaram contra os serviços efêmeros; o que eles cobrem é o que eu vi.
```

**Resumo:** a fase é, no essencial, sólida — a partição de insumo de `00` §3.2 está implementada com verificador e sete negativos, a conjunção da folha de predicado tem as três violações e o controle positivo, a derivação das nove siglas é lida da spec e cruzada com a saída real dos computadores, e os quatro efeitos de epoch valem nos dois lados. O que reprova é um só ponto, e é o item 1 da DoD: o único evento que o adapter emite nesta fase entra no store append-only sem `persona`, campo que `09_EVENT_MODEL.md` §1.1 declara obrigatório para `participant_action` e que `contracts/events.schema.yaml` exige — e o teste que cita aquela seção verifica três campos que não são o dela.
