# Auditoria de checkpoint - 2026-08-14T05:11:15.203072+00:00

- fase: **0**
- commit auditado: `8b4d62775e8379b5d6b0a14f48b9b08b47b503d4`
- session_id: `78d57f2c-c875-4b04-98f6-1e9f7cdec171`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have enough measured evidence. Here is the report.

# AUDITORIA — FASE 0

## VEREDITO: FAIL

Commit auditado: `8b4d62775e8379b5d6b0a14f48b9b08b47b503d4` (worktree fixado, árvore limpa antes e depois da execução).

## FINDINGS

### BLOCKER

- **[B1] O hook do auditor libera execução arbitrária e escrita fora do worktree por três vias não enumeradas — a capacidade que o commit declara ter removido**
  Arquivo: `user-scope/hooks/readonly_bash.py:54` (`env` na allowlist), `:16` (`git diff` na allowlist), `:88` (`git branch` só com deleção negada), `:97-102` (enumeração de flags de escrita)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done item 4; critério de admissão fixado pelo próprio projeto em `scripts/phase0_negative_tests.py:222-231` ("Escrita que alcanca o worktree principal e finding, nao defeito aceito"); `CLAUDE.md` §Fluxo por fase (razão de o auditor viver em escopo de usuário)
  Evidência — **medida por execução nesta sessão, contra a cópia instalada**:
  1. `env python -c "print('...')"` → **rc=0, executou**. `env` é allowlistado por `:54` sem nenhuma restrição sobre o que ele invoca; nada em `DENIED_ANYWHERE` casa. Isso é execução arbitrária de Python, logo escrita arbitrária em qualquer caminho, em qualquer grafia, sem depender de flag alguma. O probe `python -c 'import os'` bloqueia (`:129`, allowlist), mas prefixá-lo com `env` o libera.
  2. `git diff --output=/c_nonexistent_probe_dir/audit_probe.txt HEAD~1 HEAD` → **passou pelo hook**; a saída foi `fatal: could not open '.../audit_probe.txt' for writing`, ou seja o git chegou a **abrir o alvo para escrita** e só falhou porque o diretório não existe. `git diff` está em `:16` e `--output=` não está na enumeração de `:97-102`.
  3. `git branch -m __audit_probe_inexistente_a __audit_probe_inexistente_b` → **passou pelo hook**; o git recusou apenas por a branch não existir. `:88` nega só `-d/-D/--delete`; `-m`, `-M`, `-f` e `-c` mutam o ref store **compartilhado com o repositório principal** — exatamente o dano que `:85-87` registra como provado por execução.
  Por que é blocker: a via (1) permite sobrescrever `~/.claude/hooks/readonly_bash.py`, que é precisamente o comprometimento que a colocação em escopo de usuário existe para impedir, e foi a consequência que fez o B1 da décima auditoria ser BLOCKER. A tese central do commit — "o que se verifica por texto não é contenção, é AUSÊNCIA DE CAPACIDADE" — é refutada no próprio arquivo: `find` saiu da allowlist, mas `env` (execução arbitrária) e `git` (escrita por `--output=`, mutação de ref por `-m`/`-f`) continuam nela com capacidade de escrita. A justificativa escrita em `:92-96` — "a superfície é FECHADA POR COMANDO — são as flags de saída que essas **cinco** ferramentas documentam" — está incompleta nos próprios termos: há uma sexta família allowlistada (`git`) com flag de saída documentada, não enumerada. Nenhuma dessas três formas está declarada em `BURACOS_CONHECIDOS`, e nenhuma passa no critério de admissão de `:222-231`.

- **[B2] O harness certifica "0 escritas não bloqueadas" e tem um probe que passa pelo motivo errado**
  Arquivo: `scripts/phase0_negative_tests.py:243` (`BURACOS_CONHECIDOS: list[tuple[str, str]] = []`), `:169` (probe `env`), `:200-217` (matriz de 8 formas × 4 grafias), `:563-573` (declaração impressa)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §Teste negativo obrigatório ("Um verificador que nunca falhou contra uma violação plantada não é um verificador"); §DoD item 4
  Evidência: o probe `:169` é rotulado **"env como trampolim de execucao"** e usa `env rm -rf x`. Ele passa porque `readonly_bash.py:60` nega o token `rm` — a regra não tem relação nenhuma com `env`. O probe leva o nome da propriedade que **não** exercita: o trampolim real, `env python -c`, está aberto e foi executado por mim com rc=0. Consequência direta: a saída do harness afirma `escritas nao bloqueadas: 0` e a matriz de 32 provas cobre 8 formas escolhidas × 4 grafias, mas a matriz varia a **grafia do alvo** e mantém fixo o **conjunto de comandos** — repetindo, num eixo diferente, o erro que o B2 da décima auditoria puniu no eixo do alvo. `BURACOS_CONHECIDOS` vazia é apresentada como resultado (`:233-243`), e a lista está vazia por não terem sido procuradas as formas, não por não existirem.
  Por que é blocker: este harness é o oráculo único do item 4 da DoD, roda como gate no CI (`.github/workflows/invariants.yml:43-44`) e sai `0` afirmando uma propriedade falsa. É a mesma classe do B2 da rodada anterior, uma rodada depois, dentro da correção que se propunha a encerrá-la.

### HIGH

- **[H1] A tabela de DoD do registro contradiz a medição do próprio commit que a acompanha**
  Arquivo: `docs/progress/fase_0.md:378` (linha 4 da tabela §5) contra `docs/progress/fase_0.md:214` (tabela da décima auditoria, adicionada por este commit)
  Requisito violado: `docs/spec/07_IMPLEMENTATION_PHASES.md` §Registro de progresso ("itens de DoD com status"); `CLAUDE.md` §Quatro camadas de verdade
  Evidência: `:378` continua afirmando "**32 formas bloqueadas, 2 não bloqueadas** e afirmadas como defeito contido (`find -delete` e `find -fprint0` com alvo dentro do worktree)". Neste commit `find` **não está mais na allowlist** (`readonly_bash.py:50`), `BURACOS_CONHECIDOS` está vazia, e o harness que executei imprime `25 escritas deliberadas + 32 provas de invariante` e `0 escritas nao bloqueadas`. A seção narrativa `:214` do mesmo arquivo diz "**0**". O commit alterou `fase_0.md` (+40 linhas) e não tocou a célula. É a categoria que o próprio registro batiza em P10 e que o **H2 da nona auditoria** havia fechado ("célula agora traz os quatro números do harness deste commit") — reaberta pelo commit seguinte. A tabela §5 é a parte consultada por quem quer o status da fase.

- **[H2] O item 4 da DoD segue aberto na metade "libera testes/verificadores de leitura", e o harness sai `0` mesmo assim**
  Arquivo: `scripts/phase0_negative_tests.py:133-155` (11 falsos bloqueios afirmados), `:556-557`, `:574` (`return 0`)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4 ("…e libera testes/verificadores de leitura")
  Evidência — quatro leituras puras recusadas **nesta auditoria**, além das 11 já declaradas:
  `ls -R docs scripts tools user-scope 2>/dev/null | head -80` → "redirecionamento de saida para arquivo";
  `for t in check_contract_literals ...; do python tools/$t.py; done` → "fora da allowlist";
  `grep -n "^import\|^from ..."` → o `|` dentro das aspas é tratado como separador de segmento;
  `grep -n "write_text\|open(\|mkdir..." tools/codegen.py` → **"comando de escrita"**, porque `mkdir` aparece dentro do padrão citado — leitura pura recusada por uma palavra dentro de argumento entre aspas, que é o modo estrutural do P23.
  O item de DoD é binário; ele está aberto, e `main()` devolve `0`. Reafirmo o H1 da décima auditoria: harness verde não satisfaz o item.

- **[H3] O documento que define a DoD da fase é alterado por esta branch fora de qualquer gate, e o adiamento está registrado numa pendência inexistente**
  Arquivo: `.github/workflows/invariants.yml:110-113` (conjunto `CODE`); `docs/process/PHASE_0_CHECKLIST.md` (2 inserções, 1 remoção contra `main`); `docs/progress/fase_0.md:220`
  Requisito violado: `CLAUDE.md` §"A especificação é imutável durante a implementação"; `docs/process/WORKFLOW.md` §Ciclo por fase
  Evidência: `git diff --stat main...HEAD -- docs/process/` mostra `PHASE_0_CHECKLIST.md | 3 ++-`. O conjunto `CODE` de `:110-113` não inclui `docs/process/`, e o caminho não está em `docs/spec/` — logo nem a regra "spec e código no mesmo PR" nem a exigência de título `spec-change:` incidem. O critério de aceitação da fase é editável pelo PR que ela julga. Além disso, `fase_0.md:220` e a mensagem do commit adiam o achado "para o P36", e **não existe entrada P36 em lugar nenhum do repositório** (`grep -rn "P36"` retorna exatamente essa única linha; §6 vai de P32 a P35). Um HIGH encontrado por auditor independente foi adiado para um registro que não existe — logo não está rastreado.

### MEDIUM

- **[M1] A correção do H4 é assimétrica: os probes de defeito conhecido continuam rodando só contra a fonte versionada**
  Arquivo: `scripts/phase0_negative_tests.py:356` e `:374` (`_run_readonly_hook(command)` sem argumento `hook`), contra `:273` e `:286` (que iteram `_hooks_sob_teste()`)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4
  Evidência: `expect_hook_allows` e `expect_hook_blocks` passaram a exercitar fonte **e** cópia instalada (correção do H4, confirmada na saída: "Hooks exercitados: fonte versionada, copia instalada"). `expect_hook_blocks_known_defect` e `expect_hook_allows_known_hole` não. O argumento que justificou o H4 — "a fonte não é o que roda; a cópia instalada é" — vale igualmente para as afirmações de defeito conhecido.

- **[M2] Item 15 (auditoria de checkpoint com veredito PASS) não satisfeito**
  Arquivo: `docs/progress/fase_0.md:398-416`
  Requisito violado: `docs/process/WORKFLOW.md` §Ciclo por fase, conforme declarado pelo próprio registro
  Evidência: esta rodada emite FAIL com dois BLOCKER. A tabela de rodadas `:402-412` fica desatualizada a partir da 10ª.

### LOW

- **[L1]** `tools/README.md:3` continua afirmando "Este diretório é deliberadamente entregue sem os seis verificadores finais. A primeira tarefa real do Claude Code é implementar…", com os seis presentes e executando. É a P21, aberta desde a 3ª rodada.
- **[L2]** `docs/progress/fase_0.md:1221-1222` (§8 Próxima fase) manda executar a "**Sétima** auditoria de checkpoint" e conta "dezenove" pendências. Estamos na 11ª e §6 já chegou a P35. O plano de fechamento da fase está congelado quatro rodadas atrás.

## DoD — item a item

Referência: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done.

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis verificadores liberam árvore limpa | **PASS** | Executados um a um: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` → todos `rc=0`. `git status --short --branch` = `## HEAD (no branch)`, sem resíduo |
| 2. Os seis detectam as violações externas de `phase0_negative_tests.py` | **PASS** | 13 probes, todos `rc=1` com citação do arquivo plantado, incluindo ramo de `event_type`, isenção ancorada e divergência × ausência do `codegen`. `expect_fail` (`:66-99`) exige rc exatamente 1, saída não vazia e menção ao arquivo — distingue detecção de crash |
| 3. Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | **PASS** | Três smoke tests executados: import de `domains` em `range-core/x.py` → `exit=2`; `docs/spec/00_MASTER_SPEC.md` fora de branch `spec-change/` → `exit=2`; literal `academus.enrollment_offline` em `domains/academus/api/x.py` → `exit=2` |
| 4. Hook do auditor bloqueia escrita deliberada e libera leitura | **FAIL** | B1, B2, H1, H2 — falha nas duas direções, agora com três vias de escrita medidas e não declaradas |
| 5. Hook do `scenario-designer` bloqueia Write/Edit fora de `scenarios/` e Bash fora da allowlist | **PASS** | `scenario_scope.py` com `range-core/nope.py` → `exit=2`; `scenario_bash.py` com `python -c "1"` → `exit=2` com mensagem de allowlist |
| 6. `ground_truth.yaml` e `GM_NOTES.md` **não** no `.gitignore` | **PASS** | `.gitignore` lido integralmente (41 linhas); aparecem apenas no comentário `:39-40` que documenta o versionamento deliberado |
| 7. `.env`/secrets negados em `.claude/settings.json` | **PASS** | `:6-16` — `Read`, `Edit` e agora `Write` para `.env`, `.env.*` e `secrets/**`. Fecha o H2 da décima auditoria |
| 8. Auto Mode desabilitado | **NÃO VERIFICADO** | `.claude/settings.json:4-5` traz `"defaultMode": "default"` e `"disableAutoMode": "disable"`. Não consigo validar a chave contra o schema do Claude Code de dentro do worktree |
| 9. Push de `main` deixa `arquitetura` e `seguranca` verdes | **NÃO VERIFICADO** | Config confere (`invariants.yml:4-5,9,125`); execução não observável daqui. Registro declara atestação do operador (§5, P34) |
| 10. PR descartável: `spec_freeze` falha com spec+código juntos | **NÃO VERIFICADO** | Requer GitHub. Lógica de `:115-118` é coerente; resultado é atestação |
| 11. PR descartável: só spec exige título `spec-change:` | **NÃO VERIFICADO** | Idem, `:120-123` |
| 12. Branch protection exige os três contexts | **NÃO VERIFICADO** | Requer API do GitHub; `gh` fora da allowlist do auditor |
| 13. `spec-v1.0` depois de CI + branch protection | **NÃO VERIFICADO** | `git tag` está bloqueado até para listar (`readonly_bash.py:14-16`, falso bloqueio afirmado em `:145-146`) |
| 14. `/doctor` sem apontamentos | **NÃO VERIFICADO** | Fora do meu alcance |
| 15. (WORKFLOW.md) auditoria de checkpoint PASS | **FAIL** | Esta rodada: FAIL, 2 BLOCKER |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`scripts/phase0_negative_tests.py:169`** — probe "env como trampolim de execucao" (`env rm -rf x`). Passa pela regra do token `rm` (`readonly_bash.py:60`); nada nele exercita `env`. O trampolim que o nome anuncia (`env python -c`) está aberto e foi executado. É o exemplo canônico de asserção verde sobre propriedade não medida.
2. **`scripts/phase0_negative_tests.py:200-217`** — a matriz "8 formas × 4 grafias" varia a grafia do alvo mantendo fixo o conjunto de comandos. Prova ausência de capacidade **nos comandos lembrados**, e é apresentada em `:563-573` e em `fase_0.md:212` como prova de invariante. Três comandos allowlistados com capacidade de escrita (`env`, `git diff --output=`, `git branch -m/-f`) não aparecem em nenhuma linha.
3. **`scripts/phase0_negative_tests.py:243` + `:563-573`** — `BURACOS_CONHECIDOS` vazia é declarada como "resultado, nao omissao". A lista está vazia porque as formas não foram procuradas; a declaração impressa ("0 escritas não bloqueadas") é falsa e é a evidência citada para o item 4 da DoD.
4. **`scripts/phase0_negative_tests.py:356,374`** — os probes de defeito/buraco conhecido rodam só contra `READONLY_HOOK_SOURCE`. A fonte não é o que executa; combinado com B1, uma cópia instalada adulterada continua invisível para essas duas famílias de probe.
5. **`scripts/phase0_negative_tests.py:385-403`** (`hook_copies_in_sync`) — retorna sucesso silencioso quando `~/.claude/hooks/readonly_bash.py` não existe, isto é, **sempre no CI**, que é o único gate real. Aqui a checagem rodou e passou porque há escopo de usuário; no CI ela nunca roda.

Em contrapartida, e para o registro: os 13 probes dos seis verificadores são de boa qualidade e não encontrei entre eles nenhum que passe sem provar o requisito. `expect_fail` exige rc exatamente 1 e citação do arquivo plantado; as isenções ancoradas e o ramo de divergência do `codegen` são exercitados; os seis verificadores usam AST/parse estruturado com stdlib apenas, declaram `sys.dont_write_bytecode = True` e não contêm nenhuma chamada de escrita (`write_text`, `open(...,'w')`, `mkdir`, `unlink`) — `codegen.py --check` é read-only por construção, e a árvore ficou limpa depois de tudo.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 9 a 13 da DoD.** Dependem de GitHub, execução de CI, branch protection e tag. `gh` não está na allowlist e `git tag` está bloqueado até para listar. Continuam fechados por **atestação do operador** — terceira camada de verdade, não evidência observável, exatamente como `fase_0.md:394-396` declara. Nenhuma auditoria desta fase os verificou, e nenhuma poderá com as ferramentas atuais.
- **Item 8 e item 14.** Semântica de `disableAutoMode` e `/doctor` estão fora do meu alcance.
- **Completude do B1.** Provei que três vias existem; **não** provei que são as únicas. Pelo argumento registrado na oitava auditoria e repetido em `fase_0.md:160`, enquanto a pergunta "o bash executaria isto?" for respondida por casamento textual, a completude não é demonstrável — só refutável, e refutei mais três. Não executei nenhuma escrita real: as vias (2) e (3) foram medidas com alvos que o git recusa por inexistência, e a via (1) com `print`.
- **Impacto do P35.** Registro que existe uma decisão do operador (`fase_0.md:1151-1184`) autorizando merge com FAIL quando o único achado for a definição insatisfazível do item 4. Ela **não cobre** B1 nem B2: pelo critério de admissão que o próprio harness fixa (`:222-231`), escrita que alcança o worktree principal é finding e não defeito aceito; e a condição 3 da própria decisão diz que qualquer BLOCKER de outra natureza reprova como sempre. B2, em particular, não é sobre a definição do item 4 — é sobre o oráculo afirmar uma propriedade que não tem.
- **Modo de permissão desta sessão.** `scripts/start_checkpoint_audit.sh:129,133` lança o auditor com `--permission-mode default`; esta sessão rodou com **plan mode ativo**. Não consegui atribuir a causa. Não degradou a auditoria — executei os seis verificadores, o harness completo e os probes —, mas registro a divergência porque ela afeta a reprodutibilidade da rodada.
- **P11 (sobrevivência do relatório sem `--recover`).** Só é observável depois que esta sessão terminar; não posso apurá-lo de dentro dela.
