# Auditoria de checkpoint - 2026-08-14T16:49:17.051201+00:00

- fase: **0**
- commit auditado: `5f97e8e905b223e2b661847d21c7a415e4aafc1a`
- session_id: `2abf336c-bff2-4e1b-99c3-260550f418f6`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 0

## VEREDITO: FAIL

Commit auditado: `5f97e8e905b223e2b661847d21c7a415e4aafc1a` (== `main` == `origin/main`; `git diff main..HEAD` vazio, então auditei a árvore inteira, não um diff).

---

## FINDINGS

### BLOCKER

- **[B1] O hook do auditor admite seis formas de escrita não declaradas, todas alcançando o worktree principal**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:21,22,23,26` (allowlist) × `:151-157` (enumeração de flags de escrita)
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(d) — *"Nenhum comando de escrita não declarado passa: qualquer forma nova encontrada é finding, não defeito aceito"* — e item 4(c) — *"Escrita que alcança o worktree principal é finding, não defeito aceito: ela derrota o propósito declarado do hook"*.
  **Evidência:** executei os comandos abaixo de dentro desta sessão de auditoria. Todos retornaram `RC=127` do bash (*command not found*), **não** `BLOQUEADO` do hook — ou seja, o hook devolveu `0` e entregou a linha ao shell:

  | Comando executado | Resultado |
  |---|---|
  | `ruff check --fix /c/Projetos/nao_existe_probe_auditoria_17` | `bash: ruff: command not found` — hook admitiu |
  | `eslint --fix /c/Projetos/nao_existe_probe_auditoria_17` | `bash: eslint: command not found` — hook admitiu |
  | `pytest --basetemp=/c/Projetos/nao_existe_probe_basetemp_17 --version` | `bash: pytest: command not found` — hook admitiu |
  | `mypy --cache-dir /c/Projetos/nao_existe_probe_cache_17 --version` | `bash: mypy: command not found` — hook admitiu |
  | `docker compose config -o /c/Projetos/nao_existe_dir_probe_17/x.yml` | `bash: docker: command not found` — hook admitiu |

  Por que passam, lidas as regras: a negação de flags em `:151-157` está escopada aos comandos `pytest|ruff|mypy|eslint|tsc|sort|git` e enumera **apenas flags de saída** (`-o`, `--output-file`, `--junitxml`, `--outFile`, …). Ela não contém `--fix`, `--basetemp` nem `--cache-dir`, e `docker` não está sequer entre os comandos escopados, embora `docker compose config` esteja allowlistado em `:26` e documente `-o, --output`. A negação de travessia `:126` não alcança nenhum deles, porque a grafia absoluta não tem `..`. `ruff format <caminho>` cai no mesmo buraco por `:23`.

  Efeito de cada forma: `ruff --fix` / `ruff format` / `eslint --fix` **reescrevem arquivos-fonte no lugar**; `pytest --basetemp=DIR` **remove `DIR` se existir** (`rm_rf` em `TempPathFactory.getbasetemp`); `docker compose config -o` grava arquivo; `mypy --cache-dir` grava no diretório indicado. Com grafia absoluta, o alvo é o worktree principal — incluindo `tools/`, que contém os verificadores que o auditor audita.

  Nenhuma das seis aparece em `scripts/phase0_negative_tests.py` nem em `user-scope/hooks/readonly_bash.py`: `grep -rn -- "--fix\|basetemp\|cache-dir\|ruff format\|docker" scripts/phase0_negative_tests.py user-scope/hooks/readonly_bash.py` devolve só `docker` como nome em `COMANDOS_REVISADOS:556` e na allowlist `:26`. Também não aparecem em `docs/progress/fase_0.md`.

  **Por que é blocker:** é o mesmo eixo do B1 da 10ª auditoria e do B1 da 16ª — escrita que sai do worktree de auditoria e alcança a árvore principal —, e o próprio item 4(c) determina que essa classe **não pode ser aceita como defeito declarado**. `ruff check --fix /c/Projetos/aurora-cyber-range/tools` é exatamente o cenário que o hook existe para impedir: o auditor corrigindo em vez de reportar, sobre os verificadores que ele julga. Consequência direta em `docs/progress/fase_0.md:1664-1665`: a 17ª rodada não pode contar como primeira candidata do critério de parada P39.

  **Limite honesto desta evidência:** nenhum dos cinco binários está instalado neste ambiente, então eu **não** consegui provar a escrita por execução, como a 10ª auditoria provou o `find -fprint0`. Provei a admissão pelo hook e o comportamento documentado das ferramentas. As cinco fazem parte da stack declarada em `00_MASTER_SPEC.md` §8 e estão na allowlist justamente para as fases seguintes.

### HIGH

- **[H1] O comentário de `readonly_bash.py` afirma que a superfície de flags é "fechada por comando"; o mecanismo implementado não fecha**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:132-139`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(c) — a superfície aberta tem de ser **declarada**.
  **Evidência:** o comentário diz *"a superficie e FECHADA POR COMANDO — sao as flags de saida que essas cinco ferramentas documentam"* e conclui *"Flag nova encontrada e finding pelo item 4(d) da DoD, nao defeito aceito"*. Duas divergências verificáveis: (i) são **seis** comandos escopados na regex `:151` (`pytest|ruff|mypy|eslint|tsc|sort|git`), e `docker` — allowlistado em `:26` com um subcomando que documenta `-o` — está fora dela; (ii) "flags de saída" não é o conjunto das flags de escrita dessas ferramentas: `--fix`, `format`, `--basetemp` e `--cache-dir` escrevem sem serem flags de saída.
  **Por que é high:** é a mesma classe nomeada no próprio arquivo em `:140-146` como H1 da 14ª auditoria — *"Desenho declarado e mecanismo implementado divergiam"* —, reincidindo dentro do comentário que descreve a correção daquele achado. Enquanto o comentário afirmar fechamento, a próxima revisão vai confiar nele em vez de reabrir o eixo.

### MEDIUM

- **[M1] A célula de evidência do item 4 no registro não bate com a saída do harness deste commit**
  **Arquivo:** `docs/progress/fase_0.md:702`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(b)(e) — o registro é a evidência declarada do item.
  **Evidência:** a célula afirma *"33 formas bloqueadas"*, *"20 leituras legítimas provadas"* e *"4 falsos bloqueios listados"*. A execução de `python scripts/phase0_negative_tests.py` neste commit imprime: *"libera 24 leituras legitimas e bloqueia 36 escritas deliberadas"* e *"5 leituras legitimas bloqueadas e 0 escritas nao bloqueadas"*. Três dos quatro números divergem (33≠36, 20≠24, 4≠5).
  **Por que é medium:** é a linhagem do H2 da 15ª auditoria (`fase_0.md:178`), fechado com *"célula agora traz os quatro números do harness deste commit"*. A célula voltou a ficar defasada, e ela é a única evidência escrita do item 4 para quem não roda o harness.

- **[M2] Curinga em `tools/check_*.py` na allowlist, fechado em `.claude/hooks/` pelo motivo oposto**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:27` contra `:50-51`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(d).
  **Evidência:** `:50-51` lista **nomes explícitos** (`check_architecture|scenario_scope|scenario_bash`), com o comentário `:44-48` explicando que o curinga *"pre-autorizava o auditor a executar qualquer .claude/hooks/*.py que um commit futuro acrescentasse — incluindo um que escrevesse. Era o H1 da setima auditoria"*. A linha `:27` mantém `check_[A-Za-z0-9_.-]+\.py` — curinga sobre `tools/`, diretório do próprio commit auditado.
  **Por que é medium:** o argumento que fechou um dos dois diretórios se aplica literalmente ao outro, e `tools/` é escrito pela fase sob auditoria. Hoje os seis verificadores não escrevem (DoD §Interfaces obrigatórias item 5), então é latente, não ativo.

- **[M3] `npm test` / `npm run lint` / `npm run typecheck` são trampolim de execução arbitrária, não declarado**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:22`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(c) — superfície aberta declarada.
  **Evidência:** os três executam scripts definidos em `package.json` do commit auditado, sem restrição sobre o que o script faz. Nenhum probe em `scripts/phase0_negative_tests.py` toca `npm`, e `npm` consta em `COMANDOS_REVISADOS:555` como revisado. É a mesma propriedade que motivou a remoção de `env` da allowlist (`readonly_bash.py:62-66`, B1a da 11ª auditoria): *"allowlistado sem nenhuma restricao sobre o que invoca, ele e execucao arbitraria"*.
  **Por que é medium, e não blocker:** rodar a suíte do commit auditado é o mandato do auditor, e `pytest` tem a mesma propriedade via `conftest.py`. O finding é a **ausência de declaração**, não a capacidade em si.

- **[M4] `.claude/settings.json` nega `Read`/`Edit` de secrets, não `Write` — 4ª rodada aberta**
  **Arquivo:** `.claude/settings.json:6-15`
  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md` §DoD item 7; `CLAUDE.md` §Secrets.
  **Evidência:** o bloco `deny` tem `Read(.env)`/`Edit(.env)`/… e nenhum `Write(...)`. `docs/progress/fase_0.md:705` afirma que *"`Edit` cobre a ferramenta `Write` por desenho do Claude Code"*; essa premissa não é verificável de dentro do worktree e é a mesma que P13/P19 registram como pendência aberta desde a 3ª rodada.
  **Por que é medium:** reconfirmação declarada, não achado novo. Registro para não sair da lista por esquecimento.

### LOW

- **[L1] Falso bloqueio novo: `git -C <path> log`**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:20`
  **Requisito violado:** §DoD item 4(e) — *"Falso bloqueio novo é finding"*.
  **Evidência:** `git -C . log --oneline -1` devolveu `BLOQUEADO: comando fora da allowlist do auditor` nesta sessão. A regex exige `git\s+(diff|log|…)` imediatamente após o nome. Não consta das cinco entradas de falso bloqueio impressas pelo harness. Impacto baixo: `git log` sem `-C` funciona e foi o que usei.

- **[L2] Falso bloqueio novo: harness com filtro de leitura no pipe**
  **Arquivo:** `user-scope/hooks/readonly_bash.py:41`
  **Requisito violado:** §DoD item 4(e).
  **Evidência:** `python scripts/phase0_negative_tests.py 2>&1 | tail -80` foi bloqueado. A âncora `(?:\s+2>\s*/dev/null)?\s*$` recusa qualquer sufixo, inclusive pipe para filtro de leitura — que o harness afirma como legítimo para outros comandos (*"pipeline com filtro de leitura"*, entre as 24 leituras liberadas). Não consta das cinco entradas declaradas. Impacto baixo: rodei sem o pipe e li a saída inteira.

---

## DoD — item a item

Checklist normativo: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done.

| # | Item | Status | Evidência |
|---|---|---|---|
| 1 | Os seis verificadores liberam árvore limpa | **PASS** | Executados um a um neste worktree: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` — todos `exit=0`, sem saída |
| 2 | Os seis detectam as violações externas do harness | **PASS** | `python scripts/phase0_negative_tests.py` → `EXIT=0`, 13 linhas `OK: <verificador> detectou violacao ... (rc=1)`, cobrindo os dois ramos do invariante 2, isenções ancoradas e os três modos do `codegen` |
| 3 | Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | **PARCIAL** | Executado: import → `RC=2` com `INVARIANTE 1`; `docs/spec/00_MASTER_SPEC.md` → `RC=2` com `SPEC IMUTAVEL`. Literal de flag **não executado** (ver §limites); regra lida em `.claude/hooks/check_architecture.py:40-52` |
| 4 | Separação de papéis do auditor (a–e) | **FAIL** | (a) PASS — `user-scope/agents/checkpoint-auditor.md:4` = `tools: Read, Grep, Glob, Bash`. (b) PASS — `expect_hook_allows:433` e `expect_hook_blocks:446` asseveram nas duas direções, e a segunda também exige mensagem não vazia. (c) **FAIL** — B1. (d) **FAIL** — B1. (e) **FAIL** — L1, L2 |
| 5 | Hook do `scenario-designer` bloqueia Write/Edit fora de `scenarios/` e Bash fora da allowlist | **PARCIAL** | Write executado: `range-core/nope.py` → `RC=2`. Metade Bash **não executada** (ver §limites); allowlist lida em `.claude/hooks/scenario_bash.py:9-13` |
| 6 | `ground_truth.yaml` e `GM_NOTES.md` não estão no `.gitignore` | **PASS** | `.gitignore:39` traz apenas o comentário `IMPORTANT: ground_truth.yaml and GM_NOTES.md are intentionally versioned`; nenhuma regra os ignora |
| 7 | `.env`/secrets negados em `.claude/settings.json` | **PARCIAL** | `:6-15` nega `Read`/`Edit` de `.env`, `.env.local`, `.env.*.local`, `secrets/**` — casa `CLAUDE.md` §Secrets e não casa `.env.example`. Sem `Write` → M4 |
| 8 | Auto Mode desabilitado | **PASS (por leitura)** | `.claude/settings.json:4-5` — `"defaultMode": "default"`, `"disableAutoMode": "disable"`. Efeito em runtime não observável daqui |
| 9 | Push de `main` deixa `arquitetura` e `seguranca` verdes | **NÃO VERIFICÁVEL** | Atestação do operador (`fase_0.md:707`, §6 P34) |
| 10 | PR descartável: `spec_freeze` falha com spec+código | **NÃO VERIFICÁVEL** | Atestação do operador (`fase_0.md:708`) |
| 11 | PR descartável: só-spec exige título `spec-change:` | **NÃO VERIFICÁVEL** | Atestação do operador (`fase_0.md:709`) |
| 12 | Branch protection exige os três checks | **NÃO VERIFICÁVEL** | Atestação do operador (`fase_0.md:710`) |
| 13 | `spec-v1.0` depois de CI + branch protection | **NÃO VERIFICÁVEL** | `git for-each-ref` mostra `refs/tags/spec-v1.0 → f423c04`; a ordem de criação não é observável daqui |
| 14 | `/doctor` sem apontamentos | **NÃO VERIFICÁVEL** | Atestação do operador (`fase_0.md:712`) |
| 15 | *(exigido por `WORKFLOW.md`, não pelo checklist)* auditoria de checkpoint com veredito PASS | **FAIL** | Esta rodada é FAIL |

Registro colateral verificado: o CI invoca os seis verificadores **e** o harness (`.github/workflows/invariants.yml:17,20,23,29,43,139,142`), o que sustenta o item 2 como gate real e não só como script local.

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`allowlist_e_a_revisada()` — `scripts/phase0_negative_tests.py:567-612`.** Prova apenas que todo nome de comando extraído da allowlist pertence ao conjunto `COMANDOS_REVISADOS:554-564`. Não asserta nenhuma propriedade de capacidade de escrita. O probe imprime `OK: allowlist e o conjunto revisado (36 comandos)`, e o comentário `:552-553` afirma *"Cada nome aqui foi examinado quanto a capacidade de escrita; a lista e o registro dessa revisao"*. B1 mostra que, para `ruff`, `eslint`, `mypy`, `pytest` e `docker` — os cinco na lista —, o exame que a lista registra não aconteceu. O docstring é honesto sobre trocar a pergunta indecidível pela decidível; a linha de saída e o comentário de cabeçalho não são, e é a saída que vira evidência de DoD.

2. **`ESCRITA_POR_ALVO` — `scripts/phase0_negative_tests.py:369-378`.** As 32 provas (8 formas × 4 grafias) variam o **alvo** e mantêm fixa a **flag**. Todas as 8 formas são flags de saída ou `find`. Nenhuma forma da matriz é modificação in-place (`--fix`, `format`) ou destrutiva (`--basetemp`). A matriz prova o eixo do alvo, e `fase_0.md:702` a cita como evidência de *"0 escritas não bloqueadas"* — que é uma afirmação sobre o eixo da flag.

3. **`BURACOS_CONHECIDOS = []` — `scripts/phase0_negative_tests.py:404`.** Com a lista vazia, `expect_hook_allows_known_hole:531` nunca é chamada. A linha final *"0 escritas nao bloqueadas"* é um fato sobre o comprimento de uma lista, não um resultado medido contra o hook. Nenhum probe procura forma de escrita que não esteja na lista — que é exatamente o trabalho que o item 4(d) delega ao auditor, e onde B1 apareceu.

4. **Cobertura zero dos hooks de projeto no harness.** `grep -n "check_architecture" scripts/phase0_negative_tests.py` só encontra menção em comentário (`:641`). Os itens 3 e 5 da DoD não têm probe automatizado algum: dependem do bloco manual de `PHASE_0_CHECKLIST.md:64-81`, que não roda no CI. Não é violação de texto de DoD — o item 2 nomeia só os seis verificadores —, mas significa que duas linhas da DoD não são verificadas por nada executável e regridem em silêncio.

Nenhum `skip`/`xfail` sem justificativa foi encontrado; o harness usa `_reject()` com falha dura e não marcadores.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **A escrita efetiva das seis formas do B1.** `ruff`, `eslint`, `mypy`, `pytest` e `docker` não estão instalados neste ambiente (todos `RC=127`). Provei que o hook **admite** as linhas de comando; o efeito de escrita vem do comportamento documentado das ferramentas, não de execução minha. Diferente do B1 da 10ª auditoria, que sobrescreveu um arquivo de fato.
- **Item 3, ramo "literal de flag".** O payload JSON exige aspas duplas escapadas (`\"`) para o conteúdo Python, e `readonly_bash.py:220,277` cai para o lado fechado diante de `\\['\"]`. Bloqueio declarado (família *"smoke test do PHASE_0_CHECKLIST (payload citado)"*). Verifiquei só por leitura de `.claude/hooks/check_architecture.py:40-52`.
- **Item 5, metade Bash.** Todo payload de teste plausível (`pip install`, `git push`, `curl`) casa a negação do meu próprio hook antes de chegar ao `scenario_bash.py`. Verifiquei só por leitura de `.claude/hooks/scenario_bash.py:9-39`.
- **Itens 9 a 14.** `gh` não está na allowlist, estado de check run e de branch protection não é observável de dentro de um worktree, e `/doctor` não é invocável por mim. Permanecem fechados por atestação do operador, exatamente como `fase_0.md:718` e §6 P34 declaram. Não os converti em PASS.
- **Item 8 em runtime.** Li a chave `disableAutoMode` no arquivo; não tenho como confirmar que o Claude Code a honra nesta versão.
- **A conta de rodadas do registro.** `fase_0.md:1606-1612` declara que os relatórios das rodadas 7 a 9 vivem fora de `main`. Não reconciliei — o worktree está fixado no commit candidato e não trouxe nada de outras branches.
- **Se as seis formas do B1 esgotam o eixo da flag.** Não esgotam, e o próprio item 4 §Limite declarado diz que completude não é demonstrável neste desenho. Testei o que construí; não testei o que não construí.
