# Auditoria de checkpoint - 2026-08-21T18:38:14.453838+00:00

- fase: **6**
- commit auditado: `a411bb9bb590d4625a56b23bfa96a1e2166702b3`
- session_id: `18f43b2e-4d00-4eac-95f1-e7f9e15db853`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have what I need. Emitting the report.

```text
# AUDITORIA — FASE 6

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] `observability_hooks.yaml` declara `event_type` que nenhuma ação emite, e nenhum verificador cruza hook × emissor
  Arquivo: `domains/academus/observability_hooks.yaml:35-38` (hook `vpn_access_revoked`, `producer: federated-identity-simulator`)
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T9, primeiro critério, etiquetado **(Fase 6)** — *"Cada `event_type` declarado em `observability_hooks.yaml` é emitido pela ação correspondente."*
  Evidência: o arquivo declara dois hooks. `audit_query_performed` é emitido e está provado por rota real (`tests/test_api_emissao_pela_rota.py`, 13 casos, `TestClient` + store + repositório reais). `vpn_access_revoked` **não é emitido por nada**: o produtor nomeado é da Fase 11. `scripts/check_api_surface.py` (rc=0) confere `emite` × catálogo por rota, e `tools/check_contract_literals.py` (rc=0) confere que o `event_type` do hook está no catálogo — nenhum dos dois pergunta se alguém o emite. Rodei a suíte inteira: 684 testes, `OK (skipped=140)`, e nenhum caso cobre este eixo. O próprio registro da fase mede o mesmo e o diz com todas as letras: `docs/progress/fase_6.md:1038-1045` — *"Não existe verificador cruzando os hooks com os emissores reais… hook com `event_type` válido e produtor inexistente passa em todos os gates"* — e `:1023` — *"o critério é da Fase 6"*.
  Por que é blocker: o critério é da fase que está sendo fechada e está objetivamente não satisfeito. E ele não está apenas pendente de decisão: está **sem gate**. É a forma exata que `04_SCENARIO_SCHEMA.md` §6.2 e `09_EVENT_MODEL.md` §4 chamam de "a falha mais cara possível" — o `event_type` que nunca dispara e que ninguém percebe até o exercício ao vivo —, e a que `06` T10 chama de "registrado e nunca verificado é como um requisito morre sem que nada fique vermelho". O registro escala corretamente (P6-10 propõe duas leituras, uma exigindo `spec-change`), e escalar é o procedimento certo; mas enquanto a decisão não é tomada, T9 não pode ser certificado e a fase não é fechável por `CLAUDE.md` §Fluxo por fase.

### HIGH

- [H1] O qualificador temporal `since` de `absence_of` é declarado no contrato, aparece no exemplo normativo da spec, e é **silenciosamente descartado** pelo avaliador
  Arquivo: `range-core/engine/verificacao.py:117-120`; guarda incompleta em `range-core/engine/loader/pack_loader.py:547` (`FOLHAS_TEMPORAIS = ("before", "after")`)
  Requisito violado: `03_EXERCISE_DESIGN.md` §3.1 (predicado normativo de contenção: `absence_of: {fact_class: exfiltration, since: self}`); `contracts/ground_truth.schema.yaml:315-316` (`since` é campo declarado); doutrina do próprio módulo em `verificacao.py:99-104` — *"`before` e `after` … chegam como `PredicadoMalformado` … recusa alta é o que impede um predicado temporal de passar por 'não satisfeito'"*.
  Evidência: `avalia()` lê `no["absence_of"]`, extrai `fact_class` e **ignora `since`** (`classe = alvo if isinstance(alvo, str) else alvo["fact_class"]`). `_folhas_temporais` enumera só `before`/`after`, então o loader não recusa. Grep sobre `range-core/` inteiro: nenhuma ocorrência de `since` fora de `exercise_clock.py` e de um comentário em `events/store.py` — nenhum código lê a chave. O exemplo **positivo** de `contracts/ground_truth.schema.yaml:496-498` usa `since: self` e valida (`check_contract_examples.py`: 13 positivos, 88 negativos, todos como declarado). Nenhum teste exercita a forma: `tests/test_verificacao.py:120-123` usa `{"absence_of": {"fact_class": "exfiltration"}}`, sem `since`, e a fixture `tests/fixtures/pack_completo.py:213-218` evita `absence_of` por completo (`flag_false`).
  Por que é HIGH: um pack escrito na forma normativa de `03` §3.1 é avaliado com semântica diferente da declarada — a ausência passa a ser exigida sobre a linhagem corrente inteira em vez de a partir do próprio predicado. No caso canônico (`fact_class: exfiltration` materializado antes das ações de contenção), o predicado nunca satisfaz, `verification_predicate_satisfied` nunca é emitido, `TTCV` nunca marca, e o par TTCD/TTCV — entregável central desta fase — perde a metade de verificação **sem que nada falhe**. É a mesma classe que a P6-3 foi criada para fechar, entrando pela terceira folha temporal da mesma gramática, e é pior que a que a guarda cobre: `before`/`after` explodem alto, `since` erra em silêncio para sempre. Não há pendência declarada para ele.

### MEDIUM

- [M1] `confidence` fora da faixa do contrato alimenta o Brier e desloca `TTIV`
  Arquivo: `docs/progress/fase_6.md:1058-1095` (P6-11); contrato em `contracts/assessment.schema.yaml` (`confidence` 0–100 inteiro)
  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 6, item 8 (*"`TTIV` computado por limiar de calibração"*); `03_EXERCISE_DESIGN.md` §5.3.
  Evidência: medição registrada na própria árvore — `confidence: 900 -> brier = 64.0`, sessenta e quatro vezes o pior escore de uma entrada válida. O contrato bane; o event store não valida payload, e nenhum computador recusa. Consequência declarada: o instante de `TTIV` desloca ou nunca é atingido.
  Ressalva: está **declarado e mapeado**, com o lugar da correção escolhido (no computador, como cálculo do consumidor) e a única questão aberta sendo ignorar × recusar. Fica MEDIUM, e não HIGH, por isso.

### LOW

- [L1] `03` §3.4 exige que `TTID` seja o `integrity_validation_declared` que completa a contrassinatura; o computador aplica o predicado corretamente, mas `docs/progress/fase_6.md:166-169` registra que **não existe mecanismo de mapeamento declarado entre versões de rubrica**, e `comparavel()` recusa de forma total.
  Arquivo: `range-core/objectives/projecao.py` (`comparavel()`)
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T9, quarto critério — *"comparação entre versões distintas é recusada sem mapeamento declarado"*.
  Evidência: a recusa total satisfaz a letra do critério (recusa sem mapeamento), e a metade de renderização é T14/Fase 10. Registrado só porque o mecanismo de mapeamento não existe e a frase pode ser lida como exigindo-o.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Consulta da trilha com filtro de período emite `audit_query_performed` | PASS | `tests/test_api_emissao_pela_rota.py` — rota real, token, store e repositório reais; 13 casos incluindo os negativos (`422` sem emitir, sem token não consulta, consulta vazia emite com `result_count: 0`) e `test_a_rota_de_fato_consultou_o_repositorio` |
| 2. Cada par declaração × verificação é disparável e testado | PASS (ver [H1]) | `tests/test_laco_de_verificacao.py::test_ttcv_marca_o_instante_que_o_laco_emitiu`, `test_sem_o_laco_ttcv_nunca_marca`; `tests/test_metrics_declaracao.py` (TTCD/TTRD/TTID) |
| 3. Desconto de `technical_failure` pela **união**, nunca pela soma | PASS | `tests/test_metrics_epoch.py::UniaoNuncaSoma` (6 casos: sobrepostos, contido, adjacentes, disjuntos, ordem, vazio) e `::test_o_desconto_sobre_eles_e_a_uniao_e_nao_a_soma` |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | PASS | `tests/test_metrics_epoch.py::EnsaioDescartaAEpoch`; `tests/test_metrics_declaracao.py::test_rehearsal_tira_a_epoch_do_calculo` |
| 5. Declaração não altera nenhum valor de ground truth | PASS | `tests/test_simulation_state.py::test_declaracao_NUNCA_move_flag` (as seis declarações) **com ramo plantado** em `test_simulation_state_probes.py` |
| 6. Declaração prematura produz lista de incompatíveis no AAR | PASS | `tests/test_aar_timeline.py::AJanelaDeAsseguracaoPrematura` e `::OsEventosIncompativeis`, incluindo `test_o_fato_ANTES_da_declaracao_nao_e_listado` e `test_acao_de_participante_na_janela_NAO_entra` |
| 7. Pack sem `verification_predicates` não carrega | PASS | `tests/test_pack_completo_materializado.py::test_completo_sem_verification_predicates_RECUSA`, com controle positivo (`test_o_pacote_completo_VALIDO_carrega`) e o negativo-do-negativo (apenas-manifesto continua carregando) |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | PASS (ver [M1]) | `range-core/metrics/verificacao.py` + `tests/test_metrics_verificacao.py` |
| 9. Brier só no escopo revisado; lacuna de cobertura em separado | PASS | `tests/test_metrics_calibracao.py` — `test_caso_fora_do_escopo_nao_entra_no_brier`, `test_indevido_fora_do_escopo_vira_lacuna`, `test_a_lacuna_NAO_entra_no_brier`, `test_os_dois_sinais_convivem_e_nao_se_anulam` |
| 10. Rubrica ausente ou em versão divergente impede a carga | PASS | `tests/test_pack_loader.py::test_rubrica_ausente_da_biblioteca_impede_a_carga` e `::test_rubrica_em_versao_que_a_biblioteca_nao_tem_impede_a_carga` |
| 11. Divergência ≥ 2 pontos entre avaliadores gera alerta | PASS | `tests/test_aar_timeline.py` — `test_dois_pontos_exatos_alertam`, `test_um_ponto_nao_alerta`, `test_a_distancia_e_entre_os_EXTREMOS`, `test_um_avaliador_so_nao_diverge` |
| 12. As nove siglas classificadas pelo critério de `00` §3.2, batendo com `03` §3.0 | PASS | `tests/test_derivacao_das_nove_siglas.py` — a tabela é **lida do documento** e a conjunção é reaplicada célula a célula; `ATabelaContraOCodigo` cruza com as siglas produzidas |
| 13. Todo `event_type` tem `metric_side`; os quatro lados são disjuntos | PASS | `contracts/events.schema.yaml:692-747` (33 tipos: 7/9/5/12); cobertura e disjunção em `range-core/engine/loader/contract_rules.py:123-155` |
| 14. Folha de predicado obedece à conjunção de `09` §4.0, com probe das três violações e controle positivo | PASS | `contracts/ground_truth.schema.yaml:647-746` — as três fixtures isolando cada perna, mais o controle positivo; executadas por `check_contract_examples.py` (88 negativos, cada um recusado pela camada que declara) |
| 15. Cada computador recebe só o seu lado; só o AAR tem as duas metades | PASS | `tests/test_metrics_insumo.py::CadaLadoRecebeOSeu`; `tests/test_aar_timeline.py::test_a_janela_NAO_sai_de_computador_de_metrica_nenhum`; `scripts/check_insumo_de_metrica.py` + probes (6 violações plantadas reprovam, o consumidor que só anota passa, a árvore limpa passa) |
| 16. União, exclusão de `rehearsal` e seleção de start são cálculo do consumidor | PASS | `tests/test_metrics_epoch.py::OInsumoChegaInteiroEODescontoECalculo` — afirma a **presença** da escrituração nos dois lados **antes** da conta, que é o que distingue "certo por cálculo" de "certo por ausência de insumo" |
| 17. `inject_fired` emite os marcadores de start no payload, com teste de emissão | PASS | `tests/test_inject_engine.py::MarcadoresDeStartNoPayload` — os dois marcadores em todo disparo, e `test_o_carimbo_e_o_que_a_carga_derivou` (sem segunda avaliação do predicado) |
| **T9 (`06`), bullet 1 — todo `event_type` do `observability_hooks.yaml` é emitido** | **FAIL** | **[B1]** — `vpn_access_revoked` sem produtor, e sem verificador que o cobre |
| Fase 4, DoD item 1 — sequência do DEMO ponta a ponta | **NÃO VERIFICADO** | `check_provas_de_container.py` recusa (rc=2): prova `demo` com rc=125, stack não subiu (`docker compose up` = 1) |
| Fase 4, DoD item 4 / T5 — reinício do container do engine | **NÃO VERIFICADO** | idem, prova `reinicio` com rc=125 |

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado — e isto é afirmação medida, não impressão. Procurei especificamente as formas do meu mandato e o que achei foi o contrário delas:

- **Isolamento de papel por payload, não por UI**: `scripts/check_api_surface.py` reprova nos **69 eixos** exercitados pelos probes, com a direção inversa (rota implementada e ausente da declaração) em primeiro lugar, mais a vacuidade (`a funcao que monta o payload desapareceu`).
- **Asserção sobre mock**: os testes de emissão pela rota usam `TestClient`, store real e repositório que registra chamadas — `test_a_rota_de_fato_consultou_o_repositorio` existe precisamente para separar "emitiu" de "consultou e emitiu".
- **Teste que mede a si mesmo**: o de `bars_score_payload` foi corrigido de literais escritos no próprio teste para `$ref` entre contratos (`test_o_payload_NAO_reenumera_as_nove`, `test_competencia_ARBITRARIA_e_recusada`).
- **Verificador sem prova negativa**: 22 dos 23 verificadores de `scripts/` têm prova negativa (`check_readme_atual.py`, rc=0). `check_insumo_de_metrica_probes` prova ainda o **positivo** — o consumidor que só importa e anota passa —, sem o qual uma checagem que reprovasse todo import passaria em todos os negativos.
- **`skip`/`xfail` sem justificativa**: nenhum. Os 140 skips são todos condicionais a serviço ausente (`skipIf(_URL is None, RAZAO)`), com razão declarada, e **nenhum** deles cobre entregável da Fase 6.
- **Invariantes arquiteturais**: `check_core_boundary.py`, `check_event_envelope.py`, `check_contract_literals.py` e `codegen.py --check` saem todos com rc=0.

A única ressalva de qualidade de teste que registro está embutida em [H1]: a fixture do pacote completo e o teste de `absence_of` usam formas que **não** exercitam `since`, e é por isso que a divergência sobrevive a uma suíte verde de 684 testes.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Itens 1 e 4 da DoD (provas de container).** `check_provas_de_container.py` **recusa**, rc=2: as duas provas saíram com rc=125 porque `docker compose up` falhou (`failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`). São NÃO VERIFICADO, e a checagem explicitamente não degrada para "ok". Nunca PASS por silêncio.
2. **Prova do seed completo (M2 da Fase 5).** `check_prova_do_seed.py` **recusa**, rc=1: a prova foi gravada sobre `c119718c615fd3a614231a91d56ff646ff14ed71` e este checkout é `a411bb9b…`. Conforme o enunciado, a medição foi **TRANSPORTADA** e não medida nesta rodada — a divergência de SHA é o caso normal, não anomalia. Não afirmo nada sobre ela.
3. **Tudo que exige Postgres e Redis.** 140 testes pularam. Isso inclui o event store Postgres, o cache de projeção, a restauração em outro processo e as projeções contra banco. As métricas, o AAR, a calibração, os objetivos, as rubricas, o loader e a emissão pelas rotas **não** dependem de serviço e rodaram inteiros.
4. **O laço contínuo em produção.** `range-core/api/processo.py:145-149` monta o `LacoDeVerificacao` com o pack real; essa composição só é exercitada com banco, e o que verifiquei foi a montagem por leitura mais os testes em memória de `test_laco_de_verificacao.py`.
5. **A semântica pretendida de `since: self`.** Constatei que o campo é declarado e não lido. **Não** consigo determinar, com os documentos disponíveis, qual das leituras possíveis de `since: self` é a normativa — e por isso [H1] afirma a divergência (declarado × ignorado), não qual comportamento seria o correto.
6. **A cópia instalada do hook do auditor.** Comparei o diff de `user-scope/hooks/readonly_bash.py` com a cópia em `~/.claude/hooks/` e as entradas novas conferem; não tenho como provar que as duas são idênticas em todo o resto (é a P6-9, declarada).

---

**Nota de método, porque o veredito depende dela.** [B1] não é achado de desonestidade: `docs/progress/fase_6.md` declara a P6-10 com clareza, mede o agravante e escala corretamente — inclusive apontando que uma das saídas exige `spec-change`, que `CLAUDE.md` proíbe no mesmo PR. O que o torna blocker não é ocultação, é que um critério de aceitação **desta fase** está objetivamente não satisfeito **e sem gate**, e a fase não é fechável antes da decisão do operador. [H1], ao contrário, é divergência não declarada e sem teste, e é a que eu recomendaria olhar primeiro.
