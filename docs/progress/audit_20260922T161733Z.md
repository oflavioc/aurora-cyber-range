# Auditoria de checkpoint - 2026-09-22T16:17:33.808976+00:00

- fase: **9**
- commit auditado: `3b3e9c2a5138daf923de2cf3f7f332e2faf2000a`
- session_id: `78344577-c527-4e86-b0e5-00fc0500a8b2`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have completed the static audit. Writing the report.

---

# AUDITORIA — FASE 9

## VEREDITO: FAIL

> **Aviso de método, antes dos achados.** O worktree de auditoria (`.aurora-worktrees/audit`, o cwd desta sessão) **está vazio** — sem checkout. `git ls-files` devolve zero entradas, `python scripts/check_provas_de_container.py` falha com `No such file or directory`, e o hook `readonly_bash` recusa qualquer caminho fora do cwd. **Nenhum teste foi executado nesta auditoria, e nenhum verificador foi rodado.** Todo o trabalho abaixo é leitura de blobs por `git show HEAD:<path>` (que é a forma canônica da R2 §2) e leitura dos três artefatos de prova presentes no cwd. Os BLOCKERs abaixo são estáticos e independem de execução; mas, ainda que não existissem, **eu não teria como emitir PASS** — ver "O QUE EU NÃO CONSEGUI VERIFICAR".

Base confirmada: `docs/process/phase_anchors.tsv:51` grava a âncora da Fase 9 como `c77120c70a92bb033b0266091291b79914042596`, idêntica à base declarada pelo lançador. A auditoria é **porta**.

---

## FINDINGS

### BLOCKER

- **[B1] O `cef.log` e o `telemetry_emitted` são duas emissões independentes, não uma projeção sob um contrato só — o item 4 da DoD não é cumprido**

  **Arquivo:** `domains/academus/evidence_generators/cef.py:78,88,97-98` × `range-core/telemetry/forwarder.py:105-110`

  **Requisito violado:** `08_EVIDENCE_SIMULATOR.md` §2 ("A telemetria CEF é projeção, não emissão independente. Isso unifica evidence-simulator e telemetry-forwarder sob **um contrato só**"); `08` §1 ("Contradição entre fontes torna-se estruturalmente impossível"); `02_DOMAIN_ACADEMUS.md` §10; `contracts/events.schema.yaml:730,745` e o comentário imediatamente acima ("**o mesmo fato produz a linha do `cef.log` e este payload**"); `07_IMPLEMENTATION_PHASES.md` §Fase 9, item 4.

  **Evidência:** para o **mesmo fato**, as duas metades produzem sinais diferentes em quatro eixos:

  | eixo | `cef.log` (`cef.py`) | `telemetry_emitted` (`forwarder.py`) |
  |---|---|---|
  | Device Event Class ID | `fato["fact_class"]` → `initial_access`, `exfiltration` (`cef.py:78`) | `entrada.signature` → `SERVICE_ACCOUNT_ANOMALY`, `BULK_STUDENT_EXPORT` (`forwarder.py:105`) |
  | severity | `SEVERIDADE_PADRAO = 5`, fixo (`cef.py:51,88`) | 7 e 9, do catálogo (`forwarder.py:106`) |
  | `outcome` | ausente — e `02` §10 o lista entre os campos CEF | presente (`forwarder.py:108-109`) |
  | `cs1`/`cs2` | `credential_state`, `mfa` (`cef.py:97-98`) | deliberadamente ausentes |

  Nenhum dos `fact_class` escritos no arquivo pertence ao enum fechado `$defs/telemetry_signature` (`contracts/events.schema.yaml:730`) — `initial_access` ≠ `SERVICE_ACCOUNT_ANOMALY`, `grade_change_retroactive` ≠ `GRADE_CHANGE_RETROACTIVE`. O núcleo **recusa na carga** assinatura fora do enum (`range-core/telemetry/catalogo.py:~137`), e o arquivo a escreve livremente.

  Os conjuntos de fatos também divergem: `cef.log` cobre apenas os 2 fatos com `projections: [..., "cef"]` (`linha_a.py:147,168`), enquanto `programar()` (`forwarder.py:93-133`) não consulta `projections` e emite para **todos** os fatos cujo `fact_class` está no catálogo — incluindo `phishing_delivery` → `AUTH_FAIL` (`telemetry_events.yaml:79-83`, fato com `projections: ["email","precursor"]`) e todos os `grade_change_retroactive` da Linha B (`gabarito.py:170`, `projections: ["database_audit"]`).

  O próprio `forwarder.py:60-63` enuncia a regra correta de `cs1`–`cs4` ("`02` §10 os reserva para contexto do DOMINIO… Preenche-los com outra coisa seria usar o campo reservado para o que couber") e `cef.py:97-98` a viola — a mesma fase, os dois lados do mesmo item.

  **Por que é blocker:** é exatamente o dano que `08` §1 existe para impedir. O time azul abre `cef.log` e lê `initial_access|vpn_login|5`; a visão de SIEM alimentada por `telemetry_emitted` mostra `SERVICE_ACCOUNT_ANOMALY|7|success` para o mesmo fato, mais quatro classes de sinal que o arquivo não tem. A contradição entre fontes aparece no meio do exercício, que é o modo de falha nomeado. O item 4 da DoD está declarado **VERDE** em `docs/progress/fase_9.md:353` sobre esta implementação.

- **[B2] O teste que sustenta o item 4 compara o payload contra um `cef.log` que não existe em disco, e só sobre os dois campos que não podem divergir**

  **Arquivo:** `tests/test_telemetry_forwarder.py:138-140` (setUp) e `:147-166` (o caso)

  **Requisito violado:** `06_ACCEPTANCE_TESTS.md` T13; `08` §2; `07` §Fase 9 item 4. Citado como a prova do item em `docs/progress/fase_9.md:353` ("A prova é de valor, não de estrutura: o `src` do evento e o do `cef.log` são comparados lado a lado").

  **Evidência:** três defeitos independentes no mesmo caso.

  1. **O oráculo é fabricado.** `setUp` chama `cef_mod.fabricar(...)(self.fatos)` — o gerador cru, com **todos** os fatos —, contornando `projetar()` e o filtro de cobertura. O `self.cef` do teste tem 4 linhas; o `cef.log` que `evidence build` escreve tem 2. O teste não pode detectar que o fluxo de eventos cobre fatos que o arquivo não cobre, porque compara contra um artefato que o produtor nunca emite. (O caso irmão em `tests/test_evidence_generators.py:263` usa `_projetar(...)` e portanto o arquivo real — a divergência de método entre os dois é o que esconde o problema.)
  2. **A asserção é `assertIn` sobre o arquivo inteiro** (`:166`), não sobre a linha do mesmo fato. Como todos os fatos da Linha A compartilham `actor = ATOR_DO_INCIDENTE` (`linha_a.py:141,157,164`), o caso passa para `GT-A-019` (`privilege_escalation`), que **não tem linha nenhuma** em `cef.log` — `projections: ["identity_audit"]` (`linha_a.py:159`). O nome do teste diz "do mesmo fato"; a asserção não o verifica.
  3. **Cobre só `source_ip`→`src` e `actor`→`suser`** (`:161`) — precisamente os dois campos que ambos os lados copiam literalmente do mesmo dicionário e que, por construção, não podem divergir. Os quatro eixos que **de fato** divergem (B1) ficam fora.

  As cinco mutações de `tests/test_telemetry_forwarder_probes.py:88-140` mutam **apenas** `forwarder.py` e `catalogo.py`; nenhuma toca `cef.py`. Não há mutante que mate a divergência do lado do arquivo, e o registro de mutantes de `tests/test_evidence_generators_probes.py:147-156` só ataca o `vendor`.

  **Por que é blocker:** é o padrão que esta auditoria existe para pegar — o item de DoD está VERDE, a suíte está verde, e o requisito não está provado. Pior: dois casos da mesma fase **asseveram a divergência** como correta — `test_a_severidade_vem_do_CATALOGO_e_nao_do_fato` (`test_telemetry_forwarder.py:168`) fixa a severidade do evento no catálogo enquanto `test_NAO_julga_severidade_por_fato` (`test_evidence_generators.py:287-292`) fixa a do arquivo num valor único. A suíte congela a contradição.

### HIGH

- **[H1] A classe `evidencia` do gate de `05` §4 continua declarada como não coberta, com a Fase 9 nomeada como dona — e a Fase 9 produziu o artefato**

  **Arquivo:** `scripts/check_banner_de_simulacao.py:109` (`"evidencia": (False, "Fase 9 — …")`), e `:41`, `:227-236`

  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §4; `06_ACCEPTANCE_TESTS.md` T13 ("Banner de ambiente simulado na primeira linha de cada arquivo"); R1 INV-10, cujo gate mapeado é este script.

  **Evidência:** `check_banner_de_simulacao.py` não aparece no diff `c77120c..3b3e9c2`. O registro `CLASSES_DA_SECAO_4` segue com `evidencia: (False, "Fase 9 …")`, e o `main()` (`:227-236`) continua imprimindo "Classes ainda sem artefato, com dono: evidencia (Fase 9 …)" — afirmação que se tornou falsa nesta fase: `range-core/evidence/build.py::construir` agora escreve os arquivos e `range-core/evidence/banner.py` põe o banner. O próprio registro da fase reconhece a dona em `docs/progress/fase_9.md:592-595` ("a classe `evidencia` do mesmo gate … **é** desta fase … e `check_banner_de_simulacao.py` já a defere nominalmente para cá"), e mesmo assim o item 5 é declarado VERDE (`:354`) sem dispor do sinalizador nem abrir pendência para ele em §6.

  **Por que é high:** o gate nomeado de uma norma de `05` — documento que "não admite exceção" — atravessa a fase que o devia fechar ainda dizendo que a fase o deve. A cobertura existe em teste unitário (`test_evidence_projecao.py:111-161`, `test_evidence_build.py:115`), mas o mecanismo de CI que impede a *próxima* fase de produzir artefato sem banner continua cego para a classe. Não elevo a blocker porque `scenarios/` está fora do Git e o gate varre a árvore — o caminho de correção é decisão, não obviedade.

### MEDIUM

- **[M1] O número do item 7 no registro não é o número da prova amarrada à árvore, e a margem que justifica a escolha de volume não se sustenta sobre ela**

  **Arquivo:** `docs/progress/fase_9.md:356` ("**VERDE** — 1,884 s contra 3 s … 42% de folga, reprodutível") e `:253` (tabela §2.8: "400 — o declarado … **1,729 s** … PASSA, 42% de folga") × `.aurora-prova-do-exercicio-4h.json:33` (`com_telemetria.total_s = 2.119512399949599`)

  **Requisito violado:** R2 §1 ("Todo PASS cita o gate executado, com contagem"); `06_ACCEPTANCE_TESTS.md` T3/T12 (máquina, data e stack declaradas junto do número).

  **Evidência:** o único artefato amarrado a esta árvore (`tree: cd735acfde76d508cf852d2009699919c9c07aae`, idêntico ao `git rev-parse HEAD^{tree}` deste checkout) registra **2,1195 s** para 96.650 eventos, `passa: true`. O registro cita dois números diferentes — 1,884 s na §3 e 1,729 s na §2.8 — e nenhum deles está na prova. A folga real sobre o registrado é **29,3%**, não 42%. Como a §2.8 usa exatamente "42% de folga, reprodutível" como o argumento que descarta 500/min em favor de 400/min, o argumento é feito sobre um número que a prova não sustenta. O **veredito do item 7 não muda** (2,1195 s < 3,0 s, e a variância declarada de ~0,45 s mantém o pior caso abaixo do orçamento) — o defeito é de citação, e é o tipo que envelhece sem ninguém perceber.

- **[M2] T13 exige consistência mútua de usuário, IP e timestamp entre as projeções de cada fato, e nenhum teste a asserta**

  **Arquivo:** `tests/test_evidence_build.py:243-253` (`test_a_conferencia_e_DIRIGIDA_POR_FATO`); `tests/test_evidence_generators.py:278-285`

  **Requisito violado:** `06_ACCEPTANCE_TESTS.md` T13, primeiro critério: "Para cada `fact_id`, todas as projeções declaradas existem **e apresentam usuário, IP e timestamp mutuamente consistentes**. Teste dirigido por fato, não por seed."

  **Evidência:** a metade "dirigido por fato" está cumprida — `test_a_conferencia_e_DIRIGIDA_POR_FATO` cruza `projects_facts` do manifesto com os fatos que declaram `projections`, sem passar por seed. A metade da **consistência mútua** não tem caso: o único que se aproxima, `test_o_mesmo_fato_aparece_no_CEF_e_na_outra_fonte_dele` (`test_evidence_generators.py:278`), cobre **um** fato (`initial_access`) e **um** campo (`source_ip`), sobre duas fontes. Não há iteração por `fact_id` conferindo ator, endereço e instante em todas as projeções declaradas daquele fato. A garantia é arquitetural (`08` §1, e os geradores realmente lêem o mesmo dicionário), e a reprojeção de `conferir` amarra disco↔projetor — mas nenhuma das duas afirma consistência *entre fontes*, e geradores que transformam valores existem (`email.py::_rotulo`, `_`→`-`).

- **[M3] `programar()` não consulta `projections`, e o limite de detecção deliberado de `08` §2 não o alcança**

  **Arquivo:** `range-core/telemetry/forwarder.py:93-133`

  **Requisito violado:** `08_EVIDENCE_SIMULATOR.md` §2 ("Fato sem `projections` é invisível ao time azul — deliberado, e usado para ensinar limite de detecção"); `09_EVENT_MODEL.md` §4.1 (`telemetry_emitted` é `truth_layer: observable_evidence`).

  **Evidência:** o único filtro de `programar` é `catalogo.entrada_de(fact_class) is None` (`:100-103`). `projections` não é lido. Como `telemetry_emitted` é evidência **observável** pelo catálogo, um fato deliberadamente sem `projections` cujo `fact_class` esteja no catálogo chegaria ao time azul pelo canal de telemetria. Hoje nenhum fato do `ransomware-universidade` está nessa condição (a peça 3 fechou a lacuna da Linha B, `gabarito.py:170`), então o defeito é latente quanto à invisibilidade — mas já é **concreto** quanto à assimetria arquivo×evento descrita em B1. Nenhum teste cobre a condição: `test_fato_sem_assinatura_NAO_vira_telemetria` (`test_telemetry_forwarder.py:230`) julga `fact_class` fora do catálogo, não `projections` ausente.

### LOW

- **[L1] `range_cli/cli.py` lê o `banner_text` por caminho próprio em vez do ponto único de leitura**

  **Arquivo:** `range_cli/cli.py` (`_contexto_de_evidencia`, linha `contratos["evidence"]["x-aurora-security-constraints"]["banner_text"]`)

  **Requisito violado:** R9 §8 (helper único por semântica de invariante); o argumento da P1-13 que `range-core/evidence/banner.py:~120` (`texto()`) e `contract_source.restricoes_de_evidencia` existem para executar.

  **Evidência:** `banner.texto(contratos)` e `restricoes_de_evidencia(contratos)` foram criados nesta fase precisamente para serem o ponto único, com falha fechada nomeada quando a chave falta (`banner.py`, `ContractSourceError`). O chamador do CLI indexa o dicionário direto: não duplica a *string* (o achado da P1-13), mas cria um segundo caminho de acesso que levanta `KeyError` cru em vez da recusa nomeada. Não é divergência de valor; é divergência de superfície.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Toda fonte é projeção de `fact_id`; nenhum gerador inventa entidade | **NÃO VERIFICADO** (leitura estática coerente) | `elenco.py::elenco_de/cobertura_de/enderecos_no` são funções puras do documento, sem seed; `projecao.py::projetar` transforma o oráculo em porta (`EntidadeInventada`, `IOCEncontrado`) e filtra por `cobertura_de`. 18+29+27 casos declarados em `expected_suites.json`, conferidos por contagem estática de `def test` (18/29/27 — batem). **Nenhum executado.** |
| 2 — `range-cli evidence verify` dirigido por fato | **NÃO VERIFICADO** (leitura estática coerente) | `build.py::conferir` reprojeta em memória e compara; ordem de dependência declarada; `_evidence_verify` não escreve. `test_a_conferencia_NAO_ESCREVE_nada` compara `st_mtime_ns` antes/depois. 28 casos declarados, 28 contados. **Nenhum executado.** Ressalva M2. |
| 3 — `precursor_events.jsonl` reproduzível; edição manual detectada por hash | **NÃO VERIFICADO** (leitura estática coerente) | `precursor.py` projeta do fato de phishing; `NOMES_PROPRIOS` em `projecao.py` fixa o nome que a spec usa; `conferir` empilha `sha256` e reprojeção, com casos distintos para edição simples e para conluio manifesto+arquivo (`test_evidence_build.py:181,199,289`). **Nenhum executado.** |
| 4 — Telemetria CEF é projeção, não emissão independente | **FALHA** | **B1** e **B2**. Quatro eixos de divergência entre `cef.log` e `telemetry_emitted` para o mesmo fato, conjuntos de fatos diferentes, assinatura fora do enum fechado no arquivo; e o teste que sustenta o item compara contra um artefato fabricado, por `assertIn` no arquivo inteiro, sobre os dois únicos campos que não podem divergir. |
| 5 — Nenhum anexo, binário, IOC real ou domínio roteável (+ banner na 1ª linha) | **PARCIAL / NÃO VERIFICADO** | Metade IOC: `projecao.py::_achados_no_texto` tokeniza e delega a `dados_sinteticos.achados_no_valor` — o mesmo predicado do CI e do loader —, com import declarado em `scripts/check_core_contract_imports.py:254`. Metade banner: `banner.py` por formato de fio, com falha fechada e caso negativo de posição. **H1**: o gate nomeado de `05` §4 continua declarando a classe `evidencia` como não coberta, com esta fase como dona. **Nenhum teste executado.** |
| 6 — Replay respeita o clock de exercício | **NÃO VERIFICADO** (leitura estática coerente, com limite) | `Replay.tick` lê `clock.elapsed_seconds()`, nunca relógio de parede; `_emitidos` impede reemissão; a pausa é consequência do congelamento do exercise-clock (`01` §3). Limite declarado em `forwarder.py:~50`: `programar` agenda **tudo** em `em_segundos=0`, então o caminho real nunca exercita o vencimento — só `Programado` sintético o faz. O limite é atribuído à P6-3, que está na tabela §6. **Nenhum caso executado.** |
| 7 — Reconstrução < 3 s com `telemetry_emitted` no volume de 4 h | **PASS** | `.aurora-prova-do-exercicio-4h.json`: `com_telemetria.total_s = 2,1195 s`, `orcamento_s = 3.0`, `eventos = 96.650` (`telemetria = 96.000`, 400/min), `passa: true`, `item_7_reconstrucao_com_telemetria: true`. `tree = cd735acf…` **idêntico** ao `HEAD^{tree}` deste checkout. Máquina/data/stack declarados. Ressalva **M1** quanto ao número citado no registro. |

**Provas herdadas, conforme o lançador:**

| Prova | Status | Evidência |
|---|---|---|
| Provas de container (P4-10) — **itens 1 e 4 da DoD da Fase 4** | **NÃO VERIFICADO** | `.aurora-provas-de-container.json`: `tree` bate, mas `stack.rc = 1` — `container aurora-provas-range-api exited (1)`; as duas provas vêm `rc: 125`, "NAO EXECUTADA". O diagnóstico (`ps`, `logs`) veio `rc=3221225794` (0xC0000142), então nem a causa foi capturada. Nada em `docker-compose.yml`, `Dockerfile`, `pyproject.toml` ou no caminho de boot de `range-api` mudou nesta fase (diff vazio nesses paths; `postgres_store.py` só ganhou comentário), o que **não** descarta regressão — apenas não a localiza. |
| Prova do seed completo (M2 da Fase 5) — **itens 1 e 2 da DoD da Fase 5** | **NÃO VERIFICADO** | Artefato ausente do worktree; `check_prova_do_seed.py` recusa por ausência, e ausência não degrada para "ok". |
| Prova do exercício de 4 h — **itens 6 e 9 da DoD da Fase 7** | **PASS** | Mesmo arquivo: `item_6_dryrun_todos_os_caminhos: true`, `item_9_reconstrucao_em_menos_de_3_s: true` (0,0219 s), `lint_achados: []`, `tree` confere, SHA-256 dos sete arquivos do pack declarados. |

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_telemetry_forwarder.py:147` — `test_os_VALORES_do_payload_aparecem_na_linha_CEF_do_mesmo_fato`.** O achado central (B2). Oráculo fabricado por `cef_mod.fabricar(...)(self.fatos)` em vez do `cef.log` produzido; `assertIn` sobre o arquivo inteiro em vez da linha do fato; cobertura restrita a `src` e `suser`, os dois campos que não podem divergir. Passa para `GT-A-019`, que não tem linha no arquivo.
2. **`tests/test_evidence_generators.py:287` — `test_NAO_julga_severidade_por_fato`** e **`tests/test_telemetry_forwarder.py:168` — `test_a_severidade_vem_do_CATALOGO_e_nao_do_fato`.** Individualmente corretos; em conjunto, congelam como esperada a divergência de severidade entre as duas metades do item 4. Um par de testes verde que documenta uma contradição.
3. **`tests/test_evidence_generators.py:278` — `test_o_mesmo_fato_aparece_no_CEF_e_na_outra_fonte_dele`.** Docstring diz "A unificação sendo verificada". Verifica um fato, um campo, presença por `assertIn`. É spot-check, não a consistência mútua que T13 cobra (M2).
4. **Lacuna de mutante.** `tests/test_telemetry_forwarder_probes.py:88-140` planta cinco mutações, todas em `forwarder.py`/`catalogo.py`. Nenhuma em `cef.py`. `tests/test_evidence_generators_probes.py:147` só muta o `vendor`. Não existe mutante que mate a divergência do lado do arquivo — consistente com ela ter passado.

Fora isso, as suítes novas são sólidas: nenhum `skip`, `xfail` ou mock; as contagens de `expected_suites.json` batem com a contagem estática de `def test` em todas as seis suítes principais (18/29/27/28/20/11); os probes de `test_evidence_build_probes` e `test_evidence_generators_probes` documentam mutantes que *já encontraram* gates frouxos (§2.4, §2.5, §2.7 do registro), o que é o uso correto do instrumento.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Nada foi executado.** Esta é a limitação dominante e ela invalida o modo normal desta auditoria. O cwd (`.aurora-worktrees/audit`) contém apenas `.aurora-pack/`, `.aurora-prova-do-exercicio-4h.json` e `.aurora-provas-de-container.json` — o checkout não foi materializado. `git rev-parse --show-toplevel` aponta para o diretório **pai**, onde a árvore está, e o hook `readonly_bash` recusa tanto `../` quanto o caminho absoluto do pai ("o worktree de auditoria É o objeto da auditoria"). Consequência: **zero testes rodados, zero verificadores rodados**, apesar de o lançador declarar Postgres e Redis no ar. Meu mandato diz "confie no que os testes provam"; nesta rodada não pude. Mesmo sem B1/B2, o veredito não poderia ser PASS.
2. **`scripts/check_provas_de_container.py`, `check_prova_do_seed.py` e `check_prova_do_exercicio_4h.py` não foram executados** — os arquivos não existem no cwd. Conferi a amarração das duas provas presentes **à mão**, comparando o campo `tree` com `git rev-parse HEAD^{tree}` = `cd735acfde76d508cf852d2009699919c9c07aae`: as duas batem. Não pude conferir os SHA-256 dos sete arquivos do pack (o pack vive fora do Git e não está aqui) nem executar o modo declarado do verificador.
3. **Pins (R8).** `.claude/verify/pins.json` foi regenerado (`gerado_de_head: 1ffe918`, o commit anterior; HEAD só altera o próprio `pins.json`) e traz entradas para os sete módulos novos de `evidence_generators/`, `telemetria.py` e `telemetry_events.yaml`. **Não pude conferir nenhum hash**: `gen_pins.py` não existe no cwd e o hook recusa `python -c` com `hashlib`. A conformidade estrutural é o que afirmo; a identidade dos hashes, não.
4. **Invariantes arquiteturais 1–4.** Li os módulos novos de `range-core/` e não há import de `domains/` (`projecao.py` importa `dados_sinteticos`, pacote de topo, com o motivo declarado em `scripts/check_core_contract_imports.py:254`); `medida_do_exercicio_4h.py` usa a constante `TELEMETRY_EMITTED`; nenhum `objective_ids` foi introduzido. **Nada disso foi confirmado por AST** — `tools/check_core_boundary.py`, `check_contract_literals.py`, `check_event_envelope.py` e `codegen.py --check` não rodaram.
5. **A causa do `range-api exited (1)`.** O diagnóstico do próprio lançador falhou (`rc=3221225794`), e não tenho como subir containers. Não sei dizer se é regressão da Fase 9 ou defeito de ambiente do host. O que posso afirmar: `docker-compose.yml`, `Dockerfile` e `pyproject.toml` não mudaram entre `c77120c` e `3b3e9c2`, e `range_core.evidence`/`range_core.telemetry` já constavam de `[tool.setuptools].packages` na base — logo, a hipótese de empacotamento está descartada, e nenhuma outra foi testada.
6. **`domains/academus/evidence_generators/database_audit.py` e `identity_audit.py` com o gabarito real.** Li os módulos e os fatos sintetizados por `linha_a.py`/`gabarito.py`, mas não materializei um pack nem rodei `range-cli evidence build`, então não observei um só arquivo de evidência produzido. Tudo o que digo sobre o conteúdo dos arquivos é derivado do código, não do artefato.
7. **Achados órfãos de rodada anterior.** Não localizei registro de auditoria anterior da Fase 9 — `docs/progress/fase_9.md` não cita rodada, e §7 está explicitamente por redigir. Tratei esta como a primeira rodada; se houve outra, a conferência da regra P7-6 não foi feita.
