# Auditoria de checkpoint - 2026-09-17T20:19:19.629097+00:00

- fase: **8**
- commit auditado: `5d15e2dcb3ab82d3f4866cd038d8803292034cd2`
- session_id: `69f48ab1-46c5-4ed3-bcac-a157e1766be5`
- modo: `headless`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

I have everything I need. Full suite ran green (936/936, zero skips), all three launcher proofs bind to this tree, and the verifier battery is green except one.

# AUDITORIA — FASE 8

## VEREDITO: FAIL

---

## FINDINGS

### BLOCKER

- **[B1] O gate de CI `check_readme_atual.py` está VERMELHO no commit candidato — e ele bloqueia merge**

  **Arquivo:** `README.md:123` e `README.md:252`; gate em `scripts/check_readme_atual.py`, exigido por `.github/workflows/invariants.yml:765` e `:768`
  **Requisito violado:** `.github/workflows/invariants.yml:762-763` — *"`contratos` ja e context obrigatorio, entao isto bloqueia merge"*; R10 §3 (contagens pinadas no registro canônico)
  **Evidência (executada):**
  ```
  .aurora-audit/venv/Scripts/python scripts/check_readme_atual.py     → rc=1
    FALHAS: 2
      README.md: `testes` afirma ['868'] e a arvore diz '936'.   (x2)
  .aurora-audit/venv/Scripts/python scripts/check_readme_atual_probes.py → rc=1
    FALHA: a arvore limpa reprova no verificador principal.
  ```
  A suíte real: `Ran 936 tests in 118.076s` / `OK` (zero pulos).
  **Por que é blocker:** dois passos obrigatórios do job `contratos` falham; o PR da fase não pode ser mergeado. O vermelho nasceu **dentro desta fase**: `git show c38f890:README.md` declara 858 na base, o commit `0836944` (P7-10) subiu para 868 quando a árvore tinha 868 (`fase_8.md:270` — *"Suíte 858 → 868"*), e as Waves seguintes levaram a árvore a 936 sem reatualizar a prosa. O gate existe exatamente para impedir prosa divergente da árvore, e é o próprio número da fase que o quebra.

- **[B2] O item 2 da DoD é sustentado por um dublê: a consulta real do console de investigação não é executada por nenhum dos 936 testes**

  **Arquivo:** `domains/academus/api/repositorio.py:317-401` (`alteracoes_de_nota`, o SQL real) × `tests/test_console_investigacao.py:171-229` (`RepositorioFalso`)
  **Requisito violado:** `02_DOMAIN_ACADEMUS.md` §7:124 (os cinco filtros do console); R10 §Nascimento (*"oracle independente da implementação"*); a afirmação de `docs/progress/fase_8.md:64`
  **Evidência (observada):**
  - `grep -rn "alteracoes_de_nota\|grade-changes" tests/*.py` → os únicos chamadores são `test_console_investigacao.py` e `test_api_emissao_pela_rota.py`, **ambos com dublê**. Nenhum dos arquivos com `@exige_banco` (`test_trilha_de_auditoria.py`, `test_modelo_completo.py`, `test_business_state_postgres.py`) toca a rota ou o método — verificado por grep, saída vazia.
  - O `RepositorioFalso` **reimplementa a filtragem** sobre `dict` (linhas 223-229) e carrega a sua própria cópia do mapa filtro→coluna (`CAMPO_DA_CONSULTA`, linha 163), duplicando `_FILTRO_PARA_COLUNA` de `repositorio.py:303`. Um teste que compara duas cópias do mesmo mapa não é oráculo independente.
  - O registro (`fase_8.md:64`) cita esta suíte como **"(16, serviço)"**. O arquivo não importa `_academus_banco`, não abre conexão e não tem `@exige_banco`: roda sem serviço nenhum. São 16 testes, mas nenhum é de serviço.
  **Por que é blocker:** a correção do B2 da rodada anterior (commit `957b5c3`, *"aplica os filtros na consulta"*) mudou o SQL de produção e entrou **sem nenhum oráculo sobre o código que mudou**. O defeito que ela fecharia — *"o evento afirma um `result_count` que a consulta não produziu"* — continua indetectável no caminho real. O H1 abaixo é uma instância concreta desse defeito que passou verde neste mesmo gate.

---

### HIGH

- **[H1] O filtro "janela" liga string livre a uma coluna `Boolean` — e nada valida nem testa a junta**

  **Arquivo:** `domains/academus/api/app.py:251` (`filter_window: str | None`) → `repositorio.py:305` (`"filter_window": "within_window"`) → `repositorio.py:360-365` (`AND within_window = :filter_window`) × `alembic/versions/0004_trilha_de_auditoria.py:130` (`sa.Column("within_window", sa.Boolean(), nullable=True)`); UI em `range-core/web/investigation-console/main.tsx:142`
  **Requisito violado:** `02_DOMAIN_ACADEMUS.md` §7:124 — o console filtra por *janela*
  **Evidência (observada):** a coluna é booleana (`02` §2: *"toda alteração de nota calcula `within_window`"*); o parâmetro de rota é `str` sem validação, coerção ou enum; o campo da tela é `<input type="text">` livre (`main.tsx:142`, `campo("janela", ...)` sem o 4º argumento de tipo), enquanto a mesma tela **renderiza** o valor como booleano (`main.tsx:189`, `rotuloDaJanela(linha.within_window)`). O próprio gate alimenta `filter_window="business_hours"` (`test_console_investigacao.py:130`) — valor que nenhum `boolean` do Postgres aceita — e passa, porque o dublê compara chaves de `dict`.
  **Consequência:** `app.py:283` executa a consulta **antes** de `registrar_consulta` (`app.py:293`). Com um valor não-booleano, a consulta erra e a rota devolve 500 **sem emitir `audit_query_performed`** — o filtro nomeado na spec derruba a consulta e apaga o marcador `auto` junto. É invisível precisamente por causa do B2.
  **NÃO EXECUTADO, e declarado:** não consegui executar a consulta real contra o Postgres — a allowlist do auditor não admite execução de código ad-hoc. A forma exata do erro (`operator does not exist: boolean = text` vs `invalid input syntax for type boolean`) fica como leitura estática; o descasamento de tipo em si é verificável nas quatro linhas citadas.

---

### MEDIUM

- **[M1] A classe `exportacao` do §4 de `05` saiu da Fase 8 por edição do próprio verificador, e ficou sem fase de destino**

  **Arquivo:** `scripts/check_banner_de_simulacao.py:108` — `"exportacao": (False, "Fase 8 — academus-web: historico, diploma, PDF")` → `(False, "Fase 8/9 — artefatos gerados por academus-web: historico, diploma, PDF")`
  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §4:61 (*"no rodapé de todo artefato gerado — PDF, histórico, diploma, relatório, exportação"*); R10 §1
  **Evidência:** `07_IMPLEMENTATION_PHASES.md` §Fase 9 (linhas 235-242) **não tem item** para esses artefatos, e `docs/progress/fase_8.md` §6 não tem linha de pendência registrando a postergação. O dono passou de "Fase 8" para uma fase que não o cobra — a forma exata de requisito que morre sem nada ficar vermelho, e que o próprio `invariants.yml` documenta como perigo em outras passagens.

- **[M2] Não há relatório da(s) rodada(s) anterior(es) da Fase 8 na árvore — a conferência de achado órfão é impossível**

  **Arquivo:** `docs/progress/` (último relatório: `audit_20260913T220705Z.md`, Fase 7); `docs/progress/audit_log.jsonl` (última linha: `2026-08-23`, fase 6)
  **Requisito violado:** `docs/process/WORKFLOW.md:45-52` — *"Todo achado que não for corrigido no ciclo da rodada nasce com linha na §6 do `fase_N.md` corrente [...] e a rodada seguinte confere"*
  **Evidência:** os commits desta fase citam `B1`, `B2`, `B3`, `H2`, `M1`, `M2` de rodadas anteriores (`8bb5887`, `9905d39`, `db164d4`, `c2df575`, `80a270f`, `5d15e2d`), e `fase_8.md` §6 não contém nenhuma seção ou linha sobre elas. Sem o relatório versionado não é possível saber se algum MEDIUM/LOW daquelas rodadas ficou órfão — que é exatamente a conferência que a regra manda eu fazer. Registro isto como finding novo da regra de órfão, e não como acusação sobre um achado específico.

- **[M3] O único gate que prova a cláusula "conforme a flag" do item 1 admite zero execução como verde**

  **Arquivo:** `.claude/verify/expected_suites.json` — `"test_prova_andamento_integracao": {..., "pass": [0, 7], "fail": 0}`
  **Requisito violado:** R10 §2 (*"SKIP silencioso é FAIL"*; o intervalo declarado é tolerância, não licença)
  **Evidência:** `[0, 7]` aceita a suíte inteira pulada. É a única suíte que liga `academus.lms_session_drop_rate` ao efeito observável pela rota real (o próprio `test_prova_andamento_integracao.py:16-20` declara que sem ela *"a suíte de propriedades fica verde e a sala nunca vê ninguém cair"*). O registro canônico permite que a prova do item 1 registre-se como verde tendo executado nada.

- **[M4] A correção H2 desta rodada destravou uma entrada de allowlist que não pode funcionar**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:88` (commit `c2df575`) × `pyproject.toml:149`
  **Requisito violado:** R2 §1 (o que se declara corrigido tem de ser executável); P7-12
  **Evidência (executada):**
  ```
  .aurora-audit/venv/Scripts/python -m pytest → No module named pytest
  python -m pytest                            → C:\Python314\python.exe: No module named pytest
  pytest                                      → command not found
  ```
  `pyproject.toml:149` declara `test = ["httpx2==2.10.0"]` — **pytest não é dependência do projeto**, e o venv que o lançador cria (`scripts/start_checkpoint_audit.sh:292`, `pip install -e ".[test]"`) não o tem. A suíte é `unittest` (`readonly_bash.py:105`, e o comentário `:89` já diz isso com todas as letras). A linha corrigida continua inútil, e o próximo auditor que a ler pode concluir que `pytest` é o runner — perdendo a primeira execução, como perdi.

---

### LOW

- **[L1]** `README.md:123` e `README.md:252` afirmam *"143 pulam"* sem Postgres. A fase acrescentou suíte de serviço (`test_prova_andamento_integracao`, 7 testes com `@exige_banco`) e o número não foi revisto; o gate confere a contagem total, não esta. Prosa desatualizada no mesmo arquivo do B1.
- **[L2]** `02_DOMAIN_ACADEMUS.md` §7:117-122 enumera sete superfícies de `academus-web`; a fase intitulada *"Web completo"* entrega três telas novas. **Não é falha de DoD** — `07` §Fase 8 tem quatro itens e nenhum cobra as demais — mas nenhuma pendência de `fase_8.md` §6 registra o destino de Portal do Aluno, Portal do Professor, Secretaria, Financeiro e AVA simplificado.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Modo "Prova em andamento" perde sessões conforme `lms_session_drop_rate` | **PASS** | `tests/test_prova_andamento_integracao.py` (7) roda pela rota real `GET /exam/session-status` com store, cache, fold e Postgres reais: taxa 0 → tudo `alive`; inject `TODAS_CAINDO` → tudo `dropped` na mesma rota e mesmo objeto; taxa 0,5 parte o conjunto; minuto 0 não derruba; e o par negativo (`ORamoAllAliveMascara`) prova que o ramo all-ALIVE existe, o que dá poder discriminante ao positivo. `ADescobertaDaFlag` fixa que a descoberta de `processo.py` acha a flag na superfície real. Com serviços no ar, os 7 rodaram (0 pulos). Ressalva registrada em [M3] |
| 2 — Console de investigação emite os marcadores automáticos | **PARCIAL** | **Marcador `auto`: PASS.** `tests/test_console_investigacao.py::OConsoleSetaOMarcadorAuto` prova pela projeção `range_core.objectives.projecao.project`, que não lê payload, com controle de ausência antes da consulta — oráculo independente, correto. **Filtros de `02` §7:124: FALHA.** A consulta real nunca executa ([B2]); o filtro "janela" é tipo-incorreto contra a coluna ([H1]) |
| 3 — Cada persona vê apenas sua camada `reported` | **PASS** | `tests/test_reported_isolation.py` (10). Asserção sobre o **payload da API** (`GET /participant/view`), não sobre UI. Os eventos de `ground_truth`, `evaluator_assessment` e `facilitation` são plantados **marcados para a persona que lê** (adversarial), e o teste de rota faz `assertNotIn` sobre o corpo **inteiro** da resposta — pega vazamento em campo que a fixture não antecipou. A whitelist `REPORTAVEIS` (`reported.py:79`) cobre os 5 valores do enum de `truth_layer` (`events.schema.yaml:381-387`) por complemento. 401 sem token e com token inválido. Persona vem do token, não de parâmetro |
| 4 — As sete ações de continuidade aplicam efeito mecânico e custo | **PASS** | `tests/test_continuidade.py` (20). Conjunto das sete conferido contra o **enum do contrato** lido do YAML (oráculo independente, não lista reescrita). Efeito provado pelo **estado reconstruído** via `simulation_state.project`, com default oposto ao efeito — inclusive o caso `False` (`emergency_document_issuance`). Custos batem literalmente com a coluna "Custo" de `02` §9:150-156. INV-4 provado com flag sintética lida do payload. Negativos 422/403/401 presentes, cada um sem emitir |

**Invariantes e segurança (todos executados, todos rc=0):** `check_core_boundary` · `check_contract_literals` · `check_event_envelope` · `codegen.py --check` · `check_security_constraints` · `check_synthetic_data` · `check_core_contract_imports` · `check_banner_de_simulacao` (6 telas) · `check_web_sem_derivacao` (22 arquivos) · `check_gabarito_fora_do_git` · `check_store_read_surface` · `check_insumo_de_metrica` · `check_api_surface` (37 rotas) · `check_telas_sem_vocabulario` · `check_fabrica_liga_emissor` · `check_hooks_com_emissor` · `check_fold_authority` · `check_gate_coverage` · `check_spec_flags` · `check_progress_consistency` + probes (12 eixos) · `check_contract_examples` · `check_spec_examples` · `check_secoes_de_seguranca` · `check_regras_do_linter` · `check_pinned_images` · `check_allowlist_do_auditor` · `check_trilha_de_auditoria` · `check_volumes_da_linha_b`.
**Provas do lançador (todas amarradas à árvore `ea22647976c5`, igual à deste checkout):** `check_provas_de_container` rc=0 (demo + reinício) · `check_prova_do_seed` rc=0 (3.543.783 linhas, 172,5 s e 174,9 s contra orçamento de 300 s, 20 tabelas com SHA-256 igual) · `check_prova_do_exercicio_4h` rc=0 (650 eventos, 0,024 s contra 3 s, dryrun 4 caminhos).

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_console_investigacao.py::MatrizDeAplicacaoDeFiltro`** (linhas 502-574) — a classe é **auto-contida e não toca código de produção**. `test_M6` compara dois dublês definidos no próprio teste; `test_M7` chama `RepositorioFalso.alteracoes_de_nota(inicio, fim, False)` **reproduzindo à mão** o que o handler fazia, em vez de exercitar o handler. Consequência concreta: se `app.py:283` voltar a chamar com três posicionais, `test_M7` continua verde — ele nunca lê o handler. O docstring afirma que a classe "mata M6/M7"; ela demonstra que a *asserção* mataria, não que o *sistema* está correto.
2. **`tests/test_console_investigacao.py::OsFiltrosSaoAplicadosAConsulta`** (429-499) — prova que o handler encaminha os filtros a **um dublê**, e que o dublê os aplica. O mapa filtro→coluna do teste (`CAMPO_DA_CONSULTA:163`) é cópia do de produção (`_FILTRO_PARA_COLUNA:303`): errar o nome da coluna nos dois lugares, ou acertar no teste e errar no SQL, passa igual. É o [B2] e o [H1].
3. **`docs/progress/fase_8.md:64`** rotula essa suíte como **"(16, serviço)"**. A contagem está certa; o rótulo não — o arquivo roda sem Postgres. Um leitor do registro conclui que o caminho de banco foi exercitado, e não foi.

Fora esses, **nenhum outro encontrado**. Os gates dos itens 1, 3 e 4 têm oráculo independente, par negativo e asserção sobre payload/estado real; nenhum `skip`, `skipTest` ou `expectedFailure` nos seis arquivos novos (grep, saída vazia); a suíte completa rodou com 0 pulos.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **A forma exata do erro do [H1].** Não pude executar a consulta real contra o Postgres: a allowlist do auditor não admite código ad-hoc, e a única entrada de suíte é `python -m unittest discover -s tests` ancorada em `$`. O descasamento de tipo é verificável estaticamente; o comportamento em runtime não foi medido por mim.
- **O stage `baseline` (R8, pins × HEAD).** `.aurora-audit/venv/Scripts/python .claude/verify/check_baseline.py` é **bloqueado** pela allowlist. Verifiquei só estruturalmente: `pins.json._meta.gerado_de_head = 80a270f4`, que é `HEAD~1`, e o commit `5d15e2d` altera **apenas** `pins.json` (que se auto-exclui) — consistente, mas **não conferido por hash**.
- **As seis entradas de `expected_suites.json` individualmente.** Os `cmd` usam `-p "<padrão>"`, forma que a allowlist não admite (o `$` do `readonly_bash.py:105` fecha em `tests`). As contagens declaradas (15/16/10/20/10/[0,7]) batem com minha contagem estática de métodos, mas não as executei isoladamente.
- **`npm run typecheck` / `npm run build` das três telas novas.** Não há `node_modules` no worktree e não consigo `cd`. A prova de container mostra a camada `RUN npm run build` como `CACHED`, o que implica que conteúdo idêntico já foi construído com sucesso — mas eu não vi o build rodar.
- **O estado do gate do B1 no commit base.** Não executei a suíte em `c38f890`. A cadeia que sustento é documental: `git show c38f890:README.md` declara 858, e `fase_8.md:270` registra "Suíte 858 → 868" para o commit da abertura. A conclusão "o vermelho nasceu nesta fase" é inferência sobre esses dois fatos, não medição no base.
- **A conferência de achado órfão da rodada anterior** — impossível, por [M2]: os relatórios das rodadas da Fase 8 não estão versionados.
- **`docs/progress/fase_8.md` §7 (fechamento)** não existe ainda, por desenho: é redigida por quem implementou, após o veredito. Não a avaliei.
