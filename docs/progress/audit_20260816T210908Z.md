# Auditoria de checkpoint - 2026-08-16T21:09:08.053150+00:00

- fase: **3**
- commit auditado: `81350556eb546f2e382fe54927a857b95f17df8e`
- session_id: `24cb21ab-bea2-4a8e-aae9-cbd656fd0835`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 3

## VEREDITO: FAIL

---

## FINDINGS

### BLOCKER

- **[B1] A auditoria não conseguiu executar nada — nenhum teste, nenhum verificador. Os quatro itens da DoD ficam sem prova executável neste checkpoint.**

  **Arquivo:** `scripts/start_checkpoint_audit.sh:321` (`claude --agent checkpoint-auditor --permission-mode default`) · `.claude/settings.json:3-16` (nenhuma entrada `permissions.allow` para `Bash`)

  **Requisito violado:** `docs/process/WORKFLOW.md` §"Por que o auditor formal usa launcher de worktree" — *"**Bloqueio indevido também é defeito.** Um auditor que não consegue rodar a prova central audita por inferência de leitura de código, e continua emitindo veredito enquanto isso"*; `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4(e). E `docs/progress/fase_3.md:820`, que declara **P2-19 FECHADA** — *"o auditor não executa o que depende de serviço → ✅ FECHADA — stack efêmera no lançador"*.

  **Evidência (observada, não suposta):** o hook `user-scope/hooks/readonly_bash.py:21,38,44,102-131` **autoriza** `pytest`, `python -m pytest`, `python -m unittest discover -s tests`, `python tools/check_*.py` e os verificadores de `scripts/`. Nada disso foi bloqueado pelo hook. Foi bloqueado pela camada de permissão da sessão, que devolveu `This command requires approval` para **todas** as formas tentadas:

  | comando | resultado |
  |---|---|
  | `python -m pytest tests -q -p no:cacheprovider` | requires approval |
  | `pytest tests -q` / `PYTHONDONTWRITEBYTECODE=1 pytest -p no:cacheprovider tests` | requires approval |
  | `python -m unittest discover -s tests` / com `PYTHONDONTWRITEBYTECODE=1` | requires approval |
  | `python scripts/check_api_surface.py` | requires approval |
  | `python tools/check_contract_literals.py` | requires approval |
  | `PYTHONDONTWRITEBYTECODE=1 python tools/check_core_boundary.py` | requires approval |

  `git log`, `git diff`, `git show`, `ls` e `grep` passaram — o corte é exatamente sobre execução.

  **Por que é blocker:** o prompt desta sessão afirma *"Servicos: ATIVOS … Os testes que dependem de servico VAO RODAR; skip aqui e defeito"*. Nenhum rodou. O item 4 da DoD (`verificado por lint`) é, por redação, um item que **só existe como execução** — e o lint não foi executado. Os itens 1–3 têm teste escrito e correto por leitura, mas leitura não é a prova que a DoD cobra. Este é exatamente o modo de falha que o repositório já pagou três vezes (`user-scope/hooks/readonly_bash.py:22-37` — *"o auditor da Fase 2 nao conseguiu executar NENHUM teste da fase e voltou a avaliar por leitura de codigo — B1 da auditoria de 16/08/2026, e reincidencia do H3 da segunda auditoria da Fase 1"*). Emitir PASS aqui seria a quarta reincidência, dentro da fase que declarou a classe fechada. Minha própria regra é explícita: item de DoD não verificável com as ferramentas disponíveis **não** se assume PASS.

  **Nota de escopo, para não confundir o leitor:** B1 é sobre o *checkpoint*, não sobre defeito encontrado no código. A revisão estática abaixo **não** encontrou defeito nos quatro itens da DoD. B1 diz que ninguém provou que eles passam — não que eles falham.

---

### HIGH

- **[H1] O intervalo auditado carrega alteração de `docs/spec/` junto com o código da fase.**

  **Arquivo:** `docs/spec/01_ARCHITECTURE.md:181-187`, introduzido pelo commit `02383e4` (*"spec-change: quem traz o consumidor da ligacao e a Fase 8, nao a Fase 3"*), que está em `8c6eca6..8135055`, entre `1b17380` (peça 1) e `fce18dc` (peça 2).

  **Requisito violado:** `CLAUDE.md` §"A especificação é imutável durante a implementação" — *"crie uma branch `spec-change/<slug>` e abra um PR separado … com aprovação humana **antes de qualquer código**. Alterar spec e código no mesmo PR é proibido."* Também `docs/process/WORKFLOW.md:25-28`.

  **Evidência:** `git log --oneline 8c6eca6..8135055 -- docs/spec/` devolve exatamente `02383e4`. `git diff 8c6eca6 8135055 -- docs/spec/` devolve um hunk de 6 linhas em `01_ARCHITECTURE.md`. A base `8c6eca6` é simultaneamente a âncora declarada em `docs/process/phase_anchors.tsv:45`, então não há leitura em que o commit de spec caia fora do trabalho da fase. O gate `spec_freeze` (`.github/workflows/invariants.yml:245-254`) computaria `SPEC=1` e `CODE>0` e reprovaria o PR. O registro reconhece a situação em `docs/progress/fase_3.md:91-92` (*"corrigida em `spec-change` próprio — submetido e ainda aberto"*).

  **Por que é HIGH e não BLOCKER:** o gate do próprio repositório recusa o merge nesta forma, então não há escape silencioso; a correção é ordem de merge (mergear o PR de spec-change primeiro e rebasear a branch da fase, regravando a âncora conforme `phase_anchors.tsv:38-42`), não mudança de código; e o conteúdo do spec-change é editorial — corrige a fase que traz o `federated-identity-simulator` — e não é premissa de nenhum dos quatro itens da DoD. Não consegui verificar a ordem de merge a partir daqui; ver §"O que eu não consegui verificar".

---

### MEDIUM

- **[M1] `PostgresEventStore._head()` entra no core sem nenhum teste, e é a leitura de que a validade do cache depende.**

  **Arquivo:** `range-core/events/postgres_store.py:116-137`

  **Requisito violado:** `01_ARCHITECTURE.md` §4 — *"Simulation State … Redis (projeção) + event store"* — e §4.1 (*"toda projeção é reconstruível do zero"*). `06_ACCEPTANCE_TESTS.md` T3 (*"Reinício do processo restaura a projeção corrente sem intervenção"*) passa por este caminho na Fase 4.

  **Evidência:** `tests/test_event_store_postgres.py` não contém nenhuma ocorrência de `head` ou `StreamHead` (grep sobre o arquivo: zero matches). `tests/test_projection_cache.py` exercita `head()` apenas contra `InMemoryEventStore` (`:35`, `:81-106`, `:149`, `:168`, `:179`); a classe `NoRedis` (`:231-268`) troca o *cache* por Redis mas mantém o store em memória. O método irmão `_stored()` tem cobertura contra Postgres; `_head()` não tem nenhuma. A correção de `_head()` repousa sobre a afirmação de que `sequence` é contígua (docstring `:119-120`) — a afirmação **procede** (`alembic/versions/0001_event_store.py:53`, `autoincrement=False`, sequência calculada pela aplicação, mais o `CheckConstraint` de `:72`), mas nenhum teste a exercita contra Postgres real, que é justamente o serviço que o lançador subiu.

  **Consequência:** um `_head()` que devolvesse `count` errado ou `last_event_id` de outra linha faria `current()` (`range-core/state/cache.py:151-157`) servir projeção obsoleta sem nada ficar vermelho, e o sintoma apareceria no container da Fase 4, longe da causa.

---

### LOW

- **[L1] `test_a_serializacao_preserva_o_TIPO_do_valor` não exercita o caso que a própria docstring diz proteger.**
  `tests/test_projection_cache.py:252-258`. A docstring afirma *"Uma flag numérica que voltasse booleana degradaria o endpoint errado"*, e a asserção é `assertIsInstance(lido, bool)` sobre `FLAG_LIGADA_POR_DEFAULT`, que é `bool` no fixture (`:65-73` — as duas flags declaradas são booleanas). Nenhuma flag `number` atravessa o Redis em teste nenhum, e `academus.lms_session_drop_rate` é `number` (`domains/academus/flags.yaml:53-57`) e é lida pelo caminho `proporcional` (`domains/academus/api/degradacao.py:135`). O risco real é baixo — `json` preserva `float` —, mas a asserção prova metade do que anuncia.

- **[L2] `check_fold_authority.py` varre só `range-core/`, e o limite não está declarado.**
  `scripts/check_fold_authority.py:48` (`CORE_ROOT`) e `:82-99`. A propriedade enunciada no cabeçalho é *"o fold é a única autoridade de estado"* (citando `01_ARCHITECTURE.md` §4.1), mas uma construção de `SimulationState` dentro de `domains/` não seria vista. Hoje a árvore está limpa (grep por `SimulationState(` em `range-core/`: apenas `state/cache.py:243` e `state/simulation_state.py:322`, ambos autorizados; nenhuma em `domains/`), e a porta continua sem método que aceite estado pronto — então o efeito prático é nulo. O verificador declara outros dois limites explicitamente (`:106-108`, e o de import dinâmico em `check_api_surface.py:247-251`); este não está declarado.

- **[L3] `Cota` é estado mutável fora das cinco camadas de `01` §4 e não é reconstruível do event store.**
  `domains/academus/api/degradacao.py:95-113` e `:129`. O acumulador vive na instância do `Degradador`, é chaveado por `(rota, flag)` e não deriva de nenhum evento. A docstring do módulo (`:36-49`) justifica o desenho por determinismo — *"Reproduzivel, e o facilitador consegue prever o efeito"* —, e a reprodutibilidade vale **por processo desde o início**: reinício da API zera a cota, e um rollback (`01_ARCHITECTURE.md` §4.2) devolve a flag sem devolver o acumulador. `01_ARCHITECTURE.md` §4 enumera cinco camadas de estado e nenhuma o comporta. Não afeta item de DoD — `lms_session_drop_rate` é o terceiro endpoint pedido pelos OUTPUTS, não pela DoD.

- **[L4] O lint que fecha o item 4 não cobre `tests/` nem `scripts/`.**
  `tools/check_contract_literals.py:69` (`SCANNED_DIRS = ("range-core", "domains")`). `docs/progress/fase_3.md:692` cita como exercício do item *"o hook **recusou** a primeira versão de `tests/test_api_degradacao.py`"* — e o hook se declara **não-gate** no próprio cabeçalho (`.claude/hooks/check_architecture.py:6`: *"Feedback rapido, NAO gate: o gate real e o CI por AST"*). A árvore está de fato limpa no escopo varrido (os únicos literais `academus.<nome>` em `.py` estão em `domains/academus/generated/flags.py:6-18`, em docstrings que o `_judge` não fatia, e em fixtures de probe sob `scripts/`), e a DoD diz "código-fonte", que razoavelmente exclui teste e harness. Fica registrado porque a evidência citada no registro é o mecanismo que não é gate.

---

## DoD — item a item

`07_IMPLEMENTATION_PHASES.md` Fase 3, linhas 113-117.

| Item | Status | Evidência |
|---|---|---|
| 1 — `academus.enrollment_offline: true` faz o endpoint de matrícula retornar 503 | **NÃO VERIFICADO** (execução bloqueada — B1) | Estático: `domains/academus/api_surface.yaml:158-170` declara `POST /matricula` com `condicao: ligada`, `efeito: recusa`, `status: 503`; `degradacao.py:137-155` aplica; `tests/test_api_degradacao.py:177-182` afirma 201 antes e 503 depois de um `inject_fired` real, com o efeito vindo do fold (`store` + `current` + `InMemoryProjectionCache`, sem duplo). O teste **prova o requisito** se executado. |
| 2 — `academus.grades_readonly: true` bloqueia POST de nota com mensagem de negócio | **NÃO VERIFICADO** (B1) | Estático: flag declarada em `flags.yaml:102-111`; `api_surface.yaml:181-193` fixa 409 e o texto; `tests/test_api_degradacao.py:184-196` afirma 201→409 e `assertIn("coordenacao", …)`; `:198-206` afirma que a leitura segue 200, casando com o `effect_ui`. A varredura de `:436-450` afirma que a resposta não se explica. Prova o requisito se executado. |
| 3 — RBAC nega acesso cruzado entre perfis | **NÃO VERIFICADO** (B1) | Estático: `tests/test_api_rbac.py:76-96` — professor→aluno 403, aluno→turma 403, **e** professor→turma dele 200. É teste de **payload de API**, não de UI, na forma que `06` T6 exige; `:121-136` afirma indistinguibilidade byte a byte entre recurso existente e inexistente; `:200-212` faz o mesmo para escopo de objeto. O par positivo/negativo discrimina — um RBAC que negasse tudo reprovaria. Prova o requisito se executado. |
| 4 — Nenhuma string solta de flag no código-fonte (verificado por lint) | **NÃO VERIFICADO** (B1 — o lint É o item) | Estático: `tools/check_contract_literals.py` está no job `arquitetura` (`.github/workflows/invariants.yml:31-32`); os testes importam as constantes geradas (`tests/test_api_degradacao.py:44-49`); nenhum módulo de `api/` importa `domains.academus.generated` (guardado por `check_api_surface.py:463-493`). Limite em [L4]. |

**Critério de aceitação aplicável — `06_ACCEPTANCE_TESTS.md` T2 (Fases 1–3):** os quatro bullets dependem de `codegen.py --check`, `check_contract_literals.py` e do loader; **nenhum foi executado**.

**OUTPUTS de `07` além da DoD** (estático, não executado): JWT em `range-core/api/tokens.py`; RBAC em `domains/academus/api/auth.py`; três entidades em `domains/academus/models/registros.py:45-92`; degradação real em **três** endpoints (`/matricula`, `/turmas/{id}/notas`, `/turmas/{id}/diario`), conforme `api_surface.yaml`.

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. `tests/test_projection_cache.py:252-258` — ver [L1]: assere tipo `bool` sobre flag booleana e anuncia proteger flag numérica. Consequência pequena, mas a asserção não é a da docstring.
2. **Ausência**, não teste fraco: `PostgresEventStore._head()` — ver [M1]. Nenhum teste, em nenhum arquivo, toca este método.

Fora esses dois, a suíte desta fase é notavelmente resistente à classe que eu procuro:
- `test_api_rbac.py` testa **payload e status da API**, não interface, e compara `resposta.content` entre casos (`:136`, `:212`) — não só o código de status;
- `test_api_degradacao.py:302-433` prova a ordem `autoriza → degrada` por **comportamento** (401 sem token nas três rotas degradadas, com as flags ligadas) **e** por estrutura (`:421-433`), e a docstring de `:349-367` registra que a primeira versão do teste não discriminava nada porque a flag não estava ligada — o par com/sem token é o que o torna capaz de ficar vermelho;
- `test_api_degradacao.py:221-259` é a metade negativa (sem flag, nada muda), que impede a positiva de virar superstição;
- zero mocks: `TestClient` fala ASGI com a aplicação real, o estado vem do fold real sobre um `inject_fired` real. A única costura é `Degradador.dormir`, e é costura de tempo, não duplo de biblioteca;
- nenhum `skip`/`xfail` não justificado: os únicos `skipIf` são `AURORA_TEST_REDIS_URL`/`AURORA_TEST_DATABASE_URL` ausentes (`test_projection_cache.py:231`, `test_event_store_postgres.py:49,74,118,179`), com razão impressa — e o lançador exporta ambas.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Absolutamente nada por execução.** Nenhum teste, nenhum verificador de `tools/`, nenhum verificador de `scripts/`, nenhum probe negativo. Onze tentativas, seis formas de comando, todas recusadas pela camada de permissão (não pelo hook). Isto é B1, e é a razão do FAIL. Tudo acima é leitura de código e de diff.
2. **Se os testes passam.** Li as asserções e julguei que provam o que dizem provar; não sei se passam.
3. **Se os serviços estavam de fato no ar.** O prompt afirma ATIVOS; `docker compose ps` cai fora da allowlist do hook na forma com `-p`/`-f` (`readonly_bash.py:43` exige `docker compose ps` sem flags antes do subcomando), e a forma sem flags também exigiu aprovação. Não confirmei Postgres, Redis nem a migration.
4. **A ordem de merge do `spec-change` de [H1].** Não sei se `02383e4` será mergeado em `main` por PR próprio antes da branch da fase, nem se a âncora será regravada no rebase. Se isso acontecer, [H1] desaparece do diff do PR e o `spec_freeze` fica verde; se não acontecer, o gate reprova.
5. **Os probes negativos dos cinco verificadores novos** (`check_spec_flags_probes`, `check_api_surface_probes`, `check_fold_authority_probes`, `check_pinned_images_probes`, `check_audit_base_probes`). Li os verificadores e reconstruí à mão que a árvore atual os satisfaz — as 12 flags citadas em `docs/spec/` se dividem exatamente em 7 declaradas + 5 pendentes, as 5 rotas decoradas casam com as 5 `implementada`, `/auth/token` continua `planejada` e ausente do código, os pares `flags`↔`degradacao` são conjuntos iguais, os tipos batem com `TIPO_EXIGIDO`, nenhuma mensagem contém vocabulário de mecanismo, e os três arquivos pinam os mesmos dois digests. **Mas não sei se os verificadores enxergam**, porque não rodei as provas negativas — que é precisamente a propriedade que elas existem para estabelecer.
6. **Se o `check_audit_base.py` de fato aprovou esta sessão.** Deduzi que sim pela ausência do bloco de AVISO no prompt e pela consistência entre âncora (`8c6eca6`), base e `merge-base`; não observei a saída do lançador.
7. **Comportamento sob concorrência** do cache frio sem single-flight (P3-2, `range-core/state/cache.py:56-61`) e da `Cota` compartilhada. Declarados como pendência para a Fase 4; não exercitados.
