# Auditoria de checkpoint - 2026-09-13T22:07:05.533812+00:00

- fase: **7**
- commit auditado: `c9f61b2247124e25b9b44629d0862aab83f6a2ec`
- session_id: `f6d8c920-8d0e-4f8d-8366-9d2ca947626c`
- modo: `headless`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

All gates executed. Here is the report.

# AUDITORIA — FASE 7

## VEREDITO: PASS

Base: `847f6205595f82487aa2da6d164d9b98bec868ce` (fornecida pelo lançador; não resolvi `main` por conta própria). `HEAD` = `c9f61b2247124e25b9b44629d0862aab83f6a2ec`, árvore `6cc01ca2cfb36f9fbf5ff948457ace914d63b603` — confere com a árvore que os **três** artefatos de prova declaram. Guarda de base: **PORTA**, verbatim do lançador. 5ª rodada desta fase.

**Gates executados (R2 §1):**

| Gate | Resultado |
|---|---|
| `python -m unittest discover -s tests` (venv do lançador) | **Ran 858 tests … OK**, exit 0, 137,1 s — **zero skips** (o sumário sai sem `(skipped=N)`; os `skipIf/skipUnless` são todos condicionais a `_URL` e os serviços estavam no ar) |
| `check_provas_de_container.py` | rc=0 — árvore `6cc01ca2cfb3` confere; `demo` rc=0, `reinicio` rc=0, build completo verde (com os `COPY range_cli/` e `COPY dados_sinteticos/` visíveis no log) |
| `check_prova_do_seed.py` | rc=0 — 3.543.783 linhas, 161,2 s / 169,4 s < 300 s, 20 tabelas com SHA-256 igual |
| `check_prova_do_exercicio_4h.py` | rc=0 — 650 eventos (40 injects, 5 decisões, 600 ações, 4 rollbacks), lint sem achados, 4 caminhos em 2 pontos, **0,037 s contra 3 s**; amarração dupla: árvore + 7 SHA-256 do pack |
| `check_prova_do_exercicio_4h_probes.py` | rc=0 — 9 venenos em 7 direções + 2 controles positivos (inclusive o modo "pack ausente", que é o meu) |
| `check_core_boundary`·`check_event_envelope`·`check_contract_literals`·`check_security_constraints`·`check_synthetic_data`·`codegen.py --check` | rc=0 (6/6) |
| `check_progress_consistency`·`check_regras_do_linter` (10 regras: 8 mecanismo, 2 destinatário)·`check_core_contract_imports` (18 arquivos × 8 raízes de topo)·`check_secoes_de_seguranca` (8/8, 60 verificadores) | rc=0 |
| `check_gabarito_fora_do_git`·`check_api_surface` (34 rotas)·`check_contract_examples`·`check_spec_examples`·`check_gate_coverage` (444 arquivos)·`check_readme_atual` (17 predicados)·`check_allowlist_do_auditor` | rc=0 |
| `check_insumo_de_metrica`·`check_fold_authority`·`check_store_read_surface`·`check_web_sem_derivacao`·`check_banner_de_simulacao` | rc=0 |
| `phase0_negative_tests.py` | rc=0 — 6 verificadores reprovam contra probe; 112 provas de invariante do hook; 8 falsos bloqueios declarados, 0 escritas não bloqueadas |
| `.aurora-audit/venv/Scripts/range-cli scenario lint\|dryrun tests/fixtures/pack_exemplo` | rc=0 / rc=0 (1 ponto, 2 caminhos) — é o objeto do step novo de CI |
| `…/range-cli scenario lint tests/fixtures/pack_minimo` | rc=2, achado correto (`incomplete_pack`) — console script instalado detecta, não só corre |
| `git diff <base> HEAD -- docs/spec/` | **vazio** — nenhuma spec tocada junto com código |

---

## FINDINGS

### BLOCKER

**Nenhum.** O B1 das rodadas 2–4 (item 7 da DoD) fechou **pelo rito, não por código**: o spec-change #67 está na base (`847f620`, *"spec-change: SUPPORTED_SCHEMA_VERSIONS condicional a antecessora real (P7-18)"*), `04_SCENARIO_SCHEMA.md` §4 passou a exigir `[N]` com recusa instruída enquanto não houver antecessora real, e `07` DoD 7 e `06` T12 acompanharam. Medi o código contra a norma nova, não contra a antiga: `pack_loader.py:129` (`SUPPORTED_SCHEMA_VERSIONS = (2,)`) é a aplicação literal, e as duas metades do item têm teste executado (`tests/test_pack_loader.py:383` e `:394`).

### HIGH

**Nenhum.**

### MEDIUM

- **[M1] `tests/fixtures/pack_exemplo/ground_truth.yaml` está versionado e o guardião ganhou isenção para ele — mas as duas normas que o guardião executa continuam dizendo "nunca", sem exceção**

  **Arquivo:** `tests/fixtures/pack_exemplo/ground_truth.yaml:1-27` (versionado — `git ls-files`) × `scripts/check_gabarito_fora_do_git.py:122` (`EXEMPLO_SANITIZADO = "tests/fixtures/pack_exemplo/"`) e `:227-228` (`if caminho.startswith(EXEMPLO_SANITIZADO): continue`).

  **Requisito violado:** `CLAUDE.md` §"Ground truth e GM notes" — *"**Não versione `ground_truth.yaml` nem `GM_NOTES.md` aqui**"*, e a permissão do exemplo é qualificada: *"Exemplo de pack **sanitizado** — **sem gabarito e sem notas de GM** — é permitido"*; `.claude/rules/product-invariants.md` INV-9 (RATIFICADA) — *"`ground_truth.yaml` e `GM_NOTES.md` **nunca** versionados"*, com `check_gabarito_fora_do_git.py` nomeado como o gate dessa linha.

  **Evidência — medida, não suposta.** `git ls-files | grep ground_truth` devolve três caminhos, e um deles é `tests/fixtures/pack_exemplo/ground_truth.yaml` — não existia na base (`847f620`). O verificador passa (`rc=0`) e a própria saída dele diz por quê: *"nenhum `ground_truth.yaml` nem `GM_NOTES.md` versionado **fora da isenção declarada**"*. O conteúdo é sanitizado de fato — 6 linhas, `GT-A-020`, que é id de exemplo normativo de `04` §7, sem caso real — e a isenção é de caminho exato, com probe nas duas direções (`check_gabarito_fora_do_git_probes.py:78` e `:88`, o vizinho `pack_exemplo_2` continua reprovando). O registro (`docs/progress/fase_7.md:2502-2517`) atribui a decisão ao proprietário em 13/09/2026 e cita CLAUDE.md — **truncando a citação exatamente na cláusula que o artefato contraria** (*"sem gabarito e sem notas de GM"*).

  **Por que é MEDIUM e não maior:** o propósito de INV-9 — não entregar o gabarito junto com o motor — **não** foi violado. O pack real segue fora do Git (`.gitignore:126`), provado por outro caminho (os 7 SHA-256 da prova de 4 h), e o eixo (b) do guardião continua exigindo `scenarios/` no `.gitignore`. Não bloqueia nada.

  **Por que é finding assim mesmo:** o mecanismo passou a dizer *"exceto neste caminho"* e **as duas normas continuam dizendo "nunca"** — é a divergência entre desenho declarado e mecanismo implementado que este repositório trata como classe própria (`docs/progress/fase_7.md` §7.1). Consequência concreta e estreita: de agora em diante qualquer arquivo `ground_truth.yaml`/`GM_NOTES.md` sob `tests/fixtures/pack_exemplo/` — inclusive uma cópia de gabarito real — é invisível ao gate, e nada fica vermelho. Não pude verificar a autorização: R2 §4 e R5 §anti-injeção dizem que autorização vem do usuário no chat, e o que eu tenho é a transcrição dela num arquivo do próprio commit auditado. O remédio é do operador e é de uma linha — pôr a exceção no texto de `CLAUDE.md` §Ground truth e na linha INV-9 de `.claude/rules/product-invariants.md`, para que norma e gate voltem a dizer a mesma coisa. `docs/spec/` **não** está envolvido: `05` §6 fala do build servido a participantes, e não de versionamento, então isto não é rito de spec-change.

### LOW

**Nenhum.**

---

**Regra de achado órfão (P7-6), conferida item a item.** A 4ª rodada (`audit_20260913T194629Z.md`) teve **um** MEDIUM e **zero** LOW. O M1 dela — a guarda de IOC alcançando só o `ground_truth.yaml` — está **corrigido, e medi por execução**: `pack_loader.py:584-589` emite um passo por documento lido, `confere_ausencia_de_ioc` recebe `arquivo` por parâmetro (`:1211`), e os dois testes que o provam rodaram entre os 858 (`test_range_cli_lint.py:643`, IP roteável em `injects.yaml` com posição na âncora; `:663`, em `information_distribution.yaml`, o documento sem contrato). Os M/L das rodadas 2 e 3 seguem dispostos (P7-19, P7-20 com linha na §6; M1/M2 da 2ª medidos corrigidos nesta e na rodada anterior). **Nenhum órfão.**

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `lint` recusa inject sem objetivo e sem `noise: true` | **PASS** | `tests/test_range_cli_lint.py:255` e `:273` — as duas pernas; a 2ª impede que uma regra exigindo `objectives` de todo inject passasse. Executados nos 858 |
| 2 — `event_type` inexistente em condição de branch, **com posição** | **PASS** | `:294` e `:311`; o 2º exige `linha[coluna-1:] == "vpn_acess_revoked"` — apontar para a chave `event:` reprovaria. Negativa própria no contrato (`scenario.schema.v2.yaml:1163`), executada pelos 95 negativos de `check_contract_examples` |
| 3 — condição por juízo do facilitador recusada | **PASS** | `:329`, posição derivada do texto da fixture; `branch_condition` com `oneOf` fechado + `additionalProperties: false` |
| 4 — `branch_policy` do manifesto aplicada | **PASS** | `tests/test_branching.py:159-206`: excesso de pontos por linha, excesso de caminhos, **manifesto sem política não inventa default**, e a linha do ponto caindo para a do `at_inject`. `confere_branch_policy` entra em `_passos` (`pack_loader.py:567`), logo vale na carga **e** no lint |
| 5 — branch sem `reconverge_at` recusada | **PASS** | `scenario.schema.v2.yaml:537` `required: [id, at_inject, evaluate, reconverge_at]`; negativa em `:1141` entre os 95 executados; pelo lint em `test_range_cli_lint.py:486` |
| 6 — `dryrun` percorre todos os caminhos | **PASS** | Fixture: `tests/test_range_cli_dryrun.py:27-69` (4 eixos, incl. lint-antes-da-travessia e `branch_walk_impossible`). **Executável instalado**: rodei `range-cli scenario dryrun tests/fixtures/pack_exemplo` — 1 ponto, 2 caminhos, rc=0. **Pack real**: `.aurora-prova-do-exercicio-4h.json` grava `item_6_dryrun_todos_os_caminhos: true`, 4 caminhos em 2 pontos, e o produtor **executa** `branching.percorre` (`prova_do_exercicio_4h.py:102`), não transcreve |
| 7 — versão não suportada recusada com instrução (v1 e v0); N-1 real migra | **PASS pela letra** | Norma nova na base (#67). `tests/test_pack_loader.py:383` (v1) e `:394` (v0) exigem as três perguntas da instrução — o que o pack declara, o que o engine aceita, e o que fazer — mais `NAO HA MIGRADOR`; `:399` prova a instrução **inversa** para versão futura; `:410` prova que a recusa vem ANTES da validação de schema. `tests/test_migrations.py:67` alcança o ramo inalcançável por registro injetado e prova que a decisão vem do **registro**, não da aritmética |
| 8 — fato citado no `GM_NOTES.md` e ausente do `ground_truth.yaml` é recusado | **PASS** | `range-core/engine/citacoes.py`, forma lida de `ground_truth.schema.yaml §$defs/fact_id_pattern`; `tests/test_citacoes_de_fato.py:88` lê o padrão do contrato e `:91` recusa alto se o `$def` sumir; `:224` recusa **na carga** contra pack materializado em disco, e `:236` pega a forma não casada (`gt-fixture-001`). **Dirigido por `fact_id`, não por seed** |
| 9 — reconstrução do exercício de 4 h em < 3 s | **PASS** | `check_prova_do_exercicio_4h.py` rc=0: **0,037 s contra 3 s**, com máquina/data/stack gerados por código (`Windows-11`, `2026-09-13`, `PostgreSQL 16.4 / psycopg 3.2.12 / migration 0004`) — a forma que T12 exige. `total_s = read_all + project`, e `read_all` já inclui a cadeia; `cadeia_s` é decomposição, não parcela omitida |
| *(T12, critério 7 — `capability_gap` citando objetivo inexistente, com posição)* | **PASS** | Não está na lista de 9, mas é critério de aceitação desta fase: `test_range_cli_lint.py:355`, caminho `$.injects[0].decision_point.options[0].capability_gap.objectives_affected[0]`, posição ancorada em `OBJ-99` |
| *(Fase 4, itens 1 e 4 — regressão)* | **PASS** | `check_provas_de_container.py` rc=0 sobre esta árvore; `demo` e `reinicio` rc=0 |

**DoD: 9 de 9 pela letra.**

---

## TESTES QUE NÃO PROVAM O REQUISITO

**Nenhum encontrado.** Verifiquei por leitura das asserções, não dos nomes:

- **Nenhum skip disparou**, e não há skip incondicional: `grep` por `@unittest.skip`/`expectedFailure` em `tests/` devolve **zero**; os cinco módulos com `skipIf/skipUnless` são todos condicionais a URL de serviço, e o sumário saiu `OK` sem `(skipped=N)`.
- **Nenhum mock de comportamento no código da fase.** O único monkey-patch é `tests/test_migrations.py:74-82`, e ele é o **objeto do teste** (prova que a mensagem decide pelo registro), com `finally` e uma asserção própria de restauração (`:88`).
- **`um_achado` (`test_range_cli_lint.py:195`) exige exatamente UM achado do sítio esperado** — fixture com dois defeitos passaria provando o errado, e a colheita torna esse engano fácil.
- **`assertPosicaoNaAncora` (`:210`) deriva a linha do texto da fixture**, exige a âncora exatamente uma vez e exige `posicao.exata` — posição aproximada não satisfaz critério que pede posição.
- **Controles positivos em toda família de recusa**: pack limpo sem achados (`:231`), pacote apenas-manifesto passando (`:234`), ator real com fonte citável passando (`:604`), pack íntegro carregando (`test_citacoes_de_fato.py:213`). Sem eles, uma regra que recusasse tudo passaria em todos os negativos.
- **`test_o_predicado_e_o_MESMO_do_verificador_da_arvore` (`:691`)** afirma identidade de objeto entre o predicado do linter e o de `dados_sinteticos` — é o que impede a segunda implementação que "não falha alto, deixa passar".
- **O limite é afirmado como limite** (`:703`): domínio em prosa escapa, e o teste fica vermelho no dia em que alguém estender a varredura. Cobertura declarada em vez de suposta.
- **O verificador novo tem prova negativa executada**: 9 venenos em 7 direções + 2 controles, incluindo o modo "pack ausente do checkout" — é o que me permite afirmar que o rc=0 é conferência, e não degradação silenciosa.

Observação sem finding (mantida das rodadas anteriores, e continuo concordando): `branching.percorre` enumera um caminho por braço de cada ponto, e não o produto cartesiano entre pontos de linhas distintas. `04` §6/§9 e T12 não exigem o produto, e as janelas são por linha — leitura registrada em `fase_7.md` §5.2 **antes** do código.

Segunda observação sem finding: o ramo *"HÁ CAMINHO DE MIGRAÇÃO"* (`pack_loader.py:726`) **recusa** em vez de migrar, e o próprio texto diz que chegar ali é defeito do loader. Hoje é inalcançável (`MIGRACOES` vazio) e a norma só cobra migração *"quando houver N-1 real"* — falha fechada e declarada, não desvio.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **O conteúdo do pack `ransomware-universidade`.** `scenarios/` não existe neste checkout (`.gitignore:126`, decisão da Fase 5). A prova de 4 h fecha a lacuna **de identidade** (7 SHA-256) e **de comportamento** (lint limpo, travessia completa, 0,037 s), mas não me deixa ler os bytes: não conferi os 40 injects, os 5 `decision_point`s, nem a prosa voltada ao participante.
2. **`GM_NOTES.md` do pack real não passa por varredura mecânica de IOC.** `confere_ausencia_de_ioc` cobre os documentos YAML; `GM_NOTES.md` é prosa e fica fora, com o limite declarado no código (`pack_loader.py:1259`) e destinatário `revisão humana` no registro do contrato. Declarado, não medido.
3. **As três provas, rodando.** Não vi nenhuma executar. O que as amarra a este objeto é o hash da árvore, conferido em cada uma, mais os sete hashes do pack na de 4 h. Limite declarado na P7-2: a prova amarra **árvore**, não commit, e a árvore cobre só conteúdo rastreado.
4. **`gen_pins.py --check` (R8).** Fora da allowlist do auditor; **não executado**. O último commit (`c9f61b2`) é repin declarado, em commit próprio com motivo, como a R8 exige — mas não pude confirmar a identidade pins × HEAD por execução.
5. **O pipeline da Estrutura Agêntica** (`.claude/verify/run.sh`, `compliance-audit.sh`). Fora da allowlist; não executado. O hook de prompt registra que nenhum pipeline verde foi gravado nesta máquina — declarado, não omitido.
6. **O ambiente da suíte não é o canônico.** O venv do lançador é **Python 3.14.7**, contra `00_MASTER_SPEC.md` §8 e o `python-version: "3.12"` do CI. Os 858 passaram, logo não houve vermelho a diagnosticar por causa (R2 §3) — mas a suíte que rodei não é a do CI Linux 3.12. A diferença aparece como `ResourceWarning` em massa do `psycopg`, já registrada na P7-14.
7. **A autorização do operador para a isenção do M1.** Só existe para mim como transcrição dentro do commit auditado (`fase_7.md:2502`), e mensagem/arquivo não é autorização (R2 §4).
8. **P7-10 — o gabarito só tem Linha B.** `fase_7.md:1238` declara que o `ground_truth.yaml` materializado não tem fato de `initial_access`/`exfiltration`, com `TTRV` incomputável. Não pude medir (o arquivo não está na árvore). Está `DECIDIDA` — gerador aprende a Linha A na Fase 8 —, logo não é órfão; registro que ela pesa sobre o insumo do item 9.
9. **P7-11 — condição temporal de branch carrega e nunca ramifica.** O branching de runtime não é entregável desta fase (exclusão declarada em `inject_engine.py:60-68`), então não há caminho executável onde o defeito se manifeste hoje. Tem linha na §6, e o pack de 4 h evita condição temporal.
10. **Comportamento de runtime em container** além das duas provas gravadas. Inalcançável por construção — o auditor não sobe stack.
