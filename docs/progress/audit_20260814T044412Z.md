# Auditoria de checkpoint - 2026-08-14T04:44:12.742192+00:00

- fase: **0**
- commit auditado: `2065d2449dfd0703aee7ed8fc7e59ebbe456bd1a`
- session_id: `8e0e20c0-d353-42ec-948c-435487a75c61`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

The audit worktree was destroyed mid-session, so I must report on the evidence I collected before teardown and be explicit about the limits.

# AUDITORIA — FASE 0

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- **[B1] A regra de contenção do hook do auditor só cobre travessia relativa; caminho absoluto e `~` a atravessam intactos**
  Arquivo: `user-scope/hooks/readonly_bash.py:77` (padrão), com `:17`, `:19`, `:44` (allowlist)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §"Definition of Done" item 4 ("Hook do auditor bloqueia escrita deliberada"); `CLAUDE.md` §"Fluxo por fase" (razão de o auditor viver fora do repositório); critério de admissão declarado em `scripts/phase0_negative_tests.py:204-214`
  Evidência: o padrão negado é `\.\.[/\\]|(?:^|\s)\.\.(?:\s|$)` — casa exclusivamente a sequência `..`. A allowlist libera `find` (`:44`), `pytest`/`python -m pytest` (`:17`) e `ruff|mypy|eslint|tsc` (`:19`). Logo, trocando `../../X` pelo caminho absoluto do mesmo arquivo, nenhum padrão de `DENIED_ANYWHERE` casa e a allowlist aprova o segmento: `find . -fprint0 /c/Projetos/aurora-cyber-range/tools/codegen.py`, `pytest --junitxml=/c/Projetos/aurora-cyber-range/CLAUDE.md`, `find /c/Projetos/aurora-cyber-range -delete` e `find . -fprint0 ~/.claude/hooks/readonly_bash.py` retornam `rc=0`.
  Por que é blocker: o commit candidato tem como propósito declarado "contencao fecha o B1" e afirma reduzir os buracos de 10 para 2 "ambos contidos no worktree descartável". Sete das oito formas declaradas como fechadas reabrem literalmente pela troca de grafia do mesmo alvo. A última forma citada sobrescreve o próprio hook instalado do auditor — exatamente o comprometimento que a colocação em escopo de usuário existe para impedir.

- **[B2] O harness certifica a propriedade de contenção testando apenas uma grafia do alvo**
  Arquivo: `scripts/phase0_negative_tests.py:189-198` (`ESCRITA_DELIBERADA`) e `:215-221` (`BURACOS_CONHECIDOS`)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §"Teste negativo obrigatório" ("Um verificador que nunca falhou contra uma violação plantada não é um verificador"); DoD item 4
  Evidência: as oito entradas de travessia (`:189-198`) usam todas o literal `../../`. O comentário do código afirma "O invariante nao e a flag, e o ALVO" (`readonly_bash.py:67`), mas nenhum probe exerce um alvo absoluto. `BURACOS_CONHECIDOS:218` declara `find . -delete` como "contida no worktree", enquanto o mesmo caminho de allowlist aprova `find /c/Projetos/aurora-cyber-range -delete`, que não é contido — violando o critério de admissão que o próprio arquivo fixa em `:204-214` ("Escrita que alcanca o worktree principal e finding, nao defeito aceito").
  Por que é blocker: o harness permanece verde enquanto a propriedade que ele declara provar é falsa. É o oráculo único de DoD item 4 e ele atesta contenção que não existe.

### HIGH

- **[H1] DoD item 4 está aberto na metade "libera leitura", por declaração do próprio projeto e por medição minha**
  Arquivo: `scripts/phase0_negative_tests.py:133-155` (`FALSOS_BLOQUEIOS_CONHECIDOS`, 11 entradas)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4 ("…e libera testes/verificadores de leitura")
  Evidência: medido nesta sessão contra a cópia instalada — `git log --oneline -5 2>&1 | head -20` e `ls -R tools scripts .github docs/process 2>&1 | head -60` foram recusados com "redirecionamento de saida para arquivo"; `git ls-tree -r --name-only HEAD -- docs/progress` foi recusado como "fora da allowlist". São leituras puras. O harness afirma o defeito como aberto ("P23 reaberto") e sai `0` mesmo assim.
  Por que é high: o item de DoD é binário e está aberto; o harness verde não o satisfaz.

- **[H2] `Write` ausente do deny de secrets**
  Arquivo: `.claude/settings.json:6-13`
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 7; `CLAUDE.md` §"Secrets"; `05_SECURITY_REQUIREMENTS.md` §6
  Evidência: o deny lista `Read(.env)`, `Edit(.env)`, `Read(.env.*)`, `Edit(.env.*)`, `Read(secrets/**)`, `Edit(secrets/**)`. Não há nenhuma entrada `Write(...)`. Criar ou sobrescrever `.env` ou `secrets/<x>` pela ferramenta Write não é negado.

- **[H3] O documento que define a DoD da Fase 0 foi alterado pelo mesmo commit julgado por ela, fora de qualquer gate**
  Arquivo: `docs/process/PHASE_0_CHECKLIST.md:10-11`, alterado em `2065d244` junto de `readonly_bash.py` e `phase0_negative_tests.py`
  Requisito violado: `CLAUDE.md` §"A especificação é imutável durante a implementação"; `.github/workflows/invariants.yml:110-113`
  Evidência: o commit remove `log_audit.py` dos Entregáveis e insere `scripts/audit_report.py`. O conjunto `CODE` do job `spec_freeze` (`:110-113`) não inclui `docs/process/`, e o caminho também não está em `docs/spec/` — logo nem a regra "spec e código no mesmo PR" nem a exigência de título `spec-change:` incidem. O critério de aceitação da fase é editável pelo commit que ela avalia.

- **[H4] `hook_copies_in_sync` é advisório e nunca gateia no CI**
  Arquivo: `scripts/phase0_negative_tests.py:342-349`
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §DoD item 4
  Evidência: quando `~/.claude/hooks/readonly_bash.py` não existe, a função imprime `AVISO` e retorna, sem falhar. O próprio comentário reconhece "esperado em CI, onde nao ha escopo de usuario". Todos os probes de hook rodam contra `READONLY_HOOK_SOURCE` (`:227`). No CI — o único gate real — a divergência entre a fonte versionada e a cópia que efetivamente executa nunca é verificada.

### MEDIUM

- **[M1] O worktree de auditoria foi destruído durante a auditoria**
  Arquivo: `.aurora-worktrees/audit/` (raiz da sessão)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md` §"Auditoria das fases seguintes"
  Evidência: às 01:38 a raiz continha `docs/`, `scripts/`, `tools/`, `.github/`, `user-scope/`, `.git`; `git status --short --branch` respondeu `## manutencao-p11-p23` com árvore limpa. Após a chamada seguinte, `ls -la .` retorna apenas `.` e `..` com mtime 01:40, incluindo remoção de `.git`. `find . -maxdepth 2` confirma raiz vazia. A sessão está confinada a esse diretório, então nada mais pôde ser lido ou executado.
  Observação: não consigo atribuir a remoção a um componente específico — não a atribuo ao launcher sem prova.

### LOW

- **[L1] `docs/progress/` não foi materializado no worktree de auditoria** enquanto `git show HEAD --stat` lista `docs/progress/fase_0.md` (+116) como parte de `2065d244`. `git ls-files docs/progress` retornou vazio e o diretório não existia em disco já às 01:38, antes da destruição. Não determinei a causa (sparse-checkout, exclude local ou falha do launcher).

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis verificadores liberam árvore limpa | NÃO VERIFICADO | Execução negada pela camada de permissão da sessão |
| 2. Os seis detectam as violações de `phase0_negative_tests.py` | NÃO VERIFICADO | `python scripts/phase0_negative_tests.py` negado; probes lidos (`:364-503`) cobrem os seis, incluindo ramos de ausência e divergência do `codegen` |
| 3. Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | NÃO VERIFICADO | `.claude/hooks/check_architecture.py` não pôde ser lido antes da destruição |
| 4. Hook do auditor bloqueia escrita deliberada e libera leitura | **FAIL** | B1, B2, H1, H4 |
| 5. Hook do scenario-designer bloqueia fora de `scenarios/` | NÃO VERIFICADO | `scenario_scope.py`/`scenario_bash.py` não lidos |
| 6. `ground_truth.yaml` e `GM_NOTES.md` **não** no `.gitignore` | PASS | `.gitignore` lido integralmente (41 linhas); ausentes, com nota explícita em `:39-40` |
| 7. `.env`/secrets negados em `settings.json` | **FAIL** | H2 — `Write` ausente do deny (`.claude/settings.json:6-13`) |
| 8. Auto Mode desabilitado | NÃO VERIFICADO | `.claude/settings.json:5` usa `"disableAutoMode": "disable"`; não consigo validar a chave contra o schema do Claude Code offline |
| 9. Primeiro push deixa `arquitetura` e `seguranca` verdes; `spec_freeze` PR-only | PARCIAL | Config confere: `invariants.yml:47` restringe `spec_freeze` a `pull_request`. Resultado de execução não verificável |
| 10. PR descartável: `spec_freeze` falha com spec+código juntos | NÃO VERIFICADO | Requer GitHub; sem rede |
| 11. PR descartável: só spec exige título `spec-change:` | NÃO VERIFICADO | Idem |
| 12. Branch protection exige os três contexts | NÃO VERIFICADO | Requer API do GitHub |
| 13. `spec-v1.0` depois de CI + branch protection | NÃO VERIFICADO | `git tag` está fora da allowlist do auditor (`readonly_bash.py:16`, comentário `:14-15`) |
| 14. `/doctor` sem apontamentos | NÃO VERIFICADO | Fora do meu alcance |

## TESTES QUE NÃO PROVAM O REQUISITO

1. `scripts/phase0_negative_tests.py:189-198` — as oito provas de travessia usam só o literal `../../`. Provam a grafia, não o invariante de alvo que o código declara defender.
2. `scripts/phase0_negative_tests.py:215-221` — `BURACOS_CONHECIDOS` declara `find . -delete` e `find . -fprint0 out.txt` como contidos, mas o mesmo padrão de allowlist aprova os gêmeos não contidos com alvo absoluto. A declaração honesta é usada para legitimar exatamente o que o critério de admissão de `:204-214` proíbe.
3. `scripts/phase0_negative_tests.py:342-349` — `hook_copies_in_sync` retorna sucesso silencioso quando a cópia instalada não existe, isto é, sempre no CI. Nenhum gate real compara a fonte com o arquivo que executa.
4. Todos os probes de hook (`:227`) rodam contra a **fonte versionada**, não contra a cópia instalada. Combinado com B1 (que permite sobrescrever `~/.claude/hooks/readonly_bash.py`), o harness pode passar verde contra uma fonte íntegra enquanto o hook em execução foi alterado.

Registro em contrapartida: os probes dos seis verificadores são de qualidade alta. `expect_fail` (`:66-99`) exige `rc == 1` exato, saída não vazia e menção ao arquivo plantado, o que distingue detecção de crash de ferramenta; e os probes cobrem ramos que auditorias anteriores expuseram (isenção ancorada, ramo de `event_type`, divergência vs. ausência no `codegen`). Não encontrei ali teste que passe sem provar o requisito — apenas não pude executá-los.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Nenhuma execução.** Toda tentativa (`python scripts/phase0_negative_tests.py`, `python tools/check_core_boundary.py`, `python tools/check_contract_literals.py`) foi recusada pela camada de permissão da sessão, não pelo hook. Portanto **nada nesta auditoria é prova de execução**; B1 e B2 derivam de leitura de regex e da allowlist, não de comando executado. Considero a leitura conclusiva para B1, mas ela não substitui medição.
- **Código-fonte dos seis verificadores.** Não li `check_core_boundary.py`, `check_contract_literals.py`, `check_event_envelope.py`, `check_security_constraints.py`, `check_synthetic_data.py` nem `codegen.py`. Não posso afirmar que usam AST em vez de regex, que respeitam as seis interfaces obrigatórias (`PHASE_0_CHECKLIST.md:39-46`), nem que `codegen.py --check` é de fato read-only.
- **Os três hooks de projeto**, `scripts/audit_report.py`, `scripts/start_checkpoint_audit.sh`, `finalize_phase0.sh`, `docs/progress/fase_0.md` e `docs/spec/01_ARCHITECTURE.md` §2 — não lidos antes da destruição do worktree. Em particular, não pude confrontar as afirmações de status da DoD registradas em `fase_0.md` com o estado real, que era um dos meus alvos principais.
- **`09_EVENT_MODEL.md`** — não chegou a ser lido; a Fase 0 não produz emissão de evento, mas registro a omissão.
- **Itens 10–14 da DoD** dependem de GitHub, tags e `/doctor`, todos fora do alcance de um worktree sem rede.

Um retesto precisa rodar em ambiente onde o harness e os verificadores possam de fato ser executados, e sobre um worktree que sobreviva à sessão.
