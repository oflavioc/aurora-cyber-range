# Auditoria de checkpoint - 2026-09-26T13:43:17.131857+00:00

- fase: **9**
- commit auditado: `cb8d123113df86a1cf5caf8ca7f24ffa5557a305`
- session_id: `715d9022-37b3-4d0d-b9d4-94882b92291e`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

```text
# AUDITORIA — FASE 9

## VEREDITO: FAIL

Objeto: `cb8d123113df86a1cf5caf8ca7f24ffa5557a305`, árvore `a628269435743430e8619c4ba51bb1d054491235`, working tree limpo. Base: `c77120c` (âncora em `docs/process/phase_anchors.tsv:9`, bate). Guarda de base: PORTA, verbatim do lançador. Nenhum arquivo de `docs/spec/` no diff.

## FINDINGS

### BLOCKER

- [B1] O passo de CI `evidence verify` reprova neste commit: o `MANIFEST.json` versionado não tem o `_banner` que o contrato passou a exigir
  Arquivo: tests/fixtures/pack_exemplo/evidence/MANIFEST.json:1-2 (sem `_banner`); contracts/evidence.schema.yaml:22 (`required: [_banner, generated_from, sources]`); .github/workflows/invariants.yml:713-714
  Requisito violado: 04 §8 ("`lint`, `dryrun` e `evidence verify` rodam no CI"); 05 §4 (banner em todo artefato gerado, sem exceção); 06 T13 (banner em cada arquivo)
  Evidência: executei `.aurora-audit/venv/Scripts/range-cli evidence verify tests/fixtures/pack_exemplo`. Saída: `MANIFEST.json nao valida contra o contrato — $: '_banner' is a required property`, rc=2. É o mesmo comando do passo de `invariants.yml:714`.
    - `git log -- tests/fixtures/pack_exemplo/evidence/MANIFEST.json`: último toque em `4a19912`. Isso foi antes de `3b5c9d5`, a correção do L2 da 3ª rodada, que tornou `_banner` obrigatório.
    - O produtor (`range-core/evidence/manifesto.py:119`) escreve a chave. O artefato versionado não foi regenerado.
    - Nada na árvore acusou:
      - a suíte deu 1135 testes, OK, sem pulos, e nenhum teste confere o `evidence/` versionado (`tests/test_evidence_build.py:572-633` só olha CRLF e `.gitattributes`);
      - `check_contract_examples` diz "Instancias reais validadas: 11" e não inclui este manifesto;
      - `scripts/check_banner_de_simulacao.py:135-140` isenta `MANIFEST.json` (`SEM_BANNER`). A justificativa escrita diz que um JSON de índice "nao tem onde po-lo sem deixar de validar contra `evidence.schema.yaml` (`additionalProperties: false`)". O contrato desmente isso desde o L2.
  Por que é blocker: o gate que o H1 da 1ª rodada criou para cumprir 04 §8 está vermelho no commit candidato. Além disso, há na árvore um artefato gerado sem o banner de 05 §4, que o próprio contrato agora exige. A correção do L2 mudou o contrato e o produtor, mas nem o único manifesto versionado nem o verificador de banner acompanharam.

### HIGH

- [H1] Metade Linha B de `08` §3 adiada sem decisão do operador registrada, e com saída nomeada que contraria 00 §5.3
  Arquivo: docs/progress/fase_9.md:696 e :912-942 (P9-4); domains/academus/seed/gabarito.py:162-189
  Requisito violado: 08 §3 (fontes v1: `database_audit.jsonl` carrega "alterações de nota com IP e sessão (Linha B)"); 00 §5.3 ("toda evidência ... é projeção de fato canônico declarado em `ground_truth.yaml`. Nunca gerada independentemente"); CLAUDE.md §Fluxo ("Item inviável → pare e explique, não contorne")
  Evidência: a correção do B1 da 3ª rodada tirou `projections` dos fatos da Linha B. Com isso, nenhuma fonte v1 carrega a Linha B. A P9-4 põe o vencimento em "a fase que der ao motor de evidência uma fonte que projete da TRILHA (business state), e não de `facts`". Isso é exatamente o que 00 §5.3 proíbe, e contradiz o item 1 da própria DoD. Existe uma saída dentro da spec: declarar a população inteira da espécie como fatos, que a guarda `RespostaEntregue` aceita por desenho, "projete a população inteira daquela espécie". O registro a descarta por preferência de desenho ("o `ground_truth.yaml` viraria cópia da tabela"), sem citar norma que a proíba. Diferente da P8-5 (`fase_9.md:715`), não há registro de decisão do proprietário.
  Consequência: um requisito v1 do documento da própria fase sai da fase por decisão unilateral. A pendência que o carrega aponta para uma saída que o MASTER veda, então quem a abrir herda um conflito de spec em vez de um trabalho.

### MEDIUM

- [M1] O caminho `telemetry_emitted` não passa pela guarda `RespostaEntregue` que protege o `cef.log`
  Arquivo: range-core/engine/loader/pack_loader.py:405-443; range-core/evidence/projecao.py:399-413
  Requisito violado: 08 §2 (um contrato só para evidence-simulator e telemetry-forwarder); 05 §6 (gabarito fora do que chega ao participante)
  Evidência: `_telemetria_do_pack` faz `cobertura_de` + `programar` e nada mais. `RespostaEntregue` só existe em `projetar`, que só `evidence build`/`verify` chamam. `range_cli/lint.py` não chama `projetar` (grep).
    - Com um gabarito que declare `projections: [cef]` só nos casos de `line_b_cases`, o `evidence build` recusa.
    - O mesmo pack, porém, carrega pelo `load_pack`, e o `start()` grava no store um `GRADE_CHANGE_RETROACTIVE` por caso, e só por caso. É a resposta entregue no SIEM, que é o que o participante vê.
    - Latente: o pack atual não roteia a Linha B para `cef`.
  As outras três guardas estão cobertas por construção nesse caminho: IOC pelo loader (`pack_loader.py:1351`), e os campos do payload são todos `projectable`. Só a guarda de cobertura falta.

- [M2] O teste do 1º critério de T13 omite `database_audit`
  Arquivo: tests/test_evidence_generators.py:540-551 (`FATO.projections` sem `database_audit`) e :512-519 (`LEITORES` o declara)
  Requisito violado: 06 T13 ("para cada `fact_id`, todas as projeções declaradas ... usuário, IP e timestamp mutuamente consistentes")
  Evidência: `database_audit.py:37-44` escreve `actor` e `source_ip`, e o fato de exfiltração do gabarito real projeta nele (`linha_a.py:168`). Os únicos casos que leem essa fonte são estes:
    - `test_o_database_audit_traz_o_volume_da_leitura_em_massa`, que só olha `records_affected` (:180-185);
    - `test_todo_registro_JSONL_carrega_o_instante_do_fato`, que só olha a chave `exercise_time` (:187-191).
  Um `CAMPOS` sem `actor`/`source_ip`, ou com o ator trocado por outro do elenco, não é alcançado. É a mesma família do M1 da 3ª rodada (fonte saindo da comparação em silêncio), agora por ausência no fato de teste.

- [M3] A medição do item 7 usa um payload menor que o que o produto emite, contra a justificativa escrita no próprio script
  Arquivo: scripts/medida_do_exercicio_4h.py:276-303
  Requisito violado: 06 T13 / 07 Fase 9 item 7 ("com `telemetry_emitted` no volume de um exercício de 4 h")
  Evidência: o comentário em :276-278 diz que "um payload sintético de forma diferente mediria outra coisa: `read_all` desserializa JSON, e o custo depende do tamanho".
    - O payload medido tem `signature`, `severity`, `outcome`, `src` e `suser`.
    - Desde o H1 da 3ª rodada, o payload do produto carrega também `event_time` e `ingest_time` (`forwarder.py:116-118`, `emissao.py:112`), além de `dst`/`cnt` quando o fato os tem.
    - A prova (2,268 s contra 3 s) mede, então, eventos menores que os do range. O próprio registro diz que a folga é "da mesma ordem" da dispersão (`fase_9.md:668`).

- [M4] Duas pendências vencem "esta fase" e seguem `ABERTA`, sem disposição nem novo vencimento
  Arquivo: docs/progress/fase_9.md:688 (P9-1: "esta fase, junto do fechamento") e :692 (P2-11: "**esta fase**")
  Requisito violado: 07 §Registro de progresso (pendências e próxima fase); enum fechado da Fase 7 (`VENCIDA` existe para isto)
  Evidência: a P2-11 diz em :821-823 que "Esta fase não pode alegar tê-la fechado nem tê-la medido", e passa o gatilho à P9-2 em prosa, mas a coluna de vencimento continua "esta fase". É a mesma forma do M4 da 1ª rodada (P1-13 vencia a fase e fechava sem disposição).

### LOW

- [L1] Os laudos da 2ª e da 3ª rodada são citados e não estão na árvore
  Arquivo: docs/progress/fase_9.md:472 (`audit_20260923T191036Z.md`) e :575 (`audit_20260924T172008Z.md`); .gitignore:77-81
  Requisito violado: decisão de processo registrada em `.gitignore:77-81` ("SAO versionados de proposito: registro de auditoria que nao entra no repositorio nao e registro")
  Evidência: `git ls-files docs/progress` não tem nenhum dos dois. O laudo da 1ª rodada foi versionado em `a6fb58d`. Consequência para esta rodada: a conferência de achados órfãos (P7-6) só pôde ser feita contra a transcrição do implementador (§2.10/§2.11), e não contra os laudos.

- [L2] O docstring de `database_audit.py` afirma o que o B1 da 3ª rodada desfez
  Arquivo: domains/academus/evidence_generators/database_audit.py:5-22
  Requisito violado: 08 §3, como descrição do que a fonte carrega; é o mesmo tipo de resíduo que o M2 da 3ª rodada
  Evidência: diz que a fonte é "a unica que atravessa as duas linhas" e que "A Linha B so chega aqui desde a peca 3 da Fase 9". O gabarito (`gabarito.py:162-189`) e a P9-4 dizem o contrário.

## DoD — item a item
| Item | Status | Evidência |
|---|---|---|
| 1 — Toda fonte é projeção de `fact_id`; nenhum gerador inventa entidade | PASS (ver H1) | `projecao.py:349-484` com as quatro recusas; `elenco.py` tem oráculo de endereço e de hostname. Suíte completa: 1135 testes, OK, zero pulos, 170,4 s, Postgres/Redis no ar. `check_core_boundary` e `check_core_contract_imports` rc=0. O limite declarado (ator nu) foi verificado por leitura. A metade Linha B de 08 §3 está ausente: H1 |
| 2 — `evidence verify` dirigido por fato | PASS no verbo / FAIL no gate de CI | `build.py:145-317` reprojeta e confere `projects_facts`, `window`, `delivery_mode` e `format`. Executado: rc=2 sobre o `evidence/` versionado. O verbo acusa corretamente, e o objeto versionado está inválido: B1 |
| 3 — `precursor_events.jsonl` reproduzível; edição detectada por hash | PASS | `projecao.py:100` (nome da spec), `precursor.py:44-49`, `build.py:226-245` (sha256 e depois reprojeção). Suíte verde |
| 4 — Telemetria CEF é projeção | PASS (ver M1) | `cef.py` do adapter renderiza `programar`, e o loader usa a mesma `cobertura_de`. `tests/test_telemetry_forwarder.py:190-232` testa igualdade total; `test_inject_engine.py` `OExercicioEMITEATelemetria` tem 11 casos com o event store real (InMemory) |
| 5 — Sem anexo, binário, IOC real, domínio roteável | PASS | `check_security_constraints`, `check_synthetic_data`, `check_contract_examples` (13/98), `check_secoes_de_seguranca`: todos rc=0. O critério de banner de T13 falha no `MANIFEST.json` versionado: B1 |
| 6 — Replay respeita o clock de exercício | PASS | `forwarder.py:190-210` lê só `elapsed_seconds()` (leitura integral). Em produção tudo vence em t=0 e só o `start()` chama `tick` (grep: `inject_engine.py:326` é o único chamador). Limite declarado, preso à P6-3 |
| 7 — Reconstrução < 3 s com `telemetry_emitted` no volume de 4 h | PASS (ver M3) | `check_prova_do_exercicio_4h.py`: árvore `a62826943574` igual à deste checkout, 7 arquivos do pack idênticos, 2,268 s contra 3 s, 96.650 eventos (96.000 `telemetry_emitted`) |

## TESTES QUE NÃO PROVAM O REQUISITO
1. `tests/test_evidence_generators.py:522-621` (`AsProjecoesDoMesmoFatoSaoMUTUAMENTEConsistentes`): cobre 5 das 6 fontes que carregam ator ou origem. `database_audit` fica fora (M2).
2. A suíte inteira sobre o `evidence/` versionado: `tests/test_evidence_build.py:572-633` confere fim de linha e atributo, nunca a validade do manifesto contra o contrato. Suíte verde com CI vermelho (B1).
3. `tests/test_telemetry_forwarder.py:349-356` (`test_DURANTE_A_PAUSA_nada_novo_vence`): o próprio dublê não avança quando está pausado (`:87-90`), então o caso exercita o dublê. A propriedade vale por construção (verificada por leitura). Limite de instrumento já registrado na 1ª rodada, não é finding novo.
4. `scripts/medida_do_exercicio_4h.py:276-303`: a prova do item 7 mede um payload de forma diferente da do produto (M3).

## O QUE EU NÃO CONSEGUI VERIFICAR
- **Execução real do CI do GitHub.** B1 se apoia em ter rodado localmente o mesmo comando do passo `invariants.yml:714`, com o mesmo objeto versionado, e não numa execução do workflow.
- **Mutantes.** Não posso escrever na árvore. As afirmações de M2 ("nenhum teste pega um `CAMPOS` sem `actor`/`source_ip`") e de M1 vêm de leitura dos casos e de grep dos chamadores, não de mutação executada.
- **As provas do lançador.** Demo e reinício de container, seed completo (152,1 s / 155,0 s; o seed rodou em python 3.14.7) e exercício de 4 h: não vi rodar. Conferi só o hash da árvore (`a62826943574`, os três batem) e li a saída íntegra dos verificadores.
- **Os laudos das rodadas 2 e 3.** Não estão na árvore (L1), e o hook bloqueia leitura fora do worktree. A regra de achado órfão foi aplicada contra a transcrição de `fase_9.md` §2.10/§2.11. Por ela, todos os M/L foram dispostos, e o L2 da 3ª rodada foi disposto de forma incompleta (B1).
- **Envio syslog.** `01` §6 descreve o telemetry-forwarder com "CEF via Syslog UDP/TCP ... Modo Live e modo Replay", e esta fase não implementa envio por fio. Nenhum item da DoD da Fase 9 o cobra, e 07 põe "serviços externos" nas Fases 11–12. Não classifiquei como finding, mas também não verifiquei a qual fase isso cabe.
- **`bash .claude/verify/run.sh`.** Fora da allowlist do auditor. Em compensação rodei individualmente, todos com rc=0: `codegen --check`, `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `check_core_contract_imports`, `check_store_read_surface`, `check_fold_authority`, `check_insumo_de_metrica`, `check_contract_examples`, `check_banner_de_simulacao`, `check_secoes_de_seguranca`, `check_gabarito_fora_do_git`, `check_progress_consistency`, `check_readme_atual`.
```

ARQUIVOS_TOCADOS: nenhum

RESUMO: A Fase 9 é reprovada por um BLOCKER. O `evidence verify`, que o CI roda sobre o pack de exemplo versionado, sai com rc=2 porque o `MANIFEST.json` versionado não tem o `_banner` que a correção do L2 tornou obrigatório. A suíte passa verde porque nenhum teste valida esse objeto, e o verificador de banner isenta o manifesto com uma justificativa que o contrato hoje desmente. Há ainda 1 HIGH (a Linha B de 08 §3 adiada para uma saída que 00 §5.3 proíbe, sem decisão do operador), 4 MEDIUM e 2 LOW.

EVIDÊNCIA:
- Suíte completa no venv da auditoria: 1135 testes, OK, zero pulos, 170,4 s.
- `range-cli evidence verify tests/fixtures/pack_exemplo`: rc=2.
- 16 verificadores estáticos: rc=0.
- As três provas do lançador batem com a árvore `a62826943574`.
- Não executados: CI do GitHub, mutantes e `run.sh`, pelos motivos acima.

DEPENDÊNCIAS: implementador corrige B1 (regenerar o manifesto versionado e fechar a cegueira do teste e do verificador de banner). O HIGH (P9-4) depende de decisão do operador entre as saídas.
