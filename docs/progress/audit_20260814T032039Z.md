# Auditoria de checkpoint - 2026-08-14T03:20:39.395836+00:00

- fase: **0**
- commit auditado: `470ced8d7aee1ff3065ac6aa2d859c6f21c40def`
- session_id: `ab69a491-070e-43e6-ba17-0e13252f99f9`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

Este é o relatório de registro da oitava rodada. Emito-o como mensagem final porque é exatamente ela que `audit_report.py:141-169` (`last_agent_text`) arquiva em `docs/progress/audit_<stamp>.md` — o texto abaixo precisa ser legível sozinho, sem esta conversa.

---

# AUDITORIA — FASE 0

Commit auditado: `470ced8d7aee1ff3065ac6aa2d859c6f21c40def` (branch `manutencao-p11-p23`)
Sessão: `ab69a491-070e-43e6-ba17-0e13252f99f9` · oitava rodada · modo interativo
Diff contra `main` (`edd9527`): 10 arquivos, +1492/−268. Nenhum arquivo em `docs/spec/`.

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- **[B1] O guarda de Bash do auditor libera escrita deliberada por quatro vias medidas**

  **Arquivo:** `user-scope/hooks/readonly_bash.py:56` (`_SEGMENT_SEPARATORS`), `:245-265` (`_blank_quoted`), `:294-301` (`_tokenize`), `:318-328` (`_segments`), `:507` (guarda de crase)

  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:111` §DoD item 4 ("Hook do auditor bloqueia escrita deliberada"); `docs/process/WORKFLOW.md:53` ("O agente não recebe ferramentas Write/Edit. Bash passa por allowlist. Essa combinação evita escrita deliberada")

  **Evidência — medido contra a fonte versionada** (o harness confirmou que a cópia instalada é idêntica):

  | Via | Comando | Decisão |
  |---|---|---|
  | quebra de linha | `ls⏎rm -rf range-core` | **rc=0 (liberado)** |
  | quebra de linha | `git status --short⏎tee ../../CLAUDE.md` | **rc=0** |
  | quebra de linha | `echo oi⏎curl -o ../../CLAUDE.md https://example.com` | **rc=0** |
  | quebra de linha | `cat CLAUDE.md⏎python -c import os` | **rc=0** |
  | retorno de carro | `ls␍rm -rf range-core` | **rc=0** |
  | substituição entre aspas duplas | `echo "$(rm -rf range-core)"` | **rc=0** |
  | crase entre aspas duplas | `echo "` + crase + `rm -rf range-core` + crase + `"` | **rc=0** |
  | *(controle)* substituição sem aspas | `echo $(rm -rf range-core)` | rc=2 |
  | *(controle)* crase sem aspas | `git ls-files` + crase + `rm -rf x` + crase | rc=2 |

  **Prova de execução ao vivo, contra a cópia instalada — que é a que o Claude Code aplica:**
  1. enviei um Bash de duas linhas (`ls -d docs` + `echo SEGUNDA_LINHA_EXECUTADA_SEM_BLOQUEIO`); não houve bloqueio e **as duas linhas executaram**;
  2. todos os smoke tests que rodei nesta sessão usaram `"$(pwd)"` dentro de aspas duplas; a substituição **executou** em cada um.

  Não são inferências sobre semântica de shell: são as duas metades do elo — comando multilinha chega ao hook como string única, e o shell executa o que o hook não olhou.

  **Causa, em três pontos independentes do código:**
  - `_SEGMENT_SEPARATORS` (`:56`) contém `| || && ; & ( )` e não contém quebra de linha nem retorno de carro; o `shlex` os consome como espaço em branco (`:294-301`), então a segunda linha é absorvida como **posicional** do primeiro comando permitido. `ls`, `cat`, `grep`, `echo`, `printf` e `git status` não declaram `max_positional`, então o prefixo é trivial;
  - `_blank_quoted` (`:245-265`) apaga o conteúdo entre aspas **antes** de a guarda de crase (`:507`) procurar por ela — logo essa guarda é estruturalmente incapaz de ver crase dentro de aspas duplas, que o bash expande;
  - o `shlex` em modo posix devolve a substituição entre aspas como **uma palavra só**, então `(` e `)` nunca viram token e `_segments` (`:318-328`) não a alcança.

  **Por que é blocker:** o auditor não recebe `Write`/`Edit`; a allowlist de Bash é o **único** controle. Com estas vias, `rm`, `tee`, `curl -o`, `sed -i` e `python -c` estão disponíveis, e os alvos aceitam travessia `../../`, alcançando o worktree principal — `CLAUDE.md`, `tools/`, `docs/spec/`. Os dois princípios anunciados em `:14-27` ("os dois default-deny") valem dentro do primeiro segmento da primeira linha e não além dele.

  **Terceira instância consecutiva da mesma classe** (falso liberamento de escrita): 7ª rodada — `>&`, `sort -o`, `find -fprint0`; 8ª rodada — as quatro acima. O que se repete não é o buraco, é o método de decidir por enumeração do que se lembrou.

### HIGH

- **[H1] Os 36 probes de escrita deliberada compartilham uma forma única, e é a forma que ninguém erra**

  **Arquivo:** `scripts/phase0_negative_tests.py:144-210` (`ESCRITA_DELIBERADA`), `:411-414`

  **Requisito violado:** `docs/process/PHASE_0_CHECKLIST.md:49-61` §Teste negativo obrigatório ("um verificador que nunca falhou contra uma violação plantada não é um verificador"); DoD item 2

  **Evidência:** `python scripts/phase0_negative_tests.py` → `rc=0`, 50 probes verdes, com as quatro vias do B1 abertas. Varredura do corpus inteiro: **nenhum** dos 36 probes contém quebra de linha ou retorno de carro; **nenhum** contém substituição de comando entre aspas — o único probe de substituição (`:155`) é sem aspas, que é justamente a forma bloqueada. Todos os 36 escrevem a carga proibida em posição de comando, em uma linha, sem aspas em volta.

  O comentário `:144` afirma que os probes provam que "a protecao nao pode ter sido afrouxada pela tokenizacao", e `:160-165` já registra a lição do H2 da sétima rodada — *"probe que so cobre a forma que voce lembrou nao prova ausencia das formas que voce esqueceu"*. A rodada repete o padrão que o próprio comentário descreve.

  **Por que é HIGH:** o harness é a prova central da fase e roda no CI (`.github/workflows/invariants.yml:43-44`). Verde vazio no mesmo eixo pela terceira vez significa que o gate não pega a regressão que existe para pegar.

- **[H2] O registro declara os itens 9–13 como não executados; o repositório e o operador dizem que foram**

  **Arquivo:** `docs/progress/fase_0.md:299-303` (itens 9–13 ⛔), `:318` ("sem push, sem tag"), `:336` ("`finalize_phase0.sh` **não** foi executado"), `:310`, `:1005-1016` §8

  **Requisito violado:** `docs/spec/07_IMPLEMENTATION_PHASES.md:229` (registro com itens de DoD e status)

  **Evidência medida:** `refs/tags/spec-v1.0` existe, anotada, `tagger Flavio Costa`, assunto "Specification freeze v1.0", em `b4abd9e` — ancestral do HEAD auditado. `origin/main = main = edd9527` (houve push). Existem `origin/dod10-descartavel` (`9c72a46`), `origin/dod11-descartavel` (`77eb901`) e `origin/spec-change/ator-real-e-finalidade-comercial`.

  **Atestação do operador, registrada como tal:** `finalize_phase0.sh` rodou; os dois PRs descartáveis foram criados e o `spec_freeze` reprovou cada um pela mensagem esperada; a branch protection foi aplicada. Isso elimina a hipótese de tag criada fora de ordem e confirma que o defeito é o registro estar desatualizado.

  **Por que é HIGH:** `spec-v1.0` é o marco a partir do qual a spec fica imutável (`WORKFLOW.md:25`). Quem ler o registro conclui que a spec ainda não está congelada e que a fase parou antes do fechamento. É a categoria nomeada no M1 da segunda auditoria, agora sobre os itens de fechamento.

  **Observação estrutural que sobrevive a este commit:** nenhum auditor consegue verificar os itens 9–13 com as ferramentas que o auditor recebe. `gh` não está na allowlist do `readonly_bash.py`, e execução de CI, resultado de job e branch protection não são observáveis de um worktree. Esses itens só podem ser fechados por atestação de operador — que é declaração, não medição. Pelo modelo das quatro verdades do próprio projeto, isso precisa aparecer no registro **como atestação**, não como item verificado.

### MEDIUM

- **[M1] Entregável `.claude/hooks/log_audit.py` removido, checklist da fase não atualizado.** `docs/process/PHASE_0_CHECKLIST.md:10` continua exigindo o arquivo na lista de entregáveis; `Glob` confirma que `.claude/hooks/` tem apenas `check_architecture.py`, `scenario_bash.py`, `scenario_scope.py`. A remoção está bem argumentada em `fase_0.md:507-521`; o documento que define os entregáveis — que não é `docs/spec/` e era editável no mesmo PR — não acompanhou.

- **[M2] `detect_verdict` grava veredito em registro versionado e não tem probe algum.** `scripts/audit_report.py:179-222`; afirmação de cobertura em `fase_0.md:962` ("Sete casos cobertos"). `Grep` por `detect_verdict` retorna apenas definição, uso (`:357`) e prosa. **Dado novo desta rodada:** o `FAIL` gravado para a sétima rodada saiu do `detect_verdict` **antigo**, que devolvia `FAIL` sempre que as duas palavras apareciam — estava certo por acidente. O ramo que motivou a correção (relatório PASS citando "FAIL") segue sem probe e sem ocorrência real; sua estreia será em produção, sobre o log versionado. Requisito: `PHASE_0_CHECKLIST.md:49-61`; `07_IMPLEMENTATION_PHASES.md:229`.

- **[M3] Deny de secrets ainda não cobre `Write` — quinta rodada consecutiva.** `.claude/settings.json:6-13` contém exatamente `Read`/`Edit` para `.env`, `.env.*` e `secrets/**`; nenhuma entrada `Write(...)`, embora `:18` prove que são matchers distintos. Requisito: `PHASE_0_CHECKLIST.md:114` §DoD item 7; `CLAUDE.md` §Secrets. (P13/P19, reconfirmado nas 3ª, 4ª, 6ª e 7ª rodadas.)

- **[M4] O registro guarda evidência falsa para o item 5 da DoD.** `docs/progress/fase_0.md:295` afirma `exit=0` para `range-cli scenario validate`. **Medido:** `exit=2`; só `range-cli scenario validate scenarios/academus/pack` → `exit=0`. O L2 da sétima rodada apontou isto e `fase_0.md:968` o deixou "a apurar". O item 5 continua satisfeito nas duas direções; a evidência registrada é que está errada.

- **[M5] Seções de status do registro descrevem estado anterior a três rodadas.** `fase_0.md:312` (item 15: "quatro auditorias", ✅, quando a sétima foi FAIL com BLOCKER), `:294` (item 4 como ⚠️ parcial pelos "cinco modos" do P23, que está fechado em `:804`, enquanto a sétima o reclassificou ⛔), `:320-335` (tabela de commits termina em `6ed9993`), `:1009` (§8 manda executar a sétima auditoria, já executada). §0 registra da primeira à sexta rodada e não tem entrada para a sétima. Requisito: `07_IMPLEMENTATION_PHASES.md:229`.

### LOW

- **[L1]** Três probes bloqueiam pelo motivo errado — `phase0_negative_tests.py:158,172,178,201`. Medido: `find . -fprint0 out.txt` e `find . -delete` → *"'find' fora da allowlist"*; `uniq entrada.txt ../../CLAUDE.md` → *"'uniq' fora da allowlist"*. Os rótulos atribuem a detecção à allowlist de flags e ao limite de posicionais, mecanismos que estes probes não tocam (ambos têm cobertura real em `sort -o`, `pytest --junitxml`, `git tag v9.9.9`, `env`).
- **[L2]** Comentário e docstring citam arquivo removido neste mesmo commit: `scripts/audit_report.py:8` descreve `.claude/hooks/log_audit.py` como chamador que "importa as funcoes"; `phase0_negative_tests.py:180-182` explica o probe pela `persist()` daquele arquivo.
- **[L3]** Colisão de identificador no registro: `fase_0.md:903` ("P28 a P31") e `:926` ("P28 — [B1 BLOCKER…]") usam o mesmo ID para pendências distintas.
- **[L4]** `main()` do hook falha aberto com stdin não-JSON — `readonly_bash.py:497-501` (`except Exception: return 0`). A docstring `:42` escopa a promessa à tokenização; `fase_0.md:970` já registra a decisão de corrigir junto com o O1 do `check_architecture.py`.
- **[L5]** Falsos bloqueios remanescentes na metade "libera leitura" do item 4. Medidos: `echo "--- verificadores ---"` → `exit=2` (argumento entre aspas iniciado por `-` lido como flag — modo novo); `python tools/$t.py` em laço → `exit=2` (L4 da sétima rodada). Falham para o lado seguro. Os modos antigos fecharam: o smoke test canônico de `PHASE_0_CHECKLIST.md:71-74` roda e devolve `exit=2`, e `2>/dev/null` sobre a prova central passa.
- **[L6]** Nenhum dos seis verificadores varre os hooks — `tools/check_security_constraints.py:36`, `SCANNED_DIRS = ("range-core", "domains", "tools", "scripts")`. `.claude/hooks/` e `user-scope/hooks/` seguem fora (P9/P24, 5ª rodada). É onde nasceu o B1 desta rodada.
- **[L7]** A saída da auditoria N bloqueia o lançamento da auditoria N+1: `start_checkpoint_audit.sh:39-42` exige árvore limpa e `audit_report.py:225-248` grava `audit_<stamp>.md` e anexa ao `audit_log.jsonl` versionados no worktree principal; a mensagem impressa ("Crie o commit candidato antes da auditoria") não descreve a causa. L6 da sétima rodada, sem registro nem correção.

## DoD — item a item

Referência: `docs/process/PHASE_0_CHECKLIST.md` §Definition of Done.

| Item | Status | Evidência |
|---|---|---|
| 1 — Os seis verificadores liberam árvore limpa | ✅ | Executados um a um → todos `rc=0`; `git status --porcelain` vazio antes e depois, sem resíduo |
| 2 — Os seis detectam as violações do harness | ✅ | `phase0_negative_tests.py` → `rc=0`; 13 probes de verificador, cada um `rc=1` **e** citando o arquivo plantado (`expect_fail`, `:66-99`) |
| 3 — Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | ✅ (dois terços medidos) | `range-core/x.py` com `from domains.academus import Y` → `exit=2`; `docs/spec/00_MASTER_SPEC.md` → `exit=2`. Literal de flag não reexecutado nesta rodada |
| 4 — Hook do auditor bloqueia escrita e libera leitura | ⛔ **REPROVADO** | **B1**: quatro vias liberadas, duas com execução comprovada ao vivo. Metade "libera leitura" melhorou; persistem falsos bloqueios (**L5**) |
| 5 — Hook do `scenario-designer` | ✅ | `Write` em `range-core/nope.py` → `exit=2`; `git log --oneline` → `exit=2`; `range-cli scenario validate scenarios/academus/pack` → `exit=0`. Evidência registrada no `fase_0.md:295` está errada (**M4**) |
| 6 — `ground_truth.yaml` e `GM_NOTES.md` fora do `.gitignore` | ✅ | `.gitignore:39-40` — só em comentário que documenta o versionamento deliberado |
| 7 — `.env`/secrets negados | ⚠️ parcial | `Read`/`Edit` negados; `Write` não (**M3**) |
| 8 — Auto Mode desabilitado | ✅ | `.claude/settings.json:4-5` |
| 9 — Push com `arquitetura` e `seguranca` verdes | ✅ por atestação | `origin/main = main = edd9527` prova o push; execução do CI não observável deste worktree. Operador atesta CI verde (**H2**) |
| 10 — PR descartável: `spec_freeze` com spec+código | ✅ por atestação | `origin/dod10-descartavel` (`9c72a46`) existe; reprovação atestada pelo operador, não medida por mim |
| 11 — PR descartável: título `spec-change:` | ✅ por atestação | `origin/dod11-descartavel` (`77eb901`) existe; idem |
| 12 — Branch protection com os três contexts | ✅ por atestação | Não observável deste worktree |
| 13 — `spec-v1.0` depois de CI + branch protection | ✅ por atestação | Tag anotada existe em `b4abd9e`, ancestral do HEAD; a ordem é atestada, não medida |
| 14 — `/doctor` sem apontamentos | ❓ | Fora do meu alcance |
| 15 (`WORKFLOW.md:40`) — auditoria com veredito PASS | ⛔ | Esta rodada: **FAIL**, 1 BLOCKER |

Os itens 9 a 13 estão marcados como satisfeitos **por atestação do operador**. Nenhum deles foi medido por mim, e nenhum é mensurável com a allowlist atual do auditor — ver observação estrutural em H2. O registro precisa dizer isso com essas palavras.

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`ESCRITA_DELIBERADA` (`phase0_negative_tests.py:144-210`)** — declara cobrir o afrouxamento da proteção contra escrita; nenhum dos 36 probes tem quebra de linha, retorno de carro ou substituição entre aspas. Passa verde com quatro vias abertas (**H1**, **B1**).
2. **Probes de `find` e `uniq` (`:158,172,178,201`)** — bloqueiam por "comando fora da allowlist", não pelo mecanismo de flags/posicionais que os rótulos afirmam exercitar (**L1**, medido).
3. **`hook_copies_in_sync` (`:248-266`)** — inerte por construção no CI, onde a cópia instalada nunca existe (`:250-255`). Declarado; não deve ser lido como prova de ausência de drift. Nesta execução local rodou e as cópias eram idênticas.
4. **`detect_verdict` (`audit_report.py:179-222`)** — zero probes, apesar da afirmação de "sete casos cobertos" (**M2**).
5. **`python .claude/hooks/log_audit.py` (`:182`)** — continua provando que hook fora da lista de guardas bloqueia, mas o alvo não existe mais; já não mede o canal de escrita descrito pelo H1 da sétima rodada.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 9 a 13 da DoD.** Vi refs — `origin/main`, as duas branches descartáveis, a tag anotada — e nada além. Execução de CI, resultado dos jobs, mensagem de reprovação do `spec_freeze` e branch protection não são observáveis de um worktree, e `gh` não está na allowlist do auditor. Registrei como atestação do operador. Não afirmo que a ordem de fechamento foi respeitada; afirmo que o operador declara que foi.
- **Execução real das escritas do B1.** Provei que o hook as libera (`rc=0`) e que quebra de linha e substituição entre aspas executam de fato (duas observações ao vivo). Não executei `rm`, `tee`, `curl -o` nem `sed -i`.
- **DoD 14 (`/doctor`)** — fora do meu alcance.
- **Comportamento em Linux/CI.** Rodei em Git Bash no Windows. A corrida de `rmdir` do P1 não se manifestou (árvore limpa ao final), o que não a desmente.
- **P3 e P4** (cobertura TypeScript de `check_core_boundary.py`; formatos sem gramática em `check_synthetic_data.py`) — não verificáveis enquanto `range-core/web/` e as evidências não existirem.
- **Mérito de M4/L7 da sétima rodada** (`check_event_envelope.py` só varre `.py`; actions em tag major mutável) — reconfirmo por leitura, sem probe possível nesta fase.
- **Se `detect_verdict` classifica este relatório corretamente.** Ele tem uma única linha de veredito; pela leitura de `:198-222` deveria gravar `FAIL`. Não executei a função.
- **Estado da captura no momento em que escrevo.** O único `audit_*.md` em `docs/progress/` é o da sétima rodada; `.last_audit_session` contém a sessão desta rodada e o SHA auditado, gravados pelo `--begin`. A captura desta rodada roda depois desta mensagem, pela `trap` do launcher ou por `--recover`.

---

**Consequência processual:** por `WORKFLOW.md:40`, qualquer BLOCKER é FAIL. B1 e H1 fecham juntos — corrigir o hook sem probe para as formas novas devolve o mesmo verde vazio. H2 fecha primeiro e sozinho, porque enquanto o registro disser que não há tag, qualquer leitor — inclusive o próximo auditor — trata `docs/spec/` como não congelado.

Sobre o método: a lista de probes já falhou três vezes como evidência de completude do guarda. Na próxima rodada não vou avaliar a correção pela lista de probes que ela trouxer; vou gerar construções e comparar a decisão do hook com o comportamento real do shell. Uma correção que passe nos probes e discorde do shell em qualquer construção gerada volta como a mesma classe.
