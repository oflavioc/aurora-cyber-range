# Auditoria de checkpoint - 2026-08-15T02:32:20.813022+00:00

- fase: **1**
- commit auditado: `c704afb83186469f6fd666417d7537dc64289a55`
- session_id: `b8c12a4f-54b9-4230-86ff-1f2f446f9007`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 1

Commit auditado: `c704afb83186469f6fd666417d7537dc64289a55` (worktree fixado). Diff contra `main`: 66 arquivos, `docs/spec/` intocado (spec-changes já em `main`).

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- **[B1] A camada nova fica verde por esconder, com motivo falso, o único bloco normativo que o contrato que a governa recusaria**

  Arquivo: `scripts/check_spec_examples.py:105-107` (entrada de `IGNORADOS`); bloco escondido em `docs/spec/09_EVENT_MODEL.md:221-228`; contrato `contracts/objectives.schema.yaml:3,21,41,117-121,126`

  Requisito violado: `09_EVENT_MODEL.md` §6 (bloco normativo de `objectives.yaml`), `03_EXERCISE_DESIGN.md` §1.1 e §1.2, `CLAUDE.md` §"Autoridade" (conflito entre dois documentos não-master → parar e perguntar), e a doutrina declarada do próprio script (`check_spec_examples.py:35-38`: *"BLOCO NAO REIVINDICADO REPROVA … Blocos que legitimamente nao sao instancia de contrato ficam em IGNORADOS, cada um com motivo escrito"*).

  Evidência — observada, não suposta:
  1. A chave `("09_EVENT_MODEL.md", "6. Instrumentação", 1)` declara o motivo *"segundo bloco da mesma secao: continuacao do exemplo de hooks"*. **O bloco de índice 1 não é continuação de hooks.** `09_EVENT_MODEL.md:219` diz literalmente *"E `scenarios/<pack>/objectives.yaml` faz o binding:"* e o bloco `221-228` é uma instância de `objectives.yaml` (`OBJ-03: {competency, rubric, evidence:{auto, observed}}`). O bloco de hooks é o de índice 0, linhas 206-217, e tem entrada própria e correta em `IGNORADOS`.
  2. Existe contrato que o governa, e ele **se declara autoridade justamente dessa seção**: `contracts/objectives.schema.yaml:3` — *"Autoridade: 03_EXERCISE_DESIGN.md secao 1, **09_EVENT_MODEL.md secao 6**"*. Em `x-aurora-spec-examples` (`:194-198`) ele reivindica só `03 §1.1`.
  3. Se reivindicado, o bloco é **recusado** pelo contrato em três sítios: falta o invólucro `objectives` (`:21 required: [objectives]`); falta `title` (`:41 required: [title, competency, rubric, evidence]`); e `observed: [articulated_competing_hypotheses]` é sequência de strings, enquanto `:117-121` exige `$defs/observed_marker` com `:126 required: [id, prompt_to_evaluator]`.
  4. A saída do script é `Blocos yaml/json em docs/spec/: 20 / reivindicados e validados: 12 / ignorados com motivo declarado: 8` e a linha final *"Todo exemplo normativo da spec e aceito pelo contrato que o governa"* — afirmação que só se sustenta porque este bloco saiu da conta.
  5. Conferi os outros 7 ignorados um a um contra o texto: os motivos de `01 §3` (fragmento de envelope), `02 §6.2` e `03 §5.1` (submissão de assessment, sem contrato), `03 §2.2` (rubrica BARS, artefato de `range-core/rubrics/`), `03 §4` (`information_distribution.yaml`, P1-20), `05 §3` (fragmento de uma linha) e `09 §6` índice 0 (hooks, guardado por `check_contract_literals.py`, com probe no harness) **são verdadeiros**. Só o oitavo é falso — e é exatamente o único cujo contrato existe.

  Por que é blocker: (a) é a classe "teste que passa sem provar o requisito" na sua forma mais direta — o verde da camada criada para fechar o buraco de fidelidade da terceira auditoria vem da exclusão do caso inconveniente, com justificativa não verdadeira; (b) sob o texto, `09 §6` e `03 §1.1/§1.2` divergem sobre a forma de `observed` e sobre `title` — dois documentos não-master, caso que `CLAUDE.md` manda escalar e que este commit escalou em outras duas ocasiões (P1-14, P1-15) e aqui não; (c) o custo cai na Fase 6/7: um `objectives.yaml` escrito conforme o exemplo normativo do `09 §6` é recusado pelo loader, que é precisamente a falha que `09` §4 chama de *"a mais cara possível"* e a mesma razão que motivou o spec-change do `separate_incident_declared`.

### HIGH

- **[H1] O único gate que prova o item 1 da DoD continua sem bloquear merge**

  Arquivo: `docs/progress/fase_1.md:205` (P1-18 ⚠️ aberta) e `:585-621`; job em `.github/workflows/invariants.yml:160-202`

  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §"Template obrigatório" (*DEFINITION OF DONE — checklist binária*) e `docs/process/WORKFLOW.md` (quatro required status checks)

  Evidência: o próprio commit registra que `contratos` foi removido dos required status checks por impasse circular e que *"um PR com fixture quebrada pode ser mergeado com ele vermelho"*. `contratos` é o job que executa `check_contract_examples.py`, `check_contract_examples_probes.py`, `check_spec_examples.py` e `check_spec_examples_probes.py` — toda a prova executável dos itens 1 e da camada nova. Repetição literal do H2 da terceira auditoria, sem mudança de estado neste commit.

  Por que é high e não blocker: o diagnóstico está correto e a ordem de reposição é uma restrição real do GitHub (check inédito não é exigível), a prova roda e eu a executei; mas a propriedade "gate" não existe hoje. Não consigo confirmar o estado real da branch protection daqui.

### MEDIUM

- **[M1] Árvore de diretórios incompleta contra `01_ARCHITECTURE.md` §2, e o registro de fase afirma o contrário**

  Arquivo: registro em `docs/progress/fase_1.md:77-94`; árvore real conferida por `git ls-files` (113 arquivos) e listagem

  Requisito violado: `07_IMPLEMENTATION_PHASES.md` Fase 1 OUTPUTS (*"árvore de diretórios"*) contra o layout de `01_ARCHITECTURE.md` §2; e §229 do mesmo doc (registro de progresso fiel)

  Evidência: não existem `range-core/web/gm-console/`, `range-core/web/participant-view/`, `range-core/web/wallboard-shell/`, `domains/academus/web/`, nem `scenarios/` (nenhum arquivo versionado sob esses caminhos). O `fase_1.md:78-87` descreve `web/{gm-console,participant-view,wallboard-shell}`, `domains/academus/… web/ …` e `scenarios/academus/ vazio nesta fase` como parte da estrutura entregue. A técnica de tornar diretório vazio versionável está em uso no mesmo commit (`alembic/versions/.gitkeep`), então a ausência não é limitação do Git.

  Consequência concreta: os ramos de varredura web de `check_contract_literals.py:195` e `check_event_envelope.py:124` não têm território real nesta fase — o que é aceitável — mas o registro afirma entrega que não existe, e é o registro que a Fase 2 herda como ENTRY.

- **[M2] `check_spec_examples.py` só enxerga cercas rotuladas `yaml` ou `json`; qualquer outra grafia sai da regra "bloco não reivindicado reprova", em silêncio**

  Arquivo: `scripts/check_spec_examples.py:122` (`^```(\w*)`) e `:128` (`if lang in ("yaml", "json")`)

  Requisito violado: a garantia que o próprio script declara (`:35-38`) e que sustenta a correção do P1-19

  Evidência: enumerei as 74 cercas de `docs/spec/`. Hoje nenhuma cerca sem rótulo carrega instância de contrato (são árvores de diretório, diagramas ASCII, CLI e o banner de `05` §4), então não há bloco perdido **agora**. Mas um bloco normativo futuro escrito com ```` ```yml ```` ou sem rótulo não vira falha de "sem dono": ele simplesmente não existe para o verificador. É a mesma classe do defeito que o comentário de `:127-130` diz ter fechado para o segundo bloco da seção.

### LOW

- **[L1]** `docs/progress/fase_1.md:202`, `:441` e `:579-583` mantêm P1-15 como *"escalação aberta"* e afirmam *"O contrato o declara opcional, seguindo `09`"*. As duas afirmações estão vencidas: `09_EVENT_MODEL.md:40` já lista `exercise_timestamp` entre os obrigatórios e `contracts/events.schema.yaml:46-55` o exige. O mesmo documento registra o fechamento em P1-17 (`:204`, `:467`). Registro contradizendo o artefato — `07` §229.
- **[L2]** `pyproject.toml:68` declara `requires = ["setuptools>=68"]` — faixa, não versão exata — no mesmo arquivo que argumenta (`:25-37`) por que faixa não é pinagem. `constraints.txt` não cobre o backend de build. `06_ACCEPTANCE_TESTS.md` T15 (*"nenhuma dependência não pinada"*) e `00_MASTER_SPEC.md` §8.
- **[L3]** Assimetria em `scripts/check_spec_examples.py`: reivindicação apontando para âncora inexistente falha alto (`:234-239`), mas entrada de `IGNORADOS` apontando para âncora inexistente não é reportada. Entrada morta persiste sem aviso — e a lista de ignorados é justamente onde a auditoria mostrou (B1) que defeito se esconde.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis contratos existem e validam exemplos positivos e negativos | ⚠️ atende à letra | `check_contract_examples.py` rc=0 — 6 contratos, 9 positivos, 61 negativos recusados pela camada declarada, 2 instâncias reais; `check_contract_examples_probes.py` rc=0 em 12 eixos. Mas a camada de fidelidade que deveria completar isso está comprometida por B1, e o gate não bloqueia merge (H1) |
| 2. Constantes tipadas em Python e TypeScript para flags e event types | ✅ | `contracts/generated/events.{py,ts}` (32 tipos, `Final[str]` / `Final[tuple[str,...]]` / `as const`), `domains/{academus,prontus}/generated/flags.{py,ts}` (12+2); `codegen.py --check` rc=0 |
| 3. Teste de fronteira core/adapter passa (por AST) | ✅ | `check_core_boundary.py` rc=0; rc=1 contra `range-core/_phase0_probe_bad.py` no harness. AST cobre alias, relativo, `importlib` e `__import__` (`:6-8,46-60`) |
| 4. `event_type` fora do catálogo é rejeitado | ✅ (no que existe nesta fase) | Catálogo fechado em `$defs/event_type_*`, 32 tipos batendo linha a linha com `09` §4.1, mais `effect_class` com cobertura exata verificada; `check_contract_literals.py` rc=1 contra literal e contra hook fora do catálogo. Rejeição *na emissão* (T2) só é exercitável na Fase 2 |
| 5. Os seis verificadores liberam árvore limpa e cada um falha contra violação plantada | ✅ | Os seis rc=0; `phase0_negative_tests.py` rc=0 com 15 probes de verificador + 112 provas do hook. 10 falsos bloqueios de leitura afirmados (P23), 0 escritas passando |
| 6. `codegen.py --check` detecta dessincronia e não escreve | ✅ | `main()` (`:271-275`) só chama `write()` sob `--write`; `check()` (`:194-230`) só lê; três probes (ausente, divergente, eventos) rc=1 |
| 7. `docker compose up` sobe Postgres e Redis | ⚠️ não verificável aqui | Por leitura: imagens pinadas por digest, `ports` amarrados a `127.0.0.1`, `POSTGRES_PASSWORD` sem default. A execução em `fase_1.md:218-234` é auto-relato que não reproduzi |
| ~~8~~. `RANDOM_SEED` lido de `.env` | ➡️ fora do escopo | Confirmado em `07_IMPLEMENTATION_PHASES.md:53-61`: o item não consta mais da Fase 1 e aparece na Fase 2 (`:76`) com o texto endurecido. `docs/spec/` não é tocado por este PR |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`IGNORADOS` é escape hatch sem verificador.** Os motivos são prosa lida por ninguém. B1 é a demonstração empírica: um motivo factualmente falso remove da conta o único bloco normativo que o contrato correspondente recusaria, e a suíte imprime *"Todo exemplo normativo da spec e aceito pelo contrato que o governa"*.
2. **Os 3 eixos de `check_spec_examples_probes.py` pressupõem a honestidade da lista.** Eles plantam contrato que regride, bloco sem dono e âncora envelhecida. **Nenhum eixo planta o caso "bloco inconveniente movido para `IGNORADOS` com motivo plausível"** — que é exatamente como B1 sobrevive. É a repetição, um nível acima, do diagnóstico da terceira auditoria: o probe fecha *"o verificador enxerga?"* e deixa aberto *"o autor olhou?"*.
3. **Alcance declarado, e verdadeiro: só o que a spec mostra em exemplo.** O próprio script (`:16-23`) reconhece que `package_files.required` e `metric_binding` estão fora do alcance. Verifiquei os dois à mão nesta rodada e ambos estão corretos agora — mas por leitura, não por mecanismo.
4. **A camada foi escrita por quem escreveu os contratos que ela julga**, e a escolha de quais blocos entram em `IGNORADOS` é do mesmo autor. A mitigação que executei foi ler os 8 motivos contra o texto da spec, um a um; foi essa leitura, e não a suíte, que produziu B1.
5. **Cercas sem rótulo de linguagem não existem para o verificador** (M2) — hoje inofensivo, amanhã silencioso.
6. Nenhum teste marcado `skip`/`xfail` na árvore (varredura por `xfail|@pytest.mark.skip|unittest.skip`: zero ocorrências). `codegen.py --check` é de fato read-only. Nenhuma violação de `05_SECURITY_REQUIREMENTS.md` encontrada: `check_security_constraints.py` e `check_synthetic_data.py` rc=0, `.env.example` só com placeholders, compose em loopback, SYSLOG em `192.0.2.10` (RFC 5737).

## O QUE EU NÃO CONSEGUI VERIFICAR

- **`docker compose up` (item 7).** Docker não é executável no sandbox de auditoria; verifiquei o compose por leitura.
- **Alembic executável (P1-9).** `python -m alembic.config --help` está fora da allowlist do `readonly_bash.py` — bloqueado, como na rodada anterior. `jsonschema`, `referencing` e `pyyaml` estão instalados (os quatro executores rodaram), o que sugere o `pip install -e .`, mas não confirmei o Alembic.
- **Estado real da branch protection (H1).** Sem rede e sem `gh`; o finding se apoia no que o próprio commit registra.
- **Se a divergência de B1 se resolve mudando `09 §6` ou mudando `03 §1.1`.** Não é decisão de auditor: são dois documentos não-master, e `CLAUDE.md` manda escalar. Registro o conflito, não a resolução.
- **Fidelidade do parser YAML de subconjunto** (`tools/_common.py::parse_yaml`), único leitor dos contratos nos jobs stdlib: não comparei sua saída com a de um parser conforme.
- **Os contratos onde a spec descreve em prosa sem exemplo.** Comparei campo a campo `01 §5.2`, `03 §1.1/§1.3/§3.1`, `04 §1/§2/§3/§5/§6/§7/§9`, `08 §2/§5/§7` e `09 §1/§2/§4.0/§4.1/§6`. Fora disso, minha verificação é mais fraca e não a apresento como conclusiva.
