# Auditoria de checkpoint - 2026-08-18T12:28:56.436034+00:00

- fase: **5**
- commit auditado: `46c034718800f7f86eddd885be0c34a27d8500ae`
- session_id: `94b7070f-162a-4b93-8b55-a41a85ead204`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 5

## VEREDITO: FAIL

Guarda de base: **PORTA** (P3-7, verbatim do lançador). Provas de container P4-10: verificadas, SHA `46c034718800` confere. Spec intocada no intervalo (`git diff --name-only fd34c44..46c0347 -- docs/spec/` vazio). Suíte real: `python -m unittest discover -s tests` → **Ran 394 tests, OK**, sem `skipped=` — os testes de serviço rodaram. Os 46 verificadores executáveis pela allowlist saíram 0, com provas negativas verdes.

O FAIL vem de um único achado, e ele é da mesma família que a auditoria anterior fechou como B1.

---

## FINDINGS

### BLOCKER

- **[B1] O gabarito de 45 dos 67 casos é derivável do repositório público sem o `RANDOM_SEED` — pelo índice do aluno, pela justificativa e pelo aprovador**

  **Arquivo:** `domains/academus/seed/dataset.py:230`, `:442`, `:455`, `:465`, `:477-480`, `:489`, `:506`, `:530`, `:81`

  **Requisito violado:** `CLAUDE.md` §"Ground truth e GM notes" (*"gabarito é **quais casos**, e eles saem do `RANDOM_SEED`"* — a premissa que sustenta a D10 e a decisão de manter o gerador versionado); `05_SECURITY_REQUIREMENTS.md` §6 (gabarito fora de qualquer superfície de participante); `02_DOMAIN_ACADEMUS.md` §6.2 (a avaliação é por calibração sobre casos que exigem análise).

  **Evidência — observada no fonte, e derivável mecanicamente:**

  1. `dataset.py:225-232` constrói `alunos` em laço posicional puro: `alunos.append((f"A-{i:06d}", ...))` para `i in range(escala.alunos)`. Nenhum sorteio toca **identidade ou ordem** da lista — só `name` e `situacao`. `aleatorio.shuffle` (`:572`) embaralha `registros`, **nunca `alunos`**. Logo `alunos[k][0] == f"A-{k:06d}"` para todo seed.
  2. Os quatro conjuntos não-indevidos indexam essa lista por deslocamento fixo, escrito à mão:
     - `:465` ambíguos → `alunos[(100 + i) % len(alunos)]` → `A-000100..A-000110`
     - `:489` suspeitos → `alunos[(200 + i) % len(alunos)]` → `A-000200..A-000233`
     - `:506` ruído → `alunos[(300 + i) % len(alunos)]` → `A-000300..A-000359`
     - `:530` delegadas → `alunos[(400 + i) % len(alunos)]` → `A-000400..A-000417`
     Só os indevidos usam grupo sorteado (`:377`, `:442`, `grupo_alvo`).
     Consequência aritmética em `ESCALA_COMPLETA`: restrito às linhas `within_window = false` — que é a população investigada —, **o `student_id` sozinho particiona o gabarito**, e o complemento das três faixas conhecidas são exatamente os 22 indevidos comprovados. Nenhum seed é necessário.
  3. Corroboração independente, pela coluna `justification`: `:455` dá aos 11 ambíguos a constante literal `"Ajuste solicitado."` e `:478-479` dá aos 34 suspeitos outra constante literal única. `GROUP BY justification` devolve os dois conjuntos, e as duas strings estão no fonte versionado.
  4. Terceira corroboração: `:477` grava `approver_user_id = COORDENACAO` para os 34 suspeitos e `:455` grava `approver_user_id = conta_alvo` para os 11 ambíguos, com `COORDENACAO = "U-COORD-0"` literal em `:81`.
  5. O comentário de `:365-377` afirma o contrário do que o código faz sessenta linhas abaixo: *"AS IDENTIDADES SÃO SORTEADAS, E NÃO POSICIONAIS — e esta linha é a que sustenta a D10 inteira"*. Sorteadas são `conta_alvo` e `grupo_alvo`; os alvos dos outros quatro conjuntos permanecem posicionais.
  6. Nenhum mecanismo vê isso, e por construção: `scripts/check_gabarito_fora_do_git.py:134-135` declara *"`A-`, `U-P-` são formato de ENTIDADE: `f"A-{i:06d}"` (...) não aponta caso nenhum. Só a forma CONCRETA deles é gabarito"*. Aqui a forma concreta não é escrita — é **computada por índice**, que é a mesma evasão que o H1 desta fase já corrigiu para f-string com infixo.

  **Por que é blocker:** o repositório é público e o modelo de ameaça está escrito em `CLAUDE.md` — *"quem os lê antes do exercício tem o gabarito"*. Quem ler `dataset.py` classifica, sem análise nenhuma, os 34 legítimos de aparência suspeita e os 11 ambíguos, e obtém os 22 indevidos por complemento. `02` §6.2 chama overconfidence sobre os 34 de "o erro mais interessante deste cenário": ele deixa de ser mensurável. É a mesma propriedade que fez o B1 da rodada anterior (`object_id` prefixado) ser blocker, por um vetor que a correção daquele não alcançou — a coluna que denuncia agora é `student_id`, e a leitura exige o repositório em vez de só a coluna.

### HIGH

- **[H1] `test_os_fatos_sao_DISTINTOS_entre_os_dois_seeds` não prova o que o nome afirma**

  **Arquivo:** `tests/test_gabarito.py:226-243`

  **Requisito violado:** `06_ACCEPTANCE_TESTS.md` T8 (determinismo com a direção inversa) e a propriedade que a §D10.3 do registro declara provada — *"os `case_id` distintos (...) separa propriedade do gerador de coincidência de um seed"*.

  **Evidência:** a asserção é `assertNotEqual((atores_1, alvos_1), (atores_2, alvos_2))` — desigualdade de uma 2-tupla de conjuntos **unidos sobre todos os fatos**. Um único elemento divergente a satisfaz, e ele existe (os 22 indevidos, cujo ator e grupo são sorteados). O teste passa verde enquanto 45 dos 67 mapeamentos caso→aluno são idênticos entre os dois seeds — que é exatamente o B1. O mesmo vale para `test_o_MAPEAMENTO_caso_para_fato_muda_com_o_seed` (`:287-312`), que compara o dicionário inteiro e é satisfeito pelo embaralhamento.

  **Por que não é blocker separado:** é o instrumento que deixou o B1 passar, não um defeito adicional de produto. Mas é o teste que precisa mudar junto, sob pena de a correção do B1 nascer sem quem a segure.

### MEDIUM

- **[M1] A aplicação não conecta como `academus_app`: `REVOKE` não alcança o caminho da API**

  **Arquivo:** `domains/academus/audit/trilha.py:219` (`SET LOCAL ROLE`), `tests/test_trilha_de_auditoria.py:147-168`

  **Requisito violado:** `05_SECURITY_REQUIREMENTS.md` §7 e `02_DOMAIN_ACADEMUS.md` §4 item 2.

  **Evidência:** `OQueT7NaoProva::test_a_role_QUE_CONECTA_ainda_possui_UPDATE_na_trilha` passa — a role de conexão é dona da tabela e mantém `UPDATE`; `test_a_role_que_conecta_pode_voltar_de_SET_ROLE` mostra `RESET ROLE` a um comando de distância. O critério literal de T7 ("a role da aplicação") é satisfeito no catálogo para `academus_app`, e o trigger recusa a todos.

  **Consequência:** num deploy real, o processo da `academus-api` pode alterar a trilha; o que resta é detecção pela cadeia. Está declarado, exercido por teste que fica vermelho quando fechar, e endereçado como P5-3 ao operador — por isso MEDIUM e não HIGH.

- **[M2] Itens 1 e 2 da DoD em escala completa repousam em número transcrito para prosa, sem vínculo com o commit**

  **Arquivo:** `docs/progress/fase_5.md:1573-1574`, `scripts/prova_seed_completo.py`, `user-scope/hooks/readonly_bash.py` (exclusão declarada)

  **Requisito violado:** `07_IMPLEMENTATION_PHASES.md` Fase 5, itens 1 e 2 da DoD.

  **Evidência:** o script gera máquina, data e stack por código (`prova_seed_completo.py:102-107`), o que atende à forma de `06` T3. Mas o resultado — 144,3 s, 20 tabelas idênticas — chega ao auditor como texto em markdown. O registro traz **dois números diferentes** para a mesma medição (`:1028` 159,4 s e `:1211` 144,3 s), o que é normal em duas execuções e é justamente o que um artefato assinado tornaria não-ambíguo. A fase construiu o mecanismo certo para esta classe — `check_provas_de_container.py`, que amarra a saída ao SHA e reprova por ausência — e não o aplicou aqui. A exclusão da allowlist está argumentada; o que falta é a gravação.

### LOW

- **[L1] `test_as_dezenove_tabelas_existem_no_banco` afere contra lista escrita no próprio teste, não contra `02` §1**

  **Arquivo:** `tests/test_modelo_completo.py:57-59`, `:107-131`

  **Requisito violado:** `07` Fase 5, OUTPUTS ("modelo completo"), medido contra `02_DOMAIN_ACADEMUS.md` §1.

  **Evidência:** a lista de dezenove tabelas é literal no módulo de teste e cruzada com banco, metadata e `TRUNCATE`. É mais forte que comparar o modelo consigo mesmo, mas nenhuma leitura da spec entra — é a mesma forma que o M2 da rodada anterior recusou para os volumes, com o atenuante de que o mapeamento entidade→tabela de `02` §1 não é mecânico. As duas exclusões (`Incidente`, `Declaracao`) estão justificadas contra `01` §4 em `fase_5.md:732-743`.

- **[L2] `dump_canonico` prova identidade só sobre as colunas que o seed escreve**

  **Arquivo:** `domains/academus/seed/carga.py:47-101`, `:191-218`

  **Evidência:** o SHA por tabela é calculado sobre `COLUNAS[tabela]`. Coluna existente no esquema e ausente dessa lista não entra na comparação de "byte-idêntico". Hoje é inócuo — o seed não popula outras colunas —, e o `ORDER BY` explícito e a exclusão das chaves substitutas estão corretos e justificados.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Seed completo em < 5 min | **NÃO VERIFICADO** | `prova_seed_completo.py` fora da allowlist por decisão declarada; número (144,3 s) lido em `fase_5.md:1573`, sem artefato ligado ao SHA. Ver M2 |
| 2 — Mesmo `RANDOM_SEED` → dataset byte-idêntico | **PASSA em escala reduzida** | `Determinismo::test_o_mesmo_seed_produz_o_mesmo_dump` + a direção inversa `test_seeds_DIFERENTES_produzem_dumps_diferentes` + `test_o_gerador_nao_le_relogio` (por AST), todos executados contra Postgres. Escala completa: ver M2 |
| 3 — `UPDATE`/`DELETE` falham por trigger **e** por permissão de role | **PASSA, com limite declarado** | `TrilhaEhAppendOnly` — quatro testes: dois pelo trigger (mensagem "append-only"), `has_table_privilege` negando os três verbos e concedendo `INSERT`/`SELECT`. Migration `0004` com `REVOKE` nominal e trigger incondicional; `check_trilha_de_auditoria_probes.py` reprova em 13 eixos. Ver M1 |
| 4 — `/audit/verify-chain` detecta adulteração induzida | **PASSA** | `CadeiaDetectaAdulteracao` — campo alterado, `payload` alterado, linha removida do meio, cada um com a `sequence` exata; limite do truncamento de cauda afirmado como teste. `RotaDeVerificacao` cobre 200/401/403 e a forma estável do corpo |
| 5 — Os seis conjuntos nos volumes especificados | **PASSA em escala reduzida** | `SeisConjuntosDaLinhaB` — volumes lidos de `02` §6.1 pelo parser de `check_volumes_da_linha_b.py`, mais disjunção e cobertura (a união é a trilha inteira) e as seis características no dado |
| 6 — `GM_NOTES.md` contém a query que separa indevidos de ambíguos | **PASSA quanto à forma; comprometido pelo B1** | `test_o_GM_NOTES_contem_a_query_de_referencia` e a query **executada** devolvendo 22 e nenhum dos 11; linter `conferir` recusando órfão e vacuidade, chamado de dentro de `gerar` (provado por AST). O artefato é produzido e julgado pelo CI, não atestado — mas o gabarito que ele descreve é derivável sem ele (B1) |

Invariantes arquiteturais: os quatro verdes por execução — `check_core_boundary.py`, `check_contract_literals.py`, `check_event_envelope.py`, `codegen.py --check`. Nenhum `append` de evento no adapter (P4-2 mantida com gatilho intacto). `objective_ids` ausente. `contracts.generated.events.CONTAINMENT_DECLARED` usado como constante em `gabarito.py:160`, e não como literal.

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. `tests/test_gabarito.py:226-243` — `test_os_fatos_sao_DISTINTOS_entre_os_dois_seeds`. Desigualdade de tupla de conjuntos unidos: satisfeita por um elemento, afirma uma propriedade sobre todos. É o instrumento que deixou o B1 passar (H1).
2. `tests/test_gabarito.py:287-312` — `test_o_MAPEAMENTO_caso_para_fato_muda_com_o_seed`. Compara o dicionário inteiro; o embaralhamento de `registros` basta para satisfazê-lo, mesmo com todos os alvos por conjunto invariantes.
3. `tests/test_modelo_completo.py:107` — nome promete as dezenove tabelas de `02` §1, afere contra lista literal do próprio teste (L1).

Nenhum `skip`, `xfail` ou `expectedFailure` não-justificado: todos os `skipIf` são condicionais a DSN de serviço e nenhum disparou nesta execução (saída `OK` sem contagem de skip).

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Item 1 da DoD (seed < 5 min) e o item 2 em escala completa.** `prova_seed_completo.py` está fora da allowlist do auditor por decisão registrada, e exige `AURORA_SEED_DATABASE_URL`, que o hook não admite. Não há artefato assinado por SHA como o da P4-10. Verifiquei apenas que máquina, data e stack são gerados por código, não digitados.
- **Execução contra o banco semeado.** O hook bloqueia `python -c`, prefixo de variável de ambiente e qualquer cliente SQL, então não pude consultar a base para exibir os `student_id` concretos de cada conjunto. O B1 está estabelecido por leitura do gerador, e a derivação é mecânica e determinista — laço posicional em `dataset.py:225-232`, sem `shuffle` sobre `alunos`, com deslocamentos literais em `:465`, `:489`, `:506` e `:530`. Recomendo que a correção venha acompanhada de um teste que compare **por conjunto** o conjunto de `dest` entre dois seeds, que é a asserção que hoje não existe.
- **Provas de container (P4-10).** Não as vi rodar; conferi o SHA pelo verificador, como o protocolo prevê.
- **Comportamento em Postgres de produção.** `REVOKE` e trigger foram provados contra o Postgres efêmero da auditoria com a migration aplicada; a role de conexão do deploy real (P5-3) está fora do alcance desta auditoria.
- **`GM_NOTES.md`/`ground_truth.yaml` do exercício real**, gerados com o seed de produção: não existem no repositório, por decisão, e nenhum CI os vê. A propriedade auditável é a do gerador — e é ela que o B1 reprova.
