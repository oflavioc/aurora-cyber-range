# Auditoria de checkpoint - 2026-08-16T19:48:47.041935+00:00

- fase: **3**
- commit auditado: `5b219a7ae8c22639c4282c5061e3b88c93b4775f`
- session_id: `5611d50b-f552-4304-a1ce-ff9e8b91b8a6`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

All checks run. Here is the report.

```text
# AUDITORIA — FASE 3

## VEREDITO: PASS

Commit candidato `5b219a7`, base entregue pelo lançador `a857d76`.
Suíte executada por mim: `python -m unittest discover -s tests` → **217 testes, 0 falhas,
0 pulos** (stack efêmera no ar; nenhum `skipIf` de serviço disparou). Seis verificadores de
`tools/` rc=0; `check_api_surface` (+36 probes), `check_fold_authority` (+4), `check_store_read_surface`
(+5), `check_gate_coverage`, `check_pinned_images`, `check_core_contract_imports`,
`check_progress_consistency`, `scripts/phase0_negative_tests.py` — todos rc=0.

Nenhum BLOCKER. Os quatro itens da DoD de `07_IMPLEMENTATION_PHASES.md` §Fase 3 estão
provados por teste que executei. Dois HIGH abaixo: um é teste vacuoso introduzido pelo
próprio commit candidato, o outro é o mecanismo que este commit alega ter fechado e que
não fechou — e a prova disso é esta sessão.

## FINDINGS

### BLOCKER

Nenhum.

### HIGH

- **[H1] `test_sem_token_nao_paga_a_latencia_declarada` passa com a flag de latência DESLIGADA — a asserção é vacuosa, e o registro a declara como uma das três camadas que fecham o M2**
  Arquivo: `tests/test_api_degradacao.py:349-358` (o teste), `:319-323` (o `setUp` da classe);
  afirmação no registro: `docs/progress/fase_3.md:1083-1085`
  Requisito violado: a exigência do meu próprio escopo — *"testes que passam sem provar o
  requisito"* — sobre a propriedade que `00_MASTER_SPEC.md` §3 (consequência normativa 3,
  assimetria de informação é desenho) e `05_SECURITY_REQUIREMENTS.md` §8 sustentam, na
  leitura que o próprio código declara em `domains/academus/api/degradacao.py:158-165`.
  Evidência (leitura de caminho determinístico, quatro sítios):
  1. `setUp` da classe dispara **apenas** `MATRICULA_FORA`, `NOTAS_CONGELADAS` e
     `TODAS_AS_SESSOES` (`tests/test_api_degradacao.py:321-323`).
  2. O único inject que liga `academus.lms_degraded` é `AVA_LENTO`
     (`tests/test_api_degradacao.py:90-97`), e ele não é disparado nessa classe.
  3. `domains/academus/flags.yaml:42-44` — `academus.lms_degraded`, `type: boolean`,
     `default: false`.
  4. `domains/academus/api/degradacao.py:131-135` — para `condicao: ligada` o gatilho é
     `valor is True`. Com a flag em `false`, a entrada de `latencia` de
     `/turmas/{turma_id}/diario` (`domains/academus/api_surface.yaml:210-213`) **não dispara
     em nenhuma ordem de dependência**.
  Logo `self.c.dormidas` é `[]` com `autoriza` antes **ou** depois de `degrada`, com token
  ou sem token. O teste não discrimina nada.
  Por que é HIGH e não BLOCKER: a propriedade *"`autoriza` roda antes de `degrada`"* está de
  fato provada, por dois outros meios que executei —
  `test_sem_token_as_tres_rotas_degradadas_respondem_401_e_nao_503` (`:337-347`, comportamental,
  três rotas) e `test_a_ordem_declarada_na_aplicacao_e_autoriza_depois_degrada` (`:360-372`,
  estrutural). O que é falso é a afirmação do registro em `fase_3.md:1083-1085`: *"Sem esta
  asserção, um `degrada` que rodasse antes e só dormisse passaria na primeira e entregaria a
  flag do AVA pela cronometragem"* — o canal de temporização continua **sem teste**, e o
  achado M2 foi declarado fechado sobre uma prova que não existe. É a classe "verificação que
  parece existir" que a §7.3 do próprio registro nomeia, reintroduzida dentro da correção dela.
  Corroboração no próprio registro: `fase_3.md:1082` diz que inverter a ordem deixa
  **4** testes vermelhos "com 503, 409 e 503" — três subTests do teste comportamental mais o
  estrutural. O teste de latência **não está entre eles**, e essa é a contagem do autor.

- **[H2] O H1 da auditoria anterior foi fechado só no caso degenerado: com a fase já mergeada em `main` commit a commit, o lançador segue em silêncio — e esta sessão é o caso**
  Arquivo: `scripts/start_checkpoint_audit.sh:119-162` (a guarda nova);
  afirmação: `docs/progress/fase_3.md:1043-1045` e `:1062-1064`
  Requisito violado: `docs/process/WORKFLOW.md` §"Ciclo por fase" (linhas 29-38: commit
  candidato → auditoria → **depois** `gh pr create`) e §"Por que o auditor formal usa launcher
  de worktree", objetivo declarado *"comparação reproduzível contra `main`"* (linha 49);
  `CLAUDE.md` §"Fluxo por fase".
  Evidência observada nesta execução:
  - `git for-each-ref`: `refs/remotes/origin/main` e `refs/heads/main` = `a857d76`, que é o
    commit **"fase-3: o fechamento"**. `git log --graph` mostra `1b17380`, `fce18dc`,
    `4e4e717`, `7011628`, `4dbb205`, `a857d76` — **cinco das seis peças da Fase 3 já estão em
    `main`** no instante desta auditoria.
  - A guarda dispara só em `git merge-base --is-ancestor "$HEAD_SHA" "$BASE_SHA"`
    (`:119`). Como `5b219a7` não é ancestral de `a857d76`, ela **não disparou**: nenhum aviso,
    nenhum `rc=1`.
  - Consequência medida: `git diff a857d76 5b219a7 --stat` = **9 arquivos**. O item 6 da minha
    entrada obrigatória ("o diff do commit candidato contra `main`") não contém `app.py`,
    `auth.py`, `degradacao.py`, `repositorio.py`, `surface.py`, `api_surface.yaml`,
    `tokens.py`, `cache.py` nem nenhum dos quatro itens da DoD — todos já estão na branch
    default.
  Por que é HIGH: a consequência que o próprio commit enuncia em `:110-112` — *"um BLOCKER
  emitido contra um commit que já está em `main` já está na branch default; a auditoria vira
  laudo em vez de gate"* — **vale integralmente agora**, e o mecanismo não a impediu. A
  afirmação de `fase_3.md:1062-1064` (*"o lançador agora torna a inversão impossível de fazer
  em silêncio"*) é falsa para a forma da inversão que de fato ocorreu nas Fases 2 e 3: merge
  progressivo das peças. `BASE_SHA == HEAD_SHA` é o caso degenerado; o caso geral é
  `BASE_SHA` conter parte da fase, e ele continua silencioso.
  Não é BLOCKER pela mesma razão que a rodada anterior deu ao seu H1: não é item de DoD nem
  desvio de `docs/spec/`.

### MEDIUM

- **[M1] Business state da `academus-api` em dicionários de módulo, sem SQLAlchemy e sem Postgres** — repetido da rodada anterior, ainda aberto.
  Arquivo: `domains/academus/models/registros.py:75-92`; escrita em
  `domains/academus/api/repositorio.py` (`NOTAS.append`, `MATRICULAS.append`)
  Requisito violado: `02_DOMAIN_ACADEMUS.md` §7 — *"academus-api — FastAPI + SQLAlchemy"*;
  `01_ARCHITECTURE.md` §4, linha Business State — persistência **Postgres**, *"não reversível
  por rollback; só por reset total"*.
  Evidência: as quatro estruturas são `dataclass` com `ALUNOS`/`TURMAS`/`NOTAS`/`MATRICULAS`
  em memória de processo; nenhum modelo SQLAlchemy nem migration para elas.
  Por que é MEDIUM: o corte está declarado e datado (`registros.py:18-32`) e nenhum item da
  DoD da Fase 3 exige persistência. Registrado como **P3-5** (`fase_3.md:874-900`) com
  vencimento na Fase 4, e o argumento do redator para essa data (a linha "só por reset total"
  passa a ser falsa quando existir container que reinicia) é correto e mais forte que o meu.
  Fica registrado porque a divergência é contra texto normativo não condicionado a fase.

### LOW

- **[L1] O cabeçalho do registro da fase afirma números que este mesmo commit invalidou**
  Arquivo: `docs/progress/fase_3.md:13-14` — *"**211 testes — zero pulos** …, 183 arquivos
  classificados pelo gate"*.
  Evidência: `python -m unittest discover -s tests` → **217 testes**;
  `python scripts/check_gate_coverage.py` → *"**184 arquivos** versionados, todos
  classificados — 12 em SPEC, 112 em CODE, 60 descritivos"*. O commit acrescenta 6 testes e 1
  arquivo e não atualiza o resumo. As ocorrências de "211" em `:818`, `:868` e `:1010` são
  históricas (descrevem a auditoria de `a857d76`) e estão corretas; a de `:13` é apresentada
  como estado corrente.
  Nota: `scripts/check_progress_consistency.py` passa — ele confere tabela-resumo × seções,
  não números medidos.

- **[L2] Dois cabeçalhos de status contraditórios no mesmo registro**
  Arquivo: `docs/progress/fase_3.md:3` (*"**Status: EM CURSO.**"*) e `:6` (*"**Status:
  AUDITADA — PASS**"*), com `:16` dizendo *"fechada em 17/08/2026"*.
  Registrado apenas porque `fase_3.md` §7.1 estabelece que afirmação dentro de um registro é
  citação e não paráfrase; a mesma disciplina alcança o estado que o registro declara de si.

- **[L3] `POST /turmas/{turma_id}/notas` grava nota sem nenhum registro de auditoria** —
  repetido da rodada anterior. `domains/academus/api/app.py:107-118`. `02_DOMAIN_ACADEMUS.md`
  §4.1 lista os campos exigidos. **Não é defeito desta fase**: `07` Fase 3 põe "auditoria com
  hash" nos NON-GOALS e `06` T7 a cobra na Fase 5. Já registrado como **P3-6**.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — `academus.enrollment_offline: true` faz o endpoint de matrícula retornar 503 | **PASS** | `tests/test_api_degradacao.py:177-182` executado: 201 antes, 503 depois de um `inject_fired` real; efeito vindo do fold via `current` (`range-core/state/cache.py:135-157`), sem `set` direto. Status declarado em `api_surface.yaml:168` |
| 2 — `academus.grades_readonly: true` bloqueia POST de nota com mensagem de negócio | **PASS** | `tests/test_api_degradacao.py:184-196` executado: 409 + `assertIn("coordenacao", …)`. Mensagem em `api_surface.yaml:192`. `:198-206` prova que a leitura segue 200, coerente com `effect_ui` em `flags.yaml`. `check_api_surface.py` reprova mensagem que nomeie flag ou mecanismo (probes executados, 36 eixos) |
| 3 — RBAC nega acesso cruzado entre perfis | **PASS** | `tests/test_api_rbac.py:76-96` executado — as duas direções mais um caso positivo que discrimina. É teste de **payload de API** sobre stack ASGI real, na forma de `06` T6. Complementos executados: 401×403 (`:100-112`), indistinguibilidade byte a byte (`:121-136`, `:200-212`), escopo de objeto `proprio`/`titular` (`:170-191`), escrita escopada (`:232-250`), rota não declarada negada em runtime (`:262-280`) |
| 4 — Nenhuma string solta de flag no código-fonte (verificado por lint) | **PASS** | `python tools/check_contract_literals.py` rc=0. Varredura independente minha por `["']academus\.[a-z_]+["']` em `*.py`: **um único arquivo**, `domains/academus/generated/flags.py`. Os nomes em `api_surface.yaml` são dado declarado, conferidos contra `flags.yaml` por `check_api_surface.py` (probe *"rota consumindo flag que o adapter nao declara"* reprova) |
| OUTPUT — degradação real por flag em ≥ 3 endpoints | **PASS** | `/matricula` (recusa 503), `/turmas/{id}/notas` (recusa 409), `/turmas/{id}/diario` (latência 2,5 s + recusa proporcional determinística). Exercitados em `tests/test_api_degradacao.py` |
| OUTPUT — JWT | **PASS** | `tests/test_api_tokens.py` executado: `alg: none` recusado, token sem `exp` recusado, adulteração, outra chave, expiração com instante fixo, placeholder vazio de `.env.example` recusado, e o boot do adapter (`BootDoAdapter`, novo neste commit — fecha o L3 da rodada anterior) |
| OUTPUT — três entidades (Aluno, Turma, Nota) | **PASS com ressalva** | `domains/academus/models/registros.py:45-92`; ver [M1] |
| NON-GOALS respeitados | **PASS** | Nenhum modelo SQLAlchemy, nenhum seed, nenhuma trilha de hash; a API não emite evento |
| `06` T2 (Fases 1–3) | **PASS** | Flag não declarada impede boot: `tests/test_pack_loader.py` na suíte verde. `event_type` fora do catálogo e sincronia de constantes: `tools/check_event_envelope.py` e `tools/codegen.py --check` rc=0 |
| Invariantes 1–4 | **PASS** | `check_core_boundary`, `check_contract_literals`, `check_event_envelope` rc=0. Varredura minha: nenhum `from domains`/`import domains` em `range-core/`; `objective_ids` não aparece em nenhum caminho de emissão |
| `05` §8 — nenhum serviço sem autenticação | **PASS** | `autoriza` é dependência global e nega rota não declarada (`auth.py:140-141`); `/docs`, `/redoc`, `/openapi.json` desligados (`app.py:66-68`). A ordem `autoriza` → `degrada` agora tem prova comportamental e estrutural (ver [H1] quanto ao canal de latência) |
| `05` §6 — bind em 127.0.0.1, sem porta publicada | **PASS** | `.env.example:56-58`; `docker-compose.audit.yml:57-58,69-70` publica em `127.0.0.1:15432` e `127.0.0.1:16379` |
| `05` §1 e §3 — nada ofensivo, dado sintético | **PASS** | `tools/check_security_constraints.py` e `tools/check_synthetic_data.py` rc=0. `registros.py` não contém CPF, e-mail, IP nem domínio. Criptografia presente é JWT legítimo (HS256, `algorithms` do verificador, `require: exp`), permitido por `CLAUDE.md` |
| `06` T15 — nenhuma dependência não pinada | **PASS** | `scripts/check_pinned_images.py` rc=0: 2 imagens, 3 arquivos, sem divergência de digest |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_api_degradacao.py:349-358`** — `test_sem_token_nao_paga_a_latencia_declarada`.
   Vacuoso: ver [H1]. Passa com a ordem invertida.

Duas costuras que examinei e considero legítimas, para que não passem por não-examinadas:

2. `tests/test_api_degradacao.py:213` afirma a latência por `dormidas == [2.5]`, sobre a função
   `dormir` injetada. **Não é asserção sobre mock**: o `2.5` percorre o caminho de produção
   inteiro — `api_surface.yaml:213` → `Degradacao.segundos` → `Degradador.aplica` — e o default
   do parâmetro é `asyncio.sleep` (`degradacao.py:128`). Mesma costura de tempo já usada no
   relógio e no token.
3. `tests/test_api_degradacao.py:360-372` — o teste estrutural compara
   `app.router.dependencies` com `[autoriza, degrada]`. É asserção sobre configuração, não
   sobre comportamento, e está declarada como tal no docstring; o comportamental que a
   acompanha (`:337-347`) é que carrega a prova. O par é legítimo.

Nenhum `skip`/`xfail` sem justificativa na árvore: os únicos `skipIf` são os de
`AURORA_TEST_DATABASE_URL`/`AURORA_TEST_REDIS_URL` (`tests/test_event_store_postgres.py:49,74,118,179`
e `tests/test_projection_cache.py:231`), e com a stack no ar **nenhum pulou** — 217 executados,
zero pulos.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **A fronteira real do PR e o gate `spec_freeze`.** Pelo [H2], `main` já contém cinco das seis
   peças da fase. Auditei o conjunto `02383e4..5b219a7` além do diff de 9 arquivos que o
   lançador delimitou — isso é declaração, não escolha silenciosa de base. Não tenho como
   julgar se `spec-change` e código foram PRs separados.
2. **A afirmação de `fase_3.md:1082`** — *"inverter a ordem em `app.py` deixa 4 testes
   vermelhos"*. Não tenho `Write`/`Edit`; não plantei a inversão. A contagem é consistente com
   3 subTests + 1 estrutural, e é o que sustenta o [H1].
3. **A afirmação de `fase_3.md:1055-1057`** — o lançador saindo com `rc=1` num repositório de
   sandbox, sem criar worktree e sem tocar no Docker. Li o script e confirmo que a guarda
   (`:119-162`) está **antes** de `mkdir -p .aurora-worktrees` (`:164`) e do compose (`:209+`);
   a execução em si não reproduzi.
4. **Probes de runtime fora da suíte.** `python -c` e execução de subconjunto
   (`python -m unittest tests.X -v`) não estão na allowlist do auditor — só
   `python -m unittest discover -s tests`. O [H1] está apoiado em leitura de quatro sítios de
   caminho determinístico, não em execução instrumentada. Registro também dois bloqueios
   indevidos que encontrei (`WORKFLOW.md:55` trata falso bloqueio como defeito, não
   inconveniência): a sequência `->` dentro de string citada é lida como redirecionamento, e um
   *path* de URL (`/turmas/...`) é lido como caminho absoluto fora do worktree.
5. **Procedência do `range_core` executado (P3-4).** Desta vez consegui verificar em parte:
   `.git/HEAD` da árvore principal aponta para `refs/heads/fase-3-auditoria-r1`, isto é, o
   próprio commit candidato — então o núcleo resolvido pela instalação editável coincide com o
   auditado, e o lançador exigiu árvore limpa. A previsão de `fase_3.md:867-872` de que a P3-4
   "passa de latente a real nesta execução" **não se realizou**, porque a árvore principal está
   na branch da fase e não em `main`. O risco residual permanece: um commit na árvore principal
   *durante* a auditoria trocaria o núcleo sob os meus testes, e nenhum teste afirma a raiz de
   resolução dos três pacotes.
6. **CI real e branch protection.** Li `.github/workflows/invariants.yml` e executei localmente
   os steps que a allowlist permite; não observei execução do GitHub Actions nem os quatro
   required contexts de `WORKFLOW.md` §Gates. O auditor não tem rede, por decisão registrada
   na P2-19.
7. **Concorrência e montagem de produção.** P3-2 (cache frio sem single-flight) segue aberta
   por decisão; a suíte é sequencial e nenhum processo desta fase monta
   `academus-api` + `RedisProjectionCache` + `PostgresEventStore` — isso é Fase 4.
8. **Afirmações do registro sobre eventos passados** (o hook ter recusado a primeira versão de
   um teste, os 21 comandos conferidos contra a cópia instalada). Verifiquei o estado final —
   `phase0_negative_tests.py` confirma que fonte versionada e cópia instalada de
   `readonly_bash.py` são idênticas —, não o histórico.
```

**Resumo:** PASS. Os quatro itens da DoD estão provados por teste que executei, a suíte roda 217/217 com zero pulos e todos os verificadores estão verdes. Os dois HIGH não bloqueiam a fase, mas ambos são da mesma família e vale corrigi-los antes da Fase 4: um teste novo que não discrimina nada e é anunciado como prova, e a guarda de base que fecha o caso degenerado enquanto o caso real — merge peça a peça antes da auditoria — continua passando em silêncio, como esta própria sessão demonstra.
