# Auditoria de checkpoint - 2026-08-21T17:16:32.778093+00:00

- fase: **6**
- commit auditado: `652745d7f7d91758a8e9d1c3c6d976642bdd7ac0`
- session_id: `10b33833-a7e1-48ab-96e3-9af8f7005ee0`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 6

## VEREDITO: PASS

Suíte real executada neste worktree (`.aurora-audit/venv/Scripts/python -m unittest discover -s tests`): **678 testes, OK, 140 pulados** — todos os pulos por serviço ausente (`AURORA_TEST_DATABASE_URL` / `AURORA_TEST_REDIS_URL`), com razão declarada e impressa. Nenhum `skip`/`xfail` sem justificativa, nenhum teste da Fase 6 entre os pulados.

Os dois BLOCKERs da auditoria anterior estão fechados com mecanismo, e não com prosa: B1 (`required_for_complete_pack` virou código em `_verify_completude`, e `ground_truth.yaml`/`objectives.yaml` entraram em `x-aurora-documents`) e B2 (a rota `GET /audit/grade-changes` é exercitada por `TestClient` com emissor real, afirmando sobre o evento gravado no store). O H1 daquela rodada também caiu: `scripts/phase0_negative_tests.py` sai **rc=0** e `scripts/check_insumo_de_metrica.py` é executável — eu o rodei.

---

## FINDINGS

### BLOCKER

Nenhum.

### HIGH

- **[H1] O computador da verificação não aplica o reinício de epoch de `facilitation`/`adjudication`, e o da declaração aplica — as duas metades do par `TTID/TTIV` rodam sob políticas de epoch diferentes**

  Arquivo: `range-core/metrics/verificacao.py:130-160` (`computa`) e `:163-194` (`_instante_do_limiar`); contraste com `range-core/metrics/declaracao.py:101-110`; definição em `range-core/metrics/epoch.py:80-85` e `:158-190`

  Requisito violado: `09_EVENT_MODEL.md` §3.1, tabela dos `reason` — linhas `facilitation` (*"Métricas recomputadas a partir da nova epoch, com nota"*) e `adjudication` (*"Métricas da nova epoch, com registro do motivo da anulação"*); `00_MASTER_SPEC.md` §3.2, *"Epoch é computação do consumidor"*

  Evidência:
  - `epochs_em_calculo()` (`epoch.py:158-190`) implementa os quatro motivos, com `MOTIVOS_QUE_REINICIAM = {facilitation, adjudication}` produzindo um piso de epoch. Medido por `grep`: **o único chamador é `declaracao.py:110`** (`apenas(insumo.eventos, epochs_em_calculo(insumo.epoch))`).
  - `verificacao.py:138-142` usa apenas `no_calculo(insumo.eventos, epochs_descartadas(...))`, que remove **só** epochs de `rehearsal`. `TTCV`/`TTRV` escapam da consequência por outro caminho — `_veredito` filtra `simulation_epoch == corrente` (`:197-212`) —, mas `_instante_do_limiar` percorre **todos** os `assessment_submitted` não descartados, inclusive os de epochs anteriores a um rollback de `facilitation`/`adjudication`.
  - `range-core/metrics/declaracao.py:104-106` afirma no docstring que o consumo de epoch *"passa por `epochs_em_calculo`, que aplica os QUATRO efeitos de `09` §3.1 e não apenas os dois que `06` T10 nomeia"*. A afirmação vale para aquele lado e não vale para o outro; nenhum docstring de `verificacao.py` a corrige.
  - Nenhum teste cobre a direção: `test_metrics_verificacao.py:194-222` cobre veredito de epoch abandonada (via `corrente`), `:261` cobre `rehearsal`; não há caso de `TTIV` atravessando `facilitation`/`adjudication`.

  Por que é HIGH e não BLOCKER: os itens 3 e 4 da DoD nomeiam apenas `technical_failure` e `rehearsal`, e os dois estão corretos nos **dois** computadores — a checklist binária da fase passa. O que está violado é a outra metade de `09` §3.1, que o próprio código elegeu implementar de um lado só. E qual das duas leituras é a certa para `assessment_submitted` — submissão de caso da Linha B é juízo append-only, ou entra na anulação da epoch? — é decisão normativa do proprietário, da mesma família das P6-3/P6-5. O que **não** é ambíguo é que as duas metades do mesmo par não podem estar as duas certas.

  Consequência concreta: depois de um rollback de `adjudication` — o caso em que o facilitador anula decisão por informação fora de banda —, `TTID` reinicia e `TTIV` pode marcar num instante da epoch anulada. O par sai do AAR com um número medido contra uma linha temporal que o facilitador rebobinou, e **nada falha**: a métrica continua sendo calculada.

### MEDIUM

- **[M1] Item 5 da DoD tem prova só na direção da métrica; a direção do estado não é exercitada por nada**

  Arquivo: ausência em `tests/`; superfície em `range-core/participant/api/emissor.py:8-18`; fold em `range-core/state/simulation_state.py:440` e `:457`

  Requisito violado: `07_IMPLEMENTATION_PHASES.md` §Fase 6, item 5 da DoD (*"Declaração não altera nenhum valor de ground truth"*); `06_ACCEPTANCE_TESTS.md` T10, segundo critério; `09_EVENT_MODEL.md` §2.1

  Evidência: os dois testes que sustentam o item são `test_metrics_verificacao.py:179-182` (`test_a_declaracao_nao_move_o_veredito`) e `test_metrics_declaracao.py:358-364` (`test_o_veredito_nao_marca_ttcd`) — os dois afirmam sobre **medida**, não sobre estado. Busquei por `nao altera` / `SimulationState` junto de `CONTAINMENT_DECLARED` em `tests/`: nada. A propriedade é verdadeira hoje por construção — o fold só escreve em `INJECT_FIRED` (`:440`) e `DECISION_MADE` (`:457`), e `Emissor` só carrega `store` —, mas `scripts/check_fold_authority.py` (rc=0) prova **autoridade e sítios de construção**, não *quais `event_type` escrevem*. Acrescentar um ramo `containment_declared` ao fold deixaria a suíte inteira verde, e é a consequência normativa 1 de `00_MASTER_SPEC.md` §3 que cairia sem nada ficar vermelho.

- **[M2] `observability_hooks.yaml` declara `vpn_access_revoked` e nada na árvore o emite; T9 não tem gate**

  Arquivo: `domains/academus/observability_hooks.yaml:35-38`

  Requisito violado: `06_ACCEPTANCE_TESTS.md` T9, primeiro critério — *"Cada `event_type` declarado em `observability_hooks.yaml` é emitido pela ação correspondente"* (Fase 6)

  Evidência: medido por `grep` sobre `*.py` — `VPN_ACCESS_REVOKED` aparece em `contracts/generated/events.py`, em `range-core/engine/verificacao.py` (leitura) e em cinco arquivos de teste; **nenhum produtor**. O comentário do arquivo (`:23-27`) declara o destinatário (`federated-identity-simulator`, Fase 11 por `07` tabela de fases), e `range-core/engine/inject_engine.py:_avalia` registra a mesma medição. O critério, porém, é de **Fase 6** e está insatisfeito para metade dos hooks declarados. Agrava: não existe verificador cruzando os hooks com os emissores reais — `tests/test_api_emissao.py:117-140` cruza apenas os `payload_fields` de `audit_query_performed` com a assinatura do emissor. Hook que *deveria* emitir e não emite passaria igual.

### LOW

- **[L1] O caminho de emissão da `participant-api` não aplica o contrato fechado de `assessment_submitted`, e o Brier consome o payload cru**

  Arquivo: `range-core/participant/api/app.py:154-168` (payload = corpo menos duas chaves); `range-core/metrics/calibracao.py:226-242` (`_por_caso`, aceita qualquer `int`); contrato em `contracts/assessment.schema.yaml:94-100` (`confidence`, `0..100`, inteiro)

  Requisito violado: nenhuma cláusula manda validar payload em runtime — `03_EXERCISE_DESIGN.md` §5.1 fixa `confidence: 0–100`, e o limite é declarado em `contracts/events.schema.yaml` (*"o event store não valida payload — isto vale para TODO evento"*)

  Evidência: `POST /participant/assessment` grava o corpo do pedido como payload sem validação; `_por_caso` só exige `isinstance(confianca, int)`. Um `confidence: 900` submetido por participante entra no Brier de `03` §5.3 como `(9.0 − defensibility)²` e desloca o instante de `TTIV`. É o primeiro caso na árvore em que payload de participante alimenta métrica computada, e a guarda de contrato não morde nesse caminho.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Consulta à trilha com filtro de período emite `audit_query_performed` | **PASS** | `tests/test_api_emissao_pela_rota.py:107-245` — rota real por `TestClient`, emissor real, asserção sobre o evento no `InMemoryEventStore`; inclui os negativos (período inválido/malformado/sem token não emitem) e o positivo de `result_count: 0`. Guarda de boot em `domains/academus/api/app.py:294-318` |
| 2. Cada par declaração × verificação é disparável e testado | **PASS** | `test_metrics_declaracao.py` (TTCD/TTRD/TTID), `test_metrics_verificacao.py:124,131,316-431` (TTCV/TTRV/TTIV), `test_laco_de_verificacao.py:220-247` (laço → veredito → `TTCV`, com o negativo `test_sem_o_laco_ttcv_nunca_marca`) |
| 3. Desconto por **união**, nunca soma | **PASS** | `range-core/metrics/epoch.py:280-327`; `test_metrics_epoch.py:263-288` afirma 40 min e **nega** os 30 min que a soma produziria |
| 4. Nenhum evento de epoch `rehearsal` entra em cálculo | **PASS** | `epoch.py:128-155`; `test_metrics_epoch.py:207-232`, `:295-364`; `test_metrics_verificacao.py:261` |
| 5. Declaração não altera nenhum valor de ground truth | **PASS com ressalva** | Verdadeiro por construção (fold escreve só em `INJECT_FIRED`/`DECISION_MADE`); provado só na direção da métrica — ver **M1** |
| 6. Declaração prematura → lista de incompatíveis no AAR | **PASS** | `range-core/aar/timeline.py:182-244`; `test_aar_timeline.py:126-255` — janela, extremos, e os negativos (fato antes da declaração não entra; ação de participante não entra; lacuna de consciência não carrega incompatíveis) |
| 7. Pack sem `verification_predicates` não carrega | **PASS** | `pack_loader.py:448-505` (`_verify_completude`) + `scenario.schema.v2.yaml:32-47` (`ground_truth.yaml` em `x-aurora-documents`); `test_pack_completo_materializado.py:166-283` cobre completo-sem-predicados, apenas-manifesto (legítimo) e o **meio-termo** |
| 8. `TTIV` por limiar de calibração, não por predicado de mundo | **PASS** | `verificacao.py:163-194` (recalcula o Brier por prefixo, marca a **primeira** que cruza, `<=`); `test_metrics_verificacao.py:316-431`, incl. `test_o_limiar_vem_do_INSUMO_e_nao_de_constante` |
| 9. Brier só no escopo revisado; lacuna de cobertura em separado | **PASS** | `metrics/calibracao.py:134-261` — `None` para escopo vazio, não-avaliado como `confidence=0` e nomeado, overconfidence/underconfidence/lacuna em campos separados; `test_metrics_calibracao.py` |
| 10. Rubrica ausente ou em versão divergente impede carga | **PASS** | `contract_rules.py:177-195` (`rubric_library`, versão dentro do id); `test_pack_loader.py:434-448` nas duas metades; fixtures no contrato executadas por `check_contract_examples.py` (rc=0) |
| 11. Divergência ≥ 2 pontos gera alerta | **PASS** | `timeline.py:281-320` (distância entre **extremos**, última nota do avaliador); `test_aar_timeline.py:296-352`, com o negativo de 1 ponto e o de avaliador único |
| 12. As nove siglas classificadas pelo critério de `00` §3.2 | **PASS** | `test_derivacao_das_nove_siglas.py` — tabela **lida de `03` §3.0** e conjunção aplicada às colunas (não reimplementada); `:286-299` deriva o conjunto de siglas **rodando os dois computadores**, sem lista intermediária |
| 13. Todo `event_type` tem `metric_side`; os lados são disjuntos | **PASS** | `contract_rules.py:123-153` (cobertura nas duas direções, valores no conjunto, lado vazio reprovado; disjunção por construção do mapa); 4 defeitos plantados reprovados em `check_contract_examples_probes.py` (rc=0). Conferi o mapa contra `09` §4.1: 33 tipos, 7+9+5+12 |
| 14. Folha de predicado obedece à conjunção, com probe das três violações + controle positivo | **PASS** | `contract_rules.py:155-173` (`state_effect` **e** `verification` → exatamente os cinco de `09` §4.0); `ground_truth.schema.yaml:650-757` — as três formas (autorreferência, `assessment_submitted`, `communication_submitted`) mais typo, `containment_declared` e `decision_made`; `check_contract_examples.py`: 88 negativos executados, rc=0 |
| 15. Cada computador recebe só o seu lado; só o AAR tem as duas metades | **PASS** | `scripts/check_insumo_de_metrica.py` **executado aqui**: rc=0 (tipos próprios, ponto único de montagem, campos nas duas direções); probes rc=0 — 6 violações plantadas, 6 reprovadas, mais o positivo do consumidor que só anota; `aar/timeline.py:156-179` é o único escopo com as duas metades, com o teste `:142-164` |
| 16. Desconto, `rehearsal` e seleção de start são cálculo do consumidor | **PASS** | `insumo.py:134-165` entrega a escrituração **inteira** aos dois lados; seleção de start em `declaracao.py:139-155` sobre o payload; `test_metrics_epoch.py:428-441`. Ver **H1** para a assimetria dos outros dois motivos |
| 17. `inject_fired` emite os marcadores de start no payload, com teste de emissão | **PASS** | `inject_engine.py:310-330`; derivação das três pernas em `pack_loader.py:672-694` (com `reveals` excluído); `test_inject_engine.py:767-820` afirma sobre o payload do evento emitido, para **todos** os injects do pack, incluindo o de ruído |
| — Fase 4, itens 1 e 4 (regressão de container) | **NÃO VERIFICADO** | `check_provas_de_container.py` rc=2 — ver abaixo |
| — Fase 5, M2 (prova do seed) | **NÃO VERIFICADO** | `check_prova_do_seed.py` rc=1 — ver abaixo |

---

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`tests/test_metrics_verificacao.py:179-182` e `tests/test_metrics_declaracao.py:358-364`**, quando lidos como prova do item 5 da DoD. Eles provam que a declaração não move o **veredito** e não marca **TTCD** — propriedade da partição de insumo. O requisito é *"não altera nenhum valor de ground truth"*, que inclui flag, fato e estado de simulação, e nenhuma asserção da árvore toca essa direção (M1).
2. **`tests/test_metrics_verificacao.py:194-222` (`SoALinhagemCorrenteSustenta`)**, quando lido como prova de que o lado da verificação respeita `09` §3.1. Ele passa por causa do filtro `simulation_epoch == corrente` dos vereditos, e não pelo consumo de epoch; `TTIV` não é alcançado por ele e não é coberto por nenhum outro (H1).

Os achados M1/M2 da rodada anterior estão de fato fechados, e **não** por conserto do teste: `bars_score_payload` passou a referenciar o enum da rubrica por `$ref` entre contratos (`events.schema.yaml`), e `test_aar_timeline.py:354-435` itera as nove e recusa `banana`, `incident_triage_v2`, `INCIDENT_TRIAGE` e `""`, mais o teste de que o payload **não** reenumera. Verifiquei também que `test_derivacao_das_nove_siglas.py` não mede a si mesmo: as entradas e a operação saem da spec lida em disco, e a terceira direção roda os computadores.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 1 e 4 da DoD da Fase 4 (regressão de container).** `scripts/check_provas_de_container.py` **recusou**, rc=2: `demo` e `reinicio` saíram rc=125 porque `docker compose up` falhou (`npipe:////./pipe/dockerDesktopLinuxEngine` ausente — daemon fora do ar), conforme `.aurora-audit/stack.log`. Permanecem **NÃO VERIFICADO**, nunca PASS por silêncio.
- **Prova do seed completo (M2 da Fase 5).** `scripts/check_prova_do_seed.py` **recusou**, rc=1: gravada sobre `c119718c615fd3a614231a91d56ff646ff14ed71`, e este checkout é `652745d…`. Medição **transportada** da árvore principal, tratada como não medida — que é o caso normal declarado pelo lançador nesta rodada, e não anomalia.
- **Tudo que exige Postgres/Redis**: 140 dos 678 testes pularam. Inclui trilha de auditoria com hash, business state, store em Postgres e projeção materializada. Nenhum item da Fase 6 que julguei PASS depende deles — a instrumentação de `/audit/grade-changes` é provada com repositório duplo, e isso é decisão declarada no cabeçalho da suíte, não atalho.
- **A costura fraca de `00` §3.2 além do que a checagem alcança.** `check_insumo_de_metrica.py` prova *onde o construtor aparece* por AST sobre `range-core/` e `domains/`. Ele não alcança construção dinâmica (`getattr`, `importlib`) nem código fora desses dois diretórios; a própria §3.2 declara a costura fraca, e eu não a fortaleci — apenas confirmei que ela existe e reprova nas seis direções plantadas.
- **Comportamento em produção dos computadores de métrica.** `monta()` não tem chamador de produção: nenhuma rota ou projeção servida consome métrica nesta fase (`processo.py` liga apenas o `LacoDeVerificacao`). Os itens 2–17 estão provados em nível de unidade e integração-em-memória; o caminho ponta a ponta com a stack de pé não foi exercitado por mim.
- **`review_scope`.** `InsumoDeVerificacao.escopo_revisado` chega **resolvido** e nada na árvore resolve a prosa de `03` §5.1 num conjunto de `case_id` (P6-5, declarada e adiada para a Fase 7). Julguei o item 9 sobre o cálculo, que existe e está correto; a origem do escopo não é verificável nesta fase.
- **CI.** Li o `invariants.yml` e confirmei que `check_insumo_de_metrica` e os probes entraram como steps, junto da materialização do pacote completo antes do `up`. Não executei o workflow; o que afirmo é o conteúdo do arquivo e o resultado local dos mesmos comandos.
