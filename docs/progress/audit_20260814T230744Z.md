# Auditoria de checkpoint - 2026-08-14T23:07:44.763706+00:00

- fase: **1**
- commit auditado: `4a7b092e010e53eaf3608641908324d92c5cd04a`
- session_id: `6caa20f1-4ace-4a53-8b41-c29f7f084e7f`
- modo: `interactive`
- veredito detectado: **indeterminado**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

Auditoria concluída. Segue o relatório no formato obrigatório.

```text
# AUDITORIA — FASE 1

Commit auditado: `4a7b092e010e53eaf3608641908324d92c5cd04a` (HEAD confirmado no worktree
`.aurora-worktrees/audit`, árvore limpa antes e depois das execuções).
Comparação: `git diff main...HEAD` — 58 arquivos, 5126 inserções.

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- [B1] Item 8 da DoD não passa — `RANDOM_SEED` não é lido por nenhum código
  Arquivo: `.env.example:15` (única declaração); nenhum consumidor em `range-core/` ou `domains/`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md:61` — "- [ ] `RANDOM_SEED` lido de `.env`";
  `CLAUDE.md` §"Fluxo por fase" ("uma fase só está concluída quando **todos** os itens da
  Definition of Done passam"); ENTRY da Fase 2 (`07:67`) exige "Fase 1 completa"
  Evidência: varredura no repositório (excluindo `docs/`) devolve apenas `.env.example:15`,
  `.gitignore:38`, `CLAUDE.md:117`, `CHANGELOG_V3.md:32`, o comentário em
  `user-scope/hooks/readonly_bash.py:217` e `scripts/phase0_negative_tests.py:167`, que executa
  `grep RANDOM_SEED .env.example` — presença de linha em arquivo de exemplo, não leitura por
  código. `docs/progress/fase_1.md:169` marca o item ⚠️ e `:171` conclui "A fase não está concluída".
  Por que é blocker: a DoD é binária e este item não passa. Registro de mérito, que não altera o
  veredito: a marcação foi honesta e a recusa do operador em escrever um consumidor artificial só
  para fechar o item (`fase_1.md:173-177`) está correta — fechar DoD criando o consumidor não é
  evidência. As saídas limpas são implementar o consumidor real ou mover o item por `spec-change`;
  nenhuma delas é "auditor aceita".

- [B2] Dependências Python não pinadas, contra `00_MASTER_SPEC.md` §8 e T15
  Arquivo: `pyproject.toml:30` (`jsonschema>=4.21`), `:34` (`alembic>=1.13`), `:35` (`sqlalchemy>=2.0`)
  Requisito violado: `00_MASTER_SPEC.md` §8 ("Todas as versões pinadas") e
  `06_ACCEPTANCE_TESTS.md` T15, linha 114 ("Nenhuma dependência não pinada") — T15 vale
  "todas as fases"
  Evidência: o primeiro manifesto de dependência do projeto usa três faixas abertas `>=`.
  `.github/workflows/invariants.yml:169` instala essas dependências no job `contratos`, que é
  o único que executa `scripts/check_contract_examples.py` — a evidência do item 1 da DoD.
  No mesmo commit, `docker-compose.yml:10-22` documenta explicitamente por que tag mutável não
  é pinagem ("pinado significa conteúdo imutável, não rótulo estável") e pina Postgres e Redis
  por digest (P1-8). A mesma regra não foi aplicada ao `pyproject.toml`, e a divergência não
  está registrada em nenhuma pendência de `fase_1.md` §6.
  Por que é blocker: o único gate que sustenta o item 1 da DoD passa a depender de artefatos que
  podem mudar sem commit neste repositório. Um `jsonschema` novo que altere resolução de `$ref`,
  tratamento de `oneOf` ou a API de `referencing` muda o resultado do gate sem que nada apareça em
  diff — que é exatamente o argumento aceito para pinar as imagens por digest, aplicado de forma
  inconsistente dentro do mesmo commit. Nota: `actions/checkout@v4` e `actions/setup-python@v5`
  (`invariants.yml:12-15`) são a mesma classe, mas estão declarados como pendência herdada P25
  (`fase_1.md:417`); estes três não estão declarados em lugar nenhum.

### HIGH

- [H1] O objeto `correlation` do envelope normativo continua ausente do contrato
  Arquivo: `contracts/events.schema.yaml:37-85` (bloco `properties`), `:83-85` (`fact_id` no topo)
  Requisito violado: `09_EVENT_MODEL.md` §1 (envelope universal) e `00_MASTER_SPEC.md` §5.4
  ("Um envelope de evento")
  Evidência: busca por `correlation`, `causation_id`, `scenario_id` e `inject_id` em
  `contracts/events.schema.yaml` não retorna nenhuma ocorrência. O envelope normativo de `09` §1
  declara `"correlation": { "scenario_id", "inject_id", "causation_id", "fact_id" }`; o contrato
  declara apenas `fact_id`, e no topo, com o comentário de `:83` citando "09 secao 1" — seção onde
  o campo está dentro de `correlation`. O exemplo positivo (`:272-283`) também não traz
  `correlation`. Como `additionalProperties` fica aberto por decisão (`:101-103`), um evento com
  `correlation` valida sem que forma nenhuma seja verificada.
  Por que é high: este achado é textualmente o H1 da auditoria anterior, registrada no próprio
  commit (`docs/progress/audit_20260814T224156Z.md:38-42`). Entre `59580e6` e `4a7b092` o B1 foi
  corrigido (`baac1ca`) e este H1 não foi nem corrigido nem registrado como pendência —
  `fase_1.md` §6 lista P1-1 a P1-12 e nenhuma delas trata de `correlation`.
  `docs/process/WORKFLOW.md:36` define o ciclo como "corrigir BLOCKER/HIGH, criar novo commit e
  reauditar". Consequência concreta: a Fase 2 constrói event store, cadeia de causação e
  correlação de branch sobre este contrato, e quatro campos declarados na spec chegam lá sem
  contrato — `causation_id` e `inject_id` inclusive.

- [H2] O contrato permite evento sem `clock_multiplier`, contra o MASTER SPEC
  Arquivo: `contracts/events.schema.yaml:28-35` (bloco `required`), `:64-66` (propriedade declarada
  e não exigida)
  Requisito violado: `00_MASTER_SPEC.md` §5.6 — "**Dois relógios, sempre.** Todo evento carrega
  `exercise_time` e `wall_timestamp`, mais `clock_multiplier`"; `06_ACCEPTANCE_TESTS.md` T4,
  linha 30; `07_IMPLEMENTATION_PHASES.md:76`
  Evidência: o `required` do envelope tem sete campos e `clock_multiplier` não está entre eles;
  a propriedade existe em `:64-66` apenas com `type: number` e `exclusiveMinimum: 0`. Uma instância
  sem o campo valida. A única base para a omissão é a lista de `09_EVENT_MODEL.md` §1.1, que é
  documento não-master; `CLAUDE.md` §"Autoridade" diz que em conflito o MASTER_SPEC prevalece, e
  o conflito não está registrado como escalação em lugar nenhum de `fase_1.md`.
  Por que é high: é a mesma classe do defeito P1-12 (`simulation_epoch: minimum: 1`), no mesmo
  bloco do mesmo arquivo, na direção oposta — lá o contrato recusava o que a spec exige, aqui
  aceita o que a spec proíbe. O custo de correção é zero: o exemplo positivo de `:282` já declara
  `clock_multiplier: 1`. Se ficar como está, T4 ("Todo evento carrega ... `clock_multiplier`")
  deixa de ter guarda no contrato exatamente na fase que constrói o clock, e a reconstrução de
  timeline do AAR (`01` §3, "`clock_multiplier` gravado em cada evento para reconstrução") fica
  sem garantia estrutural.

- [H3] Falso bloqueio novo: a prova central introduzida nesta fase não é executável pelo auditor
  Arquivo: `user-scope/hooks/readonly_bash.py:11-89` (allowlist; `:41` libera nominalmente apenas
  `scripts/phase0_negative_tests.py`); `scripts/check_contract_examples.py`,
  `scripts/check_contract_examples_probes.py` (não allowlistados)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md:151-156`, item 4(e) — "Falso bloqueio novo
  é finding"; `docs/process/WORKFLOW.md:55` — "**Bloqueio indevido também é defeito.** Um auditor
  que não consegue rodar a prova central audita por inferência de leitura de código"
  Evidência: `python scripts/check_contract_examples.py` retorna
  "BLOQUEADO: comando fora da allowlist do auditor" (execução registrada nesta sessão). O commit
  cria dois executores novos em `scripts/`, cria o job `contratos` que os invoca
  (`invariants.yml:174-181`) e os declara como o quarto required status check
  (`WORKFLOW.md:118-127`), mas não estende a allowlist nem inclui os dois casos na lista de
  defeitos afirmados do harness — busca por `check_contract_examples` em
  `scripts/phase0_negative_tests.py` não retorna ocorrência, e as 10 linhas
  "AINDA BLOQUEADO (defeito aberto, P23)" impressas pela execução não mencionam nenhum dos dois.
  Por que é high: o item 1 da DoD é precisamente o item que esta fase reabriu por ter sido marcado
  ✅ **por presença** (`fase_1.md` §1.3). O mecanismo que o fecha existe e é rigoroso, mas o
  auditor não pode executá-lo: o item volta a repousar em relato do implementador, que é a
  propriedade que o checkpoint existe para não aceitar. `user-scope/` está no conjunto `CODE` do
  `spec_freeze` (`invariants.yml:115-118`), então a correção cabia neste mesmo commit.

### MEDIUM

- [M1] `check_event_envelope.py` varre apenas Python, enquanto os outros dois verificadores de
  código varrem também TypeScript
  Arquivo: `tools/check_event_envelope.py:42-43` (`SCANNED_DIRS`, `PYTHON_SUFFIXES = (".py",)`) e `:92`
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T1, linha 10 ("Nenhum evento emitido pelo core ou
  pelo adapter carrega `objective_ids`")
  Evidência: `check_core_boundary.py` e `check_contract_literals.py:160` varrem `WEB_SUFFIXES`,
  este último com justificativa explícita no docstring (`:21-24`) de que cobrir só Python deixaria
  literal em TSX passar pelo gate. O verificador do invariante 4 chama
  `iter_files(REPO_ROOT, SCANNED_DIRS, PYTHON_SUFFIXES)`. Um emissor em `range-core/web/` ou
  `domains/<adapter>/web/` que montasse envelope com `objective_ids` sairia rc=0.
  Por que não é maior: latente — não há nenhum arquivo `.ts`/`.tsx` de aplicação na árvore hoje.
  Vira exposição real na Fase 4. Registro de processo: é o M1 da auditoria anterior
  (`audit_20260814T224156Z.md:46-50`), não corrigido e não registrado como pendência.

- [M2] Duas cópias das faixas sintéticas de segurança, já divergentes, com o contrato afirmando
  um consumo que não existe
  Arquivo: `contracts/evidence.schema.yaml:168-198` (registro e a frase "Registro consumido ... por
  tools/check_synthetic_data.py") vs `tools/check_synthetic_data.py:81-97` (cópia própria)
  Requisito violado: `05_SECURITY_REQUIREMENTS.md` §3 ("IPs apenas de faixas de documentação
  (RFC 5737 / RFC 3849) ou privadas")
  Evidência: `check_synthetic_data.py` não lê o contrato; declara `RESERVED_TLDS`,
  `RESERVED_DOMAINS` e as CIDRs em constantes próprias. As duas listas já não coincidem: o
  verificador inclui `2001:db8::/32` (RFC 3849, citada nominalmente por `05` §3) e
  `localhost`/`local`/`example.net`; `allowed_ip_ranges` do contrato (`:182-188`) não tem nenhuma
  faixa IPv6, e `allowed_domain_suffixes` (`:192-197`) não tem `example.net`.
  Por que é medium: hoje quem guarda dado real é o verificador, e ele é o mais estrito nas
  faixas IP; o dano é a divergência declarada como sincronia, que na Fase 9 vira o
  evidence-simulator gerando conforme o contrato e o CI julgando conforme a constante.

- [M3] Predicado de verificação pode ser satisfeito por uma declaração de participante
  Arquivo: `contracts/ground_truth.schema.yaml:228-239` (`predicate_event`, `x-aurora-ref:
  event_catalog` sem restrição de camada)
  Requisito violado: `00_MASTER_SPEC.md` §3, consequências normativas 1 e 2 (declaração não é
  verdade; toda métrica de resposta é pareada, "uma para a declaração, outra para a verificação
  objetiva"); `09_EVENT_MODEL.md` §2.1
  Evidência: o `x-aurora-ref` resolve contra o catálogo inteiro, que inclui os `*_declared` de
  `participant_action`. `verification_predicates.containment: {all: [{event:
  containment_declared}]}` valida no schema e em todas as regras `x-aurora`. O contrato fechou
  deliberadamente a porta do juízo do facilitador (`:173-181`: "um nó como
  `facilitator_thinks_response_was_good: true` não casa com forma nenhuma"), mas não a da
  declaração — e é a declaração que o modelo das quatro verdades separa. Registro do que **não**
  é o achado: `vpn_access_revoked` e `identity_scope_disabled`, usados no exemplo normativo de
  `03` §3.1, também são `participant_action` e são legítimos — são ações com efeito no mundo,
  não afirmações sobre ele. A distinção que falta ao contrato é essa, não a camada.
  Por que é medium: nenhum pack existe ainda; a exposição é a Fase 6, onde TTCD e TTCV colapsariam
  no mesmo instante e o delta — que `03` §3.2 chama de "o achado" — deixaria de existir sem que
  nada falhe.

- [M4] `observability_hooks.yaml` carrega `event_type` sem nenhuma camada que o valide contra o
  catálogo fechado
  Arquivo: `domains/academus/observability_hooks.yaml:19,24`
  Requisito violado: `09_EVENT_MODEL.md` §4 ("registro fechado ... CI que falha com `event_type`
  não registrado")
  Evidência: os seis contratos não incluem schema para este arquivo (`01_ARCHITECTURE.md` §2 lista
  os seis, e nenhum o cobre); `tools/check_contract_literals.py:150-168` varre apenas `.py` e
  `WEB_SUFFIXES` sob `range-core/` e `domains/`, nunca `.yaml`; nenhuma regra `x-aurora-ref`
  alcança este arquivo. Um `audit_query_perfomed` aqui sai rc=0 em todos os gates.
  Por que é medium: os dois hooks presentes estão corretos e são os de `09` §6; o arquivo não
  consta dos OUTPUTS da Fase 1 (`07:47`). Mas é exatamente a falha que `09` §4 descreve como "a
  mais cara possível", e a instrumentação real chega na Fase 3.

### LOW

- [L1] A proibição de `objective_ids` no schema alcança apenas a raiz do documento
  Arquivo: `contracts/events.schema.yaml:98-99` (`not: required: [objective_ids]`), combinado com
  `additionalProperties` aberto (`:101-103`)
  Requisito violado: `09_EVENT_MODEL.md` §1.2
  Evidência: `not: {required: [objective_ids]}` avalia só o objeto raiz; um envelope com
  `payload: {objective_ids: [OBJ-03]}` valida. A fixture negativa de `:331-343` põe a chave no
  topo e nunca exercita esse caminho.
  Por que é low: `tools/check_event_envelope.py` pega o caso no código emissor, por AST, em
  qualquer profundidade — que é onde o invariante 4 é de fato aplicado. Vale para documento
  autorado à mão. Repetido da auditoria anterior (L1), não corrigido e não registrado.

- [L2] As constantes geradas em Python não são tipadas; as de TypeScript são
  Arquivo: `contracts/generated/events.py:4-30`, `domains/academus/generated/flags.py:4-30`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md:55` — "Constantes **tipadas** geradas em Python
  e TypeScript"
  Evidência: os artefatos Python são atribuições de módulo sem anotação (`X = "..."`, inferido
  como `str` mutável) e `ALL_FLAGS`/`ALL_EVENT_TYPES` como tupla sem `Final`. Os artefatos TS usam
  `export const` (tipo literal) e `as const` no agregado (`flags.ts:30`, `events.ts:70`).
  Por que é low: o propósito operacional do item — não haver string solta de flag ou `event_type`
  no código — está garantido por `check_contract_literals.py`, provado contra violação plantada.

- [L3] `exercise_timestamp` é opcional no envelope
  Arquivo: `contracts/events.schema.yaml:59-60`
  Requisito violado: `01_ARCHITECTURE.md` §3 ("Três marcas temporais, nunca uma só")
  Evidência: declarado como `type: string` sem `minLength` e fora do `required`; `09` §1 o traz no
  envelope e `09` §1.1 não o lista entre os obrigatórios. Mesma tensão de H2, com base normativa
  mais fraca (documento não-master), por isso separado e em LOW.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Os seis contratos existem e validam exemplos positivos e negativos | ⚠️ NÃO VERIFICÁVEL POR EXECUÇÃO (H3) | Os seis existem em `contracts/`. Contagem por leitura: 53 exemplos negativos (7+8+10+8+12+8) e `examples:` presente nos seis, batendo com `fase_1.md` §4.1. `scripts/check_contract_examples.py` é rigoroso na leitura — exige correspondência com `rejected_by`, exige que `rejected_by: x-aurora-*` seja **aceito** pelo schema e que a regra nomeada seja a **única** a disparar (`:395-421`). **Não pude executá-lo**: bloqueado pela allowlist (H3). Ressalva de conteúdo: o executor não exige mínimo de exemplos por contrato — um contrato sem `examples` passaria em silêncio; hoje todos os seis têm as duas espécies |
| 2 — Constantes tipadas em Python e TypeScript para flags e event types | ✅ com ressalva (L2) | `contracts/generated/events.{py,ts}`: 32 constantes cada, mesma ordem e valores, conjunto idêntico ao catálogo de `09` §4.1 (3+3+17+3+6=32), inclusive `separate_incident_declared`. `domains/academus/generated/flags.{py,ts}`: 12, batendo com `domains/academus/flags.yaml`; `domains/prontus/generated/` presente. `python tools/codegen.py --check` → rc=0 (executado) |
| 3 — Teste de fronteira core/adapter passa (por AST) | ✅ EXECUTADO | `python tools/check_core_boundary.py` → rc=0; harness: "OK: check_core_boundary.py detectou violacao em range-core/_phase0_probe_bad.py (rc=1)" |
| 4 — `event_type` fora do catálogo é rejeitado | ✅ no código e nos contratos / lacuna em YAML de domínio (M4) | Catálogo fechado por `enum` nos `$defs` `event_type_*` (`events.schema.yaml:205-253`); `tools/_common.py::load_declared_event_types` lê os mesmos `enum` que geram as constantes, sem segunda cópia; `x-aurora-ref: event_catalog` em `objectives.schema.yaml:115`, `ground_truth.schema.yaml:235` e `scenario.schema.v2.yaml:522`. Harness: "check_contract_literals.py (event_type) detectou violacao ... (rc=1)". Não alcança `observability_hooks.yaml` |
| 5 — Os seis verificadores liberam árvore limpa e cada um falha contra violação plantada | ✅ EXECUTADO | Árvore limpa: `check_core_boundary` 0, `check_contract_literals` 0, `check_event_envelope` 0, `check_security_constraints` 0, `check_synthetic_data` 0, `codegen --check` 0. `python scripts/phase0_negative_tests.py` → rc=0, com 13 linhas OK de violação plantada cobrindo os seis, e o resumo "Todos os seis verificadores falharam contra probes independentes" |
| 6 — `codegen.py --check` detecta dessincronia e não escreve | ✅ EXECUTADO | rc=0 na árvore limpa; `git status --short` vazio depois da execução (nada escrito); harness prova os dois sentidos: "codegen.py --check (artefato ausente) ... rc=1", "(artefato divergente) ... rc=1", "(artefatos de evento) ... rc=1" |
| 7 — `docker compose up` sobe Postgres e Redis | ⚠️ NÃO VERIFICADO | `docker-compose.yml` presente, imagens pinadas por digest, portas em `127.0.0.1` (`05` §6). `docker compose ps` no worktree falha na interpolação: "required variable POSTGRES_PASSWORD is missing a value" — comportamento correto de `:39` e prova de que não há default trivial, mas não sobe nada sem `.env`, que é proibido a mim. O relato de P1-1 é execução do operador, fora do meu alcance |
| 8 — `RANDOM_SEED` lido de `.env` | ❌ FAIL | B1 |

## TESTES QUE NÃO PROVAM O REQUISITO

- `scripts/phase0_negative_tests.py:167` — `("grep no .env.example", "grep RANDOM_SEED .env.example")`
  prova que a string existe no arquivo versionado de exemplo, não que a variável seja lida. Não
  sustenta o item 8, e `fase_1.md` corretamente não a usa para isso.
- As 53 fixtures negativas dos contratos provam **consistência interna**, não fidelidade à spec.
  O commit `baac1ca` já registra isso a partir do `simulation_epoch`. O H2 desta auditoria é a
  mesma classe ainda aberta, no mesmo bloco `required` do mesmo arquivo: nenhuma fixture força a
  lista de campos obrigatórios a bater com `00` §5.6, e nenhuma delas falharia se
  `clock_multiplier` nunca fosse exigido.
- `scripts/check_contract_examples.py:425-437` conta e reporta, mas não exige mínimo por contrato:
  um contrato que perdesse `examples` e `x-aurora-invalid-examples` sairia rc=0 e o item 1 da DoD
  continuaria verde. Não é defeito vivo — os seis têm as duas espécies hoje —, é a mesma classe
  "verde por ausência" que a fase corrigiu em §1.3.
- Nenhum `skip`/`xfail` encontrado: não existe suíte pytest no repositório. As "provas" da fase são
  os seis verificadores, o harness negativo e o executor de exemplos — os dois primeiros eu
  executei, o terceiro não (H3).

Registro em contrário, porque é o achado que eu procurei e não encontrei: o executor de exemplos
**não** aceita "falhou, logo está provado" (`:387-421`), e a proibição de `objective_ids` está
guardada em duas camadas independentes — schema e AST — sem nenhum caminho de emissão que a
carregue. Nenhum import de `domains/` em `range-core/`. Nenhuma violação de
`05_SECURITY_REQUIREMENTS.md` §1 encontrada: os "efeitos de ataque" são exclusivamente flags
booleanas em `domains/academus/flags.yaml`, o único IP em dado versionado é `192.0.2.10`
(`.env.example:51`, RFC 5737), o exemplo de `ground_truth` usa `198.51.100.42`, o `threat_actor`
declara fontes públicas e `note_to_facilitator: "Nenhum IOC operacional reproduzido"`, e
`evidence.schema.yaml:71` torna anexo inexpressável por `additionalProperties: false` em vez de
apenas desaconselhado.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Os exemplos dos contratos, por execução.** `python scripts/check_contract_examples.py` e
  `check_contract_examples_probes.py` são recusados pela allowlist (H3). Não confirmei que os 9
  positivos validam, que os 53 negativos são recusados pela camada declarada, nem os 6 eixos de
  probe. O item 1 da DoD está avaliado por leitura de código e contagem de fixtures — que é
  exatamente o modo de auditar que o item existe para não aceitar. Também não sei se `jsonschema`
  e `referencing` estão instalados neste ambiente. Nota correlata: `check_contract_examples.py:47-48`
  importa `referencing` diretamente, e `pyproject.toml` não o declara — hoje vem por transitividade
  de `jsonschema`, o que se agrava com B2.
- **`docker compose up`, healthchecks, bind em loopback e `alembic` executável.** Sem `.env` no
  worktree (e proibido de lê-lo), `docker compose ps` para na interpolação. Item 7 e P1-9 repousam
  em relato do operador.
- **Branch protection e os quatro required contexts** (P1-10) — ação fora do repositório.
- **A correção do parser YAML restrito de `tools/_common.py`.** O `_parse_scalar` citado na D4
  como defeito dormente corrigido não foi reexaminado; ele é quem entrega os contratos tanto aos
  verificadores quanto ao executor de exemplos, então um erro ali afeta as duas camadas ao mesmo
  tempo.
- **Fidelidade integral dos contratos a `02`, `03`, `04` e `08`.** Li `00`, `05`, `06`, `07`, `09`
  e `01` por inteiro; `03` e `04` apenas por busca dirigida (§3.1, §3.2, branching, capability_gap).
  `contracts/scenario.schema.v2.yaml` (861 linhas) e `ground_truth.schema.yaml` (605) foram lidos
  por amostragem dirigida às regras que a DoD e T12 nomeiam; `evidence`, `objectives`,
  `state_flags` e `events` foram lidos por inteiro. Podem existir divergências da classe do H2 nas
  partes que amostrei.
- **`.env`**, por proibição de `CLAUDE.md`.
```
