# Auditoria de checkpoint - 2026-08-14T03:47:19.559946+00:00

- fase: **0**
- commit auditado: `30f80c2b13165d27126a8af63097f04190a89b09`
- session_id: `ddf5437f-f6de-490b-b785-70cb8571176e`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 0

Commit auditado: `30f80c2b13165d27126a8af63097f04190a89b09` (detached, worktree `.aurora-worktrees/audit`)
Diff contra `main` (`edd9527`): 12 arquivos, +1486/−213. **Nenhum arquivo em `docs/spec/`** — `spec_freeze` não é acionado por este diff.
Documentos lidos: `00_MASTER_SPEC.md`, `09_EVENT_MODEL.md`, `05_SECURITY_REQUIREMENTS.md`, `07_IMPLEMENTATION_PHASES.md`, `06_ACCEPTANCE_TESTS.md` (Fase 0 não consta neles: é regida por `docs/process/PHASE_0_CHECKLIST.md`, conforme `fase_0.md:5`), `PHASE_0_CHECKLIST.md`, `WORKFLOW.md`, `CLAUDE.md`.

## VEREDITO: FAIL

## FINDINGS

### BLOCKER

- **[B1] O hook do auditor não bloqueia escrita deliberada — item 4 da DoD falha nas duas direções, e a fase está sendo fechada assim mesmo**
  Arquivo: `user-scope/hooks/readonly_bash.py:11-55` (allowlist textual restaurada pela reversão do P23)
  Requisito violado: `docs/process/PHASE_0_CHECKLIST.md:111` (DoD item 4); `docs/process/WORKFLOW.md:53` ("O agente não recebe ferramentas `Write`/`Edit`. Bash passa por allowlist. Essa combinação evita escrita deliberada"); `CLAUDE.md` §Fluxo por fase ("Uma fase só está concluída quando **todos** os itens da Definition of Done passam").
  Evidência — medida nesta sessão, não inferida:
  - executei `git branch -D __audit_probe_inexistente__`: **o hook liberou** e o comando rodou (`error: branch '...' not found`, rc=1 do git, não rc=2 do hook). É comando de escrita no ref store **compartilhado com o worktree principal**.
  - `python scripts/phase0_negative_tests.py` sai **0** e imprime, ele próprio: `DEFEITOS ABERTOS ... 9 leituras legitimas bloqueadas e 10 escritas nao bloqueadas`, listando `find -delete`, `find -fprint0 ../../…`, `pytest --junitxml`, `python -m pytest --junitxml`, `ruff --output-file`, `mypy --junit-xml`, `eslint -o`, `tsc --outFile`, `git branch -D`.
  - na direção inversa, três bloqueios indevidos ocorreram **nesta auditoria**: `git ls-tree`, `... >/dev/null`, e `grep -n "A\|B"` (o `|` entre aspas parte o segmento).
  Por que é blocker: os alvos aceitam travessia `../../`, então a escrita alcança o **worktree principal** — `CLAUDE.md`, `tools/`, `.claude/`, `docs/spec/`. A garantia de que "o auditor não corrige" (`WORKFLOW.md:40`) deixa de ser mecânica e passa a ser boa vontade do modelo. Este é o mesmo B1 da sétima rodada, com a direção do erro reintroduzida pela reversão; e com o item 4 aberto (e o 7 também, §5:315), a Fase 0 não satisfaz a condição de conclusão do `CLAUDE.md`, apesar de os itens 9–13 estarem marcados ✅ e a tag `spec-v1.0` constar como publicada por atestação (`fase_0.md:317-321`).

### HIGH

- **[H1] `P32` está marcada FECHADA descrevendo um desenho que a reversão desfez — inclusive o curinga `.claude/hooks/*.py` que o próprio H1 da sétima rodada mandou remover**
  Arquivo: `docs/progress/fase_0.md:1013` ("Status: FECHADA"), `:1023-1024` (default-deny de redirecionamento e de flags), `:1028` ("Os hooks passaram a ser allowlistados por **nome explícito**"), `:1038` (`git tag` com probes nas duas direções), `:1044` ("São 32 probes agora") vs. `user-scope/hooks/readonly_bash.py:36`
  Requisito violado: `docs/process/WORKFLOW.md:80-86` §"Por que o auditor não mora no repositório" (integridade: o commit auditado não pode definir o comportamento do auditor); `PHASE_0_CHECKLIST.md:111`.
  Evidência: em `readonly_bash.py:36` a allowlist é de novo `python (?:~/|\$HOME/)?\.claude/hooks/[A-Za-z0-9_.-]+\.py` — **curinga**, não nome explícito. Nenhum default-deny de flags existe no arquivo. `git tag` **não** está em `ALLOWED:16` (o harness confirma: `AINDA BLOQUEADO: git tag listando`), ao contrário do que `:1038` afirma. O harness executa 29 probes de hook + 19 afirmações de defeito, não 32.
  Por que é HIGH: o P23 foi explicitamente reaberto (`:838-848`), mas o P32 continua como FECHADA descrevendo correções que não existem no código. Hoje `.claude/hooks/` contém só guardas, então não há exploração viva — mas qualquer commit futuro pode acrescentar `.claude/hooks/qualquer.py` que escreve, e o auditor está pré-autorizado a executá-lo. É a categoria que o próprio registro batizou em P10: documentação que sobrevive à correção e orienta a próxima pessoa a desfazê-la.

- **[H2] A evidência do item 4 no quadro da DoD subdeclara o que foi medido e omite inteiramente a direção da escrita**
  Arquivo: `docs/progress/fase_0.md:312`
  Requisito violado: `PHASE_0_CHECKLIST.md:111`; `CLAUDE.md` §Quatro camadas de verdade (avaliação registrada acima da evidência disponível).
  Evidência: a célula diz "⚠️ parcial — H4 corrigido, mas **cinco** modos de bloqueio indevido persistem (§6 P23)". O harness do mesmo commit mede **9** falsos bloqueios e **10** escritas não bloqueadas, e `:848` registra os quatro números corretos. O quadro da DoD — a tabela que alguém lê para decidir se a fase fecha — não menciona que a metade "bloqueia escrita deliberada" do item 4 também falha.
  Por que é HIGH: é exatamente a classe que este commit declara ter fechado no P33 ("o registro guardava evidência falsa para o item 5 da DoD"), reintroduzida no item vizinho no mesmo commit.

- **[H3] Item 15 marcado ✅ com evidência de quatro rodadas, enquanto o mesmo arquivo registra as rodadas 7 e 8 como FAIL**
  Arquivo: `docs/progress/fase_0.md:332` vs `:142` e `:150`
  Requisito violado: `docs/process/WORKFLOW.md:35-40` §Ciclo por fase (auditoria de checkpoint; qualquer BLOCKER é FAIL); `CLAUDE.md` §Fluxo por fase.
  Evidência: `:332` diz "Status ✅ **com ressalva** — quatro auditorias: FAIL; PASS...; PASS...; e **PASS sem BLOCKER e sem HIGH**". As seções `:142` e `:150`, acrescentadas por este mesmo commit, registram sétima rodada **FAIL, 1 BLOCKER** e oitava rodada **FAIL**. `docs/progress/audit_log.jsonl` confirma `"verdict": "FAIL"` nas três últimas linhas. Esta nona rodada também é FAIL.
  Por que é HIGH: o gate que autoriza fechar a fase está declarado satisfeito por uma leitura congelada em `470ced8`+2 rodadas atrás. Quem consultar só a tabela da DoD conclui que o checkpoint passou.

### MEDIUM

- **[M1] `.claude/hooks/log_audit.py` foi removido, mas continua listado como entregável da Fase 0**
  Arquivo: `docs/process/PHASE_0_CHECKLIST.md:10` vs remoção em `.claude/hooks/log_audit.py` (−180) e `.claude/settings.json` (bloco `SubagentStop` removido)
  Requisito violado: `PHASE_0_CHECKLIST.md:5-19` §Entregáveis.
  Evidência: a linha 10 exige `.claude/hooks/` com `check_architecture.py`, `scenario_scope.py`, `scenario_bash.py`, **`log_audit.py`**. `git ls-files` mostra os três primeiros. A justificativa da remoção está em `fase_0.md:094` (P11 reaberta) e é tecnicamente sólida — o `SubagentStop` nunca dispara para agente de topo —, mas o documento de entregáveis não foi atualizado, então o checklist descreve uma árvore que não existe.

- **[M2] §5 declara os itens 9–13 fechados e a tag publicada; §"Estado do repositório" do mesmo arquivo diz "sem push, sem tag" e "`finalize_phase0.sh` não foi executado"**
  Arquivo: `docs/progress/fase_0.md:317-321` vs `:338` e `:356`
  Requisito violado: `PHASE_0_CHECKLIST.md:116-120` §DoD 9–13; `07_IMPLEMENTATION_PHASES.md:229` (registro de progresso deve refletir o estado).
  Evidência: `:317-321` marca 9, 10, 11, 12 e 13 com ✅ "por ATESTAÇÃO DO OPERADOR"; `:338` diz "`main`, working tree limpo, remoto `origin` configurado, **sem push**, sem tag"; `:356` diz "`finalize_phase0.sh` **não** foi executado". As duas afirmações não podem ser verdadeiras ao mesmo tempo. Não consigo arbitrar de dentro do worktree (ver §"O que não consegui verificar").

- **[M3] Referência cruzada errada na justificativa da atestação**
  Arquivo: `docs/progress/fase_0.md:328` ("Ver §6 **P30** para a constatação estrutural") vs `:953` (P30 é uma linha da revisão de referências cruzadas sobre `02_DOMAIN_ACADEMUS.md:126`) e `:979` (a constatação estrutural é **P34**)
  Requisito violado: `07_IMPLEMENTATION_PHASES.md:229`.
  Evidência: literal, nas três linhas citadas. Notável porque P28–P31 são precisamente o achado "referência aponta para a seção errada".

- **[M4] Duas capturas da mesma sessão viraram dois registros de rodada; o índice narrativo cita só uma, e a mensagem do commit contradiz o registro**
  Arquivo: `docs/progress/audit_20260814T030754Z.md`, `docs/progress/audit_20260814T032039Z.md`, `docs/progress/audit_log.jsonl` (2 últimas linhas), `fase_0.md:152`
  Requisito violado: `PHASE_0_CHECKLIST.md` §Definition of Done (registro como evidência); `07_IMPLEMENTATION_PHASES.md:229`.
  Evidência: os dois arquivos têm `session_id: ab69a491-070e-43e6-ba17-0e13252f99f9` e `head_sha: 470ced8…` — mesma rodada, capturada duas vezes, com conteúdos diferentes (131 e 161 linhas). `fase_0.md:152` cita apenas `audit_20260814T030754Z.md`; o `032039Z` entra no repositório sem menção no registro. A mensagem do commit `30f80c2` diz "capturado automaticamente", enquanto a linha correspondente do log diz `"recovered": true`.

### LOW

- **[L1] Contagem de probes internamente inconsistente na mesma pendência**
  `docs/progress/fase_0.md:852` ("7 probes de leitura legítima e 13 de escrita deliberada") vs `:848` ("5 leituras liberadas, 24 escritas bloqueadas") e vs a saída real do harness (5 e 24). Requisito: `07_IMPLEMENTATION_PHASES.md:229`.

- **[L2] `readonly_bash.py:59-62` é fail-open**: stdin que não parseia como JSON devolve `0`. Já registrado como observação em `fase_0.md:1042` (mesma classe do O1 de `check_architecture.py`), permanece aberto. Requisito: `PHASE_0_CHECKLIST.md:111` (o guarda que não interpreta a entrada deve negar).

- **[L3] `check_security_constraints.py:36` varre `range-core`, `domains`, `tools`, `scripts` — não varre `.claude/` nem `user-scope/`.** Os guardas e o hook do auditor ficam fora do verificador de segurança (P9/P24, abertas). Requisito: `05_SECURITY_REQUIREMENTS.md` §1 c/c `PHASE_0_CHECKLIST.md:30`.

- **[L4] Nenhum verificador cobre `.sh`.** `bootstrap.sh`, `finalize_phase0.sh` e `scripts/start_checkpoint_audit.sh` (que executa `rm -rf "$WT"` em `:57`) não passam por AST nem heurística. Requisito: `PHASE_0_CHECKLIST.md:30`.

- **[L5] `.env.example` continua ausente** embora `README_FIRST.md` e `05_SECURITY_REQUIREMENTS.md:108` o tratem como versionado (P17, aberta). Requisito: `05_SECURITY_REQUIREMENTS.md` §6.

- **[L6] O probe de dados sintéticos planta IP roteável e domínio real** (`scripts/phase0_negative_tests.py:431`: `{"src":"8.8.8.8","domain":"google.com"}` → `scenarios/_phase0_probe/fixture.jsonl`). É inerente ao teste negativo e a limpeza funcionou aqui (`git status` limpo após a execução), mas uma interrupção deixa o arquivo na árvore — resíduo em faixa proibida por `05_SECURITY_REQUIREMENTS.md` §3, num caminho de `scenarios/`.

## DoD — item a item

Referência: `docs/process/PHASE_0_CHECKLIST.md:106-121`.

| Item | Status | Evidência |
|---|---|---|
| 1 — Seis verificadores liberam árvore limpa | ✅ | Executei os seis: `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `check_security_constraints`, `check_synthetic_data`, `codegen.py --check` → todos `rc=0`; `git status --porcelain` vazio depois (não escrevem) |
| 2 — Os seis detectam as violações externas do harness | ✅ | `python scripts/phase0_negative_tests.py` → 13 probes de verificador, todos `rc=1` **com citação do arquivo plantado**; exit 0; árvore limpa após |
| 3 — Hook bloqueia import de `domains/`, edição de `docs/spec/` e literal de flag | ✅ | Três smoke tests executados por mim: `range-core/x.py` com `from domains.academus` → `exit=2`; `docs/spec/00_MASTER_SPEC.md` → `exit=2`; literal `academus.enrollment_offline` em `domains/academus/api/x.py` → `exit=2` |
| 4 — Hook do auditor bloqueia escrita e libera leitura | ⛔ **FALHA nas duas direções** | `git branch -D` **executou** (B1); harness afirma 10 escritas não bloqueadas e 9 leituras bloqueadas; 3 falsos bloqueios sofridos nesta sessão |
| 5 — Hook do `scenario-designer` bloqueia Write fora de `scenarios/` e Bash fora da allowlist | ⚠️ metade verificada | `scenario_scope.py` com `range-core/nope.py` → `exit=2` (executado por mim). A metade `scenario_bash.py` **não** foi reexecutada nesta rodada |
| 6 — `ground_truth.yaml`/`GM_NOTES.md` fora do `.gitignore` | ✅ | `.gitignore:39-41` — aparecem só em comentário que documenta o versionamento deliberado |
| 7 — `.env`/secrets negados em `.claude/settings.json` | ⚠️ parcial | `settings.json:6-13` nega `Read`/`Edit` de `.env`, `.env.*`, `secrets/**`; **`Write` continua sem deny** (P13/P19 abertas) |
| 8 — Auto Mode desabilitado | ✅ | `settings.json:4-5`: `"defaultMode": "default"`, `"disableAutoMode": "disable"` |
| 9 — Primeiro push com `arquitetura` e `seguranca` verdes | ❔ não verificável | Declaração do operador (`fase_0.md:317`), contradita por `:338`/`:356` (M2) |
| 10 — PR descartável: `spec_freeze` reprova spec+código | ❔ não verificável | Idem. O gate existe e é coerente na leitura (`invariants.yml:110-118`), mas nunca o vi executar |
| 11 — PR descartável: título `spec-change:` exigido | ❔ não verificável | Idem (`invariants.yml:120-123`) |
| 12 — Branch protection com os três contexts | ❔ não verificável | Idem |
| 13 — `spec-v1.0` criada depois de CI e proteção | ❔ não verificável | Idem; `git for-each-ref` está fora da allowlist na prática (bloqueio por outro segmento do comando) |
| 14 — `/doctor` sem apontamentos | ❔ não verificável | Fora do alcance de um worktree |
| 15 — Auditoria de checkpoint com veredito PASS (`WORKFLOW.md`) | ⛔ | Rodadas 7 e 8 = FAIL (`audit_log.jsonl`); esta rodada = FAIL. `fase_0.md:332` afirma ✅ com base em quatro rodadas (H3) |

## TESTES QUE NÃO PROVAM O REQUISITO

1. **`scripts/phase0_negative_tests.py` sai 0 declarando que o requisito do item 4 da DoD está quebrado.** `expect_hook_allows_known_hole` (`:301-316`) afirma que 10 escritas deliberadas **não** são bloqueadas e considera isso sucesso; `expect_hook_blocks_known_defect` (`:283-298`) faz o simétrico para 9 leituras legítimas. O desenho é honesto — o probe reprova se o comportamento mudar, e isso é melhor que esconder —, mas a consequência precisa ficar dita: **`invariants.yml:43-44` roda esse harness dentro do job `arquitetura`, que é context obrigatório de branch protection; logo o CI fica verde com o guarda do auditor comprovadamente furado.** Verde do harness não é evidência do item 4, e o quadro da DoD não avisa isso (H2).
2. **`hook_copies_in_sync` degrada para aviso quando a cópia instalada não existe** (`:319-326`). Em CI o probe imprime "AVISO" e segue. É a decisão certa para CI, mas significa que o único probe que compara fonte versionada × cópia efetivamente executada só vale na máquina do operador — o gate real nunca o exerce.
3. **`verdict_probes` prova `detect_verdict`/`select_report` com strings sintéticas** (`:239-280`) e não com nenhum transcript real. A correção do M2 (veredito extraído da linha de veredito) está provada como função pura; a cadeia `find_transcript → agent_text_blocks → select_report` sobre um JSONL de verdade não tem probe. Foi exatamente na junção não testada (código após a sessão) que a captura falhou na sétima rodada.

## O QUE EU NÃO CONSEGUI VERIFICAR

- **Itens 9 a 13 da DoD.** `gh` está negado (`DENIED_ANYWHERE`, acesso de rede) e execução de CI não deixa rastro no checkout. Não consigo distinguir entre "o operador executou `finalize_phase0.sh`, o CI ficou verde, a proteção foi aplicada e a tag existe" e "não". O registro afirma as duas coisas em seções diferentes (M2). Isto é a terceira camada de verdade (declaração), não a segunda — e concordo com o P34 nesse ponto: registrar atestação rotulada é correto; o que não está correto é a coexistência com `:338`/`:356`.
- **Existência de tags e refs remotos.** Tentei `git for-each-ref refs/tags refs/remotes`; o hook bloqueou o comando (por `2>&1`/pipe no mesmo segmento) e `git tag` não está na allowlist. Não sei se `spec-v1.0` existe.
- **A metade Bash do item 5** (`scenario_bash.py` distinguindo `git log --oneline` de `range-cli scenario validate <pack>`). Não reexecutei; aceito o registro corrigido em P33, sem confirmá-lo.
- **`/doctor`** (item 14) — não é executável a partir do Bash do auditor.
- **Se os 10 buracos afirmados pelo harness alcançam de fato o worktree principal.** Confirmei apenas um caminho de escrita por execução real (`git branch -D`), e deliberadamente contra um nome inexistente. Não executei nenhum comando que criasse ou alterasse arquivo, aqui ou no worktree principal; a travessia `../../` está assumida a partir de `fase_0.md:844` e do relatório da sétima rodada, não medida por mim.
- **Cobertura dos verificadores sobre `range-core/` e `domains/` reais.** Esses diretórios não existem nesta fase; toda a prova vem de probes plantados. É limitação estrutural da Fase 0, não defeito deste commit — mas significa que as isenções de caminho (P7) só serão exercitadas de verdade na Fase 1.

Uma nota de método, porque afeta como este relatório deve ser lido: **três comandos meus foram bloqueados pelo próprio guarda que estou auditando** (`git ls-tree`, redirecionamento para `/dev/null`, `grep` com `|` entre aspas). Cada bloqueio desses estreita o que consegui medir e empurra a auditoria para leitura de código — que é a degradação que o H4 da primeira rodada classificou como HIGH. O item 4 não é um item de conforto do auditor: é o item que determina quanto das outras conclusões é medição e quanto é inferência.
