# Auditoria de checkpoint - 2026-08-14T22:41:56.046730+00:00

- fase: **1**
- commit auditado: `59580e6eb8706ed0bfbe1f73fc3b93c0dbd87156`
- session_id: `13b458e4-0b12-4c4b-8729-42a23f962b9b`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

```text
# AUDITORIA — FASE 1

## VEREDITO: FAIL

Commit auditado: `59580e6eb8706ed0bfbe1f73fc3b93c0dbd87156` (HEAD confirmado no worktree).

## FINDINGS

### BLOCKER

- [B1] O contrato do envelope proíbe a `simulation_epoch` inicial que a spec declara normativa
  Arquivo: `contracts/events.schema.yaml:72` (regra) e `contracts/events.schema.yaml:357-366` (fixture)
  Requisito violado: `09_EVENT_MODEL.md` §3 (diagrama `epoch 0` → `epoch 1`) e `06_ACCEPTANCE_TESTS.md` T3, linha 23
  Evidência: o schema declara `simulation_epoch: {type: integer, minimum: 1}`. O exemplo negativo de linha 357 é `reason: 'simulation_epoch zero: epoch comeca em 1'`, `rejected_by: schema`, com `simulation_epoch: 0` na linha 366 — isto é, o contrato exige ativamente a recusa de epoch 0. A spec diz o oposto em dois lugares normativos: `09` §3 abre a linha temporal em `epoch 0` e só depois do rollback vai para `epoch 1`; `06` T3 exige literalmente "Evento de `participant_action` gravado na **epoch 0** continua legível após rollback, marcado com sua epoch". A única base textual para `minimum: 1` é o valor `"simulation_epoch": 1` do exemplo de envelope em `09` §1 — um valor de instância ilustrativo, não um piso declarado.
  Por que é blocker: o event store da Fase 2 é construído sobre este contrato. Com `minimum: 1`, todo evento da epoch inicial é rejeitado na validação, e T3 — critério de aceitação da Fase 2 — torna-se insatisfazível sem alterar o contrato, ou seja, sem alterar a `schema_version` que `04` §4 proíbe mexer com semântica. Agrava: a fixture negativa está entre os 53 negativos que o `docs/progress/fase_1.md` §5 usa como evidência do item 1 da DoD. Ela passa, e prova o contrário do requisito — exatamente a classe "teste que passa sem provar o requisito". Agrava ainda: `CLAUDE.md` ("Autoridade") manda **parar e perguntar** diante de conflito entre documentos não-master; aqui o conflito aparente entre o exemplo de `09` §1 e o texto de `09` §3 / `06` T3 foi resolvido por inferência e congelado em fixture executável.

- [B2] Item 8 da DoD não passa — `RANDOM_SEED` não é lido por código algum
  Arquivo: `.env.example:15` (única ocorrência funcional); nenhum consumidor em `range-core/` ou `domains/`
  Requisito violado: `07_IMPLEMENTATION_PHASES.md:61` — "- [ ] `RANDOM_SEED` lido de `.env`"
  Evidência: varredura por `RANDOM_SEED` em todo o repositório retorna apenas a declaração em `.env.example:15`, menções em documentação/spec, e `scripts/phase0_negative_tests.py:167`, que apenas faz `grep RANDOM_SEED .env.example` — verifica a presença da linha, não a leitura pelo código. Nenhum módulo lê a variável. O próprio `docs/progress/fase_1.md:169` marca o item como "⚠️ parcial — declarado em `.env.example`; nenhum código o lê ainda", e §5 conclui "A fase não está concluída."
  Por que é blocker: `CLAUDE.md` ("Fluxo por fase") define DoD como binária — "Uma fase só está concluída quando **todos** os itens da Definition of Done passam". A ENTRY da Fase 2 (`07` §Fase 2, linha 67) exige "Fase 1 completa". Com o item 8 aberto, a Fase 1 não pode ser declarada concluída nem liberar a Fase 2. Registro de mérito, que não altera o veredito: a lacuna foi divulgada honestamente, e a recusa do operador em escrever um consumidor artificial só para fechar o item (§5, P1-2) está correta — fechar DoD criando o consumidor não é evidência.

### HIGH

- [H1] O objeto `correlation` do envelope normativo está ausente do schema, e `fact_id` foi realocado para o topo
  Arquivo: `contracts/events.schema.yaml:75-76`; ausência verificada por busca — `correlation`, `causation_id`, `scenario_id` e `inject_id` não têm nenhuma ocorrência no arquivo
  Requisito violado: `09_EVENT_MODEL.md` §1 (envelope universal) e `00_MASTER_SPEC.md` §5.4 ("Um envelope de evento")
  Evidência: o envelope normativo de `09` §1 declara `"correlation": { "scenario_id", "inject_id", "causation_id", "fact_id" }`. O schema não declara `correlation` em nenhum nível e coloca `fact_id` como propriedade de topo (linha 75), com o comentário da linha 74 citando "09 secao 1" — seção onde `fact_id` está dentro de `correlation`, não no topo. O exemplo positivo (linhas 264-274) também não traz `correlation`. Como `additionalProperties` fica deliberadamente aberto (linhas 92-94), um evento com `correlation` passa, porém sem validação alguma de forma.
  Por que é high, e não blocker: nada é rejeitado indevidamente hoje e nenhum invariante de segurança ou de camada de verdade é violado. Mas a Fase 2 constrói o event store e a taxonomia de rollback sobre este contrato, e `causation_id` e `inject_id` são o que sustenta cadeia de causação e correlação de branch; quatro campos declarados na spec ficam sem contrato, e o único que sobrevive mudou de lugar.

### MEDIUM

- [M1] `check_event_envelope.py` varre apenas Python, enquanto os outros dois verificadores de código varrem também TypeScript
  Arquivo: `tools/check_event_envelope.py:43` (`PYTHON_SUFFIXES = (".py",)`) e `:92`
  Requisito violado: `06_ACCEPTANCE_TESTS.md` T1, linha 10 ("Nenhum evento emitido pelo core ou pelo adapter carrega `objective_ids`")
  Evidência: `check_core_boundary.py:40,191` varre `.ts/.tsx/.js/.jsx/.mts/.cts`, e `check_contract_literals.py:160` idem, com justificativa explícita no docstring (linhas 21-24) de que cobrir só Python deixaria passar literal em TSX. O verificador do invariante 4 não tem essa cobertura: `iter_files(REPO_ROOT, SCANNED_DIRS, PYTHON_SUFFIXES)`. Um emissor em `range-core/web/` ou `domains/<adapter>/web/` que montasse envelope com `objective_ids` sairia rc=0.
  Por que não é maior: latente hoje — não existe nenhum arquivo TS/TSX na árvore, e o schema (`events.schema.yaml:89-90`) recusa `objective_ids` no topo do documento. É a mesma classe do B1 da primeira auditoria da Fase 0 (verificador que sai zero sobre território que não varre), e vira exposição real na Fase 4, quando nasce o front-end.

### LOW

- [L1] A proibição de `objective_ids` no schema alcança apenas a raiz do documento
  Arquivo: `contracts/events.schema.yaml:89-90` (`not: required: [objective_ids]`), combinado com `additionalProperties` aberto (linhas 92-94)
  Requisito violado: `09_EVENT_MODEL.md` §1.2
  Evidência: `not: {required: [objective_ids]}` avalia apenas o objeto raiz. Um envelope com `payload: {objective_ids: [OBJ-03]}` valida sem erro. A fixture negativa da linha 322-334 coloca a chave no topo, então nunca exercita esse caminho.
  Por que é low: `tools/check_event_envelope.py` pega o caso no código emissor, por AST, em qualquer profundidade (`_hits`, linhas 74-87), que é onde o invariante é de fato aplicado. A lacuna vale para documento autorado à mão, não para evento emitido.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Os seis contratos existem e validam exemplos positivos e negativos | ⚠️ PARCIAL / NÃO VERIFICADO | Os seis arquivos existem (`contracts/`: `state_flags`, `events`, `scenario.schema.v2`, `ground_truth`, `objectives`, `evidence`). `scripts/check_contract_examples.py` é executor real e rigoroso: exige correspondência com `rejected_by`, e para `rejected_by: x-aurora-*` exige que o schema **aceite** e que a regra nomeada seja a única a disparar (docstring linhas 19-25). **Não consegui executá-lo** (ver limitações). E o conteúdo tem defeito: uma das fixtures negativas contradiz a spec — B1 |
| 2 — Constantes tipadas em Python e TypeScript para flags e event types | ✅ | `contracts/generated/events.py` e `.ts`: 32 constantes cada, idênticas em nome, valor e ordem, e o conjunto bate exatamente com o catálogo de `09` §4.1 (3+3+17+3+6=32), inclusive `separate_incident_declared`. `domains/academus/generated/flags.{py,ts}` e `domains/prontus/generated/flags.{py,ts}` presentes; 12 flags em `domains/academus/flags.yaml` |
| 3 — Teste de fronteira core/adapter passa (por AST) | ✅ quanto ao mecanismo / NÃO EXECUTADO | `tools/check_core_boundary.py` cobre import direto, alias, import relativo que escapa (`_relative_escapes_into_domains`, :52-64), `importlib.import_module` e `__import__` com literal (:118-130), mais varredura lexical de TS. Nenhum import de `domains/` encontrado em `range-core/` |
| 4 — `event_type` fora do catálogo é rejeitado | ✅ | Catálogo fechado por `enum` em `$defs` prefixados `event_type_` (`events.schema.yaml:196-244`); fixture negativa `audit_query_perfomed` (:285-296); `tools/_common.py::load_declared_event_types` lê os mesmos `enum` que geram as constantes — sem segunda cópia da lista |
| 5 — Os seis verificadores liberam árvore limpa e cada um falha contra violação plantada | ⚠️ NÃO VERIFICADO | Os seis existem em `tools/` e estão ligados ao CI (`.github/workflows/invariants.yml:17-44`, `:138-142`). O harness `scripts/phase0_negative_tests.py` existe. **Nenhuma execução foi possível** |
| 6 — `codegen.py --check` detecta dessincronia e não escreve | ⚠️ NÃO VERIFICADO | Invocado em `invariants.yml:29-30` com comentário correto quanto à vacuidade de `git diff` depois dele (`06` T2, linha 17). Não executado |
| 7 — `docker compose up` sobe Postgres e Redis | ⚠️ NÃO VERIFICADO | `docker-compose.yml` presente, imagens pinadas por digest, `POSTGRES_PASSWORD` sem default. A verificação relatada em `fase_1.md` P1-1 é execução do operador, fora do meu alcance |
| 8 — `RANDOM_SEED` lido de `.env` | ❌ FAIL | B2 — nenhum consumidor no código |

## TESTES QUE NÃO PROVAM O REQUISITO

- `contracts/events.schema.yaml:357-366` — fixture negativa "simulation_epoch zero: epoch comeca em 1". Passa, e prova o **oposto** do que `09` §3 e `06` T3 exigem. Está contabilizada entre os 53 negativos que sustentam o item 1 da DoD. É o B1.
- `scripts/phase0_negative_tests.py:167` — `("grep no .env.example", "grep RANDOM_SEED .env.example")` prova que a string existe no arquivo de exemplo, não que a variável seja lida. Não sustenta o item 8 da DoD, e o registro de fase corretamente não a usa para isso.
- Nenhum `skip`/`xfail` encontrado — não há suíte pytest no repositório. Os "testes" da fase são os verificadores, o harness negativo e o executor de exemplos.

Registro em contrário, porque é o achado que eu esperava e não encontrei: o executor `scripts/check_contract_examples.py` **não** aceita "falhou, logo está provado". Ele exige que `rejected_by: schema` seja recusado pelo schema e que `rejected_by: x-aurora-*` seja **aceito** pelo schema e recusado só pela regra nomeada, sendo ela a única a disparar. Isso é precisamente a defesa contra fixture mentirosa, e está implementada, não só descrita. O invariante 4 também está guardado em duas camadas independentes (schema e AST), sem `objective_ids` em nenhum caminho de emissão.

## O QUE EU NÃO CONSEGUI VERIFICAR

**Não executei nada.** Esta é a limitação dominante desta auditoria e ela é severa. Toda tentativa de rodar `python tools/check_core_boundary.py`, `check_contract_literals.py`, `check_event_envelope.py`, `check_security_constraints.py`, `check_synthetic_data.py`, `codegen.py --check`, `scripts/phase0_negative_tests.py`, `scripts/check_contract_examples.py` e `check_contract_examples_probes.py` foi recusada pela camada de permissão ("This command requires approval"), mesmo estando na allowlist do hook `readonly_bash.py`. Em consequência:

- não confirmei nenhum código de saída, nem na árvore limpa nem contra violação plantada;
- não confirmei os números declarados em `fase_1.md` §4.1 e na mensagem de commit — 112 provas do harness, 9 positivos, 53 negativos, 6 eixos de probe do executor. Li o código do executor e ele me parece honesto, mas isso é leitura, não prova;
- não confirmei que `codegen.py --check` de fato não escreve (o item exige observar o comportamento, não ler a intenção), nem que as constantes em disco batem com o que o gerador produz hoje — verifiquei apenas que Python e TypeScript são consistentes **entre si** e com o catálogo do schema, por leitura;
- não verifiquei `docker compose up`, healthchecks, bind em loopback nem `alembic` executável. O item 7 e a P1-9 repousam inteiramente em relato do operador em `fase_1.md`;
- não validei os exemplos dos contratos contra `jsonschema` — não sei se os 9 positivos validam nem se os 53 negativos são recusados pela camada declarada. B1 foi encontrado por leitura contra a spec, não por execução; podem existir outras divergências da mesma classe nas fixtures que eu não li integralmente (li `events.schema.yaml` por inteiro; `ground_truth`, `scenario.schema.v2`, `objectives`, `evidence` e `state_flags` só por amostragem dirigida).

Outros limites: não avaliei a corretude do parser YAML restrito de `tools/_common.py` (o `_parse_scalar` citado na D4 como defeito dormente corrigido não foi reexaminado); não verifiquei o conteúdo de `.env`, por proibição de `CLAUDE.md`; e não avaliei a aplicação da branch protection no GitHub (P1-10), que é ação fora do repositório.

**Recomendação de processo:** os itens 1, 5 e 6 da DoD não devem ser considerados fechados com base nesta auditoria. Eles exigem reexecução observada — e, dado o B1, a reexecução deve vir **depois** da correção da regra de `simulation_epoch`, porque hoje a suíte de exemplos passa afirmando uma regra que contraria a spec.
```
