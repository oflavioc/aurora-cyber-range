# Auditoria de checkpoint - 2026-08-15T01:27:53.771909+00:00

- fase: **1**
- commit auditado: `21c695d3622e38be47d4da115196d524a5932354`
- session_id: `76d498aa-7c99-41ed-84ad-a68e62f9bc72`
- modo: `interactive`
- veredito detectado: **FAIL**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

Auditoria concluída. Segue o relatório no formato obrigatório.

```text
# AUDITORIA — FASE 1
```

## VEREDITO: **FAIL**

---

## FINDINGS

### BLOCKER

**[B1] `evidence_release` do inject: contrato perde `source` e `window` da spec**
- Arquivo: `contracts/scenario.schema.v2.yaml:268-274`
- Requisito violado: `04_SCENARIO_SCHEMA.md` §5 (bloco `evidence_release`) e `08_EVIDENCE_SIMULATOR.md` §5
- Evidência: a spec declara, em dois documentos independentes, uma sequência de mapeamentos `- source: vpn / window: "T-9d → T-8d"`. O contrato declara `items: {type: string, minLength: 1}`. O exemplo normativo da spec é **recusado** pelo contrato; e a forma aceita (`[vpn]`) não consegue expressar a janela. Nenhum exemplo positivo do contrato exercita o campo — nenhuma fixture, positiva ou negativa, o menciona (`grep evidence_release contracts/` retorna apenas a linha 268). Não há comentário, `x-aurora-linter-rule` ou decisão registrada em `docs/progress/fase_1.md` que justifique o desvio, ao contrário de praticamente toda outra escolha do arquivo.
- Por que é blocker: `evidence.schema.yaml:106-115` exige `window` para `delivery_mode: released_by_inject`; a origem dessa janela é justamente o `evidence_release` do inject, que aqui deixou de carregá-la. Além disso, corrigir depois é proibido: `04_SCENARIO_SCHEMA.md` §4 — *"Nunca alterar semântica de campo dentro da mesma `schema_version`"*. Consertar agora custa uma linha; consertar na Fase 7 exige `schema_version: 3` e migração.

**[B2] `reveals` declarado como objeto único, sendo lista na spec**
- Arquivo: `contracts/scenario.schema.v2.yaml:276-277` (`reveals: $ref '#/$defs/reveals'`) e `:299-311` (`type: object`)
- Requisito violado: `04_SCENARIO_SCHEMA.md` §5
- Evidência: a spec declara `reveals:` como sequência (`- persona: reitoria / content: ... / confidence: low`), permitindo revelação diferenciada por persona — que é a Consequência Normativa 3 de `00_MASTER_SPEC.md` §3 (assimetria de informação é desenho). O contrato aceita **um** objeto. Instância conforme a spec falha em `type: object`. Nenhum exemplo positivo exercita o campo.
- Por que é blocker: um inject só pode revelar para uma persona. A assimetria multi-persona, que o pack `ransomware-universidade` precisa (sete personas em `04` §2), fica inexpressável — e a correção posterior é mudança de semântica de campo dentro da `schema_version: 2`, vedada por `04` §4.

**[B3] `note_to_facilitator` do braço de branch ausente, com `additionalProperties: false`**
- Arquivo: `contracts/scenario.schema.v2.yaml:450-464`
- Requisito violado: `04_SCENARIO_SCHEMA.md` §6
- Evidência: o exemplo normativo de branching traz `note_to_facilitator: "Alcance reduzido; dados já exfiltrados permanecem."` dentro do braço `evaluate[]`. As propriedades declaradas em `branch_arm` são exatamente `id`, `next`, `when`, `default`, com `additionalProperties: false` na linha 453. O `branches.yaml` escrito conforme o §6 da spec é recusado pelo contrato. O exemplo positivo do próprio contrato (`:697-713`) omite o campo, então nada falha.
- Por que é blocker: é a mesma classe do B1/B2 e o mesmo custo de correção tardia (`04` §4). Aqui o efeito é oposto ao de B1 — não perda silenciosa, mas recusa de pack legítimo — e o caminho de menor resistência na Fase 7 seria editar a spec para acomodar o contrato, que `CLAUDE.md` proíbe.

---

### HIGH

**[H1] `domains/*/flags.yaml` nunca é validado contra `state_flags.schema.yaml`**
- Arquivo: `scripts/check_contract_examples.py:111-116`; `.github/workflows/invariants.yml:160-192`
- Requisito violado: `01_ARCHITECTURE.md` §5.2 — *"`domains/<adapter>/flags.yaml`, **validado contra** `contracts/state_flags.schema.yaml`"*
- Evidência: `build_registries()` abre os `flags.yaml` apenas para colher nomes e tipos; não os valida. Busca por `state_flags` em todo `*.py` da árvore retorna somente `check_contract_examples_probes.py:41,57` (que copia o arquivo para plantar defeito no *contrato*, não na instância). O job `contratos` já tem `jsonschema` instalado e valida apenas os exemplos embutidos nos contratos. Verifiquei manualmente que as 12 flags de `domains/academus/flags.yaml` e as 2 de `domains/prontus/flags.yaml` estão conformes — mas por leitura, não por mecanismo.
- Por que é high: o único artefato real da fase governado por esse contrato é o que ninguém valida. Um `category: disponibilidade` ou um `type: string` em `flags.yaml` sai rc=0 em todos os quatro jobs, e esse arquivo é entrada do `codegen.py` e do `x-aurora-effects-match-flag-types`. É a forma exata de "o mecanismo existe, a propriedade não" que o §1.3 de `fase_1.md` diz ter corrigido para os exemplos, repetida um nível abaixo.

**[H2] O único gate do item 1 da DoD não bloqueia merge**
- Arquivo: `docs/progress/fase_1.md:203` e `:555-557`; `docs/process/WORKFLOW.md` (tabela dos quatro contexts, alterada neste mesmo commit)
- Requisito violado: `docs/process/WORKFLOW.md` — *"a fase não deve ser declarada concluída até a proteção equivalente ser configurada e comprovada"*; e o próprio texto do commit: *"um gate que roda, reporta e **não bloqueia merge**"*
- Evidência: o commit candidato `21c695d` registra que `contratos` foi **removido** dos required status checks para destravar o impasse circular, e que *"um PR com fixture quebrada pode ser mergeado com ele vermelho"*. `contratos` é o job que executa `check_contract_examples.py` e `check_contract_examples_probes.py` — a prova inteira do item 1 da DoD.
- Por que é high: a fase fecha com sua principal verificação nova em estado consultivo. Não é BLOCKER porque o impasse está diagnosticado, afirmado e tem ordem de resolução declarada, e porque a prova roda (executei-a) — mas o item 5 do template de fase ("checklist binária") não está satisfeito enquanto o gate não gatear.

---

### MEDIUM

**[M1] `package_files.required` contradiz os pacotes "apenas manifesto" de `04` §9**
- Arquivo: `contracts/scenario.schema.v2.yaml:29-41`
- Requisito violado: `04_SCENARIO_SCHEMA.md` §9 — *"**Apenas manifesto, sem injects:** `vazamento-lgpd`, `pesquisa-comprometida`. Validam que o loader lida com pacote incompleto"*
- Evidência: o registro declara `ground_truth.yaml`, `objectives.yaml` e `injects.yaml` como **required**. Dois dos quatro pacotes que a spec manda entregar não têm nenhum dos três, por desenho.
- Por que importa: o loader da Fase 7 lê este registro. Ou ele recusa dois pacotes normativos, ou ignora o registro — e nesse caso o registro é prosa marcada como contrato.

**[M2] `metric_binding` não expressa os bindings normativos de `03` §1.3**
- Arquivo: `contracts/objectives.schema.yaml:158-169`
- Requisito violado: `03_EXERCISE_DESIGN.md` §1.3 (tabela de objetivos) e `00_MASTER_SPEC.md` §8 (identificadores em inglês)
- Evidência: o enum admite um valor escalar. A tabela normativa vincula OBJ-05 a `TTCD/TTCV` e OBJ-10 — declarado ali como **o objetivo-âncora** — a `TTIV − TTRV`. Nenhum dos dois é expressável. O enum ainda inclui `calibracao`, identificador em português, que não consta da lista de métricas de `03` §3.
- Por que importa: OBJ-10 é o objetivo para o qual, segundo a própria spec, "toda a estrutura de métricas pareadas existe". O contrato que deveria carregá-lo não o carrega.

**[M3] `pack_decision_options` é registro estruturalmente vazio**
- Arquivo: `scripts/check_contract_examples.py:126-134`; `contracts/scenario.schema.v2.yaml:535-542`
- Requisito violado: `04_SCENARIO_SCHEMA.md` §6.1 (folha `option` é condição permitida) e §6.2 (*"Todo `option` referenciado deve existir no `decision_point` indicado"*)
- Evidência: o registro é montado a partir de `decision_point.options[]` dos exemplos de inject. Nenhum dos seis injects do `x-aurora-document-examples` (`:659-695`) tem `decision_point`. Logo `pack_decision_options` é `set()`, e qualquer `- option: <x>` em `when` seria reprovado. A regra nunca foi exercitada nem por fixture positiva nem por negativa.
- Por que importa: a anotação existe, aparece na tabela de `contracts/README.md:54`, e não pode disparar corretamente contra nada. É regra que passa por estar inalcançável.

---

### LOW

**[L1]** `tools/check_event_envelope.py:91-115` — a varredura web é lexical por regex, e `01_ARCHITECTURE.md` §2 diz *"todos por AST ou leitura de contrato — nunca por grep"*. Está documentado no próprio arquivo, é deliberadamente conservador (falso positivo, nunca falso negativo) e a stdlib não traz parser de TypeScript. Registro pelo desvio literal, não pela consequência.

**[L2]** `contracts/README.md:128-130` afirma "seis eixos possíveis de fixture mentirosa"; `scripts/check_contract_examples_probes.py` executa **nove** (saída real). `docs/progress/fase_1.md:162` diz "60 negativos"; a execução real reporta **61**, e a linha 149 do mesmo documento diz 61. Documentação que sobreviveu à mudança e a contradiz — a linhagem P10/P15/P22 que o próprio repositório nomeia.

**[L3]** `contracts/evidence.schema.yaml:98-115` exige `window` apenas quando `delivery_mode: released_by_inject`, enquanto `08_EVIDENCE_SIMULATOR.md` §7 lista "janela temporal" entre o que o MANIFEST carrega para **cada arquivo**.

---

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1. Os seis contratos existem e validam exemplos positivos e negativos | ⚠️ **atende à letra, falha no fundo** | `check_contract_examples.py` rc=0: 6 contratos, 9 positivos, 61 negativos, cada um recusado pela camada que declara. Mas os contratos divergem da spec em B1, B2, B3 — exatamente nos campos que nenhum exemplo positivo exercita |
| 2. Constantes tipadas em Python e TypeScript para flags e event types | ✅ | `contracts/generated/events.{py,ts}` com 32 tipos (`Final[str]`, `tuple[str, ...]`); `domains/{academus,prontus}/generated/flags.{py,ts}` com 12+2 flags; `codegen.py --check` rc=0 |
| 3. Teste de fronteira core/adapter passa (por AST) | ✅ | `check_core_boundary.py` rc=0 na árvore limpa; rc=1 contra `range-core/_phase0_probe_bad.py` |
| 4. `event_type` fora do catálogo é rejeitado | ✅ | Catálogo fechado em `$defs/event_type_*` (32 tipos, batendo com `09` §4.1); fixture negativa `audit_query_perfomed` recusada; `check_contract_literals.py` rc=1 contra literal plantado e contra hook com tipo fora do catálogo |
| 5. Os seis verificadores liberam árvore limpa e cada um falha contra violação plantada | ✅ | Todos rc=0 na árvore; `phase0_negative_tests.py` rc=0 com "Todos os seis verificadores falharam contra probes independentes" — 15 probes de verificador, um por invariante mais os derivados |
| 6. `codegen.py --check` detecta dessincronia e não escreve | ✅ | `main()` só chama `write()` sob `--write`; `check()` compara em memória. Probes cobrem artefato ausente, divergente e de evento |
| 7. `docker compose up` sobe Postgres e Redis | ⚠️ **não verificável aqui** | `docker compose config` parseia e falha apenas na exigência deliberada de `POSTGRES_PASSWORD`; imagens pinadas por digest; bind em `127.0.0.1`. A execução real está afirmada em `fase_1.md:214-228`, não reproduzida por mim |
| ~~8~~. `RANDOM_SEED` lido de `.env` | ➡️ fora do escopo | Confirmei em `07_IMPLEMENTATION_PHASES.md:53-60` que o item não consta mais da DoD da Fase 1 — o `spec-change` foi de fato aplicado à spec, não apenas alegado |

---

## TESTES QUE NÃO PROVAM O REQUISITO

**1. O executor de exemplos prova consistência interna, nunca fidelidade à spec.** `scripts/check_contract_examples.py` valida cada contrato contra fixtures que vivem **dentro do próprio contrato**, escritas pelo mesmo autor, no mesmo commit. Não existe camada que compare campo declarado na spec com campo declarado no schema. B1, B2 e B3 são a demonstração empírica: três campos normativos de `04` §5/§6 estão errados ou ausentes, os três em regiões sem exemplo positivo, e a suíte sai rc=0 com mensagem de sucesso. É a mesma classe do `simulation_epoch: minimum: 1` da rodada anterior — só que aquele foi achado por leitura da spec, não pela suíte, e nada mudou nesse aspecto desde então.

**2. `check_contract_examples_probes.py` prova o que não é a dúvida.** Os nove eixos plantam *fixture mentirosa* (positivo que não valida, negativo que o schema aceita, negativo com dois defeitos, `effect_class` incompleto). Todos partem do pressuposto de que a fixture certa existe. Nenhum eixo pergunta "existe campo da spec sem fixture nenhuma?" — e não pode perguntar, porque a spec não é entrada do executor. O probe fecha o eixo "o executor enxerga?" e deixa aberto o eixo "o autor olhou?".

**3. A coerência dos exemplos como mini-pacote é convenção do implementador, sem respaldo normativo.** `contracts/README.md:132-140` e `check_contract_examples.py:74-83` fazem `pack_facts`, `pack_objectives`, `pack_injects` e `pack_decision_options` resolverem contra os exemplos positivos dos contratos irmãos. Nenhum documento da spec pede isso. Duas consequências verificáveis: (a) o alcance da verificação passa a ser limitado ao que os exemplos por acaso contêm — daí M3, uma regra que não pode disparar porque o registro é vazio; (b) cria acoplamento entre contratos que a spec trata como independentes: editar o `fact_id` do exemplo de `ground_truth` quebra o exemplo de `evidence`. Isso produziu achado real (os defeitos 3–6 do P1-6), então não é inútil — mas é mecanismo autoconstruído medindo a si mesmo, e deve ser lido como tal.

**4. O verificador do item 1 da DoD foi escrito por quem está sendo verificado.** Isso vale para `check_contract_examples.py`, para os nove probes e para a escolha de quais fixtures existem. A mitigação disponível é a única que executei: rodar tudo e ler a saída real, e depois ler os contratos **contra os documentos da spec**, campo a campo. Foi essa segunda leitura — não a suíte — que produziu B1, B2, B3, M1 e M2. A assimetria é o achado: a suíte encontrou zero dos cinco.

**5. Nada aponta um validador para o único artefato real da fase.** Ver H1. `flags.yaml` é o arquivo que `01` §5.2 manda validar contra contrato, e é o que ninguém valida.

**Sem má-fé detectada em nenhum ponto:** os 10 `AINDA BLOQUEADO (defeito aberto, P23)` na saída do harness são falsos bloqueios de leitura legítima, afirmados em vez de escondidos, e nenhuma escrita passa. `codegen.py --check` é de fato read-only. Nenhum teste marcado `skip` ou `xfail` na árvore.

---

## O QUE EU NÃO CONSEGUI VERIFICAR

- **`docker compose up` (item 7 da DoD).** O sandbox de auditoria não executa Docker. Obtive apenas o parse do compose (`docker compose config`, que falha só na exigência deliberada de `POSTGRES_PASSWORD`), o pin por digest e o bind em loopback, por leitura. A tabela de verificação em `fase_1.md:214-228` é detalhada e plausível — Postgres 16.4, encoding UTF8, `::1` recusando conexão —, mas é auto-relato, e não a confirmo.
- **Alembic executável (P1-9).** `python -m alembic.config --help` está fora da allowlist do hook do auditor. `jsonschema` e `referencing` estão instalados neste ambiente (o executor de exemplos rodou), o que sugere que o `pip install -e .` foi feito, mas não confirmei o Alembic.
- **Estado real da branch protection no GitHub (H2).** Sem rede e sem `gh` no sandbox. O finding se apoia inteiramente no que o próprio commit candidato registra em `fase_1.md:203` e `:555-557`.
- **Fidelidade do parser YAML de subconjunto.** `tools/_common.py::parse_yaml` é escrito à mão e é o único leitor dos contratos no CI. Não comparei sua saída com a de um parser YAML conforme. Se ele misparsear um contrato, toda a validação opera sobre uma árvore diferente da que a Fase 2 lerá em produção — risco fora do escopo desta fase, mas não coberto por nada que eu tenha executado.
- **Os contratos além dos campos que a spec exibe em exemplo.** Comparei campo a campo o que `04` §2/§3/§5/§6/§7, `08` §2/§5/§7, `03` §1.1/§3 e `09` §1/§4.1 mostram explicitamente. Onde a spec descreve em prosa sem exemplo, minha verificação é mais fraca e não a apresento como conclusiva.
