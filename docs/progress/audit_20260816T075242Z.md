# Auditoria de checkpoint - 2026-08-16T07:52:42.211834+00:00

- fase: **2**
- commit auditado: `8c293e53268d321f45bfb5b37dd3b78a98932a89`
- session_id: `eec3de15-8cd6-4b1e-b1a6-f86012a9e786`
- modo: `interactive`
- veredito detectado: **PASS**

> Veredito detectado por melhor esforco a partir do relatorio abaixo, que e a fonte autoritativa.

---

# AUDITORIA — FASE 2

## VEREDITO: PASS

Auditoria executada em worktree fixado em `8c293e5` (merge-base com `main` = `f52e924`; `docs/spec/` idêntico a `main`, portanto os três `spec-change` desta linhagem já estão em `main` e não vêm no diff de código — a regra "spec e código em PRs separados" está cumprida e é verificável).

**Suíte executada de fato:** `PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests` → `Ran 155 tests ... OK (skipped=9)`. Os 9 pulos são exatamente as três classes de `tests/test_event_store_postgres.py`, com `skipIf` justificado no código e com o CI (`invariants.yml`, job `contratos`) subindo Postgres de serviço, rodando `alembic upgrade head` e definindo `AURORA_TEST_DATABASE_URL` antes da suíte.

**Verificadores executados por mim, todos rc=0:** `check_core_boundary`, `check_contract_literals`, `check_event_envelope`, `codegen.py --check`, `check_security_constraints`, `check_synthetic_data`, `check_store_read_surface` (+probes, 4/4 reprovações plantadas), `check_core_contract_imports` (+probes, 6/6 eixos), `check_contract_examples` (+probes, 13 eixos), `check_spec_examples` (+probes, 7 eixos), `check_progress_consistency`, `phase0_negative_tests`, `scripts/demo_fase2.py` (roteiro completo do DEMO da fase).

## FINDINGS

### BLOCKER

Nenhum.

### HIGH

Nenhum.

### MEDIUM

Nenhum.

### LOW

- **[L1] Comentário normativo no núcleo cita catálogo de 32 tipos; o catálogo tem 33**
  Arquivo: `range-core/state/simulation_state.py:104`
  Requisito violado: `09_EVENT_MODEL.md` §4.1 ("**Trinta e três tipos**", após os `spec-change` `separate-incident` e `exercise-resumed`)
  Evidência: a docstring de `Declarations` diz *"O catalogo de `09` §4.1 e registro FECHADO de 32 tipos"*. `contracts/generated/events.py` tem 33 constantes `Final[str]` (contadas), e `contracts/events.schema.yaml:232` diz corretamente "os outros 32 tipos" (33 menos `rollback_performed`, que ganhou schema de payload). A própria fase acrescentou `exercise_resumed` e não atualizou este ponto.
  Por que é LOW: é prosa dentro do código, sem efeito em mecanismo — nenhum teste, contrato ou gate depende do número. Mas é exatamente a classe que o registro da fase chama de §1.6 (documentação que sobrevive à mudança e a contradiz), e o argumento do módulo ("nenhum `event_type` carrega efeito de flag") se apoia na contagem.

- **[L2] A whitelist da superfície do store só enxerga subclasse direta de `EventStore`**
  Arquivo: `scripts/check_store_read_surface.py:193-195`
  Requisito violado: `01_ARCHITECTURE.md` §4.1 ("nenhum caminho de leitura **compartilhado** filtra por epoch, por abandono ou por ponto de corte")
  Evidência: `_subclasses_fora_da_linha` casa `bases` por **nome** contra `STORE_CLASS = "EventStore"`. Uma classe declarada como `class X(InMemoryEventStore)` tem `bases == {"InMemoryEventStore"}`, não casa, e pode acrescentar `read_since(cursor)` público sem reprovar. Os probes (`check_store_read_surface_probes.py`, saída lida: 4 violações plantadas) exercitam parâmetro posicional, `**kwargs`, método público não declarado e declaração órfã — nenhum deles no eixo de subclasse indireta.
  Por que é LOW: hoje só existem duas implementações (`InMemoryEventStore` e `PostgresEventStore`), ambas subclasses diretas e ambas cobertas; o buraco é de segunda ordem e a própria checagem já registra que o eixo de subclasse foi acrescentado depois por ter sido buraco uma vez.

- **[L3] O extremo `start` do intervalo congelado é derivado da âncora do rollback, não do inject falho**
  Arquivo: `range-core/engine/inject_engine.py:511-514`
  Requisito violado: `09_EVENT_MODEL.md` §3.1 ("Relógio de métricas congelado **entre o inject falho e a retomada**") e `06_ACCEPTANCE_TESTS.md` T3 ("o intervalo vai do inject falho até a retomada")
  Evidência: `_frozen_interval` devolve `start = ancora.exercise_timestamp`. A âncora é o **último evento que sobrevive** ao corte, e o inject falho é o primeiro evento **descartado** depois dela. Quando o facilitador rebobina para um ponto anterior ao inject falho — permitido e não recusado por nada —, o intervalo gravado é mais largo que o da norma, e a Fase 6 descontará tempo de exercício que correu normalmente. `test_technical_failure_registra_os_extremos_do_intervalo` fixa o comportamento derivado, não o do enunciado.
  Por que é LOW: a spec não oferece nenhum campo que identifique "o inject falho" no fluxo, então derivar da âncora é a única leitura disponível do envelope, e a decisão está declarada na docstring do método. A divergência é conservadora (desconta a mais, nunca a menos) e só se materializa na Fase 6.

## DoD — item a item

| Item | Status | Evidência |
|---|---|---|
| 1 — Todo evento carrega `exercise_time`, `exercise_timestamp`, `wall_timestamp` e `clock_multiplier` | **PASS** | Ponto único de carimbo em `range-core/events/store.py:144-159` (uma leitura de `Marks`, não quatro). `EventDraft` não tem campo para nenhuma das quatro nem para `event_id`/`simulation_epoch` — `test_event_store.Carimbo.test_o_produtor_nao_tem_onde_escrever_tempo` afirma isso por `dataclasses.fields`. `test_append_carimba_as_quatro_marcas_do_clock` compara com o relógio. `test_inject_engine.ConformeAoContrato` valida todo evento do roteiro contra `contracts/events.schema.yaml`, cujo `required` (linhas 46-55) traz as quatro. Todos executados, verdes. |
| 2 — `RANDOM_SEED` lido de `.env` por código do `range-core` | **PASS** | `range-core/determinism.py` (`random_seed`, `parse_dotenv`, `read_dotenv`, `derive_seed`, `seeded_random`); `tests/test_determinism.py` cobre as duas fontes, a precedência ambiente > arquivo e as três recusas (ausente, vazio, não-inteiro), sem valor padrão. `test_a_derivacao_e_estavel_entre_processos` ancora em valor **anotado**, não recalculado pela própria fórmula. Que a fase não *consome* o seed está declarado no módulo e no registro, com os consumidores datados (Fases 5 e 9). |
| 3 — PAUSAR congela o clock e bloqueia disparo agendado | **PASS** | Metade do clock: `test_exercise_clock.Pausar.test_durante_a_pausa_o_exercicio_congela_e_a_parede_avanca` e `…test_as_duas_marcas_de_exercicio_congelam_JUNTAS`. Metade do engine: `PausaBloqueiaAgendado` constrói o caso em que o inject **já está em atraso** no instante da pausa (`test_o_atraso_existe_antes_da_pausa`) e em que a posição não muda ao pausar (`test_a_posicao_nao_muda_ao_pausar`) — o que elimina a causa alternativa "nada venceu". A mutação `pausa deixa de bloquear` (`test_inject_engine_probes.py:73-85`) derruba exatamente 2 testes declarados. Disparo manual permitido na pausa é `01` §3 + §6, não invenção. |
| 4 — Aplicar A01 duas vezes produz projeção idêntica | **PASS** | Propriedade, não guarda: `effects` é escrita absoluta (`01` §5.4, `00` §5.2). `test_simulation_state.Propriedades.test_p3_reaplicar_o_mesmo_inject_nao_muda_o_estado` (2, 3 e 7 repetições) e `test_inject_engine.Idempotencia` pela porta do engine, com `test_mas_os_dois_disparos_ficam_no_store` fechando a leitura errada. |
| 5 — Rollback grava, incrementa epoch e reconstrói sem apagar | **PASS** | `test_inject_engine.Rollback.test_grava_incrementa_epoch_e_nao_apaga` (em memória, executado aqui) e `test_event_store.StoreMaisFold.test_rollback_gravado_reconstroi_sem_apagar`. A metade *"sobrevive ao reinício"* vem de `test_event_store_postgres.…test_rollback_persistido_reconstroi_sem_apagar`, que **pulou** localmente (ver limites). `simulation_state.py:378-454` marca posições e nunca remove. |
| 6 — `participant_action` da epoch anterior legível e marcada | **PASS** | `test_simulation_state.…test_participant_action_abandonada_permanece_no_fluxo`, `…test_rollback_atravessa_escrita_de_participant_action`, e `test_inject_engine.Rollback.test_a_decisao_da_epoch_abandonada_continua_legivel_e_marcada` (afirma `simulation_epoch == 0` na `decision_made` sobrevivente). `_verify_epochs` confere o campo com contagem **independente** de `current_epoch` (declarado em `epoch.py:14-22` para não virar tautologia). Coluna `simulation_epoch NOT NULL` em `alembic/versions/0001_event_store.py:62`. |
| 7 — `technical_failure` **registra** os extremos, em `exercise_timestamp` | **PASS** (com [L3]) | `contracts/events.schema.yaml` `$defs/rollback_payload` + `$defs/frozen_interval`, com condicional que torna `frozen_interval` **obrigatório** só para `reason: technical_failure`. `test_technical_failure_registra_os_extremos_do_intervalo`, `test_os_outros_motivos_NAO_carregam_intervalo` e `test_congelamento_contido_numa_pausa_registra_ZERO` (o caso literal de `06` T3, que fica vermelho se alguém trocar o campo por `wall_timestamp`). A mutação `o intervalo congelado comeca em agora` é detectada. Nenhum cálculo de métrica foi antecipado — o NON-GOAL é respeitado. |
| 8 — Curva volume → tempo, com ponto de quebra, máquina, data e stack | **PASS parcial — ver limites** | `scripts/bench_reconstruction.py:211-213` imprime data, `platform.platform()`, versão de Python, versão do Postgres, versão do psycopg e a revisão de migration **por código**, não por anotação. `docs/progress/fase_2.md` §3.8 traz a curva (50k–200k, formas realista e patológica), o ponto de quebra medido em ~150 mil (2,874 s) e o estouro em 200 mil (4,304 s), mais a decomposição (leitura 64%, cadeia 33%, fold 3%). **Não reproduzi os números** (exige Postgres e escrita de centenas de milhares de linhas; o script está deliberadamente fora da allowlist do auditor). A forma do item é a do `spec-change` `item-8-volume-de-4h`, já em `main`, e o critério de 4 h está cobrado em `07` Fase 7 e Fase 9 / `06` T12 e T13 — conferi os quatro sítios. |
| 9 — Flag não declarada impede boot com mensagem clara | **PASS** | `range-core/engine/loader/pack_loader.py:436-446`: sítio próprio `UNDECLARED_FLAG`, mensagem nomeando a flag (via violação de `x-aurora-ref:adapter_flags`) e o arquivo esperado (via `AdapterFlags.source`, entregue como dado — o núcleo não conhece `domains/`). `test_pack_loader.FlagNaoDeclarada` em cinco casos, incluindo `required_flags` do manifesto e `effects` de opção de decisão, mais `test_objetivo_inexistente_e_violacao_de_regra_e_nao_de_flag` como discriminante de sítio. Uma única implementação da regra, compartilhada com o gate de CI (`contract_rules.py`) — conferi que as três regras `x-aurora-*` da versão anterior sobreviveram à extração, sem perda. |

**Invariantes arquiteturais (T1/T2):** `check_core_boundary` rc=0; nenhum import de `domains/` em `range-core/`, direto ou dinâmico. `check_event_envelope` rc=0 e o contrato nega `objective_ids` no envelope **e** no payload (`events.schema.yaml:129,134`); `Event` não tem campo onde escrevê-lo. `check_contract_literals` rc=0. `codegen.py --check` rc=0. Os imports do core para `contracts/` são whitelist verificada por AST (`check_core_contract_imports`, 4 arquivos, prova negativa em 6 eixos).

**Segurança (T15 / `05`):** nenhuma dependência não pinada — `psycopg[binary]==3.2.12` entrou no `pyproject.toml` com o fecho transitivo (`psycopg-binary`, `tzdata`) em `constraints.txt`. `check_security_constraints` e `check_synthetic_data` rc=0. SHA-256 aparece em `determinism.py` e `integrity.py` como derivação determinista e encadeamento de integridade — uso legítimo, explicitamente permitido por `CLAUDE.md` e não confundido pelo verificador. `tests/mutation_harness.py` carrega fonte mutada por `importlib.util.spec_from_file_location`, e não por `exec`/`eval`/`compile` — o registro diz que a primeira versão usava os builtins proibidos e foi barrada pelo hook. Nenhum exploit, payload, criptografia de arquivo ou execução de shell a partir de entrada.

## TESTES QUE NÃO PROVAM O REQUISITO

Nenhum encontrado — e este é o achado mais forte desta auditoria, porque a fase construiu o mecanismo que o impede.

O que procurei e não encontrei:

- **Asserção sobre mock:** zero `Mock`, `patch` ou duplo de comportamento na árvore de testes (varredura completa em `tests/`). O clock recebe a fonte de tempo de parede por injeção, então o engine é exercitado contra o **clock real** com o tempo na mão; nenhum teste dorme.
- **`skip`/`xfail` sem justificativa:** os únicos `skipIf` são os quatro de `test_event_store_postgres.py`, com razão que **imprime o comando exato** para rodar, e com o CI garantindo a execução. A escolha de `AURORA_TEST_DATABASE_URL` em vez de `DATABASE_URL` é deliberada e documentada (os testes fazem `TRUNCATE`).
- **Teste que passa pelo motivo errado:** a fase encontrou dois destes por conta própria e os transformou em mutação permanente (`test_inject_engine_probes.py:16-26`) — `test_o_bloqueio_…_sobrevive_ao_reinicio`, que passava porque o clock reiniciado nasce em `T+00:00:00`, e `test_a_opcao_escolhida_move_a_flag`, que afirmava só que o estado mudou. O harness exige o **conjunto exato** de testes vermelhos por mutação, com âncora verde sem mutação — mutação grossa e teste frouxo reprovam do mesmo jeito.
- **Limite herdado como crença:** três limites estão exercidos como teste em vez de afirmados em prosa — `test_truncar_a_cauda_NAO_e_detectado`, `test_o_store_em_memoria_NAO_sobrevive_ao_reinicio` e `test_a_posicao_do_exercicio_NAO_e_restaurada`. Ficam vermelhos no dia em que deixarem de valer.
- **Recusa sem discriminante:** `MalformedStream`, `PackError` e `EngineError` carregam `site`, e os testes afirmam **qual** recusa ocorreu, não que houve exceção. `test_todo_sitio_declarado_tem_caso` fecha o conjunto.

## O QUE EU NÃO CONSEGUI VERIFICAR

1. **Os 9 testes de Postgres.** `AURORA_TEST_DATABASE_URL` não está definida neste worktree e não há banco disponível. Com eles pulam: persistência, o critério de **reinício** de `06` T3, a detecção de reescrita por cadeia de hash (3 casos), a contiguidade de `sequence`, e o caso de dois escritores concorrentes. Li o código e o CI (`invariants.yml:208-301`) — o job `contratos` sobe Postgres 16.4 pinado por digest, aplica `alembic upgrade head` e roda a suíte com a variável definida —, mas **não executei o CI e não pude confirmar que ele está verde neste commit**. Consequência: a metade *"sobrevive ao reinício"* dos itens 5 e 6 da DoD, e o critério de reinício de T3, estão verificados por leitura e por configuração, não por execução minha. O repositório declara essa dependência explicitamente em `test_event_store.LimiteDoStoreEmMemoria`.
2. **Os números do item 8.** `scripts/bench_reconstruction.py` exige Postgres, escreve centenas de milhares de linhas e está fora da allowlist do auditor por decisão registrada. Verifiquei que data, máquina e stack são **gerados por código** e que a tabela do §3.8 tem a forma que o item exige; não reproduzi 2,874 s em 150 mil eventos nem o ponto de quebra.
3. **Branch protection e required status checks.** `docs/process/WORKFLOW.md` exige quatro contexts obrigatórios (`arquitetura`, `spec_freeze`, `seguranca`, `contratos`). Não tenho acesso de rede para conferir a configuração real do repositório remoto.
4. **A auditoria rodou sob regras que esta própria fase escreveu.** O commit sob auditoria (`8c293e5`) altera `user-scope/hooks/readonly_bash.py`, que é a fonte versionada do hook que restringe o meu Bash, e `phase0_negative_tests.py` confirma que a cópia instalada em `~/.claude/hooks/` é **idêntica** à versionada — ou seja, as regras sob as quais eu operei são as deste commit. Examinei a mudança linha a linha: ela é estritamente aditiva, admite `python -m unittest discover -s tests` em **forma exata ancorada** (mais estreita que a entrada `pytest\b` que já existia desde a Fase 0) e cinco scripts **por nome explícito**, sem curinga; nenhuma negação de `DENIED_ANYWHERE` foi afrouxada. `docs/process/WORKFLOW.md` §"Por que o auditor não mora no repositório" protege a *definição* do agente, não o hook, e `invariants.yml:128-133` reconhece essa fronteira em vez de escondê-la. Registro o fato porque ele é estrutural, não porque encontrei abuso.
5. **Efeito colateral de rodar a suíte.** `tests/mutation_harness.py:141` usa `tempfile.TemporaryDirectory()`, que escreve no diretório temporário do sistema — fora do worktree de auditoria, ainda que autolimpante e sem alvo controlável por mim. Não é escrita deliberada minha, mas também não fica contida no worktree, ao contrário do que o desenho do hook assume para o resto da superfície.
6. **Comportamento sob reinício sem `restore_pause_state`.** `due_injects` consulta `self._clock.is_paused`, não o fluxo; um processo que reinicie e **não** chame `restore_pause_state()` dispararia agendado numa sala parada. O método existe, é testado, e o item de DoD que cobra o reinício completo é da Fase 4 — não julguei isto como desvio desta fase, mas fica registrado para o checkpoint seguinte.
